# Issue #11132: MultiKueue metrics needed

**Summary**: MultiKueue metrics needed

**Sources**: https://github.com/kubernetes-sigs/kueue/issues/11132

**Last updated**: 2026-06-26T15:26:38Z

---

## Metadata

- **State**: open
- **Author**: [@ns-sundar](https://github.com/ns-sundar)
- **Created**: 2026-05-11T20:34:28Z
- **Updated**: 2026-06-26T15:26:38Z
- **Closed**: —
- **Labels**: `kind/feature`, `area/multikueue`
- **Assignees**: [@Mostafahassen1](https://github.com/Mostafahassen1)
- **Comments**: 13

## Description

<!-- Please only use this template for submitting enhancement requests -->

**What would you like to be added**:
Multikueue would need a set of new metrics at various levels: cluster connectivity and status, dispatch performance and errors, evictions/preemptions across clusters, etc. (There are probably more, but these seem to be the most obvious ones.)

  * Cluster connectivity and status:
       * Cluster status: Reachable/active, disconnected, retrying. 
       * Last Reachable time by cluster: when was the cluster last reachable?
       * Sync_errors by cluster by reason: Kube API server returned 4xx/5xx, API server not reachable,  etc.
  * Dispatch performance:
       * Number of workloads dispatched to a cluster as candidate
       * Number of workloads admitted by the cluster? (This may be available in the worker cluster, but not sure if the mgmt cluster can provide more info.)
       * Admission latency by cluster (worker clusters' admission latency metric is not the same)
       * Number of dispatch rounds before admission
       * Number of clusters tried per admission 
       * Number of workloads pending that are eligible for a given cluster
  * Dispatch errors:
       * Number of dispatch rejections, faiilure to submit, etc. by cluster and by reason
  * Cross-cluster evictions:
       * Number of evictions for the same workload across clusters 

**Why is this needed**:
For MultiKueue to move from beta to production readiness, observability is important. Cross-cluster metrics will provide a global visibility. 

**Completion requirements**:

This enhancement requires the following artifacts:

- [ ] Design doc
- [ ] API change
- [ x] Docs update

The artifacts should be linked in subsequent comments.

## Discussion

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-05-12T08:13:33Z

/area multikueue

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-19T14:24:40Z

Hi @mimowo, is it okay to start working on this feature? I finished the last feature, and it’s now open for review.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-19T14:27:15Z

Hi @Mostafahassen1 , sure. We will need some   more detailed proposal for the metrics. 

I think it is a good idea to also give the proposal for discussion during wg-batch meeting

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-19T14:40:38Z

> We will need some more detailed proposal for the metrics.
> I think it is a good idea to also give the proposal for discussion during wg-batch meeting

Hi @mimowo  ,Sorry for that, this is a new step for me. Do you want me to prepare something like documentation that explains the metric names and why we need these metrics?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-19T14:54:05Z

Yes , a short doc of post which specifies the type of the metric, the labels and how they will be collected

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-21T15:13:09Z

Hi @mimowo @olekzabl @kannon92 @amy   If we can identify the high-priority metrics that we will build, that would be great. It doesn’t mean we need to cover all priorities now, but it would be helpful if we could define the most important ones first.

## Update

Based on the discussion and feedback from the meeting, I reviewed the metrics again and removed some of them .

I think this is the list that we currently need (it is also open for discussion), and now I am struggling to determine the next step.


**1. Name:** `multikueue_cluster_last_reachable_time_seconds`

* **What it measures (Type):** A *Gauge* storing the Unix timestamp of the last successful ping/connection to a worker API.
* **Why implement it:** It instantly answers the most critical operational question: *"Is this specific worker cluster down?"* By using this metric, operators can write a simple alert that triggers if the timestamp gets too old.

**2. Name:** `multikueue_workloads_dispatched_total`

* **What it measures (Type):** A *Counter* tracking the total number of workloads the manager has attempted to send to a worker.
* **Why implement it:** It provides visibility into load balancing. It shows the operator if the manager is utilizing all worker clusters evenly, or if it is unfairly flooding one specific cluster with requests.

**3. Name:** `multikueue_workloads_admitted_total`

* **What it measures (Type):** A *Counter* tracking how many workloads were actually accepted and started by the worker.
* **Why implement it:** When you divide this metric by the "dispatched" metric, you get the **Admission Success Rate**. It tells you if a specific worker is constantly rejecting the jobs sent to it.

**4. Name:** `multikueue_pending_workloads`

* **What it measures (Type):** A live *Gauge* showing how many workloads have been sent to a worker but are currently stuck waiting in its queue.
* **Why implement it:** MultiKueue has an invisible "middle state" where a job is approved by the manager but still pending on the worker. This metric exposes that blind spot and tells the operator exactly which worker cluster is currently acting as a bottleneck.

**5. Name:** `multikueue_workload_admission_latency_seconds`

* **What it measures (Type):** A *Histogram* measuring the end-to-end time it takes from the manager dispatching the workload to the worker returning a "Ready/Admitted" status.
* **Why implement it:** It measures the actual speed of the system. It proves to operators which worker clusters are responding fast, and which ones are suffering from performance degradation.


**6. Name:** `multikueue_evictions_total`

* **What it measures (Type):** A *Counter* tracking how many times a running workload was preempted (kicked out) by a worker cluster.
* **Why implement it:** While the standard Kueue manager tracks evictions, it does *not* know which remote cluster did the evicting.  telling the manager exactly which worker is aggressively kicking out jobs due to quota or resource issues.

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-22T19:11:13Z

/assign

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-25T12:09:59Z

@Mostafahassen1 Thank you for your detailed plan!

We just discussed this with @mimowo and we think that:

* Items 2-6 look solid, and worth doing.

* For item 1 ("last reachable time"), we're not sure why picking this one and not the general "worker status" metric?
  Our thinking is that "worker status" (emitted per worker) is _more general_, in that "last reachable time" can be derived from it by a relatively simple graph or query. In particular, the gain you mentioned

  > By using this metric, operators can write a simple alert that triggers if the timestamp gets too old.

  seems to still hold. (The alert would look like "status != OK over the last X minutes").

  While the other direction is not feasible: knowing just "last reachable time", you can't get a full historical picture of "worker status" over the last X hours.
  
* For the start, it seems reasonable to pick some small set of metrics to work on.
  Our favorites are: "worker status" (replacing your item 1) and "workloads dispatched total" (your item 2).

WDYT?

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-06-25T13:48:18Z

Hi @olekzabl,

Okay, can you tell me what the plan is? We now have multiple matrices that need to be implemented.

What do you prefer:

* One KEP for each matrix?
* A single KEP covering all matrices?
* Or do we not need a KEP at all?

Also, regarding the implementation, do you prefer each matrix to be implemented in a separate PR, or would you like a different approach?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-06-25T14:39:48Z

I think we don't need a new KEP for the metrics necessarily as we didn't require KEP for Cohort and other metrics historically. Certainly not a KEP per metric please :). If some of the metrics are well defined and easy to prototype then just go ahead and implement - then we may ask for KEP alignment on the design first if there are some non-trivial questions.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-25T14:49:04Z

> Also, regarding the implementation, do you prefer each matrix to be implemented in a separate PR, or would you like a different approach?

Yes, I think it's mostly reasonable, as these metrics feel different enough to have mostly independent implementations.

I can imagine some overlap between items 3 & 4 (the moment a worker admits a workload, both metrics would need an update). So, if you wish, maybe these 2 can go together. But probably they can be split as well.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-25T17:06:07Z

@Mostafahassen1 FYI we just talked with @ns-sundar , leading to some re-prioritization.

* IIUC our first 2 choices can stay.

* However, following them, as no. 3, we could do this one:

  > * Number of dispatch rejections, faiilure to submit, etc. by cluster and by reason

  Where, if incremental dispatcher failed to fit a workload on all cluster in a batch, and needs to proceed to another batch, this also should count as "rejection" (with some well-distinguishing reason).

* Then, another interesting set of info are these 2:

  > * Admission latency by cluster (worker clusters' admission latency metric is not the same)
  > * Number of dispatch rounds before admission

  where the former is the key signal for setting an alert on - but the latter also brings value for debugging such an alert.

  Here, the former may be covered by your item 5 (`multikueue_workload_admission_latency_seconds`) - but let me double-check one detail.
  Consider incremental dispatcher, step = 3, and the following timeline:

  * 12:00: Dispatching to clusters A, B, C.
  * 12:05: 1st batch failed. Dispatching to D, E, F.
  * 12:08: Cluster E admitted the workload.

  Would you emit 3 minutes or 8 minutes?
  AFAIU from your story, you'd emit 3 minutes, to inform that "Cluster E took 3 minutes to admit".
  However, IIUC @ns-sundar would prefer to see 8 minutes, to capture the "total waiting time for admission in my MK setup".

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-06-26T15:26:38Z

@mimowo @olekzabl @ns-sundar 
## This is a list of metrics after update  




**1. Name: `multikueue_cluster_worker_status`**
What it measures (Type): A Gauge emitting the current status of each worker cluster (e.g., OK, Unreachable, Unknown).
Why implement it: It is more general than "last reachable time." Operators can still alert with "status != OK over the last X minutes", and it also provides a full historical picture of worker health — something a timestamp alone cannot offer.

**2. Name: `multikueue_workloads_dispatched_total`**
What it measures (Type): A Counter tracking the total number of workloads the manager has attempted to send to a worker.
Why implement it: It provides visibility into load balancing. It shows the operator if the manager is utilizing all worker clusters evenly, or if it is unfairly flooding one specific cluster with requests.

**3. Name: `multikueue_workload_dispatch_rejections_total` 
What it measures (Type): A Counter tracking dispatch rejections and submission failures, labeled by cluster and by reason. Includes cases where the incremental dispatcher failed to fit a workload across an entire batch and moved to the next one.
Why implement it: It exposes which clusters are consistently rejecting workloads and for what reason, giving operators clear visibility into dispatch failures.

**4. Name: `multikueue_workloads_admitted_total`**
What it measures (Type): A Counter tracking how many workloads were actually accepted and started by the worker.
Why implement it: When you divide this metric by the "dispatched" metric, you get the Admission Success Rate. It tells you if a specific worker is constantly rejecting the jobs sent to it.

**5. Name: `multikueue_pending_workloads`**
What it measures (Type): A live Gauge showing how many workloads have been sent to a worker but are currently stuck waiting in its queue.
Why implement it: MultiKueue has an invisible "middle state" where a job is approved by the manager but still pending on the worker. This metric exposes that blind spot and tells the operator exactly which worker cluster is currently acting as a bottleneck.

**6. Name: `multikueue_workload_admission_latency_seconds`**
What it measures (Type): A Histogram measuring the total end-to-end time from the manager first dispatching the workload to any worker returning "Admitted" — across all batches, not just the winning cluster's batch.
Why implement it: It is the key signal for alerting on slow admission. Measures the full waiting experience in the MultiKueue setup (e.g., 8 min across all batches, not just 3 min from the final cluster).

**7. Name: `multikueue_workload_dispatch_rounds_total`
What it measures (Type): A Counter tracking how many dispatch rounds the incremental dispatcher went through before a workload was admitted.
Why implement it: It provides the debugging context behind a latency alert — telling the operator how many batches failed before admission was finally successful.

**8. Name: `multikueue_evictions_total`**
What it measures (Type): A Counter tracking how many times a running workload was preempted (kicked out) by a worker cluster.
Why implement it: While the standard Kueue manager tracks evictions, it does not know which remote cluster did the evicting — this metric tells the manager exactly which worker is aggressively kicking out jobs due to quota or resource issues.
