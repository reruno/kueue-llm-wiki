# Issue #11375: [MultiKueue] Cross-cluster preemption — reclaim cohort quota from sibling-cluster borrowers

**Summary**: [MultiKueue] Cross-cluster preemption — reclaim cohort quota from sibling-cluster borrowers

**Sources**: https://github.com/kubernetes-sigs/kueue/issues/11375

**Last updated**: 2026-06-01T10:21:52Z

---

## Metadata

- **State**: open
- **Author**: [@ningziwen](https://github.com/ningziwen)
- **Created**: 2026-05-21T06:46:19Z
- **Updated**: 2026-06-01T10:21:52Z
- **Closed**: —
- **Labels**: `kind/feature`, `area/multikueue`, `area/kep`
- **Assignees**: _none_
- **Comments**: 1

## Description

<!-- Please only use this template for submitting enhancement requests -->

**What would you like to be added**:

A new MultiKueue dispatcher mode (alpha, default-off feature gate
`MultiKueueCrossClusterPreemption`) that, when an incoming workload at
the manager finds sibling clusters full of borrowed work, evicts a
borrower on a sibling worker so the incoming workload can be admitted
there. Mirrors single-cluster Kueue's `ReclaimWithinCohort` cross-cluster
via the External-dispatcher extension point.

The load-bearing semantic is **quota-based reclaim**: a sibling
ClusterQueue is only an eligible victim when it is currently borrowing
cohort capacity (`Status.FlavorsUsage > NominalQuota`). Reclaim is
gated by the victim's `Spec.Preemption.ReclaimWithinCohort`
(Never / LowerPriority / Any). Borrow-while-preempting is gated by the
preemptor's `Spec.Preemption.BorrowWithinCohort` plus
`MaxPriorityThreshold` (victim-side cap, matching the upstream API
godoc and single-cluster `isAboveBorrowingThreshold`). Cohort
membership is the shared `ClusterQueue.spec.cohortName` string;
no new CRDs.

Out of scope (potential follow-ons): cross-cluster FairSharing weights /
DRS, hierarchical cohorts via `Cohort.spec.parentName`, cross-cluster
aggregation of `LendingLimit` / `BorrowingLimit`. Worker-local
schedulers continue to honor each of these at admission time as today.

**Why is this needed**:

Operators that split workloads across per-tenant or per-environment
worker clusters need per-tenant quota guarantees that hold across
cluster boundaries. Today MultiKueue treats each worker as a black
box: a workload admits at the manager only if some worker has free
quota, and there is no mechanism to evict a sibling cluster's borrower
— even when single-cluster Kueue would do it locally via
`ReclaimWithinCohort`. A guaranteed-tier customer's workload can be
blocked indefinitely while a borrower on a sibling cluster finishes
naturally.

The closest related work, after auditing existing KEPs and issues:

- **KEP-8692 (#10766 / #10784)** — composable MultiKueue dispatcher.
  Explicitly lists *Cross-cluster preemption scheduling* under
  Non-Goals; this is the gap that work leaves open.
- **#6790** — addresses the worker → manager eviction propagation
  half. This proposal is the manager → worker initiation half. They
  coexist; neither subsumes the other.
- **KEP-8303** — independent feature gate; coordinates concurrent
  worker-side preemptions. Joint mode is supported (validated by
  integration test).
- **KEP-1337** — single-cluster `BorrowWithinCohort` +
  `MaxPriorityThreshold`. Reused as-is; same field, same semantics,
  applied across worker clusters.

**Completion requirements**:

This enhancement requires the following artifacts:

- [ ] Design doc (KEP)
- [ ] API change: new dispatcher mode constant in
      `Configuration.MultiKueue.DispatcherName`; new feature gate
      `MultiKueueCrossClusterPreemption`.
- [ ] Docs update.

Working alpha prototype with 25 unit tests + 5 integration scenarios:
[`up/multikueue-cross-cluster-preemption`](https://github.com/ningziwen/kueue/tree/up/multikueue-cross-cluster-preemption).

/kind feature
/area multikueue
/area kep

## Discussion

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-01T10:20:47Z

> https://github.com/kubernetes-sigs/kueue/pull/6790 — addresses the worker → manager eviction propagation half. This proposal is the manager → worker initiation half.
> They
coexist; neither subsumes the other.

Please note that the manager -> worker initiation has been handled in #7350.
By this, I'm **not** saying that #7350 "subsumes" your current work (I'm reviewing the KEP currently and I still need to digest it).
For now, just leaving a quick heads up that the "closest related work" list is noticeably incomplete :)
