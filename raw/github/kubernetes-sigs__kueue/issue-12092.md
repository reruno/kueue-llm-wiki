# Issue #12092: Move core Job tests from managejobswithoutqueuename_test.go to the sequential baseline suite

**Summary**: Move core Job tests from managejobswithoutqueuename_test.go to the sequential baseline suite

**Sources**: https://github.com/kubernetes-sigs/kueue/issues/12092

**Last updated**: 2026-06-15T06:28:36Z

---

## Metadata

- **State**: closed (completed)
- **Author**: [@apullo777](https://github.com/apullo777)
- **Created**: 2026-06-10T20:03:07Z
- **Updated**: 2026-06-15T06:28:36Z
- **Closed**: 2026-06-15T06:28:36Z
- **Labels**: `kind/cleanup`
- **Assignees**: [@apullo777](https://github.com/apullo777)
- **Comments**: 2

## Description

<!-- Please only use this template for submitting clean up requests -->

**What would you like to be cleaned**:

Move the core Job tests from `managejobswithoutqueuename_test.go` (sequential/extended suite) into the sequential baseline suite.


**Why is this needed**:

> Also, I think we could move some tests for the core Job from `managejobswithoutqueuename_test.go` to the sequential baseline suite. 
 _Originally posted by @mimowo in [#11990](https://github.com/kubernetes-sigs/kueue/issues/11990#issuecomment-4649509896).

Keeps core Job coverage in the baseline suite (run on every PR) rather than the heavier extended suite, and lightens the extended shard. 

Part of #11606.

## Discussion

### Comment by [@apullo777](https://github.com/apullo777) — 2026-06-10T22:32:53Z

/assign

Hi @mimowo, I wanted to clarify the scope before opening the PR.

From `managejobswithoutqueuename_test.go`, it looks like the tests can be split by whether they depend on external operators:

- The pure core batchv1.Job tests: “should not suspend a job” and “should suspend a job”: should move cleanly to the baseline suite, since the baseline environment does not install external operators.
- The Pod / Deployment / StatefulSet tests also appear to use only built-in Kubernetes resources, so they could potentially move as well. However, they are not strictly “core Job” tests.
- The AppWrapper / JobSet / LeaderWorkerSet tests should remain in the extended suite, since they depend on those operators.

Would you prefer that I move only the two core Job tests, or should I also include the Pod / Deployment / StatefulSet cases?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-06-12T13:50:00Z

> Hi @mimowo, I wanted to clarify the scope before opening the PR.

ack

> The pure core batchv1.Job tests: “should not suspend a job” and “should suspend a job”: should move cleanly to the baseline suite, since the baseline environment does not install external operators.

yes

> The Pod / Deployment / StatefulSet tests also appear to use only built-in Kubernetes resources, so they could potentially move as well. However, they are not strictly “core Job” tests.

They should go to "baseline" because all is needed to run them is already part of k8s - no third part CRDs need installing

> The AppWrapper / JobSet / LeaderWorkerSet tests should remain in the extended suite, since they depend on those operators.

yes

> Would you prefer that I move only the two core Job tests, or should I also include the Pod / Deployment / StatefulSet cases?

Both Jobs, Pod, Deployment and STS

Actually, we also call often "Pod" Deployment and StatefulSets as Jobs in Kueue, LoL
