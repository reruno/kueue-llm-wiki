# PR #10001: Fix scheduling hashes and disable temporarily

**Summary**: Fix scheduling hashes and disable temporarily

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10001

**Last updated**: 2026-03-19T11:27:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-and-disable-hash` → `main`
- **Created**: 2026-03-19T09:22:16Z
- **Updated**: 2026-03-19T11:27:43Z
- **Merged**: 2026-03-19T11:26:31Z
- **Closed**: 2026-03-19T11:26:31Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd)
- **Changed files**: 6   **+9 / -28**

## Description


#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
To fix the problem observed on large deployments with FairSharing enabled that the hash-based bulk scheduling was no-op, because in that case allmost all inadmissible workloads are categorized as "Preempt", but with noPreemptionCandidates.

This change uses the bulk-scheduling if and only if a pair of workloads have identical scheduling hash, and the "Popped" workload is considered "inadmissible", regardless of the categorization.

#### Special notes for your reviewer:

At the same time, since this change has a bigger impact, we need more time to:
1. properly investigate the performance regression reported by our testing framework, this is probably just an artifact of the testing approach, but we should be careful
2. Fix the compatibility with MultiKueue preemption gates, cc @kshalot 
3. Adjust many unit and integration tests carefully

So, I propose to disable the feature gate by default.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scheduling: fixed SchedulingEquivalenceHashing so equivalent workloads that become inadmissible through
the preemption path with no candidates are also covered by the mechanism. 

As a safety measure while the broader fix is validated, the beta SchedulingEquivalenceHashing feature gate
is temporarily disabled by default. 
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-19T09:22:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5c04872843df911a454de4e0ba05abae402d087e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69bbca720f67ea00081ae968 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10001--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10001--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMDAxLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.OU5wZQ54YSq-cYUh8fGaJx0W6Zkx7fIMjJ4UvRib44g)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10001--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T09:22:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10001#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:18:58Z

cc @mukund-wayve @sohankunkerkar @tenzen-y @gabesaba

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-03-19T10:55:57Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-19T10:56:05Z

LGTM label has been added.  <details>Git tree hash: b7945510a8a81f04ab34170ddad512eed9a7876e</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T11:02:03Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-19T11:02:07Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10001#issuecomment-4089337978):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-19T11:10:41Z

I opened the issue to follow up with graduation: https://github.com/kubernetes-sigs/kueue/issues/10005

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-19T11:27:08Z

@mimowo: #10001 failed to apply on top of branch "release-0.16":
```
Applying: Scheduling: fix the bulk-scheduling by hash and disable temporarily
Using index info to reconstruct a base tree...
M	pkg/cache/queue/cluster_queue.go
M	pkg/cache/queue/manager.go
M	pkg/features/kube_features.go
M	pkg/scheduler/scheduler.go
M	site/data/featuregates/versioned_feature_list.yaml
M	test/compatibility_lifecycle/reference/versioned_feature_list.yaml
Falling back to patching base and 3-way merge...
Auto-merging test/compatibility_lifecycle/reference/versioned_feature_list.yaml
Auto-merging site/data/featuregates/versioned_feature_list.yaml
Auto-merging pkg/scheduler/scheduler.go
Auto-merging pkg/features/kube_features.go
CONFLICT (content): Merge conflict in pkg/features/kube_features.go
Auto-merging pkg/cache/queue/manager.go
Auto-merging pkg/cache/queue/cluster_queue.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Scheduling: fix the bulk-scheduling by hash and disable temporarily

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10001#issuecomment-4089337978):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-19T11:27:43Z

@mimowo: #10001 failed to apply on top of branch "release-0.15":
```
Applying: Scheduling: fix the bulk-scheduling by hash and disable temporarily
Using index info to reconstruct a base tree...
M	pkg/cache/queue/cluster_queue.go
M	pkg/cache/queue/manager.go
M	pkg/features/kube_features.go
M	pkg/scheduler/scheduler.go
M	site/data/featuregates/versioned_feature_list.yaml
M	test/compatibility_lifecycle/reference/versioned_feature_list.yaml
Falling back to patching base and 3-way merge...
Auto-merging test/compatibility_lifecycle/reference/versioned_feature_list.yaml
Auto-merging site/data/featuregates/versioned_feature_list.yaml
Auto-merging pkg/scheduler/scheduler.go
Auto-merging pkg/features/kube_features.go
CONFLICT (content): Merge conflict in pkg/features/kube_features.go
Auto-merging pkg/cache/queue/manager.go
CONFLICT (content): Merge conflict in pkg/cache/queue/manager.go
Auto-merging pkg/cache/queue/cluster_queue.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Scheduling: fix the bulk-scheduling by hash and disable temporarily

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10001#issuecomment-4089337978):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-03-19T10:30:45Z

- **pkg/cache/queue/manager.go:616** by [@PBundyra](https://github.com/PBundyra) — 2026-03-19T10:30:45Z
```diff
@@ -601,23 +601,6 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-// HandleInadmissibleHash bulk-moves all workloads in the ClusterQueue's heap
-// that share the given scheduling hash to inadmissibleWorkloads.
-func (m *Manager) HandleInadmissibleHash(cqName kueue.ClusterQueueReference, hash string) int {
-	m.RLock()
-	defer m.RUnlock()
-	cq := m.hm.ClusterQueue(cqName)
-	if cq == nil {
-		return 0
-	}
-	moved := cq.handleInadmissibleHash(hash)
-	if moved > 0 {
-		// Update pending metrics for the CQ and all its LocalQueues.
-		reportPendingWorkloads(m, cqName)
```

  Do we intentionally want remove reporting?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-19T10:35:12Z

- **pkg/cache/queue/cluster_queue.go:416** by [@kshalot](https://github.com/kshalot) — 2026-03-19T10:35:12Z
```diff
@@ -413,6 +413,12 @@ func (c *ClusterQueue) requeueIfNotPresent(log logr.Logger, wInfo *workload.Info
 	}
 	log.V(2).Info(logMsg, "clusterQueue", c.name, "workload", key)
 
+	if features.Enabled(features.SchedulingEquivalenceHashing) && wInfo.SchedulingHash != workload.SchedulingHashUnknown && !immediate {
```

  The reason why this is not working with preemption gates is because when a preemption gate is hit, the requeue reason is set to `RequeueReasonPreemptionGated`:
  https://github.com/kubernetes-sigs/kueue/blob/5c3974bcdbcc5b1f476ce2dfe4dea6860f0adcc5/pkg/scheduler/scheduler.go#L215
  which uses `immediate=False` when requeueing because we don't want the gated workloads to block others while waiting for the gate to be released. But now this makes the hash end up in `noFitSchedulingHashes`:
  https://github.com/kubernetes-sigs/kueue/blob/5c3974bcdbcc5b1f476ce2dfe4dea6860f0adcc5/pkg/cache/queue/cluster_queue.go#L435
  
  Before this PR we only executed `handleInadmissibleHash` when the mode was `NoFit`, so this made sense. But now we schedule it when the requeueing mode  is `immediate=False`. So the `NoFit` and `immediate` got coupled.
  
  I think if we want to hack it for the release, we could just pass some extra boolean instead of re-using `immediate`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T10:35:39Z

- **pkg/cache/queue/manager.go:616** by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:35:39Z
```diff
@@ -601,23 +601,6 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-// HandleInadmissibleHash bulk-moves all workloads in the ClusterQueue's heap
-// that share the given scheduling hash to inadmissibleWorkloads.
-func (m *Manager) HandleInadmissibleHash(cqName kueue.ClusterQueueReference, hash string) int {
-	m.RLock()
-	defer m.RUnlock()
-	cq := m.hm.ClusterQueue(cqName)
-	if cq == nil {
-		return 0
-	}
-	moved := cq.handleInadmissibleHash(hash)
-	if moved > 0 {
-		// Update pending metrics for the CQ and all its LocalQueues.
-		reportPendingWorkloads(m, cqName)
```

  I think this reporting here is not needed, because we already call reportCQPendingWorkloads from within `RequeueWorkload`, just after calling `RequeueIfNotPresent`. I think it might have been needed before when the change was in a different place. I would revisit the merics carefully as part of the enablement plan to Beta.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T10:37:23Z

- **pkg/cache/queue/cluster_queue.go:416** by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:37:23Z
```diff
@@ -413,6 +413,12 @@ func (c *ClusterQueue) requeueIfNotPresent(log logr.Logger, wInfo *workload.Info
 	}
 	log.V(2).Info(logMsg, "clusterQueue", c.name, "workload", key)
 
+	if features.Enabled(features.SchedulingEquivalenceHashing) && wInfo.SchedulingHash != workload.SchedulingHashUnknown && !immediate {
```

  Is an alernative fix to simply add the `preemptionGates` to the scheduling hash? Then, the next workload will be evaluated still, if the state of the gates is different.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-19T10:45:06Z

- **pkg/cache/queue/cluster_queue.go:416** by [@mimowo](https://github.com/mimowo) — 2026-03-19T10:45:06Z
```diff
@@ -413,6 +413,12 @@ func (c *ClusterQueue) requeueIfNotPresent(log logr.Logger, wInfo *workload.Info
 	}
 	log.V(2).Info(logMsg, "clusterQueue", c.name, "workload", key)
 
+	if features.Enabled(features.SchedulingEquivalenceHashing) && wInfo.SchedulingHash != workload.SchedulingHashUnknown && !immediate {
```

  Still, the MultiKueue preemption gates are still Alpha and 0.17+, so I think we have time to fix it as a follow up

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-19T10:47:00Z

- **pkg/cache/queue/cluster_queue.go:416** by [@kshalot](https://github.com/kshalot) — 2026-03-19T10:47:00Z
```diff
@@ -413,6 +413,12 @@ func (c *ClusterQueue) requeueIfNotPresent(log logr.Logger, wInfo *workload.Info
 	}
 	log.V(2).Info(logMsg, "clusterQueue", c.name, "workload", key)
 
+	if features.Enabled(features.SchedulingEquivalenceHashing) && wInfo.SchedulingHash != workload.SchedulingHashUnknown && !immediate {
```

  Now that I think of it, it's going to be more subtle then that. Adding the preemption gate to the hash would cause problems with signaling, because we will bulk move gated workloads which need the scheduler to pick them up and add the gated condition. In theory if we use the states of the gates that include the timestamps then it would "work", but it's hacky.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-19T10:47:31Z

- **pkg/cache/queue/cluster_queue.go:416** by [@kshalot](https://github.com/kshalot) — 2026-03-19T10:47:30Z
```diff
@@ -413,6 +413,12 @@ func (c *ClusterQueue) requeueIfNotPresent(log logr.Logger, wInfo *workload.Info
 	}
 	log.V(2).Info(logMsg, "clusterQueue", c.name, "workload", key)
 
+	if features.Enabled(features.SchedulingEquivalenceHashing) && wInfo.SchedulingHash != workload.SchedulingHashUnknown && !immediate {
```

  > Still, the MultiKueue preemption gates are still Alpha and 0.17+, so I think we have time to fix it as a follow up
  
  Yes, I think we should handle it as a follow-up, because it's delicate.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-03-19T10:56:03Z

- **pkg/cache/queue/manager.go:616** by [@PBundyra](https://github.com/PBundyra) — 2026-03-19T10:56:03Z
```diff
@@ -601,23 +601,6 @@ func (m *Manager) RequeueWorkload(ctx context.Context, info *workload.Info, reas
 	return added
 }
 
-// HandleInadmissibleHash bulk-moves all workloads in the ClusterQueue's heap
-// that share the given scheduling hash to inadmissibleWorkloads.
-func (m *Manager) HandleInadmissibleHash(cqName kueue.ClusterQueueReference, hash string) int {
-	m.RLock()
-	defer m.RUnlock()
-	cq := m.hm.ClusterQueue(cqName)
-	if cq == nil {
-		return 0
-	}
-	moved := cq.handleInadmissibleHash(hash)
-	if moved > 0 {
-		// Update pending metrics for the CQ and all its LocalQueues.
-		reportPendingWorkloads(m, cqName)
```

  I see, thanks
