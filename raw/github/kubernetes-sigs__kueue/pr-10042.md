# PR #10042: TAS NodeHotSwap: Fix performance issue due to listing all Workloads in Reconcile

**Summary**: TAS NodeHotSwap: Fix performance issue due to listing all Workloads in Reconcile

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10042

**Last updated**: 2026-03-20T11:38:11Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `improve-workload-list-perf` → `main`
- **Created**: 2026-03-20T08:18:23Z
- **Updated**: 2026-03-20T11:38:11Z
- **Merged**: 2026-03-20T11:36:12Z
- **Closed**: 2026-03-20T11:36:12Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 5   **+352 / -13**

## Description

#### What type of PR is this?
/kind bug
/area tas

#### What this PR does / why we need it:

Improves performance of `Reconcile` in node_controller by adding an indexer for workload listing

#### Which issue(s) this PR fixes:
Fixes ##10037

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Improved the performance of the node_controller Reconcile loop by introducing a new field indexer for Workloads.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-20T08:18:29Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c4f147e39b4ea614f96d89827e5365b7b640a7d5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69bd291c21378a0008571b50 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T08:18:34Z

Hi @j-skiba. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-20T08:23:27Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T11:10:22Z

LGTM label has been added.  <details>Git tree hash: 1d48d73c43107db9ca42d310af81ca634d0c9eff</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-20T11:10:24Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10042#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10042#pullrequestreview-3980891227" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-20T11:36:58Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-20T11:37:36Z

@mimowo: #10042 failed to apply on top of branch "release-0.15":
```
Applying: add indexer for listing workloads on node
Applying: extract logic to utils and add tas check for wl
Using index info to reconstruct a base tree...
M	pkg/util/tas/tas_assignment.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/util/tas/tas_assignment.go
CONFLICT (content): Merge conflict in pkg/util/tas/tas_assignment.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0002 extract logic to utils and add tas check for wl

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10042#issuecomment-4097416631):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-20T11:38:11Z

@mimowo: #10042 failed to apply on top of branch "release-0.16":
```
Applying: add indexer for listing workloads on node
Applying: extract logic to utils and add tas check for wl
Using index info to reconstruct a base tree...
M	pkg/util/tas/tas_assignment.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/util/tas/tas_assignment.go
CONFLICT (content): Merge conflict in pkg/util/tas/tas_assignment.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0002 extract logic to utils and add tas check for wl

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10042#issuecomment-4097416631):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:26:53Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:26:53Z
```diff
@@ -85,6 +86,25 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 	return []string{string(*flavor.Spec.TopologyName)}
 }
 
+func indexWorkloadTASNode(o client.Object) []string {
+	wl, ok := o.(*kueue.Workload)
+	if !ok || wl.Status.Admission == nil {
+		return nil
+	}
+	var nodes []string
```

  I think this should be set which is later flattened, because multiple PodSets could overlap on the same nodes. Please add a unit test for Reconcile in such case

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:27:50Z

- **pkg/controller/tas/indexer/indexer.go:105** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:27:50Z
```diff
@@ -85,6 +86,25 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 	return []string{string(*flavor.Spec.TopologyName)}
 }
 
+func indexWorkloadTASNode(o client.Object) []string {
+	wl, ok := o.(*kueue.Workload)
+	if !ok || wl.Status.Admission == nil {
+		return nil
+	}
+	var nodes []string
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment == nil || !utiltas.IsLowestLevelHostname(psa.TopologyAssignment.Levels) {
+			continue
+		}
+		for domain := range utiltas.InternalSeqFrom(psa.TopologyAssignment) {
+			if len(domain.Values) > 0 && domain.Count > 0 {
+				nodes = append(nodes, domain.Values[len(domain.Values)-1])
+			}
+		}
+	}
+	return nodes
```

  I would suggest to extract this to a helper in the utils, and make sure it is covered with unit tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:28:22Z

Thank you for fixing 👍 I like the approach, just need to iron out the corner cases and testing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:44:03Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:44:03Z
```diff
@@ -85,6 +86,25 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 	return []string{string(*flavor.Spec.TopologyName)}
 }
 
+func indexWorkloadTASNode(o client.Object) []string {
+	wl, ok := o.(*kueue.Workload)
+	if !ok || wl.Status.Admission == nil {
+		return nil
+	}
+	var nodes []string
```

  The indexer function is called many times, so I would suggest to do a read-only check if there is TAS used first. So that we allocate the memory (for set or slices) only for TAS workloads. IIRC we already have helpers to determine if TAS is used by workload.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T08:59:33Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@mimowo](https://github.com/mimowo) — 2026-03-20T08:59:34Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  I think it makes sense to move the check under `NodeNamesInPodSetAssignments`, wdyt

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:01:36Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:01:36Z
```diff
@@ -85,6 +86,25 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 	return []string{string(*flavor.Spec.TopologyName)}
 }
 
+func indexWorkloadTASNode(o client.Object) []string {
+	wl, ok := o.(*kueue.Workload)
+	if !ok || wl.Status.Admission == nil {
+		return nil
+	}
+	var nodes []string
```

  changed to
  ```go
  	if !ok || !workload.IsAdmittedByTAS(wl) {
  		return nil
  	}
  ```
  
  ---
  I also changed nodes slice to a set

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:03:47Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:03:47Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  it would be import cycle. workloads util already uses util tas

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:03:58Z

- **pkg/controller/tas/indexer/indexer.go:105** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:03:58Z
```diff
@@ -85,6 +86,25 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 	return []string{string(*flavor.Spec.TopologyName)}
 }
 
+func indexWorkloadTASNode(o client.Object) []string {
+	wl, ok := o.(*kueue.Workload)
+	if !ok || wl.Status.Admission == nil {
+		return nil
+	}
+	var nodes []string
+	for _, psa := range wl.Status.Admission.PodSetAssignments {
+		if psa.TopologyAssignment == nil || !utiltas.IsLowestLevelHostname(psa.TopologyAssignment.Levels) {
+			continue
+		}
+		for domain := range utiltas.InternalSeqFrom(psa.TopologyAssignment) {
+			if len(domain.Values) > 0 && domain.Count > 0 {
+				nodes = append(nodes, domain.Values[len(domain.Values)-1])
+			}
+		}
+	}
+	return nodes
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T09:14:01Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@mimowo](https://github.com/mimowo) — 2026-03-20T09:14:01Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  I see, so put the function `NodeNamesInPodSetAssignments` under workload package, and call the TAS. This will allow us to strengthen testing by unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T09:16:39Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@mimowo](https://github.com/mimowo) — 2026-03-20T09:16:39Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  Actually, there is a nuance `IsAdmittedByTAS` only checks if "Admitted" which is a stronger condition than currently where we check that the workload has status.admission != nil. I think the intention is to run NodeHotSwap only for Admitted, so it would be good to cover such corner cases with unit tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T09:18:01Z

- **pkg/util/tas/tas_assignment.go:318** by [@mimowo](https://github.com/mimowo) — 2026-03-20T09:18:01Z
```diff
@@ -313,6 +314,22 @@ func ComputeUsagePerDomain(ta *TopologyAssignment, singlePodRequests resources.R
 	return usage
 }
 
+// NodeNamesInPodSetAssignments extracts the unique set of node names that a Workload is assigned to.
+func NodeNamesInPodSetAssignments(psas []kueue.PodSetAssignment) []string {
```

  Make sure this is covered 100% by unit tests

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:35:33Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:35:33Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  ok, that sounds better :+1:
  
  I moved that to workload package and also renamed to `TASAssignedNodeNames`.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:36:06Z

- **pkg/util/tas/tas_assignment.go:318** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:36:06Z
```diff
@@ -313,6 +314,22 @@ func ComputeUsagePerDomain(ta *TopologyAssignment, singlePodRequests resources.R
 	return usage
 }
 
+// NodeNamesInPodSetAssignments extracts the unique set of node names that a Workload is assigned to.
+func NodeNamesInPodSetAssignments(psas []kueue.PodSetAssignment) []string {
```

  ```bash
  $ go test ./pkg/workload/ -coverprofile=cover.out && go tool cover -func=cover.out | grep TASAssignedNodeNames
  ok      sigs.k8s.io/kueue/pkg/workload  0.322s  coverage: 55.2% of statements
  sigs.k8s.io/kueue/pkg/workload/workload.go:1814:        TASAssignedNodeNames                    100.0%
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:44:23Z

- **pkg/controller/tas/indexer/indexer.go:94** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:44:22Z
```diff
@@ -88,21 +89,11 @@ func indexResourceFlavorTopologyName(o client.Object) []string {
 
 func indexWorkloadTASNode(o client.Object) []string {
 	wl, ok := o.(*kueue.Workload)
-	if !ok || wl.Status.Admission == nil {
+	if !ok || !workload.IsAdmittedByTAS(wl) {
 		return nil
 	}
```

  > Actually, there is a nuance IsAdmittedByTAS only checks if "Admitted" which is a stronger condition than currently where we check that the workload has status.admission != nil. I think the intention is to run NodeHotSwap only for Admitted, so it would be good to cover such corner cases with unit tests.
  
  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T09:44:51Z

- **pkg/workload/workload_test.go:2736** by [@mimowo](https://github.com/mimowo) — 2026-03-20T09:44:52Z
```diff
@@ -2654,3 +2654,185 @@ func TestSchedulingHash(t *testing.T) {
 		})
 	}
 }
+
+func TestUsedNodes(t *testing.T) {
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want []string
+	}{
+		"unadmitted workload": {
+			wl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Admission: nil,
+				},
+			},
+			want: nil,
+		},
+		"admitted without tas": {
+			wl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Conditions: []metav1.Condition{{Type: kueue.WorkloadAdmitted, Status: metav1.ConditionTrue}},
```

  Add a test case for Workload with QuotaReserved condtion, but with "Assignment" already - waiting for AdmissionCheck

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T09:50:51Z

- **pkg/workload/workload_test.go:2736** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T09:50:51Z
```diff
@@ -2654,3 +2654,185 @@ func TestSchedulingHash(t *testing.T) {
 		})
 	}
 }
+
+func TestUsedNodes(t *testing.T) {
+	cases := map[string]struct {
+		wl   *kueue.Workload
+		want []string
+	}{
+		"unadmitted workload": {
+			wl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Admission: nil,
+				},
+			},
+			want: nil,
+		},
+		"admitted without tas": {
+			wl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Conditions: []metav1.Condition{{Type: kueue.WorkloadAdmitted, Status: metav1.ConditionTrue}},
```

  added 👍

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T10:13:36Z

- **pkg/controller/tas/node_controller.go:324** by [@mimowo](https://github.com/mimowo) — 2026-03-20T10:13:37Z
```diff
@@ -308,27 +308,31 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 // getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
 // or have "late" pods assigned to this node via nodeSelector.
 func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var allWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &allWorkloads); err != nil {
+	var workloadsOnNode kueue.WorkloadList
+	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.WorkloadTASNodeKey: nodeName}); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
 	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range allWorkloads.Items {
-		wl := &allWorkloads.Items[i]
+	for i := range workloadsOnNode.Items {
+		wl := &workloadsOnNode.Items[i]
 		if workload.IsFinished(wl) || workload.IsEvicted(wl) {
 			continue
 		}
-		wlKey := types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace}
-		if wl.Status.Admission != nil && utiltas.HasTASAssignmentOnNode(wl.Status.Admission.PodSetAssignments, nodeName) {
-			tasWorkloadsOnNode.Insert(wlKey)
+		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+	}
+
+	// Also find workloads from any pods that are assigned to this node by TopologyAssignment
+	// but not yet bound. These might be stale "late" pods for a workload that has already
+	// been reassigned to another node.
```

  Do we have a unit test in node_controller_test.go for the Reconcile covering such cases?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T10:25:47Z

- **pkg/controller/tas/node_controller.go:324** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T10:25:46Z
```diff
@@ -308,27 +308,31 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 // getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
 // or have "late" pods assigned to this node via nodeSelector.
 func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var allWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &allWorkloads); err != nil {
+	var workloadsOnNode kueue.WorkloadList
+	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.WorkloadTASNodeKey: nodeName}); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
 	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range allWorkloads.Items {
-		wl := &allWorkloads.Items[i]
+	for i := range workloadsOnNode.Items {
+		wl := &workloadsOnNode.Items[i]
 		if workload.IsFinished(wl) || workload.IsEvicted(wl) {
 			continue
 		}
-		wlKey := types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace}
-		if wl.Status.Admission != nil && utiltas.HasTASAssignmentOnNode(wl.Status.Admission.PodSetAssignments, nodeName) {
-			tasWorkloadsOnNode.Insert(wlKey)
+		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+	}
+
+	// Also find workloads from any pods that are assigned to this node by TopologyAssignment
+	// but not yet bound. These might be stale "late" pods for a workload that has already
+	// been reassigned to another node.
```

  we didn't have such case in unit tests, it was tested just in integration tests.
  
  I added that and also added two test cases for evicted and finished workloads - https://github.com/kubernetes-sigs/kueue/pull/10042/changes#diff-4a0cb79ce927ca48906417d823ad3303285b2c08b5a74c0a66d66214578eb654L319

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T10:46:15Z

- **pkg/controller/tas/indexer/indexer.go:38** by [@mimowo](https://github.com/mimowo) — 2026-03-20T10:46:15Z
```diff
@@ -34,6 +35,7 @@ const (
 	PodNodeSelectorHostnameKey    = "spec.nodeSelector.hostname"
 	PodNodeNameKey                = "spec.nodeName"
 	ResourceFlavorTopologyNameKey = "spec.topologyName"
+	WorkloadTASNodeKey            = "status.admission.podSetAssignments.topologyAssignment.nodeName"
```

  I would propose to make it a bit more scoped, like AdmittedWorkloadNodesKey with `metadata.admittedWorkloadNodes`, because this is not so generic really. And I would already include here the filtering which excludes Finished and Evicted workloads. Keeping all Finished workloads may impact memory signifficantly actually

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T10:50:03Z

- **pkg/controller/tas/node_controller.go:332** by [@mimowo](https://github.com/mimowo) — 2026-03-20T10:50:03Z
```diff
@@ -308,27 +308,31 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 // getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
 // or have "late" pods assigned to this node via nodeSelector.
 func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var allWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &allWorkloads); err != nil {
+	var workloadsOnNode kueue.WorkloadList
+	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.WorkloadTASNodeKey: nodeName}); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
 	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range allWorkloads.Items {
-		wl := &allWorkloads.Items[i]
+	for i := range workloadsOnNode.Items {
+		wl := &workloadsOnNode.Items[i]
 		if workload.IsFinished(wl) || workload.IsEvicted(wl) {
 			continue
 		}
-		wlKey := types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace}
-		if wl.Status.Admission != nil && utiltas.HasTASAssignmentOnNode(wl.Status.Admission.PodSetAssignments, nodeName) {
-			tasWorkloadsOnNode.Insert(wlKey)
+		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+	}
+
+	// Also find workloads from any pods that are assigned to this node by TopologyAssignment
+	// but not yet bound. These might be stale "late" pods for a workload that has already
+	// been reassigned to another node.
+	for wlKey := range nodeSelectorPodsByWorkload {
+		if tasWorkloadsOnNode.Has(wlKey) {
 			continue
 		}
-
-		// Also find workloads from any pods that are assigned to this node by TopologyAssignment
-		// but not yet bound. These might be stale "late" pods for a workload that has already
-		// been reassigned to another node.
-		if len(nodeSelectorPodsByWorkload[wlKey]) > 0 {
-			tasWorkloadsOnNode.Insert(wlKey)
+		var wl kueue.Workload
+		if err := r.client.Get(ctx, wlKey, &wl); err == nil {
```

  Do not silence the error, completely, log it at least

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T11:02:11Z

- **pkg/controller/tas/node_controller.go:332** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T11:02:11Z
```diff
@@ -308,27 +308,31 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 // getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
 // or have "late" pods assigned to this node via nodeSelector.
 func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var allWorkloads kueue.WorkloadList
-	if err := r.client.List(ctx, &allWorkloads); err != nil {
+	var workloadsOnNode kueue.WorkloadList
+	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.WorkloadTASNodeKey: nodeName}); err != nil {
 		return nil, fmt.Errorf("failed to list workloads: %w", err)
 	}
 	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range allWorkloads.Items {
-		wl := &allWorkloads.Items[i]
+	for i := range workloadsOnNode.Items {
+		wl := &workloadsOnNode.Items[i]
 		if workload.IsFinished(wl) || workload.IsEvicted(wl) {
 			continue
 		}
-		wlKey := types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace}
-		if wl.Status.Admission != nil && utiltas.HasTASAssignmentOnNode(wl.Status.Admission.PodSetAssignments, nodeName) {
-			tasWorkloadsOnNode.Insert(wlKey)
+		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+	}
+
+	// Also find workloads from any pods that are assigned to this node by TopologyAssignment
+	// but not yet bound. These might be stale "late" pods for a workload that has already
+	// been reassigned to another node.
+	for wlKey := range nodeSelectorPodsByWorkload {
+		if tasWorkloadsOnNode.Has(wlKey) {
 			continue
 		}
-
-		// Also find workloads from any pods that are assigned to this node by TopologyAssignment
-		// but not yet bound. These might be stale "late" pods for a workload that has already
-		// been reassigned to another node.
-		if len(nodeSelectorPodsByWorkload[wlKey]) > 0 {
-			tasWorkloadsOnNode.Insert(wlKey)
+		var wl kueue.Workload
+		if err := r.client.Get(ctx, wlKey, &wl); err == nil {
```

  added v4 log on error

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-03-20T11:03:22Z

- **pkg/controller/tas/indexer/indexer.go:38** by [@j-skiba](https://github.com/j-skiba) — 2026-03-20T11:03:21Z
```diff
@@ -34,6 +35,7 @@ const (
 	PodNodeSelectorHostnameKey    = "spec.nodeSelector.hostname"
 	PodNodeNameKey                = "spec.nodeName"
 	ResourceFlavorTopologyNameKey = "spec.topologyName"
+	WorkloadTASNodeKey            = "status.admission.podSetAssignments.topologyAssignment.nodeName"
```

  I added IsEvicted` and `IsFinished` checks to the indexer, also renamed to `metadata.admittedWorkloadNodes`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-20T11:10:15Z

Thank you for the follow up 👍 Please also do some manual testing of the mechanism so that we can discover some bugs which are hard to predict just by static code analysis. Such manual exploratory tests will be incredibly valuable before the next patch release.
/lgtm
/approve
/cherrypick release-0.16
/cherrypick release-0.15
