# PR #1005: Always try to stop a job on eviction

**Summary**: Always try to stop a job on eviction

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1005

**Last updated**: 2023-07-28T07:39:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `job-suspend` → `main`
- **Created**: 2023-07-20T19:42:40Z
- **Updated**: 2023-07-28T07:39:58Z
- **Merged**: 2023-07-28T07:39:56Z
- **Closed**: 2023-07-28T07:39:56Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 8   **+178 / -30**

## Description

#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Preempting a batch/Job requires 3 API calls to the Job object because of the restrictions in the immutability of node affinity for started jobs.

This means that if any of the first 2 API calls fails, the job is not fully stopped and could have incompatible node affinities in a future admission.

This PR makes sure that a second sync is able to pick up from where the last failure happened.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Improve job controller to be resilient to API failures during preemption
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-20T19:42:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 71e91081dc581da09e507f995599cbe94d2655b4 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64c2a7d038d3b1000889cac2 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1005--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1005--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMDUtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.naOChEUe1klSPHQGfz0R-dnacr_fPD_Fp_E3eLiWqJg)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-21T20:13:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1005#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]
- ~~[pkg/util/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-21T20:14:44Z

/assign @stuton

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-21T20:14:46Z

@alculquicondor: GitHub didn't allow me to assign the following users: stuton.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) with read permissions, repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1005#issuecomment-1646191983):

>/assign @stuton 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-21T20:15:11Z

/assign @mimowo

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-24T09:58:45Z

- **pkg/controller/jobframework/reconciler.go:436** by [@mimowo](https://github.com/mimowo) — 2023-07-24T09:34:17Z
```diff
@@ -425,11 +426,20 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 }
 
 // stopJob will suspend the job, and also restore node affinity, reset job status if needed.
+// Returns whether any operation was done to stop the job or an error.
 func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload, eventMsg string) error {
 	info := getPodSetsInfoFromWorkload(wl)
 
 	if jws, implements := job.(JobWithCustomStop); implements {
-		return jws.Stop(ctx, r.client, info)
+		err := jws.Stop(ctx, r.client, info)
+		if err != nil {
+			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
```

  I assume the event should be emitted if there is no error. 
  
  But then, IIUC, the event is triggered on every `stopJob` invocation, which is on every reconciliation. Sounds excessive.
  
  It seems we could emit the event only once when actual `.spec.suspend` is transitioned.
  
  If we want to send the event at the end of the process of stopping (with reset of start time and node selectors) we may need to have a `Stopped` condition to mark that it was done without errors.

- **pkg/controller/jobs/job/job_controller_test.go:621** by [@mimowo](https://github.com/mimowo) — 2023-07-24T09:41:46Z
```diff
@@ -418,6 +419,82 @@ func TestReconciler(t *testing.T) {
 				Image("", nil).
 				Obj(),
 		},
+		"when workload is evicted, suspend, reset startTime and restore node affinity": {
+			job: *utiltestingjob.MakeJob("job", "ns").
+				Suspend(false).
+				StartTime(time.Now()).
+				NodeSelector("provisioning", "spot").
+				Parallelism(10).
+				Request(corev1.ResourceCPU, "1").
+				Image("", nil).
+				Active(10).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").
+					PodSets(*utiltesting.MakePodSet("main", 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Admit(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadEvicted,
+						Status: metav1.ConditionTrue,
+					}).
+					Obj(),
+			},
+			wantJob: *utiltestingjob.MakeJob("job", "ns").
+				Suspend(true).
+				Parallelism(10).
+				Request(corev1.ResourceCPU, "1").
+				Image("", nil).
+				Active(10).
+				Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").
+					PodSets(*utiltesting.MakePodSet("main", 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Admit(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadEvicted,
+						Status: metav1.ConditionTrue,
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted but suspended, reset startTime and restore node affinity": {
```

  I would suggest to add more test case: "when workload is evicted, but suspended, and the startTime is rest, restore node affinity"

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-27T17:28:36Z

- **pkg/controller/jobframework/reconciler.go:436** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T16:04:29Z
```diff
@@ -425,11 +426,20 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 }
 
 // stopJob will suspend the job, and also restore node affinity, reset job status if needed.
+// Returns whether any operation was done to stop the job or an error.
 func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload, eventMsg string) error {
 	info := getPodSetsInfoFromWorkload(wl)
 
 	if jws, implements := job.(JobWithCustomStop); implements {
-		return jws.Stop(ctx, r.client, info)
+		err := jws.Stop(ctx, r.client, info)
+		if err != nil {
+			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
```

  It should have been `== nil`. But you make good points. I changed the implementation of Stop to return a boolean about whether the job was stopped in this call.

- **pkg/controller/jobframework/reconciler.go:436** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T16:13:41Z
```diff
@@ -425,11 +426,20 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 }
 
 // stopJob will suspend the job, and also restore node affinity, reset job status if needed.
+// Returns whether any operation was done to stop the job or an error.
 func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload, eventMsg string) error {
 	info := getPodSetsInfoFromWorkload(wl)
 
 	if jws, implements := job.(JobWithCustomStop); implements {
-		return jws.Stop(ctx, r.client, info)
+		err := jws.Stop(ctx, r.client, info)
+		if err != nil {
+			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
```

  I'll defer a unit test for the recorder to a follow up.

- **pkg/controller/jobs/job/job_controller_test.go:621** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T17:21:22Z
```diff
@@ -418,6 +419,82 @@ func TestReconciler(t *testing.T) {
 				Image("", nil).
 				Obj(),
 		},
+		"when workload is evicted, suspend, reset startTime and restore node affinity": {
+			job: *utiltestingjob.MakeJob("job", "ns").
+				Suspend(false).
+				StartTime(time.Now()).
+				NodeSelector("provisioning", "spot").
+				Parallelism(10).
+				Request(corev1.ResourceCPU, "1").
+				Image("", nil).
+				Active(10).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").
+					PodSets(*utiltesting.MakePodSet("main", 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Admit(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadEvicted,
+						Status: metav1.ConditionTrue,
+					}).
+					Obj(),
+			},
+			wantJob: *utiltestingjob.MakeJob("job", "ns").
+				Suspend(true).
+				Parallelism(10).
+				Request(corev1.ResourceCPU, "1").
+				Image("", nil).
+				Active(10).
+				Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").
+					PodSets(*utiltesting.MakePodSet("main", 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Admit(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Condition(metav1.Condition{
+						Type:   kueue.WorkloadEvicted,
+						Status: metav1.ConditionTrue,
+					}).
+					Obj(),
+			},
+		},
+		"when workload is evicted but suspended, reset startTime and restore node affinity": {
```

  added.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-28T07:38:06Z

- **pkg/controller/jobframework/reconciler.go:436** by [@mimowo](https://github.com/mimowo) — 2023-07-28T07:38:06Z
```diff
@@ -425,11 +426,20 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 }
 
 // stopJob will suspend the job, and also restore node affinity, reset job status if needed.
+// Returns whether any operation was done to stop the job or an error.
 func (r *JobReconciler) stopJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload, eventMsg string) error {
 	info := getPodSetsInfoFromWorkload(wl)
 
 	if jws, implements := job.(JobWithCustomStop); implements {
-		return jws.Stop(ctx, r.client, info)
+		err := jws.Stop(ctx, r.client, info)
+		if err != nil {
+			r.record.Eventf(object, corev1.EventTypeNormal, "Stopped", eventMsg)
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-28T07:38:52Z

/lgtm
