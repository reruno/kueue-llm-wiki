# PR #10062: doc: Add TASMultiLayerTopology documentation for v0.17

**Summary**: doc: Add TASMultiLayerTopology documentation for v0.17

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10062

**Last updated**: 2026-03-23T06:54:18Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Huang-Wei](https://github.com/Huang-Wei)
- **Branch**: `tas-multi-layer-doc` → `main`
- **Created**: 2026-03-21T01:18:12Z
- **Updated**: 2026-03-23T06:54:18Z
- **Merged**: 2026-03-23T06:54:15Z
- **Closed**: 2026-03-23T06:54:15Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Changed files**: 1   **+41 / -11**

## Description

#### What type of PR is this?

/kind documentation
/area tas

#### What this PR does / why we need it:

Add documentation coverage for the TASMultiLayerTopology alpha feature gate, including a conceptual section in the TAS docs, the user-facing annotation reference, and a sample Job YAML demonstrating multi-layer topology constraints.

#### Which issue(s) this PR fixes:

Part of #9046

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-21T01:18:18Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b96e7d0611dd5315b0e436d6c1144a986e912abd |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c0d2fee27e530008f95a8d |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10062--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10062--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMDYyLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.Q4RFoUlLdQZWt0SheQgeT14pyv23kN0AMKfKI3PyUvY)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10062--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-21T01:19:30Z

cc @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-23T06:52:37Z

/lgtm 
/approve 
Thank you !

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T06:52:45Z

LGTM label has been added.  <details>Git tree hash: d605257aba300b09324c69ec15f049a5ad2313b9</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T06:52:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10062#" title="Author self-approved">Huang-Wei</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10062#issuecomment-4108412541" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-03-22T01:11:13Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:279** by [@kannon92](https://github.com/kannon92) — 2026-03-22T01:11:13Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
```

  Should we link to KEP for more info?
  
  otherwise this LGTM.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-22T11:12:00Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:317** by [@mimowo](https://github.com/mimowo) — 2026-03-22T11:12:00Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
+{{< feature-state state="alpha" for_version="v0.17" >}}
+{{% alert title="Note" color="primary" %}}
+`TASMultiLayerTopology` is currently an alpha feature and is disabled by default.
+
+You can enable it by editing the `TASMultiLayerTopology` feature gate. Refer to the
+[Installation guide](/docs/installation/#change-the-feature-gates-configuration)
+for instructions on configuring feature gates.
+{{% /alert %}}
+
+Multi-layer topology allows you to define up to 3 layers of slice topology constraints,
+enabling fine-grained placement across deep topology hierarchies. This is useful in data
+centers with multiple topology levels (e.g., datacenter, block, rack) where you need
+different slice sizes at each level.
+
+With the single-layer approach (`kueue.x-k8s.io/podset-slice-required-topology` +
+`kueue.x-k8s.io/podset-slice-size`), you can only specify one topology level and one
+slice size. Multi-layer topology extends this by letting you specify constraints at
+multiple levels simultaneously.
+
+For example, if you have 64 pods and want groups of 32 pods within the same block and
+groups of 16 pods within the same rack, you can express this with a single annotation:
+
+```yaml
+kueue.x-k8s.io/podset-slice-required-topology-constraints: |
+  [
+    {"topology": "cloud.provider.com/topology-block", "size": 32},
+    {"topology": "cloud.provider.com/topology-rack", "size": 16}
+  ]
+```
+
+The constraints are specified as a JSON array ordered from the outermost (coarsest) to
+the innermost (finest) topology layer. Each inner slice size must evenly divide the
+outer slice size.
+
+This annotation is mutually exclusive with `kueue.x-k8s.io/podset-slice-required-topology`
+and `kueue.x-k8s.io/podset-slice-size`.
+
+##### Example
```

  I think this is out of scope for this PR, but I think TAS deserves to have a "tasks" page to demonstrate how to use TAS for various use cases under https://kueue.sigs.k8s.io/docs/tasks/run/, say "Topology-Aware Scheduling", then we could move the examples here, I tentatively opened the dedicated issue: https://github.com/kubernetes-sigs/kueue/issues/10066
  
  Feel free to grab, but also feel free to leave it for folks interested in documenting TAS more.

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-23T05:41:16Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:279** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-23T05:41:16Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
```

  I'm following other alpha feature's style (like TASBalancedPlacement). If needed, I can thoroughly check all alpha feature and link to their KEPs, wdyt?

### Review by [@Huang-Wei](https://github.com/Huang-Wei) (commented) — 2026-03-23T05:42:19Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:317** by [@Huang-Wei](https://github.com/Huang-Wei) — 2026-03-23T05:42:19Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
+{{< feature-state state="alpha" for_version="v0.17" >}}
+{{% alert title="Note" color="primary" %}}
+`TASMultiLayerTopology` is currently an alpha feature and is disabled by default.
+
+You can enable it by editing the `TASMultiLayerTopology` feature gate. Refer to the
+[Installation guide](/docs/installation/#change-the-feature-gates-configuration)
+for instructions on configuring feature gates.
+{{% /alert %}}
+
+Multi-layer topology allows you to define up to 3 layers of slice topology constraints,
+enabling fine-grained placement across deep topology hierarchies. This is useful in data
+centers with multiple topology levels (e.g., datacenter, block, rack) where you need
+different slice sizes at each level.
+
+With the single-layer approach (`kueue.x-k8s.io/podset-slice-required-topology` +
+`kueue.x-k8s.io/podset-slice-size`), you can only specify one topology level and one
+slice size. Multi-layer topology extends this by letting you specify constraints at
+multiple levels simultaneously.
+
+For example, if you have 64 pods and want groups of 32 pods within the same block and
+groups of 16 pods within the same rack, you can express this with a single annotation:
+
+```yaml
+kueue.x-k8s.io/podset-slice-required-topology-constraints: |
+  [
+    {"topology": "cloud.provider.com/topology-block", "size": 32},
+    {"topology": "cloud.provider.com/topology-rack", "size": 16}
+  ]
+```
+
+The constraints are specified as a JSON array ordered from the outermost (coarsest) to
+the innermost (finest) topology layer. Each inner slice size must evenly divide the
+outer slice size.
+
+This annotation is mutually exclusive with `kueue.x-k8s.io/podset-slice-required-topology`
+and `kueue.x-k8s.io/podset-slice-size`.
+
+##### Example
```

  It makes sense. Removed the `Example` section to keep the styles consistent. Let's follow-up to add a dedicated Task for TAS.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T06:49:57Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:279** by [@mimowo](https://github.com/mimowo) — 2026-03-23T06:49:57Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
```

  let's consider a follow up with a section like "References" or "Learn more" where we could link the TAS KEP. There I imagine we could also link the tasks page.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-23T06:52:08Z

- **site/content/en/docs/concepts/topology_aware_scheduling.md:317** by [@mimowo](https://github.com/mimowo) — 2026-03-23T06:52:08Z
```diff
@@ -270,6 +276,48 @@ will be maximied. Also (as a second criterion) the number of domains used on the
 minimized. However, if the Job would not fit within a single domain **one level above** the indicated level,
 Kueue will not perform the balanced placement and will fallback to the standard TAS algorithm.
 
+#### Multi-Layer Topology
+{{< feature-state state="alpha" for_version="v0.17" >}}
+{{% alert title="Note" color="primary" %}}
+`TASMultiLayerTopology` is currently an alpha feature and is disabled by default.
+
+You can enable it by editing the `TASMultiLayerTopology` feature gate. Refer to the
+[Installation guide](/docs/installation/#change-the-feature-gates-configuration)
+for instructions on configuring feature gates.
+{{% /alert %}}
+
+Multi-layer topology allows you to define up to 3 layers of slice topology constraints,
+enabling fine-grained placement across deep topology hierarchies. This is useful in data
+centers with multiple topology levels (e.g., datacenter, block, rack) where you need
+different slice sizes at each level.
+
+With the single-layer approach (`kueue.x-k8s.io/podset-slice-required-topology` +
+`kueue.x-k8s.io/podset-slice-size`), you can only specify one topology level and one
+slice size. Multi-layer topology extends this by letting you specify constraints at
+multiple levels simultaneously.
+
+For example, if you have 64 pods and want groups of 32 pods within the same block and
+groups of 16 pods within the same rack, you can express this with a single annotation:
+
+```yaml
+kueue.x-k8s.io/podset-slice-required-topology-constraints: |
+  [
+    {"topology": "cloud.provider.com/topology-block", "size": 32},
+    {"topology": "cloud.provider.com/topology-rack", "size": 16}
+  ]
+```
+
+The constraints are specified as a JSON array ordered from the outermost (coarsest) to
+the innermost (finest) topology layer. Each inner slice size must evenly divide the
+outer slice size.
+
+This annotation is mutually exclusive with `kueue.x-k8s.io/podset-slice-required-topology`
+and `kueue.x-k8s.io/podset-slice-size`.
+
+##### Example
```

  sgtm, would you have cycles to contribute such a starter for the page with the new feature. I mean the page would be more generic but we could start with the multi-layer example
