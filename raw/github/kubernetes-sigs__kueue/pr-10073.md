# PR #10073: feat(kueueviz): Add resource utilization visualization to kueueviz UI

**Summary**: feat(kueueviz): Add resource utilization visualization to kueueviz UI

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10073

**Last updated**: 2026-03-27T09:01:02Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ziadmoubayed](https://github.com/ziadmoubayed)
- **Branch**: `feat-add-resource-usage-to-kueueviz` → `main`
- **Created**: 2026-03-23T17:50:28Z
- **Updated**: 2026-03-27T09:01:02Z
- **Merged**: 2026-03-27T09:00:46Z
- **Closed**: 2026-03-27T09:00:46Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`, `area/dashboard`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@akram](https://github.com/akram), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 23   **+1998 / -177**

## Description

#### What type of PR is this?

/kind feature
/area dashboard
#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
Fixes #10072

#### Special notes for your reviewer:
Tested frontend and backend. AI Assisted

#### Does this PR introduce a user-facing change?
```release-note
KueueViz: Added resource utilization reporting  and small UI improvements.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-23T17:50:34Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e7785f363e442291f12bd381c93ac6aa3346e10f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c637dd7f162b0008bf7563 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-23T17:50:39Z

Hi @ziadmoubayed. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-23T17:56:30Z

Summary of resource usage reporting:
 - Main Page / Dashboard:  Per cohort utilization table. Each row is a cohort (or "Standalone" for CQs
 without a cohort) with usage bars per resource. Cohort names link to the cohort detail page.
 - ClusterQueues list page: Each CQ row now shows inline usage bars per resource.
 - ClusterQueue detail page: The Resource Groups section now includes Usage, Borrowed reported columns.
 - Cohort detail page: Shows aggregated cohort-level utilization bars plus a per-CQ breakdown table with usage bars per resource.
 
 Other changes:
1. In the flavor details page, I add a knob to hide/show nodes labels . Makes the page more compact 
2. Sorting by time, event.reason for events.
3. Sorting by name for other resources.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-23T17:57:20Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-23T17:57:32Z

cc  @samzong ptal

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-24T08:23:00Z

/test pull-kueue-test-e2e-main-1-35

### Comment by [@mukund-wayve](https://github.com/mukund-wayve) — 2026-03-25T10:55:09Z

@ziadmoubayed This seems like a really good feature to add, thanks for raising this PR! Could you add a video with your features similar to [this description](https://github.com/kubernetes-sigs/kueue/pull/9743#issue-4042784937)? This helps visualise the changes better

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-25T11:42:43Z

> @ziadmoubayed This seems like a really good feature to add, thanks for raising this PR! Could you add a video with your features similar to [this description](https://github.com/kubernetes-sigs/kueue/pull/9743#issue-4042784937)? This helps visualise the changes better


https://github.com/user-attachments/assets/7b858be3-b46f-480a-9f2b-44d051e5d9f1

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-26T06:19:36Z

/release-note-edit
```release-note
KueueViz: Added resource utilization reporting  and small UI improvements.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-26T06:20:49Z

This is an excellent improvement 👍 Could you please add some regression e2e asserts to make sure it does not break?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-26T20:55:19Z

/test pull-kueue-verify-main
let me retry

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:39:35Z

@ziadmoubayed as I see now unit tests are added (from backend and frontend individually)  and you did manual testing, I'm leaning to merge. However, I would still like to see also a sanity e2e tests in the future, any opinion here?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:59:09Z

/lgtm
/approve
Thank you 👍

### Comment by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-27T08:59:14Z

> @ziadmoubayed as I see now unit tests are added (from backend and frontend individually) and you did manual testing, I'm leaning to merge. However, I would still like to see also a sanity e2e tests in the future, any opinion here?

Thanks for checking. I will follow up with a PR later today with e2e tests.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T08:59:17Z

LGTM label has been added.  <details>Git tree hash: 1426874139b8e5107275bfa1514c28386556e054</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T08:59:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10073#issuecomment-4141127971" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10073#" title="Author self-approved">ziadmoubayed</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T09:01:02Z

> Thanks for checking. I will follow up with a PR later today with e2e tests.

Awesome, opened the issue to track the remaining effort so that we don't block the merge: https://github.com/kubernetes-sigs/kueue/issues/10132

## Reviews

### Review by [@samzong](https://github.com/samzong) (commented) — 2026-03-25T11:13:43Z

Nice UX improvements—deterministic sorting, cleaner ResourceFlavor, and useful utilization visibility.

two small issues, these impact correctness and should be addressed before merge.

- **cmd/kueueviz/frontend/src/UsageBar.jsx:93** by [@samzong](https://github.com/samzong) — 2026-03-25T10:59:42Z
```diff
@@ -0,0 +1,273 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+import React from 'react';
+import { Box, Tooltip, Typography } from '@mui/material';
+
+/**
+ * Parse a Kubernetes resource quantity string to a numeric value.
+ * Handles: "500m" (millicores), "500Mi", "500Gi", "4250m", "0", "2", etc.
+ * Returns a numeric value in base units (cores for CPU, bytes for memory).
+ */
+export function parseResourceQuantity(str) {
+  if (str === undefined || str === null || str === '') return 0;
+  const s = String(str).trim();
+  if (s === '0') return 0;
+
+  // Millicores: e.g. "4250m"
+  if (s.endsWith('m') && !s.endsWith('Mi')) {
+    return parseFloat(s.slice(0, -1)) / 1000;
+  }
+  // Binary suffixes
+  if (s.endsWith('Ki')) return parseFloat(s.slice(0, -2)) * 1024;
+  if (s.endsWith('Mi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024;
+  if (s.endsWith('Gi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024;
+  if (s.endsWith('Ti')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024 * 1024;
+  // Decimal suffixes
+  if (s.endsWith('k')) return parseFloat(s.slice(0, -1)) * 1000;
+  if (s.endsWith('M')) return parseFloat(s.slice(0, -1)) * 1000000;
+  if (s.endsWith('G')) return parseFloat(s.slice(0, -1)) * 1000000000;
+
+  return parseFloat(s) || 0;
+}
+
+/**
+ * Format a resource value for display. Keeps it human-readable.
+ */
+export function formatResourceValue(value, resourceName) {
+  if (value === 0) return '0';
+  if (resourceName === 'cpu') {
+    if (value < 1) return `${Math.round(value * 1000)}m`;
+    return `${parseFloat(value.toFixed(2))}`;
+  }
+  if (resourceName === 'memory') {
+    const gi = value / (1024 * 1024 * 1024);
+    if (gi >= 1) return `${parseFloat(gi.toFixed(1))}Gi`;
+    const mi = value / (1024 * 1024);
+    return `${parseFloat(mi.toFixed(0))}Mi`;
+  }
+  // GPUs or other integer resources
+  return `${parseFloat(value.toFixed(2))}`;
+}
+
+/**
+ * Aggregate usage/quota for a specific resource across all flavors of a ClusterQueue.
+ *
+ * @param {object} queue - A ClusterQueue object from the backend
+ * @param {string} resourceName - e.g. "cpu", "memory", "nvidia.com/gpu"
+ * @returns {{ usage: number, borrowed: number, quota: number, borrowingLimit: number|null }}
+ *   borrowingLimit: finite number if explicitly set, null if unlimited (in cohort with no limit), 0 if not in a cohort
+ */
+export function aggregateResourceForQueue(queue, resourceName) {
+  let quota = 0;
+  let usage = 0;
+  let borrowed = 0;
+  let borrowingLimitTotal = 0;
+  let hasExplicitBorrowingLimit = false;
+  const inCohort = !!(queue.cohort || queue.spec?.cohortName);
+
+  const resourceGroups = queue.resourceGroups || queue.spec?.resourceGroups || [];
+  for (const rg of resourceGroups) {
+    for (const flavor of (rg.flavors || [])) {
+      for (const res of (flavor.resources || [])) {
+        if (String(res.name) === resourceName) {
+          quota += parseResourceQuantity(res.nominalQuota);
+          if (res.borrowingLimit != null) {
+            hasExplicitBorrowingLimit = true;
+            borrowingLimitTotal += parseResourceQuantity(res.borrowingLimit);
+          }
+        }
+      }
+    }
+  }
+
+  const flavorsUsage = queue.flavorsUsage || queue.status?.flavorsUsage || [];
+  for (const flavor of flavorsUsage) {
+    for (const res of (flavor.resources || [])) {
+      if (String(res.name) === resourceName) {
+        usage += parseResourceQuantity(res.total);
+        borrowed += parseResourceQuantity(res.borrowed);
+      }
+    }
+  }
+
+  const borrowingLimit = hasExplicitBorrowingLimit ? borrowingLimitTotal : (inCohort ? null : 0);
```

  `null` means unlimited borrowing, not zero headroom.
  
  Current logic misclassifies legitimate borrowed usage as overflow. Keep the unlimited semantics during aggregation and display it as unbounded headroom rather than capping it at `quota`.

- **cmd/kueueviz/frontend/src/UsageBar.jsx:45** by [@samzong](https://github.com/samzong) — 2026-03-25T11:02:25Z
```diff
@@ -0,0 +1,273 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+import React from 'react';
+import { Box, Tooltip, Typography } from '@mui/material';
+
+/**
+ * Parse a Kubernetes resource quantity string to a numeric value.
+ * Handles: "500m" (millicores), "500Mi", "500Gi", "4250m", "0", "2", etc.
+ * Returns a numeric value in base units (cores for CPU, bytes for memory).
+ */
+export function parseResourceQuantity(str) {
+  if (str === undefined || str === null || str === '') return 0;
+  const s = String(str).trim();
+  if (s === '0') return 0;
+
+  // Millicores: e.g. "4250m"
+  if (s.endsWith('m') && !s.endsWith('Mi')) {
+    return parseFloat(s.slice(0, -1)) / 1000;
+  }
+  // Binary suffixes
+  if (s.endsWith('Ki')) return parseFloat(s.slice(0, -2)) * 1024;
+  if (s.endsWith('Mi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024;
+  if (s.endsWith('Gi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024;
+  if (s.endsWith('Ti')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024 * 1024;
+  // Decimal suffixes
+  if (s.endsWith('k')) return parseFloat(s.slice(0, -1)) * 1000;
+  if (s.endsWith('M')) return parseFloat(s.slice(0, -1)) * 1000000;
+  if (s.endsWith('G')) return parseFloat(s.slice(0, -1)) * 1000000000;
+
+  return parseFloat(s) || 0;
+}
```

  Hand-rolling a quantity parser here is risky. `resource.Quantity` supports more suffixes (`Pi`, `Ei`, `u`, `n`, etc.) than this helper.
  
  This UI renders arbitrary Kueue resources, so this may produce incorrect values. Use a Kubernetes-compatible parser or return lossless values from the backend.

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-25T13:02:19Z

- **cmd/kueueviz/frontend/src/UsageBar.jsx:45** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-25T13:02:19Z
```diff
@@ -0,0 +1,273 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+import React from 'react';
+import { Box, Tooltip, Typography } from '@mui/material';
+
+/**
+ * Parse a Kubernetes resource quantity string to a numeric value.
+ * Handles: "500m" (millicores), "500Mi", "500Gi", "4250m", "0", "2", etc.
+ * Returns a numeric value in base units (cores for CPU, bytes for memory).
+ */
+export function parseResourceQuantity(str) {
+  if (str === undefined || str === null || str === '') return 0;
+  const s = String(str).trim();
+  if (s === '0') return 0;
+
+  // Millicores: e.g. "4250m"
+  if (s.endsWith('m') && !s.endsWith('Mi')) {
+    return parseFloat(s.slice(0, -1)) / 1000;
+  }
+  // Binary suffixes
+  if (s.endsWith('Ki')) return parseFloat(s.slice(0, -2)) * 1024;
+  if (s.endsWith('Mi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024;
+  if (s.endsWith('Gi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024;
+  if (s.endsWith('Ti')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024 * 1024;
+  // Decimal suffixes
+  if (s.endsWith('k')) return parseFloat(s.slice(0, -1)) * 1000;
+  if (s.endsWith('M')) return parseFloat(s.slice(0, -1)) * 1000000;
+  if (s.endsWith('G')) return parseFloat(s.slice(0, -1)) * 1000000000;
+
+  return parseFloat(s) || 0;
+}
```

  makes sense

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-25T14:43:47Z

- **cmd/kueueviz/frontend/src/UsageBar.jsx:93** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-25T14:43:46Z
```diff
@@ -0,0 +1,273 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+import React from 'react';
+import { Box, Tooltip, Typography } from '@mui/material';
+
+/**
+ * Parse a Kubernetes resource quantity string to a numeric value.
+ * Handles: "500m" (millicores), "500Mi", "500Gi", "4250m", "0", "2", etc.
+ * Returns a numeric value in base units (cores for CPU, bytes for memory).
+ */
+export function parseResourceQuantity(str) {
+  if (str === undefined || str === null || str === '') return 0;
+  const s = String(str).trim();
+  if (s === '0') return 0;
+
+  // Millicores: e.g. "4250m"
+  if (s.endsWith('m') && !s.endsWith('Mi')) {
+    return parseFloat(s.slice(0, -1)) / 1000;
+  }
+  // Binary suffixes
+  if (s.endsWith('Ki')) return parseFloat(s.slice(0, -2)) * 1024;
+  if (s.endsWith('Mi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024;
+  if (s.endsWith('Gi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024;
+  if (s.endsWith('Ti')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024 * 1024;
+  // Decimal suffixes
+  if (s.endsWith('k')) return parseFloat(s.slice(0, -1)) * 1000;
+  if (s.endsWith('M')) return parseFloat(s.slice(0, -1)) * 1000000;
+  if (s.endsWith('G')) return parseFloat(s.slice(0, -1)) * 1000000000;
+
+  return parseFloat(s) || 0;
+}
+
+/**
+ * Format a resource value for display. Keeps it human-readable.
+ */
+export function formatResourceValue(value, resourceName) {
+  if (value === 0) return '0';
+  if (resourceName === 'cpu') {
+    if (value < 1) return `${Math.round(value * 1000)}m`;
+    return `${parseFloat(value.toFixed(2))}`;
+  }
+  if (resourceName === 'memory') {
+    const gi = value / (1024 * 1024 * 1024);
+    if (gi >= 1) return `${parseFloat(gi.toFixed(1))}Gi`;
+    const mi = value / (1024 * 1024);
+    return `${parseFloat(mi.toFixed(0))}Mi`;
+  }
+  // GPUs or other integer resources
+  return `${parseFloat(value.toFixed(2))}`;
+}
+
+/**
+ * Aggregate usage/quota for a specific resource across all flavors of a ClusterQueue.
+ *
+ * @param {object} queue - A ClusterQueue object from the backend
+ * @param {string} resourceName - e.g. "cpu", "memory", "nvidia.com/gpu"
+ * @returns {{ usage: number, borrowed: number, quota: number, borrowingLimit: number|null }}
+ *   borrowingLimit: finite number if explicitly set, null if unlimited (in cohort with no limit), 0 if not in a cohort
+ */
+export function aggregateResourceForQueue(queue, resourceName) {
+  let quota = 0;
+  let usage = 0;
+  let borrowed = 0;
+  let borrowingLimitTotal = 0;
+  let hasExplicitBorrowingLimit = false;
+  const inCohort = !!(queue.cohort || queue.spec?.cohortName);
+
+  const resourceGroups = queue.resourceGroups || queue.spec?.resourceGroups || [];
+  for (const rg of resourceGroups) {
+    for (const flavor of (rg.flavors || [])) {
+      for (const res of (flavor.resources || [])) {
+        if (String(res.name) === resourceName) {
+          quota += parseResourceQuantity(res.nominalQuota);
+          if (res.borrowingLimit != null) {
+            hasExplicitBorrowingLimit = true;
+            borrowingLimitTotal += parseResourceQuantity(res.borrowingLimit);
+          }
+        }
+      }
+    }
+  }
+
+  const flavorsUsage = queue.flavorsUsage || queue.status?.flavorsUsage || [];
+  for (const flavor of flavorsUsage) {
+    for (const res of (flavor.resources || [])) {
+      if (String(res.name) === resourceName) {
+        usage += parseResourceQuantity(res.total);
+        borrowed += parseResourceQuantity(res.borrowed);
+      }
+    }
+  }
+
+  const borrowingLimit = hasExplicitBorrowingLimit ? borrowingLimitTotal : (inCohort ? null : 0);
```

  Now we differentiate between 3 different scenarios:
  1. Finite limit: ceiling = quota + borrowingLimit
  2. Unlimited (nil): ceiling = max(quota, usage) so the bar stretches to fit borrowed usage
  3. No borrowing (not in cohort or zero): bar uses nominal quota

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-25T14:43:54Z

- **cmd/kueueviz/frontend/src/UsageBar.jsx:93** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-25T14:43:54Z
```diff
@@ -0,0 +1,273 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+import React from 'react';
+import { Box, Tooltip, Typography } from '@mui/material';
+
+/**
+ * Parse a Kubernetes resource quantity string to a numeric value.
+ * Handles: "500m" (millicores), "500Mi", "500Gi", "4250m", "0", "2", etc.
+ * Returns a numeric value in base units (cores for CPU, bytes for memory).
+ */
+export function parseResourceQuantity(str) {
+  if (str === undefined || str === null || str === '') return 0;
+  const s = String(str).trim();
+  if (s === '0') return 0;
+
+  // Millicores: e.g. "4250m"
+  if (s.endsWith('m') && !s.endsWith('Mi')) {
+    return parseFloat(s.slice(0, -1)) / 1000;
+  }
+  // Binary suffixes
+  if (s.endsWith('Ki')) return parseFloat(s.slice(0, -2)) * 1024;
+  if (s.endsWith('Mi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024;
+  if (s.endsWith('Gi')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024;
+  if (s.endsWith('Ti')) return parseFloat(s.slice(0, -2)) * 1024 * 1024 * 1024 * 1024;
+  // Decimal suffixes
+  if (s.endsWith('k')) return parseFloat(s.slice(0, -1)) * 1000;
+  if (s.endsWith('M')) return parseFloat(s.slice(0, -1)) * 1000000;
+  if (s.endsWith('G')) return parseFloat(s.slice(0, -1)) * 1000000000;
+
+  return parseFloat(s) || 0;
+}
+
+/**
+ * Format a resource value for display. Keeps it human-readable.
+ */
+export function formatResourceValue(value, resourceName) {
+  if (value === 0) return '0';
+  if (resourceName === 'cpu') {
+    if (value < 1) return `${Math.round(value * 1000)}m`;
+    return `${parseFloat(value.toFixed(2))}`;
+  }
+  if (resourceName === 'memory') {
+    const gi = value / (1024 * 1024 * 1024);
+    if (gi >= 1) return `${parseFloat(gi.toFixed(1))}Gi`;
+    const mi = value / (1024 * 1024);
+    return `${parseFloat(mi.toFixed(0))}Mi`;
+  }
+  // GPUs or other integer resources
+  return `${parseFloat(value.toFixed(2))}`;
+}
+
+/**
+ * Aggregate usage/quota for a specific resource across all flavors of a ClusterQueue.
+ *
+ * @param {object} queue - A ClusterQueue object from the backend
+ * @param {string} resourceName - e.g. "cpu", "memory", "nvidia.com/gpu"
+ * @returns {{ usage: number, borrowed: number, quota: number, borrowingLimit: number|null }}
+ *   borrowingLimit: finite number if explicitly set, null if unlimited (in cohort with no limit), 0 if not in a cohort
+ */
+export function aggregateResourceForQueue(queue, resourceName) {
+  let quota = 0;
+  let usage = 0;
+  let borrowed = 0;
+  let borrowingLimitTotal = 0;
+  let hasExplicitBorrowingLimit = false;
+  const inCohort = !!(queue.cohort || queue.spec?.cohortName);
+
+  const resourceGroups = queue.resourceGroups || queue.spec?.resourceGroups || [];
+  for (const rg of resourceGroups) {
+    for (const flavor of (rg.flavors || [])) {
+      for (const res of (flavor.resources || [])) {
+        if (String(res.name) === resourceName) {
+          quota += parseResourceQuantity(res.nominalQuota);
+          if (res.borrowingLimit != null) {
+            hasExplicitBorrowingLimit = true;
+            borrowingLimitTotal += parseResourceQuantity(res.borrowingLimit);
+          }
+        }
+      }
+    }
+  }
+
+  const flavorsUsage = queue.flavorsUsage || queue.status?.flavorsUsage || [];
+  for (const flavor of flavorsUsage) {
+    for (const res of (flavor.resources || [])) {
+      if (String(res.name) === resourceName) {
+        usage += parseResourceQuantity(res.total);
+        borrowed += parseResourceQuantity(res.borrowed);
+      }
+    }
+  }
+
+  const borrowingLimit = hasExplicitBorrowingLimit ? borrowingLimitTotal : (inCohort ? null : 0);
```

  can you check again please

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-26T21:05:51Z

- **Makefile-verify.mk:239** by [@mimowo](https://github.com/mimowo) — 2026-03-26T21:05:47Z
```diff
@@ -226,6 +235,10 @@ helm-unit-test: helm helm-unittest-plugin ## Run Helm unit tests for the kueue c
 npm-depcheck: ## Verify frontend and e2e npm dependencies.
 	$(_npm_depcheck_recipe)
 
+.PHONY: kueueviz-unit-test
+kueueviz-unit-test: ## Run KueueViz backend (Go) and frontend (vitest) unit tests.
```

  nice thank you. Are this tests currently run from CI? I think we need to add a CI job in a follow up. Note that the CI jobs for Kueue are configured in the Kubernetes/test-infra project. Would you like to open follow up PR?

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-27T06:16:25Z

- **Makefile-verify.mk:239** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-27T06:16:25Z
```diff
@@ -226,6 +235,10 @@ helm-unit-test: helm helm-unittest-plugin ## Run Helm unit tests for the kueue c
 npm-depcheck: ## Verify frontend and e2e npm dependencies.
 	$(_npm_depcheck_recipe)
 
+.PHONY: kueueviz-unit-test
+kueueviz-unit-test: ## Run KueueViz backend (Go) and frontend (vitest) unit tests.
```

  The tests are running current in CI with `make verify`. `verify-kueueviz-unit-test` is wired into `verify-checks` I can create an independenct ci job in a follow up pr if this is makes more sense.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T07:05:12Z

- **Makefile-verify.mk:239** by [@mimowo](https://github.com/mimowo) — 2026-03-27T07:05:12Z
```diff
@@ -226,6 +235,10 @@ helm-unit-test: helm helm-unittest-plugin ## Run Helm unit tests for the kueue c
 npm-depcheck: ## Verify frontend and e2e npm dependencies.
 	$(_npm_depcheck_recipe)
 
+.PHONY: kueueviz-unit-test
+kueueviz-unit-test: ## Run KueueViz backend (Go) and frontend (vitest) unit tests.
```

  I see, I think this rather belongs to dedicated CI job or the `test` goal which is responsible for running unit tests. I would rather prefer a dedicated CI job, so that `make test` is snappy as it is very commonly used by developers of the Kueue core.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T07:10:51Z

- **Makefile-verify.mk:239** by [@mimowo](https://github.com/mimowo) — 2026-03-27T07:10:50Z
```diff
@@ -226,6 +235,10 @@ helm-unit-test: helm helm-unittest-plugin ## Run Helm unit tests for the kueue c
 npm-depcheck: ## Verify frontend and e2e npm dependencies.
 	$(_npm_depcheck_recipe)
 
+.PHONY: kueueviz-unit-test
+kueueviz-unit-test: ## Run KueueViz backend (Go) and frontend (vitest) unit tests.
```

  An alternative is to wire them into kueueviz e2e tests, which would be my preference for the short time.
  
  So:
  1. short term: feel free to keep as is assuming followed up immediately, or put under kueueviz e2e tests
  2. long term put as a dedicated CI Job

### Review by [@ziadmoubayed](https://github.com/ziadmoubayed) (commented) — 2026-03-27T07:59:39Z

- **Makefile-verify.mk:239** by [@ziadmoubayed](https://github.com/ziadmoubayed) — 2026-03-27T07:59:39Z
```diff
@@ -226,6 +235,10 @@ helm-unit-test: helm helm-unittest-plugin ## Run Helm unit tests for the kueue c
 npm-depcheck: ## Verify frontend and e2e npm dependencies.
 	$(_npm_depcheck_recipe)
 
+.PHONY: kueueviz-unit-test
+kueueviz-unit-test: ## Run KueueViz backend (Go) and frontend (vitest) unit tests.
```

  Wired with kueueviz e2e tests. Long term we can add a dedicated ci job.
