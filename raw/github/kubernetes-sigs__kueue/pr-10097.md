# PR #10097: feat: clean local queue metrics based on label change if required

**Summary**: feat: clean local queue metrics based on label change if required

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10097

**Last updated**: 2026-03-27T09:53:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `feat/lq-metric-cleanup-on-label-change` → `main`
- **Created**: 2026-03-25T12:12:35Z
- **Updated**: 2026-03-27T09:53:20Z
- **Merged**: 2026-03-27T09:38:46Z
- **Closed**: 2026-03-27T09:38:46Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@sohankunkerkar](https://github.com/sohankunkerkar), [@kshalot](https://github.com/kshalot)
- **Changed files**: 5   **+80 / -20**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Clean LocalQueue metrics on label change if the new labels are not included in the config.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8830

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: clean local queue metrics if the labels are not matching with the configuration after update.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-25T12:12:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3482c9762316ea6b70764643708b6680671a9396 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c52d4bdbf5b800080e0b89 |

### Comment by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:13:38Z

/hold
The test is highly unstable until #10096 is merged.

/cc @mbobrovskyi @mszadkow
PTAL

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:40:43Z

From the release note:

> Observability: clean local queue metrics if the labels are not matching with the configuration after update.

This is only for guage LQ metrics, or counters as well?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:42:26Z

> /hold
> The test is highly unstable until https://github.com/kubernetes-sigs/kueue/pull/10096 is merged.

/unhold
as the dependency PR is merged

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T08:43:34Z

LGTM label has been added.  <details>Git tree hash: 5eb5bb9129fbcb247e1b6dede30a4976bf6b2108</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T08:43:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10097#pullrequestreview-4019684598" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10097#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-03-25T12:27:34Z

- **pkg/cache/scheduler/cache.go:611** by [@mszadkow](https://github.com/mszadkow) — 2026-03-25T12:27:34Z
```diff
@@ -603,16 +603,17 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
-		if features.Enabled(features.CustomMetricLabels) {
-			c.RLock()
-			defer c.RUnlock()
-			cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
-			if cq != nil {
-				if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
-					lq.Lock()
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
```

  maybe defer the unlock

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T12:32:27Z

- **pkg/cache/scheduler/cache.go:611** by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:32:27Z
```diff
@@ -603,16 +603,17 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
-		if features.Enabled(features.CustomMetricLabels) {
-			c.RLock()
-			defer c.RUnlock()
-			cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
-			if cq != nil {
-				if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
-					lq.Lock()
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
```

  Sure, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T12:32:29Z

- **pkg/cache/scheduler/cache.go:614** by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:32:29Z
```diff
@@ -603,15 +605,16 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
-		if features.Enabled(features.CustomMetricLabels) {
-			c.RLock()
-			defer c.RUnlock()
-			cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
-			if cq != nil {
-				if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
-					lq.Lock()
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
+				defer lq.Unlock()
```

  What is the scope of the defer here actually? Maybe it is safer to extract that code to a helper function to make it super clear the lock is released when the function exists

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T12:33:25Z

- **pkg/cache/scheduler/cache.go:588** by [@mimowo](https://github.com/mimowo) — 2026-03-25T12:33:25Z
```diff
@@ -584,6 +584,8 @@ func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lqKey que
 }
 
 func (c *Cache) GetLocalQueueLabels(cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) (map[string]string, error) {
+	c.RLock()
+	defer c.RUnlock()
```

  Why this lock was not needed before but is needed now? Maybe this was a bug that we didn't have it?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T12:34:06Z

- **pkg/cache/scheduler/cache.go:614** by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:34:06Z
```diff
@@ -603,15 +605,16 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
-		if features.Enabled(features.CustomMetricLabels) {
-			c.RLock()
-			defer c.RUnlock()
-			cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
-			if cq != nil {
-				if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
-					lq.Lock()
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
+				defer lq.Unlock()
```

  Yeah, I guess the scope of this check is too nested either way, will introduce a helper

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T12:34:35Z

- **pkg/cache/scheduler/cache.go:588** by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:34:35Z
```diff
@@ -584,6 +584,8 @@ func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lqKey que
 }
 
 func (c *Cache) GetLocalQueueLabels(cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) (map[string]string, error) {
+	c.RLock()
+	defer c.RUnlock()
```

  Yes, a test caught the race here so I added the lock to that read as well

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T12:34:56Z

- **pkg/cache/scheduler/cache.go:588** by [@mykysha](https://github.com/mykysha) — 2026-03-25T12:34:55Z
```diff
@@ -584,6 +584,8 @@ func (c *Cache) GetCacheLocalQueue(cqName kueue.ClusterQueueReference, lqKey que
 }
 
 func (c *Cache) GetLocalQueueLabels(cqName kueue.ClusterQueueReference, lqKey queue.LocalQueueReference) (map[string]string, error) {
+	c.RLock()
+	defer c.RUnlock()
```

  Can extract to a separate fix PR if needed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T13:39:19Z

- **pkg/cache/scheduler/cache.go:614** by [@mykysha](https://github.com/mykysha) — 2026-03-25T13:39:19Z
```diff
@@ -603,15 +605,16 @@ func (c *Cache) ClusterQueueUsesAdmissionFairSharing(cqName kueue.ClusterQueueRe
 
 func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	if oldQ.Spec.ClusterQueue == newQ.Spec.ClusterQueue {
-		if features.Enabled(features.CustomMetricLabels) {
-			c.RLock()
-			defer c.RUnlock()
-			cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
-			if cq != nil {
-				if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
-					lq.Lock()
+		c.RLock()
+		defer c.RUnlock()
+		cq := c.hm.ClusterQueue(newQ.Spec.ClusterQueue)
+		if cq != nil {
+			if lq := cq.localQueues[queue.Key(newQ)]; lq != nil {
+				lq.Lock()
+				defer lq.Unlock()
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:42:41Z

- **pkg/cache/scheduler/cache.go:639** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T14:42:41Z
```diff
@@ -634,6 +625,23 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	return nil
 }
 
+func (c *Cache) updateLqMetricLabels(newLq *kueue.LocalQueue) error {
+	c.RLock()
+	defer c.RUnlock()
+	cq := c.hm.ClusterQueue(newLq.Spec.ClusterQueue)
+	if cq != nil {
+		if cachedLq := cq.localQueues[queue.Key(newLq)]; cachedLq != nil {
+			cachedLq.Lock()
+			defer cachedLq.Unlock()
+			cachedLq.labels = newLq.GetLabels()
+			if features.Enabled(features.CustomMetricLabels) {
+				cachedLq.customMetricLabelValues = c.customLabels.ExtractValues(newLq.Labels, newLq.Annotations)
+			}
```

  Oh, this logic is deeply nested. Could we split it into multiple if statements?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:44:42Z

- **pkg/cache/scheduler/cache.go:641** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T14:44:42Z
```diff
@@ -634,6 +625,23 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	return nil
 }
 
+func (c *Cache) updateLqMetricLabels(newLq *kueue.LocalQueue) error {
+	c.RLock()
+	defer c.RUnlock()
+	cq := c.hm.ClusterQueue(newLq.Spec.ClusterQueue)
+	if cq != nil {
+		if cachedLq := cq.localQueues[queue.Key(newLq)]; cachedLq != nil {
+			cachedLq.Lock()
+			defer cachedLq.Unlock()
+			cachedLq.labels = newLq.GetLabels()
+			if features.Enabled(features.CustomMetricLabels) {
+				cachedLq.customMetricLabelValues = c.customLabels.ExtractValues(newLq.Labels, newLq.Annotations)
+			}
+		}
+	}
```

  ```suggestion
  	if cq == nil {
  		return nil
  	}
  
  	lq := cq.localQueues[queue.Key(newLq)]
  	if lq == nil {
  		return nil
  	}
  
  	lq.Lock()
  	defer lq.Unlock()
  
  	lq.labels = newLq.GetLabels()
  	if features.Enabled(features.CustomMetricLabels) {
  		lq.customMetricLabelValues = c.customLabels.ExtractValues(newLq.Labels, newLq.Annotations)
  	}
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T14:52:15Z

- **test/integration/singlecluster/controller/core/localqueue_controller_test.go:734** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T14:52:15Z
```diff
@@ -690,4 +690,47 @@ var _ = ginkgo.Describe("Queue controller metrics filtering", ginkgo.Label("cont
 			util.ExpectLQPendingWorkloadsMetric(queueDynamic, 1, 0)
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should delete local queue metrics after changing labels to not matching", func() {
+		ginkgo.By("Creating a localQueue with matching labels")
+		queueDeleteMetrics := utiltestingapi.MakeLocalQueue("queue-delete-metrics", ns.Name).
+			ClusterQueue(clusterQueue.Name).
+			Label("metrics-test", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, queueDeleteMetrics)
+
+		wlDeleteMetrics := utiltestingapi.MakeWorkload("wl-delete-metrics", ns.Name).
+			Queue(kueue.LocalQueueName(queueDeleteMetrics.Name)).
+			Request(resourceGPU, "1").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wlDeleteMetrics)
+
+		ginkgo.By("Waiting for workload to be reflected in status (Pending)")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			g.Expect(updated.Status.PendingWorkloads).Should(gomega.Equal(int32(1)))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are initially available for matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 1, 0)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Updating the localQueue to non-matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			if updated.Labels == nil {
+				updated.Labels = make(map[string]string)
+			}
+			updated.Labels["metrics-test"] = "false"
+			g.Expect(k8sClient.Update(ctx, &updated)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are cleared after labels are updated to non-matching")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 0, 0)
+		}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  ```suggestion
  		}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
  ```

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T15:52:06Z

- **pkg/cache/scheduler/cache.go:639** by [@mykysha](https://github.com/mykysha) — 2026-03-25T15:52:05Z
```diff
@@ -634,6 +625,23 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	return nil
 }
 
+func (c *Cache) updateLqMetricLabels(newLq *kueue.LocalQueue) error {
+	c.RLock()
+	defer c.RUnlock()
+	cq := c.hm.ClusterQueue(newLq.Spec.ClusterQueue)
+	if cq != nil {
+		if cachedLq := cq.localQueues[queue.Key(newLq)]; cachedLq != nil {
+			cachedLq.Lock()
+			defer cachedLq.Unlock()
+			cachedLq.labels = newLq.GetLabels()
+			if features.Enabled(features.CustomMetricLabels) {
+				cachedLq.customMetricLabelValues = c.customLabels.ExtractValues(newLq.Labels, newLq.Annotations)
+			}
```

  Done

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T15:52:29Z

- **test/integration/singlecluster/controller/core/localqueue_controller_test.go:734** by [@mykysha](https://github.com/mykysha) — 2026-03-25T15:52:29Z
```diff
@@ -690,4 +690,47 @@ var _ = ginkgo.Describe("Queue controller metrics filtering", ginkgo.Label("cont
 			util.ExpectLQPendingWorkloadsMetric(queueDynamic, 1, 0)
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should delete local queue metrics after changing labels to not matching", func() {
+		ginkgo.By("Creating a localQueue with matching labels")
+		queueDeleteMetrics := utiltestingapi.MakeLocalQueue("queue-delete-metrics", ns.Name).
+			ClusterQueue(clusterQueue.Name).
+			Label("metrics-test", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, queueDeleteMetrics)
+
+		wlDeleteMetrics := utiltestingapi.MakeWorkload("wl-delete-metrics", ns.Name).
+			Queue(kueue.LocalQueueName(queueDeleteMetrics.Name)).
+			Request(resourceGPU, "1").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wlDeleteMetrics)
+
+		ginkgo.By("Waiting for workload to be reflected in status (Pending)")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			g.Expect(updated.Status.PendingWorkloads).Should(gomega.Equal(int32(1)))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are initially available for matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 1, 0)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Updating the localQueue to non-matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			if updated.Labels == nil {
+				updated.Labels = make(map[string]string)
+			}
+			updated.Labels["metrics-test"] = "false"
+			g.Expect(k8sClient.Update(ctx, &updated)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are cleared after labels are updated to non-matching")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 0, 0)
+		}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  I think that the regular timeout would be sufficient here

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-25T15:53:58Z

- **test/integration/singlecluster/controller/core/localqueue_controller_test.go:734** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-25T15:53:58Z
```diff
@@ -690,4 +690,47 @@ var _ = ginkgo.Describe("Queue controller metrics filtering", ginkgo.Label("cont
 			util.ExpectLQPendingWorkloadsMetric(queueDynamic, 1, 0)
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should delete local queue metrics after changing labels to not matching", func() {
+		ginkgo.By("Creating a localQueue with matching labels")
+		queueDeleteMetrics := utiltestingapi.MakeLocalQueue("queue-delete-metrics", ns.Name).
+			ClusterQueue(clusterQueue.Name).
+			Label("metrics-test", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, queueDeleteMetrics)
+
+		wlDeleteMetrics := utiltestingapi.MakeWorkload("wl-delete-metrics", ns.Name).
+			Queue(kueue.LocalQueueName(queueDeleteMetrics.Name)).
+			Request(resourceGPU, "1").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wlDeleteMetrics)
+
+		ginkgo.By("Waiting for workload to be reflected in status (Pending)")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			g.Expect(updated.Status.PendingWorkloads).Should(gomega.Equal(int32(1)))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are initially available for matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 1, 0)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Updating the localQueue to non-matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			if updated.Labels == nil {
+				updated.Labels = make(map[string]string)
+			}
+			updated.Labels["metrics-test"] = "false"
+			g.Expect(k8sClient.Update(ctx, &updated)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are cleared after labels are updated to non-matching")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 0, 0)
+		}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  Are you sure it's not enough 45s?

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-25T15:55:09Z

- **test/integration/singlecluster/controller/core/localqueue_controller_test.go:734** by [@mykysha](https://github.com/mykysha) — 2026-03-25T15:55:09Z
```diff
@@ -690,4 +690,47 @@ var _ = ginkgo.Describe("Queue controller metrics filtering", ginkgo.Label("cont
 			util.ExpectLQPendingWorkloadsMetric(queueDynamic, 1, 0)
 		}, util.Timeout, util.Interval).Should(gomega.Succeed())
 	})
+
+	ginkgo.It("Should delete local queue metrics after changing labels to not matching", func() {
+		ginkgo.By("Creating a localQueue with matching labels")
+		queueDeleteMetrics := utiltestingapi.MakeLocalQueue("queue-delete-metrics", ns.Name).
+			ClusterQueue(clusterQueue.Name).
+			Label("metrics-test", "true").
+			Obj()
+		util.MustCreate(ctx, k8sClient, queueDeleteMetrics)
+
+		wlDeleteMetrics := utiltestingapi.MakeWorkload("wl-delete-metrics", ns.Name).
+			Queue(kueue.LocalQueueName(queueDeleteMetrics.Name)).
+			Request(resourceGPU, "1").
+			Obj()
+		util.MustCreate(ctx, k8sClient, wlDeleteMetrics)
+
+		ginkgo.By("Waiting for workload to be reflected in status (Pending)")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			g.Expect(updated.Status.PendingWorkloads).Should(gomega.Equal(int32(1)))
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are initially available for matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 1, 0)
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Updating the localQueue to non-matching labels")
+		gomega.Eventually(func(g gomega.Gomega) {
+			var updated kueue.LocalQueue
+			g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queueDeleteMetrics), &updated)).To(gomega.Succeed())
+			if updated.Labels == nil {
+				updated.Labels = make(map[string]string)
+			}
+			updated.Labels["metrics-test"] = "false"
+			g.Expect(k8sClient.Update(ctx, &updated)).To(gomega.Succeed())
+		}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+		ginkgo.By("Verifying metrics are cleared after labels are updated to non-matching")
+		gomega.Eventually(func(g gomega.Gomega) {
+			util.ExpectLQPendingWorkloadsMetric(queueDeleteMetrics, 0, 0)
+		}, util.LongTimeout, util.Interval).Should(gomega.Succeed())
```

  I mean just the util.Timeout is good, no need for the Medium or Long one

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-25T16:12:47Z

- **pkg/cache/scheduler/cache.go:634** by [@mimowo](https://github.com/mimowo) — 2026-03-25T16:12:47Z
```diff
@@ -634,6 +625,25 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	return nil
 }
 
+func (c *Cache) updateLqMetricLabels(newLq *kueue.LocalQueue) {
+	c.RLock()
+	defer c.RUnlock()
+	cq := c.hm.ClusterQueue(newLq.Spec.ClusterQueue)
+	if cq == nil {
+		return
+	}
```

  The lock on c seems to have a longer lock than needed. I would rather have two helpers: getClusterQueueByName() which takes read lock for c, and getLocalQueueByName which takes the logkc on ClusterQueue. Then we perform the update under lock for cachedLq as currently

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-26T12:13:34Z

- **pkg/cache/scheduler/cache.go:634** by [@mykysha](https://github.com/mykysha) — 2026-03-26T12:13:34Z
```diff
@@ -634,6 +625,25 @@ func (c *Cache) UpdateLocalQueue(oldQ, newQ *kueue.LocalQueue) error {
 	return nil
 }
 
+func (c *Cache) updateLqMetricLabels(newLq *kueue.LocalQueue) {
+	c.RLock()
+	defer c.RUnlock()
+	cq := c.hm.ClusterQueue(newLq.Spec.ClusterQueue)
+	if cq == nil {
+		return
+	}
```

  We have `GetCacheLocalQueue` which seems to fit this usecase perfectly, have updated the PR to use it instead and then lock on the lq for the update

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T08:43:27Z

/lgtm
/approve
Thank you 👍
