# PR #10127: Add support for Kubeflow Trainer v2.2

**Summary**: Add support for Kubeflow Trainer v2.2

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10127

**Last updated**: 2026-03-31T07:35:51Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `9831-support-trainer-v2.2` → `main`
- **Created**: 2026-03-26T22:55:10Z
- **Updated**: 2026-03-31T07:35:51Z
- **Merged**: 2026-03-27T12:46:56Z
- **Closed**: 2026-03-27T12:46:56Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 124   **+20045 / -2239**

## Description

#### What type of PR is this?

/kind feature
/area integrations

#### What this PR does / why we need it:
This PR adds support for Kubeflow Trainer v2.2 which introduced some breaking changes due to API changes

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9831 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add support to Kubeflow Trainer v2.2
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-26T22:55:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6c6834c67f0173f6777971a9556c6c990e14b4d2 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c669a530976e0008fcc9e2 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T12:14:24Z

LGTM label has been added.  <details>Git tree hash: 432778ab1976db14e69ad2aae02ebd7ddf5194be</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T12:14:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10127#" title="Author self-approved">kaisoz</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10127#pullrequestreview-4020772855" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-31T07:35:48Z

/kind feature

## Reviews

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2026-03-26T22:57:18Z

- **pkg/controller/jobs/trainjob/trainjob_controller.go:90** by [@kaisoz](https://github.com/kaisoz) — 2026-03-26T22:57:18Z
```diff
@@ -77,25 +76,24 @@ func init() {
 // +kubebuilder:rbac:groups=trainer.kubeflow.org,resources=clustertrainingruntimes,verbs=get;list;watch
 
 type trainJobReconciler struct {
-	jr       *jobframework.JobReconciler
-	runtimes map[string]kftrainerruntime.Runtime
-	client   client.Client
+	jr     *jobframework.JobReconciler
+	client client.Client
 }
 
 const controllerName = "trainjob"
+const runtimePatchManagerName = "kueue.x-k8s.io/manager"
 
 var reconciler trainJobReconciler
 var _ jobframework.JobReconcilerInterface = (*trainJobReconciler)(nil)
 
 func NewReconciler(ctx context.Context, client client.Client, indexer client.FieldIndexer, eventRecorder record.EventRecorder, opts ...jobframework.Option) (jobframework.JobReconcilerInterface, error) {
-	runtimes, err := kftrainerruntimecore.New(ctx, client, indexer)
+	_, err := kftrainerruntimecore.New(ctx, client, indexer, nil)
```

  We don't need to keep the runtimes in the reconciler anymore, just initialise them. They'll be retrieved on-demand when needed (line 164)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T08:15:51Z

- **test/e2e/singlecluster/trainjob_test.go:196** by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:15:51Z
```diff
@@ -185,7 +186,17 @@ var _ = ginkgo.Describe("TrainJob", ginkgo.Label("area:singlecluster", "feature:
 			})
 
 			ginkgo.By("Verify the trainjob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[0].Spec.NodeSelector).To(gomega.Equal(
+				var kueuePatch *kftrainerapi.RuntimePatch
+				for i := range trainjob.Spec.RuntimePatches {
+					if trainjob.Spec.RuntimePatches[i].Manager == "kueue.x-k8s.io/manager" {
+						kueuePatch = &trainjob.Spec.RuntimePatches[i]
+						break
+					}
+				}
+				gomega.Expect(kueuePatch).ShouldNot(gomega.BeNil())
```

  This is not asserting on nodeSelector as the ginkgo.By claims, please move to dedicated STEP

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T08:16:39Z

- **test/e2e/singlecluster/tas_test.go:539** by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:16:39Z
```diff
@@ -518,8 +529,17 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+				var kueuePatch *kftrainerapi.RuntimePatch
+				for i := range trainjob.Spec.RuntimePatches {
+					if trainjob.Spec.RuntimePatches[i].Manager == "kueue.x-k8s.io/manager" {
+						kueuePatch = &trainjob.Spec.RuntimePatches[i]
+						break
+					}
+				}
+				gomega.Expect(kueuePatch).ShouldNot(gomega.BeNil())
```

  Identical code is present in trainjob_test.go, move as a helper to test/util/, and extract to a dedicated ginkgo step

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T08:19:09Z

- **test/e2e/singlecluster/tas_test.go:507** by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:19:09Z
```diff
@@ -493,13 +493,24 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				TrainerImage(util.GetAgnHostImage(), []string{"/agnhost"}, util.BehaviorExitFast).
 				TrainerRequest(corev1.ResourceCPU, "500m").
 				TrainerRequest(corev1.ResourceMemory, "200Mi").
-				PodTemplateOverrides([]kftrainerapi.PodTemplateOverride{
+				RuntimePatches([]kftrainerapi.RuntimePatch{
 					{
-						TargetJobs: []kftrainerapi.PodTemplateOverrideTargetJob{
-							{Name: "node"},
-						},
-						Metadata: &metav1.ObjectMeta{
-							Annotations: map[string]string{kueue.PodSetRequiredTopologyAnnotation: corev1.LabelHostname},
+						Manager: "test-e2e/tas",
+						TrainingRuntimeSpec: &kftrainerapi.TrainingRuntimeSpecPatch{
+							Template: &kftrainerapi.JobSetTemplatePatch{Spec: &kftrainerapi.JobSetSpecPatch{
+								ReplicatedJobs: []kftrainerapi.ReplicatedJobPatch{
+									{
+										Name: "node",
+										Template: &kftrainerapi.JobTemplatePatch{Spec: &kftrainerapi.JobSpecPatch{
+											Template: &kftrainerapi.PodTemplatePatch{
+												Metadata: &metav1.ObjectMeta{
+													Annotations: map[string]string{kueue.PodSetRequiredTopologyAnnotation: corev1.LabelHostname},
```

  I think with the new TrainJob API the annotations field is really hard to find for the users as it is very deeply nested.
  
  For now we have what we have, but maybe something to re-consider for a more user-friendly API in the new release.
  
  cc @tenzen-y @andreyvelich

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T08:21:46Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:96** by [@mimowo](https://github.com/mimowo) — 2026-03-27T08:21:46Z
```diff
@@ -79,15 +80,25 @@ func (w *TrainJobWebhook) Default(ctx context.Context, obj *kftrainerapi.TrainJo
 	if err != nil {
 		return err
 	}
+	runtimePatch := kftrainerapi.RuntimePatch{
+		Manager: runtimePatchManagerName,
+		TrainingRuntimeSpec: &kftrainerapi.TrainingRuntimeSpecPatch{
+			Template: &kftrainerapi.JobSetTemplatePatch{
+				Spec: &kftrainerapi.JobSetSpecPatch{},
+			},
+		},
+	}
 	if suspend {
 		trainJob.Suspend()
 		if trainJobQueueName := jobframework.QueueNameForObject(trainJob.Object()); trainJobQueueName != "" {
-			if trainJob.Spec.Labels == nil {
-				trainJob.Spec.Labels = make(map[string]string, 1)
+			runtimePatch.TrainingRuntimeSpec.Template.Metadata = &metav1.ObjectMeta{
+				Labels: map[string]string{
+					controllerconstants.QueueLabel: string(trainJobQueueName),
```

  Do you know already maybe why we do that? Asked the question also in the issue: https://github.com/kubernetes-sigs/kueue/issues/10099
  
  If you don't know we can follow up as the issue, I'm ok with that for now.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2026-03-27T09:59:05Z

- **pkg/controller/jobs/trainjob/trainjob_webhook.go:96** by [@kaisoz](https://github.com/kaisoz) — 2026-03-27T09:59:04Z
```diff
@@ -79,15 +80,25 @@ func (w *TrainJobWebhook) Default(ctx context.Context, obj *kftrainerapi.TrainJo
 	if err != nil {
 		return err
 	}
+	runtimePatch := kftrainerapi.RuntimePatch{
+		Manager: runtimePatchManagerName,
+		TrainingRuntimeSpec: &kftrainerapi.TrainingRuntimeSpecPatch{
+			Template: &kftrainerapi.JobSetTemplatePatch{
+				Spec: &kftrainerapi.JobSetSpecPatch{},
+			},
+		},
+	}
 	if suspend {
 		trainJob.Suspend()
 		if trainJobQueueName := jobframework.QueueNameForObject(trainJob.Object()); trainJobQueueName != "" {
-			if trainJob.Spec.Labels == nil {
-				trainJob.Spec.Labels = make(map[string]string, 1)
+			runtimePatch.TrainingRuntimeSpec.Template.Metadata = &metav1.ObjectMeta{
+				Labels: map[string]string{
+					controllerconstants.QueueLabel: string(trainJobQueueName),
```

  My intention is indeed to investigate this in #10099  😊

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T11:39:13Z

- **test/e2e/singlecluster/tas_test.go:538** by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:39:13Z
```diff
@@ -517,9 +528,21 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+			var kueueRuntimePatch *kftrainerapi.RuntimePatch
+			ginkgo.By("Verify that the trainjob has a runtime patch from kueue", func() {
+				for _, p := range trainjob.Spec.RuntimePatches {
+					if p.Manager == "kueue.x-k8s.io/manager" {
+						kueueRuntimePatch = &p
+						break
+					}
+				}
```

  Extract the code responsible for extracting the runtimePatch to a helper in test/util

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T11:39:22Z

- **test/e2e/singlecluster/tas_test.go:538** by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:39:22Z
```diff
@@ -517,9 +528,21 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+			var kueueRuntimePatch *kftrainerapi.RuntimePatch
+			ginkgo.By("Verify that the trainjob has a runtime patch from kueue", func() {
+				for _, p := range trainjob.Spec.RuntimePatches {
+					if p.Manager == "kueue.x-k8s.io/manager" {
+						kueueRuntimePatch = &p
+						break
+					}
+				}
```

  and reuse in all places

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T11:39:57Z

- **test/e2e/singlecluster/tas_test.go:539** by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:39:57Z
```diff
@@ -518,8 +529,17 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+				var kueuePatch *kftrainerapi.RuntimePatch
+				for i := range trainjob.Spec.RuntimePatches {
+					if trainjob.Spec.RuntimePatches[i].Manager == "kueue.x-k8s.io/manager" {
+						kueuePatch = &trainjob.Spec.RuntimePatches[i]
+						break
+					}
+				}
+				gomega.Expect(kueuePatch).ShouldNot(gomega.BeNil())
```

  I think this is resolved but not done, left another comment

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T12:14:17Z

/lgtm
/approve
I think it is better if we cherrypick because most users will use Kueue with Trainer v2.2 anyway. 

Could you prepare cherrypicks @kaisoz ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T12:16:35Z

- **test/e2e/singlecluster/tas_test.go:538** by [@mimowo](https://github.com/mimowo) — 2026-03-27T12:16:35Z
```diff
@@ -517,9 +528,21 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+			var kueueRuntimePatch *kftrainerapi.RuntimePatch
+			ginkgo.By("Verify that the trainjob has a runtime patch from kueue", func() {
+				for _, p := range trainjob.Spec.RuntimePatches {
+					if p.Manager == "kueue.x-k8s.io/manager" {
+						kueueRuntimePatch = &p
+						break
+					}
+				}
```

  I synced with @kaisoz and apparently it is not trivial due to some slight differences. I will investigate the differences and open a follow up cleanup issue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T12:32:43Z

- **test/e2e/singlecluster/tas_test.go:538** by [@mimowo](https://github.com/mimowo) — 2026-03-27T12:32:43Z
```diff
@@ -517,9 +528,21 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("verify the TrainJob has nodeSelector set", func() {
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides).To(gomega.HaveLen(2))
-				gomega.Expect(trainjob.Spec.PodTemplateOverrides[1].Spec.NodeSelector).To(gomega.Equal(
+			var kueueRuntimePatch *kftrainerapi.RuntimePatch
+			ginkgo.By("Verify that the trainjob has a runtime patch from kueue", func() {
+				for _, p := range trainjob.Spec.RuntimePatches {
+					if p.Manager == "kueue.x-k8s.io/manager" {
+						kueueRuntimePatch = &p
+						break
+					}
+				}
```

  Opened https://github.com/kubernetes-sigs/kueue/issues/10147 to clean it up

### Review by [@andreyvelich](https://github.com/andreyvelich) (commented) — 2026-03-27T20:30:22Z

- **test/e2e/singlecluster/tas_test.go:507** by [@andreyvelich](https://github.com/andreyvelich) — 2026-03-27T20:30:07Z
```diff
@@ -493,13 +493,24 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 				TrainerImage(util.GetAgnHostImage(), []string{"/agnhost"}, util.BehaviorExitFast).
 				TrainerRequest(corev1.ResourceCPU, "500m").
 				TrainerRequest(corev1.ResourceMemory, "200Mi").
-				PodTemplateOverrides([]kftrainerapi.PodTemplateOverride{
+				RuntimePatches([]kftrainerapi.RuntimePatch{
 					{
-						TargetJobs: []kftrainerapi.PodTemplateOverrideTargetJob{
-							{Name: "node"},
-						},
-						Metadata: &metav1.ObjectMeta{
-							Annotations: map[string]string{kueue.PodSetRequiredTopologyAnnotation: corev1.LabelHostname},
+						Manager: "test-e2e/tas",
+						TrainingRuntimeSpec: &kftrainerapi.TrainingRuntimeSpecPatch{
+							Template: &kftrainerapi.JobSetTemplatePatch{Spec: &kftrainerapi.JobSetSpecPatch{
+								ReplicatedJobs: []kftrainerapi.ReplicatedJobPatch{
+									{
+										Name: "node",
+										Template: &kftrainerapi.JobTemplatePatch{Spec: &kftrainerapi.JobSpecPatch{
+											Template: &kftrainerapi.PodTemplatePatch{
+												Metadata: &metav1.ObjectMeta{
+													Annotations: map[string]string{kueue.PodSetRequiredTopologyAnnotation: corev1.LabelHostname},
```

  This was by design as we discussed here: https://github.com/kubeflow/trainer/pull/3199
  Previously, it was unclear to which underlying resource (JobSet, Job, Pod) the annotation is applied.
  cc @astefanutti
