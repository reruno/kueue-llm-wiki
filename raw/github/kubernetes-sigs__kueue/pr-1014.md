# PR #1014: check k8s version for synchronization of completions and parallelism

**Summary**: check k8s version for synchronization of completions and parallelism

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1014

**Last updated**: 2024-03-12T08:59:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@stuton](https://github.com/stuton)
- **Branch**: `partial-admission-k8s-version` → `main`
- **Created**: 2023-07-24T11:04:49Z
- **Updated**: 2024-03-12T08:59:54Z
- **Merged**: 2023-08-03T16:58:21Z
- **Closed**: 2023-08-03T16:58:21Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@stuton](https://github.com/stuton)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 7   **+251 / -19**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Check k8s version and change condition for batch/job.completions with parallelism in case of partial admission

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #970

#### Special notes for your reviewer:
This is the second step fixing #970.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-07-24T11:04:56Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c56738571b95728d4ecc237d533b284c1c752d68 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64cbc8c5f7dea300081d7eaa |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-07-24T11:04:58Z

Hi @stuton. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@stuton](https://github.com/stuton) — 2023-07-24T11:07:42Z

/assign

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T11:14:10Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T13:56:53Z

/retest
#1021

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-03T16:57:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1014#pullrequestreview-1561513917" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1014#" title="Author self-approved">stuton</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-07-24T11:26:41Z

- **main.go:29** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T11:14:49Z
```diff
@@ -25,6 +25,8 @@ import (
 
 	// Import all Kubernetes client auth plugins (e.g. Azure, GCP, OIDC, etc.)
 	// to ensure that exec-entrypoint and run can make use of them.
+
+	"k8s.io/client-go/discovery"
```

  Should we move this to L25?

- **pkg/util/discoveryversion/discoveryversion.go:19** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T11:20:06Z
```diff
@@ -0,0 +1,69 @@
+package discoveryversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/go-logr/logr"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+)
+
+var (
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
```

  ```suggestion
  var (
  	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
  
  	serverVersion = &versionutil.Version{}
  	rwm           = &sync.RWMutex{}
  )
  ```

- **main.go:162** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T11:22:16Z
```diff
@@ -150,6 +153,14 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to discovery client")
+		os.Exit(1)
+	}
+
+	go discoveryversion.FetchServerVersion(ctx, discoveryClient)
```

  Should we register this to manager like scheduler?

- **pkg/controller/jobs/job/job_controller.go:224** by [@tenzen-y](https://github.com/tenzen-y) — 2023-07-24T11:25:17Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  Should we check the cluster version inside `syncCompletionWithparallelism`?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-24T11:49:51Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@stuton](https://github.com/stuton) — 2023-07-24T11:49:51Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  It breaks the condition in webhook [here](https://github.com/kubernetes-sigs/kueue/blob/b2e8c9d0632c25c75b3ee8dfeecdce2bb6037464/pkg/controller/jobs/job/job_webhook.go#L165)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-24T18:07:00Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@mimowo](https://github.com/mimowo) — 2023-07-24T15:54:34Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  +1 I like the idea of checking the version inside this function as its name suggests it is the gate keeper for the sync. Also it would allow not to repeat the check on restore. We could have another function to check the value of the annotation, such as `syncCompletionWithParallelismValue`. 
  
  nit: while the condition is duplicated, the comment isn't, wrapping the condition under the function would give a natural place to put the comment.

- **pkg/controller/jobs/job/job_webhook.go:165** by [@mimowo](https://github.com/mimowo) — 2023-07-24T16:01:22Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && discoveryversion.LessThan(discoveryversion.KubeVersion1_27) {
```

  I'm wondering if we need the version check here. Before this commit we didn't allow the annotation value change on unsuspend. It seems we could just check `syncCompletionWithParallelismValue` to check if the value of the annotation changes. IIUC, a user could now set the annotation at the job creation to be true. No need to update the annotation.

- **pkg/util/discoveryversion/discoveryversion.go:46** by [@mimowo](https://github.com/mimowo) — 2023-07-24T16:19:01Z
```diff
@@ -0,0 +1,67 @@
+package discoveryversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/go-logr/logr"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
+
+// FetchServerVersion gets API server version
+func FetchServerVersion(ctx context.Context, dc *discovery.DiscoveryClient) {
+	log := ctrl.LoggerFrom(ctx)
+
+	SetServerVersion(parseServerVersion(dc, log))
+
+	ticker := time.NewTicker(10 * time.Second)
+	defer ticker.Stop()
+
+	for {
+		select {
+		case <-ticker.C:
+			SetServerVersion(parseServerVersion(dc, log))
+			log.V(5).Info("Fetch kube version", "server_version", serverVersion)
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching kube version")
+			return
+		}
+	}
+}
+
+func parseServerVersion(dc *discovery.DiscoveryClient, log logr.Logger) *versionutil.Version {
+	clusterVersionInfo, err := dc.ServerVersion()
+	if err != nil {
+		log.Error(err, "couldn't fetch cluster version from the API Server")
+		return nil
```

  IIUC this means that in case of an error we leave the version nil. Doesn't the code then break with nil panic when `AtLeast` is called? Errors could happen due to transient connectivity errors, so we should be resilient to that. First, I think if there is an error fetching the new version we shouldn't update the cached version. 
  
  Still, the question remains what to do on the startup check.

- **pkg/controller/jobs/job/job_controller.go:224** by [@mimowo](https://github.com/mimowo) — 2023-07-24T17:20:06Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  Could you add some unit or integration tests for it? Seeing that https://github.com/kubernetes-sigs/kueue/pull/971/ didn't add unit tests, maybe this should be a preparatory or follow up PR?

- **pkg/util/discoveryversion/discoveryversion.go:22** by [@mimowo](https://github.com/mimowo) — 2023-07-24T17:58:33Z
```diff
@@ -0,0 +1,67 @@
+package discoveryversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	"github.com/go-logr/logr"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
+
+// FetchServerVersion gets API server version
+func FetchServerVersion(ctx context.Context, dc *discovery.DiscoveryClient) {
```

  It seems this code isn't covered with tests. It would be nice to have some unit tests at least for this method.

- **main.go:158** by [@mimowo](https://github.com/mimowo) — 2023-07-24T18:02:05Z
```diff
@@ -150,6 +153,14 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to discovery client")
```

  ```suggestion
  		setupLog.Error(err, "Unable to create the discovery client")
  ```

- **main.go:162** by [@mimowo](https://github.com/mimowo) — 2023-07-24T18:02:51Z
```diff
@@ -150,6 +153,14 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to discovery client")
+		os.Exit(1)
+	}
+
+	go discoveryversion.FetchServerVersion(ctx, discoveryClient)
```

  Yeah, it would be nice to have a generic interface such as `KubeVersionFetcher`. It could be then initialized as a caching fetcher in prod, but as a fetcher returning constant in tests. I'm not sure this is required at this point though. wdyt @alculquicondor ?

- **pkg/util/discoveryversion/discoveryversion.go:1** by [@mimowo](https://github.com/mimowo) — 2023-07-24T18:04:25Z
```diff
@@ -0,0 +1,67 @@
+package discoveryversion
```

  Maybe just, `kubeversion`?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-25T07:52:05Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@stuton](https://github.com/stuton) — 2023-07-25T07:52:05Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  There were unit tests that cover `j.syncCompletionWithParallelism` condition. But I added two tests that cover also [checking version](https://github.com/kubernetes-sigs/kueue/pull/1014/files#diff-ef20055bb8e826f00ebf7507a5576c87cc8a42a39f0a08d003cab3667c9410bcR337).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-25T08:23:30Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@mimowo](https://github.com/mimowo) — 2023-07-25T08:23:29Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  These tests are only covering the webhook. The line referenced in the comment does not seem covered.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-25T08:57:52Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@stuton](https://github.com/stuton) — 2023-07-25T08:57:52Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  Sorry, this is my misunderstanding. Sure, I will add tests for job controller

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-26T19:06:31Z

- **main.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T18:51:31Z
```diff
@@ -22,6 +22,9 @@ import (
 	"fmt"
 	"os"
 	"strings"
+	"time"
+
+	"k8s.io/client-go/discovery"
```

  move to the same block as `zaplog` below.

- **pkg/controller/jobs/job/job_controller.go:307** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T18:56:09Z
```diff
@@ -301,6 +302,11 @@ func (j *Job) syncCompletionWithParallelism() bool {
 	return false
 }
 
+// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+func (j *Job) syncCompletionWithParallelism() bool {
+	return j.syncCompletionWithParallelismValue() && kubeversion.AtLeast(kubeversion.KubeVersion1_27)
```

  We should be checking this in the webhook instead. If the version is below 1.27, we should reject jobs that try to use the annotation.

- **pkg/util/kubeversion/kubeversion.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T18:57:16Z
```diff
@@ -0,0 +1,72 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
+
+type ServerVersionFetcher struct {
+	dc     discovery.DiscoveryInterface
+	ticker *time.Ticker
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface, ticker *time.Ticker) *ServerVersionFetcher {
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: ticker,
+	}
+}
+
+// FetchServerVersionPeriodic gets API server version at intervals
+func (s *ServerVersionFetcher) FetchServerVersionPeriodic(ctx context.Context) {
+	log := ctrl.LoggerFrom(ctx)
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
+			log.V(5).Info("Fetch kube version", "server_version", serverVersion)
```

  ```suggestion
  			log.V(5).Info("Fetch kube version", "serverVersion", serverVersion)
  ```

- **pkg/util/kubeversion/kubeversion_test.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T18:59:32Z
```diff
@@ -0,0 +1,80 @@
+package kubeversion
+
+import (
+	"errors"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/apimachinery/pkg/version"
+
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclientset "k8s.io/client-go/kubernetes/fake"
+	kubetesting "k8s.io/client-go/testing"
+)
+
+// FetchServerVersion gets API server version
+func TestFetchServerVersion(t *testing.T) {
+	cases := map[string]struct {
+		ticker    *time.Ticker
+		version   string
+		wantEqual *versionutil.Version
+	}{
+		"server version equals expected version": {
```

  No need for a test table when there is just one case.

- **pkg/util/kubeversion/kubeversion_test.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T19:00:25Z
```diff
@@ -0,0 +1,80 @@
+package kubeversion
+
+import (
+	"errors"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/apimachinery/pkg/version"
+
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclientset "k8s.io/client-go/kubernetes/fake"
+	kubetesting "k8s.io/client-go/testing"
+)
+
+// FetchServerVersion gets API server version
+func TestFetchServerVersion(t *testing.T) {
+	cases := map[string]struct {
+		ticker    *time.Ticker
+		version   string
+		wantEqual *versionutil.Version
+	}{
+		"server version equals expected version": {
+			version:   "v1.0.0",
+			wantEqual: versionutil.MustParseGeneric("v1.0.0"),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			fakeClient := fakeclientset.NewSimpleClientset()
+			fakeDiscovery, ok := fakeClient.Discovery().(*fakediscovery.FakeDiscovery)
+			if !ok {
+				t.Fatalf("couldn't convert Discovery() to *FakeDiscovery")
+			}
+			fakeDiscovery.FakedServerVersion = &version.Info{
+				GitVersion: tc.version,
+			}
+			_ = NewServerVersionFetcher(fakeDiscovery, tc.ticker).FetchServerVersion()
+			if serverVersion.String() != tc.wantEqual.String() {
+				t.Errorf("Unexpected result, want %v", tc.wantEqual)
+			}
+		})
+	}
+}
+
+func TestFetchServerVersionWithError(t *testing.T) {
```

  This test looks like an overkill. I don't expect this to fail.

- **main.go:170** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-26T19:03:24Z
```diff
@@ -150,6 +154,21 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to create the discovery client")
+		os.Exit(1)
+	}
+
+	serverVersionFetcher := kubeversion.NewServerVersionFetcher(discoveryClient, time.NewTicker(time.Second*10))
+
+	if err := serverVersionFetcher.FetchServerVersion(); err != nil {
+		setupLog.Error(err, "Unable to fetch server version")
+		os.Exit(1)
+	}
+
+	go serverVersionFetcher.FetchServerVersionPeriodic(ctx)
```

  Instead of creating a go routine here, you can let the manager run it for you.
  
  You can make the ServerVersionFetcher implement `Start`, like we did for the scheduler https://github.com/kubernetes-sigs/kueue/pull/596
  
  You would do `Start` there.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-27T09:48:28Z

- **pkg/util/kubeversion/kubeversion_test.go:37** by [@mimowo](https://github.com/mimowo) — 2023-07-27T09:48:28Z
```diff
@@ -0,0 +1,80 @@
+package kubeversion
+
+import (
+	"errors"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/apimachinery/pkg/version"
+
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclientset "k8s.io/client-go/kubernetes/fake"
+	kubetesting "k8s.io/client-go/testing"
+)
+
+// FetchServerVersion gets API server version
+func TestFetchServerVersion(t *testing.T) {
+	cases := map[string]struct {
+		ticker    *time.Ticker
+		version   string
+		wantEqual *versionutil.Version
+	}{
+		"server version equals expected version": {
+			version:   "v1.0.0",
+			wantEqual: versionutil.MustParseGeneric("v1.0.0"),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			fakeClient := fakeclientset.NewSimpleClientset()
+			fakeDiscovery, ok := fakeClient.Discovery().(*fakediscovery.FakeDiscovery)
+			if !ok {
+				t.Fatalf("couldn't convert Discovery() to *FakeDiscovery")
+			}
+			fakeDiscovery.FakedServerVersion = &version.Info{
+				GitVersion: tc.version,
+			}
+			_ = NewServerVersionFetcher(fakeDiscovery, tc.ticker).FetchServerVersion()
+			if serverVersion.String() != tc.wantEqual.String() {
+				t.Errorf("Unexpected result, want %v", tc.wantEqual)
+			}
+		})
+	}
+}
+
+func TestFetchServerVersionWithError(t *testing.T) {
```

  I think the test is added as I pointed out in the initial version, there would be nil version set, and Kueue would crash on panic checking `AtLeast` on nil. While unlikely, the version check may fail in case of transient connectivity issues. Also, the API server under heavy load may respond with 429 (too many requests). The test execution time is negligible, so I think it makes sense to keep it, but no strong opinion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-27T10:27:42Z

- **main.go:163** by [@mimowo](https://github.com/mimowo) — 2023-07-27T10:24:24Z
```diff
@@ -150,6 +154,21 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to create the discovery client")
+		os.Exit(1)
+	}
+
+	serverVersionFetcher := kubeversion.NewServerVersionFetcher(discoveryClient, time.NewTicker(time.Second*10))
```

  Make the time interval a constant, maybe `kubeVersionFetchInterval`

- **main.go:170** by [@mimowo](https://github.com/mimowo) — 2023-07-27T10:27:26Z
```diff
@@ -150,6 +154,21 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to create the discovery client")
+		os.Exit(1)
+	}
+
+	serverVersionFetcher := kubeversion.NewServerVersionFetcher(discoveryClient, time.NewTicker(time.Second*10))
+
+	if err := serverVersionFetcher.FetchServerVersion(); err != nil {
+		setupLog.Error(err, "Unable to fetch server version")
+		os.Exit(1)
+	}
+
+	go serverVersionFetcher.FetchServerVersionPeriodic(ctx)
```

  +1

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-27T12:11:38Z

- **main.go:170** by [@stuton](https://github.com/stuton) — 2023-07-27T12:11:38Z
```diff
@@ -150,6 +154,21 @@ func main() {
 	ctx := ctrl.SetupSignalHandler()
 	setupIndexes(ctx, mgr, &cfg)
 
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to create the discovery client")
+		os.Exit(1)
+	}
+
+	serverVersionFetcher := kubeversion.NewServerVersionFetcher(discoveryClient, time.NewTicker(time.Second*10))
+
+	if err := serverVersionFetcher.FetchServerVersion(); err != nil {
+		setupLog.Error(err, "Unable to fetch server version")
+		os.Exit(1)
+	}
+
+	go serverVersionFetcher.FetchServerVersionPeriodic(ctx)
```

  Done

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-27T12:17:33Z

- **pkg/controller/jobs/job/job_controller.go:224** by [@stuton](https://github.com/stuton) — 2023-07-27T12:17:33Z
```diff
@@ -219,7 +220,8 @@ func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
 
 	if j.minPodsCount() != nil {
 		j.Spec.Parallelism = pointer.Int32(info.Count)
-		if j.syncCompletionWithParallelism() {
+		// only since kubernetes version 1.27 we can change parallelism and completions in tandem
+		if j.syncCompletionWithParallelism() && discoveryversion.AtLeast(discoveryversion.KubeVersion1_27) {
```

  I think we can add some unit tests then, when we merge #979

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-07-27T12:20:59Z

- **pkg/util/kubeversion/kubeversion_test.go:37** by [@stuton](https://github.com/stuton) — 2023-07-27T12:20:59Z
```diff
@@ -0,0 +1,80 @@
+package kubeversion
+
+import (
+	"errors"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/apimachinery/pkg/version"
+
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclientset "k8s.io/client-go/kubernetes/fake"
+	kubetesting "k8s.io/client-go/testing"
+)
+
+// FetchServerVersion gets API server version
+func TestFetchServerVersion(t *testing.T) {
+	cases := map[string]struct {
+		ticker    *time.Ticker
+		version   string
+		wantEqual *versionutil.Version
+	}{
+		"server version equals expected version": {
+			version:   "v1.0.0",
+			wantEqual: versionutil.MustParseGeneric("v1.0.0"),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			fakeClient := fakeclientset.NewSimpleClientset()
+			fakeDiscovery, ok := fakeClient.Discovery().(*fakediscovery.FakeDiscovery)
+			if !ok {
+				t.Fatalf("couldn't convert Discovery() to *FakeDiscovery")
+			}
+			fakeDiscovery.FakedServerVersion = &version.Info{
+				GitVersion: tc.version,
+			}
+			_ = NewServerVersionFetcher(fakeDiscovery, tc.ticker).FetchServerVersion()
+			if serverVersion.String() != tc.wantEqual.String() {
+				t.Errorf("Unexpected result, want %v", tc.wantEqual)
+			}
+		})
+	}
+}
+
+func TestFetchServerVersionWithError(t *testing.T) {
```

  I agree, the test was added for that purpose, but anyway if it seems overkill I can delete it

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-27T15:04:34Z

- **main.go:288** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T14:54:39Z
```diff
@@ -271,6 +276,26 @@ func setupScheduler(mgr ctrl.Manager, cCache *cache.Cache, queues *queue.Manager
 	}
 }
 
+func setupServerVersionFetcher(mgr ctrl.Manager, kubeConfig *rest.Config) {
+	discoveryClient, err := discovery.NewDiscoveryClientForConfig(kubeConfig)
+	if err != nil {
+		setupLog.Error(err, "Unable to create the discovery client")
+		os.Exit(1)
+	}
+
+	serverVersionFetcher := kubeversion.NewServerVersionFetcher(discoveryClient)
+
+	if err := serverVersionFetcher.FetchServerVersion(); err != nil {
```

  what if this is part of `Start`? would returning an error in that function cause the binary to terminate?

- **pkg/controller/jobs/job/job_webhook.go:165** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T14:55:39Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && kubeversion.LessThan(kubeversion.KubeVersion1_27) {
```

  Just check this on `validateCreate` (note that this is called in `validateUpdate` too).

- **pkg/util/kubeversion/kubeversion_test.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T15:01:51Z
```diff
@@ -0,0 +1,80 @@
+package kubeversion
+
+import (
+	"errors"
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+
+	"k8s.io/apimachinery/pkg/runtime"
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/apimachinery/pkg/version"
+
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclientset "k8s.io/client-go/kubernetes/fake"
+	kubetesting "k8s.io/client-go/testing"
+)
+
+// FetchServerVersion gets API server version
+func TestFetchServerVersion(t *testing.T) {
+	cases := map[string]struct {
+		ticker    *time.Ticker
+		version   string
+		wantEqual *versionutil.Version
+	}{
+		"server version equals expected version": {
+			version:   "v1.0.0",
+			wantEqual: versionutil.MustParseGeneric("v1.0.0"),
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			fakeClient := fakeclientset.NewSimpleClientset()
+			fakeDiscovery, ok := fakeClient.Discovery().(*fakediscovery.FakeDiscovery)
+			if !ok {
+				t.Fatalf("couldn't convert Discovery() to *FakeDiscovery")
+			}
+			fakeDiscovery.FakedServerVersion = &version.Info{
+				GitVersion: tc.version,
+			}
+			_ = NewServerVersionFetcher(fakeDiscovery, tc.ticker).FetchServerVersion()
+			if serverVersion.String() != tc.wantEqual.String() {
+				t.Errorf("Unexpected result, want %v", tc.wantEqual)
+			}
+		})
+	}
+}
+
+func TestFetchServerVersionWithError(t *testing.T) {
```

  I see, thanks for the context.

- **pkg/util/kubeversion/kubeversion.go:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-27T15:04:29Z
```diff
@@ -0,0 +1,75 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const fetchServerVersionInterval = time.Second * 10
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
+
+type ServerVersionFetcher struct {
+	dc     discovery.DiscoveryInterface
+	ticker *time.Ticker
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface) *ServerVersionFetcher {
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: time.NewTicker(fetchServerVersionInterval),
+	}
+}
+
+// Start implements the Runnable interface to run ServerVersionFetcher as a controller.
+func (s *ServerVersionFetcher) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("serverVersionFetcher")
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching server version")
+			return nil
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
+			log.V(5).Info("Fetch server version", "serverVersion", serverVersion)
+		}
+	}
+}
+
+// FetchServerVersion gets API server version
+func (s *ServerVersionFetcher) FetchServerVersion() error {
+	clusterVersionInfo, err := s.dc.ServerVersion()
+	if err != nil {
+		return err
+	}
+	SetServerVersion(versionutil.MustParseSemantic(clusterVersionInfo.String()))
+	return nil
+}
+
+func SetServerVersion(v *versionutil.Version) {
+	rwm.Lock()
+	defer rwm.Unlock()
+	serverVersion = v
+}
+
+func LessThan(v *versionutil.Version) bool {
```

  It doesn't look like we are setting a fetcher in integration tests.
  However, the webhook would still call this function. How does it work?
  Is the assumption that empty (`0.0.0`) is LessThan `1.27.0`?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-07-31T06:54:55Z

- **pkg/controller/jobs/job/job_webhook.go:165** by [@trasc](https://github.com/trasc) — 2023-07-31T06:46:48Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && kubeversion.LessThan(kubeversion.KubeVersion1_27) {
```

  If so, the `syncCompletionAnnotationsPath` annotation should be made  immutable.
  
  Also this looks like it should be supported only for indexed jobs,
  https://github.com/kubernetes/kubernetes/blob/05b64c6b5e11128f7aa720473a60dfd61946cf6a/pkg/apis/batch/validation/validation.go#L570-L597
  
  Additionally maybe we should document the feature gate this is guarded in k/k
  
  https://github.com/kubernetes/kubernetes/blob/41da26dbe15207cbe5b6c36b48a31d2cd3344123/pkg/registry/batch/job/strategy.go#L184.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-07-31T08:45:54Z

- **pkg/controller/jobs/job/job_webhook.go:165** by [@mimowo](https://github.com/mimowo) — 2023-07-31T08:45:54Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && kubeversion.LessThan(kubeversion.KubeVersion1_27) {
```

  > If so, the `syncCompletionAnnotationsPath` annotation should be made immutable.
  
  It might not be a very important scenario, but after an upgrade to 1.27, a user may want to add the annotation. In that case verifying in `validateCreate` should be enough, letting the annotation be mutable as long as it conforms to the conditions.
  
  > Also this looks like it should be supported only for indexed jobs,
  
  Indeed, for non-indexed jobs `completions` is immutable, so IIUC we should not let setting the annotation `=true` for indexed jobs.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-07-31T13:14:39Z

- **pkg/controller/jobs/job/job_webhook.go:165** by [@alculquicondor](https://github.com/alculquicondor) — 2023-07-31T13:14:39Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && kubeversion.LessThan(kubeversion.KubeVersion1_27) {
```

  For simplicity, I agree that it's better to just make the annotation immutable.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-01T09:38:18Z

- **pkg/controller/jobs/job/job_webhook.go:165** by [@mimowo](https://github.com/mimowo) — 2023-08-01T09:38:18Z
```diff
@@ -161,8 +162,7 @@ func validatePartialAdmissionUpdate(oldJob, newJob *Job) field.ErrorList {
 			allErrs = append(allErrs, field.Forbidden(field.NewPath("spec", "parallelism"), "cannot change when partial admission is enabled and the job is not suspended"))
 		}
 	}
-
-	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() {
+	if oldJob.IsSuspended() == newJob.IsSuspended() && !newJob.IsSuspended() && oldJob.syncCompletionWithParallelism() != newJob.syncCompletionWithParallelism() && kubeversion.LessThan(kubeversion.KubeVersion1_27) {
```

  sgtm, although IIUC this will require an extra check, but it might be conceptually simpler indeed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-01T17:05:28Z

Can you test manually that this works as intended in a live cluster?

- **pkg/controller/jobframework/reconciler.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T16:57:18Z
```diff
@@ -82,6 +84,12 @@ func WithWaitForPodsReady(f bool) Option {
 	}
 }
 
+func WithKubeServerVersion(v *kubeversion.ServerVersionFetcher) Option {
```

  I don't see any usages of this function.

- **pkg/controller/jobs/job/job_webhook.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T16:58:44Z
```diff
@@ -129,6 +132,11 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
 				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobCompletionsEqualParallelismAnnotation], "should not be enabled for NonIndexed jobs"))
 			}
+			if w.kubeServerVersion != nil {
+				if version := w.kubeServerVersion.GetServerVersion(); version != nil && version.LessThan(kubeversion.KubeVersion1_27) {
+					allErrs = append(allErrs, field.Forbidden(syncCompletionAnnotationsPath, apivalidation.FieldImmutableErrorMsg))
```

  `FieldImmutableErrorMsg` doesn't seem right.

- **pkg/controller/jobs/job/job_webhook_test.go:228** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:01:37Z
```diff
@@ -202,7 +220,15 @@ func TestValidateCreate(t *testing.T) {
 
 	for _, tc := range testcases {
 		t.Run(tc.name, func(t *testing.T) {
-			gotErr := validateCreate((*Job)(tc.job))
+			jw := &JobWebhook{}
+			if tc.serverVersion != "" {
+				fakeDiscoveryClient, _ := fakeclient.NewSimpleClientset().Discovery().(*fakediscovery.FakeDiscovery)
+				fakeDiscoveryClient.FakedServerVersion = &version.Info{GitVersion: tc.serverVersion}
+				jw.kubeServerVersion = kubeversion.NewServerVersionFetcher(fakeDiscoveryClient)
+				_ = jw.kubeServerVersion.FetchServerVersion()
```

  call `Fatal` if there is an error.

- **pkg/util/kubeversion/kubeversion.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:03:29Z
```diff
@@ -0,0 +1,92 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const fetchServerVersionInterval = time.Minute * 10
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+)
+
+type ServerVersionFetcher struct {
+	dc            discovery.DiscoveryInterface
+	ticker        *time.Ticker
+	serverVersion *versionutil.Version
+	mu            sync.Mutex
+}
+
+type Options struct {
+	Interval time.Duration
+}
+
+type Option func(*Options)
+
+var DefaultOptions = Options{
+	Interval: fetchServerVersionInterval,
+}
+
+func WithInterval(interval time.Duration) Option {
+	return func(o *Options) {
+		o.Interval = interval
+	}
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface, opts ...Option) *ServerVersionFetcher {
+	options := DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: time.NewTicker(options.Interval),
+	}
+}
+
+// Start implements the Runnable interface to run ServerVersionFetcher as a controller.
+func (s *ServerVersionFetcher) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("serverVersionFetcher")
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	if err := s.FetchServerVersion(); err != nil {
+		log.Error(err, "Unable to fetch server version")
+	}
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching server version")
+			return nil
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
```

  else?

- **pkg/util/kubeversion/kubeversion.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T17:04:10Z
```diff
@@ -0,0 +1,92 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const fetchServerVersionInterval = time.Minute * 10
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+)
+
+type ServerVersionFetcher struct {
+	dc            discovery.DiscoveryInterface
+	ticker        *time.Ticker
+	serverVersion *versionutil.Version
+	mu            sync.Mutex
+}
+
+type Options struct {
+	Interval time.Duration
+}
+
+type Option func(*Options)
+
+var DefaultOptions = Options{
+	Interval: fetchServerVersionInterval,
+}
+
+func WithInterval(interval time.Duration) Option {
+	return func(o *Options) {
+		o.Interval = interval
+	}
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface, opts ...Option) *ServerVersionFetcher {
+	options := DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: time.NewTicker(options.Interval),
+	}
+}
+
+// Start implements the Runnable interface to run ServerVersionFetcher as a controller.
+func (s *ServerVersionFetcher) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("serverVersionFetcher")
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	if err := s.FetchServerVersion(); err != nil {
+		log.Error(err, "Unable to fetch server version")
+	}
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching server version")
+			return nil
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
+			log.V(5).Info("Fetch server version", "serverVersion", s.serverVersion)
+		}
+	}
+}
+
+// FetchServerVersion gets API server version
+func (s *ServerVersionFetcher) FetchServerVersion() error {
+	clusterVersionInfo, err := s.dc.ServerVersion()
+	if err != nil {
+		return err
+	}
+	s.mu.Lock()
+	defer s.mu.Unlock()
+	s.serverVersion = versionutil.MustParseSemantic(clusterVersionInfo.String())
+	return nil
+}
+
+func (s *ServerVersionFetcher) GetServerVersion() *versionutil.Version {
```

  Why remove the read lock?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-02T12:40:12Z

- **pkg/util/kubeversion/kubeversion.go:90** by [@stuton](https://github.com/stuton) — 2023-08-02T12:40:12Z
```diff
@@ -0,0 +1,92 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const fetchServerVersionInterval = time.Minute * 10
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+)
+
+type ServerVersionFetcher struct {
+	dc            discovery.DiscoveryInterface
+	ticker        *time.Ticker
+	serverVersion *versionutil.Version
+	mu            sync.Mutex
+}
+
+type Options struct {
+	Interval time.Duration
+}
+
+type Option func(*Options)
+
+var DefaultOptions = Options{
+	Interval: fetchServerVersionInterval,
+}
+
+func WithInterval(interval time.Duration) Option {
+	return func(o *Options) {
+		o.Interval = interval
+	}
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface, opts ...Option) *ServerVersionFetcher {
+	options := DefaultOptions
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: time.NewTicker(options.Interval),
+	}
+}
+
+// Start implements the Runnable interface to run ServerVersionFetcher as a controller.
+func (s *ServerVersionFetcher) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("serverVersionFetcher")
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	if err := s.FetchServerVersion(); err != nil {
+		log.Error(err, "Unable to fetch server version")
+	}
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching server version")
+			return nil
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
+			log.V(5).Info("Fetch server version", "serverVersion", s.serverVersion)
+		}
+	}
+}
+
+// FetchServerVersion gets API server version
+func (s *ServerVersionFetcher) FetchServerVersion() error {
+	clusterVersionInfo, err := s.dc.ServerVersion()
+	if err != nil {
+		return err
+	}
+	s.mu.Lock()
+	defer s.mu.Unlock()
+	s.serverVersion = versionutil.MustParseSemantic(clusterVersionInfo.String())
+	return nil
+}
+
+func (s *ServerVersionFetcher) GetServerVersion() *versionutil.Version {
```

  return back

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-02T12:42:41Z

- **pkg/controller/jobframework/reconciler.go:87** by [@stuton](https://github.com/stuton) — 2023-08-02T12:42:40Z
```diff
@@ -82,6 +84,12 @@ func WithWaitForPodsReady(f bool) Option {
 	}
 }
 
+func WithKubeServerVersion(v *kubeversion.ServerVersionFetcher) Option {
```

  I missed this part in main, we are using [in options for webhooks](https://github.com/kubernetes-sigs/kueue/pull/1014/files#diff-2873f79a86c0d8b3335cd7731b0ecf7dd4301eb19a82ef7a1cba7589b5252261R222)

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-02T12:53:54Z

- **pkg/util/kubeversion/kubeversion.go:71** by [@stuton](https://github.com/stuton) — 2023-08-02T12:53:54Z
```diff
@@ -0,0 +1,75 @@
+package kubeversion
+
+import (
+	"context"
+	"sync"
+	"time"
+
+	versionutil "k8s.io/apimachinery/pkg/util/version"
+	"k8s.io/client-go/discovery"
+	ctrl "sigs.k8s.io/controller-runtime"
+)
+
+const fetchServerVersionInterval = time.Second * 10
+
+var (
+	KubeVersion1_27 = versionutil.MustParseSemantic("1.27.0")
+
+	serverVersion = &versionutil.Version{}
+	rwm           = &sync.RWMutex{}
+)
+
+type ServerVersionFetcher struct {
+	dc     discovery.DiscoveryInterface
+	ticker *time.Ticker
+}
+
+func NewServerVersionFetcher(dc discovery.DiscoveryInterface) *ServerVersionFetcher {
+	return &ServerVersionFetcher{
+		dc:     dc,
+		ticker: time.NewTicker(fetchServerVersionInterval),
+	}
+}
+
+// Start implements the Runnable interface to run ServerVersionFetcher as a controller.
+func (s *ServerVersionFetcher) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("serverVersionFetcher")
+	ctx = ctrl.LoggerInto(ctx, log)
+
+	defer s.ticker.Stop()
+
+	for {
+		select {
+		case <-ctx.Done():
+			log.V(5).Info("Context cancelled; stop fetching server version")
+			return nil
+		case <-s.ticker.C:
+			if err := s.FetchServerVersion(); err != nil {
+				log.Error(err, "Unable to fetch server version")
+			}
+			log.V(5).Info("Fetch server version", "serverVersion", serverVersion)
+		}
+	}
+}
+
+// FetchServerVersion gets API server version
+func (s *ServerVersionFetcher) FetchServerVersion() error {
+	clusterVersionInfo, err := s.dc.ServerVersion()
+	if err != nil {
+		return err
+	}
+	SetServerVersion(versionutil.MustParseSemantic(clusterVersionInfo.String()))
+	return nil
+}
+
+func SetServerVersion(v *versionutil.Version) {
+	rwm.Lock()
+	defer rwm.Unlock()
+	serverVersion = v
+}
+
+func LessThan(v *versionutil.Version) bool {
```

  Now, I've finally added integration test to cover version checking. I use fake discovery client for easier configure version because if take version from binary we should initialise fetcher in Setup of framework and put it in managerSetup simultaneously

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-02T12:55:34Z

- **pkg/controller/jobs/job/job_webhook.go:137** by [@stuton](https://github.com/stuton) — 2023-08-02T12:55:34Z
```diff
@@ -129,6 +132,11 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
 				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobCompletionsEqualParallelismAnnotation], "should not be enabled for NonIndexed jobs"))
 			}
+			if w.kubeServerVersion != nil {
+				if version := w.kubeServerVersion.GetServerVersion(); version != nil && version.LessThan(kubeversion.KubeVersion1_27) {
+					allErrs = append(allErrs, field.Forbidden(syncCompletionAnnotationsPath, apivalidation.FieldImmutableErrorMsg))
```

  changed message and type of error

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-02T13:29:49Z

- **config/components/webhook/manifests.yaml:162** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T13:29:49Z
```diff
@@ -159,6 +159,7 @@ webhooks:
     apiVersions:
     - v1
     operations:
+    - CREATE
```

  oh, this is a bug that we should cherry-pick.
  
  Please create a separate PR with a release note.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-02T13:36:44Z

- **config/components/webhook/manifests.yaml:162** by [@mimowo](https://github.com/mimowo) — 2023-08-02T13:36:43Z
```diff
@@ -159,6 +159,7 @@ webhooks:
     apiVersions:
     - v1
     operations:
+    - CREATE
```

  +1, it appears we have the same issue for other integrations too

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-02T13:50:14Z

- **pkg/controller/jobs/job/job_webhook.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T13:38:34Z
```diff
@@ -129,6 +132,11 @@ func validatePartialAdmissionCreate(job *Job) field.ErrorList {
 			if job.Spec.CompletionMode == nil || *job.Spec.CompletionMode == batchv1.NonIndexedCompletion {
 				allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobCompletionsEqualParallelismAnnotation], "should not be enabled for NonIndexed jobs"))
 			}
+			if w.kubeServerVersion != nil {
+				if version := w.kubeServerVersion.GetServerVersion(); version.LessThan(kubeversion.KubeVersion1_27) {
+					allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobCompletionsEqualParallelismAnnotation], "feature is not supported for server that less than 1.27"))
```

  ```suggestion
  					allErrs = append(allErrs, field.Invalid(syncCompletionAnnotationsPath, job.Annotations[JobCompletionsEqualParallelismAnnotation], "only supported in Kubernetes 1.27 or newer"))
  ```

- **test/integration/controller/job/job_webhook_test.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T13:40:57Z
```diff
@@ -21,11 +21,19 @@ import (
 	"github.com/onsi/gomega"
 	batchv1 "k8s.io/api/batch/v1"
 	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/version"
+	fakediscovery "k8s.io/client-go/discovery/fake"
+	fakeclient "k8s.io/client-go/kubernetes/fake"
+
```

  remove empty line

- **test/integration/controller/job/job_webhook_test.go:169** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T13:50:08Z
```diff
@@ -154,4 +162,52 @@ var _ = ginkgo.Describe("Job Webhook", func() {
 			gomega.Expect(k8sClient.Update(ctx, createdJob)).ShouldNot(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("With manageJobsWithoutQueueName enabled and server version less than 1.27", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+		ginkgo.BeforeAll(func() {
+			fakeDiscoveryClient, _ := fakeclient.NewSimpleClientset().Discovery().(*fakediscovery.FakeDiscovery)
+			fakeDiscoveryClient.FakedServerVersion = &version.Info{GitVersion: "1.26.3"}
+			serverVersionFetcher := kubeversion.NewServerVersionFetcher(fakeDiscoveryClient)
```

  In an integration test, the idea is that we test against the real apiserver.
  By faking the API, this test is not much more than a unit test, so I think we shouldn't need it.
  
  However, it looks like through this test you discovered that the webhook was not properly configured. But in that case, we can have an integration test for any of the other validations that doesn't require us to fake any of the clients.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-02T14:01:06Z

- **config/components/webhook/manifests.yaml:162** by [@stuton](https://github.com/stuton) — 2023-08-02T14:01:06Z
```diff
@@ -159,6 +159,7 @@ webhooks:
     apiVersions:
     - v1
     operations:
+    - CREATE
```

  Added #1035

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-02T15:08:55Z

- **test/integration/controller/job/job_webhook_test.go:169** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T15:08:54Z
```diff
@@ -154,4 +162,52 @@ var _ = ginkgo.Describe("Job Webhook", func() {
 			gomega.Expect(k8sClient.Update(ctx, createdJob)).ShouldNot(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("With manageJobsWithoutQueueName enabled and server version less than 1.27", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+		ginkgo.BeforeAll(func() {
+			fakeDiscoveryClient, _ := fakeclient.NewSimpleClientset().Discovery().(*fakediscovery.FakeDiscovery)
+			fakeDiscoveryClient.FakedServerVersion = &version.Info{GitVersion: "1.26.3"}
+			serverVersionFetcher := kubeversion.NewServerVersionFetcher(fakeDiscoveryClient)
```

  Let's remove this test and add a separate test (without fakes) as part of #1035

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-03T13:59:32Z

- **pkg/controller/jobs/job/job_webhook_test.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-03T13:59:32Z
```diff
@@ -188,6 +193,19 @@ func TestValidateCreate(t *testing.T) {
 				field.Invalid(syncCompletionAnnotationsPath, "true", "should not be enabled for NonIndexed jobs"),
 			},
 		},
+		{
+			name: "valid sync completions annotation, server version less then 1.27",
+			job: testingutil.MakeJob("job", "default").
+				Parallelism(4).
+				Completions(4).
+				SetAnnotation(JobCompletionsEqualParallelismAnnotation, "true").
+				Indexed(true).
+				Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(syncCompletionAnnotationsPath, "true", "only supported in Kubernetes 1.27 or newer"),
+			},
+			serverVersion: "1.26.3",
+		},
 		{
 			name: "valid sync completions annotation",
```

  why is this test passing if you didn't set a version?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-08-03T15:46:45Z

- **pkg/controller/jobs/job/job_webhook_test.go:225** by [@stuton](https://github.com/stuton) — 2023-08-03T15:46:45Z
```diff
@@ -188,6 +193,19 @@ func TestValidateCreate(t *testing.T) {
 				field.Invalid(syncCompletionAnnotationsPath, "true", "should not be enabled for NonIndexed jobs"),
 			},
 		},
+		{
+			name: "valid sync completions annotation, server version less then 1.27",
+			job: testingutil.MakeJob("job", "default").
+				Parallelism(4).
+				Completions(4).
+				SetAnnotation(JobCompletionsEqualParallelismAnnotation, "true").
+				Indexed(true).
+				Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(syncCompletionAnnotationsPath, "true", "only supported in Kubernetes 1.27 or newer"),
+			},
+			serverVersion: "1.26.3",
+		},
 		{
 			name: "valid sync completions annotation",
```

  it shouldn't, I added test case where we don't specify server version

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-03T16:57:45Z

/lgtm
/approve
