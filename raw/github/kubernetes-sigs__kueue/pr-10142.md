# PR #10142: cleanup: move ShouldExposeLocalQueueMetricsForWorkload to cache

**Summary**: cleanup: move ShouldExposeLocalQueueMetricsForWorkload to cache

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10142

**Last updated**: 2026-03-29T19:34:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mykysha](https://github.com/mykysha)
- **Branch**: `cleanup/move-lqmetrics-workload-to-cache` → `main`
- **Created**: 2026-03-27T10:06:07Z
- **Updated**: 2026-03-29T19:34:09Z
- **Merged**: 2026-03-27T19:46:55Z
- **Closed**: 2026-03-27T19:46:55Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@gabesaba](https://github.com/gabesaba)
- **Changed files**: 17   **+45 / -111**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Cleanup ShouldExposeLocalQueueMetricsForWorkload

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #8830

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-27T10:06:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9684e928263e139fb95f81cd96fd9e6ab3fc00ad |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c67ce7b5752f000877cb86 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T19:11:54Z

LGTM label has been added.  <details>Git tree hash: 9343e30c50c7cb896793583ec1d8be69ea9ae678</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T19:11:56Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10142#pullrequestreview-4023160684" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10142#" title="Author self-approved">mykysha</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T11:09:42Z

- **pkg/cache/scheduler/cache.go:1044** by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:09:42Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  I know this is not related to PR. It might be ok for now, but this is not universally true. For example we may have some metrics which are also reported for pending workloads.
  
  When we have that then we may need to be able to get the LocalQueue just by `queue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName)`. This is technically speaking enough to uniquely identify each LocalQueue. I think we only use `wl.Status.Admission.ClusterQueue` because we don't have a flattened map of all LocalQueues, right?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-03-27T12:21:09Z

- **pkg/cache/scheduler/cache.go:1038** by [@mwielgus](https://github.com/mwielgus) — 2026-03-27T12:16:38Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
```

  Determines.

- **pkg/cache/scheduler/cache.go:1044** by [@mwielgus](https://github.com/mwielgus) — 2026-03-27T12:17:48Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Wouldn't it log the entire workload object?

- **pkg/cache/scheduler/cache.go:1044** by [@mwielgus](https://github.com/mwielgus) — 2026-03-27T12:18:14Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  WARNING, info and V(5) seem inconsistent.

- **pkg/cache/scheduler/cache.go:1047** by [@mwielgus](https://github.com/mwielgus) — 2026-03-27T12:19:22Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
+		return false
+	}
+	lq, err := c.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, queue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName))
```

  Would queue.KeyFromWorkload(wl) fit here?

- **pkg/cache/scheduler/cache.go:1049** by [@mwielgus](https://github.com/mwielgus) — 2026-03-27T12:20:53Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
+		return false
+	}
+	lq, err := c.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, queue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName))
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
```

  If this is an actionable error, consider using log.Error without .V(5). If this is just an expected trace or debug information, consider using log.V(5).Info("Failed to get LocalQueue for metrics", "err", err, ...) instead.

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:37:08Z

- **pkg/cache/scheduler/cache.go:1044** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:37:08Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Yes, in this case we get lq instance from cache, and to get it we use cluster queue reference stored in admission. So we might need to update the cache to have a more direct access

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T12:44:49Z

- **pkg/cache/scheduler/cache.go:1044** by [@mimowo](https://github.com/mimowo) — 2026-03-27T12:44:49Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Ok, we can re-work that when needed. 
  
  For now let's just adjust the comment because I think this is misleading. First of all: is this function called for Pending workloads? If so this is misleading because it is normal that Pending workloads don't have Admission.
  
  So maybe it should rather be:
  
  // We can skip the update for LocalQueue metrics for a pending workload as currently all our 
  // LocalQueue metrics for workloads only record for workloads with admission.
  // We may need to re-work this in the  future when such metrics are also recorded.
  log.V(5).Info("Skip the update for local queue metrics for a workload without admission", "workload", klog.KObj(wl))

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:50:19Z

- **pkg/cache/scheduler/cache.go:1044** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:50:18Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Updated with the proposed message

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:50:26Z

- **pkg/cache/scheduler/cache.go:1038** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:50:26Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
```

  Fixed

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:50:51Z

- **pkg/cache/scheduler/cache.go:1044** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:50:51Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Fixed, now it logs just the reference to the workload

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:51:20Z

- **pkg/cache/scheduler/cache.go:1044** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:51:19Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
```

  Fixed via changing the message to the one proposed in another comment chain

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:52:28Z

- **pkg/cache/scheduler/cache.go:1047** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:52:28Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
+		return false
+	}
+	lq, err := c.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, queue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName))
```

  It would, have changed now

### Review by [@mykysha](https://github.com/mykysha) (commented) — 2026-03-27T12:53:47Z

- **pkg/cache/scheduler/cache.go:1049** by [@mykysha](https://github.com/mykysha) — 2026-03-27T12:53:47Z
```diff
@@ -1042,3 +1034,20 @@ func resourceFloat(name corev1.ResourceName, v int64) float64 {
 func queueKey(q *kueue.LocalQueue) queue.LocalQueueReference {
 	return queue.NewLocalQueueReference(q.Namespace, kueue.LocalQueueName(q.Name))
 }
+
+// ShouldExposeLocalQueueMetricsForWorkload detemines if LocalQueue metric reporting should be made for the associated LocalQueue.
+func (c *Cache) ShouldExposeLocalQueueMetricsForWorkload(log logr.Logger, wl *kueue.Workload) bool {
+	if !c.lqMetrics.IsEnabled() {
+		return false
+	}
+	if wl.Status.Admission == nil {
+		log.V(5).Info("WARNING: cannot expose local queue metrics of workload without status.Admission", "workload", wl)
+		return false
+	}
+	lq, err := c.GetCacheLocalQueue(wl.Status.Admission.ClusterQueue, queue.NewLocalQueueReference(wl.Namespace, wl.Spec.QueueName))
+	if err != nil {
+		log.V(5).Error(err, "Failed to get LocalQueue for metrics", "localQueue", klog.KRef(wl.Namespace, string(wl.Spec.QueueName)))
```

  I think this would be an actionable error explaining why the metrics are not appearing, so removed V(5) from the log

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T19:11:47Z

/lgtm
/approve
