# PR #10145: KEP-9988: MultiKueue Global Quotas

**Summary**: KEP-9988: MultiKueue Global Quotas

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10145

**Last updated**: 2026-05-08T12:30:51Z

---

## Metadata

- **State**: open
- **Author**: [@olekzabl](https://github.com/olekzabl)
- **Branch**: `br35` → `main`
- **Created**: 2026-03-27T10:38:06Z
- **Updated**: 2026-05-08T12:30:51Z
- **Merged**: —
- **Closed**: —
- **Labels**: `release-note-none`, `size/XL`, `cncf-cla: yes`, `area/multikueue`, `kind/kep`
- **Assignees**: _none_
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@amy](https://github.com/amy), [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+742 / -0**

## Description


#### What type of PR is this?

/kind kep
/kind api-change

/area multikueue

#### What this PR does / why we need it:

See #9988.

#### Which issue(s) this PR fixes:

#### Special notes for your reviewer:

(Disclaimer: AI was used but only for minor editorial tasks)

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T10:38:09Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-27T10:38:13Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b658d95409e93cd3e354f0eb3ff2b1d29e1dc955 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fdd0ba64f46900087310e0 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-17T08:25:18Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10145#" title="Author self-approved">olekzabl</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10145#pullrequestreview-4127198524" title="Approved">Singularity23x0</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T09:04:55Z

/remove-kind-api-change

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T09:09:45Z

@mimowo @gabesaba Is this for the 0.18 target?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T16:10:24Z

/remove-kind api-change

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T14:18:35Z

> @mimowo @gabesaba Is this for the 0.18 target?

Yes, but we consider limiting the scope of what is going into 0.18. The tentative plan I discussed with @olekzabl is to move out visibility as the future work, likely for 0.19. wdyt?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-21T16:02:01Z

> > @mimowo @gabesaba Is this for the 0.18 target?
> 
> Yes, but we consider limiting the scope of what is going into 0.18. The tentative plan I discussed with @olekzabl is to move out visibility as the future work, likely for 0.19. wdyt?

Thank you for letting me know. Alright, that sounds good to me. 
@olekzabl, please let me know once this KEP is ready. Also, before it is ready for review, can you make this KEP's scope limited as @mimowo mentioned?

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-04-24T10:42:25Z

I just pushed a major update to this KEP. Key changes:

1. **Removed the visibility part** (which I'll soon re-post in a separate KEP).
   This KEP is now only about Quota Automation which is for **0.18**.
   (@tenzen-y PTAL :)
2. Changed the proposal to deal with worker disconnections (see [this comment thread](https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3118724780)).
3. Added a longer discussion of "why we discard-or-postpone any treatment of Cohorts", following our recent brainstorming with @mimowo .
4. Added some follow-up ideas (remote client cache, and a more explicit mention of metrics).

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-04-24T11:58:13Z

@amy PTAL; I heard you were interested in this topic

### Comment by [@amy](https://github.com/amy) — 2026-04-24T22:44:04Z

@olekzabl please list me as reviewer in the kep 🙏

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T11:50:34Z

LGTM, but let me give a bit more time for other reviewers, I synced with @tenzen-y and he will likely drop initial feedback "today".

## Reviews

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-04-14T12:56:05Z

- **keps/9988-multikueue-global-quotas/README.md:327** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-14T12:18:19Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
```

  What kind of provider will you wire here? Just a simple rest.Getter?

- **keps/9988-multikueue-global-quotas/README.md:383** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-14T12:45:43Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []FlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []FlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []FlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+```
+
+All these fields use the `FlavorUsage` type already defined [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
```

  Given this, we will calculate ReservedQuota (R) and UsedQuota (U) for manager remotes, but total quota (Q) in general?
  
  If yes, there are two points that could cause confusion that I don't think are listed in the downsides:
  1. Q may include resources not listed as available on the Manager CQ, as Workers may have other resources at their disposal.
  2. Users may naturally expect that Q - (R + U) = Available Resources, which is incorrect, as R and U do not include resources held by other workloads on the worker (ones not managed by this Manager CQ). Shouldn't we include a "reserved by external workloads" stat to avoid this issue?

- **keps/9988-multikueue-global-quotas/README.md:414** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-14T12:49:50Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []FlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []FlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []FlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+```
+
+All these fields use the `FlavorUsage` type already defined [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### MultiKueue Cache
+
+Both features will be based on a new **MultiKueue Cache**, storing some information retrieved from the watched resources and fetched workloads.
+
+The cache format will be tentatively as follows: \
+(referring to the existing definitions of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) and [`LocalQueueReference`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/util/queue/local_queue.go#L25-L26))
+
+```go
+type clusterReference string
+
+type clusterQueueReference string
+
+type workloadReference struct {
+   Name string
+   Namespace string
+}
+
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved locally.
+   LocalClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   localLqToCqMap map[LocalQueueReference]clusterQueueReference
```

  For manager LQs and CQs this mapping is already recorded in the queue cache (manager). Can't we utilize that?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-14T13:37:27Z

- **keps/9988-multikueue-global-quotas/README.md:414** by [@olekzabl](https://github.com/olekzabl) — 2026-04-14T13:37:27Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []FlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []FlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []FlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+```
+
+All these fields use the `FlavorUsage` type already defined [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### MultiKueue Cache
+
+Both features will be based on a new **MultiKueue Cache**, storing some information retrieved from the watched resources and fetched workloads.
+
+The cache format will be tentatively as follows: \
+(referring to the existing definitions of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) and [`LocalQueueReference`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/util/queue/local_queue.go#L25-L26))
+
+```go
+type clusterReference string
+
+type clusterQueueReference string
+
+type workloadReference struct {
+   Name string
+   Namespace string
+}
+
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved locally.
+   LocalClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   localLqToCqMap map[LocalQueueReference]clusterQueueReference
```

  Hmm... it seems you're reviewing an old version?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-04-16T09:41:49Z

- **keps/9988-multikueue-global-quotas/README.md:537** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-15T09:02:05Z
```diff
@@ -0,0 +1,843 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size.
+
+   This is to avoid hitting large network delays or even control plane scalability bottlenecks. For context, workload count can reach hundreds of thousands, if not more.
+
+2. Tolerate API calls regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   This is intended to simplify the structure of [MultiKueue Cache](#multikueue-cache) and its handling code.
+
+   While it may seem too simplistic (especially compared to core Kueue reconcilers, not to mention Kueue scheduler), here are a few insights justifying this approach:
+
+   * Reconcilers can "afford" being less optimized if they're expected to run less often. \
+     In our case, the "slow" reconcile routines are only going to be triggered by creations / deletions / "re-bindings" (like [#10122](https://github.com/kubernetes-sigs/kueue/issues/10122)) within the Kueue setup objects, plus changes of MultiKueueConfigs and _quota changes_ in ClusterQueues (see Principle #4 below). \
+     This means a much lower rate of events than for scheduler cycles or even core reconcilers - **except** for a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * This is based on the current status quo of MultiKueue, which, in particular:
+
+     * tolerates multiple `.List()` API calls triggered by a single change event (e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890))
+
+     * tolerates cross-cluster API reads, even multiple in a single reconcile, and even if they could be avoided just by caching (e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L312-L314) in `readGroup` - where all remote `Workload` object have been already seen by the manager cluster as a result of [this `.Watch()` call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178))
+
+   * Our `.List()` calls, once we adhere to Principle #1, will involve LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q`.
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type workloadReference struct {
+   Name string
+   Namespace string
+}
```

  Why not use `workload.Reference`?

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-04-16T10:14:55Z

- **keps/9988-multikueue-global-quotas/README.md:481** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-16T10:14:52Z
```diff
@@ -0,0 +1,840 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * These API calls rely on `kube-controller` clients with local caching.
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q`.
```

  To confirm - is this where QuotaMultiplier will be used? The collected quota will be adjusted and set on the Manager?
  
  If yes - please add an explicit mention here, as the QuotaMultiplier param is only mentioned in setup and expansion-proposal and it is not clear where exacrly in the core proposal it is currently being utilized.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-16T12:41:27Z

- **keps/9988-multikueue-global-quotas/README.md:481** by [@olekzabl](https://github.com/olekzabl) — 2026-04-16T12:41:27Z
```diff
@@ -0,0 +1,840 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * These API calls rely on `kube-controller` clients with local caching.
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q`.
```

  PTAL how clear it's now.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-16T12:42:40Z

- **keps/9988-multikueue-global-quotas/README.md:537** by [@olekzabl](https://github.com/olekzabl) — 2026-04-16T12:42:40Z
```diff
@@ -0,0 +1,843 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size.
+
+   This is to avoid hitting large network delays or even control plane scalability bottlenecks. For context, workload count can reach hundreds of thousands, if not more.
+
+2. Tolerate API calls regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   This is intended to simplify the structure of [MultiKueue Cache](#multikueue-cache) and its handling code.
+
+   While it may seem too simplistic (especially compared to core Kueue reconcilers, not to mention Kueue scheduler), here are a few insights justifying this approach:
+
+   * Reconcilers can "afford" being less optimized if they're expected to run less often. \
+     In our case, the "slow" reconcile routines are only going to be triggered by creations / deletions / "re-bindings" (like [#10122](https://github.com/kubernetes-sigs/kueue/issues/10122)) within the Kueue setup objects, plus changes of MultiKueueConfigs and _quota changes_ in ClusterQueues (see Principle #4 below). \
+     This means a much lower rate of events than for scheduler cycles or even core reconcilers - **except** for a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * This is based on the current status quo of MultiKueue, which, in particular:
+
+     * tolerates multiple `.List()` API calls triggered by a single change event (e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890))
+
+     * tolerates cross-cluster API reads, even multiple in a single reconcile, and even if they could be avoided just by caching (e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L312-L314) in `readGroup` - where all remote `Workload` object have been already seen by the manager cluster as a result of [this `.Watch()` call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178))
+
+   * Our `.List()` calls, once we adhere to Principle #1, will involve LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q`.
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type workloadReference struct {
+   Name string
+   Namespace string
+}
```

  Good idea, thanks.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-16T12:44:24Z

- **keps/9988-multikueue-global-quotas/README.md:383** by [@olekzabl](https://github.com/olekzabl) — 2026-04-16T12:44:23Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []FlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []FlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []FlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+```
+
+All these fields use the `FlavorUsage` type already defined [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
```

  Ad 1. The divergence between manager-side quotas and worker-side quotas is already discussed (as sth amplified by the mulitiplier).
  
  Ad 2. Good point, I added this as a drawback.
  
  Also, BTW, you reminded me to discuss borrowing (i.e. lack of support for it as a Drawback, and adding it as a Possible Follow-Up).

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (approved) — 2026-04-17T08:25:08Z

- **keps/9988-multikueue-global-quotas/README.md:760** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-17T08:23:19Z
```diff
@@ -0,0 +1,859 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size, especially remote ones.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+   Remote API calls of that kind should be avoided even more because, even though MultiKueue already watches remote Workloads, it doesn't currently cache them. (And if we enabled client-level caching here, it would have a significantly larger memory footprint than the proposed [MultiKueue Cache](#multikueue-cache)).
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler.
+
+   * The local API calls are backed by `controller-runtime` client-side caching.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q` (using the recipe described [here](#manager-quota-automation), with `QuotaMultiplier` used as the multiplier).
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved on the manager cluster.
+   ManagerClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   wlGroupResourceStats map[workload.Reference]wlGroupResourceInfo
+}
+```
+
+(where `workload.Reference` is defined [here](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/workload/workload.go#L89)).
+
+The `FlavorsUtilization` struct refers to an existing type of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37), which differs from `WorkerFlavorUsage` used on the API surface. This is deliberate: the former representation allows more efficient and convenient summation.
+
+The cache instance will be created in the [MultiKueue `SetupControllers` method](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/controllers.go#L111), and passed down to specific controllers as necessary.
+
+### MultiKueue Workload Reconciler
+
+The MultiKueue Workload Reconciler will be responsible for maintaining the [MultiKueue Cache](#multikueue-cache).
+
+This will be handled in every run of [`Reconcile`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/controller/admissionchecks/multikueue/workload.go#L158), after [this call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L224) to `.readGroup()`. \
+For non-deletion scenarios, it will be postponed to follow [this call](https://github.com/kubernetes-sigs/kueue/blob/083e985b0f750901b813e70a1e168348b21a42f8/pkg/controller/admissionchecks/multikueue/workload.go#L240) to `.reconcileGroup()`. 
+
+On a Kueue restart, the cache will be eventually re-populated, as the controller-runtime library will schedule `Reconcile()` for all pre-existing (local) Workload objects.
+
+To ensure no "permanent zombie entries" in the cache (in a case of a missed delete event), we will add a "garbage collection routine" analogous to [this one](https://github.com/kubernetes-sigs/kueue/blob/249ad53617ee4d2aa0ee6573d4b38418bc549304/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L665). In the Alpha version, we will hook it to the same "GC interval" setting, defined [here](https://github.com/kubernetes-sigs/kueue/blob/9c92d93bf6f1988e59a459623dc9b345c2161ef6/apis/config/v1beta2/configuration_types.go#L302-L305).
+
+### Visibility Server
+
+The Visibility Server will be provided with [MultiKueue Cache](#multikueue-cache) - [via `main.go`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/cmd/kueue/main.go#L380), similarly as it's already given the core queues cache.
+
+Based on that, the new REST endpoint (`clusterqueues/workerresourcestats`), given a ClusterQueue `Q`, will do the following:
+
+1. Check if `Q` is a MultiKueue manager queue (as in steps 1-2 of the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)). \
+   If not, return an error.
+
+2. Determine and fetch the [related worker ClusterQueues](#defining-related-worker-clusterqueues), by making:
+   1. a filtered local `.List()` API call for LocalQueues,
+   2. unfiltered remote `.List()` API calls for LocalQueues (one per worker cluster),
+   3. unfiltered remote `.List()` API calls for ClusterQueues (one per worker cluster).
+
+3. Set `FlavorsQuotas` based on summing up the quotas for all related worker ClusterQueues.
+
+4. Set `FlavorsReservation` and `FlavorsUsage` to the total usage stats, summed up over all entries in `wlGroupResourceStats` which have `ManagerClusterQueue` set to `Q`.
+
+   * Notably, this involves no API calls.
+
+   * For cleaner blackboxing, this logic will be wrapped in a public method of the MultiKueue Cache object. \
+     This way, the Visibility Server won't have write access to the cache (or even read access to its internals).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* For the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler): 
+
+  * Test the reconcile logic with all exit paths (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L60)).
+
+  * Test all additional triggering paths from local watchers in the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L2266))
+
+  * Test the _non-triggering_ paths, e.g. a ClusterQueue event touching only irrelevant fields.
+
+* For the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler):
+
+  * Test triggering vs. _non-triggering_ paths (by just verifying the content of the event channel, analogous to [`wlUpdateCh`](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L95)).
+
+* For the [MultiKueue Cache](#multikueue-cache):
+
+  * Test the public helper exposed to the [Visibility Server](#visibility-server).
+
+* For the [MultiKueue Workload Reconciler](#multikueue-workload-reconciler):
+
+  * Test the updates to MultiKueue Cache.
+
+* For the [Visibility Server](#visibility-server):
+
+  * Test steps 1-3 of the recipe. (Test 4 will be covered by MultiKueue Cache unit tests).
+
+#### Integration Tests
+
+* For the integration between [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) and [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler):
+
+  * Test triggering the latter by changes in remote LocalQueues and ClusterQueues. \
+    (This could be also unit-tested, by a well-crafted watch interceptor similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go#L69-L70) - but an integration test feels a much cleaner way).
+
+* For the integration between [MultiKueue Workload Reconciler](#multikueue-workload-reconciler) and [Visibility Server](#visibility-server):
+
+  * Test some _relatively simple_ scenario of worker resource stats. \
+    (Where we'd like enough complexity to demonstrate that the resource utilization stats are not just aggregates over the values reported by worker-side ClusterQueues).
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled. Verify that:
+
+  * Manager quota is adjusted, as intended, upon:
+
+    - adding a new worker in MultiKueueConfig
+    - changing a remote ClusterQueueQuota
+    - (if we decide to support that) a worker cluster becoming unreachable
+
+  * New jobs can be admitted at all 3 phases of this scenario (quota automation disabled, then enabled, then disabled again).
+
+* Verify that manager quota auto-adjustments (in either direction: increases and decreases) promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Expand the cross-worker resource stats
+
+The [proposed format] of cross-worker resource stats ([overview](#cross-worker-resource-stats-in-visibility-api), [API](#cross-worker-resource-stats-in-visibility-api-1)) is essentially a collection of 2D arrays (indexed by flavor and resource) returned for a ClusterQueue.
+
+Early insights suggest that we might want to develop this in various directions:
+
+- Expose the above data broken down also per worker. \
+  This would make it effectively a collection of _3D arrays_, posing a question how to arrange those to be best human-readable right away (and whether we should at all aim at that). \
+  Our initial idea is to group by resource, then worker, then flavor, though for now we leave it open for feedback.
+
+- Expose per-LocalQueue stats (like in the existing Visibility API endpoint).
+
+- Support borrowing information, like [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359). \
+  This could be less clear in the MultiKueue case, due to the "stray workloads" story. (For example: if the manager-side ClusterQueue `Q` dispatched a 4 CPU Job to ClusterQueue `Q2` on `worker1`, which happens to also host another 6 CPU Job unrelated to MultiKueue, using 8 CPU of nominal quota and borrowing 2 CPU from a Cohort on `worker2`, then what value of `Borrowed` should we report for `Q`? 2 CPU? 0 CPU? Some "proportional fraction" like 0.8 CPU?)
+
+#### Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier
+
+That is, an analogue of the proposed quota mutliplier - but differing in that:
+
+* it would **not affect** the [nominal ClusterQueue quota](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L250) value,
+* it would **not increase** the "total volume" of workloads which can be **admitted** by the manager,
+* it would **only** affect the "total volume" of workloads can **reserve quota** on the manager-side ClusterQueue (and hence, get dispatched to the workers).
+
+For example, if there are 2 workers, each with 10 CPU capacity, the `QuotaMultiplier` (as proposed in this KEP) is 0.8 (to leave 20% of worker capacity for "stray workloads"), and the Quota Reservation Overbooking Multiplier (as discussed here) is 3.0, then the manager-side ClusterQueue:
+
+* will have nominal quota set to (10 + 10) * 0.8 = 16,
+* will admit workloads requesting at most 16 CPU in total,
+* will reserve quota (and hence - dispatch to workers) workloads requesting up to 16 * 3 = 48 CPU in total.
+
+As weird as it seems, this opens a way to "reconcile" various, otherwise clashing, design factors for this KEP; specifically:
+
+* seamless dispatching vs. per-team allocations (see [Drawback #3](#drawbacks)),
+* (if `QuotaMultiplier` from this KEP is `1.0`) seamless dispatching vs. intuitive quotas (see [Drawback #2](#drawbacks)).
+
+Yet, this comes with some **disadvantages**:
+
+* Adding another form of user confusion ("why this ClusterQueue reserved quota above its nominal limit, despite using no borrowing?").
+
+* Adding complexity to core Kueue code. (In particular, we'd need to carefully plan how to enforce the "don't admit over the quota" constraint. Manager-side eviction propagated to workers? But whom to evict?)
+
+* Adding conceptual complexity. (Just look at the section name for this follow-up idea; it's long enough to make it feel like a joke).
+
+* Blurring separation of concerns, by introducing new ways of MultiKueue awareness to core Kueue code.
+
+Therefore, this idea is **shelved** for now. We may consider it later, depending on the feedback.
+
+#### Track borrowable quota on the worker side
+
+The possibility of borrowing within a Cohort clearly influences the actual capacity of a ClusterQueue, and thus could be included in the calculation of "total quota" - **both** in [manager quota automation](#manager-quota-automation) and [cross-worker resource stats](#cross-worker-resource-stats-in-visibility-api).
+
+This is **shelved** for now due to the added complexity, both in implementation and specific desired behavior. Example questions:
+
+* Should we simply increase the total quota by the `borrowingLimit`s on the workers?
+* If borrowing does not allow preemption, should we rather define "borrowable quota" as "the minimum of borrowing limit and current unused capacity on the lending ClusterQueues"?
+* Should we expose an analogue of [the `Borrowed` field](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) in [cross-worker resource stats](#cross-worker-resource-stats-in-visibility-api-1)? Then, if a worker-side ClusterQueue used borrowing to host MultiKueue workloads from a manager ClusterQueue `Q` as well as various ["stray" Workloads](#potential-reasons-for-decreasing-manager-quota), how should we report that?
+
+### Graduation Criteria
+
+This design is covered by two feature gates:
+
+* `MultiKueueMaangerQuotaAutomation` - for the [quota automation part](#manager-quota-automation);
+* `MultiKueueWorkerResourceStats` - for the [cross-worker resource stats part](#cross-worker-resource-stats-in-visibility-api).
+
+Graduation criteria listed jointly for both:
+
+* **Alpha:**
+  * Features implemented behind the respective gates, disabled by default.
+  * The newly introduced API calls (local & remote) are guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * Feature gates are enabled by default.
+  * The features have been succesfully used in production environment.
+  * [Potential extensions of cross-worker resource stats](#expand-the-cross-worker-resource-stats) have been considered, and (if desired) implemented.
+  * No warning signs on performance were seen (in particular, no news of any external automation of worker ClusterQueue quotas; see [here](#guiding-principles-for-the-implementation)) - or, if any were, [MultiKueue Cache](#multikueue-cache) is extended accordingly.
+  * The "guiding principles" (see [here](#guiding-principles-for-the-implementation)) are considered fully aligned on.
+  * Issue [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) (see [Drawback #4](#drawbacks)) is diagnosed, its impact on quota automation is understood and any mitigations deemed necessary are implemented.
+
+* **Stable:**
+  * The features (offered functionality, API surface and implementation) have stabilized, without raising concerns.
+  * Feature gates are removed.
+
+## Drawbacks
+
+Besides introducing some risks (see [Risks and Mitigations](#risks-and-mitigations)), the proposed solution feels suboptimal mostly in the following ways:
+
+1. The `QuotaMultiplier` value feels very arbitrary, and it may be difficult to determine its optimal value (even for a specific use case). Two approaches coming to mind are:
+
+   1. An experienced Batch Admin can try out various values and judge the effects by observing overall system efficiency. (That, of course, assumes no other factors substantially interfering in such experimenting).
+
+   1. The quotient between manager-side reserved quota and total worker quota could be exposed as a metric and monitored. \
+      The proposed solution makes this easier to implement (in exposing the total worker quota via Visibility API) but does not include setting up such a metric specifically. Even if it did, monitoring the metric (and likely collecting it for some period of time) would be necessarily the responsibility of the user.
+
+1. With the `QuotaMultiplier`, the manager-side quota can effectively tell "what amount of workloads we want to dispatch" but may get _very divergent_ from "what amount of workloads we want to execute".
+
+   This is largely counter-intuitive, as the latter is the more "fundamental" meaning of a ClusterQueue quota, likely the most intuitive one for many users.
+
+   This drawback is the main motivation for the possible follow-up idea of [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier).
+
+1. While the `QuotaMultiplier` allows addressing individual [reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota) or [for decreasing it](#potential-reasons-for-decreasing-manager-quota), some combinations of those reasons remain not supportable in the most efficient way.
+
+   For example, in the "Per-team quotas on the manager" scenario discussed [here](#potential-reasons-for-decreasing-manager-quota), the Batch Admin will be able to configure manager-side ClusterQuotas for teams A / B / C to have 50% / 30% / 20% of the total workers quota respectively, but then each of the teams will be vulnerable to scheduling delays resulting from quota fragmentation, as explained in the "Divergence of quota reservation" scenario [here](#potential-reasons-for-increasing-manager-quota). \
+   Conversely, an attempt to increase the quotas for each of the teams would allow individual teams to consume more quotas than intended. For example, if the Batch Admin applied our default multiplier (3.0) on top of the initial per-team assignments discussed above, team B would be able to consume 90% of the total worker capacity.
+
+   Just like Drawback #2, this may be addressable by the [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier) idea, if used _together_ with the nominal quota multiplier as in the current proposal. Yet, the overall complexity of such solution feels unacceptable for now. We may consider it later depending on the feedback.
+
+1. In the quota automation feature, the proposed solution fails to react to some types of detectable worker connection issues. \
+   See [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) which describes this problem in more detail, and speculates that it may already affect existing MultiKueue reconcilers (e.g. the one for Workloads).
+
+   If this turns out problematic, it should be fixable by extending the [MultiKueue Cache](#multikueue-cache) with the "manager ClusterQueue <-> MultiKueueCluster" relationship.
+
+1. The proposed cross-worker resource stats fall short of giving a full and clear picture of resource availability and usage on the workers. \
```

  I'd consider the option of adding the "external usage" statistics as one of the "possible follow ups" and linking that section of the KEP here as a "mitigation" option.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-17T14:16:09Z

- **keps/9988-multikueue-global-quotas/kep.yaml:1** by [@kshalot](https://github.com/kshalot) — 2026-04-14T10:14:37Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Global Quota
```

  Nit: It's plural everywhere else ("Quota" -> "Quotas").

- **keps/9988-multikueue-global-quotas/README.md:594** by [@kshalot](https://github.com/kshalot) — 2026-04-17T13:51:54Z
```diff
@@ -563,6 +564,35 @@ On a Kueue restart, the cache will be eventually re-populated, as the controller
 
 To ensure no "permanent zombie entries" in the cache (in a case of a missed delete event), we will add a "garbage collection routine" analogous to [this one](https://github.com/kubernetes-sigs/kueue/blob/249ad53617ee4d2aa0ee6573d4b38418bc549304/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L665). In the Alpha version, we will hook it to the same "GC interval" setting, defined [here](https://github.com/kubernetes-sigs/kueue/blob/9c92d93bf6f1988e59a459623dc9b345c2161ef6/apis/config/v1beta2/configuration_types.go#L302-L305).
 
+#### Ensuring cache availability on non-Kueue-leader Pods
+
+Our plan of populating [MultiKueue Cache](#multikueue-cache) in the MultiKueue Workload Reconciler to be used by [Visibility Server](#visibility-server) may hit obstacles of 2 kinds:
+
+1. If Kueue controller is deployed on multiple Pods, the MultiKueue Workload Controller won't be started on the followers.
+
+   * This obstacle is already present for the Kueue core cache, where it's currently mitigated by making the core controllers ["leader aware"](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), so that the follower instances run only the predicate methods (`.Create()`/ `.Update()` / `.Delete()`) but not the `Reconcile()` method. Then, as long as the cache is populated in predicates, it can be kept up-to-date also on the followers, without the proper `.Reconcile()` methods racing for etcd updates.
+
+   * The above approach is **insufficient** in our case, as [MultiKueue Cache](#multikueue-cache) depends on _remote_ API Workloads which we need to fetch in the `.Reconcile()` method. This logic is too heavy to be moved to predicates.
+
+     Therefore, for our case, we propose a **variation** of [`leader_aware_reconciler`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), which would accept a controller implementing a richer interface
+
+     ```golang
+     interface ReconcilerWithFollowerObserver {
+       TypedReconciler[Request]
+       Observe(context.Context, request) (Result, error)
+     }
+     ```
+
+     and, when about to reconcile an event, delegate to `.Reconcile()` or `.Observe()`, depending on the current leader/follower role.
+
+     Then, we'll make the MultiKueue Workload Reconciler wrapped with that, with the `.Observe()` method responsible only for updating [MultiKueue Cache](#multikueue-cache). (Internally, there will be some code sharing to avoid duplication).
+
+2. In [#10553](https://github.com/kubernetes-sigs/kueue/issues/10553), it's been proposed to separate Visibility API into a separate Pod / Deployment. This will clearly make it impossible to pass it any cached data from the "proper" Kueue binary.
+
+   The proposed solution for this (initially agreed on in [#10553](https://github.com/kubernetes-sigs/kueue/issues/10553)) is to equip the Visibility Server binary with a sufficient set of controllers (preferably read-only ones) to provide the necessary information, partially duplicating some of the logic of the already existing Kueue controllers.
+
+   This can be implemented with a very similar mechanism as in the previous point: the Visibility Server simply needs to run the `.Observe()` method (to populate the cache) but not `.Reconcile()` (no need to update the Worlkoads). Thus, we can equip it with an **adjusted version** of the MultiKueue Workload Reconciler, which delegates `.Reconcile()` to execute just `.Observe()` even on the leader replica.
```

  ```suggestion
     This can be implemented with a very similar mechanism as in the previous point: the Visibility Server simply needs to run the `.Observe()` method (to populate the cache) but not `.Reconcile()` (no need to update the Workloads). Thus, we can equip it with an **adjusted version** of the MultiKueue Workload Reconciler, which delegates `.Reconcile()` to execute just `.Observe()` even on the leader replica.
  ```

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-21T15:54:06Z

- **keps/9988-multikueue-global-quotas/README.md:739** by [@kshalot](https://github.com/kshalot) — 2026-04-21T11:47:55Z
```diff
@@ -0,0 +1,889 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size, especially remote ones.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+   Remote API calls of that kind should be avoided even more because, even though MultiKueue already watches remote Workloads, it doesn't currently cache them. (And if we enabled client-level caching here, it would have a significantly larger memory footprint than the proposed [MultiKueue Cache](#multikueue-cache)).
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler.
+
+   * The local API calls are backed by `controller-runtime` client-side caching.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q` (using the recipe described [here](#manager-quota-automation), with `QuotaMultiplier` used as the multiplier).
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved on the manager cluster.
+   ManagerClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   wlGroupResourceStats map[workload.Reference]wlGroupResourceInfo
+}
+```
+
+(where `workload.Reference` is defined [here](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/workload/workload.go#L89)).
+
+The `FlavorsUtilization` struct refers to an existing type of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37), which differs from `WorkerFlavorUsage` used on the API surface. This is deliberate: the former representation allows more efficient and convenient summation.
+
+The cache instance will be created in the [MultiKueue `SetupControllers` method](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/controllers.go#L111), and passed down to specific controllers as necessary.
+
+### MultiKueue Workload Reconciler
+
+The MultiKueue Workload Reconciler will be responsible for maintaining the [MultiKueue Cache](#multikueue-cache).
+
+This will be handled in every run of [`Reconcile`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/controller/admissionchecks/multikueue/workload.go#L158), after [this call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L224) to `.readGroup()`. \
+For non-deletion scenarios, it will be postponed to follow [this call](https://github.com/kubernetes-sigs/kueue/blob/083e985b0f750901b813e70a1e168348b21a42f8/pkg/controller/admissionchecks/multikueue/workload.go#L240) to `.reconcileGroup()`. 
+
+On a Kueue restart, the cache will be eventually re-populated, as the controller-runtime library will schedule `Reconcile()` for all pre-existing (local) Workload objects.
+
+To ensure no "permanent zombie entries" in the cache (in a case of a missed delete event), we will add a "garbage collection routine" analogous to [this one](https://github.com/kubernetes-sigs/kueue/blob/249ad53617ee4d2aa0ee6573d4b38418bc549304/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L665). In the Alpha version, we will hook it to the same "GC interval" setting, defined [here](https://github.com/kubernetes-sigs/kueue/blob/9c92d93bf6f1988e59a459623dc9b345c2161ef6/apis/config/v1beta2/configuration_types.go#L302-L305).
+
+#### Ensuring cache availability on non-Kueue-leader Pods
+
+Our plan of populating [MultiKueue Cache](#multikueue-cache) in the MultiKueue Workload Reconciler to be used by [Visibility Server](#visibility-server) may hit obstacles of 2 kinds:
+
+1. If Kueue controller is deployed on multiple Pods, the MultiKueue Workload Controller won't be started on the followers.
+
+   * This obstacle is already present for the Kueue core cache, where it's currently mitigated by making the core controllers ["leader aware"](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), so that the follower instances run only the predicate methods (`.Create()`/ `.Update()` / `.Delete()`) but not the `Reconcile()` method. Then, as long as the cache is populated in predicates, it can be kept up-to-date also on the followers, without the proper `.Reconcile()` methods racing for etcd updates.
+
+   * The above approach is **insufficient** in our case, as [MultiKueue Cache](#multikueue-cache) depends on _remote_ API Workloads which we need to fetch in the `.Reconcile()` method. This logic is too heavy to be moved to predicates.
+
+     Therefore, for our case, we propose a **variation** of [`leader_aware_reconciler`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), which would accept a controller implementing a richer interface
+
+     ```golang
+     interface ReconcilerWithFollowerObserver {
+       TypedReconciler[Request]
+       Observe(context.Context, request) (Result, error)
+     }
+     ```
+
+     and, when about to reconcile an event, delegate to `.Reconcile()` or `.Observe()`, depending on the current leader/follower role.
+
+     Then, we'll make the MultiKueue Workload Reconciler wrapped with that, with the `.Observe()` method responsible only for updating [MultiKueue Cache](#multikueue-cache). (Internally, there will be some code sharing to avoid duplication).
+
+2. In [#10553](https://github.com/kubernetes-sigs/kueue/issues/10553), it's been proposed to separate Visibility API into a separate Pod / Deployment. This will clearly make it impossible to pass to it any cached data from the "proper" Kueue binary.
+
+   The proposed solution for this (initially agreed on in [#10553](https://github.com/kubernetes-sigs/kueue/issues/10553)) is to equip the Visibility Server binary with a sufficient set of "observing" controllers (not making updates to Kueue API resources) to provide the necessary information, partially duplicating some of the logic of the already existing Kueue controllers.
+
+   This can be implemented with a very similar mechanism as in the previous point: the Visibility Server simply needs to run the `.Observe()` method (to populate the cache) but not `.Reconcile()` (no need to update the Workloads). Thus, we can equip it with an **adjusted version** of the MultiKueue Workload Reconciler, which delegates `.Reconcile()` to execute just `.Observe()` even on the leader replica.
+
+### Visibility Server
+
+The Visibility Server will be provided with [MultiKueue Cache](#multikueue-cache) - [via `main.go`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/cmd/kueue/main.go#L380), similarly as it's already given the core queues cache.
+
+Based on that, the new REST endpoint (`clusterqueues/workerresourcestats`), given a ClusterQueue `Q`, will do the following:
+
+1. Check if `Q` is a MultiKueue manager queue (as in steps 1-2 of the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)). \
+   If not, return an error.
+
+2. Determine and fetch the [related worker ClusterQueues](#defining-related-worker-clusterqueues), by making:
+   1. a filtered local `.List()` API call for LocalQueues,
+   2. unfiltered remote `.List()` API calls for LocalQueues (one per worker cluster),
+   3. unfiltered remote `.List()` API calls for ClusterQueues (one per worker cluster).
+
+3. Set `FlavorsQuotas` based on summing up the quotas for all related worker ClusterQueues.
+
+4. Set `FlavorsReservation` and `FlavorsUsage` to the total usage stats, summed up over all entries in `wlGroupResourceStats` which have `ManagerClusterQueue` set to `Q`.
+
+   * Notably, this involves no API calls.
+
+   * For cleaner blackboxing, this logic will be wrapped in a public method of the MultiKueue Cache object. \
+     This way, the Visibility Server won't have write access to the cache (or even read access to its internals).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* For the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler): 
+
+  * Test the reconcile logic with all exit paths (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L60)).
+
+  * Test all additional triggering paths from local watchers in the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L2266))
+
+  * Test the _non-triggering_ paths, e.g. a ClusterQueue event touching only irrelevant fields.
+
+* For the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler):
+
+  * Test triggering vs. _non-triggering_ paths (by just verifying the content of the event channel, analogous to [`wlUpdateCh`](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L95)).
+
+* For the [MultiKueue Cache](#multikueue-cache):
+
+  * Test the public helper exposed to the [Visibility Server](#visibility-server).
+
+* For the [MultiKueue Workload Reconciler](#multikueue-workload-reconciler):
+
+  * Test the updates to MultiKueue Cache.
+
+* For the [Visibility Server](#visibility-server):
+
+  * Test steps 1-3 of the recipe. (Test 4 will be covered by MultiKueue Cache unit tests).
+
+#### Integration Tests
+
+* For the integration between [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) and [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler):
+
+  * Test triggering the latter by changes in remote LocalQueues and ClusterQueues. \
+    (This could be also unit-tested, by a well-crafted watch interceptor similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go#L69-L70) - but an integration test feels a much cleaner way).
+
+* For the integration between [MultiKueue Workload Reconciler](#multikueue-workload-reconciler) and [Visibility Server](#visibility-server):
+
+  * Test some _relatively simple_ scenario of worker resource stats. \
+    (Where we'd like enough complexity to demonstrate that the resource utilization stats are not just aggregates over the values reported by worker-side ClusterQueues).
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled. Verify that:
+
+  * Manager quota is adjusted, as intended, upon:
+
+    - adding a new worker in MultiKueueConfig
+    - changing a remote ClusterQueueQuota
+    - (if we decide to support that) a worker cluster becoming unreachable
+
+  * New jobs can be admitted at all 3 phases of this scenario (quota automation disabled, then enabled, then disabled again).
+
+* Verify that manager quota auto-adjustments (in either direction: increases and decreases) promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Expand the cross-worker resource stats
+
+The [proposed format] of cross-worker resource stats ([overview](#cross-worker-resource-stats-in-visibility-api), [API](#cross-worker-resource-stats-in-visibility-api-1)) is essentially a collection of 2D arrays (indexed by flavor and resource) returned for a ClusterQueue.
+
+Early insights suggest that we might want to develop this in various directions:
+
+- Expose the above data broken down also per worker. \
+  This would make it effectively a collection of _3D arrays_, posing a question how to arrange those to be best human-readable right away (and whether we should at all aim at that). \
+  Our initial idea is to group by resource, then worker, then flavor, though for now we leave it open for feedback.
+
+- Expose per-LocalQueue stats (like in the existing Visibility API endpoint).
+
+- Support borrowing information, like [here](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359). \
+  This could be less clear in the MultiKueue case, due to the "stray workloads" story. (For example: if the manager-side ClusterQueue `Q` dispatched a 4 CPU Job to ClusterQueue `Q2` on `worker1`, which happens to also host another 6 CPU Job unrelated to MultiKueue, using 8 CPU of nominal quota and borrowing 2 CPU from a Cohort on `worker2`, then what value of `Borrowed` should we report for `Q`? 2 CPU? 0 CPU? Some "proportional fraction" like 0.8 CPU?)
+
+#### Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier
+
+That is, an analogue of the proposed quota mutliplier - but differing in that:
+
+* it would **not affect** the [nominal ClusterQueue quota](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L250) value,
+* it would **not increase** the "total volume" of workloads which can be **admitted** by the manager,
+* it would **only** affect the "total volume" of workloads can **reserve quota** on the manager-side ClusterQueue (and hence, get dispatched to the workers).
+
+For example, if there are 2 workers, each with 10 CPU capacity, the `QuotaMultiplier` (as proposed in this KEP) is 0.8 (to leave 20% of worker capacity for "stray workloads"), and the Quota Reservation Overbooking Multiplier (as discussed here) is 3.0, then the manager-side ClusterQueue:
+
+* will have nominal quota set to (10 + 10) * 0.8 = 16,
+* will admit workloads requesting at most 16 CPU in total,
+* will reserve quota (and hence - dispatch to workers) workloads requesting up to 16 * 3 = 48 CPU in total.
+
+As weird as it seems, this opens a way to "reconcile" various, otherwise clashing, design factors for this KEP; specifically:
+
+* seamless dispatching vs. per-team allocations (see [Drawback #3](#drawbacks)),
+* (if `QuotaMultiplier` from this KEP is `1.0`) seamless dispatching vs. intuitive quotas (see [Drawback #2](#drawbacks)).
+
+Yet, this comes with some **disadvantages**:
+
+* Adding another form of user confusion ("why this ClusterQueue reserved quota above its nominal limit, despite using no borrowing?").
+
+* Adding complexity to core Kueue code. (In particular, we'd need to carefully plan how to enforce the "don't admit over the quota" constraint. Manager-side eviction propagated to workers? But whom to evict?)
+
+* Adding conceptual complexity. (Just look at the section name for this follow-up idea; it's long enough to make it feel like a joke).
+
+* Blurring separation of concerns, by introducing new ways of MultiKueue awareness to core Kueue code.
+
+Therefore, this idea is **shelved** for now. We may consider it later, depending on the feedback.
+
+#### Track borrowable quota on the worker side
+
+The possibility of borrowing within a Cohort clearly influences the actual capacity of a ClusterQueue, and thus could be included in the calculation of "total quota" - **both** in [manager quota automation](#manager-quota-automation) and [cross-worker resource stats](#cross-worker-resource-stats-in-visibility-api).
+
+This is **shelved** for now due to the added complexity, both in implementation and specific desired behavior. Example questions:
+
+* Should we simply increase the total quota by the `borrowingLimit`s on the workers?
+* If borrowing does not allow preemption, should we rather define "borrowable quota" as "the minimum of borrowing limit and current unused capacity on the lending ClusterQueues"?
+* Should we expose an analogue of [the `Borrowed` field](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) in [cross-worker resource stats](#cross-worker-resource-stats-in-visibility-api-1)? Then, if a worker-side ClusterQueue used borrowing to host MultiKueue workloads from a manager ClusterQueue `Q` as well as various ["stray" Workloads](#potential-reasons-for-decreasing-manager-quota), how should we report that?
+
+### Graduation Criteria
+
+This design is covered by two feature gates:
+
+* `MultiKueueMaangerQuotaAutomation` - for the [quota automation part](#manager-quota-automation);
```

  ### 
  ```suggestion
  * `MultiKueueManagerQuotaAutomation` - for the [quota automation part](#manager-quota-automation);
  ```

- **keps/9988-multikueue-global-quotas/README.md:102** by [@kshalot](https://github.com/kshalot) — 2026-04-21T15:54:04Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
```

  I briefly mentioned this offline, but I want to flag it here explicitly.
  
  How do we want to treat transient issues? Consider a case where we have N workers, running at full capacity. Suddenly one of the workers goes down. So we are dealing with an oversubscription, since IIUC we'll update the automated quota on the manager to a lower value. 
  
  Workloads have a `workerLostTimeout` and they will be unadmitted when it elapses, but in the meantime we could potentially encounter a disruption, no? For example a new batch of workloads come in. Since we are oversubscribed, we could end up preempting too many "healthy" workloads to free up quota.
  
  Regardless of whether the proposed solution to this is to have to multiplier set up, I feel that this should be more explicitly considered in this KEP. WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T15:57:40Z

- **keps/9988-multikueue-global-quotas/README.md:350** by [@mimowo](https://github.com/mimowo) — 2026-04-21T15:57:41Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
```

  I prefer to keep it out of scope for 0.18, because the code freeze is only in a month or so. I think we can move it to a follow up KEP, or update to the KEP. We can for now keep it under the Alternatives section.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-22T09:24:08Z

- **keps/9988-multikueue-global-quotas/README.md:350** by [@mimowo](https://github.com/mimowo) — 2026-04-22T09:24:08Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
```

  > We can for now keep it under the Alternatives section.
  
  Or just move it out as "non-goal" as content for another KEP. I think this makes sense as this part has big overlap with other KEPs coming.
  
  Moreover I think the new interface ReconcilerWithFollowerObserver is a good idea on its own we could use in a preparatory refactoring already.

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-04-22T14:49:43Z

- **keps/9988-multikueue-global-quotas/README.md:589** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-04-22T14:49:43Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size, especially remote ones.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+   Remote API calls of that kind should be avoided even more because, even though MultiKueue already watches remote Workloads, it doesn't currently cache them. (And if we enabled client-level caching here, it would have a significantly larger memory footprint than the proposed [MultiKueue Cache](#multikueue-cache)).
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler.
+
+   * The local API calls are backed by `controller-runtime` client-side caching.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q` (using the recipe described [here](#manager-quota-automation), with `QuotaMultiplier` used as the multiplier).
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved on the manager cluster.
+   ManagerClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   wlGroupResourceStats map[workload.Reference]wlGroupResourceInfo
+}
+```
+
+(where `workload.Reference` is defined [here](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/workload/workload.go#L89)).
+
+The `FlavorsUtilization` struct refers to an existing type of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37), which differs from `WorkerFlavorUsage` used on the API surface. This is deliberate: the former representation allows more efficient and convenient summation.
+
+The cache instance will be created in the [MultiKueue `SetupControllers` method](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/controllers.go#L111), and passed down to specific controllers as necessary.
+
+### MultiKueue Workload Reconciler
+
+The MultiKueue Workload Reconciler will be responsible for maintaining the [MultiKueue Cache](#multikueue-cache).
+
+This will be handled in every run of [`Reconcile`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/controller/admissionchecks/multikueue/workload.go#L158), after [this call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L224) to `.readGroup()`. \
+For non-deletion scenarios, it will be postponed to follow [this call](https://github.com/kubernetes-sigs/kueue/blob/083e985b0f750901b813e70a1e168348b21a42f8/pkg/controller/admissionchecks/multikueue/workload.go#L240) to `.reconcileGroup()`. 
+
+On a Kueue restart, the cache will be eventually re-populated, as the controller-runtime library will schedule `Reconcile()` for all pre-existing (local) Workload objects.
+
+To ensure no "permanent zombie entries" in the cache (in a case of a missed delete event), we will add a "garbage collection routine" analogous to [this one](https://github.com/kubernetes-sigs/kueue/blob/249ad53617ee4d2aa0ee6573d4b38418bc549304/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L665). In the Alpha version, we will hook it to the same "GC interval" setting, defined [here](https://github.com/kubernetes-sigs/kueue/blob/9c92d93bf6f1988e59a459623dc9b345c2161ef6/apis/config/v1beta2/configuration_types.go#L302-L305).
+
+#### Ensuring cache availability on non-Kueue-leader Pods
+
+Our plan of populating [MultiKueue Cache](#multikueue-cache) in the MultiKueue Workload Reconciler to be used by [Visibility Server](#visibility-server) may hit obstacles of 2 kinds:
+
+1. If Kueue controller is deployed on multiple Pods, the MultiKueue Workload Controller won't be started on the followers.
+
+   * This obstacle is already present for the Kueue core cache, where it's currently mitigated by making the core controllers ["leader aware"](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), so that the follower instances run only the predicate methods (`.Create()`/ `.Update()` / `.Delete()`) but not the `Reconcile()` method. Then, as long as the cache is populated in predicates, it can be kept up-to-date also on the followers, without the proper `.Reconcile()` methods racing for etcd updates.
+
+   * The above approach is **insufficient** in our case, as [MultiKueue Cache](#multikueue-cache) depends on _remote_ API Workloads which we need to fetch in the `.Reconcile()` method. This logic is too heavy to be moved to predicates.
+
+     Therefore, for our case, we propose a **variation** of [`leader_aware_reconciler`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), which would accept a controller implementing a richer interface
+
+     ```golang
+     interface ReconcilerWithFollowerObserver {
+       TypedReconciler[Request]
+       Observe(context.Context, request) (Result, error)
+     }
+     ```
+
+     and, when about to reconcile an event, delegate to `.Reconcile()` or `.Observe()`, depending on the current leader/follower role.
+
+     Then, we'll make the MultiKueue Workload Reconciler wrapped with that, with the `.Observe()` method responsible only for updating [MultiKueue Cache](#multikueue-cache). (Internally, there will be some code sharing to avoid duplication).
```

  How will this wrapper handle the `func (w *wlReconciler) setupWithManager(mgr ctrl.Manager) error` method? If it relies on the ReconcilerWithFollowerObserver interface, we will not have access to:
  * `w.client`
  * `w.clusters.wlUpdateCh`
  * `w.roleTracker`
  * `w.eventsBatchPeriod`
  which are used in the method to set up the Controller.
  
  How will this interface handle other Reconcilers we would like to update this way in the future (e.g. Cluster Reconciler) - each of them faces similar issues with the setupWithManager method.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-22T15:14:22Z

- **keps/9988-multikueue-global-quotas/README.md:589** by [@olekzabl](https://github.com/olekzabl) — 2026-04-22T14:58:26Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
+
+* **per-flavor utilization** stats (`FlavorsReservation`, `FlavorsUsage`),
+* **per-flavor quota** stats (`FlavorsQuota`).
+
+```go
+// ...
+// +genclient:method=GetMultiKueueWorkerResourceStats,verb=get,subresource=multikueueworkerresourcestats,result=sigs.k8s.io/kueue/apis/visibility/v1beta2.MultiKueueWorkerResourceStats
+type ClusterQueue struct {
+   // ...
+
+   // multiKueueWorkerResourceStats contain aggregated resource stats (quota and utilization)
+   // of the remote workloads dispatched from the given MultiKueue manager ClusterQueue.
+   // Retrieving this field returns an error for any other ClusterQueue,
+   // or when the feature gate MultiKueueWorkerResourceStats is disabled.
+   MultiKueueWorkerResourceStats MultiKueueWorkerResourceStats `json:"multiKueueWorkerResourceStats,omitEmpty"`
+}
+
+type MultiKueueWorkerResourceStats struct {
+   // flavorsReservation are the quotas *reserved* on MultiKueue workers
+   // by the remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsReservation []WorkerFlavorUsage `json:"flavorsReservation,omitempty"`
+
+   // flavorsUsage are the quotas used on MultiKueue workers
+   // by the *admitted* remote clones of workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsUsage []WorkerFlavorUsage `json:"flavorsUsage,omitempty"`
+
+   // flavorsQuotas are the total quotas of all ClusterQueues on the
+   // MultiKueue workers which may serve workloads from this manager ClusterQueue.
+   // They are grouped by worker-side flavors.
+   // +listType=map
+   // +listMapKey=name
+   FlavorsQuotas []WorkerFlavorUsage `json:"flavorsQuotas,omitempty"`
+}
+
+type WorkerFlavorUsage struct {
+	// name of the flavor.
+	// +required
+	Name ResourceFlavorReference `json:"name,omitempty"`
+
+	// resources lists the quota usage for the resources in this flavor.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=64
+	// +required
+	Resources []WorkerResourceUsage `json:"resources,omitempty"`
+}
+
+type WorkerResourceUsage struct {
+	// name of the resource
+	// +required
+	Name corev1.ResourceName `json:"name,omitempty"`
+
+	// total is the total quantity of used quota, including the amount borrowed
+	// from the cohort.
+	// +optional
+	Total resource.Quantity `json:"total,omitempty"`
+}
+```
+
+The `WorkerFlavorUsage` type is deliberately forked from [`FlavorUsage`]((https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L333-L344)), already used in ClusterQueueStatus; the only difference is removing the [`Borrowed`](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L359) field - for which it could be cumbersome to define the indended meaning in a MultiKueue setup (supporting that is [a possible follow-up](#expand-the-cross-worker-resource-stats)).
+
+##### Utilization stats: worker-centric (new) vs. manager-centric (existing)
+
+The above **flavor utilization** stats closely correspond to [already existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), of the same name and type.
+
+While those *existing fields* expose a *manager-centric* view of the resource stats, the **new fields** will differ by being mostly **worker-centric**, specifically:
+
+* grouped by _worker-side_ flavors,
+* tied to the _worker-side_ workload status (quota reserved / admitted),
+* aggregating _worker-side_ quota reservations, that is: if a single manager workload has multiple remotes reserving quota (which is possible following [#8592](https://github.com/kubernetes-sigs/kueue/pull/8592), especially if worker clusters observe long-running AdmissionChecks), we'll add up those quota reservations on all the workers.
+
+In comparison to the [existing manager-centric fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309), we've also removed the limit of list length (set to [64 items](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L299)). This is because the number of [related worker ClusterQueues](#defining-related-worker-clusterqueues) is theoretically unbounded, and each of those [could have different ResourceFlavor names](#treatment-of-resourceflavors).
+
+However, the new flavor utilization stats are **not** going to be just aggregates of the [existing ClusterQueueStatus fields](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309) from all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Doing so would include various kinds of "stray" workloads unrelated to the considered manager ClusterQueue (see [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)). Instead, we will aggregate resource stats **only** for remotes **dispatched from** the given manager ClusterQueue.
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Avoid API calls with O(workload count) response size, especially remote ones.
+
+   This is to avoid processing lots of data at once. For context, workload count can reach hundreds of thousands. \
+   While we'll tolerate some O(workload count) computations, we'll prefer that their input are smaller than full Workload objects, to reduce the total amount of data processed.
+
+   Remote API calls of that kind should be avoided even more because, even though MultiKueue already watches remote Workloads, it doesn't currently cache them. (And if we enabled client-level caching here, it would have a significantly larger memory footprint than the proposed [MultiKueue Cache](#multikueue-cache)).
+
+2. Tolerate API calls (`.Get()` and `.List()`) regarding other Kueue resource types, even on remote clients, and multiple of them.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * Except for Workloads, we'll be dealing with LocalQueues, ClusterQueues and AdmissionChecks, which do not tend to exist in large numbers.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler.
+
+   * The local API calls are backed by `controller-runtime` client-side caching.
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency by extending [MultiKueue Cache](#multikueue-cache).
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and make sure that these filters are covered by API indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using an unfiltered remote API `.List()` call).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using a single unfilterd remote API `.List()` call).
+
+8. Determine the new quota values for `Q` (using the recipe described [here](#manager-quota-automation), with `QuotaMultiplier` used as the multiplier).
+
+9. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+10. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### MultiKueue Cache
+
+A new **MultiKueue Cache** will store the minimal information on the watched remote workloads that is needed to avoid bulk-fetching them from worker clusters. This is needed only for the "resource visibility" part, and will be maintained only when the `MultiKueueWorkerResourceStats` feature gate is enabled.
+
+The cache format will be tentatively as follows:
+
+```go
+type FlavorsUtilization struct {
+   Reserved FlavorResourceQuantities
+   Admitted FlavorResourceQuantities 
+}
+
+type wlGroupResourceInfo struct {
+   // This is not nil if and only if the workload has quota reserved on the manager cluster.
+   ManagerClusterQueue *clusterQueueReference
+   FlavorsUtilization FlavorsUtilization
+}
+
+type MultiKueueCache struct {
+   wlGroupResourceStats map[workload.Reference]wlGroupResourceInfo
+}
+```
+
+(where `workload.Reference` is defined [here](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/workload/workload.go#L89)).
+
+The `FlavorsUtilization` struct refers to an existing type of [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37), which differs from `WorkerFlavorUsage` used on the API surface. This is deliberate: the former representation allows more efficient and convenient summation.
+
+The cache instance will be created in the [MultiKueue `SetupControllers` method](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/controllers.go#L111), and passed down to specific controllers as necessary.
+
+### MultiKueue Workload Reconciler
+
+The MultiKueue Workload Reconciler will be responsible for maintaining the [MultiKueue Cache](#multikueue-cache).
+
+This will be handled in every run of [`Reconcile`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/controller/admissionchecks/multikueue/workload.go#L158), after [this call](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L224) to `.readGroup()`. \
+For non-deletion scenarios, it will be postponed to follow [this call](https://github.com/kubernetes-sigs/kueue/blob/083e985b0f750901b813e70a1e168348b21a42f8/pkg/controller/admissionchecks/multikueue/workload.go#L240) to `.reconcileGroup()`. 
+
+On a Kueue restart, the cache will be eventually re-populated, as the controller-runtime library will schedule `Reconcile()` for all pre-existing (local) Workload objects.
+
+To ensure no "permanent zombie entries" in the cache (in a case of a missed delete event), we will add a "garbage collection routine" analogous to [this one](https://github.com/kubernetes-sigs/kueue/blob/249ad53617ee4d2aa0ee6573d4b38418bc549304/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L665). In the Alpha version, we will hook it to the same "GC interval" setting, defined [here](https://github.com/kubernetes-sigs/kueue/blob/9c92d93bf6f1988e59a459623dc9b345c2161ef6/apis/config/v1beta2/configuration_types.go#L302-L305).
+
+#### Ensuring cache availability on non-Kueue-leader Pods
+
+Our plan of populating [MultiKueue Cache](#multikueue-cache) in the MultiKueue Workload Reconciler to be used by [Visibility Server](#visibility-server) may hit obstacles of 2 kinds:
+
+1. If Kueue controller is deployed on multiple Pods, the MultiKueue Workload Controller won't be started on the followers.
+
+   * This obstacle is already present for the Kueue core cache, where it's currently mitigated by making the core controllers ["leader aware"](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), so that the follower instances run only the predicate methods (`.Create()`/ `.Update()` / `.Delete()`) but not the `Reconcile()` method. Then, as long as the cache is populated in predicates, it can be kept up-to-date also on the followers, without the proper `.Reconcile()` methods racing for etcd updates.
+
+   * The above approach is **insufficient** in our case, as [MultiKueue Cache](#multikueue-cache) depends on _remote_ API Workloads which we need to fetch in the `.Reconcile()` method. This logic is too heavy to be moved to predicates.
+
+     Therefore, for our case, we propose a **variation** of [`leader_aware_reconciler`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/leader_aware_reconciler.go), which would accept a controller implementing a richer interface
+
+     ```golang
+     interface ReconcilerWithFollowerObserver {
+       TypedReconciler[Request]
+       Observe(context.Context, request) (Result, error)
+     }
+     ```
+
+     and, when about to reconcile an event, delegate to `.Reconcile()` or `.Observe()`, depending on the current leader/follower role.
+
+     Then, we'll make the MultiKueue Workload Reconciler wrapped with that, with the `.Observe()` method responsible only for updating [MultiKueue Cache](#multikueue-cache). (Internally, there will be some code sharing to avoid duplication).
```

  I think we can re-use the solution for the existing `leader_aware_reconciler` wrapper. Namely, the wrapper is called around an already-set-up "delegate". This happens here:
  
  https://github.com/kubernetes-sigs/kueue/blob/7ec2aec50bc3bae7469f6816937555fd0e6f832f/pkg/controller/core/leader_aware_reconciler.go#L45-L58
  
  and is used e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/7ec2aec50bc3bae7469f6816937555fd0e6f832f/pkg/controller/core/clusterqueue_controller.go#L606) - so, strictly speaking, the wrapper is called around a delegate reconciler (`r`) that _is just being set up_ with the manager.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-24T10:28:08Z

- **keps/9988-multikueue-global-quotas/README.md:327** by [@olekzabl](https://github.com/olekzabl) — 2026-04-24T10:28:08Z
```diff
@@ -0,0 +1,693 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [MultiKueue Cache](#multikueue-cache)
+    - [Maintaining the cache up to date](#maintaining-the-cache-up-to-date)
+    - [The guiding principles](#the-guiding-principles)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the `MultiKueueManagerQuotaAutomation` Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Store another set of fields in MultiKueue Cache](#store-another-set-of-fields-in-multikueue-cache)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas - which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the worker-side workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats. Essentially, our "raw" data is a multi-dimensional array of numbers (where the dimensions are: worker cluster, worker ClusterQueue, ResourceFlavor, resource name, and the choice of "nominalQuota/reserved/used") - it is yet to be chosen how to arrange that information to be most useful.
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   1. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   1. the exact semantics of the new stats in Visibility API.
+
+1. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+1. Decreasing MultiKueue performance by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates as well as a permanent opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is mitigated by a careful planning of [MultiKueue Cache](#multikueue-cache); see in particular [its guiding principles](#the-guiding-principles).
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfig struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present whenever quota automation is enabled (either by `.Enabled == True` or by the feature gate).
+
+When the manager quota has been automated successfully, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (An alternative approach is discussed [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)).
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
+
+In the Kueue Visbility API, we will add a new [query](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/visibility/storage/storage.go#L26-L31), `clusterqueues/workerresourcestats`, allowing fetching two kinds of resource stats:
```

  Closing here as the Visibility feature will be moved to another KEP.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-24T10:35:40Z

- **keps/9988-multikueue-global-quotas/README.md:102** by [@olekzabl](https://github.com/olekzabl) — 2026-04-24T10:35:40Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
```

  Good question.
  My first impression was that this problem doesn't exist, but indeed, it does.
  
  My response is adding step 4 to [Proposal](https://github.com/olekzabl/kueue/blob/93ea2aeaee5867d149defc69e0a0dc03c697340b/keps/9988-multikueue-global-quotas/README.md#proposal) and correspondingly steps 7-8 to the [MK CQ Reconciler recipe](https://github.com/olekzabl/kueue/blob/93ea2aeaee5867d149defc69e0a0dc03c697340b/keps/9988-multikueue-global-quotas/README.md#multikueue-clusterqueue-reconciler). PTAL.
  
  I think it may be not _perfect_ as it can still lead to some evictions which might _turn out_ to be undesired when the worker comes back.
  OTOH, I think the proposed formula makes sense. When a worker gets disconnected, we give the grace period (`workerLostTimeout`) to whatever is already running there - but we don't dispatch new stuff to it. So, if we ask what's the total mass of workloads that can be "admitted or dispatched" right now - it's just that.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-24T10:43:45Z

- **keps/9988-multikueue-global-quotas/README.md:350** by [@olekzabl](https://github.com/olekzabl) — 2026-04-24T10:43:45Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+The **core** of the proposed solution is to let the MultiKueue controller on the manager cluster **watch** the ClusterQueue and LocalQueue objects in the workers. (Just like it already watches other worker objects, e.g. Workloads).
+
+Based on that shared core, we propose **two enhancements** of the user-facing APIs, described below.
+
+### Manager quota automation
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 2](#story-2). Also, specifying a very large multiplier is technically a way to cover [User Story 3](#story-3)).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 4](#story-4)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Cross-worker resource stats in Visibility API
+
+In the existing [Visibility API](https://github.com/kubernetes-sigs/kueue/tree/main/keps/168-2-pending-workloads-visibility), we will add a new endpoint, exposing **cross-worker resource stats** for a given MultiKueue manager ClusterQueue.
+
+In the Alpha stage, we plan this endpoint to surface the following information:
+
+* `FlavorsReservation` and `FlavorsUsage` - in the same format and analogous meaning as [already used in ClusterQueueStatus](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L294-L309); however, based on the **worker-side** workload statuses and flavor assignments.
+
+* The values of per-flavor quotas, aggregated from the specs of all [related worker ClusterQueues](#defining-related-worker-clusterqueues), exposed in the same format as the above usage stats.
+
+Depending on the user feedback from the Alpha stage, we may decide to provide analogous resource stats also in other formats (see [here](#expand-the-cross-worker-resource-stats) for details).
+
+### User stories
+
+#### Story 1
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+#### Story 2
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 4
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier;
+   3. the exact semantics of the new stats in Visibility API.
+
+2. Introducing management of ClusterQueue quotas which some users consider undesired (see [User Story 4](#story-4)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+4. Consuming too much memory with MultiKueue cache.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risk 1.iii is mitigated by meaningful comments on the new API fields.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 can be substantially mitigated by extending [MultiKueue Cache](#multikueue-cache); see our [guiding principles](#guiding-principles-for-the-implementation).
+
+* Risk 4 feels low: even though [MultiKueue Cache](#multikueue-cache) scales linearly with the number of all workloads (which can grow large), the amount of data stored per workload is low, likely below what's in the Kueue scheduler cache. \
+  Still, if this turns out problematic, the current cache format can be drastically compressed by tweaking [`FlavorResourceQuantities`](https://github.com/kubernetes-sigs/kueue/blob/3f0c4b2884fe5577d7a7ae4c3579a49718077be3/pkg/resources/resource.go#L37) to use short map keys (instead of `Flavor` and `Resource`, use their indices in some global map; these will certainly fit in an `int32`). \
+  For now, it feels a premature optimization, so we don't propose it right away.
+
+## Design details
+
+### API surface
+
+#### Manager quota automation
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+#### Cross-worker resource stats in Visibility API
```

  I removed the visibility part from this KEP.
  
  As for `ReconcilerWithFollowerObserver`, this belonged to the visibility scope, so we can come back to this in another KEP which I'll post soon.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-24T13:59:53Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@amy](https://github.com/amy) — 2026-04-24T13:59:53Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  Will take a deeper dive today. I understand the motivations for the multiplier (ie preventing dispatcher from blocking admission in the example below `Divergence of quota reservation`), but initial reaction on skim is that it doesn't feel clean and is confusing. I do agree that we need something to sync quotas though.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-24T21:53:18Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@amy](https://github.com/amy) — 2026-04-24T21:53:18Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  I would separate the multiplier from this kep as a smaller kep after you get auto-aggregating quotas working/merged. To me they're separate. Combining them bloats this KEP.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-24T22:40:19Z

- **keps/9988-multikueue-manager-quota-automation/README.md:261** by [@amy](https://github.com/amy) — 2026-04-24T22:40:19Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
```

  @olekzabl So actually for this example... the real problem is that we're not representing the capacity on a per worker CQ basis and instead representing it as an aggregation. And that's why we need a multiplier. 
  
  For instance, taking this example, you have in aggregate 20 CPU. A user makes an error and submits multiple 20 CPU workloads and then a 3CPU workload. Locking up all your capacity. Multiplier wouldnt really fix this if the approximation is too low.
  
  You really want to know "what is the largest remaining size per worker CQ that's allowed?"
  
  In your example. You could flip it and do something like:
  - wl3 arrives: "does any worker have a 6 CPU slot?" -> no -> don't block the queue                                                     
  - wl4 arrives: "does any worker have a 3 CPU slot?" -> yes -> admit it
  
  So I wonder if there's a way to introduce something thats not a cohort (bc its not about sharing). But actually a DispatchQueue submission. Where it says:
  - in aggregate I have 20 CPU
  - MultiKueue Total: {cpu: 20} 
    worker1: {cpu: 10, available: 3}                                                                                                     
    worker2: {cpu: 10, available: 3}
  
  Maybe still need to think through the details. But the gist is... you maybe shouldnt be modeling all the worker CQs as one giant queue. You want to keep it as separate worker queues, but have a wrapper around it to reason about it and make submission work. 
  
  Like this problem is probably more similar to flavor fragmentation except at a Control/worker cluster level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T08:07:31Z

- **keps/9988-multikueue-global-quotas/README.md:131** by [@mimowo](https://github.com/mimowo) — 2026-04-27T08:07:32Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
```

  AFAIK this is still a speculative part of the story. Let me check if this is actually used by the practitioners, on slack.
  
  I would rather prefer to de-couple this speculative part of the story as a substory, or another story on its own.
  
  cc @MichalZylinski, @mwielgus

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-27T08:19:31Z

- **keps/9988-multikueue-global-quotas/README.md:102** by [@kshalot](https://github.com/kshalot) — 2026-04-27T08:19:31Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
```

  I think this is an improvement and the right approach, as it preserves the existing "grace period" semantics for the workloads. That being said, maybe you could flag this edge-case in the KEP explicitly?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T08:43:09Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T08:43:09Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  > _initial reaction on skim is that it doesn't feel clean and is confusing_
  
  Agreed. I'm not happy about this multiplier. 
  
  > _I would separate the multiplier from this kep. (...) To me they're separate. Combining them bloats this KEP._
  
  Here I don't agree.
  If we implement just aggregation without the multiplier, it would actually be a _degradation_ for almost everyone. (This is the bad news from the ["Divergence of quota reservation" story](https://github.com/olekzabl/kueue/blob/b0eb72c8999d5eb6d5a74a3862398125493278e9/keps/9988-multikueue-global-quotas/README.md#potential-reasons-for-increasing-manager-quota)).
  So, on top of auto-aggregating, I think we *need* "Something" to make it useful (at least for a large part of users), even in the early version.
  
  We've brainstormed (with @mwielgus and @mimowo ) a few options of what that "Something" could be, and we found nothing better than this.
  I now expanded [one of Alternatives](https://github.com/olekzabl/kueue/blob/3fb8cd9a6c33f9fffbbd851229d85abbe7dfc6ee/keps/9988-multikueue-global-quotas/README.md#avoid-the-quota-automation-multiplier) discussing that; PTAL.
  
  (Side note: I'm still warmly thinking of the [quota reservation overbooking multiplier](https://github.com/olekzabl/kueue/blob/3fb8cd9a6c33f9fffbbd851229d85abbe7dfc6ee/keps/9988-multikueue-global-quotas/README.md#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier) instead of the proposed one, as I'm feeling this would at least make the quota values on the manager not confusing. However, this comes with its own disadvantages, and so far I've received feedback leaning against this).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T08:52:05Z

- **keps/9988-multikueue-manager-quota-automation/README.md:261** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T08:52:05Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
```

  Oh yes, this vision is very appealing.
  
  Yet, IIUC, we need to be _very careful_ about introducing such improvements, (at least) due to potential performance bottlenecks. (I've tried to discuss this in item 3 in [this subsection](https://github.com/olekzabl/kueue/blob/3fb8cd9a6c33f9fffbbd851229d85abbe7dfc6ee/keps/9988-multikueue-global-quotas/README.md#manager-vs-workers-separation) of "Motivation").
  
  Basically: MultiKueue is in a large part about scalability, and making scheduling decisions is already known to be a scalability bottleneck for Kueue. Therefore, we can't afford having a manager that is "too smart", i.e. knowing "too much" about the workers' setup. What is "too smart" remains to be verified, most likely, in benchmarks. Benchmarking MultiKueue is a task we recently started (cc @kshalot ) but I think we'd need a longer thought and experimenting before coming to a proposal here.
  
  Therefore, our tactical approach is to propose some simpler syncing mechanism before the above happens.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T09:05:18Z

- **keps/9988-multikueue-global-quotas/README.md:131** by [@mimowo](https://github.com/mimowo) — 2026-04-27T09:05:18Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
```

  asked the community on slack to guide the multiplier: https://kubernetes.slack.com/archives/C032ZE66A2X/p1777280674564329

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T09:08:04Z

- **keps/9988-multikueue-manager-quota-automation/README.md:261** by [@mimowo](https://github.com/mimowo) — 2026-04-27T09:08:04Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
```

  > You really want to know "what is the largest remaining size per worker CQ that's allowed?"
  
  This is a good question to ask, but I think it aims at a bit different problem than the KEP is attempting to, and different problem than quotaMultiplier. 
  
  This problem aims with this question is that some inadmissible workloads at the worker level may get admitted at the manager level, and this problem exists no matter if the quota is synced manually or in an automated fashion. So, I think this is a good topic for another issue / KEP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T09:10:23Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@mimowo](https://github.com/mimowo) — 2026-04-27T09:10:23Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  > Agreed. I'm not happy about this multiplier.
  
  Let's see if this is really expected by some MultiKueue practitioners: https://kubernetes.slack.com/archives/C032ZE66A2X/p1777280674564329 who already use MultiKueue -how they sync the quota currently in the manual way.
  
  I'm not happy about that either, so I would consider this like a follow up if needed.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T09:30:16Z

- **keps/9988-multikueue-global-quotas/README.md:274** by [@mimowo](https://github.com/mimowo) — 2026-04-27T09:30:17Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
```

  I can certainly understand the motivation for the "enabled: true" field like this, but currently such fields are not recommended in the core k8s https://github.com/kubernetes/community/blob/main/contributors/devel/sig-architecture/api-conventions.md#primitive-types. 
  
  Some ideas that come to me:
  
  ```yaml
  spec:
     quotaSync: Manual / Automated// enum
     quotaAutomation:
        // params specific for the quota automation
  ```
  or:
  ```yaml
  spec:
     quotaAutomation: Disabled / Enabled // enum
     quotaAutomationConfig:
        // params specific for the quota automation
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T09:51:01Z

- **keps/9988-multikueue-global-quotas/README.md:280** by [@mimowo](https://github.com/mimowo) — 2026-04-27T09:51:02Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
```

  k8s conventions on API discourage float fields: https://github.com/kubernetes/community/blob/main/contributors/devel/sig-architecture/api-conventions.md#primitive-types
  
  I would rather replace it (if we need the multiplier field at all) with 
  
  ```yaml
  quotaMultiplierPercentValue: 130
  quotaMultiplierMillisValue: 1300
  ```
  both would use ints

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T10:15:42Z

- **keps/9988-multikueue-global-quotas/README.md:279** by [@mimowo](https://github.com/mimowo) — 2026-04-27T10:15:42Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
```

  I think the default should be 1, the other +/- is still a speculative user story in my mind. I would also like to add some validation that only +/- 50% is possible at max.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T10:18:31Z

- **keps/9988-multikueue-global-quotas/README.md:313** by [@mimowo](https://github.com/mimowo) — 2026-04-27T10:18:31Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
```

  I would rather call it more generically like "UnsupportedConfiguration" I don't think MultipleFlavors are intrinsically a problem, because:
  1. we may need to disable also the automation in case of Cohorts (for now)
  2. I don't think MultipleFlavors is intrinsically problematic. I think we may atually need two flavors: TAS/non-TAS, or even more flavors per TAS type.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T10:29:57Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-27T10:29:57Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  I'm still wondering about TAS here. For TAS we may need to have another flavor at the manager. 
  
  Assume you have only one flavor on the manager, which is non-TAS, then IIUC the TAS workloads will get blocked here:  https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/flavorassigner/tas_flavorassigner.go#L186
  
  Unless you will relax this validation? In that case we should call this explicitly in the DesignDetails I thinks. The alternative approach, not sure if better, but seems more intuitive to me, would be to support multiple TAS flavors on the management cluster.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T10:49:01Z

- **keps/9988-multikueue-global-quotas/README.md:280** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T10:49:01Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
```

  Thank you for flagging.
  
  How about `resource.Quantity` then? I'm feeling it'd be clearer to see
  ```
  multiplier: 3
  multiplier: 1500m
  ```
  than
  ```
  multiplierPercentValue: 300
  multiplierPercentValue: 150
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T10:50:49Z

- **keps/9988-multikueue-global-quotas/README.md:279** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T10:50:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
```

  > I think the default should be 1
  
  The whole point of having this multiplier proposed in this KEP (and not as some possible follow-up) is that we believe the value 1 to be _wrong_ for most users.
  
  > I would also like to add some validation that only +/- 50% is possible at max.
  
  3 was suggested by @mwielgus who claimed it should be roughly that large.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:03:10Z

- **keps/9988-multikueue-global-quotas/README.md:318** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:03:10Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
```

  I think these sections here (and "MultiKueue ClusterQueue Reconciler") are too detailed for KEP. 
  
  Yes, it is ok to include some of the implementation overview in the KEP, but it would rather be **declarative** listing the responsibilities of various components, rather than **procedural** description of their work.
  
  The reason is simple: KEP maintanance overhead, becasue:
  - the points describing the Reconciler work are likely to change as we extend the KEP with new features. 
  - some implementation details are only figured out / discovered when working with the code.
  
  I fully agree it is good to thing e2e of where code needs to be changed, but for that usually prototypes are a good idea, or external design docs linked.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:05:54Z

- **keps/9988-multikueue-global-quotas/README.md:114** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:05:55Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
```

  This is too detailed for the "Proposal" section. It seems ok as a section like "Handling worker disconnection" in Design Details.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:07:49Z

- **keps/9988-multikueue-global-quotas/README.md:111** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:07:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
```

  I think this is too detailed, and is repetition of the Design Details section. What we want to have here is that we introduce a new API in MultiKueueConfig, and the mechanism, to automatically set the quota in the manager "reasonably".

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T11:09:49Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T11:09:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  I do not intend to relax this validation.
  
  I remember we discussed this topic, and it's already covered in [this Alternative section](https://github.com/olekzabl/kueue/blob/3fb8cd9a6c33f9fffbbd851229d85abbe7dfc6ee/keps/9988-multikueue-global-quotas/README.md#support-quota-automation-for-multiple-manager-side-resourceflavors).
  
  What's your vision to make this work? Is it "tolerate multiple manager flavors under the assumption that flavors (except for quota values) are _identical_ between the manager and all the workers"? And in that case, aggregate quotas per Flavor, and let the manager pick the Flavor?
  
  Though even then, what if there are 2 Flavors A, B which are not-entirely-conflicting? (Say, they have a shared resource name, and a shared topology level name, so that a workload caring only for those could fit both flavors. Or, assuming that TAS-ignorant workloads may arrive, it's even sufficient that A and B have a shared resource name).
  Then, such a workload may be admitted on A on the manager but on B on a worker. (AFAIU nothing will prevent that). And then, by aggregating quotas per Flavor, we'll get the totals skewed.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T11:19:13Z

- **keps/9988-multikueue-global-quotas/README.md:313** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T11:19:13Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
```

  > 1. we may need to disable also the automation in case of Cohorts (for now)
  
  If we decide so, it will sound like a good case for another `reason`.
  Why not make `reason` (roughly) informative for the users?
  
  > 2. I don't think MultipleFlavors is intrinsically problematic. 
  
  I do claim it is intrinsically problematic.
  See the [discussion in KEP](https://github.com/olekzabl/kueue/blob/3fb8cd9a6c33f9fffbbd851229d85abbe7dfc6ee/keps/9988-multikueue-global-quotas/README.md#support-quota-automation-for-multiple-manager-side-resourceflavors) and [in another comment](https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146579187).
  
  > I think we may atually need two flavors: TAS/non-TAS
  
  Such a need I don't see.
  If anyone would need 1 TAS flavor and 1 non-TAS flavor on the manager, the TAS flavor should cover well for both (of course, with quotas summed up). Because it will be capable of admitting also the non-TAS workloads.
  
  > or even more flavors per TAS type.
  
  See [the other comment thread](https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146579187) specifically on this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:21:59Z

- **keps/9988-multikueue-manager-quota-automation/README.md:311** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:21:59Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
```

  I think the KEP could benefit from some examples, I'm not yet totally sure which section. 
  
  Maybe we could start with a simple case:
  ```yaml
    resourceGroups:
    - coveredResources: ["cpu", "memory"]
      flavors:
      - name: "default-flavor"
        resources:
        - name: "cpu"
          nominalQuota: 9
        - name: "memory"
          nominalQuota: 36Gi
    - coveredResources: ["nvidia.com/gpu"]
      flavors:
      - name: "gpu-a4"
        resources:
        - name: "nvidia.com/gpu"
          nominalQuota: 9
      - name: "gpu-h100"
        resources:
        - name: "nvidia.com/gpu"
          nominalQuota: 9
  ```
  Then after aggregation I have the management cluster:
  
  ```yaml
    resourceGroups:
    - coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
      flavors:
      - name: "default-flavor"
        resources:
        - name: "cpu"
          nominalQuota: 18
        - name: "memory"
          nominalQuota: 72Gi
        - name: "nvidia.com/gpu"
          nominalQuota: 36
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T11:30:43Z

- **keps/9988-multikueue-global-quotas/README.md:318** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T11:30:43Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
```

  I would understand this point for "MK CQ Reconciler". 
  (Though even there, I'd still argue that I saw details of analogous level in recent KEPs I reviewed, and I considered those valuable, even without a prototype).
  
  However, in this particular section ("Guiding principles"), I disagree more fundamentally :)
  I claim this section **is** declarative (not "procedural").
  Listing maybe not "responsibilities" but "principles" on which we want to base.
  And my goal is to have this aligned on even before prototyping (as otherwise prototyping could be just wasted effort).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:38:21Z

- **keps/9988-multikueue-global-quotas/README.md:280** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:38:21Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
```

  This is a possible alternative, but `resource.Quantity` is typically used for resource / quota, not dimentionless numbers. 
  
  For example with resource.Quantity we can say "0.1Mi" which is technically correct but would be misleading to specify "102.4" (iiuc). 
  
  If we just need "percent" resolution then we have some examples for using int32 with the "percent" / "percentage". The best known would be in the core k8s for P&F, like: https://github.com/kubernetes/kubernetes/blob/75d51c440770f96b9725efea4145ba50fc6aef4b/pkg/apis/flowcontrol/types.go#L483-L492, https://github.com/kubernetes/kubernetes/blob/75d51c440770f96b9725efea4145ba50fc6aef4b/pkg/apis/flowcontrol/types.go#L459
  
  However, I understand these use cases are more about small "percent" rather than generic "multiplier" which could be >100 percent.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T11:46:50Z

- **keps/9988-multikueue-global-quotas/README.md:279** by [@mimowo](https://github.com/mimowo) — 2026-04-27T11:46:50Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
```

  > The whole point of having this multiplier proposed in this KEP (and not as some possible follow-up) is that we believe the value 1 to be wrong for most users.
  
  In my understanding we introduce the multiplier to support some speculative use cases, which may be or not there. This does not imply the value "3" is generally better than "1" for most users.
  
  The claim that "3" is better than "1" and counter-intuitive to me, and thus extraordinary. Extraordinary claims require extraordinary evidence, so I would like to collect such feedback from users. We already have some adoption of MultiKueue, so let's explore such channels, to learn how they currently establish the quotas on the manager cluster manually. I started the discussion on slack: https://kubernetes.slack.com/archives/C032ZE66A2X/p1777280674564329

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T12:08:14Z

- **keps/9988-multikueue-global-quotas/README.md:318** by [@mimowo](https://github.com/mimowo) — 2026-04-27T12:08:14Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
```

  > I would understand this point for "MK CQ Reconciler".
  
  Yes, it is the "MK CQ Reconciler" section that made me make post this comment.
  
  Once I decided to make the comment I returned back to place it on "Guiding principles for the implementation", because it also feels too detailed, but still "on the edge" (indeed, for a different reason, see below).
  
  > And my goal is to have this aligned on even before prototyping (as otherwise prototyping could be just wasted effort).
  
  I agree with the principles, but I don't get from them more than: "accept suboptimal performance practices  like infrequent requests on remote clients, until proven that we need to optimize. Once proven use caching.".  This is captured by generic enginesesing principles like KISS, and avoid premature optimization. 
  
  Yes, they add a bit more details which kind of optimizations are deferred for later, but this still feels like generic "Contributor guidelines".
  
  If you feel strongly this should be stated explicitly in this KEP, then ok. I'm just trying to minimize the unnecessary parts so that me (and other reviewers) can put all our attention into the relevant parts of the KEP.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T13:55:59Z

- **keps/9988-multikueue-global-quotas/README.md:313** by [@mimowo](https://github.com/mimowo) — 2026-04-27T13:55:59Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
```

  > If we decide so, it will sound like a good case for another reason.
  > Why not make reason (roughly) informative for the users?
  
  If this is to be informative to users, then the `message` field should be sufficient.  The `reason` field is typically for automation. 
  
  I'm not sure if automation needs to really read this "Reason". I think this is mostly something that the user will read only anyway.
  
  > I do claim it is intrinsically problematic.
  > See the discussion in KEP and in another comment.
  
  To show there are use cases: I've seen deployments, production when users would send to the same CQ GPU and CPU workloads, assuming they would land only on one of the flavors. For example see this issue: https://github.com/kubernetes-sigs/kueue/issues/8095
  
  I think having two separate quotas for GPU and CPU at the manager level is reasonable for such a deployment. Otherwise if GPU is saturated already then the manager will keep admitting GPU workloads just because it thinks there is still more of the "virtual CPU + GPU" resource. As a result the GPU workload may block the CPU processing even more. 
  
  I totally agree supporting multiple flavors is complex, and opens difficult questions. And I'm totally ok to start with a single flavor for Alpha. 
  
  I just want the "reason" to sound less strong about "MultipleFlavors" are the problem, but "We currently do not support the configuration with multiple flavors".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T14:47:34Z

- **keps/9988-multikueue-global-quotas/README.md:279** by [@mimowo](https://github.com/mimowo) — 2026-04-27T14:47:34Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
```

  Also, note that our documentation is quite clear recommending our users 1: https://kueue.sigs.k8s.io/docs/concepts/multikueue/#manager-cluster
  
  So I think it is more sensible for our defaults to correspond to the recommendation. Still I'm ok to introduce the `multiplier` to work within some reasonable renge: [50% - 200$], 300% seems like a lot. We can always relax validation if someone requests. We will then also get a clear signal that someone needs high value. If we open it, then we may never know what is really used.
  
  The quota set for the flavors of a ClusterQueue determines how many jobs are eligible for dispatching at a given time. Ideally, the quota in the manager cluster should equal the total quota available in all worker clusters:
  
  If the manager’s quota is significantly lower, worker clusters may remain underutilized.
  If the manager’s quota is significantly higher, it may dispatch and monitor workloads that are unlikely to be admitted in the worker clusters.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T15:08:50Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-27T15:08:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  > I do not intend to relax this validation.
  > I remember we discussed this topic, and it's already covered in this Alternative section.
  
  Good point, and thank you for reminding me about it - the idea is to use TAS flavors for all workloads. 
  
  This might work indeed, but there are two things which make me anxious about this approach:
  1. if different TAS flavors use different topology levels, then on the management cluster we would need to create an "uber" topology containing all levels from all clusters / flavors. This feels weird. 
  2. There might be more issues which are hard to predict, because using TAS flavors for non-TAS workloads is not a common practice currently, so I missed this as an option.
  
  > What's your vision to make this work? Is it "tolerate multiple manager flavors under the assumption that flavors (except for quota values) are identical between the manager and all the workers"? 
  
  We don't need to tolerate, I was rather thinking about validation if the assumption is satisfied. I assume if the assumptions are not satisfied then we don't synchornize and say "UnsupportedConfiguration".
  
  One idea I have:
  1. build an undirectional graph, nodes are resources, edges are added if the two resources are joint by a resource on one of the workers
  2. find connected components in the graph and they will determine the new list of disjoint flavors
  3. aggregate the quotas within the connected components for the flavors
  
  > And in that case, aggregate quotas per Flavor, and let the manager pick the Flavor?
  
  Yes, my ideas is that the flavor is picked by the manager, as currently, but the choice will agree with the worker choice. Thus, the idea of splitting into connected components.
  
  So for the example in https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146857868 I think it is reasonable to have 2 flavors:
  A) cpu_memory  
  b) gpu // put together quotas for l4 and h100
  
  > Though even then, what if there are 2 Flavors A, B which are not-entirely-conflicting? (Say, they have a shared resource name, and a shared topology level name, so that a workload caring only for those could fit both flavors. Or, assuming that TAS-ignorant workloads may arrive, it's even sufficient that A and B have a shared resource name).
  
  If they have a shared resource name then indeed we need to combine. Still, the typical case will be to have CPU,mem flavor which is non-TAS, and gpu which is TAS. In this case the split should work well based on the resource graph.
  
  > Then, such a workload may be admitted on A on the manager but on B on a worker. (AFAIU nothing will prevent that). And then, by aggregating quotas per Flavor, we'll get the totals skewed.
  
  Yes, I get the point, still I think it is reasonable to split the flavors into connected components as suggested above.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T15:31:49Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T15:31:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  Thank you for a thorough explanation.
  
  However, if we're considering manager-side flavors which are mutually disjoint in terms of resource sets... then why not simply combine them into one?
  
  IIUC your motivating example is sth like
  ```
  Flavor1:
  - topologyName: top1
  - quotas:
    - gpu: 10
  Flavor2:
  - quotas:
    - cpu: 100
    - memory: 200Gi
  ```
  
  Then, how is it functionally different from a combined one:
  ```
  CombinedFlavor:
  - topologyName: top1
  - quotas:
    - gpu: 10
    - cpu: 100
    - memory: 200Gi
  ```
  
  My thinking is:
  1. As long as a user has the original Flavors setup on workers, they'll only have workloads which request "GPU only" or "CPU / memory only".
  2. And then, any such workload can get through `CombinedFlavor` just same way as it would through `Flavor1` / `Flavor2`
  3. A theoretical disadvantage would be that `CombinedFlavor` would let through some "broken workloads" requesting both GPU & CPU, but that in my eyes feels a minor risk (given point 1). \
     (Keeping in mind that, if such workloads can exist in a legit way, it constitutes an "edge" between CPU and GPU in your proposal, thus unifying both Flavors already in your proposal).
  4. Then, what else could be the problem?
     (TAS scheduling latency wasted on the manager for CPU-only workloads? I'm hoping MultiKueue quota reservation on the manager is a super-light logic, without TAS logic actually invoked. Becasue running TAS logic on MultiKueue manager side seems useless).
  
  What have I missed? :)

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T15:48:13Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T15:48:13Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  P.S. One good point which I recall you made was that Kueue might one day block admitting non-TAS workloads to TAS flavors.
  
  If that happens, then I'll indeed vote for the proposal from your last comment above.
  However, before that happens, I don't yet see the value justifying such an effort.
  
  If this understanding is correct, then I'm happy to add a "Possible Follow-Up" regarding that.
  (Though for now I'll wait for your response).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T16:13:03Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-27T16:13:03Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  I think this works indeed in my example. I don't see what you are missing, I guess I was just trying to avoid the combining in itself, because it creates the Manager and Worker setup more different than needed. 
  
  So, IIUC in case of disjoint resources there is no benefit of combining either, except for "simplicity" of having a single flavor, or am I missing here that keeping the flavors split has drawbacks?
  
  Thinking more about scenarios where the "single flavor" assumption may not be sufficient:
  1. flavors which require different topologies, say GPU & TPU, but I've seen different topologies used for GPU workloads
  2. ResourceTransformations for x-flavor credits https://kueue.sigs.k8s.io/docs/tasks/manage/share_quotas_across_flavors

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T16:39:22Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T16:39:21Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  > _So, IIUC in case of disjoint resources there is no benefit of combining either, except for "simplicity" of having a single flavor, or am I missing here that keeping the flavors split has drawbacks?_
  
  Functionally - yes, no benefit either way. 
  
  I've so far preferred combining because (in my eyes) it makes this feature overall simpler:
  - No need to build & consider the graph of "resource correlations" which you mentioned
  - No need to think how to fit the manager into that picture, e.g.:
    - Shall "resource co-occurrence" on the manager side also constitute an edge in the graph?
      (I'm assuming "yes". Otherwise we get another bag of questions like "how to define manager-workers compatibility", "how to signal incompatibility in the Condition message", etc.)
    - What if "manager graph" has smaller components than "workers graph"?
      (Shall we then _split_ quotas into "sub-flavors"?
      E.g. {gpu: 10, cpu: 100} on worker ---> {gpu: 10}, {cpu: 100} on the manager?)
    - What if resource X appears on (some) workers but the manager does not know it at all?
      (Track by graph component; ignore if isolated, or if component is split on the manager?
      E.g. {gpu, cpu, mem, tpu} on worker, while manager has {gpu}, {cpu, mem} -> ignore TPU?)
  - No need to think how to treat multiple ResourceGroups on the manager, where:
    - Allowing them feels feasible but would add some (minor) complexity to the code
    - Disallowing them feels similarly arbitrary as disallowing multiple ResourceFlavors
  
  So altogether, plenty of questions get trivialized.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T17:00:43Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-27T17:00:43Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  > No need to think how to fit the manager into that picture, e.g.:
  
  I would just ignore the "manager graph". Whatever is on the workers determines the flavors on the manager. If the flavors are changed then we override what is on the manager. I think the same is true for the CombinedFlavor experience. In practice I expect the two approaches will not differ much. My approach will pretty much combine everything into 3 groups: cpu/gpu/tpu, putting together different families of gpus or tpus.
  
  > So altogether, plenty of questions get trivialized.
  
  Yes, given the simplicity of the proposed solution I"m totally ok to try with it as Alpha. 
  
  I'm just not convinced "multiple flavors" are intrinsically problematic as mentioned in https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146843738.
  
  The single flavor approach might be too simplifying for some use cases as mentioned in the two points above:  https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3148692333. Curious what are your thoughts on them.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T23:11:26Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T23:11:26Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  > _I would just ignore the "manager graph". Whatever is on the workers determines the flavors on the manager. If the flavors are changed then we override what is on the manager._
  
  Hmm, really? Just override?
  
  So imagine there are 10 workers with {gpu}, {cpu}, {tpu}, but then someone registers (maybe by mistake) a 11th worker which has a different set of flavors, {gpu, cpu, tpu} in a single flavor.
  This collapses all graph components into a single one.
  And now, does "override" mean that you'd react by switching the manager-side CQ from the {gpu}, {cpu}, {tpu} flavors to a single {gpu, cpu, tpu} flavor (possibly having deleted the former and created the latter)?
  This feels invasive.
  
  Am I suspecting correctly that you actually wouldn't _ignore_ the "manager graph" but you'd rather _assume_ that it's _identical_ to the "workers graph" (and _refuse_ to automate otherwise)?
  This would be less invasive: never modifying flavors or CQ <-> flavor relations.
  OTOH this would mean that adding a new worker may "break" quota automation.
  
  > _I think the same is true for the CombinedFlavor experience. In practice I expect the two approaches will not differ much._
  
  The CombinedFlavor experience would:
  1. Never modify flavors or CQ <-> flavor relations.
     (Instead, just check if manager has single flavor. Yes -> automate, no -> refuse).
  2. Never disable automation due to worker-side changes.
  
  Here I think (1) is essential; (2) is maybe not so important but still nice to have.
  
  > Thinking more about scenarios where the "single flavor" assumption may not be sufficient:
  > 
  > 1. flavors which require different topologies, say GPU & TPU, but I've seen different topologies used for GPU workloads
  
  IIUC this is the case which I had mentioned in the "Treatment of ResourceFlavors" section:
  
  _Theoretical cases when it would be needed include:_
  
  * (...)
  * _Using TAS on the workers, with incompatible names of topology levels on different workers._
  
  Yes, that would require supporting multiple manager flavors.
  I can promote that remark to a Possible Follow-Up.
  (Yet, before any further work on this, I'd need hints from interested users about which semantics would be actually intended for them. Because, as the conversation above shows, defining the desired behavior seems cumbersome).
  
  > 2. ResourceTransformations for x-flavor credits https://kueue.sigs.k8s.io/docs/tasks/manage/share_quotas_across_flavors
  
  Thank you for this link (I didn't know this feature, and it is interesting).
  
  Yet, I don't yet see how this feature could pose problems in our context.
  These transformations are global, and additive (T(A) + T(B) = T(A + B)), so CombinedFlavor seems still feasible whenever it would be feasible without the transformations.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-04-27T23:21:05Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-04-27T23:21:05Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  Side note: 
  
  Now that you made me aware of [KEP-2937](https://github.com/kubernetes-sigs/kueue/tree/main/keps/2937-resource-transformer), I'm curious how it interplays with MultiKueue in general :)
  
  For example: what if we have 1 manager & 1 worker, and each CPU costs 10 credits on the manager but 100 credits on the worker? When we dispatch a Job requesting 2 CPU via MultiKueue, what will happen?
  
  My quick guess based on glancing through the code:
  
  - The Job will create a local Workload, requesting 20 credits ("manager's pricing")
  - The remote Workload will clone the local one, hence also requesting 20 credits
  - Then, the remote Job will clone the local one, hence requesting 2 CPUs.
  
  Summing up: MultiKueue seems to _ignore_ "worker's pricings".
  I'm wondering if that's indeed what happens (should be easy to check in practice) and if there are any problems with this.
  
  Though my first impression is that _some form_ of confusion _must happen_ for clashing pricings :|
  
  Thoughts?

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-28T00:53:26Z

- **keps/9988-multikueue-manager-quota-automation/README.md:265** by [@amy](https://github.com/amy) — 2026-04-28T00:53:26Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
```

  Back to this example. So wl4 will get scheduled, understood. Can you explain what happens to wl3 if you increase capacity to 23 or more? 
  
  So 7, 7, 6, 3, capacity 46 (2 x 23):
  - {wl1, 7} -> admitted on manager, lands on worker-1
  - {wl2, 7} -> admitted on manager, lands on worker-2
  - {wl3, 6} -> admitted on manager only?
  - {wl4, 3} -> admitted on manager, lands on worker-1
  
  So then... wl3 hits some sort of a timeout? Or... i guess the wl3 copies sit there indefinitely until it can be admitted by a worker?

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-28T01:31:08Z

- **keps/9988-multikueue-global-quotas/README.md:318** by [@amy](https://github.com/amy) — 2026-04-28T01:31:08Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
```

  Yeah... there's a lot detail here. I'd remove these sections:
  - Generously tolerate API calls for resources which typically don't exist in large numbers.
  - Still, to keep up with Kueue code standards, we will
  - MultiKueue ClusterQueue Reconciler
  - MultiKueue Cluster Reconciler
  
  Tbh you can shave down some of the testing section. I like when people keep testing section to testing stuff that's not obvious. Because its not like you won't be involved in the implementation to see this vision through as either implementer or reviewer for the obvious bits.
  
  If you really want principals, pull it out and generalize Kueue developer principals in the docs or something. Even better make a new reviewer or implementation skill in the new AGENTS.md file (shameless plug 😂) Or a new linting rule if thats possible for cicd. 
  
  More seriously, these all seem generalizable and principals people tend to follow for controller development. There's already a lot to process for this doc.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T07:09:57Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-28T07:09:57Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  Re: on handling inconsistencies in between workers and manager. 
  
  > Am I suspecting correctly that you actually wouldn't ignore the "manager graph" but you'd rather assume that it's identical to the "workers graph" (and refuse to automate otherwise)?
  
  Indeed, my general guiding principle would be to reject any "unrecognized/unsupported state". 
  
  After second round of thinking on the issue, I would adjust some parts to my proposal:
  1. I would determine the Flavors per worker, and manager separately
  2. By default, if there is any disagreement about the split between workers and manager, then I stop the synchronization and flag that with UnsupportedConfiguration
  3. As a follow up I make that policy configurable, for example: `spec,quotaAutomation.flavorConsistency: Full`, future policies could be `WorkerMajority` or `AllExceptOneWorker`.
  
  The good thing about this is that then we have a place, the message for the condition with the  `UnsupportedConfiguration` reason to indicate which worker is inconsistent. 
  
  I claim that glueing all resources without verification can be more troublesome to identify which single worker is messing the final flavor.
  
  > Yet, I don't yet see how this feature could pose problems in our context.
  
  For this feature to work the "cpu" and "cpu_credits" resources need to be in different ResourceGroups so that they can be assigned both at the same time.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T07:55:22Z

- **keps/9988-multikueue-manager-quota-automation/README.md:601** by [@mimowo](https://github.com/mimowo) — 2026-04-28T07:55:22Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Generously tolerate API calls for resources which typically don't exist in large numbers.
+
+   * This includes: LocalQueues, ClusterQueues, AdmissionChecks, and MultiKueueConfigs.
+
+   * This includes `.Get()`, `.List()` and `.Watch()` calls, even on remote clients.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler, even if they may be redundant (see [#10629](https://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency with caching.
+
+2. Generally tolerate local API calls, as they use cached clients.
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and (for local calls) make sure that these filters are covered by client indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+5. When feasible, replace multiple remote `.Get()`s with a single `.List()` call.
+
+   Even if this means increased amount of data transferred, the benefit is reduction of actual API QPS, as well as combining good latency with code simplicity (no need to parallelize for multiple `.Get()` calls).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using unfiltered remote API `.List()` calls, one per worker).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using unfilterd remote API `.List()` calls, at most one per worker).
+
+8. Check if there are currently disconnected remote clients (using their [`.connecting`](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L102) field).
+
+9. For every disconnected client for a remote cluster `R`, fetch all manager Workloads which are Admitted on `Q`, and last seen running on `R` \
+   (using a filtered local API `.List()` call, based on `.Status.Admission.ClusterQueue` as well as `.Status.ClusterName`).
+
+10. Determine the new quota values for `Q` (using the recipe described [here](#proposal), with `QuotaMultiplier` used as the multiplier).
+
+11. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+12. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* For the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler): 
+
+  * Test the reconcile logic with all exit paths (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L60)).
+
+  * Test all additional triggering paths from local watchers in the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L2266))
+
+  * Test the _non-triggering_ paths, e.g. a ClusterQueue event touching only irrelevant fields.
+
+* For the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler):
+
+  * Test triggering vs. _non-triggering_ paths (by just verifying the content of the event channel, analogous to [`wlUpdateCh`](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L95)).
+
+#### Integration Tests
+
+* For the integration between [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) and [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler):
+
+  * Test triggering the latter by changes in remote LocalQueues and ClusterQueues. \
+    (This could be also unit-tested, by a well-crafted watch interceptor similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go#L69-L70) - but an integration test feels a much cleaner way).
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled. Verify that:
+
+  * Manager quota is adjusted, as intended, upon:
+
+    - adding a new worker in MultiKueueConfig
+    - changing a remote ClusterQueueQuota
+    - (if we decide to support that) a worker cluster becoming unreachable
+
+  * New jobs can be admitted at all 3 phases of this scenario (quota automation disabled, then enabled, then disabled again).
+
+* Verify that manager quota auto-adjustments (in either direction: increases and decreases) promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier
+
+That is, an analogue of the proposed quota mutliplier - but differing in that:
+
+* it would **not affect** the [nominal ClusterQueue quota](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L250) value,
+* it would **not increase** the "total volume" of workloads which can be **admitted** by the manager,
+* it would **only** affect the "total volume" of workloads can **reserve quota** on the manager-side ClusterQueue (and hence, get dispatched to the workers).
+
+For example, if there are 2 workers, each with 10 CPU capacity, the `QuotaMultiplier` (as proposed in this KEP) is 0.8 (to leave 20% of worker capacity for "stray workloads"), and the Quota Reservation Overbooking Multiplier (as discussed here) is 3.0, then the manager-side ClusterQueue:
+
+* will have nominal quota set to (10 + 10) * 0.8 = 16,
+* will admit workloads requesting at most 16 CPU in total,
+* will reserve quota (and hence - dispatch to workers) workloads requesting up to 16 * 3 = 48 CPU in total.
+
+As weird as it seems, this opens a way to "reconcile" various, otherwise clashing, design factors for this KEP; specifically:
+
+* seamless dispatching vs. per-team allocations (see [Drawback #3](#drawbacks)),
+* (if `QuotaMultiplier` from this KEP is `1.0`) seamless dispatching vs. intuitive quotas (see [Drawback #2](#drawbacks)).
+
+Yet, this comes with some **disadvantages**:
+
+* Adding another form of user confusion ("why this ClusterQueue reserved quota above its nominal limit, despite using no borrowing?").
+
+* Adding complexity to Kueue behavior.
+  * In particular, we'd need to carefully plan how to enforce the "don't admit over the quota" constraint. \
+    One approach would be to make the workers ask the manager for a green light before admitting the workload (in some resemblance with [KEP-8303](https://github.com/kubernetes-sigs/kueue/blob/main/keps/8303-multikueue-orchestrated-preemption/README.md)).
+
+* Adding conceptual complexity. (As demonstrated by the length of the name of this section).
+
+* Blurring separation of concerns, by introducing new ways of MultiKueue awareness to core Kueue code.
+
+Therefore, this idea is **shelved** for now. We may consider it later, depending on the feedback.
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We **postpone** this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add metrics for multiplier usage
+
+Such metrics could help users determine an optimal value of the multiplier (see [Drawback #1](#drawbacks)).
+
+Example ideas:
+
+* "Multiplier usage": Whenever the manager _reserves quota_ for a workload, emit the quotient between the total quota reserved on the manager side and the total quota on the workers side.
+
+* "Effective multiplier usage": Whenever the manager _admits_ a workload, emit the "multiplier usage" value from the time when that workload obtained _quota reserved_ on the manager. \
+  (Rationale: this measure is "effective" in that, if the manager ClusterQueue were configured with a multiplier below the emitted metric value, it would actually have prevented admitting that workload at that time).
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * The feature gate is enabled by default.
+  * The feature has been succesfully used in production environment.
+  * No warning signs on performance were seen (in particular, no news of any external automation of worker ClusterQueue quotas; see [here](#guiding-principles-for-the-implementation)) - or, if any were, they have been addressed.
+  * The "guiding principles" (see [here](#guiding-principles-for-the-implementation)) are considered fully aligned on.
+  * Issue [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) (see [Drawback #4](#drawbacks)) is diagnosed, its impact on quota automation is understood and any mitigations deemed necessary are implemented.
+
+* **Stable:**
+  * The feature (offered functionality, API surface and implementation) has stabilized, without raising concerns.
+  * The feature gate is removed.
+
+## Drawbacks
+
+Besides introducing some risks (see [Risks and Mitigations](#risks-and-mitigations)), the proposed solution feels suboptimal mostly in the following ways:
+
+1. The `QuotaMultiplier` value feels very arbitrary, and it may be difficult to determine its optimal value (even for a specific use case). Two approaches coming to mind are:
+
+   1. An experienced Batch Admin can try out various values and judge the effects by observing overall system efficiency. (That, of course, assumes no other factors substantially interfering in such experimenting).
+
+   1. A more data-driven approach could be based on metrics, considered in [this possible follow-up](#add-metrics-for-multiplier-usage).
+
+1. With the `QuotaMultiplier`, the manager-side quota can effectively tell "what amount of workloads we want to dispatch" but may get _very divergent_ from "what amount of workloads we want to execute".
+
+   This is largely counter-intuitive, as the latter is the more "fundamental" meaning of a ClusterQueue quota, likely the most intuitive one for many users.
+
+   This drawback is the main motivation for the possible follow-up idea of [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier).
+
+1. While the `QuotaMultiplier` allows addressing individual [reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota) or [for decreasing it](#potential-reasons-for-decreasing-manager-quota), some combinations of those reasons remain not supportable in the most efficient way.
+
+   For example, in the "Per-team quotas on the manager" scenario discussed [here](#potential-reasons-for-decreasing-manager-quota), the Batch Admin will be able to configure manager-side ClusterQuotas for teams A / B / C to have 50% / 30% / 20% of the total workers quota respectively, but then each of the teams will be vulnerable to scheduling delays resulting from quota fragmentation, as explained in the "Divergence of quota reservation" scenario [here](#potential-reasons-for-increasing-manager-quota). \
+   Conversely, an attempt to increase the quotas for each of the teams would allow individual teams to consume more quotas than intended. For example, if the Batch Admin applied our default multiplier (3.0) on top of the initial per-team assignments discussed above, team B would be able to consume 90% of the total worker capacity.
+
+   Just like Drawback #2, this may be addressable by the [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier) idea, if used _together_ with the nominal quota multiplier as in the current proposal. Yet, the overall complexity of such solution feels unacceptable for now. We may consider it later depending on the feedback.
+
+1. The proposed solution fails to react to some types of detectable worker connection issues. \
+   See [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) which describes this problem in more detail, and speculates that it may already affect existing MultiKueue reconcilers (e.g. the one for Workloads).
+
+   If this turns out problematic, it should be fixable by caching the "manager ClusterQueue <-> MultiKueueCluster" relationship.
+
+1. We have fully ignored the topic of Cohorts, shared quota and quota borrowing. This is deliberate, and intended to retain any reasonable simplicity of the feature, at least in the Alpha stage. (See [Take Cohorts into account](#take-cohorts-into-account) in the Alternatives section).
+
+## Alternatives
+
+### Support quota automation for multiple manager-side ResourceFlavors
+
+The proposed constraint that quota automation requires a single ResourceFlavor on the manager side may feel inconvenient; for example, it's an obstacle (independent from the quota multiplier) for supporting [User Story 4](#story-4-left-unaddressed) in its full scope ("broken down per worker-side ResourceFlavors") via the manager ClusterQueue quota, which would be the most intuitive place.
+
+**Reasons for discarding**
+
+* As explained [here](#treatment-of-resourceflavors), ResourceFlavors on the manager side are typically meaningless for MultiKueue scheduling. Even if we enforced alignment between ResourceFlavors on the manager and worker sides (e.g. two ResourceFlavors A, B on both sides), there could easily exist workloads e.g. admitted on A on the manager but on B on the worker, which would be unnecessarily confusing.
+
+* Even if we introduced an enforced alignment of flavor assignments between the manager and workers (to address the above issue), this would open a new set of difficult questions:
+
+  * What should determine the flavor assignment on the ClusterQueue side? \
+    (It could be manager-side scheduler logic or the outcome of MultiKueue dispatching, neither option perfect).
+  * What if the flavors setup is changed on one of the workers? Should we re-arrange manager flavors accordingly?
+  * What if the flavors setup is conflicting across the workers, so that no compatible manager-side arrangement exists? \
+    (For example, worker1 has `flavorA` covering `cpu` and `flavorB` covering `memory` while worker2 has only `flavorA` covering both. A "naive aggregation" on the manager would produce `flavorA` covering both and `flavorB` covering just `memory`, which is invalid inside `.Spec.ResourceGroups` of a ClusterQueue). \
+    Should we disable quota automation in those cases? Or validate against them? (Again, neither option perfect).
+
+### Support quota automation for no manager-side ResourceFlavors (auto-create one)
```

  This seems quite useful. I think with the proposal to have multiple flavors per disjoint group of resources it will be easier in practice. We would only create the Flavor if all workers are consistent. This is related to the other discussion on "single flavor" vs "multiple flavors". I think we could drive this by a dedicated API field in a follow up, say `spec.quotaAutomation.flavorCreation: Enabled/ Disabled`. We may add this to graduation criteria for beta to "re-evaluate the automated flavor creation".

### Review by [@fg91](https://github.com/fg91) (commented) — 2026-04-28T11:48:08Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@fg91](https://github.com/fg91) — 2026-04-28T11:48:08Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  > it doesn't feel clean and is confusing
  
  Was my first gut feeling too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T10:53:07Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@mimowo](https://github.com/mimowo) — 2026-04-30T10:53:07Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  As we synced with @olekzabl and @mwielgus the multiplier is not part of the initial phase. We are planning to use it in the follow up, but in a slightly different form. I would suggest to drop it from the "Design Details" and for now mention only in the Notes section or Alternatives.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T10:53:55Z

- **keps/9988-multikueue-global-quotas/README.md:280** by [@mimowo](https://github.com/mimowo) — 2026-04-30T10:53:55Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
```

  As discussed https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3167389033

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T11:01:06Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@mimowo](https://github.com/mimowo) — 2026-04-30T11:01:06Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  As we discussed at length this topic with @olekzabl and I couldn't find any good counter example to the idea of a single flavor. Still, I think use cases for multiple flavors may pop up from various angles:
  1. we missed something just by static code analysis, and TAS flavors turn out harder than we think to combine with non-TAS (this remains my worry)
  2. users will have some user stories which requires multiple flavors on the management cluster
  3. users may just get confused by binding everything into one flavor, even if this works, because in their simplified mental model ResourceFlavor  - machine type
  4. there is no obvious signal to the user like "I don't sync quotas because your configuration is inconsistent" - my intuition is that users who have 10 clusters will really value such signal, but maybe we can merge unconditionally, and provide that signal for inconsistent configuration by other means (events / conditions / etc.)
  
  Even if this idea works on paper, then the user confusion and potential misconfigurations are potential real problems.
  
  So, I'm ok to give it a go with merging as Alpha for simplicity. however, let's add a point to graduation crieria for Beta to "Re-evaluate the default mode to merge all flavors into one on the management cluster based on user feedback", or something along the lines.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:14:49Z

- **keps/9988-multikueue-global-quotas/README.md:110** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:14:49Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
```

  Dropped, and mentioned as a new [Possible Follow-Up](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#add-a-way-to-adjust-the-manager-side-quota).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:18:09Z

- **keps/9988-multikueue-global-quotas/README.md:131** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:18:09Z
```diff
@@ -0,0 +1,726 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
```

  Please note that it's not framed as a User Story - but as "Potential reasons (...)" under "Notes/Constraints/Caveats".
  
  And it's not just my happy imagination; it's mostly based on early-collected feedback.
  
  > asked the community on slack to guide the multiplier:
  
  Thank you for this, and I value the responses... yet, I think the number of responses there is too low to draw strong conclusions about "how much speculative" is that part.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:19:24Z

- **keps/9988-multikueue-global-quotas/README.md:102** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:19:24Z
```diff
@@ -0,0 +1,892 @@
+# KEP-9988: MultiKueue Global Quotas
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Manager quota automation](#manager-quota-automation)
+  - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+    - [Manager quota automation](#manager-quota-automation-1)
+    - [Cross-worker resource stats in Visibility API](#cross-worker-resource-stats-in-visibility-api-1)
+      - [Utilization stats: worker-centric (new) vs. manager-centric (existing)](#utilization-stats-worker-centric-new-vs-manager-centric-existing)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [MultiKueue Cache](#multikueue-cache)
+  - [MultiKueue Workload Reconciler](#multikueue-workload-reconciler)
+    - [Ensuring cache availability on non-Kueue-leader Pods](#ensuring-cache-availability-on-non-kueue-leader-pods)
+  - [Visibility Server](#visibility-server)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Expand the cross-worker resource stats](#expand-the-cross-worker-resource-stats)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Track borrowable quota on the worker side](#track-borrowable-quota-on-the-worker-side)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose the cross-worker resource stats in the manager ClusterQueue Status](#expose-the-cross-worker-resource-stats-in-the-manager-clusterqueue-status)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic aggregation of quotas of worker ClusterQueues in a MultiKueue setup. In this area, we distinguish two aspects: 
+
+* surfacing aggregated quota information on the manager for user visibility;
+* automatically adjusting the manager-side quota for effective gatekeeping.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the  role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+- As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+- As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 2](#story-2)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) makes dispatching choices less effective. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, they all **depend** on building a mechanism allowing for manager awareness of workers' quotas. \
+   Such a mechanism can be naturally implemented as an extension of the [MultiKueue Cache](#multikueue-cache) which this KEP proposes.
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas, at real time.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
```

  Update: we decided that it's too "advanced" for the alpha scope. Degraded to a [Possible Follow-up](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#account-for-temporarily-unavailable-workers).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:23:48Z

- **keps/9988-multikueue-global-quotas/README.md:274** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:23:48Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
```

  I take the point that "booleans are not recommended"; thank you for this.
  
  Yet, I don't feel why split the management of a single feature to 2 top-level fields?
  (Just to avoid nesting?)
  
  In my head, it's still rather sth like
  ```yaml
  spec:
    quotaAutomation:
      mode: Enabled / Disabled // enum
      param1: ...
      param2: ...
  ```
  
  So for now I made sth like this. PTAL.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:24:10Z

- **keps/9988-multikueue-global-quotas/README.md:280** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:24:09Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
```

  This got obsolete.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:27:13Z

- **keps/9988-multikueue-global-quotas/README.md:279** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:27:13Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
```

  This got obsolete.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:29:26Z

- **keps/9988-multikueue-global-quotas/README.md:313** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:29:25Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
```

  I renamed the reason.
  
  As for the rest of this discussion - we already followed up deeper in [another comment thread](https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146579187).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:30:59Z

- **keps/9988-multikueue-manager-quota-automation/README.md:309** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:30:59Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
```

  I tried to describe the potential follow-up - possibly generally & concisely - [here](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#change-the-treatment-of-resourceflavors).
  
  And, on top of this, I added a following Beta graduation criterion:
  
  > Possible follow-ups have been reconsidered, based on any newly gathered feedback.
  
  Which, in particular, covers the multi-flavor topic.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:35:47Z

- **keps/9988-multikueue-global-quotas/README.md:318** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:35:47Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
```

  OK, so it seems I got out of sync in at least 2 aspects ("what is expected from a KEP" & "what is obvious to a Kueue developer"). Thank you for the hints; I've followed _in that direction_.
  
  Still, out of the "guiding principles", I'm keeping [this](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#a-note-on-client-usage). I'm still feeling there's some small bit here that isn't "just generic to any Kueue development"; after all, not every feature can afford happily sending client calls of any type. So I'm trying to distill what's special in this one which allows such "relaxed" approach.
  
  And I hope this time it's digestably short :)

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:36:48Z

- **keps/9988-multikueue-global-quotas/README.md:114** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:36:48Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
```

  This got obsolete. (Now discussed more roughly as a [Possible Follow-up](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#account-for-temporarily-unavailable-workers)).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:37:49Z

- **keps/9988-multikueue-global-quotas/README.md:111** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:37:49Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
```

  The multiplier got removed, and I shortened Design Details following the other feedback bits. So no longer repetition, I think. PTAL.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:39:01Z

- **keps/9988-multikueue-manager-quota-automation/README.md:601** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:39:00Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+If a MultiKueue AdmissionCheck is attached to a manger ClusterQueue with multiple ResourceFlavors, it must apply to **all** those flavors (since [#2047](https://github.com/kubernetes-sigs/kueue/pull/2047)). Then, the selection of ResourceFlavor on the manager side has typically **no effect** on the remotes - it doesn't affect neither the built-in dispatchers nor scheduling on the worker side.
+
+Therefore, the proposed restriction of the quota automation feature to single-flavor manager configs is _very mild_: multiple flavors on the manager side _should not be needed for most users_. Theoretical cases when it would be needed include:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed in Alternatives, see [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by, and have troubles understanding, several aspects:
+   
+   1. ClusterQueue quotas being adjusted automatically, without their initiative;
+   2. divergences between the "official" quotas on the manager and worker sides, caused by the quota automation multiplier.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risks 1.i and 1.ii are mitigated by a dedicated ClusterQueue condition, explaining the automation process as well as the multiplier.
+
+* Risks 1 and 2 are mitigated by introducing feature gates combined with the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low (see our [guiding principles](#guiding-principles-for-the-implementation)). If needed, it can be substantially mitigated by caching the relevant information. \
+  While this would require introducing a new MultiKueue-specific cache, such a cache seems anyway needed for other tasks approaching, including [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105), [#10614](https://github.com/kubernetes-sigs/kueue/issues/10614) and dispatching improvements (see item 3 [here](#manager-vs-workers-separation)). Hence, potential mitigations to Risk 3 will likely take the form of re-using that cache, possibly combined with extending its content.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies whether (and how) the ClusterQueue quotas 
+   // in the manager cluster should be automatically updated 
+   // based on the total quota in all related worker ClusterQueues.
+   QuotaAutomation *QuotaAutomation`json:"quotaAutomation,omitEmpty"`
+}
+
+type QuotaAutomation struct {
+   // enabled specifies whether ClusterQueue quotas in the manager cluster
+   // should be automatically set based on worker-side quotas.
+   // The default value depends on the feature gate MultiKueueManagerQuotaAutomation.
+   Enabled *bool `json:"enabled,omitEmpty"`
+
+   // quotaMultiplier will be applied on top of the total worker-side quotas
+   // in order to define the manager-side quota to be automatically set.
+   // This value is ignored if the quota automation feature is disabled.
+   // Defaults to 3.
+   QuotaMultiplier *float32 `json:"quotaMultiplier,omitEmpty"`
+}
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers. Applying total worker capacity with a multiplier specified in MultiKueueConfig.QuotaAutomation.QuotaMultiplier.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: MultipleFlavors
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### Guiding principles for the implementation
+
+The implementation details discussed below are based on the following guiding principles:
+
+1. Generously tolerate API calls for resources which typically don't exist in large numbers.
+
+   * This includes: LocalQueues, ClusterQueues, AdmissionChecks, and MultiKueueConfigs.
+
+   * This includes `.Get()`, `.List()` and `.Watch()` calls, even on remote clients.
+
+   If this seems overly simplistic, keep in mind that:
+
+   * We only add new API calls in infrequently running procedures (and thus have much less need for sophisticated caching than, say, Kueue scheduler code). \
+     This **may need revisiting** in a (hypothetical) scenario when some automation external to Kueue frequently updates e.g. worker ClusterQueue quotas.
+
+   * This feels in line with the current MultiKueue approach, in which we e.g. tolerate remote `.Get()` calls [here](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/workload.go#L314) inside the MultiKueue Workload Reconciler, even if they may be redundant (see [#10629](https://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+   * If this approach ever turns too relaxed, we have numerous ways to win more efficiency with caching.
+
+2. Generally tolerate local API calls, as they use cached clients.
+
+Still, to keep up with Kueue code standards, we will:
+
+3. When possible, make `.List()` calls filtered (by API fields), and (for local calls) make sure that these filters are covered by client indexes.
+
+4. When feasible, pre-guard reconcile routines and `.Watches()` calls with predicates, ensuring that a reconcile is not triggered by an update which only changed irrelevant fields. \
+   (Such predicates will be clearly indicated in the recipes below).
+
+5. When feasible, replace multiple remote `.Get()`s with a single `.List()` call.
+
+   Even if this means increased amount of data transferred, the benefit is reduction of actual API QPS, as well as combining good latency with code simplicity (no need to parallelize for multiple `.Get()` calls).
+
+### MultiKueue ClusterQueue Reconciler
+
+Besides the [existing MultiKueue reconcilers](https://github.com/kubernetes-sigs/kueue/blob/161a2abc9e484b9fe0d6b3025c0bcac4fb8ef3a4/pkg/controller/admissionchecks/multikueue/controllers.go#L151-L165), we'll add a new one for ClusterQueue objects (mostly on the manager side). Its operation will be enabled by the `MultiKueueManagerQuotaAutomation` feature gate.
+
+Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. Fetch `Q` from the API client.
+
+2. Find the MultiKueue AdmissionCheck for `Q` (using an unfiltered local API `.List()` call). \
+   Then, fetch the MultiKueueConfig for `Q` from the API client. \
+   If any of this fails, clear the `MultiKueueManagerQuotaAutomation` Condition (if present) and exit.
+
+3. Determine whether quota automation has been requested and can be supported for `Q` (based on the MultiKueueConfig, the feature gate, and the number of ResourceFlavors). \
+   If not, set the `MultiKueueManagerQuotaAutomation` Condition accordingly (to `False`) and exit.
+
+5. Find all manager LocalQueues connected to `Q` (using a filtered local API `.List()` call).
+
+6. Fetch all worker LocalQueues of those names (using unfiltered remote API `.List()` calls, one per worker).
+
+7. Fetch all worker ClusterQueues connected to those LocalQueues (using unfilterd remote API `.List()` calls, at most one per worker).
+
+8. Check if there are currently disconnected remote clients (using their [`.connecting`](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L102) field).
+
+9. For every disconnected client for a remote cluster `R`, fetch all manager Workloads which are Admitted on `Q`, and last seen running on `R` \
+   (using a filtered local API `.List()` call, based on `.Status.Admission.ClusterQueue` as well as `.Status.ClusterName`).
+
+10. Determine the new quota values for `Q` (using the recipe described [here](#proposal), with `QuotaMultiplier` used as the multiplier).
+
+11. If `MultiKueueManagerQuotaAutomation` is not `True`, set it to `True`.
+
+12. Update quotas for `Q` if the desired values differ from the current ones.
+
+(See the "Takeaway" part [here](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) for the rationale for the ordering of the last 2 steps).
+
+This reconciler will be triggered in the following ways:
+
+1. For (manager) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups` or of `.Spec.AdmissionChecksStrategy`).
+
+2. For (manager) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Trigger reconcile for the corresponding ClusterQueue.
+
+3. For (manager) AdmissionCheck events (creations, deletions, changes of "does `.Spec.ControllerName` indicate MultiKueue?" or, if it does, of `.Spec.Parameters`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using a filtered API `.List` call).
+   2. Trigger reconcile for all those ClusterQueues.
+
+4. For (manager) MultiKueueConfig events (creations, deletions, changes of `.Spec.QuotaAutomation`), where the update handler function will:
+   1. Find all manager ClusterQueues using it (using 2 levels of filtered API `.List` calls, similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L880-L890)).
+   2. Trigger reconcile for all those ClusterQueues.
+
+5. For remote objects events, propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) (see there for details).
+
+Cases 2-4 will be implemented by additional `.Watches()` calls on the reconciler (like e.g. [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L915)). Case 5 will be implemented by watching a custom channel (like [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload.go#L914)).
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, where the `startWatcher()` method calls `.Watch()` on the remote clusters [for Workloads](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L178) as well as [for the supported job types](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L190), we'll add additional `.Watch()` calls:
+
+1. For (remote) LocalQueue events (creations, deletions), where the update handler function will:
+   1. Fetch the corresponding manager LocalQueue from the API client, and check its ClusterQueue.
+   2. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for that ClusterQueue.
+
+2. For (remote) ClusterQueue events (creations, deletions, changes of `.Spec.ResourceGroups`), where the update handler function will, for a ClusterQueue `Q` on worker `W`:
+   1. Find all LocalQueues on `W` pointing to `Q` (using a filtered remote `.List` API call).
+   2. Find all manager ClusterQueues connected with manager LocalQueues of the same names (using a single unfiltered local `.List` API call).
+   3. Trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for all those ClusterQueues.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* For the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler): 
+
+  * Test the reconcile logic with all exit paths (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L60)).
+
+  * Test all additional triggering paths from local watchers in the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) (similarly to e.g. [this suite](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/workload_test.go#L2266))
+
+  * Test the _non-triggering_ paths, e.g. a ClusterQueue event touching only irrelevant fields.
+
+* For the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler):
+
+  * Test triggering vs. _non-triggering_ paths (by just verifying the content of the event channel, analogous to [`wlUpdateCh`](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L95)).
+
+#### Integration Tests
+
+* For the integration between [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) and [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler):
+
+  * Test triggering the latter by changes in remote LocalQueues and ClusterQueues. \
+    (This could be also unit-tested, by a well-crafted watch interceptor similarly as [here](https://github.com/kubernetes-sigs/kueue/blob/25538a4ea2979d975d75792c1f9a7124a0475c4a/pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go#L69-L70) - but an integration test feels a much cleaner way).
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled. Verify that:
+
+  * Manager quota is adjusted, as intended, upon:
+
+    - adding a new worker in MultiKueueConfig
+    - changing a remote ClusterQueueQuota
+    - (if we decide to support that) a worker cluster becoming unreachable
+
+  * New jobs can be admitted at all 3 phases of this scenario (quota automation disabled, then enabled, then disabled again).
+
+* Verify that manager quota auto-adjustments (in either direction: increases and decreases) promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier
+
+That is, an analogue of the proposed quota mutliplier - but differing in that:
+
+* it would **not affect** the [nominal ClusterQueue quota](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/apis/kueue/v1beta2/clusterqueue_types.go#L250) value,
+* it would **not increase** the "total volume" of workloads which can be **admitted** by the manager,
+* it would **only** affect the "total volume" of workloads can **reserve quota** on the manager-side ClusterQueue (and hence, get dispatched to the workers).
+
+For example, if there are 2 workers, each with 10 CPU capacity, the `QuotaMultiplier` (as proposed in this KEP) is 0.8 (to leave 20% of worker capacity for "stray workloads"), and the Quota Reservation Overbooking Multiplier (as discussed here) is 3.0, then the manager-side ClusterQueue:
+
+* will have nominal quota set to (10 + 10) * 0.8 = 16,
+* will admit workloads requesting at most 16 CPU in total,
+* will reserve quota (and hence - dispatch to workers) workloads requesting up to 16 * 3 = 48 CPU in total.
+
+As weird as it seems, this opens a way to "reconcile" various, otherwise clashing, design factors for this KEP; specifically:
+
+* seamless dispatching vs. per-team allocations (see [Drawback #3](#drawbacks)),
+* (if `QuotaMultiplier` from this KEP is `1.0`) seamless dispatching vs. intuitive quotas (see [Drawback #2](#drawbacks)).
+
+Yet, this comes with some **disadvantages**:
+
+* Adding another form of user confusion ("why this ClusterQueue reserved quota above its nominal limit, despite using no borrowing?").
+
+* Adding complexity to Kueue behavior.
+  * In particular, we'd need to carefully plan how to enforce the "don't admit over the quota" constraint. \
+    One approach would be to make the workers ask the manager for a green light before admitting the workload (in some resemblance with [KEP-8303](https://github.com/kubernetes-sigs/kueue/blob/main/keps/8303-multikueue-orchestrated-preemption/README.md)).
+
+* Adding conceptual complexity. (As demonstrated by the length of the name of this section).
+
+* Blurring separation of concerns, by introducing new ways of MultiKueue awareness to core Kueue code.
+
+Therefore, this idea is **shelved** for now. We may consider it later, depending on the feedback.
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We **postpone** this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add metrics for multiplier usage
+
+Such metrics could help users determine an optimal value of the multiplier (see [Drawback #1](#drawbacks)).
+
+Example ideas:
+
+* "Multiplier usage": Whenever the manager _reserves quota_ for a workload, emit the quotient between the total quota reserved on the manager side and the total quota on the workers side.
+
+* "Effective multiplier usage": Whenever the manager _admits_ a workload, emit the "multiplier usage" value from the time when that workload obtained _quota reserved_ on the manager. \
+  (Rationale: this measure is "effective" in that, if the manager ClusterQueue were configured with a multiplier below the emitted metric value, it would actually have prevented admitting that workload at that time).
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * The feature gate is enabled by default.
+  * The feature has been succesfully used in production environment.
+  * No warning signs on performance were seen (in particular, no news of any external automation of worker ClusterQueue quotas; see [here](#guiding-principles-for-the-implementation)) - or, if any were, they have been addressed.
+  * The "guiding principles" (see [here](#guiding-principles-for-the-implementation)) are considered fully aligned on.
+  * Issue [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) (see [Drawback #4](#drawbacks)) is diagnosed, its impact on quota automation is understood and any mitigations deemed necessary are implemented.
+
+* **Stable:**
+  * The feature (offered functionality, API surface and implementation) has stabilized, without raising concerns.
+  * The feature gate is removed.
+
+## Drawbacks
+
+Besides introducing some risks (see [Risks and Mitigations](#risks-and-mitigations)), the proposed solution feels suboptimal mostly in the following ways:
+
+1. The `QuotaMultiplier` value feels very arbitrary, and it may be difficult to determine its optimal value (even for a specific use case). Two approaches coming to mind are:
+
+   1. An experienced Batch Admin can try out various values and judge the effects by observing overall system efficiency. (That, of course, assumes no other factors substantially interfering in such experimenting).
+
+   1. A more data-driven approach could be based on metrics, considered in [this possible follow-up](#add-metrics-for-multiplier-usage).
+
+1. With the `QuotaMultiplier`, the manager-side quota can effectively tell "what amount of workloads we want to dispatch" but may get _very divergent_ from "what amount of workloads we want to execute".
+
+   This is largely counter-intuitive, as the latter is the more "fundamental" meaning of a ClusterQueue quota, likely the most intuitive one for many users.
+
+   This drawback is the main motivation for the possible follow-up idea of [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier).
+
+1. While the `QuotaMultiplier` allows addressing individual [reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota) or [for decreasing it](#potential-reasons-for-decreasing-manager-quota), some combinations of those reasons remain not supportable in the most efficient way.
+
+   For example, in the "Per-team quotas on the manager" scenario discussed [here](#potential-reasons-for-decreasing-manager-quota), the Batch Admin will be able to configure manager-side ClusterQuotas for teams A / B / C to have 50% / 30% / 20% of the total workers quota respectively, but then each of the teams will be vulnerable to scheduling delays resulting from quota fragmentation, as explained in the "Divergence of quota reservation" scenario [here](#potential-reasons-for-increasing-manager-quota). \
+   Conversely, an attempt to increase the quotas for each of the teams would allow individual teams to consume more quotas than intended. For example, if the Batch Admin applied our default multiplier (3.0) on top of the initial per-team assignments discussed above, team B would be able to consume 90% of the total worker capacity.
+
+   Just like Drawback #2, this may be addressable by the [quota reservation overbooking](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier) idea, if used _together_ with the nominal quota multiplier as in the current proposal. Yet, the overall complexity of such solution feels unacceptable for now. We may consider it later depending on the feedback.
+
+1. The proposed solution fails to react to some types of detectable worker connection issues. \
+   See [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) which describes this problem in more detail, and speculates that it may already affect existing MultiKueue reconcilers (e.g. the one for Workloads).
+
+   If this turns out problematic, it should be fixable by caching the "manager ClusterQueue <-> MultiKueueCluster" relationship.
+
+1. We have fully ignored the topic of Cohorts, shared quota and quota borrowing. This is deliberate, and intended to retain any reasonable simplicity of the feature, at least in the Alpha stage. (See [Take Cohorts into account](#take-cohorts-into-account) in the Alternatives section).
+
+## Alternatives
+
+### Support quota automation for multiple manager-side ResourceFlavors
+
+The proposed constraint that quota automation requires a single ResourceFlavor on the manager side may feel inconvenient; for example, it's an obstacle (independent from the quota multiplier) for supporting [User Story 4](#story-4-left-unaddressed) in its full scope ("broken down per worker-side ResourceFlavors") via the manager ClusterQueue quota, which would be the most intuitive place.
+
+**Reasons for discarding**
+
+* As explained [here](#treatment-of-resourceflavors), ResourceFlavors on the manager side are typically meaningless for MultiKueue scheduling. Even if we enforced alignment between ResourceFlavors on the manager and worker sides (e.g. two ResourceFlavors A, B on both sides), there could easily exist workloads e.g. admitted on A on the manager but on B on the worker, which would be unnecessarily confusing.
+
+* Even if we introduced an enforced alignment of flavor assignments between the manager and workers (to address the above issue), this would open a new set of difficult questions:
+
+  * What should determine the flavor assignment on the ClusterQueue side? \
+    (It could be manager-side scheduler logic or the outcome of MultiKueue dispatching, neither option perfect).
+  * What if the flavors setup is changed on one of the workers? Should we re-arrange manager flavors accordingly?
+  * What if the flavors setup is conflicting across the workers, so that no compatible manager-side arrangement exists? \
+    (For example, worker1 has `flavorA` covering `cpu` and `flavorB` covering `memory` while worker2 has only `flavorA` covering both. A "naive aggregation" on the manager would produce `flavorA` covering both and `flavorB` covering just `memory`, which is invalid inside `.Spec.ResourceGroups` of a ClusterQueue). \
+    Should we disable quota automation in those cases? Or validate against them? (Again, neither option perfect).
+
+### Support quota automation for no manager-side ResourceFlavors (auto-create one)
```

  I mentioned this at the end of [this Possible Follow-up](https://github.com/olekzabl/kueue/blob/br35/keps/9988-multikueue-global-quotas/README.md#change-the-treatment-of-resourceflavors), and Possible Follow-Ups now act _in bulk_ as a Beta graduation criterion (I mean, to have them reconsidered).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-04T23:43:58Z

- **keps/9988-multikueue-manager-quota-automation/README.md:265** by [@olekzabl](https://github.com/olekzabl) — 2026-05-04T23:43:58Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
```

  To be strict, wl3 won't be _admitted_ on the manager; it will have _quota reserved_ there.
  
  And yes, IIRC in this scenario it will hold that quota until it gets admitted on a worker, i.e. until one of wl1, wl2 finishes. (Generally, we could also have some priority-based preemptions. But not a timeout, I think).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T20:25:04Z

LGTM, the remaining comments are mostly editorial. I think this is ready to be implemented 👍 

cc @tenzen-y @amy ptal if you have additional comments

- **keps/9988-multikueue-global-quotas/kep.yaml:11** by [@mimowo](https://github.com/mimowo) — 2026-05-05T19:48:43Z
```diff
@@ -1,20 +1,14 @@
-title: Concurrent Admission
-kep-number: 8691
+title: MultiKueue Global Manager Quota Automation
+kep-number: 9988
 authors:
-  - "@mwielgus"
-  - "@pbundyra"
-status: implementable
-creation-date: 2026-01-21
-reviewers:
-  - "@mimowo"
-  - "@ichekrygin"
-  - "@pajakd"
-  - "@tenzen-y"
   - "@olekzabl"
+status: provisional
+creation-date: 2026-04-24
+reviewers:
+  - mimowo
+  - amy
 approvers:
-  - "@mimowo"
-  - "@tenzen-y"
-  
+  - TBD
```

  Add me here

- **keps/9988-multikueue-global-quotas/README.md:84** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:09:10Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
```

  nit
  ```suggestion
     While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
  ```
  or "such"?

- **keps/9988-multikueue-global-quotas/README.md:169** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:13:53Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
```

  Something reads off in this paragraph. I suppose you want to say "there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:":
  
  Then you say "the workload could be submitted directly" - analogously in other points.

- **keps/9988-multikueue-global-quotas/README.md:206** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:14:23Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
```

  ```suggestion
  Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
  ```
  nit

- **keps/9988-multikueue-global-quotas/README.md:238** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:15:49Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
```

  Let's also add a comment, that the quotaAutomation is disabled when the structure is left unspecified.

- **keps/9988-multikueue-global-quotas/README.md:406** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:20:30Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We postpone this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add a way to adjust the manager-side quota
+
+As explained in [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota), some users may want to keep their manager-side quota higher or lower than the sum of worker quotas. A configurable **quota multiplier** seems robust enough to address most use cases.
+
+Our initial idea was to let it affect the manager-side `nominalQuota`; however, that was considered confusing by most reviewers. 
+
+Another option is to re-use the concept of "effective quota", or "accessible quota" in terms of [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864); that is, leave `nominalQuota` unchanged but apply the mulitplier on the level of "accessible quota" which would be the one actually enforced. This would allow to keep `nominalQuota` serving [User Story 4](#story-4) while letting the multiplier affect the actual dispatching behavior.
+
+#### Change the treatment of ResourceFlavors
+
+While the proposed treatment of flavors (require a single flavor on the manager; aggregate all worker quotas into it) is the simplest one which avoids the [issue of manager/worker flavor skew](#treatment-of-resourceflavors), it may be too simplistic for some use cases involving _mutually exclusive_ flavors on the workers (e.g. CPU-only vs. GPU-only, or TAS flavors based on mutually incompatible topologies). In those cases, the flavor skew can't occur; on the other hand, a single manager-side flavor combining all the worker flavors could be misleading, or even impossible to define.
+
+To handle such cases well, without introducing confusion in the other cases, we'd need roughly to:
+
+- capture the "mutually exclusive" relationship among ResourceFlavors with a strict definition;
+- replace the constraint guarding quota automation with a more complex one, in the spirit of "there is exactly one manager-side flavor _per equivalence class of non-mutually-exclusive flavors_".
+
+Then, on top of that, we could consider auto-creating missing manager flavors, per such "equivalence classes".
+
+This seems promising but needs more discussion and research of use cases to hammer out the details.
+
+#### Account for temporarily unavailable workers
+
+When a worker cluster becomes unreachable (e.g. due to network issues), its admitted workloads are still considered admitted on the manager side, until `workerLostTimeout` expires. On the other hand, our proposed approach for determining total worker-side quotas (relying on fetching `nominalQuota` from all workers) will not include quotas of any disconnected worker clusters. 
+
+This discrepancy may lead to temporary under-utilization of the fleet resources. For example, if there are 3 workers, each with 10 CPU capacity, and we're running a single workload, consuming 7 CPU, on `worker1` which gets disconnected, then the manager ClusterQueue will have its `nominalQuota` set to 20, with 7 of that already used, leaving just 13 CPU for new workloads on `worker2` and `worker3` which are both empty.
+
+To cover for that, we could increase the manager-side quota by the sum of requests of all workloads which are admitted on the manager but their assigned worker clusters have remote clients which are currently `.connecting`. (Notably, all this info is available on the manager side, without querying the workers).
+
+We postpone this because it adds some complexity, and the impact feels minor.
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * The feature gate is enabled by default.
+  * The feature has been succesfully used in production environment.
+  * [Possible follow-ups](#possible-follow-ups) have been reconsidered, based on any newly gathered feedback.
+  * No warning signs on performance were seen (in particular, no news of any external automation of worker ClusterQueue quotas) - or, if any were, they have been addressed.
+  * Issue [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) (see [Drawback #1](#drawbacks)) is diagnosed, its impact on quota automation is understood and any mitigations deemed necessary are implemented.
+
+* **Stable:**
+  * The feature (offered functionality, API surface and implementation) has stabilized, without raising concerns.
+  * The feature gate is removed.
```

  Actually the practice is:
  - Lock the feature gate for "stable"
  - remove the FG in N+2 (N  - version for stable)
  
  The reason is that a feature entering stable may happen still to have bugs, and sometimes we need to disable. If the FG is kept it is much less work. For this reason k8s introduces the concept of "locking" FG.
  
  We only remove in N+2 to also give users more time to remove the setting from their env - Kueue would not startup if a dropped FG is specified in config.

- **keps/9988-multikueue-global-quotas/README.md:399** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:21:22Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We postpone this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add a way to adjust the manager-side quota
+
+As explained in [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota), some users may want to keep their manager-side quota higher or lower than the sum of worker quotas. A configurable **quota multiplier** seems robust enough to address most use cases.
+
+Our initial idea was to let it affect the manager-side `nominalQuota`; however, that was considered confusing by most reviewers. 
+
+Another option is to re-use the concept of "effective quota", or "accessible quota" in terms of [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864); that is, leave `nominalQuota` unchanged but apply the mulitplier on the level of "accessible quota" which would be the one actually enforced. This would allow to keep `nominalQuota` serving [User Story 4](#story-4) while letting the multiplier affect the actual dispatching behavior.
+
+#### Change the treatment of ResourceFlavors
+
+While the proposed treatment of flavors (require a single flavor on the manager; aggregate all worker quotas into it) is the simplest one which avoids the [issue of manager/worker flavor skew](#treatment-of-resourceflavors), it may be too simplistic for some use cases involving _mutually exclusive_ flavors on the workers (e.g. CPU-only vs. GPU-only, or TAS flavors based on mutually incompatible topologies). In those cases, the flavor skew can't occur; on the other hand, a single manager-side flavor combining all the worker flavors could be misleading, or even impossible to define.
+
+To handle such cases well, without introducing confusion in the other cases, we'd need roughly to:
+
+- capture the "mutually exclusive" relationship among ResourceFlavors with a strict definition;
+- replace the constraint guarding quota automation with a more complex one, in the spirit of "there is exactly one manager-side flavor _per equivalence class of non-mutually-exclusive flavors_".
+
+Then, on top of that, we could consider auto-creating missing manager flavors, per such "equivalence classes".
+
+This seems promising but needs more discussion and research of use cases to hammer out the details.
+
+#### Account for temporarily unavailable workers
+
+When a worker cluster becomes unreachable (e.g. due to network issues), its admitted workloads are still considered admitted on the manager side, until `workerLostTimeout` expires. On the other hand, our proposed approach for determining total worker-side quotas (relying on fetching `nominalQuota` from all workers) will not include quotas of any disconnected worker clusters. 
+
+This discrepancy may lead to temporary under-utilization of the fleet resources. For example, if there are 3 workers, each with 10 CPU capacity, and we're running a single workload, consuming 7 CPU, on `worker1` which gets disconnected, then the manager ClusterQueue will have its `nominalQuota` set to 20, with 7 of that already used, leaving just 13 CPU for new workloads on `worker2` and `worker3` which are both empty.
+
+To cover for that, we could increase the manager-side quota by the sum of requests of all workloads which are admitted on the manager but their assigned worker clusters have remote clients which are currently `.connecting`. (Notably, all this info is available on the manager side, without querying the workers).
+
+We postpone this because it adds some complexity, and the impact feels minor.
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * The feature gate is enabled by default.
+  * The feature has been succesfully used in production environment.
```

  ```suggestion
    * The feature has been successfully used in a production environment.
  ```

- **keps/9988-multikueue-manager-quota-automation/README.md:548** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:21:39Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We postpone this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add a way to adjust the manager-side quota
+
+As explained in [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota), some users may want to keep their manager-side quota higher or lower than the sum of worker quotas. A configurable **quota multiplier** seems robust enough to address most use cases.
+
+Our initial idea was to let it affect the manager-side `nominalQuota`; however, that was considered confusing by most reviewers. 
+
+Another option is to re-use the concept of "effective quota", or "accessible quota" in terms of [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864); that is, leave `nominalQuota` unchanged but apply the mulitplier on the level of "accessible quota" which would be the one actually enforced. This would allow to keep `nominalQuota` serving [User Story 4](#story-4) while letting the multiplier affect the actual dispatching behavior.
+
+#### Change the treatment of ResourceFlavors
+
+While the proposed treatment of flavors (require a single flavor on the manager; aggregate all worker quotas into it) is the simplest one which avoids the [issue of manager/worker flavor skew](#treatment-of-resourceflavors), it may be too simplistic for some use cases involving _mutually exclusive_ flavors on the workers (e.g. CPU-only vs. GPU-only, or TAS flavors based on mutually incompatible topologies). In those cases, the flavor skew can't occur; on the other hand, a single manager-side flavor combining all the worker flavors could be misleading, or even impossible to define.
+
+To handle such cases well, without introducing confusion in the other cases, we'd need roughly to:
+
+- capture the "mutually exclusive" relationship among ResourceFlavors with a strict definition;
+- replace the constraint guarding quota automation with a more complex one, in the spirit of "there is exactly one manager-side flavor _per equivalence class of non-mutually-exclusive flavors_".
+
+Then, on top of that, we could consider auto-creating missing manager flavors, per such "equivalence classes".
+
+This seems promising but needs more discussion and research of use cases to hammer out the details.
+
+#### Account for temporarily unavailable workers
+
+When a worker cluster becomes unreachable (e.g. due to network issues), its admitted workloads are still considered admitted on the manager side, until `workerLostTimeout` expires. On the other hand, our proposed approach for determining total worker-side quotas (relying on fetching `nominalQuota` from all workers) will not include quotas of any disconnected worker clusters. 
+
+This discrepancy may lead to temporary under-utilization of the fleet resources. For example, if there are 3 workers, each with 10 CPU capacity, and we're running a single workload, consuming 7 CPU, on `worker1` which gets disconnected, then the manager ClusterQueue will have its `nominalQuota` set to 20, with 7 of that already used, leaving just 13 CPU for new workloads on `worker2` and `worker3` which are both empty.
+
+To cover for that, we could increase the manager-side quota by the sum of requests of all workloads which are admitted on the manager but their assigned worker clusters have remote clients which are currently `.connecting`. (Notably, all this info is available on the manager side, without querying the workers).
+
+We postpone this because it adds some complexity, and the impact feels minor.
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
```

  Let's add a point about documentation for the feature

- **keps/9988-multikueue-global-quotas/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:22:49Z
```diff
@@ -1,20 +1,14 @@
-title: Concurrent Admission
-kep-number: 8691
+title: MultiKueue Global Manager Quota Automation
```

  nit, maybe align the PR title with that, and the folder directory for the KEP, wdyt?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T09:11:09Z

- **keps/9988-multikueue-global-quotas/README.md:406** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T09:11:09Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that the manager ClusterQueue has exactly one ResourceFlavor. This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
+
+#### Use cached remote clients for low-volume resource kinds
+
+Currently, the MultiKueue controllers use cached K8s clients for local (manager-side) resources but uncached clients for remote resources. Just enabling cache for remote clients does feel risky, as it could use much memory for Workloads and supported job kinds (which can exist in large numbers).
+
+However, the remote API calls introduced in this KEP involve only "memory-light" resource kinds (LocalQueues and ClusterQueues), for which caching would pose no risk, while avoiding duplication in some cases. \
+(In case of a [many-to-many relation](#defining-related-worker-clusterqueues) between manager-side and worker-side ClusterQueues, a change of worker-side `Q'` would trigger reconciles of multiple manager-side ClusterQueues, each of which would make the same remote API `.List()` calls, almost simultaneously).
+
+Then, it seems a promising optimization to cache remote clients _only for selected resource kinds_. \
+This could be implemented by equipping the internal [`remoteClient` structure](https://github.com/kubernetes-sigs/kueue/blob/76676f599fc87883156c81516ea91b9b65cc70fd/pkg/controller/admissionchecks/multikueue/multikueuecluster.go#L91-L109) with **two** API clients, the old uncached one (to be used for Workloads etc.), and a new cached one (to be used for LocalQueues etc.). To ensure proper choices among them, both could be wrapped with tiny facades throwing on calls for resource kinds outside an allowlist.
+
+We postpone this as doing it right may still involve some complexity. For example, it's not clear how to avoid duplicating API watches (see item 3 in the description of [#10629](http://github.com/kubernetes-sigs/kueue/issues/10629)).
+
+#### Add a way to adjust the manager-side quota
+
+As explained in [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota), some users may want to keep their manager-side quota higher or lower than the sum of worker quotas. A configurable **quota multiplier** seems robust enough to address most use cases.
+
+Our initial idea was to let it affect the manager-side `nominalQuota`; however, that was considered confusing by most reviewers. 
+
+Another option is to re-use the concept of "effective quota", or "accessible quota" in terms of [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864); that is, leave `nominalQuota` unchanged but apply the mulitplier on the level of "accessible quota" which would be the one actually enforced. This would allow to keep `nominalQuota` serving [User Story 4](#story-4) while letting the multiplier affect the actual dispatching behavior.
+
+#### Change the treatment of ResourceFlavors
+
+While the proposed treatment of flavors (require a single flavor on the manager; aggregate all worker quotas into it) is the simplest one which avoids the [issue of manager/worker flavor skew](#treatment-of-resourceflavors), it may be too simplistic for some use cases involving _mutually exclusive_ flavors on the workers (e.g. CPU-only vs. GPU-only, or TAS flavors based on mutually incompatible topologies). In those cases, the flavor skew can't occur; on the other hand, a single manager-side flavor combining all the worker flavors could be misleading, or even impossible to define.
+
+To handle such cases well, without introducing confusion in the other cases, we'd need roughly to:
+
+- capture the "mutually exclusive" relationship among ResourceFlavors with a strict definition;
+- replace the constraint guarding quota automation with a more complex one, in the spirit of "there is exactly one manager-side flavor _per equivalence class of non-mutually-exclusive flavors_".
+
+Then, on top of that, we could consider auto-creating missing manager flavors, per such "equivalence classes".
+
+This seems promising but needs more discussion and research of use cases to hammer out the details.
+
+#### Account for temporarily unavailable workers
+
+When a worker cluster becomes unreachable (e.g. due to network issues), its admitted workloads are still considered admitted on the manager side, until `workerLostTimeout` expires. On the other hand, our proposed approach for determining total worker-side quotas (relying on fetching `nominalQuota` from all workers) will not include quotas of any disconnected worker clusters. 
+
+This discrepancy may lead to temporary under-utilization of the fleet resources. For example, if there are 3 workers, each with 10 CPU capacity, and we're running a single workload, consuming 7 CPU, on `worker1` which gets disconnected, then the manager ClusterQueue will have its `nominalQuota` set to 20, with 7 of that already used, leaving just 13 CPU for new workloads on `worker2` and `worker3` which are both empty.
+
+To cover for that, we could increase the manager-side quota by the sum of requests of all workloads which are admitted on the manager but their assigned worker clusters have remote clients which are currently `.connecting`. (Notably, all this info is available on the manager side, without querying the workers).
+
+We postpone this because it adds some complexity, and the impact feels minor.
+
+### Graduation Criteria
+
+* **Alpha:**
+  * The feature implemented behind the `MultiKueueManagerQuotaAutomation` feature gate, disabled by default.
+  * Execution of the newly introduced API calls (local & remote) is guarded by feature gates.
+  * Tests are implemented.
+
+* **Beta:**
+  * The feature gate is enabled by default.
+  * The feature has been succesfully used in production environment.
+  * [Possible follow-ups](#possible-follow-ups) have been reconsidered, based on any newly gathered feedback.
+  * No warning signs on performance were seen (in particular, no news of any external automation of worker ClusterQueue quotas) - or, if any were, they have been addressed.
+  * Issue [#10428](https://github.com/kubernetes-sigs/kueue/issues/10428) (see [Drawback #1](#drawbacks)) is diagnosed, its impact on quota automation is understood and any mitigations deemed necessary are implemented.
+
+* **Stable:**
+  * The feature (offered functionality, API surface and implementation) has stabilized, without raising concerns.
+  * The feature gate is removed.
```

  Updated.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T09:13:08Z

- **keps/9988-multikueue-global-quotas/README.md:238** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T09:13:08Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
```

  Adding a comment to clarify this is a great idea.
  
  However, IIRC we actually intended it to be eventually _enabled_ by default?
  So that default setting = feature gate state.
  
  For now, I specified it that way. PTAL if it makes sense.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T09:13:37Z

- **keps/9988-multikueue-global-quotas/README.md:169** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T09:13:37Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
```

  Good points. Updated.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T09:57:36Z

- **keps/9988-multikueue-manager-quota-automation/README.md:311** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T09:57:36Z
```diff
@@ -0,0 +1,742 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4 (left unaddressed)](#story-4-left-unaddressed)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [Guiding principles for the implementation](#guiding-principles-for-the-implementation)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Introduce a MultiKueue manager ClusterQueue quota reservation overbooking multiplier](#introduce-a-multikueue-manager-clusterqueue-quota-reservation-overbooking-multiplier)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add metrics for multiplier usage](#add-metrics-for-multiplier-usage)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Avoid the quota automation multiplier](#avoid-the-quota-automation-multiplier)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Expose &quot;non-multiplied&quot; total quotas in the manager ClusterQueue object](#expose-non-multiplied-total-quotas-in-the-manager-clusterqueue-object)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker's quotas.
+* Enable automated maintenance of manager quotas to keep it in sync with total worker quotas.
+* Make the above automation configurable enough to fit, at least roughly, various use cases.
+
+### Non-Goals
+
+* Fine-tune manager quota automation to achieve the optimal scheduling throughput.
+* Expose in the manager cluster a view of resource availability and usage across all connected worker clusters. (This is tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+1. Multiply the above sum by a user-configurable **multiplier**. \
+   (This is intended to address several [reasons for adjusting the manager quota](#factors-influencing-desired-manager-quota), and thus cover most cases of [User Story 1](#story-1). Also, specifying a very large multiplier is technically a way to cover [User Story 2](#story-2)).
+
+1. Last, add the requests of all (manager-side) Workloads which are currently Admitted in `Q` but their remote cluster is currently unreachable. \
+   (This is to avoid exaggerated reduction of quotas for `Q` in case when a worker cluster disconnects temporarily, and the workloads running on it still hold quota on `Q`, until `workerLostTimeout` elapses).
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+
+As a Batch Admin, I want to maintain manager quotas  **reasonably synced** to total woker quotas, to achieve a desired gate-keeping behavior on the manager.
+
+Here, the meaning of "reasonably synced" can vary by use case:
+
+* The **baseline approach** (most intuitive, and currently recommended in MultiKueue documentation) is to keep the manager quota **equal to the sum** of worker quotas.
+
+* Yet, there are several reasons for which the user may want to keep the manager quota **higher or lower** than the abovementioned sum. \
+  See [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota) for more details.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I need to fine-tune the manager gate-keeping functionality in a way which cannot be easily expressed as "total workers capacity with a multiplier".
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4 (left unaddressed)
+
+_This Story is left unaddressed in this KEP - however, we keep it mentioned here to enhance the discussion._ \
+_Addressing this Story is a goal of [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)._
+
+As a MultiKueue user (Workload Owner or Batch Admin), I want to see a summary of resource availability and usage of my whole MultiKueue setup, surfaced by the manager cluster (which I'm treating as my single control plane).
+
+In particular, I may want to see this summary broken down per worker-side ResourceFlavors.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to maintain manager quotas at a higher or a lower level.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
```

  I added an example, though a different one, PTAL if it helps!
  
  BTW thank you for requesting this; this made me realize that "single flavor" is **only a part** of what we need to require. (The other part is "all relevant resources covered by the manager-side ResourceGroup").
  I adjusted the phrasing of the constraint accordingly.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-06T10:46:50Z

- **keps/9988-multikueue-global-quotas/README.md:238** by [@mimowo](https://github.com/mimowo) — 2026-05-06T10:46:50Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
```

  I see, thanks for clarifying that. the comments lgtm.
  
  I'm also ok with the idea of enabling the sync by default, but we need to make sure this is well communicated in the release notes when graduating as a potentially breaking change for some users. Users who rely on custom sync logic (we don't know yet how common that would be) should set `quotaAutomation.mode=Disabled` prior to the upgrade to the version when the sync is enabled by default.
  
  I would just add a remainder to communicate that clearly in release notes as another graduation point for Beta.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T11:46:18Z

- **keps/9988-multikueue-global-quotas/README.md:238** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T11:46:18Z
```diff
@@ -0,0 +1,541 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all this improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on the worker but on the manager:
+
+   * the worker could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the worker could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave this cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster.
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-06T11:51:45Z

- **keps/9988-multikueue-manager-quota-automation/kep.yaml:26** by [@mimowo](https://github.com/mimowo) — 2026-05-06T11:51:45Z
```diff
@@ -27,11 +22,15 @@ latest-milestone: "v0.18"
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
   alpha: "v0.18"
-#   beta: "v0.3"
-#   stable: "v0.5"
+  beta: "v0.19"
+  stable: "v0.20"
```

  nit, these 0.19 and 0.20 timeline is too aggressive imo, let's leave the fields blank or set TBD

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T12:45:30Z

- **keps/9988-multikueue-manager-quota-automation/kep.yaml:26** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:45:30Z
```diff
@@ -27,11 +22,15 @@ latest-milestone: "v0.18"
 # The milestone at which this feature was, or is targeted to be, at each stage.
 milestone:
   alpha: "v0.18"
-#   beta: "v0.3"
-#   stable: "v0.5"
+  beta: "v0.19"
+  stable: "v0.20"
```

  Done. I set `TBD`, following the format from [KEP-3258](https://github.com/kubernetes-sigs/kueue/blob/83dfb0199f77ff25fadadc825d86e64aa8df3963/keps/3258-delayed-admission-check-retries/kep.yaml#L26).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-07T16:00:43Z

@olekzabl Thank you for working on this enhancement!

- **keps/9988-multikueue-manager-quota-automation/README.md:463** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:24:28Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that:
+
+* The manager ClusterQueue has exactly one ResourceFlavor.
+* Also, the `coveredResources` of the ResourceGroup containing that flavor must contain all resources which appear in the quotas of any related worker ClusterQueue.
+
+This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition, for example:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
```

  ```suggestion
  ### Future works
  ```
  
  Let's make this more realizable. Even if we decide not to support the following things, we should evaluate everything before the GA graduation.

- **keps/9988-multikueue-manager-quota-automation/README.md:224** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:41:03Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
```

  I'm ok with these limitations at this time, but we would relax these limitations for the future.
  For example, worker clusters have different flavors based on accelerators like worker cluster A has only alpha.vendor.com/gpu, but worker cluster B has only beta.vendor.com/gpu. Also, I can imagine the situation where some worker clusters have reserved instances, but others have only spot instances.
  
  Can this KEP proposed approach expand to such a future scenario?
  The reason why I am asking this is that "Change the treatment of ResourceFlavors" mentions just "need more discussions". I just want to confirm if we have enough space to add further mechanisms for the use cases.

- **keps/9988-multikueue-manager-quota-automation/README.md:346** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:46:47Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
```

  I'm wondering if we can consider https://github.com/kubernetes-sigs/kueue/issues/5704 support in the future without any blockers by this Global Quotas mechanism.
  
  I don't say that we should consider the design to collaborate between cluster-role sharing and Global Quotas, but I want to raise potential risks here as much as we can.

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:50:24Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  Does this mechanism aim to update the manager cluster ClusterQueue by the Kueue self?
  If yes, it's obviously unti-pattern and a nightmare (see: https://github.com/kubernetes/community/blob/main/contributors/devel/sig-architecture/api-conventions.md#spec-and-status)
  
  I would strongly propose (1 to implement such a mechanism in kueue-populator or have a new separate controller or (2 introduce new fields to record aggregated quotas to the ClusterQueue status.
  
  Or we might need to introduce both (1 and (2 because in the motivation section, you mention about gate-keeping responsibility. Hence, kueue-controller-manager can record the aggregated quotas to ClusterQueue status, then kueue-populator (or another controller) automatically updates ClusterQueue specs based on ClusterQueue status aggregated quotas.

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:52:49Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  Is there any reason to use `QuotaAutomationMode` enum?
  This is the same as `Mode bool json:"mode"`.
  
  If you consider other modes in the future, we should consider a more descriptive enum instead of enable/disable.

- **keps/9988-multikueue-manager-quota-automation/kep.yaml:14** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T15:59:57Z
```diff
@@ -1,20 +1,15 @@
-title: Concurrent Admission
-kep-number: 8691
+title: MultiKueue Manager Quota Automation
+kep-number: 9988
 authors:
-  - "@mwielgus"
-  - "@pbundyra"
-status: implementable
-creation-date: 2026-01-21
-reviewers:
-  - "@mimowo"
-  - "@ichekrygin"
-  - "@pajakd"
-  - "@tenzen-y"
   - "@olekzabl"
+status: provisional
+creation-date: 2026-04-24
+reviewers:
+  - mimowo
+  - amy
 approvers:
-  - "@mimowo"
-  - "@tenzen-y"
-  
+  - mimowo
+
```

  ```suggestion
    - amy
    - tenzen-y
  approvers:
    - mimowo
    - tenzen-y
  ```
  
  Just adding me to this list.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T16:42:25Z

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@mimowo](https://github.com/mimowo) — 2026-05-07T16:42:25Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  Maybe we could for now drop also the wrapping struct, and just have "quotaSync: Manual / Automated", wdyt? We introduced the wrapping struct for the multiplier, but that does not feel needed any longer - we can add it later.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-07T17:37:32Z

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T17:37:32Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  > Is there any reason to use QuotaAutomationMode enum?
  > This is the same as Mode bool json:"mode".
  
  I started with a bool but then learned from @mimowo that bools are discouraged... Please see [this thread](https://github.com/kubernetes-sigs/kueue/pull/10145#discussion_r3146244085).
  
  I can make it "manual / automated", though, if it's acceptable for both of you.
  
  > Maybe we could for now drop also the wrapping struct, and just have "quotaSync: Manual / Automated", wdyt? We introduced the wrapping struct for the multiplier, but that does not feel needed any longer - we can add it later.
  
  Then, if we add anything later (which I'm sensing is likely, due to the number of follow-ups considered), we'll have two top-level API fields for configuring "quota automation" behavior, which I'm feeling is not nice.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T17:45:18Z

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@mimowo](https://github.com/mimowo) — 2026-05-07T17:45:17Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  > Then, if we add anything later (which I'm sensing is likely, due to the number of follow-ups considered), we'll have two top-level API fields for configuring "quota automation" behavior, which I'm feeling is not nice.
  
  That will be one field to choose the mode, and another to configure within the mode. That pattern happens in the core k8s, for example:
  
  ```yaml
      restartPolicy: Always
      restartPolicyRules:
  ```
  Or for job `completionMode: Indexed`, and then only indexed mode has `backoffLimitPerIndex`.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-07T21:43:57Z

- **keps/9988-multikueue-manager-quota-automation/README.md:224** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T21:43:57Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
```

  > For example, worker clusters have different flavors based on accelerators like worker cluster A has only alpha.vendor.com/gpu, but worker cluster B has only beta.vendor.com/gpu. Also, I can imagine the situation where some worker clusters have reserved instances, but others have only spot instances.
  
  Hmm, I'm not sure if some misunderstanding hasn't sneaked in.
  
  As long as the flavor landscape differs across _workers_, it's **not** a blocker for using this feature. Our only requirement is a single flavor *on the manager side*. The provided [Example section](https://github.com/olekzabl/kueue/blob/1a7877ca2dfb4867a950822aa785d03798ee125f/keps/9988-multikueue-manager-quota-automation/README.md#example) also demonstrates this.
  
  So, with your scenario that "worker A has (...) but worker B has (...)" - what is the problem?
  I claim we can add up quotas of those flavors into the single manager-side flavor.
  
  Or maybe you've (a bit implicitly) assumed that *every* worker-side flavor should *also exist on the manager side*? That might be intuitively appealing but I think it *can't work in general* (as described [here](https://github.com/olekzabl/kueue/blob/1a7877ca2dfb4867a950822aa785d03798ee125f/keps/9988-multikueue-manager-quota-automation/README.md#treatment-of-resourceflavors)). To be strict, it probably *can work* under *some carefully chosen assumptions*, but our brainstorming with @mimowo proved that defining those assumptions is tricky (as described [here](https://github.com/olekzabl/kueue/blob/1a7877ca2dfb4867a950822aa785d03798ee125f/keps/9988-multikueue-manager-quota-automation/README.md#change-the-treatment-of-resourceflavors)).
  
  > Can this KEP proposed approach expand to such a future scenario? The reason why I am asking this is that "Change the treatment of ResourceFlavors" mentions just "need more discussions".
  
  Hmm, I'd disagree that I've mentioned "just need more discussions". That's the final summary, true.
  However, in the full text of that section, I have summarized a draft idea of what direction of generalizing we've found (with @mimowo ) to be potentially valuable. Let me extract key points:
  
  * Supporting multiple manager-side flavors may make sense if they're *mutually exclusive*, in that (at least, in practice) every workload will be admittable only to one flavor (based on resource set, or topology level names, or both).
  
  * In those cases, we could try mapping "worker flavor classes" to their "corresponding" manager flavors and aggregate in isolation within each such class.
  
  If you're envisioning some other kind of feasible generalization, and would like it discussed, please let me know!

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-07T21:44:56Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T21:44:55Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  > Does this mechanism aim to update the manager cluster ClusterQueue by the Kueue self?
  
  Yes.
  To be precise: by the new MultiKueue ClusterQueue Reconciler.
  
  > If yes, it's obviously unti-pattern and a nightmare (see: [link])
  
  I've read the link you provided (thank you for it!).
  However, based on that link, I can't see why it's "obviously an anti-pattern", or a "nightmare".
  I can brainstorm some potential problems with it, like controller permissions, or a threat of reconcile loops... but it's just my guessing, not based on the API Conventions doc, and I don't clearly see how large these problems are.
  
  So - could you elaborate? (Apologies in advance for my short-sightedness).
  
  To prepare better, I just had a long discussion with some strong AI bot to help me understand your remark.
  In the end, I convinced the bot :) that any sort of "pragmatic trouble" brought by this proposal will *still exist* if we move updating `.Spec.NominalQuota` to an external actor like kube-populator.
  The bot then kept convincing me that such a move is "much better" from the viewpoint of "Kubernetes philosophy" - but this part I'm still not buying.
  Basically, auto-managing `.Spec` is what it is; I don't see why moving this to another box (from Kueue to kueue-populator) would be a significant gain?
  
  But I'll be happy to learn.
  
  > I would strongly propose
  > (1 to implement such a mechanism in kueue-populator or have a new separate controller
  > or
  > (2 introduce new fields to record aggregated quotas to the ClusterQueue status.
  > Or we might need to introduce both (1 and (2
  
  Let me share my view on this:
  
  * (2 alone won't let us update gate-keeping behavior, as you noted. So I'm opposed to this.
  
  * (1 alone makes me worried about duplicating the logic. This feature sits quite deep inside MultiKueue; it relies on the whole worker client management, following the CQ -> AC -> MKConfig chain, etc.
    While I understand we could technically let kueue-populator import various bits of code from `pkg/admissioncheck/multikueue`, it feels intuitively suspicious to me.
  
  * Hence, "(1 + (2" feels most promising to me (though up to my earlier remarks that I don't understand how it'd be fundamentally better than the current proposal).
    However, if we'd go for this, I'd like to double-check with @mwielgus who IIRC once expressed a strong opinion that "sum of worker quotas should not belong to manager ClusterQueue status" (where I also didn't well understand the rationale).
  
  * Yet another approach could be to wait until the "accessible quotas" from [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864) exist, and let the worker-aggregated quotas from this KEP somehow combine directly into that.
    (Though still, for visibility, it feels quite desired to surface those aggregates in the API, in `.Spec` or `.Status` - and I'm feeling that either choice leads to some philosophy-level issues...)
  
  ---
  
  As this choice has a relatively high influence on the implementation, I feel it's essential to reach an alignment on this among the reviewers.
  
  @mimowo @mwielgus @amy PTAL

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-07T22:27:57Z

- **keps/9988-multikueue-manager-quota-automation/README.md:346** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T22:27:57Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
```

  Good point. My rough thinking is this:
  
  * If we introduce LQ/CQ-level separation (as I just suggested [here](https://github.com/kubernetes-sigs/kueue/issues/5704#issuecomment-4401615443)), this seems like no problem for this KEP.
  * Otherwise, this KEP "will be in trouble", with the CQ quota theoretically self-influencing.
    In such case, the simplest way out for this KEP would be to refuse automating in such cases (just like we currently propose to refuse automating for multiple manager-side flavors).
    Other approaches could also work (e.g.: maintain a constant diff between manager-side quota and total worker-side quotas, excluding self) - but that would require a deeper thought.
  
  I'll later try mentioning the potential interaction with #5704 in this KEP.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T08:19:09Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T08:19:09Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  P.S. In my eyes, this feature does "belong" to the scope of Kueue.
  In particular, we intend to make it eventually enabled by default (just retaining an API field to opt out).
  
  If we move it to kueue-populator or anything like this, that would defeat "enabling by default": the users would have to install & run this additional tool in order to have this feature.
  
  I think that's a downside of "(1 + (2" compared to the proposed approach.
  And that's why I'd like to understand what are the reasons to pay such a price.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T10:06:07Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T10:06:06Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  Rough idea of how to make it (hopefully) "philosophically clean" while still working as intended:
  
  1. Keep this all in `kueue-controller-manager` (no kueue-populator etc.).
  2. Equip CQ with a new field, named e.g. `.status.effectiveResourceGroups`.
     This will be the **source of truth** for the CQ quotas.
  3. When quota automation is disabled:
      * Just keep `.status.effectiveResourceGroups` synced to the user-provided `.spec.resourceGroups`.
      * Later on, e.g. per [KEP-8826](https://github.com/kubernetes-sigs/kueue/pull/8864), this "sync" may evolve into some non-trivial logic.
  4. When quota automation is enabled:
     * Set `.status.effectiveResourceGroups` based on the aggregation, as currently in this KEP.
     * **Require** that `.spec.resourceGroups` is empty.
       * *Why require?* Because otherwise we'd have to ignore it, and ignoring a well-established spec field feels terrible. OTOH, if that field is empty, it's a clear sign that sth new is in charge.
       * *How require?* I see 2 options:
         * A: Move `quotaAutomationMode` to CQSpec, then we can have a strict validation.
         * B: Keep `quotaAutomationMode` in MKConfig, then we can't have a strict validation, but we can make CQ `Inactive` if the constraint is not satisfied.
  5. Treatment of flavors:
     * For Alpha, for mental simplicity, I'd keep the rule of "single flavor on the manager".
       That is, in `.status.effectiveRGs`, I'd set up a single flavor, named e.g. `total-worker-capacity`.
     * For Beta, we can consider various extensions - even more than now - but it's still a tricky area.
       (I can elaborate if there's interest. For now, I'll stop here to keep this comment shorter).
  
  **Risks** (I'm not sure how bad):
  1. CQs with automation enabled will be interpreted as empty by any older Kueue binaries.
  2. The new "source of truth" field will be often transiently empty upon CQ creation.
  
  ---
       
  I'd be grateful for early thoughts on this, so that I know if it makes any sense to evolve the KEP in this direction.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T10:19:31Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@mimowo](https://github.com/mimowo) — 2026-05-08T10:19:30Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  I talked about this with @tenzen-y and @olekzabl. I think that the UX will be better with updating the CQ .spec, but I understand also the concern that updating the .spec by controller is "philosophically a design smell" - still this is used in multiple CRDs in core k8s, like for example .spec.NodeName, or Node's spec.taints
  
  The alternative to update the .status.effectiveResourceGroups sounds good to me, and this is the direction we want to also take in the future with other features, like Hero Workloads, so I think it makes sense. 
  
  I would propose for Alpha to update `.spec` as this is simplest and does not commit us to any new APIs we would need more time to review. So we could probably still aim for 0.18 with Alpha.
  
  At the same time we would add a graduation crtieria for Beta to re-evaluate the `status.effectiveResourceGroups` approach, and I'm totally ok to end up with that approach.
  
  wdyt @tenzen-y ?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T12:03:55Z

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T12:03:55Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  I flattened this field, and renamed to
  ```yaml
  quotaManagement: Manual / Automated
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T12:06:25Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T12:06:25Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  I described the `.status.effectiveResourceGroups` idea in [this future work idea](https://github.com/olekzabl/kueue/blob/b658d95409e93cd3e354f0eb3ff2b1d29e1dc955/keps/9988-multikueue-manager-quota-automation/README.md#move-the-aggregated-quota-out-of-clusterqueuespec).
  
  Keep in mind that the following is already listed as a Beta graduation criterion:
  > [Future work ideas](https://github.com/olekzabl/kueue/blob/b658d95409e93cd3e354f0eb3ff2b1d29e1dc955/keps/9988-multikueue-manager-quota-automation/README.md#future-work-ideas) have been reconsidered, based on any newly gathered feedback.
  
  Please LMK if this is satisfactory, or if anyone claims we should switch to this direction already in Alpha.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T12:06:58Z

- **keps/9988-multikueue-manager-quota-automation/README.md:346** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T12:06:58Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
```

  Added [this future work idea](https://github.com/olekzabl/kueue/blob/b658d95409e93cd3e354f0eb3ff2b1d29e1dc955/keps/9988-multikueue-manager-quota-automation/README.md#support-multikueue-cluster-role-sharing).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-08T12:10:00Z

- **keps/9988-multikueue-manager-quota-automation/README.md:463** by [@olekzabl](https://github.com/olekzabl) — 2026-05-08T12:10:00Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
+}
+
+type QuotaAutomationMode string
+
+const (
+   QuotaAutomationDisabled QuotaAutomationMode = "Disabled"
+   QuotaAutomationEnabled  QuotaAutomationMode = "Enabled"
+)
+```
+
+The status of the feature will be communicated by a new Condition added to ClusterQueueStatus. The Condition will be present in all ClusterQueues enrolled in MultiKueue.
+
+When quota automation is requested and supported, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: True
+reason: QuotaAutomated
+message: ClusterQueue quota is automatically managed based on MultiKueue workers.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+where `lastTransitionTime` only tracks changes affecting the Condition `status` and `message` (i.e. feature enablement, potential errors) but **not** subsequent quota updates. (See [this section](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative) in Alternatives).
+
+When quota automation is not requested, the condition will look like this:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: NotRequested
+message: MultiKueue manager quota automation has not been requested.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+Enabling quota automation will be subject to a **constraint** that:
+
+* The manager ClusterQueue has exactly one ResourceFlavor.
+* Also, the `coveredResources` of the ResourceGroup containing that flavor must contain all resources which appear in the quotas of any related worker ClusterQueue.
+
+This constraint will **not** be enforced via validation (e.g. a webhook) but by Kueue controllers code (similarly to [analogous existing AdmissionCheck-related constraints](https://github.com/kubernetes-sigs/kueue/blob/6163e91e5a62befbdd421097fe0ec38b37d406e0/pkg/cache/scheduler/clusterqueue.go#L284-L307), evaluated in the core ClusterQueue controller). A violation of this new constraint will **not** make the ClusterQueue Inactive (as is the case for the existing analogous constraints). In this case of a violation, the queue itself will be still operational, just not supportable by the quota automation feature. This will be communicated as an error of that feature, by its status Condition, for example:
+
+```yaml
+type: MultiKueueManagerQuotaAutomation
+status: False
+reason: UnsupportedConfiguration
+message: MultiKueue manager quota automation does not support ClusterQueues with multiple ResourceFlavors.
+lastTransitionTime: 2026-01-01T00:00:00Z
+```
+
+### MultiKueue ClusterQueue Reconciler
+
+We will introduce a new reconciler for manager-side ClusterQueue objects. Reconciling a manager ClusterQueue `Q` will proceed as follows:
+
+1. **Verify enablement:** Fetch `Q`, its associated MultiKueue AdmissionCheck, and MultiKueueConfig. Verify that quota automation is requested and supported (e.g., `Q` must have exactly one ResourceFlavor).
+
+2. **Update the Condition:** If needed, update the `MultiKueueManagerQuotaAutomation` status Condition.
+
+3. **Aggregate quotas:** Find all [related worker ClusterQueues](#defining-related-worker-clusterqueues). Sum their quotas and update `Q`'s quotas accordingly.
+
+The reconciler will be triggered by:
+* **Manager events:** Creation, deletion, or relevant updates to ClusterQueues, LocalQueues, AdmissionChecks, or MultiKueueConfigs.
+* **Remote events:** Propagated from the [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler) when remote LocalQueues or ClusterQueues change.
+
+### MultiKueue Cluster Reconciler
+
+In `multikueuecluster.go`, we will add watches on remote clusters for **LocalQueue** and **ClusterQueue** events. When any of those change, we will find all [related manager ClusterQueues](#defining-related-worker-clusterqueues), and trigger the [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler) for each of them.
+
+### A note on client usage
+
+In the above reconcilers, we generally intend to happily tolerate K8s client calls, without introducing any custom caching.
+
+This is because:
+
+1. Local clients are already cached.
+2. Remote calls will be made only for "memory-light" resource kinds (LocalQueues and ClusterQueues), which typically do not exist in large numbers.
+3. The reconcilers involved are not expected to run frequently.
+
+Item 3 may not hold if worker clusters use any tooling (external to Kueue) for automated management of their queue quotas. If this is ever observed, we may need to optimize, e.g. by [caching our remote calls](#use-cached-remote-clients-for-low-volume-resource-kinds).
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes necessary
+to implement this enhancement.
+
+#### Unit Tests
+
+* Test the reconcile logic with all exit paths.
+* Test all additional triggering paths from local watchers.
+
+#### Integration Tests
+
+* Test triggering the MultiKueue ClusterQueue reconciler by changes in remote LocalQueues and ClusterQueues.
+
+#### E2e Tests
+
+* Test the scenario of a ClusterQueue having quota automation disabled -> then enabled (and reacting to some change) -> then disabled.
+* Verify that manager quota auto-adjustments promptly trigger the expected re-assignments of Workloads.
+
+### Possible Follow-ups
```

  > Let's make this more realizable. 
  
  I renamed it to "Future work ideas". (I wanted to keep it a bit less strict, given that we're still unsure if all these ideas will be pursued).
  
  > Even if we decide not to support the following things, we should evaluate everything before the GA graduation.
  
  Please note that the following is already listed as a graduation criterion for **Beta**:
  
  _[Future work ideas](https://github.com/olekzabl/kueue/blob/b658d95409e93cd3e354f0eb3ff2b1d29e1dc955/keps/9988-multikueue-manager-quota-automation/README.md#future-work-ideas) have been reconsidered, based on any newly gathered feedback._

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T12:24:40Z

- **keps/9988-multikueue-manager-quota-automation/README.md:363** by [@mimowo](https://github.com/mimowo) — 2026-05-08T12:24:39Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
+### API surface
+
+The configuration for this feature will be added to the existing `MultiKueueConfig` struct. (For context, `MultiKueueConfig` objects are referenced from `AdmissionCheckSpec` as [`Parameters`](https://github.com/kubernetes-sigs/kueue/blob/24209c461b72fd6519581aca2234fb8f05dd1ce7/apis/kueue/v1beta2/admissioncheck_types.go#L60); this will allow controlling the feature independently for each ClusterQueue in the manager cluster).
+
+```go
+type MultiKueueConfigSpec struct {
+   // ...
+
+   // quotaAutomation specifies the automation behavior of ClusterQueue quotas 
+   // in the manager cluster. 
+   // If unspecified, the default mode depends on the MultiKueueQuotaAutomation feature gate.
+   QuotaAutomation *QuotaAutomation `json:"quotaAutomation,omitempty"`
+}
+
+type QuotaAutomation struct {
+   // mode specifies the automation mode.
+   // Supported modes:
+   // - `Disabled`: Quota automation is disabled.
+   // - `Enabled`: Quota automation is enabled (provided that the MultiKueueQuotaAutomation feature gate is enabled).
+   //
+   // +kubebuilder:validation:Enum=Disabled;Enabled
+   // +kubebuilder:default=Enabled
+   Mode QuotaAutomationMode `json:"mode"`
```

  sgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T12:30:51Z

- **keps/9988-multikueue-manager-quota-automation/README.md:347** by [@mimowo](https://github.com/mimowo) — 2026-05-08T12:30:51Z
```diff
@@ -0,0 +1,661 @@
+# KEP-9988: MultiKueue Manager Quota Automation
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [The role of manager quotas](#the-role-of-manager-quotas)
+  - [Manager vs. workers separation](#manager-vs-workers-separation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Example](#example)
+  - [User stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+    - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats](#notesconstraintscaveats)
+    - [Factors influencing desired manager quota](#factors-influencing-desired-manager-quota)
+      - [Potential reasons for increasing manager quota](#potential-reasons-for-increasing-manager-quota)
+      - [Potential reasons for decreasing manager quota](#potential-reasons-for-decreasing-manager-quota)
+    - [Defining related worker ClusterQueues](#defining-related-worker-clusterqueues)
+    - [Treatment of ResourceFlavors](#treatment-of-resourceflavors)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design details](#design-details)
+  - [API surface](#api-surface)
+  - [MultiKueue ClusterQueue Reconciler](#multikueue-clusterqueue-reconciler)
+  - [MultiKueue Cluster Reconciler](#multikueue-cluster-reconciler)
+  - [A note on client usage](#a-note-on-client-usage)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+    - [E2e Tests](#e2e-tests)
+  - [Possible Follow-ups](#possible-follow-ups)
+    - [Use cached remote clients for low-volume resource kinds](#use-cached-remote-clients-for-low-volume-resource-kinds)
+    - [Add a way to adjust the manager-side quota](#add-a-way-to-adjust-the-manager-side-quota)
+    - [Change the treatment of ResourceFlavors](#change-the-treatment-of-resourceflavors)
+    - [Account for temporarily unavailable workers](#account-for-temporarily-unavailable-workers)
+  - [Graduation Criteria](#graduation-criteria)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Support quota automation for multiple manager-side ResourceFlavors](#support-quota-automation-for-multiple-manager-side-resourceflavors)
+  - [Support quota automation for no manager-side ResourceFlavors (auto-create one)](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)
+  - [Different handling of many-to-many relationships between manager-side and worker-side ClusterQueues](#different-handling-of-many-to-many-relationships-between-manager-side-and-worker-side-clusterqueues)
+  - [Make the <code>MultiKueueManagerQuotaAutomation</code> Condition message more informative](#make-the-multikueuemanagerquotaautomation-condition-message-more-informative)
+  - [Take Cohorts into account](#take-cohorts-into-account)
+<!-- /toc -->
+
+## Summary
+
+This KEP outlines the design for automatic management of ClusterQueue quotas on the manager cluster in a MultiKueue setup, based on aggregation of quotas of worker ClusterQueues.
+
+## Motivation
+
+MultiKueue involves **two layers** of Kueue quota management. A MultiKueue workload must first reserve quota on the **manager** cluster in order to be dispatched to (one or more) workers; then it must reserve quota on a **worker** in order to execute there.
+
+### The role of manager quotas
+
+While _worker quotas_ are the _ultimate definition_ of the actual resource availability, the role of _manager quotas_ is subtler, yet still important.
+
+Specifically, manager quotas **may** serve:
+
+* As a **gate-keeping** mechanism, which allows distributing the _scheduling load_ between the manager and the workers. \
+  (This may be irrelevant for small MultiKueue setups - but is essential for scalability of the largest ones).
+
+* As a **representation** of resource availability and usage, viewable by the user without connecting to the worker clusters.
+
+Manager quotas are **effectively optional**: when considered not useful, the Batch Admin can effectively disable them by specifying very large values ("infinite quota"). This may work well for smaller setups (and some users actually do this) - but is not recommended generally.
+
+### Manager vs. workers separation
+
+Currently, quota management is **fully separated** between the manager and workers, up to the point that both sides are **unaware** of each other's quotas.
+
+While this approach offers simplicity and network savings, it also leads to problems:
+
+1. For the manager quotas to serve their purposes discussed [above](#the-role-of-manager-quotas), they often must be **maintained in sync** with the worker quotas. \
+   (The exact meaning of "in sync" may vary by use case; see [User Story 1](#story-1)). \
+   Currently, MultiKueue does not help in this maintenance, making it entirely **user's responsibility**. \
+   This is inconvenient and error-prone.
+
+2. For a user to get the overview of actual resource availability and usage across the worker fleet, it is necessary to connect to all worker clusters and aggregate the information on their own.
+
+3. For the MultiKueue controller on the manager cluster, having no awareness of workers' quotas (and their usage) **prevents more effective dispatching** choices. \
+   Even though we generally don't want the manager to take over _the whole_ orchestrating work from the workers (as we intend to share it between both sides), some improvements are likely possible. For example, if a workload fits within the quota of worker A but not of worker B, there is no point in dispatching it to B before A. (At least, unless B could admit it through borrowing).
+
+   While designing specific improvements to dispatching is **out of scope** of this KEP, all these improvements require as the **first step** that the manager *knows* workers' quotas, which this KEP proposes. \
+   (Then, the second step would be to let the manager *persist* this information. That part is not proposed in this KEP, as it's not needed for its goals; however, it will be proposed in the KEP for issue [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+
+### Goals
+
+* Make the manager cluster aware of worker quotas.
+* Enable automated maintenance of manager quotas to keep them in sync with worker capacity.
+
+### Non-Goals
+
+* Fine-tune manager quota automation for optimal scheduling throughput.
+* Expose a central view of resource availability and usage across all workers (tracked in [#10105](https://github.com/kubernetes-sigs/kueue/issues/10105)).
+* Improve MultiKueue dispatching.
+
+## Proposal
+
+We will add an **optional** feature of **auto-aggregating quotas** from the workers to the manager.
+
+**When enabled** for a manager ClusterQueue `Q`, this feature will set its quota for a resource `R` according to the following recipe:
+
+1. Determine the set of [related worker ClusterQueues](#defining-related-worker-clusterqueues), i.e. those which can serve the dispatched remotes of the workloads submitted to `Q`.
+
+1. Compute the **sum** of quotas for `R` in **all** _related_ worker ClusterQueues, across **all** ResourceFlavors.
+
+Enabling this feature will be controlled by an API field (not just by a feature gate), as we want to permanently retain a way to opt out (see [User Story 3](#story-3)).
+
+Enabling this feature will **require** that the manager ClusterQueue `Q` has **exactly one ResourceFlavor**. (See [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for rationale).
+
+### Example
+
+Suppose we have a MultiKueue setup with a manager cluster and two worker clusters (`worker1` and `worker2`), using the following configuration for LocalQueues (LQ) and ClusterQueues (CQ):
+
+```mermaid
+graph BT
+    subgraph Manager ["Manager Cluster"]
+        m_cq["cq"]
+        m_lq1["lq1"]
+        m_lq2["lq2"]
+        m_lq1 --> m_cq
+        m_lq2 --> m_cq
+    end
+
+    subgraph Worker2 ["Worker 2"]
+        w2_cq["cq"]
+        w2_lq1["lq1"]
+        w2_lq2["lq2"]
+        w2_lq1 --> w2_cq
+        w2_lq2 --> w2_cq
+    end
+
+    subgraph Worker1 ["Worker 1"]
+        w1_cq1["cq1"]
+        w1_cq2["cq2"]
+        w1_lq1["lq1"]
+        w1_lq2["lq2"]
+        w1_lq1 --> w1_cq1
+        w1_lq2 --> w1_cq2
+    end
+
+    m_lq1 -.-> w1_lq1
+    m_lq1 -.-> w2_lq1
+    m_lq2 -.-> w1_lq2
+    m_lq2 -.-> w2_lq2
+```
+
+
+On `worker1`, both related ClusterQueues are configured with CPU and memory (in `default-flavor`):
+
+*   `cq1`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 6
+        - name: "memory"
+          nominalQuota: 24Gi
+    ```
+*   `cq2`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 7
+        - name: "memory"
+          nominalQuota: 28Gi
+    ```
+
+On `worker2`, the related ClusterQueue `cq` handles both `lq1` and `lq2`, and is configured with CPU, memory, and GPU resources:
+
+*   `cq`:
+    ```yaml
+    resourceGroups:
+    - coveredResources: ["cpu", "memory"]
+      flavors:
+      - name: "default-flavor"
+        resources:
+        - name: "cpu"
+          nominalQuota: 8
+        - name: "memory"
+          nominalQuota: 32Gi
+    - coveredResources: ["nvidia.com/gpu"]
+      flavors:
+      - name: "gpu-a4"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 5
+      - name: "gpu-h100"
+        resources:
+        - name: "nvidia.com/gpu"
+          nominalQuota: 7
+    ```
+
+When quota aggregation is enabled on the manager-side ClusterQueue `cq`, the aggregated limits combine all resources from the related worker ClusterQueues (`cq1` and `cq2` on `worker1`, and `cq` on `worker2`), summing their limits across all worker-side flavors:
+
+```yaml
+resourceGroups:
+- coveredResources: ["cpu", "memory", "nvidia.com/gpu"]
+  flavors:
+  - name: "default-flavor"
+    resources:
+    - name: "cpu"
+      nominalQuota: 21  // 6 + 7 + 8
+    - name: "memory"
+      nominalQuota: 84Gi  // 24 + 28 + 32
+    - name: "nvidia.com/gpu"
+      nominalQuota: 12  // 5 + 7
+```
+
+This will work only if:
+
+* `cq` on the manager is configured with a single flavor, and
+* The `coveredResources` for this flavor contains all involved resources (CPU, memory and GPU).
+
+### User stories
+
+#### Story 1
+As a Batch Admin, I want to maintain manager quotas automatically synced to the sum of total worker quotas to achieve optimal gate-keeping without manual effort.
+
+#### Story 2
+
+As a Batch Admin, I want to keep MultiKueue manager quotas "infinite". 
+
+In my use case, the potential gains from meaningful ("finite") manager quotas are outweighed by the effort of maintaining them in "appropriate sync", or by the cost of missed opportunities to schedule (in case we haven't sufficiently [increased the manager quota](#potential-reasons-for-increasing-manager-quota)).
+
+#### Story 3
+
+As a Batch Admin, I want to keep MultiKueue manager quotas meaningful ("finite") but **not managed** by the feature proposed in this KEP.
+
+This could be because:
+
+* I need multiple manager ResourceFlavors (due to e.g. using a special dispatcher or a heterogenous topology across the workers; see [Treatment of ResourceFlavors](#treatment-of-resourceflavors) for details).
+
+* I want to control the manager quotas manually (e.g. to make them more stable, or less confusing).
+
+#### Story 4
+
+As a Batch Admin using MultiKueue, I want to see the total worker capacity for a manager-side ClusterQueue, without a need to connect to the worker clusters.
+
+### Notes/Constraints/Caveats
+
+#### Factors influencing desired manager quota
+
+While the "baseline approach" (keep manager quota **equal** to total worker quota) is the most intuitive one, there are several reasons for which the user may wish to adjust this.
+
+##### Potential reasons for increasing manager quota
+
+1. **Divergence of quota reservation** between manager and workers - caused e.g. by how MultiKueue design deals with quota fragmentation among the workers.
+
+   For example, suppose there are 2 workers of capacity 10 CPU, and workloads `wl1`, `wl2`, `wl3`, `wl4` of sizes 7, 7, 6, 3 CPU respectively are submitted to the manager (in that order).
+
+   Then, if manager quota is 10+10, `wl3` will block quota on the manager and get stuck on both workers, preventing `wl4` from reserving quota on the manager.
+
+   OTOH, if manager quota is 23 or more, `wl4` will reserve quota on the manager, and get promptly scheduled on one of the workers, with no unnecessary delays.
+
+2. **Divergence of admission** between manager and workers - caused e.g. by issue [#8585](https://github.com/kubernetes-sigs/kueue/issues/8585) or issue [#9338](https://github.com/kubernetes-sigs/kueue/issues/9338).
+
+   In both these issues, it is possible to have a workload which is _admitted_ on the manager but _not admitted_ on _any_ of the workers (and to have such state for a prolonged time). Whenever that happens, a "perfect sync" between manager and workers quotas could lead to under-utilizing worker resources.
+
+   Even when both those issues become closed, the fixes will be placed behind feature gates, making them still observable in the upcoming release.
+
+3. **Borrowing on a worker**. This may raise the effective capacity of a worker CQ beyond its nominal quota. If the manager quota does not account for that, we risk under-utilization.
+
+4. Other reasons _may_ also exist. For example - assuming hypothetically that a user wants to apply different preemption policies on different worker ClusterQueues (e.g. `Never` on one vs. `LowerPriority` on another) - "over-booking quota" on the manager would be the only way to have both policies honored without extra delays.
+
+##### Potential reasons for decreasing manager quota
+
+1. **Stray workloads on a worker**. When a LocalQueue `lq` points to a ClusterQueue `cq`, both on the manager and a worker, there are numerous ways to have a workload admitted to `cq` on a worker but not on the manager:
+
+   * the workload could be submitted directly to `lq` on the worker, "by-passing" MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, where `lq2` also points to `cq` on the worker but is unrelated to MultiKueue;
+   * the workload could be submitted to another LocalQueue `lq2` on the worker, while `lq2` exists on the manager but points to another `cq2` with MultiKueue enabled.
+
+   Some users consider such scenarios valid, and expressed a desire to keep a part of the worker capacity reserved for such "stray workloads". In such cases, the manager quota should be accordingly _decreased_.
+
+2. **Per-team quotas on the manager** (strictly speaking, a special case of "stray workloads").
+
+   A Batch Admin may want to maintain a set of per-team manager ClusterQueues in order to specify separate quotas for them (possibly, but not necessarily, including Cohorts and borrowing), while simplifying the worker-side setup to single ClusterQueues representing actual resource availability.
+
+   In this scenario, each manager ClusterQueue should have quota _significantly below_ the total worker quota; we'd rather consider _the sum_ of all manager quotas to be "on par" with total worker quotas (up to all the divergences considered above).
+
+   Nevertheless, some automatic synchronization may be still useful, if equipped with per-team multipliers. For example: "Let the nominal quota for teams A / B / C be respectively defined as 50% / 30% / 20% of the total available worker quota".
+
+#### Defining related worker ClusterQueues
+
+MultiKueue dispatching connects **LocalQueues** by **their names**: for a workload submitted to a LocalQueue `lq1` on the manager, its remotes will be submitted to LocalQueues named `lq1` on the workers. The **ClusterQueue names** on either side are **irrelevant** for this connection.
+
+Therefore, for a given manager ClusterQueue `Q`, the set of **related worker ClusterQueues** is the set `R` defined by the following recipe:
+
+1. Let `N` = the set of names of all manager LocalQueues pointing to `Q`.
+
+2. Let `L` = the set of LocalQueues on all workers whose name belongs to `N`.
+
+3. Let `R` = the set of all ClusterQueues on all workers to which at least one LocalQueue from `L` points.
+
+(For simplicity, we've ignored resource borrowing between ClusterQueues, on either manager or worker side).
+
+#### Treatment of ResourceFlavors
+
+MultiKueue enforces **no relationships** between ResourceFlavors on the manager and worker sides. The flavors on both sides may have different names; they may even be differently grouped into ResourceGroups.
+
+Even if the flavor names on both sides agree, having multiple flavors on the manager side may likely lead to **manager/worker flavor skew**, where a workload could be admitted on flavor A on the manager but on flavor B on a worker (as the manager's flavor assignment has typically no effect on the remotes). As long as such skews can happen, aggregating quotas per flavor would be meaningless.
+
+As a result, multiple flavors on the manager side _should not be needed for most users_. Yet, they still may be needed in some special cases, for example:
+
+* Using a flavor-aware custom MultiKueue dispatcher.
+* Using TAS on the workers, with incompatible names of topology levels on different workers.
+
+Our proposal for now is to leave these cases unsupported. (Some other ideas are discussed below, see [here](#change-the-treatment-of-resourceflavors), [here](#support-quota-automation-for-multiple-manager-side-resourceflavors) and [here](#support-quota-automation-for-no-manager-side-resourceflavors-auto-create-one)).
+
+### Risks and Mitigations
+
+The main risks of this proposal are the following:
+
+1. User confusion - the users may be surprised by ClusterQueue quotas being adjusted automatically, without their initiative.
+
+2. Introducing management of ClusterQueue quotas which some users may consider undesired (see [User Story 3](#story-3)).
+
+3. Slowing down MultiKueue by adding more reconcilers and computations.
+
+For these, we propose the following mitigations:
+
+* Risk 1 is mitigated by a dedicated ClusterQueue condition, explaining the automation process.
+
+* Risks 1 and 2 are mitigated by introducing a feature gate and the opt-in/opt-out API. \
+  This also mitigates Risk 3, as long as we pay attention to skip computations which are not necessary per the ClusterQueue configuration.
+
+* Risk 3 is considered low. If needed, it can be substantially mitigated by caching the relevant information.
+
+## Design details
+
```

  > Please LMK if this is satisfactory, or if anyone claims we should switch to this direction already in Alpha.
  
  We may also consider this direction for the second iteration of Alpha.
  
  So I would propose:
  1. first alpha is based on spec 
  2. second alpha is based on `status.effectiveResourceGroups` (which we need anyway for multiplier and other features likely)
  3. beta may or may not drop setting the spec (tbd on @tenzen-y's position here)
  
  This way we can separate the two big implementation steps  (1.) and (2.).
