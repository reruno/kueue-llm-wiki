# PR #10146: Fix running ginkgo when focus and label filter is used

**Summary**: Fix running ginkgo when focus and label filter is used

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10146

**Last updated**: 2026-03-27T14:46:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `fix-running-e2e-tests` → `main`
- **Created**: 2026-03-27T11:31:40Z
- **Updated**: 2026-03-27T14:46:03Z
- **Merged**: 2026-03-27T14:44:48Z
- **Closed**: 2026-03-27T14:44:48Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@gabesaba](https://github.com/gabesaba), [@kshalot](https://github.com/kshalot)
- **Changed files**: 3   **+32 / -5**

## Description


#### What type of PR is this?

/kind bug
/kind cleanup

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10087 

#### Special notes for your reviewer:

Fix assisted with llm

I checked experimentally this works well:

```sh
GINKGO_ARGS="--repeat=1 --label-filter=feature:kuberay" E2E_MODE=dev make test-e2e
```
if the focus is enabled on a test with `ginkgo.FIt(`

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T11:31:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10146#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/testing/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/testing/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-27T11:31:59Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 89db77d227a4cab2b171bb1386c189d19cee6a91 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69c66adcb482360008e45fad |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:34:03Z

cc @mbobrovskyi ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-03-27T11:36:42Z

/cherrypick release-0.16
/cherrypick release-0.15

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-27T11:36:44Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.15`, `release-0.16` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10146#issuecomment-4141981770):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-27T14:05:19Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-27T14:05:28Z

LGTM label has been added.  <details>Git tree hash: 06fd718de641cf5b2de6d83ece0bc95fd34be7b7</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-27T14:45:28Z

@mimowo: #10146 failed to apply on top of branch "release-0.16":
```
Applying: Fix running ginkgo when focus and label filter is used
Using index info to reconstruct a base tree...
M	hack/testing/e2e-common.sh
M	hack/testing/e2e-multikueue-test.sh
M	hack/testing/e2e-test.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/testing/e2e-test.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-test.sh
Auto-merging hack/testing/e2e-multikueue-test.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-multikueue-test.sh
Auto-merging hack/testing/e2e-common.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix running ginkgo when focus and label filter is used

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10146#issuecomment-4141981770):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-03-27T14:46:03Z

@mimowo: #10146 failed to apply on top of branch "release-0.15":
```
Applying: Fix running ginkgo when focus and label filter is used
Using index info to reconstruct a base tree...
M	hack/testing/e2e-common.sh
M	hack/testing/e2e-multikueue-test.sh
M	hack/testing/e2e-test.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/testing/e2e-test.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-test.sh
Auto-merging hack/testing/e2e-multikueue-test.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-multikueue-test.sh
Auto-merging hack/testing/e2e-common.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Fix running ginkgo when focus and label filter is used

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10146#issuecomment-4141981770):

>/cherrypick release-0.16
>/cherrypick release-0.15


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-27T13:08:07Z

- **hack/testing/e2e-common.sh:1292** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-27T13:08:07Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  ```suggestion
    set -x
  ```
  
  Can we use just `set -x` to enable debug mode? It should print executed commands

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T13:18:12Z

- **hack/testing/e2e-common.sh:1292** by [@mimowo](https://github.com/mimowo) — 2026-03-27T13:18:12Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  but doesn't it leak outside the function? I think it would be weird if this function enables debug logging for all downstream functions

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-27T13:20:04Z

- **hack/testing/e2e-common.sh:1292** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-27T13:20:03Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  I don't think so but to make sure we can set `set +x` after command.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-27T13:32:35Z

- **hack/testing/e2e-common.sh:1292** by [@kshalot](https://github.com/kshalot) — 2026-03-27T13:32:35Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  I think it will leak. That's why there's handling for `set -f` (globbing above).
  
  But I'm wondering whether we could just run this function in a subshell (parentheses) instead of handling setting/unsetting. This would localize the setting to this function without having to manage it, but there might be some nuance with vars/envvars not being propagated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T13:35:04Z

- **hack/testing/e2e-common.sh:1292** by [@mimowo](https://github.com/mimowo) — 2026-03-27T13:35:04Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  Actually I confirmed that it leaks, and so using `set +x` also leaks, see that output:
  
  
  The "+" indicate the debug lines due to "set -x", and clearly that leaks outside the ginkgo helper.
  ```
  validatingwebhookconfiguration.admissionregistration.k8s.io/kueue-validating-webhook-configuration serverside-applied
  + /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/hack/testing/../../bin/ginkgo run --repeat=0 --label-filter=feature:lws -procs=2 --junit-report=junit.xml --json-report=e2e.json --output-dir=/usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/bin/run-test-e2e-singlecluster-1.35.0 -v ./test/e2e/singlecluster/...
  Running Suite: End To End Suite: kindest/node:v1.35.0 - /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/test/e2e/singlecluster
  ============================================================================================================================================
  Random Seed: 1774618287
  
  Will run 0 of 88 specs
  Running in parallel across 2 processes
  SSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSSS
  ------------------------------
  [ReportAfterSuite] PASSED [0.010 seconds]
  [ReportAfterSuite] Autogenerated ReportAfterSuite for --json-report --junit-report
  autogenerated by Ginkgo
  ------------------------------
  
  Ran 0 of 88 Specs in 0.080 seconds
  SUCCESS! -- 0 Passed | 0 Failed | 0 Pending | 88 Skipped
  
  
  Ginkgo ran 1 suite in 3.829813788s
  Test Suite Passed
  Detected Programmatic Focus - setting exit status to 197
  + cleanup
  + '[' '!' -d /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/bin/run-test-e2e-singlecluster-1.35.0 ']'
  + cluster_collect_artifacts kind ''
  + local name=kind
  + local kubeconfig=
  + kubectl_args=()
  + local -a kubectl_args
  + [[ -n '' ]]
  + kubectl config use-context kind-kind
  Switched to context "kind-kind".
  + /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/hack/testing/../../bin/kind export logs /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/bin/run-test-e2e-singlecluster-1.35.0 --name kind
  Exporting logs for cluster "kind" to:
  /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/bin/run-test-e2e-singlecluster-1.35.0
  + kubectl describe pods -n kueue-system
  + kubectl describe pods
  + e2e_should_delete_cluster
  + [[ dev != dev ]]
  + echo 'Keeping kind cluster '\''kind'\'' (E2E_MODE=dev).'
  + echo 'To delete it:'
  + echo '  kind delete clusters kind'
  Keeping kind cluster 'kind' (E2E_MODE=dev).
  To delete it:
    kind delete clusters kind
  make: *** [Makefile-test.mk:183: run-test-e2e-singlecluster-1.35.0] Error 197
  ```
  
  Using "set +x"  is also not ideal, because it leaks disabling debug, so if a developer runs `GINKGO_ARGS="--repeat=0 --label-filter=feature:lws" E2E_MODE=dev make SHELL=/bin/bash '.SHELLFLAGS=-x -c' test-e2e`, then using that command will disable debug

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-03-27T13:48:51Z

- **hack/testing/e2e-common.sh:1292** by [@mimowo](https://github.com/mimowo) — 2026-03-27T13:48:51Z
```diff
@@ -1263,3 +1263,33 @@ EOF
     echo "Upgrade complete (rolling update finished)"
     echo "========================================="
 }
+
+# Run ginkgo e2e tests with extra CLI flags from GINKGO_ARGS.
+#
+# GINKGO_ARGS may contain multiple flags and quoted values with spaces, e.g.
+#   GINKGO_ARGS='--repeat=2 --focus="should run one case"'
+# so it must be reparsed into an array before invocation.
+# Package paths must not be passed via GINKGO_ARGS; they come from "$@".
+run_e2e_ginkgo() {
+  local ginkgo_extra=()
+  local had_noglob=0
+  local rc=0
+
+  if [[ -n ${GINKGO_ARGS:-} ]]; then
+    # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
+    # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
+    [[ $- == *f* ]] && had_noglob=1
+    set -f
+    eval "ginkgo_extra=($GINKGO_ARGS)"
+    rc=$?
+    (( had_noglob == 0 )) && set +f
+    (( rc == 0 )) || return "$rc"
+  fi
+
+  # Print the exact argv for troubleshooting quoting / flag-order issues.
+  printf 'running:' >&2
+  printf ' %q' "$GINKGO" run "${ginkgo_extra[@]}" "$@" >&2
+  printf '\n' >&2
```

  > I think it will leak. That's why there's handling for `set -f` (globbing above).
  > 
  > But I'm wondering whether we could just run this function in a subshell (parentheses) instead of handling setting/unsetting. This would localize the setting to this function without having to manage it, but there might be some nuance with vars/envvars not being propagated.
  
  Ok, I confirmed this works and does not leak:
  
  ```sh
  run_e2e_ginkgo() (
    set -f
    set -x
    local ginkgo_extra=()
    local had_noglob=0
    local rc=0
  
    if [[ -n ${GINKGO_ARGS:-} ]]; then
      # Preserve the caller's globbing state. While reparsing GINKGO_ARGS we want
      # shell quoting to work, but we do not want '*' or '?' expanded to filenames.
      [[ $- == *f* ]] && had_noglob=1
      set -f
      eval "ginkgo_extra=($GINKGO_ARGS)"
      rc=$?
      (( had_noglob == 0 )) && set +f
      (( rc == 0 )) || return "$rc"
    fi
  
    "$GINKGO" run "${ginkgo_extra[@]}" "$@"
  )
  ```
  but the output looks quite messy:
  ```
  + ginkgo_extra=()
  + local ginkgo_extra
  + local had_noglob=0
  + local rc=0
  + [[ -n --repeat=0 --label-filter=feature:lws -procs=2 ]]
  + [[ efhuxB == *f* ]]
  + had_noglob=1
  + set -f
  + eval 'ginkgo_extra=(--repeat=0 --label-filter=feature:lws -procs=2)'
  ++ ginkgo_extra=(--repeat=0 --label-filter=feature:lws -procs=2)
  + rc=0
  + ((  had_noglob == 0  ))
  + ((  rc == 0  ))
  + /usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/hack/testing/../../bin/ginkgo run --repeat=0 --label-filter=feature:lws -procs=2 --junit-report=junit.xml --json-report=e2e.json --output-dir=/usr/local/google/home/michalwozniak/go/src/sigs.k8s.io/kueue/bin/run-test-e2e-singlecluster-1.35.0 -v ./test/e2e/singlecluster/...
  ```
  So, I think the tidy output from just "printf" is preferred. If someone is debugging a complex issue they could always use `make SHELL=/bin/bash '.SHELLFLAGS=-x -c' test-e2e`, but not strong view, it just seems overkill
