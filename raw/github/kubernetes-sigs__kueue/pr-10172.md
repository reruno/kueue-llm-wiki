# PR #10172: e2e dev mode - add an option to skip image reload #10094

**Summary**: e2e dev mode - add an option to skip image reload #10094

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10172

**Last updated**: 2026-04-21T12:34:15Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `e2e-dev-mode-add-an-option-to-skip-image-reload-#10094` → `main`
- **Created**: 2026-03-30T04:36:39Z
- **Updated**: 2026-04-21T12:34:15Z
- **Merged**: 2026-04-21T11:25:54Z
- **Closed**: 2026-04-21T11:25:54Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow), [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@mszadkow](https://github.com/mszadkow), [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y), [@reruno](https://github.com/reruno)
- **Changed files**: 2   **+84 / -27**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
/area testing

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Adds an option to skip image re-load.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10094

#### Special notes for your reviewer:
Verification:
```bash
# First run -- creates cluster, pulls images, loads everything
time E2E_MODE=dev E2E_RUN_ONLY_ENV=true \
  GINKGO_ARGS='--label-filter=feature:kuberay' \
  make kind-image-build test-e2e
# E2E_MODE=dev E2E_RUN_ONLY_ENV=true GINKGO_ARGS= make kind-image-build test-e2  172.28s user 38.88s system 66% cpu 5:19.49 total  

# Second run -- should be much faster (images cached and loaded)
time E2E_MODE=dev E2E_RUN_ONLY_ENV=true E2E_SKIP_REINSTALL=true E2E_SKIP_IMAGE_RELOAD=true \
  GINKGO_ARGS='--label-filter=feature:kuberay' \
  make test-e2e
# E2E_MODE=dev E2E_RUN_ONLY_ENV=true E2E_SKIP_REINSTALL=true GINKGO_ARGS= make   5.72s user 5.59s system 36% cpu 31.227 total
```

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-30T04:36:41Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-30T04:36:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9623efd342f6dd8ee0684b6c914df5c426869364 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e624eb95dca60008f6eb2f |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-03-30T04:43:32Z

/cc @mbobrovskyi @mimowo 
For this feature there's a need to keep track of cluster lifecycle, is there better way to do it apart from what I've come up with. WDYT?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-30T06:43:19Z

/test all

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-14T08:36:50Z

/test pull-kueue-test-e2e-multikueue-sequential-main

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-14T10:11:01Z

/retest

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-14T13:03:50Z

/assign

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:05:04Z

overall looks pretty good, one comment from my side

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-16T06:26:36Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-16T06:26:44Z

LGTM label has been added.  <details>Git tree hash: 23e60c61e7591ca147c4872625536ccf8549e498</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T13:53:56Z

LGTM label has been added.  <details>Git tree hash: 3af871253f177daa596d087a28e7eb9e32ee0f92</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T13:54:56Z

@mimowo could you please take a look?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-20T16:12:09Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10172/pull-kueue-build-image-main/2046214054078320640 

Could this be related @vladikkuzn @mbobrovskyi ? I've never seen this before

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-20T16:13:00Z

/hold
To determine if this failure is related https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10172/pull-kueue-build-image-main/2046214054078320640

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-20T16:13:01Z

@mimowo something is with `npm install -g serve`, probably some connection issue

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-20T16:13:09Z

/test pull-kueue-build-image-main

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-20T16:34:23Z

@mimowo successful after restart

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-20T16:37:46Z

/unhold

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:45:30Z

@mimowo can we merge it?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T08:53:00Z

/approve
I think, so, but I haven't had a chance to test it myself yet. Would you give it a try @mbobrovskyi to independently confirm that it works?
for manual check if this works ok:
/hold

In particular I would like to see how much time it takes for kuberay to run a single test for the same time, and then time to retry the same test.

Maybe also @vladikkuzn could provide the manual confirmation.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-21T08:53:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10172#pullrequestreview-4140606629" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287190024" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10172#" title="Author self-approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/testing/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/testing/OWNERS)~~ [mbobrovskyi,mimowo]
- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T09:20:58Z

```
Image 'registry.k8s.io/e2e-test-images/agnhost:2.52@sha256:b173c7d0ffe3d805d49f4dfe48375169b7b8d2e1feb81783efd61eb9d08042e6' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'registry.k8s.io/e2e-test-images/agnhost:2.63.0@sha256:a62f40481e05709988fe44e5035b2e0f553cd52bf0ae4e83614928255b00e8c1' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Reusing kind cluster: kind (E2E_MODE=dev)
Image 'quay.io/ibm/appwrapper:v1.2.0' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'registry.k8s.io/jobset/jobset:v0.11.1' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'kubeflow/training-operator:v1-d6eb98e' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'ghcr.io/kubeflow/trainer/trainer-controller-manager:v2.2.0' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'quay.io/kuberay/operator:v1.6.0' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'registry.k8s.io/lws/lws:v0.8.0' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'registry.k8s.io/e2e-test-images/agnhost:2.52' already loaded in cluster 'kind'; skipping (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Image 'registry.k8s.io/e2e-test-images/agnhost:2.63.0' already loaded in cluster 'kind'; skipping (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
Saving image 'us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.18.0-devel-127-gcf0dac2c7'...
Loading image 'us-central1-docker.pkg.dev/k8s-staging-images/kueue/kueue:v0.18.0-devel-127-gcf0dac2c7' to cluster 'kind' (parallel)
  Loading image to node: kind-worker2
  Loading image to node: kind-worker
us central1 docker.pkg.dev/k8s staging i        saved   
application/vnd.oci.image.manifest.v1+json sha256:19bb6e946a85526e714a1fbf52ce40d661c32a0fc3f5407707ad05a8d0a26bea
Importing       elapsed: 2.7 s  total:   0.0 B  (0.0 B/s)       
us central1 docker.pkg.dev/k8s staging i        saved   
application/vnd.oci.image.manifest.v1+json sha256:19bb6e946a85526e714a1fbf52ce40d661c32a0fc3f5407707ad05a8d0a26bea
Importing       elapsed: 2.7 s  total:   0.0 B  (0.0 B/s)       
AppWrapper already installed (v1.2.0); skipping install (E2E_MODE=dev).
JobSet already installed (v0.11.1); skipping install (E2E_MODE=dev).
Kubeflow Training Operator already installed (v1-d6eb98e); skipping install (E2E_MODE=dev).
Kubeflow Trainer already installed (v2.2.0); skipping install (E2E_MODE=dev).
LeaderWorkerSet already installed (v0.8.0); skipping install (E2E_MODE=dev).
KubeRay operator already installed (v1.6.0); skipping install (E2E_MODE=dev).
Prometheus operator already installed (v0.89.0); skipping install (E2E_MODE=dev).
Kueue controller already exists, using --force-conflicts
```

Looks like it works.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:22:34Z

/unhold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:22:47Z

/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T09:22:50Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287380544):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:24:59Z

Thank you folks, I think this is really important 👍

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:25:14Z

/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T09:25:17Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `website` in a new PR and assign it to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287397075):

>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T10:27:12Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10172/pull-kueue-test-e2e-main-1-35/2046520282943852544
flake, let me open issue

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T10:27:22Z

/test pull-kueue-test-e2e-main-1-35

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T10:28:41Z

opened: https://github.com/kubernetes-sigs/kueue/issues/10642

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T11:26:33Z

@mimowo: #10172 failed to apply on top of branch "release-0.16":
```
Applying: e2e dev mode - add an option to skip image reload #10094
Using index info to reconstruct a base tree...
M	hack/testing/e2e-common.sh
M	site/content/en/docs/contribution_guidelines/testing.md
Falling back to patching base and 3-way merge...
Auto-merging site/content/en/docs/contribution_guidelines/testing.md
Auto-merging hack/testing/e2e-common.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-common.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 e2e dev mode - add an option to skip image reload #10094

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287380544):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T11:27:10Z

@mimowo: new pull request created: #10647

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287380544):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-21T11:27:47Z

@mimowo: new pull request created: #10648

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10172#issuecomment-4287397075):

>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-15T12:04:15Z

- **hack/testing/e2e-common.sh:558** by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:04:15Z
```diff
@@ -511,13 +539,26 @@ function cluster_kind_load_image {
     local failed=0
     for i in "${!pids[@]}"; do
         if ! wait "${pids[$i]}"; then
-            echo "Failed to load image '$2' to node '${nodes[$i]}'"
+            echo "Failed to load image '${image}' to node '${nodes[$i]}'"
             failed=1
         fi
     done
     return "$failed"
 }
 
+function cluster_kind_load_image {
+    # In dev mode, skip if the image is already in the cluster
+    if [[ "${E2E_MODE}" == "dev" ]] && cluster_kind_image_exists "$1" "$2"; then
+        echo "Image '$2' already loaded in cluster '$1'; skipping (E2E_MODE=dev)"
+        return 0
+    fi
+    cluster_kind_load_image_impl "$1" "$2"
+}
+
+function cluster_kind_load_image_force {
```

  is this _force function really needed?
  if we call `cluster_kind_load_image` in line 429, wouldn't that be the same?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-15T12:21:52Z

- **hack/testing/e2e-common.sh:507** by [@reruno](https://github.com/reruno) — 2026-04-15T12:21:52Z
```diff
@@ -472,17 +488,29 @@ function kind_load {
     fi
 }
 
+# $1 cluster name
+# $2 image reference (must match how the image appears in ctr images ls -q on workers)
+function cluster_kind_image_exists {
+    local first_worker
+    first_worker=$($KIND get nodes --name "$1" | grep -v 'control-plane' | head -1)
+    [[ -n "$first_worker" ]] && \
+        docker exec "$first_worker" ctr --namespace=k8s.io images ls -q 2>/dev/null | grep -qF "$2"
+}
+
```

  maybe this would be better?  like in cluster_kind_load_image_impl
  
  ```
      local cluster="$1"
      local image="$2"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:44:09Z

- **site/content/en/docs/contribution_guidelines/testing.md:145** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:44:09Z
```diff
@@ -141,6 +141,8 @@ E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
 {{% alert title="Note" color="primary" %}}
 When reusing a kept cluster in `E2E_MODE=dev`, external operators (MPI, KubeRay, etc.) are installed only once.
 To force re-installing them on every run, set `E2E_ENFORCE_OPERATOR_UPDATE=true`.
+
+In dev mode, e2e setup skips `docker pull` for dependency images that are already in your local Docker cache, and skips loading an image into kind worker nodes when that image reference is already present in the node containerd store. That makes repeat runs much faster, especially on multi-node clusters. The Kueue controller image is always reloaded into the cluster unless `E2E_SKIP_REINSTALL=true`, because you may rebuild it with `make kind-image-build` under the same tag.
```

  nit: Please split this line to avoid horizontal scrolling.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:51:47Z

- **hack/testing/e2e-common.sh:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:51:48Z
```diff
@@ -46,6 +46,17 @@ function e2e_is_truthy {
     esac
 }
 
+# In dev mode, skip docker pull when the image already exists locally. In CI, always pull.
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && docker image inspect "$image" > /dev/null 2>&1; then
```

  I thought we only need to skip this when the E2E_SKIP_IMAGE_RELOAD variable is set.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T08:54:52Z

- **hack/testing/e2e-common.sh:53** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T08:54:51Z
```diff
@@ -46,6 +46,17 @@ function e2e_is_truthy {
     esac
 }
 
+# In dev mode, skip docker pull when the image already exists locally. In CI, always pull.
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && docker image inspect "$image" > /dev/null 2>&1; then
```

  I can add it in here, but do we need to repull if the tag has not changed locally?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T09:35:35Z

- **hack/testing/e2e-common.sh:53** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T09:35:35Z
```diff
@@ -46,6 +46,17 @@ function e2e_is_truthy {
     esac
 }
 
+# In dev mode, skip docker pull when the image already exists locally. In CI, always pull.
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && docker image inspect "$image" > /dev/null 2>&1; then
```

  Sounds reasonable. What if we add a variable to control image pulling, rather than skipping it? WDYT?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T14:40:17Z

- **hack/testing/e2e-common.sh:53** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T14:40:17Z
```diff
@@ -46,6 +46,17 @@ function e2e_is_truthy {
     esac
 }
 
+# In dev mode, skip docker pull when the image already exists locally. In CI, always pull.
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && docker image inspect "$image" > /dev/null 2>&1; then
```

  Added E2E_SKIP_IMAGE_RELOAD

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-17T11:18:10Z

- **hack/testing/e2e-common.sh:57** by [@reruno](https://github.com/reruno) — 2026-04-17T11:18:11Z
```diff
@@ -46,6 +50,16 @@ function e2e_is_truthy {
     esac
 }
 
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && docker image inspect "$image" > /dev/null 2>&1; then
+        echo "Image '$image' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD set)"
```

  maybe better log message is 
  ```
  already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD=true)
  ```

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-17T11:19:33Z

- **hack/testing/e2e-common.sh:561** by [@reruno](https://github.com/reruno) — 2026-04-17T11:19:33Z
```diff
@@ -511,13 +547,23 @@ function cluster_kind_load_image {
     local failed=0
     for i in "${!pids[@]}"; do
         if ! wait "${pids[$i]}"; then
-            echo "Failed to load image '$2' to node '${nodes[$i]}'"
+            echo "Failed to load image '${image}' to node '${nodes[$i]}'"
             failed=1
         fi
     done
     return "$failed"
 }
 
+function cluster_kind_load_image {
+    local cluster="$1"
+    local image="$2"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && cluster_kind_image_exists "$cluster" "$image"; then
+        echo "Image '$image' already loaded in cluster '$cluster'; skipping (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD set)"
```

  
  Instead of `E2E_SKIP_IMAGE_RELOAD set`, `E2E_SKIP_IMAGE_RELOAD=true`

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-17T11:34:45Z

- **hack/testing/e2e-common.sh:431** by [@reruno](https://github.com/reruno) — 2026-04-17T11:34:45Z
```diff
@@ -375,49 +389,57 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
     if [[ -n ${APPWRAPPER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(appwrapper|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${APPWRAPPER_IMAGE}"
+        e2e_docker_pull_if_needed "${APPWRAPPER_IMAGE}"
     fi
     if [[ -n ${JOBSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jobset|tas|trainjob|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${JOBSET_IMAGE}"
+        e2e_docker_pull_if_needed "${JOBSET_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jaxjob|pytorchjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBEFLOW_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_TRAINER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(tas|trainjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KF_TRAINER_IMAGE}"
+        e2e_docker_pull_if_needed "${KF_TRAINER_IMAGE}"
     fi
 
     if [[ -n ${KUBEFLOW_MPI_VERSION:-} ]]; then
-        docker pull "${KUBEFLOW_MPI_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_MPI_IMAGE}"
     fi
     if [[ -n ${KUBERAY_VERSION:-} && ("$GINKGO_ARGS" =~ feature:kuberay || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBERAY_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBERAY_IMAGE}"
         determine_kuberay_ray_image
         if [[ ${USE_RAY_FOR_TESTS:-} == "ray" ]]; then
-            docker pull "${KUBERAY_RAY_IMAGE}"
+            e2e_docker_pull_if_needed "${KUBERAY_RAY_IMAGE}"
         fi
     fi
     if [[ -n ${LEADERWORKERSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(leaderworkerset|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${LEADERWORKERSET_IMAGE}"
+        e2e_docker_pull_if_needed "${LEADERWORKERSET_IMAGE}"
     fi
     if [[ -n ${KUEUE_UPGRADE_FROM_VERSION:-} ]]; then
-        docker pull "${KUEUE_OLD_VERSION_IMAGE}"
+        e2e_docker_pull_if_needed "${KUEUE_OLD_VERSION_IMAGE}"
     fi
     if [[ -n ${SPARKOPERATOR_VERSION:-} && ("$GINKGO_ARGS" =~ feature:spark || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${SPARKOPERATOR_IMAGE}"
+        e2e_docker_pull_if_needed "${SPARKOPERATOR_IMAGE}"
     fi
 }
 
 # $1 cluster
 function cluster_kind_load {
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE}"
-    cluster_kind_load_image "$1" "$IMAGE_TAG"
+    local cluster="$1"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE}"
+
+    if [[ "${E2E_MODE}" == "dev" && "${E2E_SKIP_REINSTALL}" == "true" ]]; then
```

  Use e2e_is_truthy

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-17T11:40:36Z

- **hack/testing/e2e-common.sh:560** by [@reruno](https://github.com/reruno) — 2026-04-17T11:40:36Z
```diff
@@ -511,13 +547,23 @@ function cluster_kind_load_image {
     local failed=0
     for i in "${!pids[@]}"; do
         if ! wait "${pids[$i]}"; then
-            echo "Failed to load image '$2' to node '${nodes[$i]}'"
+            echo "Failed to load image '${image}' to node '${nodes[$i]}'"
             failed=1
         fi
     done
     return "$failed"
 }
 
+function cluster_kind_load_image {
+    local cluster="$1"
+    local image="$2"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && cluster_kind_image_exists "$cluster" "$image"; then
```

  I really wonder if we should change e2e_is_truthy behavior, because 0 means true and 1 means error/false. 
  
  from
  ```
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 1 ;;
          *) return 0 ;;
      esac
  }
  ```
  
  to
  ```
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 0 ;;
          *) return 1 ;;
      esac
  }
  ```

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T12:35:33Z

- **hack/testing/e2e-common.sh:57** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T12:35:33Z
```diff
@@ -46,6 +50,16 @@ function e2e_is_truthy {
     esac
 }
 
+# $1 image reference
+function e2e_docker_pull_if_needed {
+    local image="$1"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && docker image inspect "$image" > /dev/null 2>&1; then
+        echo "Image '$image' already cached locally; skipping pull (E2E_MODE=dev, E2E_SKIP_IMAGE_RELOAD set)"
```

  Looks better, but I would still leave the `Image '$image'`

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T14:06:36Z

- **hack/testing/e2e-common.sh:754** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T14:06:36Z
```diff
@@ -705,7 +751,7 @@ function install_kubeflow {
     local deployment_name="${KUBEFLOW_TRAINING_OPERATOR_DEPLOYMENT:-training-operator}"
     local expected_version="${KUBEFLOW_IMAGE_VERSION:-}"
 
-    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
```

  Shouldn't E2E_ENFORCE_OPERATOR_UPDATE be true to update?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-17T14:18:00Z

- **site/content/en/docs/contribution_guidelines/testing.md:141** by [@mimowo](https://github.com/mimowo) — 2026-04-17T14:18:00Z
```diff
@@ -136,11 +136,22 @@ E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 # Skip reinstallation of kueue (works only in dev mode)
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-e2e
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
+
+# Skip re-pulling dependency images and re-importing them into kind when already present (dev mode only)
+E2E_MODE=dev E2E_SKIP_IMAGE_RELOAD=true make kind-image-build test-e2e
```

  question: will this command work when creating a new cluster, so that the images are pulled and loaded if this is a new cluster?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T14:20:25Z

- **hack/testing/e2e-common.sh:754** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T14:20:25Z
```diff
@@ -705,7 +751,7 @@ function install_kubeflow {
     local deployment_name="${KUBEFLOW_TRAINING_OPERATOR_DEPLOYMENT:-training-operator}"
     local expected_version="${KUBEFLOW_IMAGE_VERSION:-}"
 
-    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
```

  Values like true / 1 / yes → return 1 → if e2e_is_truthy ... treats the condition as false (do not enter then).
  Maybe I should revert the changes to e2e_is_truthy, because it's confusing during usage

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T14:22:23Z

- **hack/testing/e2e-common.sh:754** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T14:22:23Z
```diff
@@ -705,7 +751,7 @@ function install_kubeflow {
     local deployment_name="${KUBEFLOW_TRAINING_OPERATOR_DEPLOYMENT:-training-operator}"
     local expected_version="${KUBEFLOW_IMAGE_VERSION:-}"
 
-    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
```

  Can we move this changes to separate PR?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T14:36:46Z

- **hack/testing/e2e-common.sh:560** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T14:36:46Z
```diff
@@ -511,13 +547,23 @@ function cluster_kind_load_image {
     local failed=0
     for i in "${!pids[@]}"; do
         if ! wait "${pids[$i]}"; then
-            echo "Failed to load image '$2' to node '${nodes[$i]}'"
+            echo "Failed to load image '${image}' to node '${nodes[$i]}'"
             failed=1
         fi
     done
     return "$failed"
 }
 
+function cluster_kind_load_image {
+    local cluster="$1"
+    local image="$2"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && cluster_kind_image_exists "$cluster" "$image"; then
```

  1 is true, and 0 is false in the context of a function
  in the context of exit code, it's vice-versa; 0 - success, !0 - failure
  that's why this is the implementation is correct and no changes needed

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T14:54:50Z

- **site/content/en/docs/contribution_guidelines/testing.md:141** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T14:54:50Z
```diff
@@ -136,11 +136,22 @@ E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 # Skip reinstallation of kueue (works only in dev mode)
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-e2e
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
+
+# Skip re-pulling dependency images and re-importing them into kind when already present (dev mode only)
+E2E_MODE=dev E2E_SKIP_IMAGE_RELOAD=true make kind-image-build test-e2e
```

  E2E_SKIP_IMAGE_RELOAD=true does not skip loading into kind when the cluster is new (or recreated after kind delete); it only skips pull when Docker already has the image, and skips kind import only when cluster_kind_image_exists is true for that cluster.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-17T15:07:09Z

- **site/content/en/docs/contribution_guidelines/testing.md:141** by [@mimowo](https://github.com/mimowo) — 2026-04-17T15:07:09Z
```diff
@@ -136,11 +136,22 @@ E2E_MODE=dev GINKGO_ARGS="--until-it-fails" make kind-image-build  test-e2e
 # Skip reinstallation of kueue (works only in dev mode)
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-e2e
 E2E_MODE=dev E2E_SKIP_REINSTALL=true make kind-image-build test-multikueue-e2e
+
+# Skip re-pulling dependency images and re-importing them into kind when already present (dev mode only)
+E2E_MODE=dev E2E_SKIP_IMAGE_RELOAD=true make kind-image-build test-e2e
```

  Awesome, thanks

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T15:22:40Z

- **hack/testing/e2e-common.sh:609** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T15:22:40Z
```diff
@@ -560,7 +606,7 @@ function cluster_kueue_deploy {
         return
     fi
 
-    if [[ "${E2E_MODE}" == "dev" && "${E2E_SKIP_REINSTALL}" == "true" ]]; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  ```suggestion
      if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
  ```
  ?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-18T21:59:59Z

- **hack/testing/e2e-common.sh:609** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-18T21:59:59Z
```diff
@@ -560,7 +606,7 @@ function cluster_kueue_deploy {
         return
     fi
 
-    if [[ "${E2E_MODE}" == "dev" && "${E2E_SKIP_REINSTALL}" == "true" ]]; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  e2e_is_truthy uses inverted exit codes so ! means “is truthy”

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T03:01:03Z

- **hack/testing/e2e-common.sh:609** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T03:01:03Z
```diff
@@ -560,7 +606,7 @@ function cluster_kueue_deploy {
         return
     fi
 
-    if [[ "${E2E_MODE}" == "dev" && "${E2E_SKIP_REINSTALL}" == "true" ]]; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  But you reverted this changes.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T03:08:36Z

- **hack/testing/e2e-common.sh:609** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T03:08:36Z
```diff
@@ -560,7 +606,7 @@ function cluster_kueue_deploy {
         return
     fi
 
-    if [[ "${E2E_MODE}" == "dev" && "${E2E_SKIP_REINSTALL}" == "true" ]]; then
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  Ah, I think I see it – there’s an error in the original code. It should check for false.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T03:12:21Z

- **hack/testing/e2e-common.sh:431** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T03:12:21Z
```diff
@@ -375,49 +389,57 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
     if [[ -n ${APPWRAPPER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(appwrapper|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${APPWRAPPER_IMAGE}"
+        e2e_docker_pull_if_needed "${APPWRAPPER_IMAGE}"
     fi
     if [[ -n ${JOBSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jobset|tas|trainjob|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${JOBSET_IMAGE}"
+        e2e_docker_pull_if_needed "${JOBSET_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jaxjob|pytorchjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBEFLOW_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_TRAINER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(tas|trainjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KF_TRAINER_IMAGE}"
+        e2e_docker_pull_if_needed "${KF_TRAINER_IMAGE}"
     fi
 
     if [[ -n ${KUBEFLOW_MPI_VERSION:-} ]]; then
-        docker pull "${KUBEFLOW_MPI_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_MPI_IMAGE}"
     fi
     if [[ -n ${KUBERAY_VERSION:-} && ("$GINKGO_ARGS" =~ feature:kuberay || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBERAY_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBERAY_IMAGE}"
         determine_kuberay_ray_image
         if [[ ${USE_RAY_FOR_TESTS:-} == "ray" ]]; then
-            docker pull "${KUBERAY_RAY_IMAGE}"
+            e2e_docker_pull_if_needed "${KUBERAY_RAY_IMAGE}"
         fi
     fi
     if [[ -n ${LEADERWORKERSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(leaderworkerset|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${LEADERWORKERSET_IMAGE}"
+        e2e_docker_pull_if_needed "${LEADERWORKERSET_IMAGE}"
     fi
     if [[ -n ${KUEUE_UPGRADE_FROM_VERSION:-} ]]; then
-        docker pull "${KUEUE_OLD_VERSION_IMAGE}"
+        e2e_docker_pull_if_needed "${KUEUE_OLD_VERSION_IMAGE}"
     fi
     if [[ -n ${SPARKOPERATOR_VERSION:-} && ("$GINKGO_ARGS" =~ feature:spark || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${SPARKOPERATOR_IMAGE}"
+        e2e_docker_pull_if_needed "${SPARKOPERATOR_IMAGE}"
     fi
 }
 
 # $1 cluster
 function cluster_kind_load {
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE}"
-    cluster_kind_load_image "$1" "$IMAGE_TAG"
+    local cluster="$1"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE}"
+
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  ```suggestion
      if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
  ```
  But here I think it should be true.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-20T11:09:36Z

- **hack/testing/e2e-common.sh:974** by [@reruno](https://github.com/reruno) — 2026-04-20T11:09:36Z
```diff
@@ -925,7 +978,7 @@ function install_sparkoperator {
 
     ${HELM} repo add --force-update spark-operator https://kubeflow.github.io/spark-operator
 
-    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}" ; then
+    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
```

  https://github.com/kubernetes-sigs/kueue/pull/10172/changes#diff-d0e0610cb3a2f8dfce32b5f707f250f1d4a55c83b91ce5d77340ebf0b7bfef54R616
  
  Shouldn't `e2e_is_truthy` be replaced with negated `e2e_env_truthy` here?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-20T11:35:47Z

- **hack/testing/e2e-common.sh:560** by [@reruno](https://github.com/reruno) — 2026-04-20T11:35:47Z
```diff
@@ -511,13 +547,23 @@ function cluster_kind_load_image {
     local failed=0
     for i in "${!pids[@]}"; do
         if ! wait "${pids[$i]}"; then
-            echo "Failed to load image '$2' to node '${nodes[$i]}'"
+            echo "Failed to load image '${image}' to node '${nodes[$i]}'"
             failed=1
         fi
     done
     return "$failed"
 }
 
+function cluster_kind_load_image {
+    local cluster="$1"
+    local image="$2"
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_IMAGE_RELOAD:-}" && cluster_kind_image_exists "$cluster" "$image"; then
```

  > 1 is true, and 0 is false in the context of a function
  
  Could you please show where we use e2e_is_truthy in the context of a function, I was looking, but I couldn't find a good example.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:37:44Z

- **hack/testing/e2e-common.sh:974** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T12:37:44Z
```diff
@@ -925,7 +978,7 @@ function install_sparkoperator {
 
     ${HELM} repo add --force-update spark-operator https://kubeflow.github.io/spark-operator
 
-    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}" ; then
+    if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
```

  As far as I can see, the original value is true, so it should evaluate as truthy.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:47:25Z

- **hack/testing/e2e-common.sh:50** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T12:47:25Z
```diff
@@ -37,15 +37,30 @@ export E2E_ENFORCE_OPERATOR_UPDATE="${E2E_ENFORCE_OPERATOR_UPDATE:-false}"
 # When set to value "true", skip Kueue re-install in E2E_MODE=dev if the controller deployment already exists.
 export E2E_SKIP_REINSTALL="${E2E_SKIP_REINSTALL:-false}"
 
+# When set to a truthy value with E2E_MODE=dev, skip docker pull and kind image import for dependency images
+# when they already exist locally / on kind worker nodes. CI mode always pulls and loads.
+export E2E_SKIP_IMAGE_RELOAD="${E2E_SKIP_IMAGE_RELOAD:-false}"
+
 export KIND_VERSION="${E2E_KIND_VERSION/"kindest/node:v"/}"
 
+# Returns 0 (true) if the input value is a common representation of "true", otherwise returns 1 (false).
 function e2e_is_truthy {
     case "${1:-}" in
-        1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 1 ;;
-        *) return 0 ;;
+        1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 0 ;;
+        *) return 1 ;;
```

  Why this changes?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:48:05Z

- **hack/testing/e2e-common.sh:50** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T12:48:04Z
```diff
@@ -37,15 +37,30 @@ export E2E_ENFORCE_OPERATOR_UPDATE="${E2E_ENFORCE_OPERATOR_UPDATE:-false}"
 # When set to value "true", skip Kueue re-install in E2E_MODE=dev if the controller deployment already exists.
 export E2E_SKIP_REINSTALL="${E2E_SKIP_REINSTALL:-false}"
 
+# When set to a truthy value with E2E_MODE=dev, skip docker pull and kind image import for dependency images
+# when they already exist locally / on kind worker nodes. CI mode always pulls and loads.
+export E2E_SKIP_IMAGE_RELOAD="${E2E_SKIP_IMAGE_RELOAD:-false}"
+
 export KIND_VERSION="${E2E_KIND_VERSION/"kindest/node:v"/}"
 
+# Returns 0 (true) if the input value is a common representation of "true", otherwise returns 1 (false).
 function e2e_is_truthy {
     case "${1:-}" in
-        1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 1 ;;
-        *) return 0 ;;
+        1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 0 ;;
+        *) return 1 ;;
```

  If it's really need could you please prepare separate PR for this changes?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-20T13:09:30Z

- **hack/testing/e2e-common.sh:431** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-20T13:09:30Z
```diff
@@ -375,49 +389,57 @@ function prepare_docker_images {
     docker tag "$E2E_TEST_AGNHOST_IMAGE_WITH_SHA" "$E2E_TEST_AGNHOST_IMAGE"
 
     if [[ -n ${APPWRAPPER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(appwrapper|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${APPWRAPPER_IMAGE}"
+        e2e_docker_pull_if_needed "${APPWRAPPER_IMAGE}"
     fi
     if [[ -n ${JOBSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jobset|tas|trainjob|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${JOBSET_IMAGE}"
+        e2e_docker_pull_if_needed "${JOBSET_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(jaxjob|pytorchjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBEFLOW_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_IMAGE}"
     fi
     if [[ -n ${KUBEFLOW_TRAINER_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(tas|trainjob) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KF_TRAINER_IMAGE}"
+        e2e_docker_pull_if_needed "${KF_TRAINER_IMAGE}"
     fi
 
     if [[ -n ${KUBEFLOW_MPI_VERSION:-} ]]; then
-        docker pull "${KUBEFLOW_MPI_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBEFLOW_MPI_IMAGE}"
     fi
     if [[ -n ${KUBERAY_VERSION:-} && ("$GINKGO_ARGS" =~ feature:kuberay || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${KUBERAY_IMAGE}"
+        e2e_docker_pull_if_needed "${KUBERAY_IMAGE}"
         determine_kuberay_ray_image
         if [[ ${USE_RAY_FOR_TESTS:-} == "ray" ]]; then
-            docker pull "${KUBERAY_RAY_IMAGE}"
+            e2e_docker_pull_if_needed "${KUBERAY_RAY_IMAGE}"
         fi
     fi
     if [[ -n ${LEADERWORKERSET_VERSION:-} && ("$GINKGO_ARGS" =~ feature:(leaderworkerset|managejobswithoutqueuename) || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${LEADERWORKERSET_IMAGE}"
+        e2e_docker_pull_if_needed "${LEADERWORKERSET_IMAGE}"
     fi
     if [[ -n ${KUEUE_UPGRADE_FROM_VERSION:-} ]]; then
-        docker pull "${KUEUE_OLD_VERSION_IMAGE}"
+        e2e_docker_pull_if_needed "${KUEUE_OLD_VERSION_IMAGE}"
     fi
     if [[ -n ${SPARKOPERATOR_VERSION:-} && ("$GINKGO_ARGS" =~ feature:spark || ! "$GINKGO_ARGS" =~ "--label-filter") ]]; then
-        docker pull "${SPARKOPERATOR_IMAGE}"
+        e2e_docker_pull_if_needed "${SPARKOPERATOR_IMAGE}"
     fi
 }
 
 # $1 cluster
 function cluster_kind_load {
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
-    cluster_kind_load_image "$1" "${E2E_TEST_AGNHOST_IMAGE}"
-    cluster_kind_load_image "$1" "$IMAGE_TAG"
+    local cluster="$1"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE_OLD}"
+    cluster_kind_load_image "$cluster" "${E2E_TEST_AGNHOST_IMAGE}"
+
+    if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_SKIP_REINSTALL:-}"; then
```

  I think we should do a check in cluster_kind_load_image if E2E_SKIP_REINSTALL==true, this means E2E_SKIP_REINSTALL==true -> e2e_is_truthy "${E2E_SKIP_REINSTALL:-}" == false

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T13:53:48Z

Thank you!

/lgtm
/approve
