# PR #10199: Fix adding a trailing space to `GINKGO_ARGS`

**Summary**: Fix adding a trailing space to `GINKGO_ARGS`

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10199

**Last updated**: 2026-04-01T09:23:09Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `fix/passing-ginkgo-args-in-makefile` → `main`
- **Created**: 2026-03-30T16:11:12Z
- **Updated**: 2026-04-01T09:23:09Z
- **Merged**: 2026-03-31T10:52:13Z
- **Closed**: 2026-03-31T10:52:13Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/XS`, `cncf-cla: yes`, `area/testing`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Changed files**: 1   **+6 / -1**

## Description

#### What type of PR is this?
/kind bug
/area testing

#### What this PR does / why we need it:
`+=` adds an implicit space in Makefile. So invocations like:
```
GINKGO_ARGS="--repeat=10" make test-multikueue-e2e
```
fail with:
```
invalid value "10 " for flag -repeat: parse error
```

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:
Found by @Singularity23x0, but I'm also checking out a flake and I saw the issue so I'm posting a fix.

We can also consider using `strip`.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-03-30T16:11:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8320af016f216c0674310e6bc2f0860dbaeb3d1b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69cb8b164778c2000825dd09 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-31T07:16:31Z

> We can also consider using strip.

I think we can simplify this to a single line using strip, right?

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-31T07:48:10Z

> > We can also consider using strip.
> 
> I think we can simplify this to a single line using strip, right?

I think it would be two lines, sth like:
```Makefile
GINKGO_ARGS += $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
GINKGO_ARGS := $(strip ${GINKGO_ARGS})
```

Or we could maybe do sth like:
```Makefile
GINKGO_ARGS := "${GINKGO_ARGS}$(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))"
```
But this one is very dense.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-31T07:51:04Z

Can we do like this:

```
GINKGO_ARGS += $(strip $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS)))
```

?

### Comment by [@kshalot](https://github.com/kshalot) — 2026-03-31T08:13:32Z

> Can we do like this:
> 
> ```
> GINKGO_ARGS += $(strip $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS)))
> ```
> 
> ?

IIUC no, because the problem is doing `+=` with an empty string, not that the right hand side is a space. The problem is we are doing this, which also adds a space:
```Makefile
GINKGO_ARGS += ""
```

### Comment by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-31T10:07:03Z

> > > We can also consider using strip.
> > 
> > 
> > I think we can simplify this to a single line using strip, right?
> 
> I think it would be two lines, sth like:
> 
> ```makefile
> GINKGO_ARGS += $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
> GINKGO_ARGS := $(strip ${GINKGO_ARGS})
> ```
> 
> Or we could maybe do sth like:
> 
> ```makefile
> GINKGO_ARGS := "${GINKGO_ARGS}$(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))"
> ```
> 
> But this one is very dense.

I think the way @kshalot implemented it currently is more explicitly readable, even though it uses more lines than the strip variant. I'm in favor of leaving it as proposed.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-31T10:09:23Z

> I think the way @kshalot implemented it currently is more explicitly readable, even though it uses more lines than the strip variant. I'm in favor of leaving it as proposed.

Agree. Thank you guys!

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-31T10:09:32Z

LGTM label has been added.  <details>Git tree hash: 16267706e9232a46dd0f8961288aa16d73fba1cc</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-03-31T10:09:34Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10199#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10199#issuecomment-4161471160" title="Approved">mbobrovskyi</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-03-31T11:49:00Z

I manually exclude this change in the current on-going release process (v0.17.0).

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-01T09:23:02Z

/milestone v0.18

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-03-31T02:12:43Z

- **Makefile-test.mk:66** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-03-31T02:12:43Z
```diff
@@ -60,7 +60,12 @@ KIND_CLUSTER_NAME ?= kind
 # Number of processes to use during e2e tests.
 E2E_NPROCS ?= 1
 
-GINKGO_ARGS += $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Inject the number of procs into GINKGO_ARGS
+GINKGO_PROC_ARG := $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Check empty string to avoid adding a trailing space
+ifneq ($(EXTRA_PROCS),)
```

  What does it mean EXTRA_PROCS?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-03-31T07:11:48Z

- **Makefile-test.mk:66** by [@kshalot](https://github.com/kshalot) — 2026-03-31T07:11:48Z
```diff
@@ -60,7 +60,12 @@ KIND_CLUSTER_NAME ?= kind
 # Number of processes to use during e2e tests.
 E2E_NPROCS ?= 1
 
-GINKGO_ARGS += $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Inject the number of procs into GINKGO_ARGS
+GINKGO_PROC_ARG := $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Check empty string to avoid adding a trailing space
+ifneq ($(EXTRA_PROCS),)
```

  Oh no, I was changing the name of the arg and I guess it did not auto update here or I just missed this. Thanks

### Review by [@Singularity23x0](https://github.com/Singularity23x0) (commented) — 2026-03-31T10:10:22Z

- **Makefile-test.mk:68** by [@Singularity23x0](https://github.com/Singularity23x0) — 2026-03-31T10:10:22Z
```diff
@@ -60,7 +60,12 @@ KIND_CLUSTER_NAME ?= kind
 # Number of processes to use during e2e tests.
 E2E_NPROCS ?= 1
 
-GINKGO_ARGS += $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Inject the number of procs into GINKGO_ARGS
+GINKGO_PROC_ARG := $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
+# Check empty string to avoid adding a trailing space
+ifneq ($(GINKGO_PROC_ARG),)
+    GINKGO_ARGS += $(GINKGO_PROC_ARG)
+endif
```

  Nit: code clarity; non mandatory suggestion
  ```suggestion
  # Read the declared number of procs
  GINKGO_PROC_ARG := $(if $(filter-out 1,$(E2E_NPROCS)),-procs=$(E2E_NPROCS))
  # Inject the number of procs into GINKGO_ARGS if declared
  ifneq ($(GINKGO_PROC_ARG),)
      GINKGO_ARGS += $(GINKGO_PROC_ARG)
  endif
  ```
