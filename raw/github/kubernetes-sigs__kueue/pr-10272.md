# PR #10272: Refactor elastic job pod ungating to dedicated controller

**Summary**: Refactor elastic job pod ungating to dedicated controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10272

**Last updated**: 2026-04-16T12:01:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `elastic-job-ungater` → `main`
- **Created**: 2026-04-01T23:29:46Z
- **Updated**: 2026-04-16T12:01:10Z
- **Merged**: 2026-04-10T06:32:20Z
- **Closed**: 2026-04-10T06:32:20Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 7   **+747 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/10258

#### Special notes for your reviewer:
- I dropped the `OwnerReference` fallback from the pod listing. The old `StartWorkloadSlicePods` had this for pods created before the `WorkloadSliceNameAnnotation` was introduced. The new controller doesn't need it as any pods that pre-date the annotation were already ungated by the old code path before this refactor takes effect
- This PR addresses all the motivations from [#10258](https://github.com/kubernetes-sigs/kueue/issues/10258) except 3, which is partially addressed. The ungating path is decoupled from jobframework, but jobframework still depends on workloadslicing for slice lifecycle management. I can handle a full package decoupling as a separate refactor. The existing `StartWorkloadSlicePods` helper is kept for safety and should be deprecated and will be removed in a follow-up once the controller path is validated in CI.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
ElasticJobs: Fix a bug where pods stay gated after scale-up by allowing finished workloads to ungate their own pods.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-01T23:29:53Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d55c321f495d0a0461e1cdcd012501d0307c7dd3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69d7d9f69238850008c4cce0 |

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-02T04:42:59Z

Failure is related to https://github.com/kubernetes-sigs/kueue/pull/10273

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-02T07:01:56Z

To consider: extend the PR with the e2e test from @hiboyang for the RayJob replacement of deleted Pods and call it a bugfix. Or at least to verify the hypothesis that beyond the refactoring it is actually fixing the pod replacement issue.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-02T07:04:13Z

> This PR addresses all the motivations from #10258 except 3, which is partially addressed. The ungating path is decoupled from jobframework, but jobframework still depends on workloadslicing for slice lifecycle management. I can handle a full package decoupling as a separate refactor. 

Thanks for the summary. Yes this is a follow up refactor, lets keep this PR minimal, as Im leaning to backport as a bugfix (comment above)

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-02T12:28:13Z

> To consider: extend the PR with the e2e test from @hiboyang for the RayJob replacement of deleted Pods and call it a bugfix. Or at least to verify the hypothesis that beyond the refactoring it is actually fixing the pod replacement issue.

Yes, I did one round of testing with that scenario locally and it works fine. Do you want me to fold that test commit from @hiboyang in this PR?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-02T13:12:39Z

I think so, the test fragment from https://github.com/kubernetes-sigs/kueue/pull/10035, and ai would like to label the PR as bugfix with a release note.

### Comment by [@hiboyang](https://github.com/hiboyang) — 2026-04-06T23:56:58Z

Can we merge this PR first? then working on follow-up tests if there are any @sohankunkerkar

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-10T06:30:47Z

LGTM label has been added.  <details>Git tree hash: f1f3fe9ac36021d3e2639437a584408d73b42bee</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-10T06:30:50Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10272#pullrequestreview-4087783324" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10272#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T06:33:22Z

/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-10T06:34:00Z

@mimowo: #10272 failed to apply on top of branch "release-0.16":
```
Applying: Refactor elastic job pod ungating to dedicated controller
Using index info to reconstruct a base tree...
M	cmd/kueue/main.go
M	pkg/controller/jobframework/reconciler.go
M	test/integration/singlecluster/controller/jobs/job/suite_test.go
M	test/integration/singlecluster/controller/jobs/raycluster/suite_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/integration/singlecluster/controller/jobs/raycluster/suite_test.go
Auto-merging test/integration/singlecluster/controller/jobs/job/suite_test.go
CONFLICT (content): Merge conflict in test/integration/singlecluster/controller/jobs/job/suite_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
Auto-merging cmd/kueue/main.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Refactor elastic job pod ungating to dedicated controller

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10272#issuecomment-4221614312):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T06:34:32Z

/cherrypick release-0.17
@hiboyang I merged the PR as this LGTM, but an extra round of manual testing before the release would be great

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-10T06:34:37Z

@mimowo: new pull request created: #10364

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10272#issuecomment-4221614312):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-10T06:35:12Z

@mimowo: new pull request could not be created: failed to create pull request against kubernetes-sigs/kueue#release-0.17 from head k8s-infra-cherrypick-robot:cherry-pick-10272-to-release-0.17: status code 422 not one of [201], body: {"message":"Validation Failed","errors":[{"resource":"PullRequest","code":"custom","message":"A pull request already exists for k8s-infra-cherrypick-robot:cherry-pick-10272-to-release-0.17."}],"documentation_url":"https://docs.github.com/rest/pulls/pulls#create-a-pull-request","status":"422"}

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10272#issuecomment-4221626383):

>/cherrypick release-0.17
>@hiboyang I merged the PR as this LGTM, but an extra round of manual testing before the release would be great


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T14:33:44Z

/remove-kind cleanup
/kind bug

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T12:01:07Z

/release-note-edit
```release-note
ElasticJobs: Fix a bug where pods stay gated after scale-up by allowing finished workloads to ungate their own pods.                                                                                                                                                                                      
```

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-04-01T23:35:39Z

## Pull request overview

This PR refactors elastic job pod ungating out of the jobframework reconciler into a dedicated, job-agnostic controller (`ElasticJobUngater`), aligning the design with the existing TAS topology ungater pattern.

**Changes:**
- Add a new `ElasticJobUngater` controller that watches admitted elastic workloads and ungates schedule-gated Pods via workload-slice indexing.
- Remove the workload-slice pod ungating trigger from `JobReconciler.ReconcileGenericJob`.
- Wire the new controller into the main binary and relevant integration test manager setups (behind `ElasticJobsViaWorkloadSlices`).

### Reviewed changes

Copilot reviewed 6 out of 6 changed files in this pull request and generated 1 comment.

<details>
<summary>Show a summary per file</summary>

| File | Description |
| ---- | ----------- |
| test/integration/singlecluster/controller/jobs/raycluster/suite_test.go | Registers the new controller in integration manager setup when the feature gate is enabled. |
| test/integration/singlecluster/controller/jobs/job/suite_test.go | Registers the new controller in integration manager setup when the feature gate is enabled. |
| pkg/controller/jobframework/reconciler.go | Stops jobframework from directly ungating elastic workload-slice Pods. |
| pkg/controller/elasticjobs/elastic_job_ungater.go | Introduces the new controller + pod event handler for ungating elastic scheduling gates. |
| pkg/controller/elasticjobs/elastic_job_ungater_test.go | Adds unit tests for the controller reconcile behavior. |
| cmd/kueue/main.go | Wires the new controller into the manager when the feature gate is enabled. |
</details>






---

💡 <a href="/kubernetes-sigs/kueue/new/main?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **pkg/controller/elasticjobs/elastic_job_ungater.go:217** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-04-01T23:35:38Z
```diff
@@ -0,0 +1,231 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var podList corev1.PodList
+	if err := r.client.List(ctx, &podList,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	var gated []*corev1.Pod
+	for i := range podList.Items {
+		if utilpod.HasGate(&podList.Items[i], kueue.ElasticJobSchedulingGate) {
+			gated = append(gated, &podList.Items[i])
+		}
+	}
+	return gated, nil
+}
+
+// Workload predicates
+
+func (r *elasticJobUngater) Create(e event.TypedCreateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.Object) && workloadslicing.IsElasticWorkload(e.Object)
+}
+
+func (r *elasticJobUngater) Update(e event.TypedUpdateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.ObjectNew) && workloadslicing.IsElasticWorkload(e.ObjectNew)
+}
+
+func (r *elasticJobUngater) Delete(event.TypedDeleteEvent[*kueue.Workload]) bool {
+	return false
+}
+
+func (r *elasticJobUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool {
+	return false
+}
+
+// Pod event handler
+
+var _ handler.EventHandler = (*elasticPodHandler)(nil)
+
+type elasticPodHandler struct {
+	expectationsStore *expectations.Store
+}
+
+func (h *elasticPodHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, false, q)
+}
+
+func (h *elasticPodHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.ObjectNew, false, q)
+}
+
+func (h *elasticPodHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, true, q)
+}
+
+func (h *elasticPodHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *elasticPodHandler) queueReconcileForPod(ctx context.Context, object client.Object, deleted bool, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	if !utilpod.HasGate(pod, kueue.ElasticJobSchedulingGate) && !deleted {
+		return
+	}
```

  `queueReconcileForPod` returns early when the Pod no longer has `ElasticJobSchedulingGate` (and the event isn't a delete). This prevents calling `expectationsStore.ObservedUID` on the update event generated by a successful ungate patch, so expectations can remain permanently unsatisfied and subsequent reconciles will keep returning `errPendingUngateOps`. Consider removing this early-return (matching the TAS ungater pattern), or at least ensuring the ungate/removal update path still calls `ObservedUID` (and enqueues the workload) when the gate was removed.
  ```suggestion
  
  ```

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-04-02T16:58:39Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:119** by [@hiboyang](https://github.com/hiboyang) — 2026-04-02T16:58:15Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
```

  Curious about performance and memory impact here if calling `listGatedPods(ctx, wl)` frequently. If there is a large number of pods for many workloads, the processing here may be delayed, then pending reconciliation events can be accumulated and cause pressure to internal event queue?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-02T17:12:50Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:119** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-02T17:12:49Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
```

  The `listGatedPods` call uses the `WorkloadSliceNameKey` field index, so it's an indexed lookup scoped to a single workload slice, not a full pod list. The workload predicate also filters to only admitted elastic workloads, and the expectations pattern prevents re-processing once pods are ungated. This is the same pattern TopologyUngater uses for TAS. (no additional performance overhead compared to what's already in production)

### Review by [@hiboyang](https://github.com/hiboyang) (commented) — 2026-04-02T17:19:06Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:119** by [@hiboyang](https://github.com/hiboyang) — 2026-04-02T17:19:05Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
```

  Cool, thanks for the clarification!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T09:44:05Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:224** by [@mimowo](https://github.com/mimowo) — 2026-04-07T09:44:05Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var podList corev1.PodList
+	if err := r.client.List(ctx, &podList,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	var gated []*corev1.Pod
+	for i := range podList.Items {
+		if utilpod.HasGate(&podList.Items[i], kueue.ElasticJobSchedulingGate) {
+			gated = append(gated, &podList.Items[i])
+		}
+	}
+	return gated, nil
+}
+
+// Workload predicates
+
+func (r *elasticJobUngater) Create(e event.TypedCreateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.Object) && workloadslicing.IsElasticWorkload(e.Object)
+}
+
+func (r *elasticJobUngater) Update(e event.TypedUpdateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.ObjectNew) && workloadslicing.IsElasticWorkload(e.ObjectNew)
+}
+
+func (r *elasticJobUngater) Delete(event.TypedDeleteEvent[*kueue.Workload]) bool {
+	return false
+}
+
+func (r *elasticJobUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool {
+	return false
+}
+
+// Pod event handler
+
+var _ handler.EventHandler = (*elasticPodHandler)(nil)
+
+type elasticPodHandler struct {
+	expectationsStore *expectations.Store
+}
+
+func (h *elasticPodHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, false, q)
+}
+
+func (h *elasticPodHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.ObjectNew, false, q)
+}
+
+func (h *elasticPodHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, true, q)
+}
+
+func (h *elasticPodHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *elasticPodHandler) queueReconcileForPod(ctx context.Context, object client.Object, deleted bool, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	wlName, found := pod.Annotations[kueue.WorkloadAnnotation]
```

  I think this is not fully correct, because this will not catch the case when a Pod is re-created for a Workload which no longer exists. So, I'm thinking the unit of reconciliation should rather be the "WorkloadSlice" rather than a specific workload. Once we get the slice then we can check the status of the "last not finished workload in the slice". 
  
  I will add a separate comment to cover this in elastic_job_ungater_test.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T09:47:05Z

- **pkg/controller/elasticjobs/elastic_job_ungater_test.go:60** by [@mimowo](https://github.com/mimowo) — 2026-04-07T09:47:05Z
```diff
@@ -0,0 +1,361 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"testing"
+	"time"
+
+	gocmp "github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/interceptor"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	coreindexer "sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	testingpod "sigs.k8s.io/kueue/pkg/util/testingjobs/pod"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+var podCmpOpts = []gocmp.Option{
+	cmpopts.EquateEmpty(),
+	cmpopts.IgnoreFields(corev1.Pod{}, "TypeMeta", "ObjectMeta.ResourceVersion",
+		"ObjectMeta.DeletionTimestamp"),
+}
+
+func TestReconcile(t *testing.T) {
+	features.SetFeatureGateDuringTest(t, features.ElasticJobsViaWorkloadSlices, true)
+	now := time.Now().Truncate(time.Second)
+
+	testCases := map[string]struct {
+		expectUIDs []types.UID
+		workloads  []kueue.Workload
+		pods       []corev1.Pod
+		wantPods   []corev1.Pod
+		wantErr    error
+	}{
+		"ungate single pod": {
```

  Please add a test case for scenario when WorkloadSliceNameAnnotation!= WorkloadAnnotation, because the workload was already scaled up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T09:52:20Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:224** by [@mimowo](https://github.com/mimowo) — 2026-04-07T09:52:20Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var podList corev1.PodList
+	if err := r.client.List(ctx, &podList,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	var gated []*corev1.Pod
+	for i := range podList.Items {
+		if utilpod.HasGate(&podList.Items[i], kueue.ElasticJobSchedulingGate) {
+			gated = append(gated, &podList.Items[i])
+		}
+	}
+	return gated, nil
+}
+
+// Workload predicates
+
+func (r *elasticJobUngater) Create(e event.TypedCreateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.Object) && workloadslicing.IsElasticWorkload(e.Object)
+}
+
+func (r *elasticJobUngater) Update(e event.TypedUpdateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.ObjectNew) && workloadslicing.IsElasticWorkload(e.ObjectNew)
+}
+
+func (r *elasticJobUngater) Delete(event.TypedDeleteEvent[*kueue.Workload]) bool {
+	return false
+}
+
+func (r *elasticJobUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool {
+	return false
+}
+
+// Pod event handler
+
+var _ handler.EventHandler = (*elasticPodHandler)(nil)
+
+type elasticPodHandler struct {
+	expectationsStore *expectations.Store
+}
+
+func (h *elasticPodHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, false, q)
+}
+
+func (h *elasticPodHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.ObjectNew, false, q)
+}
+
+func (h *elasticPodHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, true, q)
+}
+
+func (h *elasticPodHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *elasticPodHandler) queueReconcileForPod(ctx context.Context, object client.Object, deleted bool, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	wlName, found := pod.Annotations[kueue.WorkloadAnnotation]
```

  Ahhh, ok this is less of an issue that I initially thought, because the replacement Pods will already have the Workload annotation pointing to the last admitted workload (still worth a unit test).
  
  So the only small gap we could close by using the sliceName annotation here is when there is a Pod created, the Workload is scaled up before ungating, and the old Workload Slice is quickly deleted. This is pretty unlikely, but might be good to cover nonetheless. Let me know what you think.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-07T12:41:13Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:224** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-07T12:41:13Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var podList corev1.PodList
+	if err := r.client.List(ctx, &podList,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	var gated []*corev1.Pod
+	for i := range podList.Items {
+		if utilpod.HasGate(&podList.Items[i], kueue.ElasticJobSchedulingGate) {
+			gated = append(gated, &podList.Items[i])
+		}
+	}
+	return gated, nil
+}
+
+// Workload predicates
+
+func (r *elasticJobUngater) Create(e event.TypedCreateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.Object) && workloadslicing.IsElasticWorkload(e.Object)
+}
+
+func (r *elasticJobUngater) Update(e event.TypedUpdateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.ObjectNew) && workloadslicing.IsElasticWorkload(e.ObjectNew)
+}
+
+func (r *elasticJobUngater) Delete(event.TypedDeleteEvent[*kueue.Workload]) bool {
+	return false
+}
+
+func (r *elasticJobUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool {
+	return false
+}
+
+// Pod event handler
+
+var _ handler.EventHandler = (*elasticPodHandler)(nil)
+
+type elasticPodHandler struct {
+	expectationsStore *expectations.Store
+}
+
+func (h *elasticPodHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, false, q)
+}
+
+func (h *elasticPodHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.ObjectNew, false, q)
+}
+
+func (h *elasticPodHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, true, q)
+}
+
+func (h *elasticPodHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *elasticPodHandler) queueReconcileForPod(ctx context.Context, object client.Object, deleted bool, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	wlName, found := pod.Annotations[kueue.WorkloadAnnotation]
```

  Thanks, I agree the original concern is smaller than it first looked, since replacement Pods already point via `WorkloadAnnotation` to the last admitted Workload. The remaining gap is the narrow race you described: Pod created, Workload scales up before ungating, old WorkloadSliceNameAnnotation doesn't match the new slice name. I'll add the unit test for `WorkloadSliceNameAnnotation != WorkloadAnnotation`. If the test shows the current code misses those pods, I'll fix it in the smallest way possible here. Otherwise I'll keep slice-based reconciliation for a follow-up, thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T13:33:59Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:224** by [@mimowo](https://github.com/mimowo) — 2026-04-07T13:33:59Z
```diff
@@ -0,0 +1,229 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var podList corev1.PodList
+	if err := r.client.List(ctx, &podList,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	var gated []*corev1.Pod
+	for i := range podList.Items {
+		if utilpod.HasGate(&podList.Items[i], kueue.ElasticJobSchedulingGate) {
+			gated = append(gated, &podList.Items[i])
+		}
+	}
+	return gated, nil
+}
+
+// Workload predicates
+
+func (r *elasticJobUngater) Create(e event.TypedCreateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.Object) && workloadslicing.IsElasticWorkload(e.Object)
+}
+
+func (r *elasticJobUngater) Update(e event.TypedUpdateEvent[*kueue.Workload]) bool {
+	return workload.IsAdmitted(e.ObjectNew) && workloadslicing.IsElasticWorkload(e.ObjectNew)
+}
+
+func (r *elasticJobUngater) Delete(event.TypedDeleteEvent[*kueue.Workload]) bool {
+	return false
+}
+
+func (r *elasticJobUngater) Generic(event.TypedGenericEvent[*kueue.Workload]) bool {
+	return false
+}
+
+// Pod event handler
+
+var _ handler.EventHandler = (*elasticPodHandler)(nil)
+
+type elasticPodHandler struct {
+	expectationsStore *expectations.Store
+}
+
+func (h *elasticPodHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, false, q)
+}
+
+func (h *elasticPodHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.ObjectNew, false, q)
+}
+
+func (h *elasticPodHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForPod(ctx, e.Object, true, q)
+}
+
+func (h *elasticPodHandler) Generic(context.Context, event.GenericEvent, workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *elasticPodHandler) queueReconcileForPod(ctx context.Context, object client.Object, deleted bool, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	pod, isPod := object.(*corev1.Pod)
+	if !isPod {
+		return
+	}
+	wlName, found := pod.Annotations[kueue.WorkloadAnnotation]
```

  Unless good reasons I would prefer to contain the fix for this scenario in this PR, because I consider this a bugfix and I would like to cherrypick. If it is not included then we may risk releasing 0.17.1 with the gap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T16:08:27Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:144** by [@mimowo](https://github.com/mimowo) — 2026-04-08T16:08:27Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
```

  I'm not sure I understand this code, why not just:
  
  ```golang
  		if e != nil {
  			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
  			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
  		}
  		return e
  ```
  current code looks like may call `r.expectationsStore.ObservedUID1` twice on error, which seems harmless, but wrong

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T16:13:30Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@mimowo](https://github.com/mimowo) — 2026-04-08T16:13:31Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  IIUC this is just for legacy workloads before the introduction of the Workload SliceName annotation, right? 
  
  I would like to make that comment explicit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T16:15:12Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:153** by [@mimowo](https://github.com/mimowo) — 2026-04-08T16:15:12Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
```

  I would like to commonize this function with StartWorkloadSlicePods because they actually solve the same problem. For that we should decouple into two stages: findGatedPods, and actual ungating,
  
  On that note, do we still need `StartWorkloadSlicePods` at all?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-08T16:17:36Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:144** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-08T16:17:36Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
```

  Good catch! the `!ungated` case is effectively unreachable here since `listGatedPods` already filters for pods with the gate.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-08T16:37:58Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-08T16:37:58Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  Not just legacy but non-elastic workloads (regular Jobs with Kueue management but without `ElasticJobsViaWorkloadSlices`) only have the `WorkloadAnnotation`, never the        `WorkloadSliceNameAnnotation`. TAS also uses this index for those pods. I'll update the comment to make both cases explicit

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-08T16:39:49Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:153** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-08T16:39:48Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
```

  `StartWorkloadSlicePods` is no longer called from `ReconcileGenericJob` (removed in this PR), so yes, it's effectively dead code now. I kept it to avoid removing it in the same PR as the refactor, per your earlier comment about keeping this minimal for cherrypick. I can remove it and commonize in a follow-up, or if you prefer I can do it here. Let me know your thoughts.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T16:56:59Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@mimowo](https://github.com/mimowo) — 2026-04-08T16:56:59Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  Hold on, this code is only used for ElasticWorkloads even per the file name.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-08T18:13:38Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-08T18:13:38Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  Yeah sorry, wrong context. This query is here for the scale-up mismatch you asked to fix earlier. After scale-up, pods still have the old slice annotation but already point to the new workload via WorkloadAnnotation. The slice name lookup alone misses them.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T18:39:12Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@mimowo](https://github.com/mimowo) — 2026-04-08T18:39:11Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  > After scale-up, pods still have the old slice annotation but already point to the new workload via WorkloadAnnotation. The slice name lookup alone misses them.
  
  This actually might be a bug here, because when the Pods are listed by WorkloadSliceNameKey (above), they may be listed for the new Workload which is not amitted yet. If the new workload is not admitted then the Pods should stay gated. 
  
  Actually, I would like to add an e2e test case for this scenario. We attempt to scale up, but there is not enough qouta, so the new Workload stays pending - then verify the newly created Pods remain gated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T18:44:15Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:153** by [@mimowo](https://github.com/mimowo) — 2026-04-08T18:44:15Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
```

  I'm ok with both approaches, let's focus on the remaining issues with the controller itself.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T19:25:06Z

- **test/e2e/singlecluster/kuberay_test.go:230** by [@mimowo](https://github.com/mimowo) — 2026-04-08T19:25:06Z
```diff
@@ -222,12 +222,12 @@ def my_task(x, s):
 print([ray.get(my_task.remote(i, 1)) for i in range(4)])
 
 # run tasks in parallel to trigger autoscaling (scaling up)
-# Use longer sleep (8s) to give autoscaler time to detect demand,
+# Use longer sleep (16s) to give autoscaler time to detect demand,
 # create workload slices, and schedule new workers.
-print(ray.get([my_task.remote(i, 8) for i in range(16)]))
+print(ray.get([my_task.remote(i, 16) for i in range(16)]))
 
 # run tasks in sequence to trigger scaling down
-print([ray.get(my_task.remote(i, 1)) for i in range(32)])`,
+print([ray.get(my_task.remote(i, 1)) for i in range(256)])`,
```

  I think these are just moved from the other PR, can we revert? I don't see a need for that here - actually it was also my comment in the previous PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T19:31:14Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@mimowo](https://github.com/mimowo) — 2026-04-08T19:31:14Z
```diff
@@ -0,0 +1,250 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, replacement pods created by the operator (e.g. KubeRay)
+	// may carry the parent's WorkloadSliceNameAnnotation while their
+	// WorkloadAnnotation already points to the current admitted workload.
+	// Query by workload name to find these pods.
```

  I'm actually unclear for the need for this. I thought this is just for backwards compatibility for Workloads where pods may not have the WorkloadSliceName annotation, but for an Alpha feature this is a secondary concern.
  
  Assuming the WorkloadSliceNameAnnotation is populated to the Pods, they should all be found by the previous check.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-09T07:08:52Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@mimowo](https://github.com/mimowo) — 2026-04-09T07:08:52Z
```diff
@@ -0,0 +1,250 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, replacement pods created by the operator (e.g. KubeRay)
+	// may carry the parent's WorkloadSliceNameAnnotation while their
+	// WorkloadAnnotation already points to the current admitted workload.
+	// Query by workload name to find these pods.
```

  OTOH, now I'm wondering the opposite - we probably could fully rely on the WorkloadNameAnnotation - could you remind me why we actually check the WorkloadSliceName also here?
  
  I think it was needed on the previous approach when the ungating was triggered by the Job reconciler to make sure all Pods are ungated no matter which Workload they belong to.
  
  Now, if we have a mix of Pods belonging to different Workloads, then we will schedule two reconcile events, and they will both ungate their subset of Pods, if the workload is admitted. So in the new approach I actually don't see a need currently to use WorkloadSliceName.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-09T14:13:45Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-09T14:13:44Z
```diff
@@ -0,0 +1,252 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		if !ungated {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, pods may still carry the old slice annotation while
```

  Addressed. `listGatedPods` now filters pods through `eligibleWorkloadNames`, which walks the replacement chain and only includes workload names in the admitted chain. Pods from a pending replacement workload are excluded. Added unit test to cover this case

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-09T14:16:58Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-09T14:16:58Z
```diff
@@ -0,0 +1,250 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, replacement pods created by the operator (e.g. KubeRay)
+	// may carry the parent's WorkloadSliceNameAnnotation while their
+	// WorkloadAnnotation already points to the current admitted workload.
+	// Query by workload name to find these pods.
```

  After scale-up the parent workload is finished, so its reconcile returns early at `IsAdmitted`. Parent pods with `WorkloadAnnotation: "wl"` would never get ungated if we only query by `WorkloadAnnotation` as no one reconciles wl as admitted anymore. The slice name query lets the child workload find all pods in the chain with one List, then `eligibleWorkloadNames` filters out pods from non-admitted workloads.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-04-09T17:00:30Z

- **pkg/controller/elasticjobs/elastic_job_ungater.go:160** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-04-09T17:00:29Z
```diff
@@ -0,0 +1,250 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package elasticjobs
+
+import (
+	"context"
+	"errors"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	utilclient "sigs.k8s.io/kueue/pkg/util/client"
+	"sigs.k8s.io/kueue/pkg/util/expectations"
+	"sigs.k8s.io/kueue/pkg/util/parallelize"
+	utilpod "sigs.k8s.io/kueue/pkg/util/pod"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/pkg/workloadslicing"
+)
+
+const ControllerName = "ElasticJobUngater"
+
+var errPendingUngateOps = errors.New("pending elastic ungate operations")
+
+type elasticJobUngater struct {
+	client            client.Client
+	expectationsStore *expectations.Store
+	roleTracker       *roletracker.RoleTracker
+}
+
+var _ reconcile.Reconciler = (*elasticJobUngater)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*elasticJobUngater)(nil)
+
+// +kubebuilder:rbac:groups="",resources=pods,verbs=get;list;watch;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	r := &elasticJobUngater{
+		client:            mgr.GetClient(),
+		expectationsStore: expectations.NewStore(ControllerName),
+		roleTracker:       roleTracker,
+	}
+	podHandler := elasticPodHandler{
+		expectationsStore: r.expectationsStore,
+	}
+	return ControllerName, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named("elastic_job_ungater").
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			&handler.TypedEnqueueRequestForObject[*kueue.Workload]{},
+			r,
+		)).
+		Watches(&corev1.Pod{}, &podHandler).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ControllerName)).
+		Complete(core.WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+}
+
+func (r *elasticJobUngater) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile ElasticJobUngater")
+
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if client.IgnoreNotFound(err) != nil {
+			return reconcile.Result{}, err
+		}
+		return reconcile.Result{}, nil
+	}
+	if !r.expectationsStore.Satisfied(log, req.NamespacedName) {
+		return reconcile.Result{}, errPendingUngateOps
+	}
+	if !workload.IsAdmitted(wl) || !workloadslicing.IsElasticWorkload(wl) {
+		return reconcile.Result{}, nil
+	}
+
+	pods, err := r.listGatedPods(ctx, wl)
+	if err != nil {
+		return reconcile.Result{}, err
+	}
+	if len(pods) == 0 {
+		return reconcile.Result{}, nil
+	}
+
+	log.V(2).Info("identified elastic pods to ungate", "count", len(pods))
+	uids := make([]types.UID, len(pods))
+	for i := range pods {
+		uids[i] = pods[i].UID
+	}
+	r.expectationsStore.ExpectUIDs(log, req.NamespacedName, uids)
+
+	err = parallelize.Until(ctx, len(pods), func(i int) error {
+		pod := pods[i]
+		var ungated bool
+		e := utilclient.Patch(ctx, r.client, pod, func() (bool, error) {
+			ungated = utilpod.Ungate(pod, kueue.ElasticJobSchedulingGate)
+			if ungated {
+				log.V(3).Info("ungating elastic pod", "pod", klog.KObj(pod))
+			}
+			return ungated, nil
+		})
+		if e != nil {
+			r.expectationsStore.ObservedUID(log, req.NamespacedName, pod.UID)
+			log.Error(e, "failed ungating elastic pod", "pod", klog.KObj(pod))
+		}
+		return e
+	})
+	return reconcile.Result{}, err
+}
+
+func (r *elasticJobUngater) listGatedPods(ctx context.Context, wl *kueue.Workload) ([]*corev1.Pod, error) {
+	sliceName := workloadslicing.SliceName(wl)
+	var slicePods corev1.PodList
+	if err := r.client.List(ctx, &slicePods,
+		client.InNamespace(wl.Namespace),
+		client.MatchingFields{indexer.WorkloadSliceNameKey: sliceName},
+	); err != nil {
+		return nil, fmt.Errorf("listing pods for workload slice: %w", err)
+	}
+
+	// After scale-up, replacement pods created by the operator (e.g. KubeRay)
+	// may carry the parent's WorkloadSliceNameAnnotation while their
+	// WorkloadAnnotation already points to the current admitted workload.
+	// Query by workload name to find these pods.
```

  As discussed in DM with @mimowo , I simplified the ungating approach: each workload now handles its own pods (filtered by WorkloadAnnotation), and finished workloads that were previously admitted can still ungate their pods. This removes the chain-walking logic and keeps the controller lean.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-10T06:30:40Z

Thank you 👍 cc @hiboyang 
/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16
