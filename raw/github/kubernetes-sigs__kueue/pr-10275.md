# PR #10275: LeaderWorkerSet: Relax PodSpec validation to allow changing nodeSelector.

**Summary**: LeaderWorkerSet: Relax PodSpec validation to allow changing nodeSelector.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10275

**Last updated**: 2026-05-15T08:36:59Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `feature/relax-lws-validation` → `main`
- **Created**: 2026-04-02T08:02:59Z
- **Updated**: 2026-05-15T08:36:59Z
- **Merged**: 2026-05-04T12:23:42Z
- **Closed**: 2026-05-04T12:23:42Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 8   **+181 / -31**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
LeaderWorkerSet: Relax PodSpec validation to allow changing nodeSelector.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10178

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-02T08:03:06Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 7ce4d15a74f386cacd68c98c34925e2dd06b81cc |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f882c4a2cf250008b2746c |

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-04T09:12:53Z

/approve

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T11:33:13Z

/lgtm
/approve
Thank you 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T11:33:21Z

LGTM label has been added.  <details>Git tree hash: 7ecbea4547e62179ac83170d90770fa95be7cced</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T11:33:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10275#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10275#issuecomment-4370681063" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10275#issuecomment-4186815017" title="Approved">vladikkuzn</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T17:36:46Z

/kind feature

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T17:37:35Z

/cherrypick release-0.17

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-04T17:38:12Z

@mimowo: #10275 failed to apply on top of branch "release-0.17":
```
Applying: Relax PodSpec validation.
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobframework/validation_test.go
M	pkg/util/equality/podset.go
M	pkg/util/equality/podset_test.go
M	test/e2e/singlecluster/leaderworkerset_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/singlecluster/leaderworkerset_test.go
Auto-merging pkg/util/equality/podset_test.go
CONFLICT (content): Merge conflict in pkg/util/equality/podset_test.go
Auto-merging pkg/util/equality/podset.go
CONFLICT (content): Merge conflict in pkg/util/equality/podset.go
Auto-merging pkg/controller/jobframework/validation_test.go
Auto-merging pkg/controller/jobframework/reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobframework/reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Relax PodSpec validation.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10275#issuecomment-4373128667):

>/cherrypick release-0.17


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T17:38:36Z

cc @mbobrovskyi could you prepare the cherrypicks here?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-04T18:17:02Z

/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-04T18:17:39Z

@mbobrovskyi: #10275 failed to apply on top of branch "release-0.16":
```
Applying: Relax PodSpec validation.
Using index info to reconstruct a base tree...
M	pkg/controller/jobframework/reconciler.go
M	pkg/controller/jobframework/validation.go
M	pkg/controller/jobframework/validation_test.go
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_webhook_test.go
M	test/e2e/singlecluster/leaderworkerset_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/singlecluster/leaderworkerset_test.go
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_webhook_test.go
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
Auto-merging pkg/controller/jobframework/validation_test.go
Auto-merging pkg/controller/jobframework/validation.go
Auto-merging pkg/controller/jobframework/reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Relax PodSpec validation.

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10275#issuecomment-4373384881):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-04T18:18:16Z

@mbobrovskyi: new pull request created: #10930

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10275#issuecomment-4373384881):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-15T08:36:54Z

/release-note-edit
```release-note
NONE
```
We decided to revert in https://github.com/kubernetes-sigs/kueue/pull/11214 due to complications encountered during manual testing. We may return to it later.

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T15:17:04Z

- **pkg/controller/jobframework/reconciler.go:1227** by [@kannon92](https://github.com/kannon92) — 2026-04-06T15:17:04Z
```diff
@@ -1212,18 +1222,23 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
+	opts := make([]equality.ComparePodSetSlicesOption, 0, 2)
+	if workload.IsAdmitted(wl) {
+		opts = append(opts, equality.WithIgnoreTolerations(), equality.WithIgnoreNodeSelector())
```

  If we are adding tolerations also can we add a e2e test for that also?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-06T15:18:12Z

- **pkg/util/equality/podset_test.go:33** by [@kannon92](https://github.com/kannon92) — 2026-04-06T15:18:12Z
```diff
@@ -28,10 +28,11 @@ import (
 
 func TestComparePodSetSlices(t *testing.T) {
 	cases := map[string]struct {
-		a                 []kueue.PodSet
-		b                 []kueue.PodSet
-		ignoreTolerations bool
-		wantEquivalent    bool
+		a                  []kueue.PodSet
+		b                  []kueue.PodSet
+		ignoreTolerations  bool
```

  Can you add a unit test for using this field? I think its unusued right now.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-10T05:24:29Z

- **pkg/util/equality/podset_test.go:33** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-10T05:24:29Z
```diff
@@ -28,10 +28,11 @@ import (
 
 func TestComparePodSetSlices(t *testing.T) {
 	cases := map[string]struct {
-		a                 []kueue.PodSet
-		b                 []kueue.PodSet
-		ignoreTolerations bool
-		wantEquivalent    bool
+		a                  []kueue.PodSet
+		b                  []kueue.PodSet
+		ignoreTolerations  bool
```

  We already have it.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-10T05:24:57Z

- **pkg/controller/jobframework/reconciler.go:1227** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-10T05:24:57Z
```diff
@@ -1212,18 +1222,23 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
+	opts := make([]equality.ComparePodSetSlicesOption, 0, 2)
+	if workload.IsAdmitted(wl) {
+		opts = append(opts, equality.WithIgnoreTolerations(), equality.WithIgnoreNodeSelector())
```

  Actually, we added nodeSelector.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T12:55:00Z

- **pkg/util/equality/podset_test.go:33** by [@kannon92](https://github.com/kannon92) — 2026-04-10T12:55:00Z
```diff
@@ -28,10 +28,11 @@ import (
 
 func TestComparePodSetSlices(t *testing.T) {
 	cases := map[string]struct {
-		a                 []kueue.PodSet
-		b                 []kueue.PodSet
-		ignoreTolerations bool
-		wantEquivalent    bool
+		a                  []kueue.PodSet
+		b                  []kueue.PodSet
+		ignoreTolerations  bool
```

  Ah sorry git diffs confused me here.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T12:56:02Z

- **pkg/controller/jobframework/reconciler.go:1227** by [@kannon92](https://github.com/kannon92) — 2026-04-10T12:56:02Z
```diff
@@ -1212,18 +1222,23 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
+	opts := make([]equality.ComparePodSetSlicesOption, 0, 2)
+	if workload.IsAdmitted(wl) {
+		opts = append(opts, equality.WithIgnoreTolerations(), equality.WithIgnoreNodeSelector())
```

  Ah but I guess we don't have a e2e test for mutating tolerations. 
  
  This can be done as a followup.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T08:54:22Z

- **pkg/controller/jobframework/reconciler.go:1211** by [@mimowo](https://github.com/mimowo) — 2026-05-04T08:54:22Z
```diff
@@ -1207,7 +1207,8 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
-	opts := make([]equality.ComparePodSetsOption, 0, 1)
+	opts := make([]equality.ComparePodSetsOption, 0, 2)
+	opts = append(opts, equality.WithIgnoreNodeSelector())
```

  Can you elaborate why / if this is needed? This seems surprising to me,  because:
  1. JobReconciler in the jobframework iiuc is not responsible for creating or managing workloads for LWS
  2. it affects other Jobs and so the consequences are unclear to me
  
  So my suspicion is that this is not necessary / unrelated change.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-04T09:43:46Z

- **pkg/controller/jobframework/reconciler.go:1211** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-04T09:43:46Z
```diff
@@ -1207,7 +1207,8 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
-	opts := make([]equality.ComparePodSetsOption, 0, 1)
+	opts := make([]equality.ComparePodSetsOption, 0, 2)
+	opts = append(opts, equality.WithIgnoreNodeSelector())
```

  Yes, exactly. But if we don’t add WithIgnoreNodeSelector() to the jobframework, the behavior will change and we’ll end up comparing nodeSelectors as well. We don’t need to make any changes to the jobframework for this fix.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-04T09:47:26Z

- **pkg/controller/jobframework/reconciler.go:1211** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-04T09:47:26Z
```diff
@@ -1207,7 +1207,8 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
-	opts := make([]equality.ComparePodSetsOption, 0, 1)
+	opts := make([]equality.ComparePodSetsOption, 0, 2)
+	opts = append(opts, equality.WithIgnoreNodeSelector())
```

  WithIgnoreNodeSelector() ignores the behavior I added in comparePodTemplate().

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T10:05:52Z

- **pkg/controller/jobframework/reconciler.go:1211** by [@mimowo](https://github.com/mimowo) — 2026-05-04T10:05:52Z
```diff
@@ -1207,7 +1207,8 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
-	opts := make([]equality.ComparePodSetsOption, 0, 1)
+	opts := make([]equality.ComparePodSetsOption, 0, 2)
+	opts = append(opts, equality.WithIgnoreNodeSelector())
```

  Ahh, I see because `IgnoreNodeSelector` was previously the default (there was not code to verify that). THis makes sense.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T10:07:08Z

- **test/e2e/singlecluster/leaderworkerset_test.go:185** by [@mimowo](https://github.com/mimowo) — 2026-05-04T10:07:08Z
```diff
@@ -135,6 +135,55 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 			})
 		})
 
+		ginkgo.It("should allow to mutate nodeSelector", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws", ns.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(1).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				TerminationGracePeriod(1).
+				Queue(lq.Name).
+				Obj()
+
+			ginkgo.By("Create a LeaderWorkerSet", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			ginkgo.By("Waiting for replicas to be ready", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(1)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			ginkgo.By("Check workload is created and has JobUID label", func() {
+				gomega.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+				gomega.Expect(createdWorkload.Labels[ctrlconstants.JobUIDLabel]).To(gomega.Equal(string(lws.UID)))
+			})
+
+			oldUID := createdWorkload.UID
+
+			ginkgo.By("Updating nodeSelector", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.NodeSelector = map[string]string{"instance-type": "on-demand"}
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that workload recreated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+					g.Expect(createdWorkload.UID).ToNot(gomega.Equal(oldUID))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
```

  Let's also verify the Pods (replicas) get running (Ready) after the change.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T10:07:35Z

- **pkg/controller/jobframework/reconciler.go:1211** by [@mimowo](https://github.com/mimowo) — 2026-05-04T10:07:35Z
```diff
@@ -1207,7 +1207,8 @@ func EquivalentToWorkload(ctx context.Context, c client.Client, job GenericJob,
 	}
 	jobPodSets := clearMinCountsIfFeatureDisabled(getPodSets)
 
-	opts := make([]equality.ComparePodSetsOption, 0, 1)
+	opts := make([]equality.ComparePodSetsOption, 0, 2)
+	opts = append(opts, equality.WithIgnoreNodeSelector())
```

  lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T11:01:26Z

- **test/e2e/singlecluster/leaderworkerset_test.go:180** by [@mimowo](https://github.com/mimowo) — 2026-05-04T11:01:26Z
```diff
@@ -135,6 +135,63 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 			})
 		})
 
+		ginkgo.It("should allow to mutate nodeSelector", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws", ns.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(1).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				TerminationGracePeriod(1).
+				Queue(lq.Name).
+				Obj()
+
+			ginkgo.By("Create a LeaderWorkerSet", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			ginkgo.By("Waiting for replicas to be ready", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(1)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			ginkgo.By("Check workload is created and has JobUID label", func() {
+				gomega.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+				gomega.Expect(createdWorkload.Labels[ctrlconstants.JobUIDLabel]).To(gomega.Equal(string(lws.UID)))
+			})
+
+			oldUID := createdWorkload.UID
+
+			ginkgo.By("Updating nodeSelector", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.NodeSelector = map[string]string{"instance-type": "on-demand"}
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that workload recreated", func() {
```

  Let's also await that the newly craeted workload is admitted. This checks the admission worked properly, and also gives more time before the ReadyReplicas check so that it would go down if the Pods were not restared.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-04T11:28:09Z

- **test/e2e/singlecluster/leaderworkerset_test.go:185** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-04T11:28:09Z
```diff
@@ -135,6 +135,55 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 			})
 		})
 
+		ginkgo.It("should allow to mutate nodeSelector", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws", ns.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(1).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				TerminationGracePeriod(1).
+				Queue(lq.Name).
+				Obj()
+
+			ginkgo.By("Create a LeaderWorkerSet", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			ginkgo.By("Waiting for replicas to be ready", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(1)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			ginkgo.By("Check workload is created and has JobUID label", func() {
+				gomega.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+				gomega.Expect(createdWorkload.Labels[ctrlconstants.JobUIDLabel]).To(gomega.Equal(string(lws.UID)))
+			})
+
+			oldUID := createdWorkload.UID
+
+			ginkgo.By("Updating nodeSelector", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.NodeSelector = map[string]string{"instance-type": "on-demand"}
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that workload recreated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+					g.Expect(createdWorkload.UID).ToNot(gomega.Equal(oldUID))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-04T11:28:14Z

- **test/e2e/singlecluster/leaderworkerset_test.go:180** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-04T11:28:14Z
```diff
@@ -135,6 +135,63 @@ var _ = ginkgo.Describe("LeaderWorkerSet integration", ginkgo.Label("area:single
 			})
 		})
 
+		ginkgo.It("should allow to mutate nodeSelector", func() {
+			lws := leaderworkersettesting.MakeLeaderWorkerSet("lws", ns.Name).
+				Image(util.GetAgnHostImage(), util.BehaviorWaitForDeletion).
+				Size(1).
+				Replicas(1).
+				RequestAndLimit(corev1.ResourceCPU, "200m").
+				TerminationGracePeriod(1).
+				Queue(lq.Name).
+				Obj()
+
+			ginkgo.By("Create a LeaderWorkerSet", func() {
+				util.MustCreate(ctx, k8sClient, lws)
+			})
+
+			ginkgo.By("Waiting for replicas to be ready", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					g.Expect(createdLeaderWorkerSet.Status.ReadyReplicas).To(gomega.Equal(int32(1)))
+					g.Expect(createdLeaderWorkerSet.Status.Conditions).To(utiltesting.HaveConditionStatusTrueAndReason("Available", "AllGroupsReady"))
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdWorkload := &kueue.Workload{}
+			ginkgo.By("Check workload is created and has JobUID label", func() {
+				gomega.Expect(k8sClient.Get(ctx, util.WorkloadKeyForLeaderWorkerSet(lws, "0"), createdWorkload)).To(gomega.Succeed())
+				gomega.Expect(createdWorkload.Labels[ctrlconstants.JobUIDLabel]).To(gomega.Equal(string(lws.UID)))
+			})
+
+			oldUID := createdWorkload.UID
+
+			ginkgo.By("Updating nodeSelector", func() {
+				createdLeaderWorkerSet := &leaderworkersetv1.LeaderWorkerSet{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lws), createdLeaderWorkerSet)).To(gomega.Succeed())
+					createdLeaderWorkerSet.Spec.LeaderWorkerTemplate.WorkerTemplate.Spec.NodeSelector = map[string]string{"instance-type": "on-demand"}
+					g.Expect(k8sClient.Update(ctx, createdLeaderWorkerSet)).To(gomega.Succeed())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that workload recreated", func() {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T17:36:22Z

Let me cherrypick as the diff is pretty small, and I've got an ask from a user.
/cherrypick release-0.17
/cherrypick release-0.16
