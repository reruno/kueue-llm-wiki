# PR #10279: [Cleanup] Return boolean for hierarchy modification in AddOrUpdateCohort

**Summary**: [Cleanup] Return boolean for hierarchy modification in AddOrUpdateCohort

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10279

**Last updated**: 2026-04-14T08:30:39Z

---

## Metadata

- **State**: open
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `feat/7539-prep-add-or-update-cohort-return-hierarchy-change` → `main`
- **Created**: 2026-04-02T11:19:58Z
- **Updated**: 2026-04-14T08:30:39Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mykysha](https://github.com/mykysha), [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi), [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 15   **+57 / -49**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Prep for [Provide a metric for cohort and clusterqueue info](https://github.com/kubernetes-sigs/kueue/pull/10004)

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates #7539

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-02T11:20:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cbf5463adf670ea05c688de2a3a0d2012e6051b5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69dca62f131037000730cb17 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10279--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10279--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMjc5LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.i8Hnp_RuEc7QuWhSXpO2vKk9SLHlq6t84lDJTxR61bM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10279--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-02T12:59:18Z

/cc @vladikkuzn @mykysha @ivnovakov

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-02T12:59:23Z

@mszadkow: GitHub didn't allow me to request PR reviews from the following users: ivnovakov.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10279#issuecomment-4177701535):

>/cc @vladikkuzn @mykysha @ivnovakov 


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-03T08:16:44Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-09T10:19:33Z

LGTM label has been added.  <details>Git tree hash: fffdaa1dff9b96ab76565c08d9ee7f7da92439a8</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-09T10:19:38Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-13T08:15:45Z

New changes are detected. LGTM label has been removed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-13T08:15:50Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10279#" title="Author self-approved">mszadkow</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10279#issuecomment-4182473296" title="Approved">vladikkuzn</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [mbobrovskyi](https://github.com/mbobrovskyi) and additionally assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba","mbobrovskyi"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-14T08:30:39Z

@mszadkow: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-multikueue-sequential-main | cbf5463adf670ea05c688de2a3a0d2012e6051b5 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10279/pull-kueue-test-e2e-multikueue-sequential-main/2043967329905152000) | true | `/test pull-kueue-test-e2e-multikueue-sequential-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=10279). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Amszadkow). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:34:55Z

- **pkg/cache/scheduler/cache.go:526** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:34:55Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
```

  ```suggestion
  // AddOrUpdateCohort adds a cohort to the cache or updates its hierarchy and properties if it already exists.
  // Returns a boolean indicating if the cohort hierarchy has changed, and an error if any issue occurs during the update.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-02T14:40:44Z

- **pkg/cache/scheduler/cache.go:548** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-02T14:40:44Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  Actually, I’m not sure we need to check the root. I think checking whether the parent changed is enough.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-02T16:24:59Z

- **pkg/cache/scheduler/cache.go:548** by [@mszadkow](https://github.com/mszadkow) — 2026-04-02T16:24:59Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  Maybe here it's not visible why, but we will need it as this is trigger to update the hierarchy info in the metrics

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-03T08:16:36Z

- **pkg/cache/scheduler/cache.go:549** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-03T08:16:36Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort adds a cohort to the cache or updates its hierarchy and properties if it already exists.
+// Returns a boolean indicating if the cohort hierarchy has changed, and an error if any issue occurs during the update.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
+	hierarchyChanged := oldParentName != newParentName || oldRootName != newRootName
```

  Stupid question: can we say that there's a change in the hierarchy if parent hasn't changed, root hasn't changed, but there're changes in another brach of the hierarchy?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-03T09:20:22Z

- **pkg/cache/scheduler/cache.go:549** by [@mszadkow](https://github.com/mszadkow) — 2026-04-03T09:20:22Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort adds a cohort to the cache or updates its hierarchy and properties if it already exists.
+// Returns a boolean indicating if the cohort hierarchy has changed, and an error if any issue occurs during the update.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
+	hierarchyChanged := oldParentName != newParentName || oldRootName != newRootName
```

  Our goal here is to detect hierarchy changed that refers to the cohort.
  The concern is either parent or root has changed.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-03T09:33:09Z

- **pkg/cache/scheduler/cache.go:548** by [@mszadkow](https://github.com/mszadkow) — 2026-04-03T09:33:09Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  root is important for the reparenting with root change

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-06T04:32:11Z

- **pkg/cache/scheduler/cache.go:548** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-06T04:32:11Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  We update apiCohort, right? After that, we check newParentName. If it changed, then we return true. But I still don’t understand how it’s possible for the root to change if the parent didn’t.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-09T08:58:48Z

- **pkg/cache/scheduler/cache.go:548** by [@mszadkow](https://github.com/mszadkow) — 2026-04-09T08:58:48Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  I thought it will be in the scenario when we do the reparenting on the higher levels, but after some investigation I think you are correct.
  The root should be already updated and we can't detect it here.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-09T09:00:36Z

- **pkg/cache/scheduler/cache.go:548** by [@mszadkow](https://github.com/mszadkow) — 2026-04-09T09:00:36Z
```diff
@@ -522,15 +522,32 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort returns (hierarchyChanged, error), hierarchyChanged is true
+// when the cohort's parent or root cohort changed.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName, oldRootName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+	if !hierarchy.HasCycle(cohort) {
+		oldRootName = cohort.getRootUnsafe().Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	newRootName := cohort.getRootUnsafe().Name
```

  @mbobrovskyi updated

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-09T09:20:01Z

- **pkg/cache/scheduler/cache.go:545** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-09T09:20:01Z
```diff
@@ -522,15 +522,28 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort adds a cohort to the cache or updates its hierarchy and properties if it already exists.
+// Returns a boolean indicating if the cohort hierarchy has changed, and an error if any issue occurs during the update.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
+
 	c.hm.UpdateCohortEdge(cohortName, apiCohort.Spec.ParentName)
-	return cohort.updateCohort(apiCohort, oldParent)
+	if err := cohort.updateCohort(apiCohort, oldParent); err != nil {
+		return false, err
+	}
+
+	newParentName := apiCohort.Spec.ParentName
+	hierarchyChanged := oldParentName != newParentName
```

  ```suggestion
  	hierarchyChanged := oldParentName != apiCohort.Spec.ParentName
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-09T09:21:04Z

- **pkg/cache/scheduler/cache.go:537** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-09T09:21:04Z
```diff
@@ -522,15 +522,28 @@ func (c *Cache) DeleteClusterQueue(cq *kueue.ClusterQueue) {
 	}
 }
 
-func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) error {
+// AddOrUpdateCohort adds a cohort to the cache or updates its hierarchy and properties if it already exists.
+// Returns a boolean indicating if the cohort hierarchy has changed, and an error if any issue occurs during the update.
+func (c *Cache) AddOrUpdateCohort(apiCohort *kueue.Cohort) (bool, error) {
 	c.Lock()
 	defer c.Unlock()
 	cohortName := kueue.CohortReference(apiCohort.Name)
 	c.hm.AddCohort(cohortName)
 	cohort := c.hm.Cohort(cohortName)
 	oldParent := cohort.Parent()
+	var oldParentName kueue.CohortReference
+	if oldParent != nil {
+		oldParentName = oldParent.Name
+	}
```

  nit: can we move it after updateCohort?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-09T09:21:11Z

Overall LGTM.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-09T10:19:26Z

/lgtm

Thank you!
