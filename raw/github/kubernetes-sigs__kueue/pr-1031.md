# PR #1031: Add KEP for Pod support

**Summary**: Add KEP for Pod support

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1031

**Last updated**: 2023-09-12T06:54:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@alculquicondor](https://github.com/alculquicondor)
- **Branch**: `pod-kep` → `main`
- **Created**: 2023-08-01T20:31:56Z
- **Updated**: 2023-09-12T06:54:14Z
- **Merged**: 2023-09-12T06:54:12Z
- **Closed**: 2023-09-12T06:54:12Z
- **Labels**: `kind/feature`, `lgtm`, `tide/merge-method-merge`, `release-note-none`, `size/XL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@kerthcet](https://github.com/kerthcet), [@ahg-g](https://github.com/ahg-g), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 2   **+709 / -0**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Design for support for plain Pods. 

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #976 

#### Special notes for your reviewer:

I listed all the stories that we can potentially support, but we might prioritize only 1 and 4.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-01T20:32:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1031#" title="Author self-approved">alculquicondor</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-01T20:32:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | a6ea91172efa4db47350c15dba7c6ea4aadb168a |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64ff2e600b03500008a3c205 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-01T20:32:36Z

cc @ahg-g @tenzen-y @denkensk

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T15:10:45Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:45:02Z

Otherwise LGTM

/assign @trasc @ahg-g

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:23:17Z

/lgtm

### Comment by [@kannon92](https://github.com/kannon92) — 2023-08-14T13:32:40Z

KEP LGTM!

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-30T19:10:21Z

I decided to simplify the support for multiple pod shapes with a focus on easy of use.
See last commit for the change.

### Comment by [@ahaysx](https://github.com/ahaysx) — 2023-08-31T23:03:24Z

LGTM

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-06T18:58:33Z

/hold cancel
Anything else @tenzen-y @kerthcet ?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T10:51:07Z

Please give me a little time.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-09-07T15:22:24Z

I will take a look of this KEP tomorrow, but if this blocks your development, feel free to merge this first. Sorry for that.

### Comment by [@kerthcet](https://github.com/kerthcet) — 2023-09-12T06:52:07Z

/label tide/merge-method-merge
/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-12T06:52:13Z

LGTM label has been added.  <details>Git tree hash: 1d566f2d96b724abeb5766dea0f3b14f8fd0f310</details>

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-03T22:00:52Z

- **keps/976-plain-pods/kep.yaml:22** by [@kannon92](https://github.com/kannon92) — 2023-08-03T22:00:51Z
```diff
@@ -0,0 +1,31 @@
+title: Plain Pods
+kep-number: 976
+authors:
+  - "@alculquicondor"
+status: provisional
+creation-date: 2023-07-31
+reviewers:
+  - "@trasc"
+approvers:
+  - "@ahg-g"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.5"
```

  ```suggestion
    beta: "v0.5"
  ```
  
  Not sure if you all have alpha in Kueue.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-07T11:40:37Z

- **keps/976-plain-pods/README.md:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T05:40:32Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
```

  ```suggestion
  - Support queueing of individual Pods.
  ```

- **keps/976-plain-pods/README.md:263** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T06:37:01Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
```

  ```suggestion
  - the webhook should not add a scheduling gate.
  ```

- **keps/976-plain-pods/README.md:259** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T07:22:12Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
```

  Is this similar to https://github.com/kubernetes-sigs/kueue/blob/7f2b00b796dd719e7add27c244278ce251f48dfe/pkg/controller/jobframework/reconciler.go#L304-L318?

- **keps/976-plain-pods/README.md:283** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T07:31:49Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
```

  I think moving this to `Non-Goals` would be worth it.

- **keps/976-plain-pods/README.md:311** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T07:47:23Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
```

  I'm wondering if making the `Template` a pointer means breaking API. However, the existing environment wouldn't be broken since CRD wouldn't be broken.
  @alculquicondor In k/k, does making a field pointer mean breaking API?

- **keps/976-plain-pods/README.md:324** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T08:15:55Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
```

  If other webhooks/controllers inject other scheduling gates into the pods, what happens?
  Maybe, the kueue-controller will try to start the pod many times?
  
  So we should prevent to be injected other scheduling gates into the pods managed by the kueue or, the kueue-controller should wait for all scheduling gates to be removed.
  
  @alculquicondor WDYT?

- **keps/976-plain-pods/README.md:372** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T09:45:00Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
```

  What conditions does the webhook add the label, `kueue.x-k8s.io/managed: true` to the pod?
  
  1. Pod has `kueue.x-k8s.io/queue-name` label.
  2. Pod's namespace matches `.integrations.namespaceSelector`.
  3. Pod isn't owned by the object managed by the kueue.
  
  Is there anything else?

- **keps/976-plain-pods/README.md:373** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T09:47:26Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
```

  Does `manually delete the Workload object` mean that the kueue will delete the Workload once the kueue marks the Workload as finished in the .status.condition?
  Or, human should delete the Workload?

- **keps/976-plain-pods/README.md:421** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T11:26:02Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
```

  +1

- **keps/976-plain-pods/README.md:434** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T11:32:10Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
```

  Which field will the kueue increment count? `.status.reclaimablePods[*].count`?

- **keps/976-plain-pods/README.md:436** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T11:34:00Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
```

  I think the reason is `
  While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
  costly for framework or platform developers to do so.` (motivation)?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-07T12:30:11Z

- **keps/976-plain-pods/README.md:524** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:26:39Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
+
+<<[/UNRESOLVED]>>
+
+The Workload object can be generated after observing the first Pod.
+The Workload for the Pod in [story 2](#story-2) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 10
+    name: main # this name is irrelevant.
+    podRef: pod-index-0 # any pod would do.
+```
+
+#### Groups of pods with multiple shapes or roles
+
+When a group has multiple shapes, sometimes known as roles, we need to know how many shapes there
+are. The Pods declare their role with the label `kueue.x-k8s.io/pod-group-role`. The annotation
+`kueue.k8s.io/pod-group-role-count` contains how many pods belong to the role.
+
+We can only build the Workload object once we observe at least one Pod per role.
+
+The Workload for the Pod in [story 3](#story-3) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+#### Groups of pods where driver generates workers
+
+When most Pods of a group are only created after a subset of them start running, users need to
+provide the shapes of the following pods before hand.
+
+Users can provide the shapes of the remaining roles in an annotation
+`kueue.x-k8s.io/pod-group-sets`, taking a yaml/json with the same structure as the Workload PodSets.
+The template for the initial pods can be left empty, as it can be populated by Kueue.
+
+The Workload for the Pod in [story 4](#story-4) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+### Tracking admitted and finished Pods
+
+Pods need to have finalizers so that we can reliably track how many of them run to completion and be
+able to determine when the Workload is Finished.
+
+When a Workload is admitted, the Workload reconciler would keep an in-memory cache of expected
+admissions: the number of admitted pods that are not reflected in the informers yet, per Workload.
+
+In the Pod event handler, we decrement the counter when we see a transition from having
+the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+
+In the Workload reconciler:
+1. admitted_pods_informer: the number of non-terminated pods in the informer that are admitted.
```

  ```suggestion
  1. admitted_pods_in_informer: the number of non-terminated pods in the informer that are admitted.
  ```
  
  Is this right?

- **keps/976-plain-pods/README.md:573** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:30:07Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
+
+<<[/UNRESOLVED]>>
+
+The Workload object can be generated after observing the first Pod.
+The Workload for the Pod in [story 2](#story-2) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 10
+    name: main # this name is irrelevant.
+    podRef: pod-index-0 # any pod would do.
+```
+
+#### Groups of pods with multiple shapes or roles
+
+When a group has multiple shapes, sometimes known as roles, we need to know how many shapes there
+are. The Pods declare their role with the label `kueue.x-k8s.io/pod-group-role`. The annotation
+`kueue.k8s.io/pod-group-role-count` contains how many pods belong to the role.
+
+We can only build the Workload object once we observe at least one Pod per role.
+
+The Workload for the Pod in [story 3](#story-3) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+#### Groups of pods where driver generates workers
+
+When most Pods of a group are only created after a subset of them start running, users need to
+provide the shapes of the following pods before hand.
+
+Users can provide the shapes of the remaining roles in an annotation
+`kueue.x-k8s.io/pod-group-sets`, taking a yaml/json with the same structure as the Workload PodSets.
+The template for the initial pods can be left empty, as it can be populated by Kueue.
+
+The Workload for the Pod in [story 4](#story-4) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+### Tracking admitted and finished Pods
+
+Pods need to have finalizers so that we can reliably track how many of them run to completion and be
+able to determine when the Workload is Finished.
+
+When a Workload is admitted, the Workload reconciler would keep an in-memory cache of expected
+admissions: the number of admitted pods that are not reflected in the informers yet, per Workload.
+
+In the Pod event handler, we decrement the counter when we see a transition from having
+the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+
+In the Workload reconciler:
+1. admitted_pods_informer: the number of non-terminated pods in the informer that are admitted.
+   We only look at non-terminated pods to allow for terminated pods to be replaced.
+1. admitted_pods = admitted_pods_in_informer + expected_admissions. Note that this might temporarily
+   lead to double counting.
+2. For gated pods:
+  - If admitted_pods < admission.count, remove the gate, set nodeSelector, an increase expected_admissions
+  - Else,
+    - If admitted_pods_in_informer < admission.count, we can't admit this Pod now to prevent
+      overbooking, but requeue this Pod for retry.
+    - Else, remove finalizer and delete the Pod, as it's beyond the allowed admission.
+3. If the number of terminated pods with a finalizer is greater than or equal to the admission
+  count, and there are no non-terminated Pods, mark the Workload as Finished and remove the
+  finalizers from the Pods.
+
+In the Pod reconciler:
+0. If the Pod is not terminated,
+  create a Workload for the pod group if one does not exist.
+1. If the Pod is terminated,
+   - If the Workloald doesn't exist or the workload is finished, remove the finalizer.
+
+Note that we are only removing Pod finalizers once the Workload is finished. This is a simple way
+of managing finalizers, but it might lead to too many Pods lingering in etcd for a long time after
+terminated. In a future version, we can consider a better scheme similar to [Pod tracking in Jobs](https://kubernetes.io/blog/2022/12/29/scalable-job-tracking-ga/).
```

  It's great!

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2023-08-09T13:21:55Z

- **keps/976-plain-pods/README.md:277** by [@denkensk](https://github.com/denkensk) — 2023-08-09T13:21:49Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
```

  Will the recreated pods enqueue again or not?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T15:09:05Z

- **keps/976-plain-pods/README.md:259** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T13:52:51Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
```

  Yes, it would be a similar implementation.

- **keps/976-plain-pods/README.md:277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:11:37Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
```

  They won't. Clarified.

- **keps/976-plain-pods/README.md:283** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:15:00Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
```

  Good idea. Added.

- **keps/976-plain-pods/README.md:311** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:16:20Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
```

  It would be breaking for users of the golang types, but not for the REST API.
  However, based on https://github.com/kubernetes-sigs/kueue/issues/1004#issuecomment-1673492782, I'm dropping this section.

- **keps/976-plain-pods/README.md:324** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:17:34Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
```

  The kueue-controller would just remove the kueue-specific gate. If there are other gates, the pods would remain pending, but kueue wouldn't be concerned with that.
  
  Added a paragraph to clarify this.

- **keps/976-plain-pods/README.md:372** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:20:25Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
```

  > Pod has kueue.x-k8s.io/queue-name label.
  
  This depends on the setting for `manageJobsWithoutQueueName`
  
  I also added a pod label selector, just in case. See the previous section.

- **keps/976-plain-pods/README.md:373** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:23:40Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
```

  Kueue would delete the Workload after it has the Finished condition. Fixed.

- **keps/976-plain-pods/README.md:421** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T14:30:21Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
```

  Actually, even if there is no CRD, there might be an external system (like a job dispatcher not based on CRDs) that can handle recreations. So there can be recreations, as already updated in the Tracking section.
  Removing this UNRESOLVED section.

- **keps/976-plain-pods/README.md:434** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T15:05:30Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
```

  I didn't want to be specific, but I'm discarding this section for now, in favor of keeping the pods until the entire Workload is finished.

- **keps/976-plain-pods/README.md:436** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T15:06:01Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
```

  True, but it would be expensive for Kueue to justify re-implementing Job.

- **keps/976-plain-pods/README.md:524** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T15:06:32Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
+
+<<[/UNRESOLVED]>>
+
+The Workload object can be generated after observing the first Pod.
+The Workload for the Pod in [story 2](#story-2) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 10
+    name: main # this name is irrelevant.
+    podRef: pod-index-0 # any pod would do.
+```
+
+#### Groups of pods with multiple shapes or roles
+
+When a group has multiple shapes, sometimes known as roles, we need to know how many shapes there
+are. The Pods declare their role with the label `kueue.x-k8s.io/pod-group-role`. The annotation
+`kueue.k8s.io/pod-group-role-count` contains how many pods belong to the role.
+
+We can only build the Workload object once we observe at least one Pod per role.
+
+The Workload for the Pod in [story 3](#story-3) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+#### Groups of pods where driver generates workers
+
+When most Pods of a group are only created after a subset of them start running, users need to
+provide the shapes of the following pods before hand.
+
+Users can provide the shapes of the remaining roles in an annotation
+`kueue.x-k8s.io/pod-group-sets`, taking a yaml/json with the same structure as the Workload PodSets.
+The template for the initial pods can be left empty, as it can be populated by Kueue.
+
+The Workload for the Pod in [story 4](#story-4) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-group
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: driver 
+    podRef: job-driver
+  - count: 10
+    name: worker
+    podRef: job-worker-0 # any pod of the role would do
+```
+
+### Tracking admitted and finished Pods
+
+Pods need to have finalizers so that we can reliably track how many of them run to completion and be
+able to determine when the Workload is Finished.
+
+When a Workload is admitted, the Workload reconciler would keep an in-memory cache of expected
+admissions: the number of admitted pods that are not reflected in the informers yet, per Workload.
+
+In the Pod event handler, we decrement the counter when we see a transition from having
+the scheduling gate `kueue.x-k8s.io/admission` to not having it.
+
+In the Workload reconciler:
+1. admitted_pods_informer: the number of non-terminated pods in the informer that are admitted.
```

  yes, thanks

- **keps/976-plain-pods/kep.yaml:22** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T15:07:19Z
```diff
@@ -0,0 +1,31 @@
+title: Plain Pods
+kep-number: 976
+authors:
+  - "@alculquicondor"
+status: provisional
+creation-date: 2023-07-31
+reviewers:
+  - "@trasc"
+approvers:
+  - "@ahg-g"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.5"
```

  We do, but in this case, we won't have a feature gate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:16:53Z

- **keps/976-plain-pods/README.md:259** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:16:53Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
```

  It sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:21:26Z

- **keps/976-plain-pods/README.md:311** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:21:26Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
```

  > It would be breaking for users of the golang types, but not for the REST API.
  
  It makes sense.
  
  @alculquicondor As I can see in other sections, the `podRef` field still leaves. Can we clean up the `podRef` field from the whole of this KEP?
  
  https://github.com/kubernetes-sigs/kueue/blob/d63751dee105caccd3c74f3a229f8fd88e7f4263/keps/976-plain-pods/README.md#single-pods

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:40:30Z

- **keps/976-plain-pods/README.md:324** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:40:29Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:44:52Z

- **keps/976-plain-pods/README.md:353** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:44:52Z
```diff
@@ -0,0 +1,628 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+In some scenarios, users might have custom job objects that own Pods through an indirect object.
+In these cases, it might be simpler to identify the pods through a label selector.
+
+2. Pods belonging to specific namespaces (such as kube-system or kueue-system).
+
+The namespaces and pod selectors are defined in Configuration.Integrations.
+For a Pod to qualify for queueing by Kueue, it needs to satisfy both the namespace and pod selector.
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+  PodSelector *metav1Selector
```

  ```suggestion
    PodSelector *metav1.LabelSelector
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T17:05:25Z

- **keps/976-plain-pods/README.md:421** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:05:25Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
```

  It makes sense. Maybe, there would be no pods unmanaged by anyone (CRDs or not CRDs) in the production (Raw Pods).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T17:37:35Z

- **keps/976-plain-pods/README.md:182** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:36:26Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
```

  Should we limit the number of `pod-group-role-count` per the same `pod-group-name` to `8`, similar to the limit of the workload podSets?
  
  https://github.com/kubernetes-sigs/kueue/blob/acec9e1bd99f0a51e555abfe42692d52adc216a1/apis/kueue/v1beta1/workload_types.go#L31-L35
  
  IIUC, the `pod-group-role` concept is the same as Workload PodSet, right?

- **keps/976-plain-pods/README.md:182** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:37:31Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
```

  What is the difference `kueue.x-k8s.io/pod-group-roles` with `kueue.x-k8s.io/pod-group-roles-count`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T17:47:23Z

- **keps/976-plain-pods/README.md:436** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:47:23Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
+  PodRef *string
+}
+```
+
+The value of PodRef is the name of the Pod object, in the same namespace.
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+2. Pods belonging to specific namespaces (such as kube-system).
+
+The set of namespaces is defined in Configuration.Integrations
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that we need to manually delete the Workload object once we have determined that all pods
+have finished.
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+When constructing the Workload object, kueue only populates a single podset using the podRef field.
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    podRef: foo
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
+
+#### Groups of Pods with the same shape
+
+When multiple pods belong to the same group and have the same shape, we need to know how many pods
+belong to the group.
+
+These groups of Pods can be identified when they have:
+- the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group.
+- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group
+
+<<[UNRESOLVED expectations]>>
+
+1. Can we assume that when a Pod fails it won't be recreated?
+2. Alternatively, is there be a pod controller that handles recreation? If so, how do we know
+   if the job finished, so that there wouldn't be more recreations?
+
+Most likely, we should assume that the Pods are just plain Pods not controlled by a custom resource.
+Otherwise, users should prefer to integrate directly with the Workload API, which is not a
+significant effort, but would be more reliable.
+
+3. Can we assume that the workloads are thrustworthy (no more pods are sent than the annotation states).
+
+Annotations across multiple objects cannot easily be validated, as such, we should probably guard
+against misuse.
+
+<<[/UNRESOLVED]>>
+
+<<[UNRESOLVED finalizers and Job API]>>
+
+As pods terminate (success or failure), we need to free quota.
+In order to reliably track terminated Pods, we need to add finalizers to pods. When the pods finish,
+we increment a counter in the Workload status and remove the finalizer.
+
+This problem is already solved by the Job API, so the natural question is: why not just use a Job?
```

  That makes sense.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T17:51:53Z

- **keps/976-plain-pods/README.md:50** by [@kannon92](https://github.com/kannon92) — 2023-08-11T17:41:00Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
```

  ```suggestion
  Some batch systems or AI/ML frameworks create plain Pods to represent Jobs or tasks of a Job.
  ```
  This is a huge nit.  I noticed that the docs team tends to prefer caps for K8 resources and you capitalize Pods but not jobs throughout.

- **keps/976-plain-pods/README.md:348** by [@kannon92](https://github.com/kannon92) — 2023-08-11T17:49:38Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+In some scenarios, users might have custom job objects that own Pods through an indirect object.
+In these cases, it might be simpler to identify the pods through a label selector.
+
+2. Pods belonging to specific namespaces (such as kube-system or kueue-system).
+
+The namespaces and pod selectors are defined in Configuration.Integrations.
+For a Pod to qualify for queueing by Kueue, it needs to satisfy both the namespace and pod selector.
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
```

  ```suggestion
    PodOptions *PodIntegrationOptions
  ```
  I think.

- **keps/976-plain-pods/README.md:332** by [@kannon92](https://github.com/kannon92) — 2023-08-11T17:51:23Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
```

  Should you maybe also consider pods owned by workloads (Deployment, StatefulSet, DaemonSets, etc) to not be included in kueue?  
  
  I worry that this configuration would be too limiting for a highly used cluster.  One that has deployments/statefulsets/daemon sets spread across many different namespaces.  I guess you could set up namespaces for all these ones but I could see that being difficult to keep up with.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:53:00Z

- **keps/976-plain-pods/README.md:182** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:53:00Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
```

  Yes, added these details in the Design section.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:53:16Z

- **keps/976-plain-pods/README.md:182** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:53:16Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
```

  Clarified

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:54:05Z

- **keps/976-plain-pods/README.md:50** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:54:04Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
```

  in this case, it doesn't refer to k8s resources, but the generic idea of job

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T17:54:09Z

- **keps/976-plain-pods/README.md:228** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:54:09Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
```

  I think that if we allow users to set unlimited strings here, there is a potential issue with the size of the annotation. IIRC, the annotations size limit is 256KiB.
  
  So, can we limit the size of ` kueue.x-k8s.io/pod-group-sets` or mention the potential issue in `Risks and Mitigations` section?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:55:25Z

- **keps/976-plain-pods/README.md:332** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:55:25Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
```

  I added a PodSelector as well.
  I don't want to plainly exclude Pods from (say) Deployments, as there could be valid reasons for queueing them.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T17:55:28Z

- **keps/976-plain-pods/README.md:50** by [@kannon92](https://github.com/kannon92) — 2023-08-11T17:55:27Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
```

  Sounds good.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:55:45Z

- **keps/976-plain-pods/README.md:228** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:55:45Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
```

  Let me add to risks

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T17:56:32Z

- **keps/976-plain-pods/README.md:182** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T17:56:31Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
```

  SGTM

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:00:43Z

- **keps/976-plain-pods/kep.yaml:31** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:00:43Z
```diff
@@ -0,0 +1,31 @@
+title: Plain Pods
+kep-number: 976
+authors:
+  - "@alculquicondor"
+status: provisional
+creation-date: 2023-07-31
+reviewers:
+  - "@trasc"
+approvers:
+  - "@ahg-g"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.5"
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+# No associated feature gates. The feature can be disabled through the Configuration API.
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  Maybe, you forgot to set some value here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:01:41Z

- **keps/976-plain-pods/kep.yaml:31** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:01:41Z
```diff
@@ -0,0 +1,31 @@
+title: Plain Pods
+kep-number: 976
+authors:
+  - "@alculquicondor"
+status: provisional
+creation-date: 2023-07-31
+reviewers:
+  - "@trasc"
+approvers:
+  - "@ahg-g"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.5"
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+# No associated feature gates. The feature can be disabled through the Configuration API.
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  If there aren't any metrics, we can remove this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T18:08:45Z

- **keps/976-plain-pods/README.md:163** by [@kannon92](https://github.com/kannon92) — 2023-08-11T18:08:45Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  So reading this I do wonder why would we not suggest something like JobSet?
  
  There are a lot of downsides to using naked Pods and I'm not sure you call it out that this should be only used in the case that you can't migrate to a CRD. 
  
  I think items like self-healing, retries, state handling are really difficult problems to solve with a pod only API.  I understand that you feel the same way about this but I'm sure there are reasons to not migrate.  
  
  Maybe this is sorta an implementation detail for documentaiton but I do worry that this should be more of bandaid solution rather than a permanent solution.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:12:11Z

- **keps/976-plain-pods/kep.yaml:31** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:12:11Z
```diff
@@ -0,0 +1,31 @@
+title: Plain Pods
+kep-number: 976
+authors:
+  - "@alculquicondor"
+status: provisional
+creation-date: 2023-07-31
+reviewers:
+  - "@trasc"
+approvers:
+  - "@ahg-g"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: beta
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  beta: "v0.5"
+
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+# No associated feature gates. The feature can be disabled through the Configuration API.
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - my_feature_metric
```

  I was leaving for after we resolve all the open questions, but added now.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:13:40Z

- **keps/976-plain-pods/README.md:163** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:13:40Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  I fully agree with you! In an ideal world, users would be using Jobs and JobSets. But the motivation for this proposal is to meet users where they are today to ease their transition to Kueue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:14:16Z

- **keps/976-plain-pods/README.md:311** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:14:16Z
```diff
@@ -0,0 +1,644 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Simplifying the Workload object](#simplifying-the-workload-object)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes](#groups-of-pods-with-multiple-shapes)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- the webhook should not add a scheduling gate
+- the pod reconciler should not create a corresponding Workload object.
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Simplifying the Workload object
+
+When a Workload just represents a single Pod, it's wasteful to duplicate the pod spec on the
+Workload.
+Instead, the Workload podset can refer back to the Pod itself.
+
+Note that, for other objects, such as Job, the spec in the Workload serves as a snapshot of the
+original Job, so that it is possible to modify a Job during admission (to inject affinities) and
+revert the change on preemption. This is not a concern for Pods, because Pods can't be suspended.
+They terminate as Failed if preempted. As a result, there is no need for a snapshot of the
+original spec.
+
+The Workload's PodSet will look as follows:
+
+```golang
+type PodSet struct {
+  Name string
+  Count int32
+  Template *corev1.PodTemplateSpec
```

  Fixed all references. Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:19:11Z

- **keps/976-plain-pods/README.md:163** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:19:11Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  In the real world, migrating existing systems using naked pods to the system using JobSet or Job is too hard since there are cases of the system depending on other systems or other systems depending on the system.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:25:20Z

- **keps/976-plain-pods/README.md:413** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:25:20Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+In some scenarios, users might have custom job objects that own Pods through an indirect object.
+In these cases, it might be simpler to identify the pods through a label selector.
+
+2. Pods belonging to specific namespaces (such as kube-system or kueue-system).
+
+The namespaces and pod selectors are defined in Configuration.Integrations.
+For a Pod to qualify for queueing by Kueue, it needs to satisfy both the namespace and pod selector.
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+  PodSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system, kueue-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that the controller needs to manually delete the Workload object once it has the
+Finished condition (after we have determined that all pods have finished).
+
+<<[UNRESOLVED TTL after finished]>>
+A possible extension here is to add a TTL, but we will get user feedback first.
+<<[/UNRESOLVED]>>
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    template:
+      spec:
+        containers:
+        - name: job
+          image: hello-world
+          resources:
+            requests:
+              cpu: 1m
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
```

  Can we remove this? I think the `Users create a Workload object beforehand` section covers here.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T18:32:05Z

- **keps/976-plain-pods/README.md:163** by [@kannon92](https://github.com/kannon92) — 2023-08-11T18:32:04Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  Yea.  I say this as someone who works at an organization that uses naked pods.   There are a lot of things we are missing out on and we are getting requests for things like automatic retries, failure/success policies, and gang scheduling.  I have been trying to convince my org to adopt the Job API but I have been unsuccessful mostly for legacy reasons.  I just worry that this will continue to make the sell hard if we have an easy way to add queueing/essentially a PodSet API.  
  
  I just worry that if we aren't strong enough in wording that we would just end up prolonging the transition to Jobs/Jobsets since we allow for the use of pods.  I guess if people are using a controller to manage those pods that isn't a bad thing.  But supporting pods without any owner is a code smell and I just wonder if we should maybe mention that in this KEP?
  
  Anyway I know you feel the same way so I want press too much.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T18:32:58Z

- **keps/976-plain-pods/README.md:163** by [@kannon92](https://github.com/kannon92) — 2023-08-11T18:32:58Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  That being said, things like preemption and automatic retires wouldn't be here with naked pods so maybe we hope that would be sufficient to get people to migrate eventually.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:44:08Z

- **keps/976-plain-pods/README.md:75** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:44:08Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
```

  ```suggestion
  - Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:46:48Z

- **keps/976-plain-pods/README.md:413** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:46:47Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
+
+Not all Pods in a cluster should be subject to queueing.
+In particular the following pods should be excluded from getting the scheduling gate or label.
+
+1. Pods owned by other job APIs managed by kueue.
+
+They can be identified by the ownerReference, based on the list of enabled integrations.
+
+In some scenarios, users might have custom job objects that own Pods through an indirect object.
+In these cases, it might be simpler to identify the pods through a label selector.
+
+2. Pods belonging to specific namespaces (such as kube-system or kueue-system).
+
+The namespaces and pod selectors are defined in Configuration.Integrations.
+For a Pod to qualify for queueing by Kueue, it needs to satisfy both the namespace and pod selector.
+
+```golang
+type Integrations struct {
+  Frameworks []string
+  PodOptions *PodIntegrationPolicy
+}
+
+type PodIntegrationOptions struct {
+  NamespaceSelector *metav1.LabelSelector
+  PodSelector *metav1.LabelSelector
+}
+```
+
+When empty, Kueue uses the following NamespaceSelector internally:
+
+```yaml
+matchExpressions:
+- key: kubernetes.io/metadata.name
+  operator: NotIn
+  values: [kube-system, kueue-system]
+```
+
+### Constructing Workload objects
+
+Once the webhook has marked Pods subject to queuing with the `kueue.x-k8s.io/managed: true` label,
+the Pod reconciler can create the corresponding Workload object to feed the Kueue admission logic.
+
+Note that the Workload cannot be owned by the Pod. Otherwise any cascade deletion of the Pod would
+delete the Workload object, even before the Pod terminates (if it has a grace period).
+This means that the controller needs to manually delete the Workload object once it has the
+Finished condition (after we have determined that all pods have finished).
+
+<<[UNRESOLVED TTL after finished]>>
+A possible extension here is to add a TTL, but we will get user feedback first.
+<<[/UNRESOLVED]>>
+
+#### Single Pods
+
+The simplest case we want to support is single Pod jobs. These Pods only have the label
+`kueue.x-k8s.io/queue-name`, indicating the local queue where they will be queued.
+
+The Workload for the Pod in [story 1](#story-1) would look as follows:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: pod-foo
+  namespace: pod-namespace
+spec:
+  queueName: queue-name
+  podSets:
+  - count: 1
+    name: main # this name is irrelevant.
+    template:
+      spec:
+        containers:
+        - name: job
+          image: hello-world
+          resources:
+            requests:
+              cpu: 1m
+```
+
+<<[UNRESOLVED creating a Workload beforehand]>>
+Could users create a Workload object before hand for groups of Pods?
+This way, Pods would only need to have a label for the group name and the role. This would
+simplify validation.
+Should we make this a supported mode in addition to pure labels/annotations?
+<<[/UNRESOLVED]>>
```

  Oops, I thought I did already.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:49:16Z

- **keps/976-plain-pods/README.md:163** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:49:16Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  I feel your frustration :(
  We are already limiting the usefulness of the naked pods integration, for example, without partial admission or dynamic reclaiming. And keeping the pod finalizers until all the pods finish. I hope this is enough to keep this integration just as a stepping stone. We certainly would never support retries in Kueue. There is Job for that :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T18:53:02Z

- **keps/976-plain-pods/README.md:163** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T18:53:01Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  Added to Non-goals

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:29:40Z

- **keps/976-plain-pods/README.md:163** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:29:40Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  Sounds good.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:33:19Z

- **keps/976-plain-pods/README.md:332** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:33:19Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
```

  I am afraid I don't quite understand this.  I worry that if you have to explicitly remove items for Kueue it could be disruptive on a highly congested cluster.  
  
  How will a PodSelector address this issue?
  
  I'm envision a k8 cluster with a lot of users with a mix of services/batch.  A user has a namespace and they deploy services and batch jobs in that namespace.  Would it be simple for them to opt in on certain items for Kueue but avoid queueing services?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T13:18:24Z

- **keps/976-plain-pods/README.md:332** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:18:24Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
```

  Ah, I misunderstood your intent.
  
  So far, we are not offering a mechanism for tracking quota usage of pods that were not queued (other than manually creating an admitted Workload). Let me add this to non-goals
  
  I thought you were asking for a mechanism to exclude certain pods from queueing (and kueue overall) that was more granular than namespaces.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-14T13:32:21Z

- **keps/976-plain-pods/README.md:332** by [@kannon92](https://github.com/kannon92) — 2023-08-14T13:32:21Z
```diff
@@ -0,0 +1,669 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
+   https://github.com/kubernetes/kubernetes/blob/99190634ab252604a4496882912ac328542d649d/pkg/scheduler/scheduler.go#L496
+
+## Design Details
+
+### Gating Pod Scheduling
+
+Pods subject to queueing should be prevented from scheduling until Kueue has admitted them in a
+specific flavor.
+
+Kubernetes 1.27 and newer provide the mechanism of [scheduling readiness](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/)
+to prevent kube-scheduler from assigning Nodes to Pods.
+
+A Kueue webhook will inject to [Pods subject to queueing](#pods-subject-to-queueing):
+- A scheduling gate `kueue.x-k8s.io/admission` to prevent the Pod from scheduling.
+- A label `kueue.x-k8s.io/managed: true` so that users can easily identify pods that are/were
+  managed by Kueue.
+- A finalizer `kueue.x-k8s.io/managed` in order to reliably track pod terminations.
+
+A Pod reconciler will be responsible for removing the `kueue.x-k8s.io/admission` gate. If the Pods
+have other gates, they will remain Pending, but would be considered active from Kueue's perspective.
+
+#### Pods subject to queueing
```

  Looks good!

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-21T06:17:06Z

- **keps/976-plain-pods/README.md:227** by [@trasc](https://github.com/trasc) — 2023-08-21T06:15:54Z
```diff
@@ -0,0 +1,692 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
```

  What happens if the number of crated worker pods is not 10? 
  If less, should the workload remain admitted? For how long?
  If more, should the extra pods be "killed" without running? Or should they be "unsuspended" while limiting the "parallelism" (number of unsuspended pods at the same time ) to 10?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-21T12:12:53Z

- **keps/976-plain-pods/README.md:227** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T12:12:53Z
```diff
@@ -0,0 +1,692 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
+
+As a platform developer, I can queue groups of Pods that have multiple shapes.
+In addition to the queue name, I can specify how many shapes to expect for the group and how
+many Pods are expected for the shape.
+
+The pods of a job following a driver-workers paradigm look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "1"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+  annotations:
+    kueue.x-k8s.io/pod-group-roles: "2"
+    kueue.x-k8s.io/pod-group-role-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Story 4
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: driver
+  annotations:
+    kueue.x-k8s.io/pod-group-role-count: "1" # optional
+    # The template in the driver group can be left empty. Kueue will populate it from the Pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
```

  They will be killed, as explained in the tracking section.

### Review by [@samos123](https://github.com/samos123) (commented) — 2023-08-22T17:12:49Z

- **keps/976-plain-pods/README.md:153** by [@samos123](https://github.com/samos123) — 2023-08-22T17:12:49Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  why is this an annotation vs a label? People might accidentally set it as a label too. As a user, having it as another label sounds more natural.

### Review by [@samos123](https://github.com/samos123) (commented) — 2023-08-22T17:13:33Z

- **keps/976-plain-pods/README.md:153** by [@samos123](https://github.com/samos123) — 2023-08-22T17:13:33Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  It would be great to make this optional and deduct the max by waiting for e.g. 5 minutes. Where that 5 minute timer is also adjustable through a label.

### Review by [@samos123](https://github.com/samos123) (commented) — 2023-08-22T17:16:21Z

- **keps/976-plain-pods/README.md:163** by [@samos123](https://github.com/samos123) — 2023-08-22T17:16:21Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  +1 that folks should adopt Jobs but sadly large ML platforms have been built that rely on naked pods. Those ML platforms have very small appetite for change and risk and would rather built a custom controller that does part of what Kueue does than to adopt Kueue with Job.
  
  Thank you for willing to meet users where they are even though it doesn't make sense from a technology pov.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-22T18:28:02Z

- **keps/976-plain-pods/README.md:153** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T18:28:01Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  label values have validation rules. Unfortunately, integers don't pass the validation :(

### Review by [@ahaysx](https://github.com/ahaysx) (commented) — 2023-08-31T23:01:44Z

- **keps/976-plain-pods/README.md:163** by [@ahaysx](https://github.com/ahaysx) — 2023-08-31T23:01:44Z
```diff
@@ -0,0 +1,678 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for partial-admission.
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 3
```

  I think to be fair there are mature external controllers that were created well before Kueue and to address gaps in the Jobs API over the years. Supporting the transition of users of these controllers (which Kueue may not yet even fully cover) is an important investment and I'm glad to see this proposal.

### Review by [@samos123](https://github.com/samos123) (commented) — 2023-08-31T23:14:05Z

- **keps/976-plain-pods/README.md:153** by [@samos123](https://github.com/samos123) — 2023-08-31T23:14:05Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  damn I was expecting some good reason and it turned out to be a core K8s limitation. I never knew about this.
  
  I guess currently the way you put it in the annotation it would pass since you're passing `"10"` instead of `10`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-01T13:47:32Z

- **keps/976-plain-pods/README.md:153** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-01T13:47:32Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  there is a regex validation on the string. It cannot start with a number.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-07T14:21:05Z

- **keps/976-plain-pods/README.md:426** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T14:02:42Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
```

  In the future, we should consider resourceClaim field.

- **keps/976-plain-pods/README.md:438** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T14:09:56Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
```

  Will a sha256 be calculated based on the whole of podSpec or based on only specific fields (the fields that are
  relevant to admission, scheduling and/or autoscaling)?
  Also, will the sha256 be calculated for every reconcile loop? Maybe we should save the sha256 in the annotations.
  WDYT?

- **keps/976-plain-pods/README.md:448** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T14:13:42Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
+
+We can only build the Workload object once we observe the number of Pods defined by the
+`kueue.x-k8s.io/pod-group-total-count` annotation.
+
+If Pods with the same `pod-group-name` have different values for the `pod-group-total-count`
+annotation, or if Kueue observes a different amount of Pods than the count, it will not create a
+Workload object and it will emit an event for the Pod indicating the reason.
```

  Also, workload creation will fail when the number of shapes is greater than 8.
  
  https://github.com/kubernetes-sigs/kueue/blob/1a3bcca2e76eac1a041bbf45446af88dee04407c/apis/kueue/v1beta1/workload_types.go#L31-L35

- **keps/976-plain-pods/README.md:448** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T14:15:52Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
+
+We can only build the Workload object once we observe the number of Pods defined by the
+`kueue.x-k8s.io/pod-group-total-count` annotation.
+
+If Pods with the same `pod-group-name` have different values for the `pod-group-total-count`
+annotation, or if Kueue observes a different amount of Pods than the count, it will not create a
+Workload object and it will emit an event for the Pod indicating the reason.
```

  What happens if Kueue absorbs a different amount of Pods than the count after creating Workload? The Workload will be deleted?

- **keps/976-plain-pods/README.md:485** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T14:20:41Z
```diff
@@ -494,22 +461,32 @@ spec:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: driver
           resources:
             requests:
               cpu: 1m
-  - count: 10
+  - count: 2
     name: worker
     template:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: worker
           resources:
             requests:
               cpu: 1m
+              vendor.com/gpu: 1
 ```
 
+**Caveats:**
+
+If the number of different sha256s obtained from the groups of Pods, Workload creation will fail.
+This generally shouldn't be a problem, unless multiple Pods (that should be considered the same
+from an admission perspective) have different label values or reference different volume claims.
```

  Kueue recognizes multiple pods as the same shape even though pods have different commands and args. 
  Because Kueue doesn't consider commands and args when grouping multiple pods as the same shape.
  Is it right?

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-09-08T16:26:58Z

- **keps/976-plain-pods/README.md:67** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:02:05Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
```

  Can we make this narrower as supporting **label** only? Consist with the  queue name.

- **keps/976-plain-pods/README.md:68** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:02:59Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
```

  ```suggestion
  - Opt-in or opt-out Pods from specific namespaces from queueing.
  ```
  
  Does this mean we can choose pods from specific namespaces? Doesn't this conflict with localQueue, which is namespaced.

- **keps/976-plain-pods/README.md:77** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:17:08Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
```

  The partial admission and elastic scaling are same to me, any difference?

- **keps/976-plain-pods/README.md:126** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:17:21Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
```

  IIRC, resource requests are required when enqueueing, I think this also applies to Pod.

- **keps/976-plain-pods/README.md:209** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:26:02Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
```

  For PS architecture, should the head pod has a label like `kueue.x-k8s.io/pod-group-role: head` ?

- **keps/976-plain-pods/README.md:109** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:33:52Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
```

  What about Pods managed by controllers like deployment?

- **keps/976-plain-pods/README.md:267** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:37:09Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
```

  We support JobSet and RayJob already, any scenario we want to manage the pods directly? Take the ray for example, do you mean the pods running in the raycluster but not created by RayJob?

- **keps/976-plain-pods/README.md:297** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T15:48:49Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
```

  Can you improve this by setting a labelSelector as `kueue.x-k8s.io/manager: kueue` to Pod.

- **keps/976-plain-pods/README.md:216** by [@kerthcet](https://github.com/kerthcet) — 2023-09-08T16:04:29Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
```

  If the driver count is 3, then we'll create two more pods?  Generally it seems a little weird as if we're implementing a controller for this kind of job and increase the complexity a lot. Maybe start with the first two stories?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-08T18:33:05Z

- **keps/976-plain-pods/README.md:153** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:33:05Z
```diff
@@ -0,0 +1,695 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+    - [Story 3](#story-3)
+  - [Story 4](#story-4)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods with the same shape](#groups-of-pods-with-the-same-shape)
+    - [Groups of pods with multiple shapes or roles](#groups-of-pods-with-multiple-shapes-or-roles)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that share the same shape (Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+<<[UNRESOLVED usability of labels and annotations ]>>
+Could the split among labels and annotations cause user mistakes?
+Note that integers cannot be used as label values.
+<<[/UNRESOLVED]>>
+
+The pods of a job with a single spec (similar to Pods in an Indexed Job), look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: pod-index-0
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-count: "10"
```

  I think it might be better to work on elastic jobs, instead of that. For now, I hope that having a single parameter for total number of pods is enough.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-08T18:35:44Z

- **keps/976-plain-pods/README.md:426** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:35:44Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
```

  Let's add it already, even if Kueue doesn't support it yet.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T18:38:06Z

- **keps/976-plain-pods/README.md:426** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T18:38:05Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
```

  SGTM

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-08T19:00:36Z

- **keps/976-plain-pods/README.md:67** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:37:06Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
```

  I agree :)

- **keps/976-plain-pods/README.md:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:38:44Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
```

  This would be a global setting. The idea is that you don't want pods from certain namespaces to be queued, like kube-system. This is explained in the design below.

- **keps/976-plain-pods/README.md:77** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:40:05Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
```

  Very different :)
  
  Partial admission is a static choice: you can only run Y pods, instead of X, for the entirety of the job. An MPI Job for example, can't be easily resized once it started.
  
  In an elastic Job, the number can vary over time.

- **keps/976-plain-pods/README.md:109** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:40:25Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
```

  They would be supported as well. Added a clarification.

- **keps/976-plain-pods/README.md:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:41:25Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
```

  yes. I'm not sure what is your suggestion here.

- **keps/976-plain-pods/README.md:209** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:42:16Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
```

  Kueue would deduce the roles automatically.

- **keps/976-plain-pods/README.md:216** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:43:00Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
```

  Yes, we will start with the first story.
  
  We won't create the Pods. We expect the driver pod or some other controller to create the remaining pods.

- **keps/976-plain-pods/README.md:267** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:46:02Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
```

  We just want to make sure that the Pods that are created within the RayJob (through RayCluster) don't get queued again. It's sufficient to look at the owner reference of the Pod, which will be RayCluster.

- **keps/976-plain-pods/README.md:297** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:47:19Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
```

  I think we could. Let's explore that.
  cc @achernevskii

- **keps/976-plain-pods/README.md:297** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:48:14Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Filter out terminal Pods from informers, as they no longer influence quota usage
```

  Oh, also we can't filter out terminal Pods, because we need to make sure the Workload object is updated based on it.

- **keps/976-plain-pods/README.md:438** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:52:10Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
```

  only the fields relevant to scheduling, so that we can identify "similar" Pods and group them together.
  
  Storing the sha might be necessary, actually, as we will change the node selector on admission. Updated.

- **keps/976-plain-pods/README.md:448** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:55:02Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
+
+We can only build the Workload object once we observe the number of Pods defined by the
+`kueue.x-k8s.io/pod-group-total-count` annotation.
+
+If Pods with the same `pod-group-name` have different values for the `pod-group-total-count`
+annotation, or if Kueue observes a different amount of Pods than the count, it will not create a
+Workload object and it will emit an event for the Pod indicating the reason.
```

  Added.

- **keps/976-plain-pods/README.md:448** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:56:50Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
+
+We can only build the Workload object once we observe the number of Pods defined by the
+`kueue.x-k8s.io/pod-group-total-count` annotation.
+
+If Pods with the same `pod-group-name` have different values for the `pod-group-total-count`
+annotation, or if Kueue observes a different amount of Pods than the count, it will not create a
+Workload object and it will emit an event for the Pod indicating the reason.
```

  The extra pods will be terminated, as expressed in "tracking admitted and finished pods"

- **keps/976-plain-pods/README.md:485** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-08T18:58:02Z
```diff
@@ -494,22 +461,32 @@ spec:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: driver
           resources:
             requests:
               cpu: 1m
-  - count: 10
+  - count: 2
     name: worker
     template:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: worker
           resources:
             requests:
               cpu: 1m
+              vendor.com/gpu: 1
 ```
 
+**Caveats:**
+
+If the number of different sha256s obtained from the groups of Pods, Workload creation will fail.
+This generally shouldn't be a problem, unless multiple Pods (that should be considered the same
+from an admission perspective) have different label values or reference different volume claims.
```

  Yes, it wouldn't consider the commands.
  
  However, consider an index label such as the one given by an IndexedJob. That would cause problems. But users should be encouraged to use Job or JobSet if they face this specific problem.
  
  But we can iterate based on user feedback.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T19:12:13Z

- **keps/976-plain-pods/README.md:448** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T19:12:13Z
```diff
@@ -427,58 +406,46 @@ spec:
               cpu: 1m
 ```
 
-#### Groups of Pods with the same shape
+#### Groups of Pods created beforehand
 
-When multiple pods belong to the same group and have the same shape, we need to know how many pods
-belong to the group.
+When a group of pods have different shapes, we need to group them into buckets of similar specs in
+order to create a Workload object.
 
-These groups of Pods can be identified when they have:
+To fully identify the group of pods, the pods need the following:
 - the label `kueue.x-k8s.io/pod-group-name`, as a unique identifier for the group. This should
   be a valid CRD name.
-- the annotation `kueue.x-k8s.io/pod-group-count`, the number of pods to expect in the group.
-  This number is interpreted as int32.
+- The annotation `kueue.x-k8s.io/pod-group-total-count` to indicate how many pods to expect in
+  the group.
+
+The Pod reconciler would group the pods into similar buckets by only looking at the fields that are
+relevant to admission, scheduling and/or autoscaling.
+This list might need to be updated for Kubernetes versions that add new fields relevant to 
+scheduling. The list of fields to keep are:
+- In `metadata`: `labels` (ignoring labels with the `kueue.x-k8s.io/` prefix)
+- In `spec`:
+  - In `initContainers` and `containers`: `image`, `requests` and `ports`.
+  - `nodeSelector`
+  - `affinity`
+  - `tolerations`
+  - `runtimeClassName`
+  - `priority`
+  - `preemptionPolicy`
+  - `topologySpreadConstraints`
+  - `overhead`
+  - `volumes`
+
+A sha256 of the reamining Pod spec will be used as a name for a Workload podSet. The count for the
+podSet will be the number of Pods that match the same sha256.
+
+We can only build the Workload object once we observe the number of Pods defined by the
+`kueue.x-k8s.io/pod-group-total-count` annotation.
+
+If Pods with the same `pod-group-name` have different values for the `pod-group-total-count`
+annotation, or if Kueue observes a different amount of Pods than the count, it will not create a
+Workload object and it will emit an event for the Pod indicating the reason.
```

  Ah, I see. Thanks for the clarification.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T19:13:34Z

- **keps/976-plain-pods/README.md:485** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T19:13:34Z
```diff
@@ -494,22 +461,32 @@ spec:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: driver
           resources:
             requests:
               cpu: 1m
-  - count: 10
+  - count: 2
     name: worker
     template:
       spec:
         containers:
         - name: job
-          image: hello-world
+          image: worker
           resources:
             requests:
               cpu: 1m
+              vendor.com/gpu: 1
 ```
 
+**Caveats:**
+
+If the number of different sha256s obtained from the groups of Pods, Workload creation will fail.
+This generally shouldn't be a problem, unless multiple Pods (that should be considered the same
+from an admission perspective) have different label values or reference different volume claims.
```

  > But users should be encouraged to use Job or JobSet if they face this specific problem.
  
  Yes, it definitely is right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T19:23:14Z

Others LGTM.

- **keps/976-plain-pods/README.md:298** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T19:22:11Z
```diff
@@ -0,0 +1,675 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label.
+- Opt-in or opt-out Pods from specific namespaces from queueing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods or Pods owned by an object not integrated with
+Kueue. I just add a queue name to the Pods through a label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Apply a selector in the informer to only keep the Pods that have the `kueue.x-k8s.io/managed: true`.
+   The webhook should still affect all Pods.
```

  Should we restrict Namespace and Pods that perform the webhook processes? In my experience, performing webhook processes against anything would be degrading the K8s cluster's performance. Actually, I often face the issue in the large cluster with Gatekeeper.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-09-11T10:52:49Z

- **keps/976-plain-pods/README.md:77** by [@kerthcet](https://github.com/kerthcet) — 2023-09-11T10:52:49Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
```

  Ah yes, I just focused on the point of admission time.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-09-11T10:53:53Z

- **keps/976-plain-pods/README.md:126** by [@kerthcet](https://github.com/kerthcet) — 2023-09-11T10:53:53Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
```

  I mean pod should have requests the same.

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-09-11T11:01:36Z

- **keps/976-plain-pods/README.md:209** by [@kerthcet](https://github.com/kerthcet) — 2023-09-11T11:01:36Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
```

  Why not a pair? 😄

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-09-11T11:11:13Z

- **keps/976-plain-pods/README.md:68** by [@kerthcet](https://github.com/kerthcet) — 2023-09-11T11:11:12Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
```

  So a localQueue can queueing pods from different ns chosen by lables. Will this violate the multi-tenant privacy?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-11T15:12:39Z

- **keps/976-plain-pods/README.md:77** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-11T15:03:40Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
```

  Perhaps. But worth clarifying that this proposal is not addressing any of them.

- **keps/976-plain-pods/README.md:126** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-11T15:04:07Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
```

  This is just one pod.

- **keps/976-plain-pods/README.md:209** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-11T15:05:03Z
```diff
@@ -0,0 +1,672 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label or annotation.
+- Opt-in or opt-out Pods from specific namespaces from queuing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods. I just add a queue name to the Pods through a
+label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
```

  I don't follow.

- **keps/976-plain-pods/README.md:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-11T15:11:36Z
```diff
@@ -0,0 +1,675 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label.
+- Opt-in or opt-out Pods from specific namespaces from queueing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods or Pods owned by an object not integrated with
+Kueue. I just add a queue name to the Pods through a label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Apply a selector in the informer to only keep the Pods that have the `kueue.x-k8s.io/managed: true`.
+   The webhook should still affect all Pods.
```

  Let's put namespaceSelector by default to exclude kueue-system and kube-system. Maybe we can make it configurable through helm, although I'm not sure if it's possible.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-11T18:05:30Z

- **keps/976-plain-pods/README.md:298** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-11T18:05:29Z
```diff
@@ -0,0 +1,675 @@
+# KEP-976: Plain Pods
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Story 3](#story-3)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+    - [Skipping Pods belonging to queued objects](#skipping-pods-belonging-to-queued-objects)
+    - [Pods replaced on failure](#pods-replaced-on-failure)
+  - [Risks and Mitigations](#risks-and-mitigations)
+    - [Increased memory usage](#increased-memory-usage)
+    - [Limited size for annotation values](#limited-size-for-annotation-values)
+- [Design Details](#design-details)
+  - [Gating Pod Scheduling](#gating-pod-scheduling)
+    - [Pods subject to queueing](#pods-subject-to-queueing)
+  - [Constructing Workload objects](#constructing-workload-objects)
+    - [Single Pods](#single-pods)
+    - [Groups of Pods created beforehand](#groups-of-pods-created-beforehand)
+    - [Groups of pods where driver generates workers](#groups-of-pods-where-driver-generates-workers)
+  - [Tracking admitted and finished Pods](#tracking-admitted-and-finished-pods)
+  - [Metrics](#metrics)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+    - [Beta](#beta)
+    - [GA](#ga)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+  - [Users create a Workload object beforehand](#users-create-a-workload-object-beforehand)
+<!-- /toc -->
+
+## Summary
+
+Some batch applications create plain Pods directly, as opposed to managing the Pods through the Job
+API or a CRD that supports [suspend](https://kubernetes.io/docs/concepts/workloads/controllers/job/#suspending-a-job) semantics.
+This KEP proposes mechanisms to queue plain Pods through Kueue, individually or in groups,
+leveraging [pod scheduling gates](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-scheduling-readiness/).
+
+## Motivation
+
+Some batch systems or AI/ML frameworks create plain Pods to represent jobs or tasks of a job.
+Currently, Kueue relies on the Job API or CRDs that support suspend semantics
+to control whether the Pods of a job can exist and can be scheduled to Nodes.
+
+While it is sometimes possible to wrap Pods on a CRD or migrate to the Job API, it could be
+costly for framework or platform developers to do so.
+In some scenarios, the framework doesn't know how many Pods belong to a single job. In more extreme
+cases, Pods are created dynamically once the first Pod starts running. These are sometimes known
+as elastic jobs.
+
+A recent enhancement to Kubernetes Pods, scheduling gates, introduced in 1.26 as Alpha, and 1.27 as
+Beta, allows an external controller to prevent kube-scheduler from scheduling Pods. Kueue can make
+use of this API to implement queuing semantics for Pods.
+
+### Goals
+
+- Support queueing of individual Pods.
+- Support queueing of groups of Pods of fixed size, identified by a common label.
+- Opt-in or opt-out Pods from specific namespaces from queueing.
+
+### Non-Goals
+
+- Support for [dynamic reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+
+  This feature is incompatible with supporting Pod replacements without knowing the behavior of a
+  parent controller for the Pods.
+
+- Support for [partial-admission](https://github.com/kubernetes-sigs/kueue/issues/420).
+
+  Since all pods are already created, an implementation of partial admission would imply the
+  deletion of some pods. It is not clear if this matches users expectations, as opposed to support
+  for elastic groups.
+
+- Support elastic groups of Pods, where the number of Pods changes after the job started.
+
+  While these jobs are one of the motivations for this KEP, the current proposal doesn't support
+  them. These jobs can be addressed in follow up KEPs.
+
+- Support for advanced Pod retry policies
+
+  Kueue shouldn't re-implement core functionalities that are already available in the Job API.
+
+- Tracking usage of Pods that were not queued through Kueue.
+
+## Proposal
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories (Optional)
+
+#### Story 1
+
+As an platform developer, I can queue plain Pods or Pods owned by an object not integrated with
+Kueue. I just add a queue name to the Pods through a label.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: foo
+  namespace: pod-namespace
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+#### Story 2
+
+As a platform developer, I can queue groups of Pods that might or might not have the same shape
+(Pod specs).
+In addition to the queue name, I can specify how many Pods belong to the group.
+
+The pods of a job following a driver-workers paradigm would look like follows:
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: driver
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-0
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "0"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    kueue.x-k8s.io/pod-group-total-count: "3"
+spec:
+  containers:
+  - name: job
+    image: worker
+    args: ["--index", "1"]
+    resources:
+      requests:
+        cpu: 1m
+        vendor.com/gpu: 1
+```
+
+### Story 3
+
+Motivation: In frameworks like Spark, worker Pods are only created after by the driver Pod. As
+such, the worker Pods specs cannot be predicted beforehand. Even though the job could be considered
+elastic, generally users wouldn't want to start a spark driver to run if no workers would fit.
+
+As a Spark user, I can queue the driver Pod while providing the expected shape of the worker Pods.
+
+```yaml
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-driver
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+  annotations:
+    # If the template is left empty, it means that it will match the spec of this pod.
+    kueue.x-k8s.io/pod-group-sets: |-
+      [
+        {
+          name: driver,
+          count: 1,
+        },
+        {
+          name: workers,
+          count: 10,
+          template:
+            spec:
+              containers:
+                - name: worker
+                  requests:
+                    cpu: 1m
+        }
+      ]
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+---
+apiVersion: v1
+kind: Pod
+metadata:
+  name: job-worker-1
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/pod-group-name: pod-group
+    kueue.x-k8s.io/pod-group-role: worker
+spec:
+  containers:
+  - name: job
+    image: hello-world
+    resources:
+      requests:
+        cpu: 1m
+```
+
+### Notes/Constraints/Caveats (Optional)
+
+#### Skipping Pods belonging to queued objects
+
+Pods owned by jobs managed by Kueue should not be subject to extra management.
+These Pods can be identified based on the ownerReference. For these pods:
+- The webhook should not add a scheduling gate.
+- The pod reconciler should not create a corresponding Workload object.
+
+Note that sometimes the Pods might not be directly owned by a known job object. Here are some
+special cases:
+- MPIJob: The launcher Pod is created through a batch/Job, which is also an known to Kueue, so
+  it's not an issue.
+- JobSet: Also creates Jobs, so not problematic.
+- RayJob: Pods are owned by a RayCluster object, which we don't currently support. This could be
+  hardcoded a known parent, or we could use label selectors for:
+  ```yaml
+  app.kubernetes.io/created-by: kuberay-operator
+  app.kubernetes.io/name: kuberay
+  ```
+
+#### Pods replaced on failure
+
+It is possible that users of plain Pods have a controller for them to handle failures and
+re-creations. These Pods should be able to use the quota that was already assigned to the Workload.
+
+Because Kueue can't know if Pods will be recreated or not, it will hold the entirety of the
+quota until it can determine that the whole Workload finished (all pods are terminated).
+In other words, Kueue won't support [dynamically reclaiming quota](https://github.com/kubernetes-sigs/kueue/issues/78)
+for plain Pods.
+
+### Risks and Mitigations
+
+#### Increased memory usage
+
+In order to support plain Pods, we need to start watching all Pods, even if they are not supposed
+to be managed by Kueue. This will increase the memory usage of Kueue just to maintain the
+informers.
+
+We can use the following mitigations:
+
+1. Drop the unused managedFields field from the Pod spec, like kube-scheduler is doing
+   https://github.com/kubernetes/kubernetes/pull/119556
+2. Apply a selector in the informer to only keep the Pods that have the `kueue.x-k8s.io/managed: true`.
+   The webhook should still affect all Pods.
```

  It makes sense. Thanks for the updates.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-11T18:06:09Z

LGTM
leaving `/lgtm` on @kerthcet
