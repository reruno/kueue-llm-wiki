# PR #10313: Move auxiliary outputs to artifacts and release bundles to release-artifacts

**Summary**: Move auxiliary outputs to artifacts and release bundles to release-artifacts

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10313

**Last updated**: 2026-04-16T15:47:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@reruno](https://github.com/reruno)
- **Branch**: `cleanup/10256` → `main`
- **Created**: 2026-04-07T09:59:27Z
- **Updated**: 2026-04-16T15:47:58Z
- **Merged**: 2026-04-08T13:35:34Z
- **Closed**: 2026-04-08T13:35:34Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mszadkow](https://github.com/mszadkow), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl), [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 12   **+56 / -37**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Put auxiliary files (xml reports, logs) into dedicated artifacts folder, and release artifacts into release-artifacts folder

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10256 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-07T09:59:35Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 315bfa0cb896c85be0a7860843fb3d6f19227bea |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69d643c81586df0008d52cb9 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-07T09:59:39Z

Hi @reruno. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-07T10:44:00Z

/ok-to-test

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-08T10:53:28Z

overall looks very good, I am ready for lgtm as you address last comment: https://github.com/kubernetes-sigs/kueue/pull/10313#discussion_r3050839956

### Comment by [@reruno](https://github.com/reruno) — 2026-04-08T12:51:47Z

/retest

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-08T13:26:59Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-08T13:27:08Z

LGTM label has been added.  <details>Git tree hash: ec4fb9b79373f958cdde50f4b1ba881ad6ead11d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-08T13:34:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10313#pullrequestreview-4075477311" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10313#" title="Author self-approved">reruno</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T15:39:08Z

/cherrypick website

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T15:39:36Z

> /cherrypick website

This change caused https://github.com/kubernetes-sigs/kueue/pull/10530#issuecomment-4261298722

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-16T15:40:01Z

@tenzen-y: #10313 failed to apply on top of branch "website":
```
Applying: Move auxiliary outputs to artifacts and release bundles to release-artifacts
.git/rebase-apply/patch:225: trailing whitespace.
		.flat |  [] + . ' artifacts/run-performance-scheduler/summary.yaml >> "$SUMMARY_FILE" 
warning: 1 line adds whitespace errors.
Using index info to reconstruct a base tree...
M	Makefile
M	Makefile-test.mk
M	test/util/constants.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
CONFLICT (content): Merge conflict in test/util/util.go
Auto-merging test/util/constants.go
CONFLICT (content): Merge conflict in test/util/constants.go
Auto-merging Makefile-test.mk
Auto-merging Makefile
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Move auxiliary outputs to artifacts and release bundles to release-artifacts

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10313#issuecomment-4261367088):

>/cherrypick website


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T15:47:20Z


/cherrypick website

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-16T15:47:58Z

@tenzen-y: new pull request created: #10534

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10313#issuecomment-4261424704):

>
>/cherrypick website
>
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-07T10:55:58Z

- **test/util/constants.go:61** by [@mszadkow](https://github.com/mszadkow) — 2026-04-07T10:55:59Z
```diff
@@ -58,7 +58,7 @@ var (
 
 var (
 	ProjectBaseDir           = getProjectBaseDir()
-	BinDir                   = filepath.Join(ProjectBaseDir, "bin")
+	BuildReportsDir          = filepath.Join(ProjectBaseDir, "build-reports")
```

  Overall we don't need BinDir for anything else?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-07T10:56:55Z

- **Makefile:56** by [@mszadkow](https://github.com/mszadkow) — 2026-04-07T10:56:55Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  `ARTIFACTS` might appear unused when run locally, but please double check what happens in CI

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-07T10:58:44Z

- **test/util/constants.go:61** by [@reruno](https://github.com/reruno) — 2026-04-07T10:58:43Z
```diff
@@ -58,7 +58,7 @@ var (
 
 var (
 	ProjectBaseDir           = getProjectBaseDir()
-	BinDir                   = filepath.Join(ProjectBaseDir, "bin")
+	BuildReportsDir          = filepath.Join(ProjectBaseDir, "build-reports")
```

  No, we only used BinDir to save logs into the log directory, since logs directory has changed to build-reports, BinDir is not needed

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-07T11:02:53Z

- **Makefile:56** by [@reruno](https://github.com/reruno) — 2026-04-07T11:02:53Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  In CI CD kubernetes test infra populates `ARTIFACTS` variable, so test infra defines where logs should be saved 
  
  https://github.com/kubernetes/test-infra/blob/c968bbd9c0629f1f7d6d36423c959057c9fbc2f1/config/jobs/kubernetes-sigs/kueue/kueue-presubmits-main.yaml#L33
  https://github.com/kubernetes-sigs/prow/blob/main/pkg/pod-utils/decorate/podspec.go#L51

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T11:04:02Z

- **Makefile:56** by [@mimowo](https://github.com/mimowo) — 2026-04-07T11:04:01Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  Should we rather have 2 variables then:
  ARTIFACTS = "artifacts/" points to the folder where all release artifacts are stored
  BUILD_REPORTS = "build-reports/" points to where the build reports are stored

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-07T11:04:03Z

- **Makefile:56** by [@reruno](https://github.com/reruno) — 2026-04-07T11:04:03Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  In the CI CD pipeline output we see that prow parsed correctly our junit yaml files 
  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10313/pull-kueue-test-integration-multikueue-main/2041467043387019264

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T11:09:32Z

- **Makefile:56** by [@mimowo](https://github.com/mimowo) — 2026-04-07T11:09:31Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  Another alternative approach would be to have:
  RELEASE_ARTIFACTS = "release-artifacts/" points to the folder where all release artifacts are stored (instead of ARTIFACTS)
  ARTIFACTS = "artifacts/" points to where the build reports are stored (instead of BUILD_REPORTS)
  
  Maybe the idea of "artifacts" is more consistent with prow? cc @mbobrovskyi

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-07T11:13:56Z

- **Makefile:56** by [@reruno](https://github.com/reruno) — 2026-04-07T11:13:56Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  > Should we rather have 2 variables then: ARTIFACTS = "artifacts/" points to the folder where all release artifacts are stored BUILD_REPORTS = "build-reports/" points to where the build reports are stored
  
  I think `ARTIFACTS` is hard coded variable that is populated by kubernetes/test-infra pipeline, we should take that into account. https://github.com/kubernetes-sigs/prow/blob/main/pkg/pod-utils/decorate/podspec.go#L51

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-07T11:17:04Z

- **Makefile:56** by [@reruno](https://github.com/reruno) — 2026-04-07T11:17:04Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  > Another alternative approach would be to have: RELEASE_ARTIFACTS = "release-artifacts/" points to the folder where all release artifacts are stored (instead of ARTIFACTS) ARTIFACTS = "artifacts/" points to where the build reports are stored (instead of BUILD_REPORTS)
  > 
  > Maybe the idea of "artifacts" is more consistent with prow? cc @mbobrovskyi
  
  Right now release artifacts are saved in hard coded `artifacts/` folder. We can add two different variable RELEASE_ARTIFACTS (for release files) and ARTIFACTS (report logs, junit reports etc etc)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-07T11:18:06Z

- **Makefile:56** by [@mimowo](https://github.com/mimowo) — 2026-04-07T11:18:06Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  I see, so we can keep use the artifacts folder both for CI and local builds. 
  Just to make it less confusing use `release-artifacts` folder for the release process, and update the release steps in https://github.com/kubernetes-sigs/kueue/blob/main/.github/ISSUE_TEMPLATE/NEW_RELEASE.md
  In that case I would also rename the artifacts Makefile goal to release-artifacts

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-07T11:18:34Z

- **Makefile:56** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-07T11:18:33Z
```diff
@@ -53,7 +53,7 @@ CLUSTERPROFILE_PLUGIN_IMAGE_VERSION ?= 0.0.1
 
 PROJECT_DIR := $(shell dirname $(abspath $(lastword $(MAKEFILE_LIST))))
 BIN_DIR ?= $(PROJECT_DIR)/bin
-ARTIFACTS ?= $(BIN_DIR)
+ARTIFACTS ?= $(PROJECT_DIR)/build-reports
```

  Yes, we should use the ARTIFACTS variable, as it is hardcoded in Prow.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T08:57:34Z

- **Makefile:319** by [@mimowo](https://github.com/mimowo) — 2026-04-08T08:57:34Z
```diff
@@ -316,18 +317,19 @@ prepare-manifests:
 	cd config/components/kueueviz && $(KUSTOMIZE) edit set image frontend=$(IMAGE_TAG_KUEUEVIZ_FRONTEND)
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
-.PHONY: artifacts
```

  I'm wondering if we could keep the `artifacts` goal still which would allow to build Kueue locally for non-release purposes? 
  
  Then the `release-artifacts` goal could call `artifacts` just using different target folder.
  
  This way we are fully backwards compatible, and we have a clear path for not mixing `artifacts` and `release-artifacts`. 
  
  Additionally the `release-artifacts` make goal could add validation for the GIT_TAG, but this is out-of-scope for the PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T08:57:49Z

- **Makefile:319** by [@mimowo](https://github.com/mimowo) — 2026-04-08T08:57:49Z
```diff
@@ -316,18 +317,19 @@ prepare-manifests:
 	cd config/components/kueueviz && $(KUSTOMIZE) edit set image frontend=$(IMAGE_TAG_KUEUEVIZ_FRONTEND)
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
-.PHONY: artifacts
```

  cc @mbobrovskyi @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T09:00:03Z

LGTM overall, I left an additional comment to preserve the "artifacts" goal which could be still used for local non-release builds and store the yamls and everything in "artifacts" folder as until now. 
It would also be great to have a pass from @mbobrovskyi 
cc @tenzen-y @gabesaba as this slightly affects the release process, but I think in a good way to have the clear separation of "work artifacts" and "release artficats"

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-08T10:52:20Z

- **Makefile:338** by [@mszadkow](https://github.com/mszadkow) — 2026-04-08T10:52:20Z
```diff
@@ -316,18 +321,23 @@ prepare-manifests:
 	cd config/components/kueueviz && $(KUSTOMIZE) edit set image frontend=$(IMAGE_TAG_KUEUEVIZ_FRONTEND)
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
+
 .PHONY: artifacts
-artifacts: DEST_CHART_DIR="artifacts"
-artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate release artifacts.
-	$(KUSTOMIZE) build config/default -o artifacts/manifests.yaml
-	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
-	$(KUSTOMIZE) build config/alpha-enabled -o artifacts/manifests-alpha-enabled.yaml
-	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
-	$(KUSTOMIZE) build config/visibility-apf -o artifacts/visibility-apf.yaml
-	$(KUSTOMIZE) build config/kueueviz -o artifacts/kueueviz.yaml
-	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o artifacts/kueue-populator.yaml
+artifacts: DEST_CHART_DIR="$(ARTIFACTS)"
+artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate local artifacts.
+	$(KUSTOMIZE) build config/default -o $(ARTIFACTS)/manifests.yaml
+	$(KUSTOMIZE) build config/dev -o $(ARTIFACTS)/manifests-dev.yaml
+	$(KUSTOMIZE) build config/alpha-enabled -o $(ARTIFACTS)/manifests-alpha-enabled.yaml
+	$(KUSTOMIZE) build config/prometheus -o $(ARTIFACTS)/prometheus.yaml
+	$(KUSTOMIZE) build config/visibility-apf -o $(ARTIFACTS)/visibility-apf.yaml
+	$(KUSTOMIZE) build config/kueueviz -o $(ARTIFACTS)/kueueviz.yaml
+	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o $(ARTIFACTS)/kueue-populator.yaml
 	@$(call clean-manifests)
-	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="artifacts" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="$(ARTIFACTS)" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+
+.PHONY: release-artifacts
+release-artifacts: ## Generate release artifacts.
```

  should we clear them if exists as we do for artifacts?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-08T10:52:42Z

- **Makefile-test.mk:470** by [@mszadkow](https://github.com/mszadkow) — 2026-04-08T10:52:42Z
```diff
@@ -464,8 +467,8 @@ run-tas-performance-scheduler: envtest performance-scheduler-runner minimalkueue
 .PHONY: test-tas-performance-scheduler-once
 test-tas-performance-scheduler-once: gotestsum run-tas-performance-scheduler
 	$(GOTESTSUM) --junitfile $(ARTIFACTS)/junit.xml -- $(GO_TEST_FLAGS) ./test/performance/scheduler/checker  \
-		--summary=$(ARTIFACTS)/summary.yaml \
-		--cmdStats=$(ARTIFACTS)/minimalkueue.stats.yaml \
+		--summary=$(ARTIFACTS)/run-tas-performance-scheduler/summary.yaml \
```

  good catch

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T10:55:31Z

- **Makefile:338** by [@mimowo](https://github.com/mimowo) — 2026-04-08T10:55:31Z
```diff
@@ -316,18 +321,23 @@ prepare-manifests:
 	cd config/components/kueueviz && $(KUSTOMIZE) edit set image frontend=$(IMAGE_TAG_KUEUEVIZ_FRONTEND)
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
+
 .PHONY: artifacts
-artifacts: DEST_CHART_DIR="artifacts"
-artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate release artifacts.
-	$(KUSTOMIZE) build config/default -o artifacts/manifests.yaml
-	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
-	$(KUSTOMIZE) build config/alpha-enabled -o artifacts/manifests-alpha-enabled.yaml
-	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
-	$(KUSTOMIZE) build config/visibility-apf -o artifacts/visibility-apf.yaml
-	$(KUSTOMIZE) build config/kueueviz -o artifacts/kueueviz.yaml
-	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o artifacts/kueue-populator.yaml
+artifacts: DEST_CHART_DIR="$(ARTIFACTS)"
+artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate local artifacts.
+	$(KUSTOMIZE) build config/default -o $(ARTIFACTS)/manifests.yaml
+	$(KUSTOMIZE) build config/dev -o $(ARTIFACTS)/manifests-dev.yaml
+	$(KUSTOMIZE) build config/alpha-enabled -o $(ARTIFACTS)/manifests-alpha-enabled.yaml
+	$(KUSTOMIZE) build config/prometheus -o $(ARTIFACTS)/prometheus.yaml
+	$(KUSTOMIZE) build config/visibility-apf -o $(ARTIFACTS)/visibility-apf.yaml
+	$(KUSTOMIZE) build config/kueueviz -o $(ARTIFACTS)/kueueviz.yaml
+	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o $(ARTIFACTS)/kueue-populator.yaml
 	@$(call clean-manifests)
-	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="artifacts" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="$(ARTIFACTS)" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+
+.PHONY: release-artifacts
+release-artifacts: ## Generate release artifacts.
```

  yesh I always clear thr folder manually during release porcess. Making this automated sgtm

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-08T10:56:13Z

- **Makefile:338** by [@reruno](https://github.com/reruno) — 2026-04-08T10:56:13Z
```diff
@@ -316,18 +321,23 @@ prepare-manifests:
 	cd config/components/kueueviz && $(KUSTOMIZE) edit set image frontend=$(IMAGE_TAG_KUEUEVIZ_FRONTEND)
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
+
 .PHONY: artifacts
-artifacts: DEST_CHART_DIR="artifacts"
-artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate release artifacts.
-	$(KUSTOMIZE) build config/default -o artifacts/manifests.yaml
-	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
-	$(KUSTOMIZE) build config/alpha-enabled -o artifacts/manifests-alpha-enabled.yaml
-	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
-	$(KUSTOMIZE) build config/visibility-apf -o artifacts/visibility-apf.yaml
-	$(KUSTOMIZE) build config/kueueviz -o artifacts/kueueviz.yaml
-	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o artifacts/kueue-populator.yaml
+artifacts: DEST_CHART_DIR="$(ARTIFACTS)"
+artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate local artifacts.
+	$(KUSTOMIZE) build config/default -o $(ARTIFACTS)/manifests.yaml
+	$(KUSTOMIZE) build config/dev -o $(ARTIFACTS)/manifests-dev.yaml
+	$(KUSTOMIZE) build config/alpha-enabled -o $(ARTIFACTS)/manifests-alpha-enabled.yaml
+	$(KUSTOMIZE) build config/prometheus -o $(ARTIFACTS)/prometheus.yaml
+	$(KUSTOMIZE) build config/visibility-apf -o $(ARTIFACTS)/visibility-apf.yaml
+	$(KUSTOMIZE) build config/kueueviz -o $(ARTIFACTS)/kueueviz.yaml
+	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o $(ARTIFACTS)/kueue-populator.yaml
 	@$(call clean-manifests)
-	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="artifacts" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="$(ARTIFACTS)" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+
+.PHONY: release-artifacts
+release-artifacts: ## Generate release artifacts.
```

  We already clear release artifacts as well because artifacts target clears them, we reuse artifacts target with changed artifact directory to `release-artifacts`
  ```
  artifacts: **clean-artifacts** kustomize helm-chart-package prepare-manifests 
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T11:13:49Z

- **hack/testing/e2e-kueueviz-backend.sh:24** by [@mimowo](https://github.com/mimowo) — 2026-04-08T11:13:49Z
```diff
@@ -20,6 +20,9 @@ SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
 ROOT_DIR="$SOURCE_DIR/../.."
 # shellcheck source=hack/testing/e2e-common.sh
 source "${SOURCE_DIR}/e2e-common.sh"
+
+mkdir -p "${ARTIFACTS}" "${CYPRESS_SCREENSHOTS_FOLDER}" "${CYPRESS_VIDEOS_FOLDER}"
```

  What does CYPRESS_SCREENSHOTS_FOLDER resolve to, and why we need to create it now, but not before?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-08T11:17:34Z

- **hack/testing/e2e-kueueviz-backend.sh:24** by [@reruno](https://github.com/reruno) — 2026-04-08T11:17:34Z
```diff
@@ -20,6 +20,9 @@ SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
 ROOT_DIR="$SOURCE_DIR/../.."
 # shellcheck source=hack/testing/e2e-common.sh
 source "${SOURCE_DIR}/e2e-common.sh"
+
+mkdir -p "${ARTIFACTS}" "${CYPRESS_SCREENSHOTS_FOLDER}" "${CYPRESS_VIDEOS_FOLDER}"
```

  I couldn't find where we create these folders, I created explicit mkdir to make sure that we have folder structure that we need
  ```
  CYPRESS_SCREENSHOTS_FOLDER=$(ARTIFACTS)/cypress/screenshots 
  CYPRESS_VIDEOS_FOLDER=$(ARTIFACTS)/cypress/videos
  ```
  for test-e2e-kueueviz-local and test-e2e-kueueviz makefile targets

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T11:27:15Z

- **hack/testing/e2e-kueueviz-backend.sh:24** by [@mimowo](https://github.com/mimowo) — 2026-04-08T11:27:15Z
```diff
@@ -20,6 +20,9 @@ SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
 ROOT_DIR="$SOURCE_DIR/../.."
 # shellcheck source=hack/testing/e2e-common.sh
 source "${SOURCE_DIR}/e2e-common.sh"
+
+mkdir -p "${ARTIFACTS}" "${CYPRESS_SCREENSHOTS_FOLDER}" "${CYPRESS_VIDEOS_FOLDER}"
```

  got it, in that case they are probably created by Cypress itself. Would it work if you just create the ARTIFACTS? If it worked before, then probably it will also work now too, wdyt?

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-08T12:01:10Z

- **hack/testing/e2e-kueueviz-backend.sh:24** by [@reruno](https://github.com/reruno) — 2026-04-08T12:01:09Z
```diff
@@ -20,6 +20,9 @@ SOURCE_DIR="$(cd "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
 ROOT_DIR="$SOURCE_DIR/../.."
 # shellcheck source=hack/testing/e2e-common.sh
 source "${SOURCE_DIR}/e2e-common.sh"
+
+mkdir -p "${ARTIFACTS}" "${CYPRESS_SCREENSHOTS_FOLDER}" "${CYPRESS_VIDEOS_FOLDER}"
```

  After some investigation I found that test-e2e-kueueviz-local called setup-e2e-env that created `bin/` directory, since we changed the output folder to `artifacts/`, we must ensure that at least `artifacts/` is created, I will simplify it to this 
  ```
  mkdir -p "${ARTIFACTS}"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-08T13:33:56Z

Thank you 👍 
/lgtm
/approve

Let me try to cherrypick to align the artifacts management,
/cherrypick release-0.17
/cherrypick release-0.16

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-09T04:38:16Z

- **Makefile:337** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-09T04:38:16Z
```diff
@@ -317,17 +322,21 @@ prepare-manifests:
 	cd cmd/experimental/kueue-populator/config && $(KUSTOMIZE) edit set image controller=$(IMAGE_TAG_KUEUE_POPULATOR)
 
 .PHONY: artifacts
-artifacts: DEST_CHART_DIR="artifacts"
-artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate release artifacts.
-	$(KUSTOMIZE) build config/default -o artifacts/manifests.yaml
-	$(KUSTOMIZE) build config/dev -o artifacts/manifests-dev.yaml
-	$(KUSTOMIZE) build config/alpha-enabled -o artifacts/manifests-alpha-enabled.yaml
-	$(KUSTOMIZE) build config/prometheus -o artifacts/prometheus.yaml
-	$(KUSTOMIZE) build config/visibility-apf -o artifacts/visibility-apf.yaml
-	$(KUSTOMIZE) build config/kueueviz -o artifacts/kueueviz.yaml
-	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o artifacts/kueue-populator.yaml
+artifacts: DEST_CHART_DIR="$(ARTIFACTS)"
+artifacts: clean-artifacts kustomize helm-chart-package prepare-manifests ## Generate local artifacts.
+	$(KUSTOMIZE) build config/default -o $(ARTIFACTS)/manifests.yaml
+	$(KUSTOMIZE) build config/dev -o $(ARTIFACTS)/manifests-dev.yaml
+	$(KUSTOMIZE) build config/alpha-enabled -o $(ARTIFACTS)/manifests-alpha-enabled.yaml
+	$(KUSTOMIZE) build config/prometheus -o $(ARTIFACTS)/prometheus.yaml
+	$(KUSTOMIZE) build config/visibility-apf -o $(ARTIFACTS)/visibility-apf.yaml
+	$(KUSTOMIZE) build config/kueueviz -o $(ARTIFACTS)/kueueviz.yaml
+	$(KUSTOMIZE) build cmd/experimental/kueue-populator/config -o $(ARTIFACTS)/kueue-populator.yaml
 	@$(call clean-manifests)
-	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_DIR="artifacts" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+	CGO_ENABLED=$(CGO_ENABLED) GO_CMD="$(GO_CMD)" LD_FLAGS="$(LD_FLAGS)" BUILD_PATH="$(ARTIFACTS)" BUILD_NAME=kubectl-kueue PLATFORMS="$(CLI_PLATFORMS)" ./hack/multiplatform-build.sh ./cmd/kueuectl/main.go
+
+.PHONY: release-artifacts
```

  I’m not sure I understand why we have two targets: artifacts and release-artifacts.
