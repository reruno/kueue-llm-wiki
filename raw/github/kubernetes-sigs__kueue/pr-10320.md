# PR #10320: Implement support for partitionable devices

**Summary**: Implement support for partitionable devices

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10320

**Last updated**: 2026-05-29T12:34:38Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `implement-partitionable-devices` → `main`
- **Created**: 2026-04-08T05:09:30Z
- **Updated**: 2026-05-29T12:34:38Z
- **Merged**: 2026-05-28T13:40:52Z
- **Closed**: 2026-05-28T13:40:52Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@olekzabl](https://github.com/olekzabl), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 24   **+2030 / -40**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes # https://github.com/kubernetes-sigs/kueue/issues/10108

#### Special notes for your reviewer:


#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
DRA: Add counter-based quota support for partitionable DRA devices (e.g., NVIDIA MIG) via counterMappings configuration.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-08T05:09:42Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-08T05:36:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 70010cd987f227e4f63dc7b3aa1816d050cb5620 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a17687af2607500094c1dcb |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-08T08:52:21Z

/ok-to-test

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-09T17:08:27Z

/assign

@PannagaRao has mentioned to me that this is ready for review so going to put this on my plate this week.

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:35:47Z

> This PR has a dependency on https://github.com/kubernetes-sigs/kueue/pull/9742

CEL work has merged so this is ready to go. I guess we still need the KEP merged though.

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-12T19:41:52Z

I verified the current implementation on Nvidia A100 GPU with MIGenabled

Configuration:                                                                                                                                                                                                                                                                                                                                                                            
```
  resources:                                                                                                                                                                                                                                                                                                                                                                                
    deviceClassMappings:                                                                                                                                                                                                                                                                                                                                                                    
    - name: gpu.memory                                                                                                                                                                                                                                                                                                                                                                      
      deviceClassNames:                                                                                                                                                                                                                                                                                                                                                                     
      - gpu.nvidia.com                                                                                                                                                                                                                                                                                                                                                                      
      - mig.nvidia.com                                                                                                                                                                                                                                                                                                                                                                      
      counters:                                                                                                                                                                                                                                                                                                                                                                             
      - driverName: gpu.nvidia.com                                                                                                                                                                                                                                                                                                                                                          
        counterName: memory                                                                                                                                                                                                                                                                                                                                                                 
        defaultConsumption: "40Gi"
```
        
  
Mixed workload — whole GPU + MIG in one pod:                                                                                                                                                                                                                                                                                                                                              
                                                                                                                                                                                                                                                                                                                                                                                            
  RCT 1 (whole GPU, no CEL):                                                                                                                                                                                                                                                                                                                                                                
```
  deviceClassName: gpu.nvidia.com                                                                                                                                                                                                                                                                                                                                                           
  count: 1  
```                                                                                                                                                                                                                                                                                                                                                                                
                                                                                                                                                                                                                                                                                                                                                                                            
  RCT 2 (MIG 1g.5gb, with CEL):
```
  deviceClassName: mig.nvidia.com                                                                                                                                                                                                                                                                                                                                                           
  count: 1                                                                                                                                                                                                                                                                                                                                                                                
  selectors:                                                                                                                                                                                                                                                                                                                                                                                
  - cel:          
      expression: "device.attributes['gpu.nvidia.com'].profile == '1g.5gb'" 
```                                                                                                                                                                                                                                                                                                                
                                                                           
  Pod spec:                                                                                                                                                                                                                                                                                                                                                                                 
```
  resourceClaims:                                                                                                                                                                                                                                                                                                                                                                           
  - name: whole                                                                                                                                                                                                                                                                                                                                                                             
    resourceClaimTemplateName: whole-gpu-rct                                                                                                                                                                                                                                                                                                                                                
  - name: mig                               
    resourceClaimTemplateName: mig-rct
```
    
 
ClusterQueue quota reservation:                                                                                                                                                                                                                                                                                                                                                           
```
  flavorsReservation:                                                                                                                                                                                                                                                                                                                                                                       
  - name: gpu-flavor                                                                                                                                                                                                                                                                                                                                                                        
    resources:                                                                                                                                                                                                                                                                                                                                                                              
    - name: cpu                                                                                                                                                                                                                                                                                                                                                                             
      total: 100m                                                                                                                                                                                                                                                                                                                                                                           
    - name: gpu.memory                                                                                                                                                                                                                                                                                                                                                                      
      total: "48049946624"   # 40Gi (whole GPU) + 4864Mi (MIG 1g.5gb) = 44864Mi                                                                                                                                                                                                                                                                                                           
    - name: memory                                                             
      total: 256Mi
```                                                                                                                                                                                                                                                                                                                                                  
                                                                                                                                                                                                                                                                                                                                                                                             Both charges against the same gpu.memory quota pool — unified accounting for whole-GPU and MIG workloads.

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-22T21:32:45Z

/test pull-kueue-test-e2e-main-1-36

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T13:12:46Z

Just a note: e2e tests have not been added to this PR to keep the PR size manageable for reviewers. You can take a look here: https://github.com/kubernetes-sigs/kueue/pull/11420

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T19:22:25Z

/test pull-kueue-test-e2e-multikueue-sequential-main
This flake should be already fixed

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T21:44:10Z

@PannagaRao: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-large-scale-scheduling-perf-main | 26071e82c9ea37857d6d09c16f138e997526112b | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10320/pull-kueue-test-large-scale-scheduling-perf-main/2052315007651155968) | true | `/test pull-kueue-test-large-scale-scheduling-perf-main`
pull-kueue-test-e2e-main-1-36 | 98ab83c701d57f43600d07910a74303948306dad | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10320/pull-kueue-test-e2e-main-1-36/2057937800677822464) | true | `/test pull-kueue-test-e2e-main-1-36`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=10320). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3APannagaRao). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-28T04:01:45Z

/cc @mimowo PTAL

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-28T04:01:49Z

@sohankunkerkar: GitHub didn't allow me to request PR reviews from the following users: PTAL.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) and repo collaborators can review this PR, and authors cannot review their own PRs.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10320#issuecomment-4560734372):

>/cc @mimowo PTAL


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-28T13:13:14Z

LGTM label has been added.  <details>Git tree hash: 0988531df3bbabd460d5d1133214bb694c5e956a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-28T13:13:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10320#pullrequestreview-4381226311" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10320#" title="Author self-approved">PannagaRao</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-29T12:34:36Z

/release-note-edit

```release-note
DRA: Add counter-based quota support for partitionable DRA devices (e.g., NVIDIA MIG) via counterMappings configuration.  
```

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T18:24:15Z

- **apis/config/v1beta2/configuration_types.go:589** by [@kannon92](https://github.com/kannon92) — 2026-04-09T18:24:15Z
```diff
@@ -569,6 +574,37 @@ type DeviceClassMapping struct {
 	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
 }
 
+// CounterMapping maps a DRA driver counter to a Kueue quota resource for
+// partitionable device support.
+type CounterMapping struct {
+	// Name is the Kueue resource name used in ClusterQueue coveredResources
+	// and Workload resourceUsage.
+	// Must be a valid fully qualified name consisting of an optional DNS subdomain prefix
+	// followed by a slash and a DNS label, or just a DNS label.
+	// DNS labels consist of lower-case alphanumeric characters or hyphens,
+	// and must start and end with an alphanumeric character.
+	// DNS subdomain prefixes follow the same rules as DNS labels but can contain periods.
+	// The total length must not exceed 253 characters.
+	// +required
+	// +kubebuilder:validation:MaxLength=253
```

  I don't think these kubebuilder validations do anything for config apis. These are not CRDs so I don't think there is any enforcement here.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T18:25:08Z

- **cmd/experimental/kueue-populator/go.mod:20** by [@kannon92](https://github.com/kannon92) — 2026-04-09T18:25:08Z
```diff
@@ -18,7 +18,9 @@ require (
 )
 
 require (
```

  I don't really get why you changed this file.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T18:26:11Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-04-09T18:26:12Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  This feature gate is not complete.
  
  I think you also need PartitionableDevices in addition to DRA.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T18:28:11Z

- **pkg/dra/claims.go:404** by [@kannon92](https://github.com/kannon92) — 2026-04-09T18:28:11Z
```diff
@@ -182,3 +215,209 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
```

  This reads like my work in https://github.com/kubernetes-sigs/kueue/pull/9742.
  
  Are you waiting on this PR to merge? Or do you want to close mine?

### Review by [@kannon92](https://github.com/kannon92) (changes_requested) — 2026-04-09T18:30:34Z

There are a few items that need to be addressed (feature gates especially).

I am also curious on the dependency on CEL https://github.com/kubernetes-sigs/kueue/pull/9742 and do we have a dependency we should call out or are you aiming to merge this?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-09T18:31:00Z

- **cmd/experimental/kueue-populator/go.mod:20** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-09T18:31:00Z
```diff
@@ -18,7 +18,9 @@ require (
 )
 
 require (
```

  The populator verify CI (pull-kueue-populator-verify-main) failed without it as the populator go.mod/go.sum were out of sync.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T19:13:47Z

- **cmd/experimental/kueue-populator/go.mod:20** by [@kannon92](https://github.com/kannon92) — 2026-04-09T19:13:47Z
```diff
@@ -18,7 +18,9 @@ require (
 )
 
 require (
```

  cc @mbobrovskyi 
  
  Is this expected?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-09T19:28:04Z

- **pkg/dra/claims.go:404** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-09T19:28:04Z
```diff
@@ -182,3 +215,209 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
```

  This PR has a dependency on https://github.com/kubernetes-sigs/kueue/pull/9742. So I'm waiting for it to merge.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-09T19:29:55Z

- **pkg/dra/claims.go:404** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-09T19:29:55Z
```diff
@@ -182,3 +215,209 @@ func GetResourceRequestsForResourceClaimTemplates(
 
 	return perPodSet, nil
 }
+
+// validateCELSelectors compiles each CEL expression in the given selectors using
+// the upstream DRA CEL compiler. This catches invalid CEL syntax, type errors,
+// and other compilation issues before quota admission.
+func validateCELSelectors(selectors []resourcev1.DeviceSelector, fldPath *field.Path) error {
+	if len(selectors) == 0 {
+		return nil
+	}
+
+	compiler := dracel.GetCompiler(dracel.Features{})
+	var errs []error
+	for i, sel := range selectors {
+		if sel.CEL == nil {
+			continue
+		}
+		result := compiler.CompileCELExpression(sel.CEL.Expression, dracel.Options{})
+		if result.Error != nil {
+			errs = append(errs, field.Invalid(
+				fldPath.Index(i).Child("cel", "expression"),
+				sel.CEL.Expression,
+				fmt.Sprintf("CEL compilation failed: %s", result.Error.Detail),
+			))
+		}
+	}
+	return errors.Join(errs...)
+}
+
+// extractCELRequests returns the device requests from a claim spec that have CEL selectors.
+func extractCELRequests(claimSpec *resourcev1.ResourceClaimSpec) []celDeviceRequest {
+	if claimSpec == nil {
+		return nil
+	}
+	var result []celDeviceRequest
+	for i, req := range claimSpec.Devices.Requests {
+		if req.Exactly == nil || len(req.Exactly.Selectors) == 0 {
+			continue
+		}
+		hasCEL := false
+		for _, sel := range req.Exactly.Selectors {
+			if sel.CEL != nil {
+				hasCEL = true
+				break
+			}
+		}
+		if hasCEL {
+			result = append(result, celDeviceRequest{
+				index:           i,
+				count:           req.Exactly.Count,
+				deviceClassName: req.Exactly.DeviceClassName,
+				selectors:       req.Exactly.Selectors,
+			})
+		}
+	}
+	return result
+}
+
+// validateCELSelectorsAgainstDevices lists all ResourceSlices in the cluster and
```

  Added it in the PR description.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-10T07:04:48Z

- **apis/config/v1beta2/configuration_types.go:589** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-10T07:04:48Z
```diff
@@ -569,6 +574,37 @@ type DeviceClassMapping struct {
 	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
 }
 
+// CounterMapping maps a DRA driver counter to a Kueue quota resource for
+// partitionable device support.
+type CounterMapping struct {
+	// Name is the Kueue resource name used in ClusterQueue coveredResources
+	// and Workload resourceUsage.
+	// Must be a valid fully qualified name consisting of an optional DNS subdomain prefix
+	// followed by a slash and a DNS label, or just a DNS label.
+	// DNS labels consist of lower-case alphanumeric characters or hyphens,
+	// and must start and end with an alphanumeric character.
+	// DNS subdomain prefixes follow the same rules as DNS labels but can contain periods.
+	// The total length must not exceed 253 characters.
+	// +required
+	// +kubebuilder:validation:MaxLength=253
```

  Got it. I'll remove them

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-10T07:09:16Z

- **cmd/kueue/main.go:306** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-10T07:09:14Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  Good catch. Gated counterMappings behind DRAPartitionableDevices — when the gate is off, nil is passed to CreateMapperFromConfiguration which already handles nil.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T15:44:19Z

- **apis/config/v1beta2/configuration_types.go:524** by [@kannon92](https://github.com/kannon92) — 2026-04-10T15:44:19Z
```diff
@@ -520,6 +520,11 @@ type Resources struct {
 	// for Dynamic Resource Allocation support.
 	// +optional
 	DeviceClassMappings []DeviceClassMapping `json:"deviceClassMappings,omitempty"`
+
+	// CounterMappings defines mappings from DRA driver counters to logical resources
```

  Usually when we add a new API based on a feature gate, we also want to make sure that if this feature gate is disabled this API is not valid.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T15:44:46Z

- **apis/config/v1beta2/configuration_types.go:525** by [@kannon92](https://github.com/kannon92) — 2026-04-10T15:44:46Z
```diff
@@ -520,6 +520,11 @@ type Resources struct {
 	// for Dynamic Resource Allocation support.
 	// +optional
 	DeviceClassMappings []DeviceClassMapping `json:"deviceClassMappings,omitempty"`
+
+	// CounterMappings defines mappings from DRA driver counters to logical resources
+	// for partitionable device quota management.
```

  We should add a comment that this field is under a feature gate.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-10T15:45:50Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-04-10T15:45:49Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  I think we need to change this a bit. This logic is a bit confusing.
  
  Should we fail validation if CounterMappings is set with feature gate off?
  
  We should handle this in our default logic for pkg/config.
  
  ie if feature is off, disable this field.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-10T18:37:09Z

- **cmd/kueue/main.go:306** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-10T18:37:09Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  DeviceClassMappings doesn't reject when DynamicResourceAllocation is off either — failing the validation for CounterMappings alone would be inconsistent.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-10T18:41:10Z

- **cmd/kueue/main.go:306** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-10T18:41:10Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  We could nil out CounterMappings in pkg/config/config.go after config loading when the gate is off. WDYT?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-13T14:59:25Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-04-13T14:59:25Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  That sounds good to me.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-13T15:00:21Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-04-13T15:00:21Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  > DeviceClassMappings doesn't reject when DynamicResourceAllocation is off either — failing the validation for CounterMappings alone would be inconsistent.
  
  @mimowo @tenzen-y how should we handle API changes if feature gate is disabled?
  
  I think validation errors in config or defaulting are both good options. I'd prefer failing validation honestly over defaulting.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-16T04:08:57Z

- **cmd/kueue/main.go:306** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-16T04:08:57Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  > I think we need to change this a bit. This logic is a bit confusing.
  > 
  > Should we fail validation if CounterMappings is set with feature gate off?
  > 
  > We should handle this in our default logic for pkg/config.
  > 
  > ie if feature is off, disable this field.
  
  @kannon92 Moved the counterMappings nil-out to config.go using cfg.FeatureGates.
  
  Wanted to discuss one case though — if an admin sets DRAPartitionableDevices via --feature-gates CLI flag but has counterMappings in the config file, cfg.FeatureGates won't have the gate and counterMappings gets dropped. Is this a case we don't have to worry about or would it be safer to put this check in main.go after feature gates are fully resolved from both sources?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-16T15:39:03Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-04-16T15:39:03Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  I guess I'd like to simplify the code so that we don't need to check counterMappings plus feature gate in a few places.
  
  We really want to encourage the use of feature gates in the config rather than the CLI.
  
  I wonder if we should announce deprecation of cli feature gates? WDYT @mimowo @tenzen-y ?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-20T19:17:40Z

- **cmd/kueue/main.go:308** by [@kannon92](https://github.com/kannon92) — 2026-04-20T19:17:41Z
```diff
@@ -304,8 +304,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
+		(len(cfg.Resources.DeviceClassMappings) > 0 || len(cfg.Resources.CounterMappings) > 0) {
```

  This logic seems way more complicated than it needs to be. Plus we aren't testing any of the edge cases.
  
  Could you refactor this?
  
  Maybe if DRA is true, call dra.CreateMapperFromConfiguration.
  
  And in that code you can handle checking for nils of the resources values. And that way you can add some test cases for this.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-04-21T03:22:41Z

- **cmd/kueue/main.go:308** by [@PannagaRao](https://github.com/PannagaRao) — 2026-04-21T03:22:41Z
```diff
@@ -304,8 +304,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
+		(len(cfg.Resources.DeviceClassMappings) > 0 || len(cfg.Resources.CounterMappings) > 0) {
```

  Simplified — removed the length checks from main.go. CreateMapperFromConfiguration already handles nil inputs internally. 
  
  I have added test cases for CreateMapperFromConfiguration covering: both nil, deviceClassMappings only, counterMappings only, and both configured. 
  
  PTAL!

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T13:34:36Z

- **cmd/experimental/kueue-populator/go.mod:20** by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:34:36Z
```diff
@@ -18,7 +18,9 @@ require (
 )
 
 require (
```

  This is no longer relevant. Please resolve.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T13:34:50Z

- **cmd/kueue/main.go:306** by [@kannon92](https://github.com/kannon92) — 2026-05-12T13:34:50Z
```diff
@@ -303,8 +303,9 @@ func main() {
 		cacheOptions = append(cacheOptions, schdcache.WithResourceTransformations(cfg.Resources.Transformations))
 		queueOptions = append(queueOptions, qcache.WithResourceTransformations(cfg.Resources.Transformations))
 	}
-	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil && len(cfg.Resources.DeviceClassMappings) > 0 {
-		if err := dra.CreateMapperFromConfiguration(cfg.Resources.DeviceClassMappings); err != nil {
+	if features.Enabled(features.DynamicResourceAllocation) && cfg.Resources != nil &&
```

  Thank you for fixing this.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (changes_requested) — 2026-05-19T04:37:42Z

- **pkg/config/config.go:228** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:03:51Z
```diff
@@ -225,6 +225,11 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 			return options, cfg, err
 		}
 	}
+	if cfg.Resources != nil && !cfg.FeatureGates[string(features.KueueDRAIntegrationPartitionableDevices)] {
```

  Here it strips Counters from `deviceClassMappings` when the gate is not in cfg.FeatureGates. But `cmd/kueue/main.go` applies CLI feature gates after Load() returns `(Set(featureGates)`. So `--feature-gates=KueueDRAIntegrationPartitionableDevices=true` won't work as counters are already gone before the CLI flag is processed. This gate-dependent stripping needs to happen after feature gates are finalized, or move out of Load().

- **pkg/config/validation.go:523** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:08:01Z
```diff
@@ -503,6 +503,28 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
+		if len(mapping.Counters) > 0 {
+			if len(mapping.Counters) > 1 {
+				allErrs = append(allErrs, field.TooMany(mappingPath.Child("counters"), len(mapping.Counters), 1))
+			}
+			for cIdx, counter := range mapping.Counters {
+				counterPath := mappingPath.Child("counters").Index(cIdx)
+				if counter.DriverName == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("driverName"), ""))
+				} else if len(counter.DriverName) > 253 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("driverName"), counter.DriverName, "must not exceed 253 characters"))
+				}
+				if counter.CounterName == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("counterName"), ""))
+				} else if len(counter.CounterName) > 63 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("counterName"), counter.CounterName, "must not exceed 63 characters"))
+				}
+				if counter.ConsumesCounterSelector.CEL == nil || counter.ConsumesCounterSelector.CEL.Expression == "" {
```

  Here we check that the CEL expression is non-empty, but not that it compiles. A bad expression here won't be caught until a workload hits this mapping. KEP talks about `compiled at config load time.` Could we compile/validate the expression during config validation so invalid config fails early?

- **pkg/dra/counters.go:176** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:09:49Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := Mapper().lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := Mapper().getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.consumesCounterSelector},
+		reqPath, "consumesCounterSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driverName}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driverName)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driverName, selectorSelectors, classSelectors, requestSelectors)
+	if len(matched) == 0 {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			"no devices matching selectors found in any ResourceSlice pool for configured counter driver",
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, _ := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
```

  Should we log the error here with V(3) instead of discarding it altogether?

- **pkg/dra/counters.go:179** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:10:27Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := Mapper().lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := Mapper().getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.consumesCounterSelector},
+		reqPath, "consumesCounterSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driverName}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driverName)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driverName, selectorSelectors, classSelectors, requestSelectors)
+	if len(matched) == 0 {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			"no devices matching selectors found in any ResourceSlice pool for configured counter driver",
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, _ := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
+				continue
+			}
+			if classMatch, _ := evaluateSelectorsOnDevice(ctx, classSelectors, celDev); !classMatch {
```

  https://github.com/kubernetes-sigs/kueue/pull/10320/changes#r3263580105

- **pkg/dra/counters.go:182** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:10:35Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := Mapper().lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := Mapper().getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.consumesCounterSelector},
+		reqPath, "consumesCounterSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driverName}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driverName)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driverName, selectorSelectors, classSelectors, requestSelectors)
+	if len(matched) == 0 {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			"no devices matching selectors found in any ResourceSlice pool for configured counter driver",
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, _ := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
+				continue
+			}
+			if classMatch, _ := evaluateSelectorsOnDevice(ctx, classSelectors, celDev); !classMatch {
+				continue
+			}
+			if requestMatch, _ := evaluateSelectorsOnDevice(ctx, requestSelectors, celDev); requestMatch {
```

  https://github.com/kubernetes-sigs/kueue/pull/10320/changes#r3263580105

- **pkg/controller/core/resourceslice_controller.go:53** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-19T04:15:12Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceSliceReconciler) notifyAll() {
```

  nit: `notifyAll()` requeues inadmissible workloads for all ClusterQueues on every ResourceSlice event, regardless of driver. Not a blocker for alpha, but in clusters with unrelated DRA drivers this is broader than necessary.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-20T18:57:03Z

- **pkg/dra/counters.go:176** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-20T18:57:03Z
```diff
@@ -0,0 +1,279 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := Mapper().lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := Mapper().getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.consumesCounterSelector},
+		reqPath, "consumesCounterSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driverName}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driverName)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driverName, selectorSelectors, classSelectors, requestSelectors)
+	if len(matched) == 0 {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			"no devices matching selectors found in any ResourceSlice pool for configured counter driver",
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, _ := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-20T18:57:32Z

- **pkg/config/validation.go:523** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-20T18:57:32Z
```diff
@@ -503,6 +503,28 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
+		if len(mapping.Counters) > 0 {
+			if len(mapping.Counters) > 1 {
+				allErrs = append(allErrs, field.TooMany(mappingPath.Child("counters"), len(mapping.Counters), 1))
+			}
+			for cIdx, counter := range mapping.Counters {
+				counterPath := mappingPath.Child("counters").Index(cIdx)
+				if counter.DriverName == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("driverName"), ""))
+				} else if len(counter.DriverName) > 253 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("driverName"), counter.DriverName, "must not exceed 253 characters"))
+				}
+				if counter.CounterName == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("counterName"), ""))
+				} else if len(counter.CounterName) > 63 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("counterName"), counter.CounterName, "must not exceed 63 characters"))
+				}
+				if counter.ConsumesCounterSelector.CEL == nil || counter.ConsumesCounterSelector.CEL.Expression == "" {
```

  I have added the validation. PTAL!

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-20T18:59:34Z

- **pkg/config/config.go:228** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-20T18:59:34Z
```diff
@@ -225,6 +225,11 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 			return options, cfg, err
 		}
 	}
+	if cfg.Resources != nil && !cfg.FeatureGates[string(features.KueueDRAIntegrationPartitionableDevices)] {
```

  Good catch — I raised this same concern [here](https://github.com/kubernetes-sigs/kueue/pull/10320#discussion_r3090729350). The stripping was in Load() which runs before CLI feature gates are applied via LoadAndValidateFeatureGates. Kevin suggested deprecating CLI feature gates but nothing was decided. Should I move the stripping to main.go after both config and CLI gates are finalized?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-20T19:28:03Z

- **pkg/config/config.go:228** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-20T19:28:03Z
```diff
@@ -225,6 +225,11 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 			return options, cfg, err
 		}
 	}
+	if cfg.Resources != nil && !cfg.FeatureGates[string(features.KueueDRAIntegrationPartitionableDevices)] {
```

  @mimowo do you have any insights here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-20T19:44:04Z

- **pkg/config/config.go:228** by [@mimowo](https://github.com/mimowo) — 2026-05-20T19:44:04Z
```diff
@@ -225,6 +225,11 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 			return options, cfg, err
 		}
 	}
+	if cfg.Resources != nil && !cfg.FeatureGates[string(features.KueueDRAIntegrationPartitionableDevices)] {
```

  > Kevin suggested deprecating CLI feature gates but nothing was decided. 
  
  We cannot deprecate them as part of this PR. Make the code work with both ways of setting the FGs.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-21T19:53:42Z

- **pkg/config/config.go:228** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-21T19:53:42Z
```diff
@@ -225,6 +225,11 @@ func Load(scheme *runtime.Scheme, configFile string) (ctrl.Options, configapi.Co
 			return options, cfg, err
 		}
 	}
+	if cfg.Resources != nil && !cfg.FeatureGates[string(features.KueueDRAIntegrationPartitionableDevices)] {
```

  Done.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-24T04:32:27Z

- **pkg/dra/claims.go:220** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-24T04:27:34Z
```diff
@@ -217,6 +217,9 @@ func GetResourceRequestsForResourceClaimTemplates(
 					))
 					return nil, allErrs
 				}
+				if Mapper().getCounterConfig(dc) != nil {
```

  ```suggestion
  				if features.Enabled(features.KueueDRAIntegrationPartitionableDevices) && Mapper().getCounterConfig(dc) != nil { 
  ```
  When the `KueueDRAIntegrationPartitionableDevices` gate is off, this continue skips device-count charging but `GetCounterResourcesForWorkload` is never called either,  resulting in zero charge for the DeviceClass.

- **apis/config/v1beta2/configuration_types.go:607** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-24T04:28:21Z
```diff
@@ -586,6 +587,53 @@ type DeviceClassMapping struct {
 	// DNS subdomain prefixes follow the same rules as DNS labels but can contain periods.
 	// The total length of each name must not exceed 253 characters.
 	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+
+	// Sources configures resource accounting sources for this mapping.
+	// Each source defines how quota is tracked for this DeviceClass.
+	// Currently only counter sources are supported (for partitionable devices).
+	// Extended resource requests that resolve to a DeviceClass with sources
+	// configured are marked inadmissible.
+	// Requires the KueueDRAIntegrationPartitionableDevices feature gate.
+	// +optional
+	Sources []DeviceClassSourceConfig `json:"sources,omitempty"`
+}
+
+// DeviceClassSourceConfig defines a resource accounting source for a DeviceClassMapping.
+// Exactly one of the source types must be set.
+type DeviceClassSourceConfig struct {
+	// Counter configures counter-based quota for partitionable devices.
+	// Maps a DRA driver counter to the parent DeviceClassMapping's Kueue quota resource.
+	// +optional
+	Counter *CounterSource `json:"counter,omitempty"`
```

  ```suggestion
  	Counter *DeviceClassCounterSource `json:"counter,omitempty"`
  ```

- **apis/config/v1beta2/configuration_types.go:610** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-24T04:28:30Z
```diff
@@ -586,6 +587,53 @@ type DeviceClassMapping struct {
 	// DNS subdomain prefixes follow the same rules as DNS labels but can contain periods.
 	// The total length of each name must not exceed 253 characters.
 	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+
+	// Sources configures resource accounting sources for this mapping.
+	// Each source defines how quota is tracked for this DeviceClass.
+	// Currently only counter sources are supported (for partitionable devices).
+	// Extended resource requests that resolve to a DeviceClass with sources
+	// configured are marked inadmissible.
+	// Requires the KueueDRAIntegrationPartitionableDevices feature gate.
+	// +optional
+	Sources []DeviceClassSourceConfig `json:"sources,omitempty"`
+}
+
+// DeviceClassSourceConfig defines a resource accounting source for a DeviceClassMapping.
+// Exactly one of the source types must be set.
+type DeviceClassSourceConfig struct {
+	// Counter configures counter-based quota for partitionable devices.
+	// Maps a DRA driver counter to the parent DeviceClassMapping's Kueue quota resource.
+	// +optional
+	Counter *CounterSource `json:"counter,omitempty"`
+}
+
+// CounterSource identifies where to read counter data from and which counter to track.
```

  ```suggestion
  // DeviceClassCounterSource identifies where to read counter data from and which counter to track.
  ```

- **apis/config/v1beta2/configuration_types.go:611** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-24T04:28:38Z
```diff
@@ -586,6 +587,53 @@ type DeviceClassMapping struct {
 	// DNS subdomain prefixes follow the same rules as DNS labels but can contain periods.
 	// The total length of each name must not exceed 253 characters.
 	DeviceClassNames []corev1.ResourceName `json:"deviceClassNames"`
+
+	// Sources configures resource accounting sources for this mapping.
+	// Each source defines how quota is tracked for this DeviceClass.
+	// Currently only counter sources are supported (for partitionable devices).
+	// Extended resource requests that resolve to a DeviceClass with sources
+	// configured are marked inadmissible.
+	// Requires the KueueDRAIntegrationPartitionableDevices feature gate.
+	// +optional
+	Sources []DeviceClassSourceConfig `json:"sources,omitempty"`
+}
+
+// DeviceClassSourceConfig defines a resource accounting source for a DeviceClassMapping.
+// Exactly one of the source types must be set.
+type DeviceClassSourceConfig struct {
+	// Counter configures counter-based quota for partitionable devices.
+	// Maps a DRA driver counter to the parent DeviceClassMapping's Kueue quota resource.
+	// +optional
+	Counter *CounterSource `json:"counter,omitempty"`
+}
+
+// CounterSource identifies where to read counter data from and which counter to track.
+type CounterSource struct {
```

  ```suggestion
  type DeviceClassCounterSource struct {
  ```

- **pkg/config/validation.go:533** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-24T04:32:07Z
```diff
@@ -503,6 +504,44 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
+		if len(mapping.Sources) > 0 {
+			if len(mapping.Sources) > 1 {
+				allErrs = append(allErrs, field.TooMany(mappingPath.Child("sources"), len(mapping.Sources), 1))
+			}
+			for sIdx, source := range mapping.Sources {
+				sourcePath := mappingPath.Child("sources").Index(sIdx)
+				if source.Counter == nil {
+					allErrs = append(allErrs, field.Required(sourcePath.Child("counter"), "exactly one source type must be set"))
+					continue
+				}
+				counterPath := sourcePath.Child("counter")
+				if source.Counter.Name == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("name"), ""))
+				} else if len(source.Counter.Name) > 63 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("name"), source.Counter.Name, "must not exceed 63 characters"))
+				}
+				if source.Counter.Driver == "" {
+					allErrs = append(allErrs, field.Required(counterPath.Child("driver"), ""))
+				} else if len(source.Counter.Driver) > 253 {
+					allErrs = append(allErrs, field.Invalid(counterPath.Child("driver"), source.Counter.Driver, "must not exceed 253 characters"))
+				}
+				selectorPath := counterPath.Child("deviceSelector", "cel", "expression")
+				if source.Counter.DeviceSelector.CEL == nil || source.Counter.DeviceSelector.CEL.Expression == "" {
+					allErrs = append(allErrs, field.Required(selectorPath, ""))
+				} else {
+					cache := dracel.NewCache(1, dracel.Features{})
```

  This creates a new cache inside the loop for every source entry. Move it before https://github.com/kubernetes-sigs/kueue/pull/10320/changes#diff-3e0b3707cc5d712d06d1eeb6a67c10b45a7ba0b27e972924d71df3c8e539f23bR512 loop and reuse.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-25T05:40:02Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1259** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-25T05:40:02Z
```diff
@@ -1254,3 +1255,842 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 		})
 	})
 })
+
+var _ = ginkgo.Describe("DRA Partitionable Devices Integration", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
```

  I think we should extract this test into a separate file, maybe?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-25T20:36:49Z

- **pkg/dra/claims.go:220** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-25T20:36:49Z
```diff
@@ -217,6 +217,9 @@ func GetResourceRequestsForResourceClaimTemplates(
 					))
 					return nil, allErrs
 				}
+				if Mapper().getCounterConfig(dc) != nil {
```

  The guard in main.go (after LoadAndValidateFeatureGates) nils out Sources from all DeviceClassMappings when the gate is off, before CreateMapperFromConfiguration is called. So getCounterConfig() can never return non-nil when KueueDRAIntegrationPartitionableDevices is disabled.
  
  I didn't add the gate check here to keep the feature gate enforcement at a single point (main.go), per the [earlier discussion](https://github.com/kubernetes-sigs/kueue/pull/10320#discussion_r3094440434). Happy to add it if you see a code path where getCounterConfig() could return non-nil with the gate off.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T02:41:07Z

- **pkg/dra/claims.go:220** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T02:41:07Z
```diff
@@ -217,6 +217,9 @@ func GetResourceRequestsForResourceClaimTemplates(
 					))
 					return nil, allErrs
 				}
+				if Mapper().getCounterConfig(dc) != nil {
```

  ah, right! I  think this makes sense. Thanks for pointing this out.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:15:33Z

- **pkg/controller/core/resourceslice_controller.go:80** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:15:34Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceSliceReconciler) notifyAll() {
+	cqNames := sets.New(r.qManager.GetClusterQueueNames()...)
+	qcache.NotifyRetryInadmissible(r.qManager, cqNames)
+}
+
+func (r *ResourceSliceReconciler) Create(event.TypedCreateEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Update(event.TypedUpdateEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Delete(event.TypedDeleteEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Generic(event.TypedGenericEvent[*resourcev1.ResourceSlice]) bool {
+	return false
+}
+
+// +kubebuilder:rbac:groups=resource.k8s.io,resources=resourceslices,verbs=get;list;watch
+
+func (r *ResourceSliceReconciler) SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) error {
+	if err := mgr.GetFieldIndexer().IndexField(context.Background(), &resourcev1.ResourceSlice{},
```

  Don\t use background context, pass the main context

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:17:57Z

- **pkg/controller/core/resourceslice_controller.go:70** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:17:57Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceSliceReconciler) notifyAll() {
+	cqNames := sets.New(r.qManager.GetClusterQueueNames()...)
+	qcache.NotifyRetryInadmissible(r.qManager, cqNames)
+}
```

  This looks like having a potential perf issue.
  
  I think `NotifyRetryInadmissible` should be called from Reconcile. 
  
  Only relevent changes to the ResourceSlice should be triggering reconcile

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:18:27Z

- **pkg/controller/core/resourceslice_controller.go:50** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:18:28Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
```

  wire logger with roleTracker and log the reconcile events with V3

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:19:02Z

- **pkg/dra/claims.go:220** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:19:03Z
```diff
@@ -217,6 +217,9 @@ func GetResourceRequestsForResourceClaimTemplates(
 					))
 					return nil, allErrs
 				}
+				if Mapper().getCounterConfig(dc) != nil {
```

  Wrap in FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:19:55Z

- **pkg/config/validation.go:508** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:19:55Z
```diff
@@ -503,6 +504,44 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
+		if len(mapping.Sources) > 0 {
```

  check FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:21:05Z

- **cmd/kueue/main.go:162** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:21:06Z
```diff
@@ -155,6 +155,12 @@ func main() {
 		os.Exit(1)
 	}
 
+	if cfg.Resources != nil && !features.Enabled(features.KueueDRAIntegrationPartitionableDevices) {
+		for i := range cfg.Resources.DeviceClassMappings {
+			cfg.Resources.DeviceClassMappings[i].Sources = nil
+		}
+	}
```

  Hm, why clearing the field rather than just ignore based on the FG from within the code? I mean this approach works too, but ignoring depending on the FG is more consistent with the rest of Kueue code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:22:13Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1655** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:22:13Z
```diff
@@ -1254,3 +1255,842 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 		})
 	})
 })
+
+var _ = ginkgo.Describe("DRA Partitionable Devices Integration", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+			c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings,
+				config.DeviceClassMapping{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			)
+		}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Partitionable devices are configured", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			migDeviceClass *resourcev1.DeviceClass
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "dra-pd-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			migDeviceClass = &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "mig.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					Selectors: []resourcev1.DeviceSelector{
+						{CEL: &resourcev1.CELDeviceSelector{Expression: "device.attributes['gpu.example.com'].type == 'mig'"}},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, migDeviceClass)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-pd-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "pd-cq-",
+				},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{
+						{
+							CoveredResources: []corev1.ResourceName{"cpu", "memory", "gpu.memory"},
+							Flavors: []kueue.FlavorQuotas{
+								{
+									Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+									Resources: []kueue.ResourceQuota{
+										{Name: "cpu", NominalQuota: resource.MustParse("8")},
+										{Name: "memory", NominalQuota: resource.MustParse("16Gi")},
+										{Name: "gpu.memory", NominalQuota: resource.MustParse("160Gi")},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("pd-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, migDeviceClass, true)
+		})
+
+		ginkgo.It("Should admit MIG workload with correct counter-based gpu.memory charge", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-7g40gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("7g.40gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector for 1g.5gb")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload requesting MIG devices")
+			wl := utiltestingapi.MakeWorkload("pd-test-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is admitted with counter-based gpu.memory charge")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("4864Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should multiply counter charge by request count", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-count-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with count=2")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-count2", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 2).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-count2-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-count2")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is 4864Mi * 2")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64(4864 * 1024 * 1024 * 2)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge MAX when CEL matches multiple profiles", func() {
+			ginkgo.By("Creating a ResourceSlice with different MIG profiles")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-broad-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-3g20gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("3g.20gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("20Gi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with broad CEL matching both profiles")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-broad", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb' || device.attributes['gpu.example.com'].profile == '3g.20gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-broad-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-broad")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is MAX(4864Mi, 20Gi) = 20Gi")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("20Gi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible when CEL matches no devices", framework.SlowSpec, func() {
+			ginkgo.By("Creating a ResourceSlice with devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-nomatch-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
```

  We need to introduce the Wrappers pattern for this

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:24:15Z

- **test/integration/singlecluster/controller/dra/dra_test.go:2072** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:24:15Z
```diff
@@ -1254,3 +1255,842 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 		})
 	})
 })
+
+var _ = ginkgo.Describe("DRA Partitionable Devices Integration", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+			c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings,
+				config.DeviceClassMapping{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			)
+		}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Partitionable devices are configured", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			migDeviceClass *resourcev1.DeviceClass
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "dra-pd-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			migDeviceClass = &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "mig.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					Selectors: []resourcev1.DeviceSelector{
+						{CEL: &resourcev1.CELDeviceSelector{Expression: "device.attributes['gpu.example.com'].type == 'mig'"}},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, migDeviceClass)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-pd-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "pd-cq-",
+				},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{
+						{
+							CoveredResources: []corev1.ResourceName{"cpu", "memory", "gpu.memory"},
+							Flavors: []kueue.FlavorQuotas{
+								{
+									Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+									Resources: []kueue.ResourceQuota{
+										{Name: "cpu", NominalQuota: resource.MustParse("8")},
+										{Name: "memory", NominalQuota: resource.MustParse("16Gi")},
+										{Name: "gpu.memory", NominalQuota: resource.MustParse("160Gi")},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("pd-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, migDeviceClass, true)
+		})
+
+		ginkgo.It("Should admit MIG workload with correct counter-based gpu.memory charge", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-7g40gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("7g.40gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector for 1g.5gb")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload requesting MIG devices")
+			wl := utiltestingapi.MakeWorkload("pd-test-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is admitted with counter-based gpu.memory charge")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("4864Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should multiply counter charge by request count", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-count-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with count=2")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-count2", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 2).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-count2-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-count2")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is 4864Mi * 2")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64(4864 * 1024 * 1024 * 2)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge MAX when CEL matches multiple profiles", func() {
+			ginkgo.By("Creating a ResourceSlice with different MIG profiles")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-broad-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-3g20gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("3g.20gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("20Gi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with broad CEL matching both profiles")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-broad", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb' || device.attributes['gpu.example.com'].profile == '3g.20gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-broad-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-broad")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is MAX(4864Mi, 20Gi) = 20Gi")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("20Gi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible when CEL matches no devices", framework.SlowSpec, func() {
+			ginkgo.By("Creating a ResourceSlice with devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-nomatch-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with CEL that matches no device")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-nonexistent", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == 'nonexistent'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-nomatch-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-nonexistent")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is marked as inadmissible")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should admit workload with unified whole GPU and MIG charges", func() {
+			ginkgo.By("Creating a ResourceSlice with whole GPU and MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-unified-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a DeviceClass for whole GPUs")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCTs for whole GPU and MIG")
+			rctGPU := utiltesting.MakeResourceClaimTemplate("whole-gpu", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rctGPU)).To(gomega.Succeed())
+
+			rctMIG := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rctMIG)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload with both RCTs")
+			wl := utiltestingapi.MakeWorkload("pd-unified-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "full", ResourceClaimTemplateName: new("whole-gpu")},
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "full"},
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying both charges are under gpu.memory")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64((40320 + 4864) * 1024 * 1024)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible with incomplete pool", framework.SlowSpec, func() {
+			ginkgo.By("Creating only 1 of 2 ResourceSlices for a pool")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-incomplete-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 2,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with CEL selector")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-incomplete", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-incomplete-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-incomplete")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is inadmissible due to incomplete pool")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when ResourceSlice is created", framework.SlowSpec, func() {
+			ginkgo.By("Creating RCT with CEL selector")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-requeue", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload without any ResourceSlices")
+			wl := utiltestingapi.MakeWorkload("pd-requeue-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-requeue")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is initially inadmissible")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating ResourceSlice — should trigger requeue")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-requeue-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Verifying workload is now admitted after requeue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge consumesCounters for whole GPU request without CEL", func() {
+			ginkgo.By("Creating a ResourceSlice with whole GPU device")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-whole-gpu-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a DeviceClass for whole GPUs")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT for whole GPU without CEL selectors")
+			rct := utiltesting.MakeResourceClaimTemplate("whole-gpu-nocel", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-whole-gpu-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "gpu", ResourceClaimTemplateName: new("whole-gpu-nocel")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "gpu"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload charges gpu.memory from consumesCounters")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("40320Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject extended resource when DeviceClass has counters", framework.SlowSpec, func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.KueueDRAIntegrationExtendedResource, true)
+
+			ginkgo.By("Creating a DeviceClass with extendedResourceName and counters mapping")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu-er-counter.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: new("example.com/gpu-counter"),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Updating mapper to include ER DeviceClass with counters")
+			err := dra.CreateMapperFromConfiguration([]config.DeviceClassMapping{
+				{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com", "gpu-er-counter.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			})
+			gomega.Expect(err).NotTo(gomega.HaveOccurred())
+			defer func() {
+				_ = dra.CreateMapperFromConfiguration([]config.DeviceClassMapping{
+					{
+						Name:             "gpu.memory",
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+						Sources: []config.DeviceClassSourceConfig{
+							{Counter: &config.DeviceClassCounterSource{
+								Name:   "memory",
+								Driver: "gpu.example.com",
+								DeviceSelector: resourcev1.DeviceSelector{
+									CEL: &resourcev1.CELDeviceSelector{
+										Expression: "device.driver == 'gpu.example.com'",
+									},
+								},
+							}},
+						},
+					},
+				})
+			}()
```

  oh this is very heavy to read. First of all don't use defer func in ginkgo test.
  
  But I'm also not clear what is the role of the code. Is it to revert, then use AfterEach / After All

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:25:58Z

- **pkg/dra/mapper.go:34** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:25:58Z
```diff
@@ -18,10 +18,18 @@ package dra
 
 import (
 	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
 
 	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
 )
 
+// deviceClassCounterConfig holds counter configuration for a specific DeviceClass.
+type deviceClassCounterConfig struct {
+	driver         string
+	counterName    string
+	deviceSelector resourcev1.DeviceSelector
+}
+
 var (
 	globalMapper *ResourceMapper
```

  It is not this PR, but I think we need to get rid of the global state for DRA.
  
  This will be problematic for integration tests for MultiKueue, but the global state is generally making the code hard to follow. 
  
  I think we could move the cache under `/pkg/cache/dra/`

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T21:55:19Z

- **cmd/kueue/main.go:162** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T21:55:19Z
```diff
@@ -155,6 +155,12 @@ func main() {
 		os.Exit(1)
 	}
 
+	if cfg.Resources != nil && !features.Enabled(features.KueueDRAIntegrationPartitionableDevices) {
+		for i := range cfg.Resources.DeviceClassMappings {
+			cfg.Resources.DeviceClassMappings[i].Sources = nil
+		}
+	}
```

  We discussed this approach [here](https://github.com/kubernetes-sigs/kueue/pull/10320#discussion_r3094440434) - centralized stripping in main.go after both config and CLI gates are finalized to keep the FG enforcement in one place. Downstream code (claims.go, validation.go, counters.go) just sees getCounterConfig() == nil when the gate is off, avoiding the risk of a missed FG check.
  
  Happy to switch to distributed FG checks if you prefer consistency with the rest of Kueue.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T21:56:04Z

- **pkg/dra/claims.go:220** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T21:56:04Z
```diff
@@ -217,6 +217,9 @@ func GetResourceRequestsForResourceClaimTemplates(
 					))
 					return nil, allErrs
 				}
+				if Mapper().getCounterConfig(dc) != nil {
```

  This is guarded by the stripping in main.go. getCounterConfig() can't return non-nil when the gate is off because Sources are nilled out before the mapper is created.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T22:18:05Z

- **pkg/controller/core/resourceslice_controller.go:80** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T22:18:05Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceSliceReconciler) notifyAll() {
+	cqNames := sets.New(r.qManager.GetClusterQueueNames()...)
+	qcache.NotifyRetryInadmissible(r.qManager, cqNames)
+}
+
+func (r *ResourceSliceReconciler) Create(event.TypedCreateEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Update(event.TypedUpdateEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Delete(event.TypedDeleteEvent[*resourcev1.ResourceSlice]) bool {
+	r.notifyAll()
+	return false
+}
+
+func (r *ResourceSliceReconciler) Generic(event.TypedGenericEvent[*resourcev1.ResourceSlice]) bool {
+	return false
+}
+
+// +kubebuilder:rbac:groups=resource.k8s.io,resources=resourceslices,verbs=get;list;watch
+
+func (r *ResourceSliceReconciler) SetupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) error {
+	if err := mgr.GetFieldIndexer().IndexField(context.Background(), &resourcev1.ResourceSlice{},
```

  Moved the field indexer into a standalone SetupResourceSliceIndexer(ctx, indexer) function, called from setupIndexes in main.go where the main context is available to match the existing pattern.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T22:38:31Z

- **pkg/controller/core/resourceslice_controller.go:70** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T22:38:31Z
```diff
@@ -0,0 +1,100 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"context"
+
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+)
+
+type ResourceSliceReconciler struct {
+	qManager *qcache.Manager
+}
+
+var _ reconcile.Reconciler = (*ResourceSliceReconciler)(nil)
+
+func NewResourceSliceReconciler(qManager *qcache.Manager) *ResourceSliceReconciler {
+	return &ResourceSliceReconciler{
+		qManager: qManager,
+	}
+}
+
+func (r *ResourceSliceReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	return ctrl.Result{}, nil
+}
+
+func (r *ResourceSliceReconciler) notifyAll() {
+	cqNames := sets.New(r.qManager.GetClusterQueueNames()...)
+	qcache.NotifyRetryInadmissible(r.qManager, cqNames)
+}
```

  Moved NotifyRetryInadmissible into Reconcile. The predicates now filter by driver (a drivers set built at startup) and only return true for ResourceSlices matching those drivers.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T23:12:11Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1655** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T23:12:11Z
```diff
@@ -1254,3 +1255,842 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 		})
 	})
 })
+
+var _ = ginkgo.Describe("DRA Partitionable Devices Integration", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+			c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings,
+				config.DeviceClassMapping{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			)
+		}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Partitionable devices are configured", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			migDeviceClass *resourcev1.DeviceClass
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "dra-pd-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			migDeviceClass = &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "mig.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					Selectors: []resourcev1.DeviceSelector{
+						{CEL: &resourcev1.CELDeviceSelector{Expression: "device.attributes['gpu.example.com'].type == 'mig'"}},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, migDeviceClass)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-pd-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "pd-cq-",
+				},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{
+						{
+							CoveredResources: []corev1.ResourceName{"cpu", "memory", "gpu.memory"},
+							Flavors: []kueue.FlavorQuotas{
+								{
+									Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+									Resources: []kueue.ResourceQuota{
+										{Name: "cpu", NominalQuota: resource.MustParse("8")},
+										{Name: "memory", NominalQuota: resource.MustParse("16Gi")},
+										{Name: "gpu.memory", NominalQuota: resource.MustParse("160Gi")},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("pd-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, migDeviceClass, true)
+		})
+
+		ginkgo.It("Should admit MIG workload with correct counter-based gpu.memory charge", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-7g40gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("7g.40gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector for 1g.5gb")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload requesting MIG devices")
+			wl := utiltestingapi.MakeWorkload("pd-test-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is admitted with counter-based gpu.memory charge")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("4864Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should multiply counter charge by request count", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-count-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with count=2")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-count2", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 2).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-count2-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-count2")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is 4864Mi * 2")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64(4864 * 1024 * 1024 * 2)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge MAX when CEL matches multiple profiles", func() {
+			ginkgo.By("Creating a ResourceSlice with different MIG profiles")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-broad-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-3g20gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("3g.20gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("20Gi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with broad CEL matching both profiles")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-broad", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb' || device.attributes['gpu.example.com'].profile == '3g.20gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-broad-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-broad")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is MAX(4864Mi, 20Gi) = 20Gi")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("20Gi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible when CEL matches no devices", framework.SlowSpec, func() {
+			ginkgo.By("Creating a ResourceSlice with devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-nomatch-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T23:34:28Z

- **test/integration/singlecluster/controller/dra/dra_test.go:2072** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T23:34:27Z
```diff
@@ -1254,3 +1255,842 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 		})
 	})
 })
+
+var _ = ginkgo.Describe("DRA Partitionable Devices Integration", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+			c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings,
+				config.DeviceClassMapping{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			)
+		}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Partitionable devices are configured", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			migDeviceClass *resourcev1.DeviceClass
+		)
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "dra-pd-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			migDeviceClass = &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "mig.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					Selectors: []resourcev1.DeviceSelector{
+						{CEL: &resourcev1.CELDeviceSelector{Expression: "device.attributes['gpu.example.com'].type == 'mig'"}},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, migDeviceClass)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-pd-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "pd-cq-",
+				},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{
+						{
+							CoveredResources: []corev1.ResourceName{"cpu", "memory", "gpu.memory"},
+							Flavors: []kueue.FlavorQuotas{
+								{
+									Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+									Resources: []kueue.ResourceQuota{
+										{Name: "cpu", NominalQuota: resource.MustParse("8")},
+										{Name: "memory", NominalQuota: resource.MustParse("16Gi")},
+										{Name: "gpu.memory", NominalQuota: resource.MustParse("160Gi")},
+									},
+								},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("pd-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, migDeviceClass, true)
+		})
+
+		ginkgo.It("Should admit MIG workload with correct counter-based gpu.memory charge", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-test-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-7g40gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("7g.40gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a ResourceClaimTemplate with CEL selector for 1g.5gb")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating a workload requesting MIG devices")
+			wl := utiltestingapi.MakeWorkload("pd-test-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is admitted with counter-based gpu.memory charge")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("4864Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should multiply counter charge by request count", func() {
+			ginkgo.By("Creating a ResourceSlice with MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-count-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-1",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with count=2")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-count2", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 2).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-count2-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-count2")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is 4864Mi * 2")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64(4864 * 1024 * 1024 * 2)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge MAX when CEL matches multiple profiles", func() {
+			ginkgo.By("Creating a ResourceSlice with different MIG profiles")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-broad-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-3g20gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("3g.20gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("20Gi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with broad CEL matching both profiles")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-broad", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb' || device.attributes['gpu.example.com'].profile == '3g.20gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-broad-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-broad")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying charge is MAX(4864Mi, 20Gi) = 20Gi")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("20Gi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible when CEL matches no devices", framework.SlowSpec, func() {
+			ginkgo.By("Creating a ResourceSlice with devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-nomatch-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with CEL that matches no device")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-nonexistent", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == 'nonexistent'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-nomatch-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-nonexistent")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is marked as inadmissible")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should admit workload with unified whole GPU and MIG charges", func() {
+			ginkgo.By("Creating a ResourceSlice with whole GPU and MIG devices")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-unified-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a DeviceClass for whole GPUs")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCTs for whole GPU and MIG")
+			rctGPU := utiltesting.MakeResourceClaimTemplate("whole-gpu", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rctGPU)).To(gomega.Succeed())
+
+			rctMIG := utiltesting.MakeResourceClaimTemplate("mig-1g5gb", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rctMIG)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload with both RCTs")
+			wl := utiltestingapi.MakeWorkload("pd-unified-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "full", ResourceClaimTemplateName: new("whole-gpu")},
+				{Name: "mig", ResourceClaimTemplateName: new("mig-1g5gb")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "full"},
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying both charges are under gpu.memory")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				expectedBytes := int64((40320 + 4864) * 1024 * 1024)
+				g.Expect(memUsage.Value()).To(gomega.Equal(expectedBytes))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should mark workload inadmissible with incomplete pool", framework.SlowSpec, func() {
+			ginkgo.By("Creating only 1 of 2 ResourceSlices for a pool")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-incomplete-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 2,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT with CEL selector")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-incomplete", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-incomplete-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-incomplete")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is inadmissible due to incomplete pool")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when ResourceSlice is created", framework.SlowSpec, func() {
+			ginkgo.By("Creating RCT with CEL selector")
+			rct := utiltesting.MakeResourceClaimTemplate("mig-requeue", ns.Name).
+				DeviceRequest("gpu", "mig.example.com", 1).
+				WithCELSelectors("device.attributes['gpu.example.com'].profile == '1g.5gb'").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload without any ResourceSlices")
+			wl := utiltestingapi.MakeWorkload("pd-requeue-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "mig", ResourceClaimTemplateName: new("mig-requeue")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "mig"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is initially inadmissible")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(updatedWl.Status.Conditions).To(gomega.ContainElement(gomega.And(
+					gomega.HaveField("Type", kueue.WorkloadQuotaReserved),
+					gomega.HaveField("Status", metav1.ConditionFalse),
+					gomega.HaveField("Reason", kueue.WorkloadInadmissible),
+				)))
+			}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating ResourceSlice — should trigger requeue")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-requeue-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+						{
+							Name: "gpu-0-mig-1g5gb-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type":    {StringValue: new("mig")},
+								"profile": {StringValue: new("1g.5gb")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("4864Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Verifying workload is now admitted after requeue")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should charge consumesCounters for whole GPU request without CEL", func() {
+			ginkgo.By("Creating a ResourceSlice with whole GPU device")
+			slice := &resourcev1.ResourceSlice{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "pd-whole-gpu-slice",
+				},
+				Spec: resourcev1.ResourceSliceSpec{
+					Driver: "gpu.example.com",
+					Pool: resourcev1.ResourcePool{
+						Name:               "node1-gpu0",
+						Generation:         1,
+						ResourceSliceCount: 1,
+					},
+					NodeName: new("fake-node"),
+					Devices: []resourcev1.Device{
+						{
+							Name: "gpu-0",
+							Attributes: map[resourcev1.QualifiedName]resourcev1.DeviceAttribute{
+								"type": {StringValue: new("gpu")},
+							},
+							ConsumesCounters: []resourcev1.DeviceCounterConsumption{
+								{CounterSet: "gpu-0-counter-set", Counters: map[string]resourcev1.Counter{"memory": {Value: resource.MustParse("40320Mi")}}},
+							},
+						},
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, slice)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, slice)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating a DeviceClass for whole GPUs")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Creating RCT for whole GPU without CEL selectors")
+			rct := utiltesting.MakeResourceClaimTemplate("whole-gpu-nocel", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, rct)).To(gomega.Succeed())
+
+			ginkgo.By("Creating workload")
+			wl := utiltestingapi.MakeWorkload("pd-whole-gpu-wl", ns.Name).
+				Queue("pd-lq").
+				Obj()
+			wl.Spec.PodSets[0].Template.Spec.ResourceClaims = []corev1.PodResourceClaim{
+				{Name: "gpu", ResourceClaimTemplateName: new("whole-gpu-nocel")},
+			}
+			wl.Spec.PodSets[0].Template.Spec.Containers[0].Resources.Claims = []corev1.ResourceClaim{
+				{Name: "gpu"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload charges gpu.memory from consumesCounters")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName("gpu.memory")))
+				memUsage := assignment.ResourceUsage["gpu.memory"]
+				g.Expect(memUsage.Cmp(resource.MustParse("40320Mi"))).To(gomega.Equal(0))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should reject extended resource when DeviceClass has counters", framework.SlowSpec, func() {
+			features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.KueueDRAIntegrationExtendedResource, true)
+
+			ginkgo.By("Creating a DeviceClass with extendedResourceName and counters mapping")
+			dc := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu-er-counter.example.com",
+				},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: new("example.com/gpu-counter"),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, dc)).To(gomega.Succeed())
+			defer func() {
+				gomega.Expect(k8sClient.Delete(ctx, dc)).To(gomega.Succeed())
+			}()
+
+			ginkgo.By("Updating mapper to include ER DeviceClass with counters")
+			err := dra.CreateMapperFromConfiguration([]config.DeviceClassMapping{
+				{
+					Name:             "gpu.memory",
+					DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com", "gpu-er-counter.example.com"},
+					Sources: []config.DeviceClassSourceConfig{
+						{Counter: &config.DeviceClassCounterSource{
+							Name:   "memory",
+							Driver: "gpu.example.com",
+							DeviceSelector: resourcev1.DeviceSelector{
+								CEL: &resourcev1.CELDeviceSelector{
+									Expression: "device.driver == 'gpu.example.com'",
+								},
+							},
+						}},
+					},
+				},
+			})
+			gomega.Expect(err).NotTo(gomega.HaveOccurred())
+			defer func() {
+				_ = dra.CreateMapperFromConfiguration([]config.DeviceClassMapping{
+					{
+						Name:             "gpu.memory",
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com", "mig.example.com"},
+						Sources: []config.DeviceClassSourceConfig{
+							{Counter: &config.DeviceClassCounterSource{
+								Name:   "memory",
+								Driver: "gpu.example.com",
+								DeviceSelector: resourcev1.DeviceSelector{
+									CEL: &resourcev1.CELDeviceSelector{
+										Expression: "device.driver == 'gpu.example.com'",
+									},
+								},
+							}},
+						},
+					},
+				})
+			}()
```

  Replaced all defer func with ginkgo.DeferCleanup.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T05:30:34Z

- **pkg/config/validation.go:508** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T05:30:34Z
```diff
@@ -503,6 +504,44 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
+		if len(mapping.Sources) > 0 {
```

  Same as above. This is also  guarded by the stripping in main.go. When the gate is off because Sources are nilled out before the mapper is created.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T19:04:46Z

- **cmd/kueue/main.go:162** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T19:04:45Z
```diff
@@ -155,6 +155,12 @@ func main() {
 		os.Exit(1)
 	}
 
+	if cfg.Resources != nil && !features.Enabled(features.KueueDRAIntegrationPartitionableDevices) {
+		for i := range cfg.Resources.DeviceClassMappings {
+			cfg.Resources.DeviceClassMappings[i].Sources = nil
+		}
+	}
```

  @mimowo I believe I have addressed all the other comments and would appreciate your thoughts on this one. Could you PTAL?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T19:21:59Z

- **cmd/kueue/main.go:162** by [@mimowo](https://github.com/mimowo) — 2026-05-27T19:21:59Z
```diff
@@ -155,6 +155,12 @@ func main() {
 		os.Exit(1)
 	}
 
+	if cfg.Resources != nil && !features.Enabled(features.KueueDRAIntegrationPartitionableDevices) {
+		for i := range cfg.Resources.DeviceClassMappings {
+			cfg.Resources.DeviceClassMappings[i].Sources = nil
+		}
+	}
```

  > Downstream code (claims.go, validation.go, counters.go) just sees getCounterConfig() == nil when the gate is off, avoiding the risk of a missed FG check.
  
  Let's add the FG checks to all places in the downsteam code. Making the check explicit is actually also good for searching of the logic injection. Otherwise it is really hard to find which logic relies on the cleaning. With FG check I can easily find all the logic injection points by simple search.
  
  Sure, there is a risk of missing a check, but that is still better for code readabilty.
  
  > Happy to switch to distributed FG checks if you prefer consistency with the rest of Kueue.
  
  Yes, let's do that

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T21:31:52Z

- **cmd/kueue/main.go:162** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T21:31:52Z
```diff
@@ -155,6 +155,12 @@ func main() {
 		os.Exit(1)
 	}
 
+	if cfg.Resources != nil && !features.Enabled(features.KueueDRAIntegrationPartitionableDevices) {
+		for i := range cfg.Resources.DeviceClassMappings {
+			cfg.Resources.DeviceClassMappings[i].Sources = nil
+		}
+	}
```

  Done. Added FG checks and removed the stripping of Sources code.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:01:36Z

- **pkg/dra/counters.go:197** by [@mimowo](https://github.com/mimowo) — 2026-05-28T13:01:37Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	mapper *ResourceMapper,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := mapper.Lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := mapper.getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.deviceSelector},
+		reqPath, "deviceSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driver}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driver)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driver, selectorSelectors, classSelectors, requestSelectors)
+	if int64(len(matched)) < count {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			fmt.Sprintf("insufficient matching devices for counter driver: %d device(s) match but %d requested",
+				len(matched), count),
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	log := ctrl.LoggerFrom(ctx)
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, err := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in deviceSelector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, celDev); !classMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in DeviceClass selector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if requestMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, celDev); requestMatch {
+				allMatched = append(allMatched, *dev)
+			} else if err != nil {
+				log.V(3).Info("CEL evaluation error in request selector", "device", dev.Name, "error", err)
+			}
+		}
```

  I'm wonering when the CEL evaluation errors may happen, but it seems to me they should not be silenced, but propagated up. Silencing errors like this is usually as sign of potential problems downsteam.
  
  If we need to silence for some reason then it warrants comments,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:01:46Z

- **pkg/dra/counters.go:197** by [@mimowo](https://github.com/mimowo) — 2026-05-28T13:01:46Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	mapper *ResourceMapper,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := mapper.Lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := mapper.getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.deviceSelector},
+		reqPath, "deviceSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driver}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driver)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driver, selectorSelectors, classSelectors, requestSelectors)
+	if int64(len(matched)) < count {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			fmt.Sprintf("insufficient matching devices for counter driver: %d device(s) match but %d requested",
+				len(matched), count),
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	log := ctrl.LoggerFrom(ctx)
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, err := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in deviceSelector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, celDev); !classMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in DeviceClass selector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if requestMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, celDev); requestMatch {
+				allMatched = append(allMatched, *dev)
+			} else if err != nil {
+				log.V(3).Info("CEL evaluation error in request selector", "device", dev.Name, "error", err)
+			}
+		}
```

  cc @sohankunkerkar wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:02:59Z

- **pkg/dra/counters.go:158** by [@mimowo](https://github.com/mimowo) — 2026-05-28T13:02:59Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	mapper *ResourceMapper,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := mapper.Lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := mapper.getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.deviceSelector},
+		reqPath, "deviceSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driver}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driver)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driver, selectorSelectors, classSelectors, requestSelectors)
+	if int64(len(matched)) < count {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			fmt.Sprintf("insufficient matching devices for counter driver: %d device(s) match but %d requested",
+				len(matched), count),
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
```

  I think this function is complex enough to also have some logging in positive scenarios, probably at v3 or v4.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:03:23Z

- **pkg/dra/counters.go:34** by [@mimowo](https://github.com/mimowo) — 2026-05-28T13:03:23Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
```

  I think this function is complex enough to also have some logging, probably at v3 or v4.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:09:50Z

- **pkg/dra/counters.go:101** by [@mimowo](https://github.com/mimowo) — 2026-05-28T13:09:50Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	mapper *ResourceMapper,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := mapper.Lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := mapper.getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
```

  This function is certainly complex enough to have logging at v3 or v4 at least

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-28T13:09:59Z

- **pkg/dra/counters.go:197** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-28T13:09:59Z
```diff
@@ -0,0 +1,291 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"context"
+	"fmt"
+
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	"k8s.io/apimachinery/pkg/util/validation/field"
+	dracel "k8s.io/dynamic-resource-allocation/cel"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func GetCounterResourcesForWorkload(
+	ctx context.Context,
+	cl client.Client,
+	mapper *ResourceMapper,
+	wl *kueue.Workload,
+) (map[kueue.PodSetReference]corev1.ResourceList, field.ErrorList) {
+	perPodSet := make(map[kueue.PodSetReference]corev1.ResourceList)
+	var allErrs field.ErrorList
+	classCache := make(map[string]*resourcev1.DeviceClass)
+
+	for i := range wl.Spec.PodSets {
+		ps := &wl.Spec.PodSets[i]
+		aggregated := corev1.ResourceList{}
+
+		for j, prc := range ps.Template.Spec.ResourceClaims {
+			if prc.ResourceClaimTemplateName == nil {
+				continue
+			}
+			spec, err := getClaimSpec(ctx, cl, wl.Namespace, prc)
+			if err != nil {
+				allErrs = append(allErrs, field.InternalError(
+					field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j),
+					fmt.Errorf("failed to get claim spec for ResourceClaimTemplate %s: %w", *prc.ResourceClaimTemplateName, err),
+				))
+				return nil, allErrs
+			}
+			if spec == nil {
+				continue
+			}
+
+			for reqIdx, req := range spec.Devices.Requests {
+				if req.Exactly == nil {
+					continue
+				}
+				deviceClass := corev1.ResourceName(req.Exactly.DeviceClassName)
+				quotaResource, found := mapper.Lookup(deviceClass)
+				if !found {
+					continue
+				}
+				counterConfig := mapper.getCounterConfig(deviceClass)
+				if counterConfig == nil {
+					continue
+				}
+
+				reqPath := field.NewPath("spec", "podSets").Index(i).Child("template", "spec", "resourceClaims").Index(j).Child("devices", "requests").Index(reqIdx)
+
+				charges, errs := processCounterCharge(ctx, cl, counterConfig, quotaResource, req.Exactly.DeviceClassName, req.Exactly.Selectors, req.Exactly.Count, classCache, reqPath)
+				if len(errs) > 0 {
+					allErrs = append(allErrs, errs...)
+					return nil, allErrs
+				}
+				for resName, qty := range charges {
+					existing := aggregated[resName]
+					existing.Add(qty)
+					aggregated[resName] = existing
+				}
+			}
+		}
+
+		if len(aggregated) > 0 {
+			perPodSet[ps.Name] = aggregated
+		}
+	}
+
+	return perPodSet, nil
+}
+
+func processCounterCharge(
+	ctx context.Context,
+	cl client.Client,
+	counterConfig *deviceClassCounterConfig,
+	quotaResource corev1.ResourceName,
+	deviceClassName string,
+	selectors []resourcev1.DeviceSelector,
+	count int64,
+	classCache map[string]*resourcev1.DeviceClass,
+	reqPath *field.Path,
+) (corev1.ResourceList, field.ErrorList) {
+	selectorSelectors, compErrs := compileCELSelectors(
+		[]resourcev1.DeviceSelector{counterConfig.deviceSelector},
+		reqPath, "deviceSelector CEL compilation failed",
+	)
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	classSelectors, classErr := resolveAndCompileDeviceClass(ctx, cl, deviceClassName, classCache, reqPath, 0)
+	if classErr != nil {
+		return nil, field.ErrorList{classErr}
+	}
+
+	requestSelectors, compErrs := compileCELSelectors(selectors, reqPath.Child("exactly", "selectors"), "CEL compilation failed")
+	if len(compErrs) > 0 {
+		return nil, compErrs
+	}
+
+	var sliceList resourcev1.ResourceSliceList
+	if err := cl.List(ctx, &sliceList, client.MatchingFields{"spec.driver": counterConfig.driver}); err != nil {
+		return nil, field.ErrorList{field.InternalError(reqPath, fmt.Errorf("failed to list ResourceSlices: %w", err))}
+	}
+	pools := groupSlicesByPool(sliceList.Items, counterConfig.driver)
+
+	matched := matchDevicesWithSelectors(ctx, pools, counterConfig.driver, selectorSelectors, classSelectors, requestSelectors)
+	if int64(len(matched)) < count {
+		return nil, field.ErrorList{field.Invalid(
+			reqPath,
+			nil,
+			fmt.Sprintf("insufficient matching devices for counter driver: %d device(s) match but %d requested",
+				len(matched), count),
+		)}
+	}
+
+	charges := computeCounterCharges(counterConfig, quotaResource, matched, count)
+	if len(charges) > 0 {
+		return charges, nil
+	}
+
+	return nil, field.ErrorList{field.Invalid(
+		reqPath,
+		nil,
+		fmt.Sprintf("matched devices have no consumesCounters entry for counter %q", counterConfig.counterName),
+	)}
+}
+
+func matchDevicesWithSelectors(
+	ctx context.Context,
+	pools map[string]*poolInfo,
+	driver string,
+	selectorSelectors []dracel.CompilationResult,
+	classSelectors []dracel.CompilationResult,
+	requestSelectors []dracel.CompilationResult,
+) []resourcev1.Device {
+	log := ctrl.LoggerFrom(ctx)
+	var allMatched []resourcev1.Device
+	for _, pool := range pools {
+		if !pool.isComplete() {
+			continue
+		}
+		devices := pool.collectDevices()
+		for i := range devices {
+			dev := &devices[i]
+			celDev := dracel.Device{
+				Driver:     driver,
+				Attributes: dev.Attributes,
+				Capacity:   dev.Capacity,
+			}
+			if selectorMatch, err := evaluateSelectorsOnDevice(ctx, selectorSelectors, celDev); !selectorMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in deviceSelector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if classMatch, err := evaluateSelectorsOnDevice(ctx, classSelectors, celDev); !classMatch {
+				if err != nil {
+					log.V(3).Info("CEL evaluation error in DeviceClass selector", "device", dev.Name, "error", err)
+				}
+				continue
+			}
+			if requestMatch, err := evaluateSelectorsOnDevice(ctx, requestSelectors, celDev); requestMatch {
+				allMatched = append(allMatched, *dev)
+			} else if err != nil {
+				log.V(3).Info("CEL evaluation error in request selector", "device", dev.Name, "error", err)
+			}
+		}
```

  Yeah, I second this opinion.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T13:13:03Z

/lgtm
/approve
Since this is alpha feature I mostly focused on reviewing the isolation of the new code from the old code. 

In terms of the logic I rely mostly on the reviews from the DRA experts like @sohankunkerkar and @kannon92 . 

Still, I skimmed through and nothing really stood out. Proposed some logging improvements as follow ups, but nothing is blocking here for me.

Thank  you for the PR for DRA 👍
