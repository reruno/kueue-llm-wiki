# PR #10323: Add PreemptionEvictionToPendingSeconds metric

**Summary**: Add PreemptionEvictionToPendingSeconds metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10323

**Last updated**: 2026-04-21T19:26:41Z

---

## Metadata

- **State**: open
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `feature/10124` → `main`
- **Created**: 2026-04-08T08:39:48Z
- **Updated**: 2026-04-21T19:26:41Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note-none`, `size/XL`, `cncf-cla: yes`
- **Assignees**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@mykysha](https://github.com/mykysha), [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 9   **+526 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Adds PreemptionEvictionToPendingSeconds metric

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #10124

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-08T08:40:01Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cd207ce508912df6758e70451c8b2096b7f794e0 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e7cd1a29c4ee0008187c2e |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10323--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10323--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMzIzLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.ZkKuj0YOWIfEq0J5UawhJlrLxo6le11OvBN5vtQGHus)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10323--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-08T08:40:02Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10323#" title="Author self-approved">vladikkuzn</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [gabesaba](https://github.com/gabesaba) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["gabesaba"]} -->

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-08T08:40:36Z

/assign
/cc @mbobrovskyi @mykysha @mszadkow

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-10T08:39:32Z

/retest-required

### Comment by [@mykysha](https://github.com/mykysha) — 2026-04-15T12:18:34Z

Nit: I believe we can use doc comments in some places instead of named returns

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:21:08Z

looks pretty good, were you able to test it manually?

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T20:45:40Z

Metrics looks like this: 
<img width="880" height="677" alt="image" src="https://github.com/user-attachments/assets/803736e6-d635-44a3-ab7c-29b4427d24ab" />

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T14:42:27Z

/cc @mimowo @tenzen-y

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T18:07:23Z

Nice one, thank you 👍 

cc @ns-sundar @amy

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-08T12:31:22Z

- **pkg/controller/core/workload_controller.go:1041** by [@mwielgus](https://github.com/mwielgus) — 2026-04-08T12:30:30Z
```diff
@@ -1038,9 +1038,27 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	r.logger().V(2).Info("Workload delete event", "workload", klog.KObj(e.Object), "queue", e.Object.Spec.QueueName, "status", status)
 	r.preemptionExpectations.ObservedUID(r.logger(),
 		client.ObjectKeyFromObject(e.Object), e.Object.UID)
+	if !e.DeleteStateUnknown {
```

  Is it strictly necessary to exclude cases where DeleteStateUnknown is true? In controller-runtime, when an informer misses a deletion, e.Object still contains the last known state of the object, which might be perfectly valid and sufficient for extracting the preemption completion metric.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-09T14:14:28Z

- **pkg/controller/core/workload_controller.go:1041** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T14:10:10Z
```diff
@@ -1038,9 +1038,25 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	r.logger().V(2).Info("Workload delete event", "workload", klog.KObj(e.Object), "queue", e.Object.Spec.QueueName, "status", status)
 	r.preemptionExpectations.ObservedUID(r.logger(),
 		client.ObjectKeyFromObject(e.Object), e.Object.UID)
+	r.observePreemptionCompletionMetrics(e.Object)
```

  Why is it updated on workload delete? Preemption doesn't delete the Workload.

- **pkg/controller/core/workload_controller.go:1054** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T14:13:15Z
```diff
@@ -1038,9 +1038,25 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	r.logger().V(2).Info("Workload delete event", "workload", klog.KObj(e.Object), "queue", e.Object.Spec.QueueName, "status", status)
 	r.preemptionExpectations.ObservedUID(r.logger(),
 		client.ObjectKeyFromObject(e.Object), e.Object.UID)
+	r.observePreemptionCompletionMetrics(e.Object)
 	return true
 }
 
+func (r *WorkloadReconciler) observePreemptionCompletionMetrics(wl *kueue.Workload) {
+	c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted)
+	if c == nil || c.Status != metav1.ConditionTrue || c.Reason != kueue.WorkloadEvictedByPreemption {
+		return
+	}
+	var cqRef kueue.ClusterQueueReference
+	if wl.Status.Admission != nil && wl.Status.Admission.ClusterQueue != "" {
+		cqRef = wl.Status.Admission.ClusterQueue
+	} else {
+		cqRef = "unknown"
```

  If wl.Status.Admission is nil, cqRef defaults to "unknown". With the cleanup of 
  clears Status.Admission when a workload is preempted, would  hisresult in many
  "unknown" entries in the metrics?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-09T18:03:39Z

- **pkg/controller/core/workload_controller.go:1054** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-09T18:03:39Z
```diff
@@ -1038,9 +1038,25 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	r.logger().V(2).Info("Workload delete event", "workload", klog.KObj(e.Object), "queue", e.Object.Spec.QueueName, "status", status)
 	r.preemptionExpectations.ObservedUID(r.logger(),
 		client.ObjectKeyFromObject(e.Object), e.Object.UID)
+	r.observePreemptionCompletionMetrics(e.Object)
 	return true
 }
 
+func (r *WorkloadReconciler) observePreemptionCompletionMetrics(wl *kueue.Workload) {
+	c := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadEvicted)
+	if c == nil || c.Status != metav1.ConditionTrue || c.Reason != kueue.WorkloadEvictedByPreemption {
+		return
+	}
+	var cqRef kueue.ClusterQueueReference
+	if wl.Status.Admission != nil && wl.Status.Admission.ClusterQueue != "" {
+		cqRef = wl.Status.Admission.ClusterQueue
+	} else {
+		cqRef = "unknown"
```

  Agree, imo it should be read from oldWl.Status.Admission instead

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:35:49Z

- **pkg/cache/queue/cluster_queue.go:500** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:35:50Z
```diff
@@ -482,6 +482,23 @@ func (c *ClusterQueue) Pending() (int, int) {
 	return c.pendingActive(), c.pendingInadmissible()
 }
 
+// PendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible, matching Pending() counts. Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) PendingWorkloadInfos() (active []*workload.Info, inadmissible []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	active = make([]*workload.Info, 0, c.heap.Len()+1)
+	active = append(active, c.heap.List()...)
+	if c.inflight != nil {
+		active = append(active, c.inflight)
+	}
+	inadmissible = make([]*workload.Info, 0, c.inadmissibleWorkloads.len())
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmissible = append(inadmissible, wi)
+	}
+	return active, inadmissible
+}
```

  ```suggestion
  func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
  	c.rwm.RLock()
  	defer c.rwm.RUnlock()
  	active := c.heap.List()
  	if c.inflight != nil {
  		active = append(active, c.inflight)
  	}
  	return active, slices.Collect(maps.Values(c.inadmissibleWorkloads))
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:38:29Z

- **pkg/cache/queue/metrics.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:38:29Z
```diff
@@ -43,12 +48,36 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.PendingWorkloadInfos()
+	active, inadmissible := len(activeInfos), len(inadmissibleInfos)
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+		inadmissible = len(inadmissibleInfos)
 		active = 0
 	}
-	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, m.customLabels.CQGet(cq.name), m.roleTracker)
+	cl := m.clock
```

  Looks like redundant variable.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:40:56Z

- **pkg/cache/queue/metrics.go:67** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:40:56Z
```diff
@@ -43,12 +48,36 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.PendingWorkloadInfos()
+	active, inadmissible := len(activeInfos), len(inadmissibleInfos)
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+		inadmissible = len(inadmissibleInfos)
 		active = 0
 	}
-	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, m.customLabels.CQGet(cq.name), m.roleTracker)
+	cl := m.clock
+	custom := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, custom, m.roleTracker)
+	activeMax, activeMean := pendingWaitStats(activeInfos, cl)
+	inadmissibleMax, inadmissibleMean := pendingWaitStats(inadmissibleInfos, cl)
+	metrics.ReportPendingWorkloadWaitTimes(cq.name, activeMax, activeMean, inadmissibleMax, inadmissibleMean, custom, m.roleTracker)
+}
+
+func pendingWaitStats(infos []*workload.Info, cl clock.Clock) (maxSeconds, meanSeconds float64) {
```

  ```suggestion
  func pendingWaitStats(infos []*workload.Info, cl clock.Clock) (float64, float64) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:47:05Z

- **pkg/cache/queue/metrics.go:52** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:47:05Z
```diff
@@ -43,12 +48,36 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.PendingWorkloadInfos()
+	active, inadmissible := len(activeInfos), len(inadmissibleInfos)
```

  I think we can calculate it after if.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:48:12Z

- **pkg/cache/queue/metrics.go:61** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:48:12Z
```diff
@@ -43,12 +48,36 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.PendingWorkloadInfos()
+	active, inadmissible := len(activeInfos), len(inadmissibleInfos)
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+		inadmissible = len(inadmissibleInfos)
 		active = 0
 	}
-	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, m.customLabels.CQGet(cq.name), m.roleTracker)
+	cl := m.clock
+	custom := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, custom, m.roleTracker)
```

  ```suggestion
  	metrics.ReportPendingWorkloads(cq.name, len(activeInfos), len(inadmissibleInfos), custom, m.roleTracker)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:51:15Z

- **pkg/metrics/metrics.go:982** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:51:16Z
```diff
@@ -913,6 +956,33 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPendingWorkloadWaitTimes sets max and mean queued-wait time (seconds) for pending workloads
+// per status bucket, matching kueue_pending_workloads partitioning.
+func ReportPendingWorkloadWaitTimes(
+	cqName kueue.ClusterQueueReference,
+	activeMaxWait, activeMeanWait, inadmissibleMaxWait, inadmissibleMeanWait float64,
+	customLabelValues []string,
+	tracker *roletracker.RoleTracker,
+) {
+	role := roletracker.GetRole(tracker)
+	activeLabels := append([]string{string(cqName), PendingStatusActive, role}, customLabelValues...)
+	inadmissibleLabels := append([]string{string(cqName), PendingStatusInadmissible, role}, customLabelValues...)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMeanWait)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMeanWait)
+}
+
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	if seconds < 0 {
+		seconds = 0
+	}
```

  Is it possible?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-15T18:08:06Z

- **pkg/metrics/metrics.go:982** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-15T18:08:06Z
```diff
@@ -913,6 +956,33 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPendingWorkloadWaitTimes sets max and mean queued-wait time (seconds) for pending workloads
+// per status bucket, matching kueue_pending_workloads partitioning.
+func ReportPendingWorkloadWaitTimes(
+	cqName kueue.ClusterQueueReference,
+	activeMaxWait, activeMeanWait, inadmissibleMaxWait, inadmissibleMeanWait float64,
+	customLabelValues []string,
+	tracker *roletracker.RoleTracker,
+) {
+	role := roletracker.GetRole(tracker)
+	activeLabels := append([]string{string(cqName), PendingStatusActive, role}, customLabelValues...)
+	inadmissibleLabels := append([]string{string(cqName), PendingStatusInadmissible, role}, customLabelValues...)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMeanWait)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMeanWait)
+}
+
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	if seconds < 0 {
+		seconds = 0
+	}
```

  Added a comment:
  ```
  // Negative duration is possible if the condition's LastTransitionTime is after the
  // controller clock (clock skew, delayed watches). Clamp to zero for the histogram.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T05:53:49Z

- **pkg/metrics/metrics.go:982** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T05:53:49Z
```diff
@@ -913,6 +956,33 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPendingWorkloadWaitTimes sets max and mean queued-wait time (seconds) for pending workloads
+// per status bucket, matching kueue_pending_workloads partitioning.
+func ReportPendingWorkloadWaitTimes(
+	cqName kueue.ClusterQueueReference,
+	activeMaxWait, activeMeanWait, inadmissibleMaxWait, inadmissibleMeanWait float64,
+	customLabelValues []string,
+	tracker *roletracker.RoleTracker,
+) {
+	role := roletracker.GetRole(tracker)
+	activeLabels := append([]string{string(cqName), PendingStatusActive, role}, customLabelValues...)
+	inadmissibleLabels := append([]string{string(cqName), PendingStatusInadmissible, role}, customLabelValues...)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMeanWait)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMeanWait)
+}
+
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	if seconds < 0 {
+		seconds = 0
+	}
```

  Yeah, but is it possible for LastTransitionTime to be later than clock.Now()?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:00:17Z

- **pkg/cache/queue/metrics.go:74** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:00:17Z
```diff
@@ -43,12 +48,32 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.PendingWorkloadInfos()
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
-		active = 0
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+	}
+	custom := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, len(activeInfos), len(inadmissibleInfos), custom, m.roleTracker)
+	activeMax, activeMean := pendingWaitStats(activeInfos, m.clock)
+	inadmissibleMax, inadmissibleMean := pendingWaitStats(inadmissibleInfos, m.clock)
+	metrics.ReportPendingWorkloadWaitTimes(cq.name, activeMax, activeMean, inadmissibleMax, inadmissibleMean, custom, m.roleTracker)
+}
+
+func pendingWaitStats(infos []*workload.Info, cl clock.Clock) (float64, float64) {
+	if len(infos) == 0 {
+		return 0, 0
+	}
+	var maxD time.Duration
+	var sum time.Duration
+	for _, wi := range infos {
+		d := workload.QueuedWaitTime(wi.Obj, cl)
+		sum += d
+		if d > maxD {
+			maxD = d
+		}
```

  ```suggestion
  		maxD = max(d, maxD)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:02:27Z

- **pkg/cache/queue/cluster_queue.go:488** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:02:27Z
```diff
@@ -482,6 +483,18 @@ func (c *ClusterQueue) Pending() (int, int) {
 	return c.pendingActive(), c.pendingInadmissible()
 }
 
+// PendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible, matching Pending() counts. Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) PendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
```

  ```suggestion
  func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:11:45Z

- **pkg/cache/queue/cluster_queue.go:482** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:11:45Z
```diff
@@ -482,6 +483,18 @@ func (c *ClusterQueue) Pending() (int, int) {
 	return c.pendingActive(), c.pendingInadmissible()
```

  It looks like we’re duplicating logic. Can we reuse pendingWorkloadInfos() instead?
  
  ```suggestion
  	activeInfos, inadmissibleInfos := c.pendingWorkloadInfos()
  	return len(activeInfos), len(inadmissibleInfos)
  ```
  
  In that case we don't need pendingActive() and pendingInadmissible().
  
  WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:17:04Z

- **pkg/controller/core/workload_controller.go:1095** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:17:04Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
+	if prevStatus != workload.StatusQuotaReserved && prevStatus != workload.StatusAdmitted {
+		return "", 0, false
+	}
+	if newStatus != workload.StatusPending {
+		return "", 0, false
+	}
```

  ```suggestion
  ```
  
  Do we need to double check this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:36:31Z

- **pkg/controller/core/workload_controller.go:1089** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:18:23Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
```

  ```suggestion
  func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload now time.Time) (kueue.ClusterQueueReference, time.Duration, bool) {
  ```

- **pkg/controller/core/workload_controller.go:1104** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:27:09Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
+	if prevStatus != workload.StatusQuotaReserved && prevStatus != workload.StatusAdmitted {
+		return "", 0, false
+	}
+	if newStatus != workload.StatusPending {
+		return "", 0, false
+	}
+	c := apimeta.FindStatusCondition(newWl.Status.Conditions, kueue.WorkloadEvicted)
+	if c == nil || c.Status != metav1.ConditionTrue || c.Reason != kueue.WorkloadEvictedByPreemption {
+		return "", 0, false
+	}
+	if oldWl.Status.Admission != nil && oldWl.Status.Admission.ClusterQueue != "" {
+		cq = oldWl.Status.Admission.ClusterQueue
+	} else {
+		cq = "unknown"
+	}
```

  If we don't have cq I think we should skip report.

- **pkg/controller/core/workload_controller.go:1104** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:27:56Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
+	if prevStatus != workload.StatusQuotaReserved && prevStatus != workload.StatusAdmitted {
+		return "", 0, false
+	}
+	if newStatus != workload.StatusPending {
+		return "", 0, false
+	}
+	c := apimeta.FindStatusCondition(newWl.Status.Conditions, kueue.WorkloadEvicted)
+	if c == nil || c.Status != metav1.ConditionTrue || c.Reason != kueue.WorkloadEvictedByPreemption {
+		return "", 0, false
+	}
+	if oldWl.Status.Admission != nil && oldWl.Status.Admission.ClusterQueue != "" {
+		cq = oldWl.Status.Admission.ClusterQueue
+	} else {
+		cq = "unknown"
+	}
```

  I think this function does too much. It would be helpful to add a workload.WorkloadClusterQueue(oldWl) helper and use it outside this function.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-16T06:37:36Z

- **test/e2e/singlecluster/metrics_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-16T06:37:36Z
  Could you please add integration tests?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T19:38:15Z

- **test/e2e/singlecluster/metrics_test.go:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T19:38:15Z
  Is it worth putting those under `feature:prometheus` test tag?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T20:58:05Z

- **pkg/metrics/metrics.go:982** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T20:58:05Z
```diff
@@ -913,6 +956,33 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPendingWorkloadWaitTimes sets max and mean queued-wait time (seconds) for pending workloads
+// per status bucket, matching kueue_pending_workloads partitioning.
+func ReportPendingWorkloadWaitTimes(
+	cqName kueue.ClusterQueueReference,
+	activeMaxWait, activeMeanWait, inadmissibleMaxWait, inadmissibleMeanWait float64,
+	customLabelValues []string,
+	tracker *roletracker.RoleTracker,
+) {
+	role := roletracker.GetRole(tracker)
+	activeLabels := append([]string{string(cqName), PendingStatusActive, role}, customLabelValues...)
+	inadmissibleLabels := append([]string{string(cqName), PendingStatusInadmissible, role}, customLabelValues...)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMeanWait)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMeanWait)
+}
+
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	if seconds < 0 {
+		seconds = 0
+	}
```

  Yes, it can happen in real clusters, because:
  - LastTransitionTime is set using the apiserver’s notion of time when the object is written.
  - r.clock.Now() is the controller’s clock.
  - Skew between control-plane and node can make now.Sub(LastTransitionTime) negative.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T21:00:26Z

- **pkg/controller/core/workload_controller.go:1095** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T21:00:26Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
+	if prevStatus != workload.StatusQuotaReserved && prevStatus != workload.StatusAdmitted {
+		return "", 0, false
+	}
+	if newStatus != workload.StatusPending {
+		return "", 0, false
+	}
```

  I would leave it if somebody uses this function in different place

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-16T21:08:07Z

- **test/integration/singlecluster/controller/core/workload_preemption_eviction_metrics_test.go:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-16T21:08:08Z
  Is it better to put this test closer to metrics related tests?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T13:29:19Z

- **pkg/metrics/metrics.go:982** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T13:29:19Z
```diff
@@ -913,6 +956,33 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPendingWorkloadWaitTimes sets max and mean queued-wait time (seconds) for pending workloads
+// per status bucket, matching kueue_pending_workloads partitioning.
+func ReportPendingWorkloadWaitTimes(
+	cqName kueue.ClusterQueueReference,
+	activeMaxWait, activeMeanWait, inadmissibleMaxWait, inadmissibleMeanWait float64,
+	customLabelValues []string,
+	tracker *roletracker.RoleTracker,
+) {
+	role := roletracker.GetRole(tracker)
+	activeLabels := append([]string{string(cqName), PendingStatusActive, role}, customLabelValues...)
+	inadmissibleLabels := append([]string{string(cqName), PendingStatusInadmissible, role}, customLabelValues...)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(activeLabels...).Set(activeMeanWait)
+	PendingWorkloadMaxWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMaxWait)
+	PendingWorkloadMeanWaitTimeSeconds.WithLabelValues(inadmissibleLabels...).Set(inadmissibleMeanWait)
+}
+
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	if seconds < 0 {
+		seconds = 0
+	}
```

  OK, I think adding an additional check wouldn’t hurt.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T13:31:32Z

- **pkg/controller/core/workload_controller.go:1095** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T13:31:32Z
```diff
@@ -1090,6 +1083,28 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// cluster_queue is taken from oldWl's admission; latency is now minus the WorkloadEvicted condition's
+// LastTransitionTime on newWl (preemption reason).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, prevStatus, newStatus string, now time.Time) (cq kueue.ClusterQueueReference, latency time.Duration, ok bool) {
+	if prevStatus != workload.StatusQuotaReserved && prevStatus != workload.StatusAdmitted {
+		return "", 0, false
+	}
+	if newStatus != workload.StatusPending {
+		return "", 0, false
+	}
```

  Can we move `(prevStatus == workload.StatusQuotaReserved || prevStatus == workload.StatusAdmitted) && status == workload.StatusPending` to some helper? We can reuse it in both places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-17T13:38:10Z

- **test/e2e/singlecluster/metrics_test.go:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-17T13:38:10Z
  Not sure – I don’t see this label in the integration tests.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T13:46:44Z

- **test/e2e/singlecluster/metrics_test.go:1** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T13:46:44Z
  Oh, yeah, good point, it's only for e2e

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-17T14:46:48Z

- **pkg/metrics/metrics.go:97** by [@mimowo](https://github.com/mimowo) — 2026-04-17T14:46:48Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  Instead of the two metrics could we rather publish the Historgram metric, then we could explain users how to compute, mean/max/median/95% etc using PromQL in here https://kueue.sigs.k8s.io/docs/tasks/manage/observability/common_grafana_queries/

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-17T14:52:04Z

- **pkg/metrics/metrics.go:97** by [@mimowo](https://github.com/mimowo) — 2026-04-17T14:52:04Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  Ah, I think I see, the metric is just for those currently Pending, so it is "guage" in nature, rather than cummulative like histogram or counter. 
  
  I see the case, still wondering if people solve similar problems somewhat more generically, basically I would love to avoid soon introducing "95%" metric, but for now no better idea from me.

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T14:56:59Z

- **pkg/metrics/metrics.go:97** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T14:56:59Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  Yeah, I though to introduce such metrics per WL, but this will blow up quickly I think

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-17T15:06:23Z

- **pkg/metrics/metrics.go:97** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-17T15:06:23Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  (cardinality explosion)

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-04-20T06:37:07Z

- **pkg/cache/queue/cluster_queue.go:483** by [@dkaluza](https://github.com/dkaluza) — 2026-04-17T14:42:42Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
```

  nit: we are increasing the memory usage with the new implementation, previous one used constant memory IIUC

- **pkg/controller/core/workload_controller.go:1099** by [@dkaluza](https://github.com/dkaluza) — 2026-04-17T16:53:04Z
```diff
@@ -1093,6 +1093,26 @@ func (r *WorkloadReconciler) Delete(e event.TypedDeleteEvent[*kueue.Workload]) b
 	return true
 }
 
+// preemptionEvictionToPendingObserveInput determines whether to record preemption eviction→pending latency.
+// Latency is now minus the WorkloadEvicted condition's LastTransitionTime on newWl (preemption reason).
+// Returns ok when status.admission.cluster_queue on oldWl is set and non-empty (see workload.WorkloadClusterQueue).
+func preemptionEvictionToPendingObserveInput(oldWl, newWl *kueue.Workload, now time.Time) (kueue.ClusterQueueReference, time.Duration, bool) {
```

  nit: this function documentation is unclear. It looks like it has multiple responsibilities - it is computing the time difference and returning from which clusterqueue wl was evicted.
  
  Consider separating those to make the code simpler.
  We can just have a function that returns boolean if workload was evicted and transformed to pending state and easily compute the sub and retrieve the cq in the calling code.
  
  ```
  func isPreemptionEvictionToPending(oldWl, newWl *kueue.Workload) bool {
  	prevStatus := workload.Status(oldWl)
  	newStatus := workload.Status(newWl)
  	if !workload.FromQuotaReservedOrAdmittedToPending(prevStatus, newStatus) {
  		return false
  	}
  	c := apimeta.FindStatusCondition(newWl.Status.Conditions, kueue.WorkloadEvicted)
  	if c == nil || c.Status != metav1.ConditionTrue || c.Reason != kueue.WorkloadEvictedByPreemption {
  		return  false
  	}
  	cq, found := workload.WorkloadClusterQueue(oldWl)
  	if !found {
  		return  false
  	}
  	return true
  ```
  
  And then in the calling place:
  ```
  if cq, found := workload.WorkloadClusterQueue(e.ObjectOld); found && preemptionEvictionToPendingObserveInput(e.ObjectOld, e.ObjectNew) {
              latency := r.clock.Now().Sub(c.LastTransitionTime.Time)
  			metrics.ReportPreemptionEvictionToPendingTime(cq, latency, r.customLabels.CQGet(cq), r.roleTracker)
  		}
  ```

- **pkg/controller/core/preemption_eviction_to_pending_test.go:82** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T05:39:06Z
```diff
@@ -0,0 +1,222 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package core
+
+import (
+	"testing"
+	"time"
+
+	"github.com/google/go-cmp/cmp"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+)
+
+func TestPreemptionEvictionToPendingObserveInput(t *testing.T) {
+	evictTime := time.Date(2024, 3, 15, 12, 0, 0, 0, time.UTC)
+	now := evictTime.Add(30 * time.Second)
+
+	preemptedEvicted := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		Reason:             kueue.WorkloadEvictedByPreemption,
+		LastTransitionTime: metav1.NewTime(evictTime),
+	}
+	otherEvicted := metav1.Condition{
+		Type:               kueue.WorkloadEvicted,
+		Status:             metav1.ConditionTrue,
+		Reason:             kueue.WorkloadEvictedByPodsReadyTimeout,
+		LastTransitionTime: metav1.NewTime(evictTime),
+	}
+	admittedTrue := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.NewTime(evictTime),
+	}
+	quotaReservedTrue := metav1.Condition{
+		Type:               kueue.WorkloadQuotaReserved,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.NewTime(evictTime),
+	}
+
+	cases := []struct {
+		name        string
+		oldWl       *kueue.Workload
+		newWl       *kueue.Workload
+		wantOK      bool
+		wantCQ      kueue.ClusterQueueReference
+		wantLatency time.Duration
+	}{
+		{
+			name: "emit admitted to pending with cq from old admission",
+			oldWl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Admission:  &kueue.Admission{ClusterQueue: "cq-a"},
+					Conditions: []metav1.Condition{admittedTrue},
+				},
+			},
+			newWl: &kueue.Workload{
+				Status: kueue.WorkloadStatus{
+					Conditions: []metav1.Condition{preemptedEvicted},
+				},
+			},
+			wantOK:      true,
+			wantCQ:      "cq-a",
+			wantLatency: 30 * time.Second,
+		},
+		{
+			name: "emit quota reserved to pending",
```

  Is preempted evicted the only reason of workload being in pending status?
  
  If i understand correctly there can be also other reasons why workload is pending, e.g. admission check failing.
  
  Maybe it is worth to be more accurate in the name to avoid confusion.

- **pkg/metrics/metrics.go:669** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T05:59:49Z
```diff
@@ -628,6 +660,17 @@ The label 'reason' can have the following values:
 		}, append([]string{"preempting_cluster_queue", "reason", "replica_role"}, extraLabels...),
 	)
 
+	PreemptionEvictionToPendingSeconds = prometheus.NewHistogramVec(
+		prometheus.HistogramOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preemption_eviction_to_pending_seconds",
+			Help: `The time from preemption eviction (WorkloadEvicted with reason Preempted) until the preemptee workload returns to Pending (quota released).
+Observed on status transition from admitted or quota-reserved to pending while still evicted by preemption.
+Uses the eviction condition LastTransitionTime on the updated object as start; cluster_queue is taken from status.admission.cluster_queue on the pre-update object when set and non-empty (otherwise the histogram is not recorded).`,
```

  ```suggestion
  Uses the eviction condition LastTransitionTime on the updated object as start; cluster_queue is taken from status.admission.cluster_queue on the pre-update object when set and non-empty (otherwise the entry is not recorded).`,
  ```
  
  nit: A single emission from one workload is not a hist, it is just a latency value. They create a histogram after aggregation with other entries.

- **pkg/cache/queue/metrics.go:60** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T06:11:50Z
```diff
@@ -43,12 +48,30 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.pendingWorkloadInfos()
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
-		active = 0
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+	}
+	custom := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, len(activeInfos), len(inadmissibleInfos), custom, m.roleTracker)
+	activeMax, activeMean := pendingWaitStats(activeInfos, m.clock)
+	inadmissibleMax, inadmissibleMean := pendingWaitStats(inadmissibleInfos, m.clock)
+	metrics.ReportPendingWorkloadWaitTimes(cq.name, activeMax, activeMean, inadmissibleMax, inadmissibleMean, custom, m.roleTracker)
```

  Reporting 0 mean and 0 max when there are no workloads for me is highly misleading.  IMO it would be best to not report metric entries in this case to indicate no data - this can be probably easily fixed if we switch to hist based pending wait times.

- **pkg/metrics/metrics.go:999** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T06:18:22Z
```diff
@@ -968,6 +1041,7 @@ func ClearClusterQueueMetrics(cq kueue.ClusterQueueReference) {
 	EvictedWorkloadsTotal.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
 	EvictedWorkloadsOnceTotal.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
 	PreemptedWorkloadsTotal.DeletePartialMatch(prometheus.Labels{"preempting_cluster_queue": cqName})
+	PreemptionEvictionToPendingSeconds.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
```

  Why are we not clearing other pending workload metrics for cq here?

- **test/e2e/singlecluster/metrics_test.go:186** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T06:32:28Z
```diff
@@ -180,6 +182,8 @@ var _ = ginkgo.Describe("Metrics", ginkgo.Label("area:singlecluster", "feature:m
 
 			deletedMetrics := [][]string{
 				{"kueue_pending_workloads", clusterQueue.Name},
+				{"kueue_pending_workload_max_wait_time_seconds", clusterQueue.Name},
+				{"kueue_pending_workload_mean_wait_time_seconds", clusterQueue.Name},
```

  What about preemption metric?

- **site/content/en/docs/reference/metrics.md:53** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T06:34:06Z
```diff
@@ -45,9 +45,12 @@ Use the following metrics to monitor the status of your ClusterQueues:
 | `kueue_evicted_workloads_total` | Counter | The number of evicted workloads per 'cluster_queue',<br>The label 'reason' can have the following values:<br>- "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.<br>- "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.<br>- "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.<br>- "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.<br>- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.<br>- "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.<br>- "Deactivated" means that the workload was evicted because spec.active is set to false.<br>The label 'underlying_cause' can have the following values:<br>- "" means that the value in 'reason' label is the root cause for eviction.<br>- "AdmissionCheck" means that the workload was evicted by Kueue due to a rejected admission check.<br>- "MaximumExecutionTimeExceeded" means that the workload was evicted by Kueue due to maximum execution time exceeded.<br>- "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded. | `cluster_queue`: the name of the ClusterQueue<br> `reason`: eviction or preemption reason<br> `underlying_cause`: root cause for eviction<br> `priority_class`: the priority class name<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_finished_workloads` | Gauge | The number of finished workloads per 'cluster_queue'. | `cluster_queue`: the name of the ClusterQueue<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_finished_workloads_total` | Counter | The total number of finished workloads per 'cluster_queue' | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the priority class name<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
+| `kueue_pending_workload_max_wait_time_seconds` | Gauge | The maximum time in seconds that any still-pending workload in the ClusterQueue has been waiting since creation or last requeue (same basis as kueue_admission_wait_time_seconds), per 'status'.<br>'status' matches kueue_pending_workloads: "active" (heap + inflight) or "inadmissible". | `cluster_queue`: the name of the ClusterQueue<br> `status`: one of `active` or `inadmissible` (same as kueue_pending_workloads)<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
+| `kueue_pending_workload_mean_wait_time_seconds` | Gauge | The mean time in seconds that pending workloads in the ClusterQueue have been waiting since creation or last requeue (same basis as kueue_admission_wait_time_seconds), per 'status'.<br>Zero when there are no workloads in that bucket. 'status' matches kueue_pending_workloads. | `cluster_queue`: the name of the ClusterQueue<br> `status`: one of `active` or `inadmissible` (same as kueue_pending_workloads)<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_pending_workloads` | Gauge | The number of pending workloads, per 'cluster_queue' and 'status'.<br>'status' can have the following values:<br>- "active" means that the workloads are in the admission queue.<br>- "inadmissible" means there was a failed admission attempt for these workloads and they won't be retried until cluster conditions, which could make this workload admissible, change | `cluster_queue`: the name of the ClusterQueue<br> `status`: status label (varies by metric)<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_pods_ready_to_evicted_time_seconds` | Histogram | The number of seconds between a workload's pods being ready and eviction workloads per 'cluster_queue',<br>The label 'reason' can have the following values:<br>- "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.<br>- "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.<br>- "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.<br>- "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.<br>- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.<br>- "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.<br>- "Deactivated" means that the workload was evicted because spec.active is set to false.<br>The label 'underlying_cause' can have the following values:<br>- "" means that the value in 'reason' label is the root cause for eviction.<br>- "AdmissionCheck" means that the workload was evicted by Kueue due to a rejected admission check.<br>- "MaximumExecutionTimeExceeded" means that the workload was evicted by Kueue due to maximum execution time exceeded.<br>- "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded. | `cluster_queue`: the name of the ClusterQueue<br> `reason`: eviction or preemption reason<br> `underlying_cause`: root cause for eviction<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_preempted_workloads_total` | Counter | The number of preempted workloads per 'preempting_cluster_queue',<br>The label 'reason' can have the following values:<br>- "InClusterQueue" means that the workload was preempted by a workload in the same ClusterQueue.<br>- "InCohortReclamation" means that the workload was preempted by a workload in the same cohort due to reclamation of nominal quota.<br>- "InCohortFairSharing" means that the workload was preempted by a workload in the same cohort Fair Sharing.<br>- "InCohortReclaimWhileBorrowing" means that the workload was preempted by a workload in the same cohort due to reclamation of nominal quota while borrowing. | `preempting_cluster_queue`: the ClusterQueue executing preemption<br> `reason`: eviction or preemption reason<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
+| `kueue_preemption_eviction_to_pending_seconds` | Histogram | The time from preemption eviction (WorkloadEvicted with reason Preempted) until the preemptee workload returns to Pending (quota released).<br>Observed on status transition from admitted or quota-reserved to pending while still evicted by preemption.<br>Uses the eviction condition LastTransitionTime on the updated object as start; cluster_queue is taken from status.admission.cluster_queue on the pre-update object when set and non-empty (otherwise the histogram is not recorded). | `cluster_queue`: the preempted workload's ClusterQueue from status.admission before quota was released, or `unknown` if unset<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
```

  Can the cq really be `unknown` here? Aren't we checking that cq exists with every emission?

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-04-20T06:52:10Z

- **pkg/metrics/metrics.go:97** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T06:52:09Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  But cannot gauge histogram be easily achieved by histogram and rate expression?
  
  Gauge histograms are [mentioned](https://prometheus.io/docs/concepts/metric_types/#histogram) in Prometheus as achievable this way. And functions like [quantiles](https://prometheus.io/docs/prometheus/latest/querying/functions/#histogram_quantile) support that IIUC.
  
  However getting an accurate max value can be tricky in this way

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T07:16:20Z

- **pkg/cache/queue/cluster_queue.go:493** by [@mimowo](https://github.com/mimowo) — 2026-04-20T07:16:20Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
+	return len(active), len(inadmissible)
 }
 
-// pendingActive returns the number of active pending workloads,
-// workloads that are in the admission queue.
-func (c *ClusterQueue) pendingActive() int {
-	result := c.heap.Len()
+// pendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible.
+// Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	active := c.heap.List()
```

  Actually, I'm concerned about the performance impact of this change, because this is something we call on every scheduling cycle via reportCQPendingWorkloads. So, effectively it makes the use of heap useless, because we do  Pop head with O(1) just in the next function call to list all workloads .
  I'm not sure if this would show up in production use, but this is risky change actually.
  
  I'm wondering a better design for such heavy metrics (and maybe actually all metrics) would be to trigger their computation asynchronously, similarly as we trigger the requeue asynchronously, using the workqueue. Then here we would just say enqueue the computation task, that would also update the metric at the at. For reference check how we do it with workload [requeuer](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/cache/queue/inadmissible_workloads.go#L243).
  
  This is ok, because Prometheus metrics are scraped only with 15s intervals, so users anyway need to understand they might be lagging a little bit.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T07:27:07Z

- **pkg/metrics/metrics.go:97** by [@mimowo](https://github.com/mimowo) — 2026-04-20T07:27:07Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  > Gauge histograms are mentioned in Prometheus as achievable this way. And functions like quantiles support that IIUC.
  
  If there is something standard then great, that should be investigated more.
  
  Otherwise I'm ok to consolidate the two metrics into `PendingWorkloadsWaitSeconds{quantile: 0.5|0.95|0.99}`.
  Then PendingWorkloadsWaitSeconds with 0.5 quantile would effectively be covering the Average use case, while PendingWorkloadsWaitSeconds 0.99 would cover the  PendingWorkloadMaxWaitTimeSeconds. 
  
  > However getting an accurate max value can be tricky in this way
  
  I wouldn't worry about the max metrics, I think it can be approximated pretty well in the standard way by 0.99 or 0.999 percentile, effectively.

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-04-20T08:27:06Z

- **pkg/metrics/metrics.go:97** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T08:27:06Z
```diff
@@ -88,6 +88,14 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="status label (varies by metric)",replica_role="one of `leader`, `follower`, or `standalone`"
 	PendingWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMaxWaitTimeSeconds *prometheus.GaugeVec
+
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",status="one of `active` or `inadmissible` (same as kueue_pending_workloads)",replica_role="one of `leader`, `follower`, or `standalone`"
+	PendingWorkloadMeanWaitTimeSeconds *prometheus.GaugeVec
```

  > I wouldn't worry about the max metrics, I think it can be approximated pretty well in the standard way by 0.99 or 0.999 percentile, effectively.
  
  Fully agree

### Review by [@dkaluza](https://github.com/dkaluza) (commented) — 2026-04-20T17:17:14Z

- **pkg/cache/queue/cluster_queue.go:493** by [@dkaluza](https://github.com/dkaluza) — 2026-04-20T17:17:13Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
+	return len(active), len(inadmissible)
 }
 
-// pendingActive returns the number of active pending workloads,
-// workloads that are in the admission queue.
-func (c *ClusterQueue) pendingActive() int {
-	result := c.heap.Len()
+// pendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible.
+// Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	active := c.heap.List()
```

  From my point of view, the problem is not with metrics in general, but with metrics that are describing a currently changing state like "live latency" of pending workloads. The fact that those are live requires recomputation here as some time delta occurred.
  
  I wouldn't generalize this to all of the metrics as in many situations we have the exact value in hand - e.g. when workload transitions the state from pending to admitted and we are reporting number of pending workloads. Or when we are reporting latency on status change.
  Going through historic status changes after the event IMO woukd lead only to unnecessary additional computation.
  
  Here though a job measuring the values every X seconds sounds like a good solution.
  
  The other that I see is to remove the "live" factor from them and report them on status change, in this case they will no longer change, and therefore no iteration is needrd. However it has a downside of larger lag and lack of visibility if they actually never change the status (besides count).
  
  3rd approach is to try to optimize this with some dedicated structure, however I do not think it is worth it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T17:45:20Z

- **pkg/cache/queue/cluster_queue.go:493** by [@mimowo](https://github.com/mimowo) — 2026-04-20T17:45:20Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
+	return len(active), len(inadmissible)
 }
 
-// pendingActive returns the number of active pending workloads,
-// workloads that are in the admission queue.
-func (c *ClusterQueue) pendingActive() int {
-	result := c.heap.Len()
+// pendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible.
+// Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	active := c.heap.List()
```

  > I wouldn't generalize this to all of the metrics as in many situations we have the exact value in hand - e.g. when workload transitions the state from pending to admitted and we are reporting number of pending workloads. Or when we are reporting latency on status change.
  Going through historic status changes after the event IMO woukd lead only to unnecessary additional computation.
  
  Agree, it may not be silver bullet, but probably there are more metrics which would fit into this category that delayed computation is ok, for example the pending_workloads guage metric. 
  
  > Here though a job measuring the values every X seconds sounds like a good solution.
  
  Indeed, this is were I'm leaning to now, so thank you for confirming. 
  
  Also, we recently wanted to add priority_class to pending_workloads guage metric with @mbobrovskyi and this turned out very tricky to make performance reasons - in the previous "sync" approach it requires predefined tricky structures bucketing by priority, and so complexity is big. With this new async approach we could have all the time in the world to aggregate all the listed workloads per priority.
  
  For reference: https://github.com/kubernetes-sigs/kueue/issues/8249
  
  > The other that I see is to remove the "live" factor from them and report them on status change, in this case they will no longer change, and therefore no iteration is needrd. However it has a downside of larger lag and lack of visibility if they actually never change the status (besides count).
  
  Maybe this would work sensibly in practice, but it would not give good results when there is no status change on CQ for 30min, because all workloads continue computations, and no new workloads are submitted. Then we would show the stale pending_wait_time, while the waiting time actually increased by 30min. It would be worth-considering alternative if we couldn't just make it async for some reason.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:14:32Z

- **pkg/cache/queue/cluster_queue.go:492** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:14:32Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
+	return len(active), len(inadmissible)
 }
 
-// pendingActive returns the number of active pending workloads,
-// workloads that are in the admission queue.
-func (c *ClusterQueue) pendingActive() int {
-	result := c.heap.Len()
+// pendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible.
+// Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
```

  Why are we locking it again in this function? We already have a lock in the calling functions.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T18:05:26Z

- **pkg/metrics/metrics.go:940** by [@mimowo](https://github.com/mimowo) — 2026-04-21T18:05:26Z
```diff
@@ -913,6 +928,19 @@ func ReportPendingWorkloads(cqName kueue.ClusterQueueReference, active, inadmiss
 	PendingWorkloads.WithLabelValues(inadmissibleLabels...).Set(float64(inadmissible))
 }
 
+// ReportPreemptionEvictionToPendingTime records latency from preemption eviction until the preemptee workload returns to Pending (quota released).
+func ReportPreemptionEvictionToPendingTime(cqName kueue.ClusterQueueReference, latency time.Duration, customLabelValues []string, tracker *roletracker.RoleTracker) {
+	labels := append([]string{string(cqName), roletracker.GetRole(tracker)}, customLabelValues...)
+	seconds := latency.Seconds()
+	// LastTransitionTime comes from the API object (apiserver clock). This path compares it to the
+	// controller's clock.Now(), so skew between apiserver and controller (or delayed watch delivery)
+	// can make the transition time appear after "now" and yield a negative duration. Clamp to zero.
+	if seconds < 0 {
+		seconds = 0
+	}
```

  nit, just inline `seconds = max(0, latency.Seconds())`

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-21T18:45:50Z

- **pkg/cache/queue/cluster_queue.go:483** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-21T18:45:50Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
```

  This metric is moved to a separate PR: https://github.com/kubernetes-sigs/kueue/pull/10633

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-21T18:46:00Z

- **pkg/cache/queue/metrics.go:60** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-21T18:46:00Z
```diff
@@ -43,12 +48,30 @@ func reportPendingWorkloads(m *Manager, cqRef kueue.ClusterQueueReference) {
 }
 
 func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
-	active, inadmissible := cq.Pending()
+	activeInfos, inadmissibleInfos := cq.pendingWorkloadInfos()
 	if m.statusChecker != nil && !m.statusChecker.ClusterQueueActive(cq.name) {
-		inadmissible += active
-		active = 0
+		inadmissibleInfos = append(inadmissibleInfos, activeInfos...)
+		activeInfos = nil
+	}
+	custom := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, len(activeInfos), len(inadmissibleInfos), custom, m.roleTracker)
+	activeMax, activeMean := pendingWaitStats(activeInfos, m.clock)
+	inadmissibleMax, inadmissibleMean := pendingWaitStats(inadmissibleInfos, m.clock)
+	metrics.ReportPendingWorkloadWaitTimes(cq.name, activeMax, activeMean, inadmissibleMax, inadmissibleMean, custom, m.roleTracker)
```

  This metric is moved to a separate PR: https://github.com/kubernetes-sigs/kueue/pull/10633

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-21T18:46:23Z

- **pkg/cache/queue/cluster_queue.go:492** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-21T18:46:23Z
```diff
@@ -479,24 +480,21 @@ func (c *ClusterQueue) PendingTotal() int {
 func (c *ClusterQueue) Pending() (int, int) {
 	c.rwm.RLock()
 	defer c.rwm.RUnlock()
-	return c.pendingActive(), c.pendingInadmissible()
+	active, inadmissible := c.pendingWorkloadInfos()
+	return len(active), len(inadmissible)
 }
 
-// pendingActive returns the number of active pending workloads,
-// workloads that are in the admission queue.
-func (c *ClusterQueue) pendingActive() int {
-	result := c.heap.Len()
+// pendingWorkloadInfos returns pending workload infos split into active (heap + inflight, no double-count)
+// and inadmissible.
+// Callers must not mutate returned *workload.Info or Obj.
+func (c *ClusterQueue) pendingWorkloadInfos() ([]*workload.Info, []*workload.Info) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
```

  This metric is moved to a separate PR: https://github.com/kubernetes-sigs/kueue/pull/10633

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-21T19:05:33Z

- **pkg/metrics/metrics.go:999** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-21T19:05:33Z
```diff
@@ -968,6 +1041,7 @@ func ClearClusterQueueMetrics(cq kueue.ClusterQueueReference) {
 	EvictedWorkloadsTotal.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
 	EvictedWorkloadsOnceTotal.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
 	PreemptedWorkloadsTotal.DeletePartialMatch(prometheus.Labels{"preempting_cluster_queue": cqName})
+	PreemptionEvictionToPendingSeconds.DeletePartialMatch(prometheus.Labels{"cluster_queue": cqName})
```

  Isn't it covered by clearScopedGaugeMetrics already, or am I missing something?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T19:20:55Z

- **pkg/metrics/metrics.go:635** by [@mimowo](https://github.com/mimowo) — 2026-04-21T19:20:55Z
```diff
@@ -628,6 +632,18 @@ The label 'reason' can have the following values:
 		}, append([]string{"preempting_cluster_queue", "reason", "replica_role"}, extraLabels...),
 	)
 
+	PreemptionEvictionToPendingSeconds = prometheus.NewHistogramVec(
```

  Instead of introducing preemption-oriented metric I would prefer to generalize and add the "reason" label as we do for workload_evictions_total metric, and other eviction-related metrics.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T19:22:08Z

- **pkg/metrics/metrics.go:638** by [@mimowo](https://github.com/mimowo) — 2026-04-21T19:22:09Z
```diff
@@ -628,6 +632,18 @@ The label 'reason' can have the following values:
 		}, append([]string{"preempting_cluster_queue", "reason", "replica_role"}, extraLabels...),
 	)
 
+	PreemptionEvictionToPendingSeconds = prometheus.NewHistogramVec(
+		prometheus.HistogramOpts{
+			Subsystem: constants.KueueName,
+			Name:      "preemption_eviction_to_pending_seconds",
```

  Can we maybe name it `workload_eviction_latency_seconds`, because:
  - I think this would be more descriptive to users rather than "eviction_to_pending"
  - generalize the "preemption (also another comment)

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-21T19:24:01Z

- **pkg/metrics/metrics.go:635** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-21T19:24:01Z
```diff
@@ -628,6 +632,18 @@ The label 'reason' can have the following values:
 		}, append([]string{"preempting_cluster_queue", "reason", "replica_role"}, extraLabels...),
 	)
 
+	PreemptionEvictionToPendingSeconds = prometheus.NewHistogramVec(
```

  Do you mean to remove this filter, and add reason as a label: `c.Reason != kueue.WorkloadEvictedByPreemption`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T19:26:41Z

- **pkg/metrics/metrics.go:635** by [@mimowo](https://github.com/mimowo) — 2026-04-21T19:26:41Z
```diff
@@ -628,6 +632,18 @@ The label 'reason' can have the following values:
 		}, append([]string{"preempting_cluster_queue", "reason", "replica_role"}, extraLabels...),
 	)
 
+	PreemptionEvictionToPendingSeconds = prometheus.NewHistogramVec(
```

  Exactly, and record the reason as a label, check as we do for https://github.com/kubernetes-sigs/kueue/blob/main/pkg/metrics/metrics.go#L545-L564
