# PR #1033: [jobframework] Check the potential failure of RunWithPodSetsInfo

**Summary**: [jobframework] Check the potential failure of RunWithPodSetsInfo

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1033

**Last updated**: 2023-10-24T06:38:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `check-run-with-info` → `main`
- **Created**: 2023-08-02T13:49:25Z
- **Updated**: 2023-10-24T06:38:39Z
- **Merged**: 2023-08-04T12:16:23Z
- **Closed**: 2023-08-04T12:16:23Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk)
- **Changed files**: 8   **+97 / -22**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add a way to verify if the provided  PodSetsInfo is accepted by the job controller.


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-02T13:49:33Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cd35acd0d4b74911e2200af72c7a2f8224cfc07f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64cccc29acd4700008c794aa |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-04T12:15:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1033#pullrequestreview-1562757549" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1033#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/controller/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-02T14:20:50Z

- **pkg/controller/jobs/job/job_controller.go:216** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:10:57Z
```diff
@@ -210,10 +210,10 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
-func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
+func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = pointer.Bool(false)
-	if len(podSetsInfo) == 0 {
-		return
+	if len(podSetsInfo) != 1 {
+		return fmt.Errorf("%w: expecting 1 got %d", jobframework.ErrInvalidPodsetInfo, len(podSetsInfo))
```

  ```suggestion
  		return fmt.Errorf("%w: expecting 1 podset got %d", jobframework.ErrInvalidPodsetInfo, len(podSetsInfo))
  ```

- **pkg/controller/jobframework/reconciler.go:255** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:12:43Z
```diff
@@ -239,6 +241,13 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := r.startJob(ctx, job, object, wl)
 			if err != nil {
```

  This means that the Job never starts, but also never gets a Failed condition, meaning that it's stuck suspended until the user manually deletes it.
  But that's no worse that the current behavior, so +1 on the change.

- **pkg/controller/jobs/job/job_controller_test.go:271** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:16:47Z
```diff
@@ -240,12 +242,35 @@ func TestPodSetsInfo(t *testing.T) {
 				},
 			},
 		},
+		"noInfoOnRun": {
+			job: (*Job)(utiltestingjob.MakeJob("job", "ns").
+				Parallelism(5).
+				SetAnnotation(JobMinParallelismAnnotation, "2").
+				Obj()),
+			runInfo: []jobframework.PodSetInfo{},
+			wantUnsuspended: utiltestingjob.MakeJob("job", "ns").
+				Parallelism(5).
+				SetAnnotation(JobMinParallelismAnnotation, "2").
+				Suspend(false).
+				Obj(),
+			restoreInfo: []jobframework.PodSetInfo{
+				{
+					Count: 5,
+				},
+			},
+			wantRunError: jobframework.ErrInvalidPodsetInfo,
+		},
 	}
 	for name, tc := range testcases {
 		t.Run(name, func(t *testing.T) {
 			origSpec := *tc.job.Spec.DeepCopy()
 
-			tc.job.RunWithPodSetsInfo(tc.runInfo)
+			gotErr := tc.job.RunWithPodSetsInfo(tc.runInfo)
+
+			if !errors.Is(gotErr, tc.wantRunError) {
+				diff := cmp.Diff(tc.wantRunError, gotErr)
```

  ```suggestion
  			if diff := cmp.Diff(tc.wantRunError, gotErr, cmpopts.EquateErrors()); diff != "" {
  ```

- **pkg/controller/jobs/jobset/jobset_controller.go:120** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:17:14Z
```diff
@@ -113,12 +114,10 @@ func (j *JobSet) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *JobSet) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *JobSet) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = pointer.Bool(false)
 	if len(podSetInfos) != len(j.Spec.ReplicatedJobs) {
-		// this is very unlikely, however in order to avoid any potential
-		// out of bounds access
-		return
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, len(j.Spec.ReplicatedJobs), len(podSetInfos))
```

  ```suggestion
  		return fmt.Errorf("%w: expecting %d podsets got %d", jobframework.ErrInvalidPodsetInfo, len(j.Spec.ReplicatedJobs), len(podSetInfos))
  ```

- **pkg/controller/jobs/mpijob/mpijob_controller.go:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:17:55Z
```diff
@@ -114,20 +115,23 @@ func (j *MPIJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *MPIJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *MPIJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.RunPolicy.Suspend = pointer.Bool(false)
-	if len(podSetInfos) == 0 {
-		return
+	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
+
+	if len(podSetInfos) != len(orderedReplicaTypes) {
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, len(orderedReplicaTypes), len(podSetInfos))
```

  ```suggestion
  		return fmt.Errorf("%w: expecting %d podsets, got %d", jobframework.ErrInvalidPodsetInfo, len(orderedReplicaTypes), len(podSetInfos))
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller.go:118** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:18:15Z
```diff
@@ -112,10 +113,11 @@ func (j *RayJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = false
-	if len(podSetInfos) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
-		return
+	exoectedLen := len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1
```

  ```suggestion
  	expectedLen := len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1
  ```

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-02T14:20:49Z
```diff
@@ -151,8 +152,26 @@ func TestNodeSelectors(t *testing.T) {
 		}).
 		Obj())
 
+	// RunWithPodSetsInfo with invalid info should fail
+	runErr := job.RunWithPodSetsInfo([]jobframework.PodSetInfo{
```

  It should be a test table instead.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-08-04T07:32:21Z

- **pkg/controller/jobframework/reconciler.go:429** by [@mimowo](https://github.com/mimowo) — 2023-08-04T07:19:46Z
```diff
@@ -413,7 +426,11 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 	if err != nil {
 		return err
 	}
-	job.RunWithPodSetsInfo(info)
+	runErr := job.RunWithPodSetsInfo(info)
```

  ```suggestion
  	if runErr := job.RunWithPodSetsInfo(info); runErr != nil {
  ```

- **pkg/controller/jobframework/reconciler.go:431** by [@mimowo](https://github.com/mimowo) — 2023-08-04T07:21:17Z
```diff
@@ -413,7 +426,11 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 	if err != nil {
 		return err
 	}
-	job.RunWithPodSetsInfo(info)
+	runErr := job.RunWithPodSetsInfo(info)
+	if runErr != nil {
+		// mark the workload as failed
```

  It seems `finished` rather than failed,  but I would drop this comment from here as it only propagates the error up, so interpretation happens layer up.

- **pkg/controller/jobframework/reconciler.go:245** by [@mimowo](https://github.com/mimowo) — 2023-08-04T07:29:56Z
```diff
@@ -239,6 +241,13 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := r.startJob(ctx, job, object, wl)
 			if err != nil {
 				log.Error(err, "Unsuspending job")
+				if isPermanent(err) {
+					errUpdateStatus := workload.UpdateStatus(ctx, r.client, wl, kueue.WorkloadFinished, metav1.ConditionTrue, "CannotStart", err.Error(), constants.JobControllerName)
```

  Use constant for the reason, with a comment.

- **pkg/controller/jobs/job/job_controller.go:216** by [@mimowo](https://github.com/mimowo) — 2023-08-04T07:31:21Z
```diff
@@ -210,10 +210,10 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
-func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
+func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = pointer.Bool(false)
-	if len(podSetsInfo) == 0 {
-		return
+	if len(podSetsInfo) != 1 {
+		return fmt.Errorf("%w: expecting 1 got %d", jobframework.ErrInvalidPodsetInfo, len(podSetsInfo))
```

  and ","

- **pkg/controller/jobs/rayjob/rayjob_controller.go:120** by [@mimowo](https://github.com/mimowo) — 2023-08-04T07:32:06Z
```diff
@@ -112,10 +113,11 @@ func (j *RayJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = false
-	if len(podSetInfos) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
-		return
+	exoectedLen := len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1
+	if len(podSetInfos) != exoectedLen {
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, exoectedLen, len(podSetInfos))
```

  ```suggestion
  		return fmt.Errorf("%w: expecting %d podsets, got %d", jobframework.ErrInvalidPodsetInfo, exoectedLen, len(podSetInfos))
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-04T10:00:43Z

- **pkg/controller/jobframework/reconciler.go:245** by [@trasc](https://github.com/trasc) — 2023-08-04T09:17:50Z
```diff
@@ -239,6 +241,13 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 			err := r.startJob(ctx, job, object, wl)
 			if err != nil {
 				log.Error(err, "Unsuspending job")
+				if isPermanent(err) {
+					errUpdateStatus := workload.UpdateStatus(ctx, r.client, wl, kueue.WorkloadFinished, metav1.ConditionTrue, "CannotStart", err.Error(), constants.JobControllerName)
```

  done

- **pkg/controller/jobframework/reconciler.go:429** by [@trasc](https://github.com/trasc) — 2023-08-04T09:19:18Z
```diff
@@ -413,7 +426,11 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 	if err != nil {
 		return err
 	}
-	job.RunWithPodSetsInfo(info)
+	runErr := job.RunWithPodSetsInfo(info)
```

  done

- **pkg/controller/jobframework/reconciler.go:431** by [@trasc](https://github.com/trasc) — 2023-08-04T09:21:02Z
```diff
@@ -413,7 +426,11 @@ func (r *JobReconciler) startJob(ctx context.Context, job GenericJob, object cli
 	if err != nil {
 		return err
 	}
-	job.RunWithPodSetsInfo(info)
+	runErr := job.RunWithPodSetsInfo(info)
+	if runErr != nil {
+		// mark the workload as failed
```

  removed

- **pkg/controller/jobs/job/job_controller.go:216** by [@trasc](https://github.com/trasc) — 2023-08-04T09:28:19Z
```diff
@@ -210,10 +210,10 @@ func (j *Job) PodSets() []kueue.PodSet {
 	}
 }
 
-func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) {
+func (j *Job) RunWithPodSetsInfo(podSetsInfo []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = pointer.Bool(false)
-	if len(podSetsInfo) == 0 {
-		return
+	if len(podSetsInfo) != 1 {
+		return fmt.Errorf("%w: expecting 1 got %d", jobframework.ErrInvalidPodsetInfo, len(podSetsInfo))
```

  done

- **pkg/controller/jobs/job/job_controller_test.go:271** by [@trasc](https://github.com/trasc) — 2023-08-04T09:34:20Z
```diff
@@ -240,12 +242,35 @@ func TestPodSetsInfo(t *testing.T) {
 				},
 			},
 		},
+		"noInfoOnRun": {
+			job: (*Job)(utiltestingjob.MakeJob("job", "ns").
+				Parallelism(5).
+				SetAnnotation(JobMinParallelismAnnotation, "2").
+				Obj()),
+			runInfo: []jobframework.PodSetInfo{},
+			wantUnsuspended: utiltestingjob.MakeJob("job", "ns").
+				Parallelism(5).
+				SetAnnotation(JobMinParallelismAnnotation, "2").
+				Suspend(false).
+				Obj(),
+			restoreInfo: []jobframework.PodSetInfo{
+				{
+					Count: 5,
+				},
+			},
+			wantRunError: jobframework.ErrInvalidPodsetInfo,
+		},
 	}
 	for name, tc := range testcases {
 		t.Run(name, func(t *testing.T) {
 			origSpec := *tc.job.Spec.DeepCopy()
 
-			tc.job.RunWithPodSetsInfo(tc.runInfo)
+			gotErr := tc.job.RunWithPodSetsInfo(tc.runInfo)
+
+			if !errors.Is(gotErr, tc.wantRunError) {
+				diff := cmp.Diff(tc.wantRunError, gotErr)
```

  thanks, done

- **pkg/controller/jobs/jobset/jobset_controller.go:120** by [@trasc](https://github.com/trasc) — 2023-08-04T09:34:28Z
```diff
@@ -113,12 +114,10 @@ func (j *JobSet) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *JobSet) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *JobSet) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = pointer.Bool(false)
 	if len(podSetInfos) != len(j.Spec.ReplicatedJobs) {
-		// this is very unlikely, however in order to avoid any potential
-		// out of bounds access
-		return
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, len(j.Spec.ReplicatedJobs), len(podSetInfos))
```

  done

- **pkg/controller/jobs/mpijob/mpijob_controller.go:123** by [@trasc](https://github.com/trasc) — 2023-08-04T09:34:40Z
```diff
@@ -114,20 +115,23 @@ func (j *MPIJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *MPIJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *MPIJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.RunPolicy.Suspend = pointer.Bool(false)
-	if len(podSetInfos) == 0 {
-		return
+	orderedReplicaTypes := orderedReplicaTypes(&j.Spec)
+
+	if len(podSetInfos) != len(orderedReplicaTypes) {
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, len(orderedReplicaTypes), len(podSetInfos))
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:118** by [@trasc](https://github.com/trasc) — 2023-08-04T09:35:38Z
```diff
@@ -112,10 +113,11 @@ func (j *RayJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = false
-	if len(podSetInfos) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
-		return
+	exoectedLen := len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1
```

  thanks, done

- **pkg/controller/jobs/rayjob/rayjob_controller.go:120** by [@trasc](https://github.com/trasc) — 2023-08-04T09:35:47Z
```diff
@@ -112,10 +113,11 @@ func (j *RayJob) PodSets() []kueue.PodSet {
 	return podSets
 }
 
-func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) {
+func (j *RayJob) RunWithPodSetsInfo(podSetInfos []jobframework.PodSetInfo) error {
 	j.Spec.Suspend = false
-	if len(podSetInfos) != len(j.Spec.RayClusterSpec.WorkerGroupSpecs)+1 {
-		return
+	exoectedLen := len(j.Spec.RayClusterSpec.WorkerGroupSpecs) + 1
+	if len(podSetInfos) != exoectedLen {
+		return fmt.Errorf("%w: expecting %d got %d", jobframework.ErrInvalidPodsetInfo, exoectedLen, len(podSetInfos))
```

  done

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:156** by [@trasc](https://github.com/trasc) — 2023-08-04T09:36:57Z
```diff
@@ -151,8 +152,26 @@ func TestNodeSelectors(t *testing.T) {
 		}).
 		Obj())
 
+	// RunWithPodSetsInfo with invalid info should fail
+	runErr := job.RunWithPodSetsInfo([]jobframework.PodSetInfo{
```

  Agree, Maybe we can do it in another PR

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-04T12:15:22Z

/lgtm
/approve

- **pkg/controller/jobs/rayjob/rayjob_controller_test.go:156** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-04T12:15:15Z
```diff
@@ -151,8 +152,26 @@ func TestNodeSelectors(t *testing.T) {
 		}).
 		Obj())
 
+	// RunWithPodSetsInfo with invalid info should fail
+	runErr := job.RunWithPodSetsInfo([]jobframework.PodSetInfo{
```

  Ack, please do.
