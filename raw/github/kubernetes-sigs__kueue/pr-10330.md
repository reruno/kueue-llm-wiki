# PR #10330: Propagate LeaderWorkerSet PodTemplate metadata to PodSet

**Summary**: Propagate LeaderWorkerSet PodTemplate metadata to PodSet

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10330

**Last updated**: 2026-04-13T08:43:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@pajakd](https://github.com/pajakd)
- **Branch**: `lws_propagate` → `main`
- **Created**: 2026-04-08T09:42:24Z
- **Updated**: 2026-04-13T08:43:46Z
- **Merged**: 2026-04-10T09:30:20Z
- **Closed**: 2026-04-10T09:30:20Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 2   **+150 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Propagate Labels/Annotations from LeaderWorkerSet Leader/Worker PodTemplates to PodSets when creating a Workload.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10326

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
LeaderWorkerSet integration: fix the bug that the PodTemplate metadata wasn't propagated to the Workload's PodSets.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-08T09:42:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 530e8ea82897d4adeb6aa19da3c65757cef9218f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69d79e661025a200080611dd |

### Comment by [@mwielgus](https://github.com/mwielgus) — 2026-04-10T08:30:07Z

/lgtm
Please squash the commits.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-10T08:30:16Z

LGTM label has been added.  <details>Git tree hash: 4bc6d660563b3ea31454dbeb616fd5ef4559af76</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-10T08:47:28Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10330#pullrequestreview-4088443119" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10330#" title="Author self-approved">pajakd</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T08:48:13Z

/release-note-edit
```release-note
LeaderWorkerSet integration: fix the bug that the PodTemplate metadata wasn't propagated to the Workload's PodSets.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T16:25:18Z

/cherrypick release-0.17

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T16:25:23Z

/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-10T16:25:58Z

@mimowo: new pull request created: #10399

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10330#issuecomment-4225224660):

>/cherrypick release-0.17


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-10T16:26:01Z

@mimowo: #10330 failed to apply on top of branch "release-0.16":
```
Applying: Propagate PodTemplate metadata for LWS
Using index info to reconstruct a base tree...
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
M	pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler_test.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler_test.go
Auto-merging pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
CONFLICT (content): Merge conflict in pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Propagate PodTemplate metadata for LWS

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10330#issuecomment-4225225113):

>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T16:27:09Z

@pajakd please CP to 0.16 manually

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-13T08:43:46Z

@pajakd please address the follow up comments, and cherrypick them all combined.

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-09T10:59:02Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:291** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T10:54:29Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
+	keysToDelete := []string{
```

  Can we have it formally declared in API?

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:282** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T10:57:24Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
```

  Rename to clearly indicate the purpose - drop keys with a specific prefix from map.

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:290** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T10:58:32Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
```

  Inconsistency. One function is "clear" while the other is "clean".

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:308** by [@mwielgus](https://github.com/mwielgus) — 2026-04-09T10:58:59Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
+	keysToDelete := []string{
+		kueue.PodSetRequiredTopologyAnnotation,
+		kueue.PodSetPreferredTopologyAnnotation,
+		kueue.PodSetSliceRequiredTopologyAnnotation,
+		kueue.PodSetSliceSizeAnnotation,
+	}
+	for _, k := range keysToDelete {
+		delete(annotations, k)
+	}
+}
+
 func newPodSet(name kueue.PodSetReference, count int32, template *corev1.PodTemplateSpec, podIndexLabel *string) (*kueue.PodSet, error) {
 	podSet := &kueue.PodSet{
-		Name:  name,
-		Count: count,
-		Template: corev1.PodTemplateSpec{
-			Spec: *template.Spec.DeepCopy(),
-		},
+		Name:     name,
+		Count:    count,
+		Template: *template.DeepCopy(),
 	}
+	cleanMetadata(podSet.Template.Labels)
```

  Why is it done only for LWS?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-09T11:47:47Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:308** by [@pajakd](https://github.com/pajakd) — 2026-04-09T11:47:47Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
+	keysToDelete := []string{
+		kueue.PodSetRequiredTopologyAnnotation,
+		kueue.PodSetPreferredTopologyAnnotation,
+		kueue.PodSetSliceRequiredTopologyAnnotation,
+		kueue.PodSetSliceSizeAnnotation,
+	}
+	for _, k := range keysToDelete {
+		delete(annotations, k)
+	}
+}
+
 func newPodSet(name kueue.PodSetReference, count int32, template *corev1.PodTemplateSpec, podIndexLabel *string) (*kueue.PodSet, error) {
 	podSet := &kueue.PodSet{
-		Name:  name,
-		Count: count,
-		Template: corev1.PodTemplateSpec{
-			Spec: *template.Spec.DeepCopy(),
-		},
+		Name:     name,
+		Count:    count,
+		Template: *template.DeepCopy(),
 	}
+	cleanMetadata(podSet.Template.Labels)
```

  It is analogous to what we do for Jobs, where we copy everything and then drop labels that are managed by the job controller (except job-name label) https://github.com/kubernetes-sigs/kueue/blob/2b07fac8fd8f4916bfc1d31161ba068a88029f11/pkg/controller/jobs/job/job_controller.go#L239-L253

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-09T11:56:51Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:282** by [@pajakd](https://github.com/pajakd) — 2026-04-09T11:56:51Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
```

  Done.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-09T11:56:55Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:290** by [@pajakd](https://github.com/pajakd) — 2026-04-09T11:56:54Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
```

  Renamed both to start with "drop"

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-09T12:43:56Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go:291** by [@pajakd](https://github.com/pajakd) — 2026-04-09T12:43:56Z
```diff
@@ -276,14 +279,35 @@ func (r *Reconciler) constructWorkload(lws *leaderworkersetv1.LeaderWorkerSet, w
 	return createdWorkload, nil
 }
 
+func cleanMetadata(m map[string]string) {
+	for k := range m {
+		if strings.HasPrefix(k, lwsDomainPrefix) && k != lwsNameLabel {
+			delete(m, k)
+		}
+	}
+}
+
+func clearTASAnnotations(annotations map[string]string) {
+	keysToDelete := []string{
```

  Actually I asked @mimowo about this and he said that its fine to copy the TAS annotations (we do the same for Jobs). So, removed this code entirely.
  
  I had to update two Unit Tests that were testing the case with these annotations and TAS featuregate on/off.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-10T08:47:18Z

/kind bug
/remove-kind feature
SInce this is already done for all other Jobs, see example for [job](https://github.com/kubernetes-sigs/kueue/blob/ccb36fcf89731f330cfc5a54284af043ec5ca37c/pkg/controller/jobs/job/job_controller.go#L253), [jobset](https://github.com/kubernetes-sigs/kueue/blob/ccb36fcf89731f330cfc5a54284af043ec5ca37c/pkg/controller/jobs/jobset/jobset_controller.go#L119), [ray](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/jobs/raycluster/raycluster_controller.go#L112)

 I think it is just bug / omission of our LWS integration 

I'm ok to filter out the LWS-auxiliary labels and annotations as in the Job integration.
/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-10T17:06:29Z

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler_test.go:369** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-10T17:06:11Z
```diff
@@ -264,6 +264,135 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"should create prebuilt workload with custom annotations propagated to podsets": {
+			featureGates: map[featuregate.Feature]bool{
+				features.TopologyAwareScheduling: false,
+			},
+			leaderWorkerSet: leaderworkerset.MakeLeaderWorkerSet(testLWS, testNS).
+				UID(testUID).
+				Replicas(1).
+				Size(3).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					ObjectMeta: metav1.ObjectMeta{
+						Annotations: map[string]string{
+							"custom-leader-annotation":                  "leader-value",
+							"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+						},
+						Labels: map[string]string{
+							"leaderworkerset.sigs.k8s.io/name":        testLWS,
+							"leaderworkerset.sigs.k8s.io/group-index": "1",
+						},
+					},
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{Name: "c", Image: "pause"},
+						},
+					},
+				}).
+				WorkerTemplate(corev1.PodTemplateSpec{
+					ObjectMeta: metav1.ObjectMeta{
+						Annotations: map[string]string{
+							"custom-worker-annotation":                  "worker-value",
+							"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+						},
+						Labels: map[string]string{
+							"leaderworkerset.sigs.k8s.io/name":        testLWS,
+							"leaderworkerset.sigs.k8s.io/group-index": "1",
+						},
+					},
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{Name: "c", Image: "pause"},
+						},
+					},
+				}).
+				Obj(),
+			wantLeaderWorkerSets: []leaderworkersetv1.LeaderWorkerSet{
+				*leaderworkerset.MakeLeaderWorkerSet(testLWS, testNS).
+					UID(testUID).
+					Replicas(1).
+					Size(3).
+					LeaderTemplate(corev1.PodTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Annotations: map[string]string{
+								"custom-leader-annotation":                  "leader-value",
+								"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+							},
+							Labels: map[string]string{
+								"leaderworkerset.sigs.k8s.io/name":        testLWS,
+								"leaderworkerset.sigs.k8s.io/group-index": "1",
+							},
+						},
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{Name: "c", Image: "pause"},
+							},
+						},
+					}).
+					WorkerTemplate(corev1.PodTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Annotations: map[string]string{
+								"custom-worker-annotation":                  "worker-value",
+								"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+							},
+							Labels: map[string]string{
+								"leaderworkerset.sigs.k8s.io/name":        testLWS,
+								"leaderworkerset.sigs.k8s.io/group-index": "1",
+							},
+						},
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{Name: "c", Image: "pause"},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload(GetWorkloadName(types.UID(testUID), testLWS, "0"), testNS).
+					JobUID(testUID).
+					OwnerReference(gvk, testLWS, testUID).
+					Annotation(podconstants.IsGroupWorkloadAnnotationKey, podconstants.IsGroupWorkloadAnnotationValue).
+					Annotation(constants.JobOwnerGVKAnnotation, gvk.String()).
+					Annotation(constants.JobOwnerNameAnnotation, testLWS).
+					Annotation(constants.ComponentWorkloadIndexAnnotation, "0").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(
+						func() kueue.PodSet {
+							ps := *utiltestingapi.MakePodSet(leaderPodSetName, 1).
+								RestartPolicy("").
+								Image("pause").
+								Obj()
+							ps.Template.Annotations = map[string]string{"custom-leader-annotation": "leader-value"}
+							ps.Template.Labels = map[string]string{"leaderworkerset.sigs.k8s.io/name": testLWS}
+							return ps
+						}(),
```

  ```suggestion
  						*utiltestingapi.MakePodSet(leaderPodSetName, 1).
  								Annotations(map[string]string{"custom-leader-annotation": "leader-value"}).
  								Labels(map[string]string{"leaderworkerset.sigs.k8s.io/name": testLWS}).
  								RestartPolicy("").
  								Image("pause").
  								Obj()
  ```
  
  @pajakd Can we use the wrapper?

- **pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler_test.go:378** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-10T17:06:19Z
```diff
@@ -264,6 +264,135 @@ func TestReconciler(t *testing.T) {
 				},
 			},
 		},
+		"should create prebuilt workload with custom annotations propagated to podsets": {
+			featureGates: map[featuregate.Feature]bool{
+				features.TopologyAwareScheduling: false,
+			},
+			leaderWorkerSet: leaderworkerset.MakeLeaderWorkerSet(testLWS, testNS).
+				UID(testUID).
+				Replicas(1).
+				Size(3).
+				LeaderTemplate(corev1.PodTemplateSpec{
+					ObjectMeta: metav1.ObjectMeta{
+						Annotations: map[string]string{
+							"custom-leader-annotation":                  "leader-value",
+							"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+						},
+						Labels: map[string]string{
+							"leaderworkerset.sigs.k8s.io/name":        testLWS,
+							"leaderworkerset.sigs.k8s.io/group-index": "1",
+						},
+					},
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{Name: "c", Image: "pause"},
+						},
+					},
+				}).
+				WorkerTemplate(corev1.PodTemplateSpec{
+					ObjectMeta: metav1.ObjectMeta{
+						Annotations: map[string]string{
+							"custom-worker-annotation":                  "worker-value",
+							"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+						},
+						Labels: map[string]string{
+							"leaderworkerset.sigs.k8s.io/name":        testLWS,
+							"leaderworkerset.sigs.k8s.io/group-index": "1",
+						},
+					},
+					Spec: corev1.PodSpec{
+						Containers: []corev1.Container{
+							{Name: "c", Image: "pause"},
+						},
+					},
+				}).
+				Obj(),
+			wantLeaderWorkerSets: []leaderworkersetv1.LeaderWorkerSet{
+				*leaderworkerset.MakeLeaderWorkerSet(testLWS, testNS).
+					UID(testUID).
+					Replicas(1).
+					Size(3).
+					LeaderTemplate(corev1.PodTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Annotations: map[string]string{
+								"custom-leader-annotation":                  "leader-value",
+								"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+							},
+							Labels: map[string]string{
+								"leaderworkerset.sigs.k8s.io/name":        testLWS,
+								"leaderworkerset.sigs.k8s.io/group-index": "1",
+							},
+						},
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{Name: "c", Image: "pause"},
+							},
+						},
+					}).
+					WorkerTemplate(corev1.PodTemplateSpec{
+						ObjectMeta: metav1.ObjectMeta{
+							Annotations: map[string]string{
+								"custom-worker-annotation":                  "worker-value",
+								"leaderworkerset.sigs.k8s.io/template-hash": "12345",
+							},
+							Labels: map[string]string{
+								"leaderworkerset.sigs.k8s.io/name":        testLWS,
+								"leaderworkerset.sigs.k8s.io/group-index": "1",
+							},
+						},
+						Spec: corev1.PodSpec{
+							Containers: []corev1.Container{
+								{Name: "c", Image: "pause"},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload(GetWorkloadName(types.UID(testUID), testLWS, "0"), testNS).
+					JobUID(testUID).
+					OwnerReference(gvk, testLWS, testUID).
+					Annotation(podconstants.IsGroupWorkloadAnnotationKey, podconstants.IsGroupWorkloadAnnotationValue).
+					Annotation(constants.JobOwnerGVKAnnotation, gvk.String()).
+					Annotation(constants.JobOwnerNameAnnotation, testLWS).
+					Annotation(constants.ComponentWorkloadIndexAnnotation, "0").
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(
+						func() kueue.PodSet {
+							ps := *utiltestingapi.MakePodSet(leaderPodSetName, 1).
+								RestartPolicy("").
+								Image("pause").
+								Obj()
+							ps.Template.Annotations = map[string]string{"custom-leader-annotation": "leader-value"}
+							ps.Template.Labels = map[string]string{"leaderworkerset.sigs.k8s.io/name": testLWS}
+							return ps
+						}(),
+						func() kueue.PodSet {
+							ps := *utiltestingapi.MakePodSet(workerPodSetName, 2).
+								RestartPolicy("").
+								Image("pause").
+								Obj()
+							ps.Template.Annotations = map[string]string{"custom-worker-annotation": "worker-value"}
+							ps.Template.Labels = map[string]string{"leaderworkerset.sigs.k8s.io/name": testLWS}
+							return ps
+						}(),
```

  ditto
