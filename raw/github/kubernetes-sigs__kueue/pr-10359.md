# PR #10359: add integration test for AFS plus DRA

**Summary**: add integration test for AFS plus DRA

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10359

**Last updated**: 2026-04-20T14:47:55Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `dra-admission-fair-sharing` → `main`
- **Created**: 2026-04-09T17:06:55Z
- **Updated**: 2026-04-20T14:47:55Z
- **Merged**: 2026-04-20T14:47:53Z
- **Closed**: 2026-04-20T14:47:53Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@mwielgus](https://github.com/mwielgus), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 6   **+694 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Add integration test verifying DRA + AFS work together
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10343 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-09T17:07:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | dd5c083bc6f2cbd8fed26963e3efc119f8cabd96 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e630364f96b400084d0e1b |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10359--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10359--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwMzU5LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.E8S3M5lSetBsBcB_20rCJo2yA2qHqXuJ9YfS9pi8a-s)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10359--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-15T15:00:13Z

@mimowo @mwielgus This is ready for review again.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-15T16:04:24Z

Checking stability 1/5
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-baseline-main

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-15T16:43:06Z

Checking stability 2/5
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-baseline-main

### Comment by [@kannon92](https://github.com/kannon92) — 2026-04-16T13:36:34Z

Last run passed so skipping 3/5.

Checking stability 4/5
/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-integration-baseline-main

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T14:46:04Z

LGTM label has been added.  <details>Git tree hash: af01f55e3449a80e7b64de87064e987480d234c1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T14:46:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10359#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10359#pullrequestreview-4140992260" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-09T17:07:24Z

- **pkg/config/validation.go:473** by [@kannon92](https://github.com/kannon92) — 2026-04-09T17:07:24Z
```diff
@@ -470,6 +470,7 @@ func validateDeviceClassMappings(c *configapi.Configuration) field.ErrorList {
 				deviceClassToResource[deviceClass] = mapping.Name
 			}
 		}
+
```

  remove this whitespace.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T07:55:29Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:216** by [@mimowo](https://github.com/mimowo) — 2026-04-13T07:55:30Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", func() {
+		ginkgo.BeforeEach(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			wls = nil
+			lqs = nil
+			cqs = nil
+
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a", "1")
+			wlA2 := createWorkloadWithDRA("lq-a", "1")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "1")
+			wlA3 := createWorkloadCPUOnly("lq-a", "1")
+
+			ginkgo.By("Verifying lq-b workload is admitted (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is also admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed", "1")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", framework.SlowSpec, func() {
```

  Can we move this test to a dedicated When to avoid the double Stop/Start?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T07:55:53Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:74** by [@mimowo](https://github.com/mimowo) — 2026-04-13T07:55:54Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
```

  Can we move the Start/Stop manager to BeforeAll, AfterAll, so that there is no need to restart in-between tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T07:58:43Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:121** by [@mimowo](https://github.com/mimowo) — 2026-04-13T07:58:43Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", func() {
+		ginkgo.BeforeEach(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			wls = nil
+			lqs = nil
+			cqs = nil
```

  I suppose this is not needed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T07:59:42Z

Thank you for the update, looks great, posted some possible improvements to the tests.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-13T23:47:12Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:216** by [@kannon92](https://github.com/kannon92) — 2026-04-13T23:47:12Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", func() {
+		ginkgo.BeforeEach(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			wls = nil
+			lqs = nil
+			cqs = nil
+
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a", "1")
+			wlA2 := createWorkloadWithDRA("lq-a", "1")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "1")
+			wlA3 := createWorkloadCPUOnly("lq-a", "1")
+
+			ginkgo.By("Verifying lq-b workload is admitted (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is also admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed", "1")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", framework.SlowSpec, func() {
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-13T23:47:18Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:74** by [@kannon92](https://github.com/kannon92) — 2026-04-13T23:47:18Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
```

  done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-14T11:44:21Z

- **pkg/util/admissionfairsharing/admission_fair_sharing_test.go:31** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T11:37:41Z
```diff
@@ -0,0 +1,59 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissionfairsharing
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestCalculateEntryPenaltyWithDRAResources(t *testing.T) {
+	afs := &config.AdmissionFairSharing{
+		UsageSamplingInterval: metav1.Duration{Duration: 5 * 60 * 1e9},  // 5min
```

  Use 5 * time.Minute and 10 * time.Minute.

- **test/integration/singlecluster/scheduler/dra_fairsharing/suite_test.go:88** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T11:39:59Z
```diff
@@ -0,0 +1,147 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"context"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/dra"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption/fairsharing"
+	"sigs.k8s.io/kueue/pkg/webhooks"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	cfg       *rest.Config
+	k8sClient client.Client
+	ctx       context.Context
+	fwk       *framework.Framework
+)
+
+func TestScheduler(t *testing.T) {
+	gomega.RegisterFailHandler(ginkgo.Fail)
+
+	ginkgo.RunSpecs(t,
+		"DRA + Fair Sharing Suite",
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicResourceAllocation, true)
+
+	fwk = &framework.Framework{
+		WebhookPath: util.WebhookPath,
+		APIServerFeatureGates: []string{
+			"DynamicResourceAllocation=true",
+		},
+		APIServerRuntimeConfig: []string{
+			"resource.k8s.io/v1beta2=true",
+		},
+	}
+	cfg = fwk.Init()
+	ctx, k8sClient = fwk.SetupClient(cfg)
+})
+
+var _ = ginkgo.AfterSuite(func() {
+	fwk.Teardown()
+})
+
+var _ = ginkgo.ReportAfterSuite("Generate JUnit Report", func(report ginkgo.Report) {
+	err := util.ConfigureSuiteReporting(report)
+	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+})
+
+var deviceClassMappings = []config.DeviceClassMapping{
```

  This is declared as a package-level mutable global variable. To prevent accidental mutation by tests running in parallel, it is safer to return this from a function (e.g., func getDeviceClassMappings() []config.DeviceClassMapping) or define it locally within the test setups.

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:119** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T11:42:21Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
```

  deviceClass and rct (ResourceClaimTemplate) are created in BeforeAll but are not cleaned up in AfterAll. This could cause test flakiness or cross-test contamination. Add `util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)` and the same for rct in AfterAll.

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:132** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T11:44:17Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
```

  The CPU quota is set to 10, and both competing workloads (wlB1 and wlA3) request only 1 CPU. Because there is no resource contention, the scheduler will admit both workloads immediately regardless of their AFS order.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-14T15:36:32Z

- **pkg/util/admissionfairsharing/admission_fair_sharing_test.go:31** by [@kannon92](https://github.com/kannon92) — 2026-04-14T15:36:32Z
```diff
@@ -0,0 +1,59 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package admissionfairsharing
+
+import (
+	"testing"
+
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+)
+
+func TestCalculateEntryPenaltyWithDRAResources(t *testing.T) {
+	afs := &config.AdmissionFairSharing{
+		UsageSamplingInterval: metav1.Duration{Duration: 5 * 60 * 1e9},  // 5min
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T02:34:37Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:119** by [@kannon92](https://github.com/kannon92) — 2026-04-15T02:34:37Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T14:59:38Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:121** by [@kannon92](https://github.com/kannon92) — 2026-04-15T14:59:38Z
```diff
@@ -0,0 +1,272 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	var (
+		defaultFlavor *kueue.ResourceFlavor
+		gpuFlavor     *kueue.ResourceFlavor
+		ns            *corev1.Namespace
+
+		cqs []*kueue.ClusterQueue
+		lqs []*kueue.LocalQueue
+		wls []*kueue.Workload
+	)
+
+	var createWorkloadWithDRA = func(queue string, gpuCount string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+				Request(corev1.ResourceCPU, "1").
+				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+				Obj()).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	var createWorkloadCPUOnly = func(queue string, cpuRequests string) *kueue.Workload {
+		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+			Queue(kueue.LocalQueueName(queue)).
+			Request(corev1.ResourceCPU, cpuRequests).
+			Obj()
+
+		util.MustCreate(ctx, k8sClient, wl)
+		wls = append(wls, wl)
+		return wl
+	}
+
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", func() {
+		ginkgo.BeforeEach(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			wls = nil
+			lqs = nil
+			cqs = nil
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T14:59:48Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/suite_test.go:88** by [@kannon92](https://github.com/kannon92) — 2026-04-15T14:59:48Z
```diff
@@ -0,0 +1,147 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"context"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/dra"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption/fairsharing"
+	"sigs.k8s.io/kueue/pkg/webhooks"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	cfg       *rest.Config
+	k8sClient client.Client
+	ctx       context.Context
+	fwk       *framework.Framework
+)
+
+func TestScheduler(t *testing.T) {
+	gomega.RegisterFailHandler(ginkgo.Fail)
+
+	ginkgo.RunSpecs(t,
+		"DRA + Fair Sharing Suite",
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicResourceAllocation, true)
+
+	fwk = &framework.Framework{
+		WebhookPath: util.WebhookPath,
+		APIServerFeatureGates: []string{
+			"DynamicResourceAllocation=true",
+		},
+		APIServerRuntimeConfig: []string{
+			"resource.k8s.io/v1beta2=true",
+		},
+	}
+	cfg = fwk.Init()
+	ctx, k8sClient = fwk.SetupClient(cfg)
+})
+
+var _ = ginkgo.AfterSuite(func() {
+	fwk.Teardown()
+})
+
+var _ = ginkgo.ReportAfterSuite("Generate JUnit Report", func(report ginkgo.Report) {
+	err := util.ConfigureSuiteReporting(report)
+	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+})
+
+var deviceClassMappings = []config.DeviceClassMapping{
```

  done.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T14:59:55Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:132** by [@kannon92](https://github.com/kannon92) — 2026-04-15T14:59:55Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
```

  done.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T16:38:01Z

Sorry
I forgot to submit review comments.

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:237** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-14T08:09:41Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "1")
+			wlA3 := createWorkloadCPUOnly("lq-a", "1")
+
+			ginkgo.By("Verifying lq-b workload is admitted (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is also admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
```

  Why do we need an additional anonymous function call? Isn't the following pattern enough?
  
  ```go
  		var (
  ---
  			wl *corev1.Workload
  
  ---
  		)
  
  		wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
  			Queue(kueue.LocalQueueName(queue)).
  			PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
  				Request(corev1.ResourceCPU, "1").
  				ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
  				Obj()).
  			Obj()
  ---
  
  		wl.Clone().Queue("FOO").Obj()
  ```

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:307** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-14T08:21:12Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "1")
+			wlA3 := createWorkloadCPUOnly("lq-a", "1")
+
+			ginkgo.By("Verifying lq-b workload is admitted (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is also admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
```

  Why are these needed?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T16:42:21Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T16:42:21Z
  Shouldn't we move this file to test/integration/singlecluster/scheduler/fairsharing?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T16:42:24Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:307** by [@kannon92](https://github.com/kannon92) — 2026-04-15T16:42:24Z
```diff
@@ -0,0 +1,354 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "1")
+			wlA3 := createWorkloadCPUOnly("lq-a", "1")
+
+			ginkgo.By("Verifying lq-b workload is admitted (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is also admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				deviceClassMappings,
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct := utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+			wls = nil
+			lqs = nil
+			cqs = nil
```

  I removed this last night.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T16:45:28Z

- **pkg/workload/workload_test.go:3183** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T16:45:28Z
```diff
@@ -3092,3 +3092,130 @@ func TestIsExplicitlyRequestingTAS(t *testing.T) {
 		})
 	}
 }
+
+func TestCalcFSUsageFromResourcesWithDRA(t *testing.T) {
+	tests := map[string]struct {
+		consumed   corev1.ResourceList
+		penalty    corev1.ResourceList
+		lqWeight   float64
+		resWeights map[corev1.ResourceName]float64
+		wantUsage  float64
+	}{
+		"DRA resource with default weight": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty:    corev1.ResourceList{},
+			lqWeight:   1,
+			resWeights: map[corev1.ResourceName]float64{},
+			wantUsage:  2, // default weight is 1, so 1 * 2 / 1 = 2
+		},
+		"DRA resource with explicit weight": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 3.0,
+			},
+			wantUsage: 6, // 3 * 2 / 1 = 6
+		},
+		"mixed CPU and DRA resources": {
+			consumed: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("4"),
+				"gpu-logical":      resource.MustParse("2"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				corev1.ResourceCPU: 1.0,
+				"gpu-logical":      5.0,
+			},
+			wantUsage: 14, // (1*4 + 5*2) / 1 = 14
+		},
+		"DRA resource with weight zero contributes nothing": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("10"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 0,
+			},
+			wantUsage: 0,
+		},
+		"DRA resource in penalty only": {
+			consumed: corev1.ResourceList{},
+			penalty: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("3"),
+			},
+			lqWeight:   1,
+			resWeights: map[corev1.ResourceName]float64{},
+			wantUsage:  3, // default weight 1, 1 * 3 / 1 = 3
+		},
+		"DRA resource in both consumed and penalty": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("1"),
+			},
+			lqWeight: 2,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 4.0,
+			},
+			wantUsage: 6, // 4 * (2+1) / 2 = 6
+		},
+	}
+
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			got := CalcFSUsageFromResources(tc.consumed, tc.penalty, tc.lqWeight, tc.resWeights)
+			if got != tc.wantUsage {
+				t.Errorf("CalcFSUsageFromResources() = %v, want %v", got, tc.wantUsage)
+			}
+		})
+	}
+}
+
+func TestSumTotalRequestsWithDRAFromAdmission(t *testing.T) {
+	now := time.Now()
```

  can you use the fake clock instead of real clock?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T16:48:12Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/suite_test.go:95** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T16:48:12Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"context"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/dra"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption/fairsharing"
+	"sigs.k8s.io/kueue/pkg/webhooks"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	cfg       *rest.Config
+	k8sClient client.Client
+	ctx       context.Context
+	fwk       *framework.Framework
+)
+
+func TestScheduler(t *testing.T) {
+	gomega.RegisterFailHandler(ginkgo.Fail)
+
+	ginkgo.RunSpecs(t,
+		"DRA + Fair Sharing Suite",
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicResourceAllocation, true)
+
+	fwk = &framework.Framework{
+		WebhookPath: util.WebhookPath,
+		APIServerFeatureGates: []string{
+			"DynamicResourceAllocation=true",
+		},
+		APIServerRuntimeConfig: []string{
+			"resource.k8s.io/v1beta2=true",
+		},
+	}
+	cfg = fwk.Init()
+	ctx, k8sClient = fwk.SetupClient(cfg)
+})
+
+var _ = ginkgo.AfterSuite(func() {
+	fwk.Teardown()
+})
+
+var _ = ginkgo.ReportAfterSuite("Generate JUnit Report", func(report ginkgo.Report) {
+	err := util.ConfigureSuiteReporting(report)
+	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+})
+
+func getDeviceClassMappings() []config.DeviceClassMapping {
+	return []config.DeviceClassMapping{
+		{
+			Name:             corev1.ResourceName("whole-gpus"),
+			DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+		},
+	}
+}
```

  Instead of preparing this getter, can you define this struct in each test case for descriptive test implementations?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T17:19:54Z

- **keps/2941-DRA/README.md:834** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T17:19:54Z
```diff
@@ -826,6 +827,33 @@ Processing Flow:
 
 This architecture separates concerns between DRA processing (controller) and queue management (scheduler), enabling robust error handling and retry logic for DRA-specific operations.
 
+### Integration with Admission Fair Sharing
+
+DRA logical resources participate in Admission Fair Sharing (AFS) when both DRA and AFS are enabled.
+The logical resource from `deviceClassMappings.name` lands in the workload's admitted `ResourceUsage`,
+and `CalcFSUsageFromResources` applies weights to all resources without filtering for DRA. This means
```

  ```suggestion
  and the AFS penalty accounting mechanism applies weights to all resources without filtering for DRA. This means
  ```
  
  Can you avoid mentioning the implemented function name?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T17:28:26Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T17:28:26Z
```diff
@@ -0,0 +1,360 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
```

  Also, this doesn't follow Go convention (the package name should not include `_`).
  After we address https://github.com/kubernetes-sigs/kueue/pull/10359/changes#r3087999447, this could be resolved.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T17:31:34Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:1** by [@kannon92](https://github.com/kannon92) — 2026-04-15T17:31:34Z
  I have to enable DRA specific stuff to get this to work. Would that be a problem for the existing fair sharing?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T17:31:36Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:318** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-15T17:31:36Z
```diff
@@ -0,0 +1,360 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				getDeviceClassMappings(),
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "2").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "2")
+			wlA3 := createWorkloadCPUOnly("lq-a", "2")
+
+			ginkgo.By("Verifying lq-b workload is admitted first (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Finishing lq-b workload to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is admitted after quota is freed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				getDeviceClassMappings(),
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		// This test verifies that the existing resourceWeights in AdmissionFairSharing
+		// works for DRA logical resources without needing any new API fields.
+		ginkgo.It("should prioritize lower weighted usage", framework.SlowSpec, func() {
```

  I think that there is the same problem with https://github.com/kubernetes-sigs/kueue/pull/10359#discussion_r3079168853 here.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-15T17:33:50Z

Otherwise, LGTM

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T19:59:13Z

- **keps/2941-DRA/README.md:834** by [@kannon92](https://github.com/kannon92) — 2026-04-15T19:59:13Z
```diff
@@ -826,6 +827,33 @@ Processing Flow:
 
 This architecture separates concerns between DRA processing (controller) and queue management (scheduler), enabling robust error handling and retry logic for DRA-specific operations.
 
+### Integration with Admission Fair Sharing
+
+DRA logical resources participate in Admission Fair Sharing (AFS) when both DRA and AFS are enabled.
+The logical resource from `deviceClassMappings.name` lands in the workload's admitted `ResourceUsage`,
+and `CalcFSUsageFromResources` applies weights to all resources without filtering for DRA. This means
```

  good call. updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T19:59:30Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/dra_fair_sharing_test.go:17** by [@kannon92](https://github.com/kannon92) — 2026-04-15T19:59:29Z
```diff
@@ -0,0 +1,360 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
```

  ah thanks. addressed by moving this to fair sharing.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T19:59:51Z

- **test/integration/singlecluster/scheduler/dra_fairsharing/suite_test.go:95** by [@kannon92](https://github.com/kannon92) — 2026-04-15T19:59:51Z
```diff
@@ -0,0 +1,149 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"context"
+	"testing"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/rest"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	schdcache "sigs.k8s.io/kueue/pkg/cache/scheduler"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/controller/core"
+	"sigs.k8s.io/kueue/pkg/controller/core/indexer"
+	"sigs.k8s.io/kueue/pkg/dra"
+	"sigs.k8s.io/kueue/pkg/features"
+	"sigs.k8s.io/kueue/pkg/scheduler"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption/fairsharing"
+	"sigs.k8s.io/kueue/pkg/webhooks"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var (
+	cfg       *rest.Config
+	k8sClient client.Client
+	ctx       context.Context
+	fwk       *framework.Framework
+)
+
+func TestScheduler(t *testing.T) {
+	gomega.RegisterFailHandler(ginkgo.Fail)
+
+	ginkgo.RunSpecs(t,
+		"DRA + Fair Sharing Suite",
+	)
+}
+
+var _ = ginkgo.BeforeSuite(func() {
+	features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.DynamicResourceAllocation, true)
+
+	fwk = &framework.Framework{
+		WebhookPath: util.WebhookPath,
+		APIServerFeatureGates: []string{
+			"DynamicResourceAllocation=true",
+		},
+		APIServerRuntimeConfig: []string{
+			"resource.k8s.io/v1beta2=true",
+		},
+	}
+	cfg = fwk.Init()
+	ctx, k8sClient = fwk.SetupClient(cfg)
+})
+
+var _ = ginkgo.AfterSuite(func() {
+	fwk.Teardown()
+})
+
+var _ = ginkgo.ReportAfterSuite("Generate JUnit Report", func(report ginkgo.Report) {
+	err := util.ConfigureSuiteReporting(report)
+	gomega.Expect(err).NotTo(gomega.HaveOccurred())
+})
+
+func getDeviceClassMappings() []config.DeviceClassMapping {
+	return []config.DeviceClassMapping{
+		{
+			Name:             corev1.ResourceName("whole-gpus"),
+			DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+		},
+	}
+}
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-15T20:00:48Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:1** by [@kannon92](https://github.com/kannon92) — 2026-04-15T20:00:47Z
  removed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-16T13:35:59Z

- **pkg/workload/workload_test.go:3183** by [@kannon92](https://github.com/kannon92) — 2026-04-16T13:35:59Z
```diff
@@ -3092,3 +3092,130 @@ func TestIsExplicitlyRequestingTAS(t *testing.T) {
 		})
 	}
 }
+
+func TestCalcFSUsageFromResourcesWithDRA(t *testing.T) {
+	tests := map[string]struct {
+		consumed   corev1.ResourceList
+		penalty    corev1.ResourceList
+		lqWeight   float64
+		resWeights map[corev1.ResourceName]float64
+		wantUsage  float64
+	}{
+		"DRA resource with default weight": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty:    corev1.ResourceList{},
+			lqWeight:   1,
+			resWeights: map[corev1.ResourceName]float64{},
+			wantUsage:  2, // default weight is 1, so 1 * 2 / 1 = 2
+		},
+		"DRA resource with explicit weight": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 3.0,
+			},
+			wantUsage: 6, // 3 * 2 / 1 = 6
+		},
+		"mixed CPU and DRA resources": {
+			consumed: corev1.ResourceList{
+				corev1.ResourceCPU: resource.MustParse("4"),
+				"gpu-logical":      resource.MustParse("2"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				corev1.ResourceCPU: 1.0,
+				"gpu-logical":      5.0,
+			},
+			wantUsage: 14, // (1*4 + 5*2) / 1 = 14
+		},
+		"DRA resource with weight zero contributes nothing": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("10"),
+			},
+			penalty:  corev1.ResourceList{},
+			lqWeight: 1,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 0,
+			},
+			wantUsage: 0,
+		},
+		"DRA resource in penalty only": {
+			consumed: corev1.ResourceList{},
+			penalty: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("3"),
+			},
+			lqWeight:   1,
+			resWeights: map[corev1.ResourceName]float64{},
+			wantUsage:  3, // default weight 1, 1 * 3 / 1 = 3
+		},
+		"DRA resource in both consumed and penalty": {
+			consumed: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("2"),
+			},
+			penalty: corev1.ResourceList{
+				"gpu-logical": resource.MustParse("1"),
+			},
+			lqWeight: 2,
+			resWeights: map[corev1.ResourceName]float64{
+				"gpu-logical": 4.0,
+			},
+			wantUsage: 6, // 4 * (2+1) / 2 = 6
+		},
+	}
+
+	for name, tc := range tests {
+		t.Run(name, func(t *testing.T) {
+			got := CalcFSUsageFromResources(tc.consumed, tc.penalty, tc.lqWeight, tc.resWeights)
+			if got != tc.wantUsage {
+				t.Errorf("CalcFSUsageFromResources() = %v, want %v", got, tc.wantUsage)
+			}
+		})
+	}
+}
+
+func TestSumTotalRequestsWithDRAFromAdmission(t *testing.T) {
+	now := time.Now()
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-16T13:36:11Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:318** by [@kannon92](https://github.com/kannon92) — 2026-04-16T13:36:11Z
```diff
@@ -0,0 +1,360 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra_fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				getDeviceClassMappings(),
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "2").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "2")
+			wlA3 := createWorkloadCPUOnly("lq-a", "2")
+
+			ginkgo.By("Verifying lq-b workload is admitted first (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Finishing lq-b workload to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is admitted after quota is freed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				getDeviceClassMappings(),
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		// This test verifies that the existing resourceWeights in AdmissionFairSharing
+		// works for DRA logical resources without needing any new API fields.
+		ginkgo.It("should prioritize lower weighted usage", framework.SlowSpec, func() {
```

  fixed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-16T17:39:31Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T17:28:07Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
```

  I'm not sure if this scenario proves AFS × DRA works well. Because either the combination works or not can succeed this scenario.
  
  Let's dig into the current scenario in the following:
  
  
  1. Saturate lq-a with DRA workloads:
  - wlA1 (lq-a): CPU=1, ResourceClaimTemplate(1 GPU)
  - wlA2 (lq-a): CPU=1, ResourceClaimTemplate(1 GPU)
  
  Both admitted. lq-a is consuming:
  - CPU = 2 (1 + 1)
  - whole-gpus = 2 (1 + 1)
  
  2. Competing CPU-only workloads:
  - wlB1 (lq-b): CPU=2  (lq-b has ZERO prior history)
  - wlA3 (lq-a): CPU=2
  
  Only one can be admitted (quota is only CPU=2), and the AFS picks the LQ with lower accumulated usage.
  
  As a result, lq-a historical usage is either:
  
  - If DRA is counted (the feature working), lq-b wins in the DRS tournament based on the following usage:
     - usage(lq-a) = 1×CPU(2) + 1×whole-gpus(2) = 4
     - usage(lq-b) = 0
  - If DRA is ignored (DRA × AFS combination is broken), lq-b still wins in the DRS tournament based on the following usage:
     - usage(lq-a) = 1×CPU(2) = 2
     - usage(lq-b) = 0

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:41** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T17:38:40Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
```

  So, I would propose to refine the current scenario, like the following A or B scenario.
  I believe that Scenario B is even better in this case, but please let me know what you think @mimowo @gabesaba 
  
  - Scenario A
  
  Remove `Request(corev1.ResourceCPU, "1")` from createWorkloadWithDRA so that wlA1 and wlA2 consume GPU only. This scenario A allows us to simulate the following DRS tournament:
  
  - DRA counted (feature works correctly): lq-b wins in the DRS tournament based on:
    - lq-a usage = 2 (from GPUs)
    - lq-b = 0
  - DRA ignored (feature doesn't work correctly): the result will be randomly (It probably fails at some time)
    - lq-a usage = 0
    - lq-b = 0
  
  - Scenario B 
  
  Give lq-b a small CPU-only workload to wlA1 and wlA2 so that lq-b history is > 0 but < lq-a GPU history. 
  This scenario B allows us to simulate the following DRS tournament based on:
  
  - DRA counted (feature works correctly): lq-a GPU history dominates, and then lq-b always wins.
  - DRA ignored (feature doesn't work correctly): lq-b actually has more CPU history and than lq-a wins.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-16T18:16:19Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T17:55:46Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "2").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "2")
+			wlA3 := createWorkloadCPUOnly("lq-a", "2")
+
+			ginkgo.By("Verifying lq-b workload is admitted first (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Finishing lq-b workload to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is admitted after quota is freed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		// This test verifies that the existing resourceWeights in AdmissionFairSharing
+		// works for DRA logical resources without needing any new API fields.
+		ginkgo.It("should prioritize lower weighted usage", framework.SlowSpec, func() {
```

  I am also not sure if this scenario really verifies the weight's effect in the case of AFS and DRA usage.
  Let's dig into the current scenario in the following steps:
  
  1. The lq-a runs a DRA workload:
  - wlA1 (lq-a): CPU=1, ResourceClaimTemplate(1 GPU)
  
   The wlA1 is admitted, then finished. lq-a historical usage will be the following:
  - CPU: 1 (weight: 1) → weighted resource amount: 1
  - whole-gpus: 1 (weight: 5.0) → weighted resource amount: 5
  - Total usage = 6 (1 + 5)
  
  2. competing CPU-only workloads like the following:
  - wlB (lq-b): CPU=1
  - wlA2 (lq-a): CPU=1
  
  The only one wl (wlB or wlA2) can run because the CQ CPU quota is only 1. AFS picks the LQ with lower usage. So, even if resourceWeights are `{"whole-gpus": 1.0}`, this scenario will pass.

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:303** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-16T18:16:13Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "2").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "2")
+			wlA3 := createWorkloadCPUOnly("lq-a", "2")
+
+			ginkgo.By("Verifying lq-b workload is admitted first (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Finishing lq-b workload to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is admitted after quota is freed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		// This test verifies that the existing resourceWeights in AdmissionFairSharing
+		// works for DRA logical resources without needing any new API fields.
+		ginkgo.It("should prioritize lower weighted usage", framework.SlowSpec, func() {
```

  So, I would propose the following scenario instead:
  
  0. Prepare CQ with 3 CPU quota and 4 whole-gpus quota
  1. Deploy the following workloads:
  - wlA1 (lq-a): CPU=1, ResourceClaimTemplate(1 GPU) → weighted resource amount: 6 (CPU:1, whole-gpus:1)
  - wlB1, wlB2, wlB3 (lq-b): CPU=1 → weighted resource amount: 3 (CPU:3, whole-gpus:0)
  2. wlB1, wlB2, wlB3 should be respected because wlA1 consume 5 weighted resource ammount.
  
  This scenario allows us to check if the DRA ResouceClaimTemplate weight can properly contribute to AFS mechanism because if the weight is not considered in the DRA resource (weight: 1), the considered lq-a resource will be 2, and the considered lq-b resource will be 3, then the lq-a workloads are respected.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-17T20:43:02Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:41** by [@kannon92](https://github.com/kannon92) — 2026-04-17T20:43:02Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
```

  I pushed an update for this.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-04-17T20:43:09Z

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:303** by [@kannon92](https://github.com/kannon92) — 2026-04-17T20:43:09Z
```diff
@@ -0,0 +1,370 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "2").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Saturating the CQ with lq-a using DRA workloads")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+
+			ginkgo.By("Waiting for lq-a workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Finishing lq-a workloads to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2)
+
+			ginkgo.By("Creating competing workloads from both LQs")
+			wlB1 := createWorkloadCPUOnly("lq-b", "2")
+			wlA3 := createWorkloadCPUOnly("lq-a", "2")
+
+			ginkgo.By("Verifying lq-b workload is admitted first (lower usage due to DRA history)")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Finishing lq-b workload to free quota")
+			util.FinishWorkloads(ctx, k8sClient, wlB1)
+
+			ginkgo.By("Verifying lq-a workload is admitted after quota is freed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA3)
+		})
+
+		ginkgo.It("LocalQueue status ConsumedResources includes DRA logical resources", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq-consumed").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "10").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "4").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-consumed", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA)
+			util.MustCreate(ctx, k8sClient, lqA)
+
+			ginkgo.By("Creating DRA workload")
+			wl := createWorkloadWithDRA("lq-consumed")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+
+			ginkgo.By("Verifying ConsumedResources includes DRA logical resource")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var lq kueue.LocalQueue
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA), &lq)).To(gomega.Succeed())
+				g.Expect(lq.Status.FairSharing).NotTo(gomega.BeNil())
+				g.Expect(lq.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
+				consumed := lq.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
+				g.Expect(consumed).NotTo(gomega.BeEmpty())
+				// Check that the DRA logical resource "whole-gpus" is tracked
+				gpuQuantity, hasGPU := consumed["whole-gpus"]
+				g.Expect(hasGPU).To(gomega.BeTrue(), "ConsumedResources should include DRA logical resource 'whole-gpus'")
+				g.Expect(gpuQuantity.Cmp(resource.MustParse("0"))).To(gomega.BeNumerically(">", 0), "whole-gpus consumed should be positive")
+			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+	})
+
+	ginkgo.When("AdmissionFairSharing.ResourceWeights applies weight to DRA logical resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				&config.AdmissionFairSharing{
+					UsageSamplingInterval: metav1.Duration{Duration: 1 * time.Second},
+					UsageHalfLifeTime:     metav1.Duration{Duration: 1 * time.Second},
+					ResourceWeights: map[corev1.ResourceName]float64{
+						"whole-gpus": 5.0, // Weight DRA logical resource via existing resourceWeights
+					},
+				},
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		// This test verifies that the existing resourceWeights in AdmissionFairSharing
+		// works for DRA logical resources without needing any new API fields.
+		ginkgo.It("should prioritize lower weighted usage", framework.SlowSpec, func() {
```

  updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T06:40:23Z

Thank you for updating that!
I left small comments.

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:135** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T06:28:39Z
```diff
@@ -0,0 +1,396 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
```

  The scenario looks great, but I think that this has some flaky test risks for the following reasons:
  
  A. Based on sampling timing, the usage balance could be inverted because the current usageHalfLifeTime is very short (1sec)
  B. The usage balance between lq-a and lq-b could be incorrect because the current test doesn't check LocalQueue status before phase 2 (`With DRA counted: lq-a=CPU(2)+GPU(2)=4 > lq-b=CPU(3) → lq-b admitted first.`).
  
  So, could you fix the parameters based on the following suggestions?

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:83** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T06:29:16Z
```diff
@@ -0,0 +1,396 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
```

  ```suggestion
  					metav1.Duration{Duration: 5 * time.Second},
  ```
  
  Due to reason A

- **test/integration/singlecluster/scheduler/fairsharing/dra_fair_sharing_test.go:172** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-20T06:34:46Z
```diff
@@ -0,0 +1,396 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package fairsharing
+
+import (
+	"time"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	resourcev1 "k8s.io/api/resource/v1"
+	resourceapi "k8s.io/api/resource/v1beta2"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	config "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/test/integration/framework"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("DRA + Admission Fair Sharing", func() {
+	ginkgo.When("Using AdmissionFairSharing with DRA resources", ginkgo.Ordered, func() {
+		var (
+			defaultFlavor *kueue.ResourceFlavor
+			gpuFlavor     *kueue.ResourceFlavor
+			ns            *corev1.Namespace
+			deviceClass   *resourceapi.DeviceClass
+			rct           *resourcev1.ResourceClaimTemplate
+
+			cqs []*kueue.ClusterQueue
+			lqs []*kueue.LocalQueue
+			wls []*kueue.Workload
+		)
+
+		createWorkloadWithDRA := func(queue string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-dra-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).
+					Request(corev1.ResourceCPU, "1").
+					ResourceClaimTemplate("gpu-claim", "gpu-claim-template").
+					Obj()).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		createWorkloadCPUOnly := func(queue string, cpuRequests string) *kueue.Workload {
+			wl := utiltestingapi.MakeWorkloadWithGeneratedName("wl-cpu-", ns.Name).
+				Queue(kueue.LocalQueueName(queue)).
+				Request(corev1.ResourceCPU, cpuRequests).
+				Obj()
+
+			util.MustCreate(ctx, k8sClient, wl)
+			wls = append(wls, wl)
+			return wl
+		}
+
+		ginkgo.BeforeAll(func() {
+			fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(
+				afsConfig(
+					metav1.Duration{Duration: 1 * time.Second},
+					metav1.Duration{Duration: 1 * time.Second},
+				),
+				[]config.DeviceClassMapping{
+					{
+						Name:             corev1.ResourceName("whole-gpus"),
+						DeviceClassNames: []corev1.ResourceName{"gpu.example.com"},
+					},
+				},
+			))
+
+			defaultFlavor = utiltestingapi.MakeResourceFlavor("default").Obj()
+			util.MustCreate(ctx, k8sClient, defaultFlavor)
+			gpuFlavor = utiltestingapi.MakeResourceFlavor("gpu-flavor").Obj()
+			util.MustCreate(ctx, k8sClient, gpuFlavor)
+
+			ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "dra-afs-")
+
+			// Create DeviceClass for DRA
+			deviceClass = &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "gpu.example.com",
+				},
+			}
+			util.MustCreate(ctx, k8sClient, deviceClass)
+
+			// Create ResourceClaimTemplate
+			rct = utiltesting.MakeResourceClaimTemplate("gpu-claim-template", ns.Name).
+				DeviceRequest("gpu", "gpu.example.com", 1).
+				Obj()
+			util.MustCreate(ctx, k8sClient, rct)
+		})
+
+		ginkgo.AfterAll(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, rct, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, gpuFlavor, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, defaultFlavor, true)
+			fwk.StopManager(ctx)
+		})
+
+		ginkgo.AfterEach(func() {
+			for _, wl := range wls {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, wl, true)
+			}
+			for _, lq := range lqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
+			}
+			for _, cq := range cqs {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, cq, true)
+			}
+		})
+
+		ginkgo.It("DRA resource usage affects AFS admission ordering", framework.SlowSpec, func() {
+			cq := utiltestingapi.MakeClusterQueue("cq").
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(defaultFlavor.Name).
+						Resource(corev1.ResourceCPU, "5").
+						Obj(),
+				).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(gpuFlavor.Name).
+						Resource("whole-gpus", "2").
+						Obj(),
+				).
+				AdmissionMode(kueue.UsageBasedAdmissionFairSharing).
+				Obj()
+			cqs = append(cqs, cq)
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lqA := utiltestingapi.MakeLocalQueue("lq-a", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqB := utiltestingapi.MakeLocalQueue("lq-b", ns.Name).
+				FairSharing(&kueue.FairSharing{Weight: ptr.To(resource.MustParse("1"))}).
+				ClusterQueue(cq.Name).Obj()
+			lqs = append(lqs, lqA, lqB)
+			util.MustCreate(ctx, k8sClient, lqA)
+			util.MustCreate(ctx, k8sClient, lqB)
+
+			ginkgo.By("Building usage history: DRA workloads on lq-a (CPU=2,GPU=2), CPU workload on lq-b (CPU=3)")
+			wlA1 := createWorkloadWithDRA("lq-a")
+			wlA2 := createWorkloadWithDRA("lq-a")
+			wlBInit := createWorkloadCPUOnly("lq-b", "3")
+
+			ginkgo.By("Waiting for all initial workloads to be admitted")
+			util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wlA1, wlA2, wlBInit)
+
+			ginkgo.By("Finishing all initial workloads to build AFS history")
+			util.FinishWorkloads(ctx, k8sClient, wlA1, wlA2, wlBInit)
+
```

  ```suggestion
  
  			ginkgo.By("Waiting for AFS ConsumedResources to reflect phase-1 usage and invariant to
    hold")
  			gomega.Eventually(func(g gomega.Gomega) {
  						var lqAObj, lqBObj kueue.LocalQueue
  						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqA),
    &lqAObj)).To(gomega.Succeed())
  						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(lqB),
    &lqBObj)).To(gomega.Succeed())
  						g.Expect(lqAObj.Status.FairSharing).NotTo(gomega.BeNil())
  						g.Expect(lqBObj.Status.FairSharing).NotTo(gomega.BeNil())
  						g.Expect(lqAObj.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
  						g.Expect(lqBObj.Status.FairSharing.AdmissionFairSharingStatus).NotTo(gomega.BeNil())
  
  						lqAConsumed := lqAObj.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
  						lqBConsumed := lqBObj.Status.FairSharing.AdmissionFairSharingStatus.ConsumedResources
  
  						g.Expect(lqAConsumed).To(gomega.HaveKey(corev1.ResourceCPU))
  						g.Expect(lqAConsumed).To(gomega.HaveKey(corev1.ResourceName("whole-gpus")))
  						g.Expect(lqBConsumed).To(gomega.HaveKey(corev1.ResourceCPU))
  
  						lqATotal := lqAConsumed[corev1.ResourceCPU].AsApproximateFloat64() +
  						lqAConsumed["whole-gpus"].AsApproximateFloat64()
  						lqBTotal := lqBConsumed[corev1.ResourceCPU].AsApproximateFloat64()
  						g.Expect(lqATotal).To(gomega.BeNumerically(">", lqBTotal),
                "expected lq-a usage (CPU+GPU=%v) > lq-b usage (CPU=%v) before phase 2", lqATotal,
    lqBTotal)
  			}, util.Timeout, util.ShortInterval).Should(gomega.Succeed())
  ```
  
  Due to reason B.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-20T14:45:55Z

Awesome! This was NOT the truly trivial verifications, thank you!
/lgtm
/approve
