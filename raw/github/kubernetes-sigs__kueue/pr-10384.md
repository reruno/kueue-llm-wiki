# PR #10384: Add feature gate for invalid ClusterQueue onFlavors updates

**Summary**: Add feature gate for invalid ClusterQueue onFlavors updates

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10384

**Last updated**: 2026-04-13T11:14:05Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ShaanveerS](https://github.com/ShaanveerS)
- **Branch**: `fg-ac-onflavors` → `main`
- **Created**: 2026-04-10T12:36:24Z
- **Updated**: 2026-04-13T11:14:05Z
- **Merged**: 2026-04-13T10:16:12Z
- **Closed**: 2026-04-13T10:16:13Z
- **Labels**: `kind/feature`, `lgtm`, `release-note-action-required`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 5   **+64 / -8**

## Description

#### What type of PR is this?
/kind feature
#### What this PR does / why we need it:
Introduce the `RejectUpdatesToCQWithInvalidOnFlavors` feature gate.
When the feature gate is disabled, updates to legacy ClusterQueues that already contain invalid `AdmissionCheckStrategy.OnFlavors` references are still allowed as long as those entries are unchanged.
When enabled, updates to such ClusterQueues are rejected until the invalid flavor references are fixed.

#### Which issue(s) this PR fixes:
Follow up to https://github.com/kubernetes-sigs/kueue/pull/10336

#### Does this PR introduce a user-facing change?
```release-note
AdmissionChecks: Add the alpha `RejectUpdatesToCQWithInvalidOnFlavors` feature gate (disabled by default) to reject updates to existing ClusterQueues with invalid `AdmissionCheckStrategy.OnFlavors` references. 
action required: when enabling this feature gate, fix any existing invalid `OnFlavors` references before updating the affected ClusterQueues.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-10T12:36:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ec5ebcb8e5033c597ec87a809afab7be705ec9a3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69dca4a5eada910008a0dfb0 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-10T12:36:35Z

Hi @ShaanveerS. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-10T12:38:06Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-13T07:46:46Z

LGTM label has been added.  <details>Git tree hash: 65bdf3d03f51765a8257a5114c70ba763360a955</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-13T07:46:48Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10384#pullrequestreview-4097123904" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10384#" title="Author self-approved">ShaanveerS</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-13T08:57:59Z

/lgtm
/unhold
as the remaining comment is now addressed

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-13T08:58:06Z

LGTM label has been added.  <details>Git tree hash: a598aed48e6438e338c8334381fd99cd7faf4311</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T07:46:39Z

Thank you 👍 
/lgtm
/approve

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-13T07:56:46Z

Otherwise LGTM, thank you!

/hold

- **pkg/features/kube_features.go:358** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-13T07:56:24Z
```diff
@@ -352,6 +352,13 @@ const (
 	// Pod integration's IsActive() check, allowing quota to be released immediately
 	// when preempted pods begin terminating rather than waiting for the grace period.
 	FastQuotaReleaseInPodIntegration featuregate.Feature = "FastQuotaReleaseInPodIntegration"
+
+	// owner: @ShaanveerS
+	//
+	// pr: https://github.com/kubernetes-sigs/kueue/pull/10336
```

  ```suggestion
  	// issue: https://github.com/kubernetes-sigs/kueue/issues/7259
  ```
  
  Could you use the tracking issue?

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-04-13T08:09:19Z

- **pkg/features/kube_features.go:358** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-04-13T08:09:19Z
```diff
@@ -352,6 +352,13 @@ const (
 	// Pod integration's IsActive() check, allowing quota to be released immediately
 	// when preempted pods begin terminating rather than waiting for the grace period.
 	FastQuotaReleaseInPodIntegration featuregate.Feature = "FastQuotaReleaseInPodIntegration"
+
+	// owner: @ShaanveerS
+	//
+	// pr: https://github.com/kubernetes-sigs/kueue/pull/10336
```

  done!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-13T09:26:19Z

- **pkg/features/kube_features.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-13T09:26:19Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  ```suggestion
  	RejectUpdatesToCQWithInvalidOnFlavors: {
  		{Version: version.MustParse("0.16"), Default: false, PreRelease: featuregate.Alpha},
  	},
  ```
  
  @mimowo Isn't this `0.16`? Because we will cherry-pick this to released branch, right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T10:06:32Z

- **pkg/features/kube_features.go:555** by [@mimowo](https://github.com/mimowo) — 2026-04-13T10:06:32Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  we are not going to cherrypick the FG I think, should we?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T10:11:47Z

- **pkg/features/kube_features.go:555** by [@mimowo](https://github.com/mimowo) — 2026-04-13T10:11:47Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  But even if we decide to cherrypick, Im ok with that I would prepare a dedicated PR for each branch setting the version as the branhc. This way the decision is decoupled. So on 0.17 release branch it would be 0.17.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-13T10:17:25Z

- **pkg/features/kube_features.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-13T10:17:25Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  > we are not going to cherrypick the FG I think, should we?
  
  Right? IIUC, we introduced this FG to avoid breaking their production just by updating Kueue version.
  So, cherry-picking as alpha would be worth it.
  
  If we fully align with K8S compatible rules, we can not make validations strong, but we will ignore the rule here. So, at least, I think we should disable it by default for a while.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T10:45:00Z

- **pkg/features/kube_features.go:555** by [@mimowo](https://github.com/mimowo) — 2026-04-13T10:45:00Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  > Right? IIUC, we introduced this FG to avoid breaking their production just by updating Kueue version.
  So, cherry-picking as alpha would be worth it.
  
  But the default behavior already cherrypicked to 0.16 and 0.17 is to ignore the pre-existing validation errors, so as RejectUpdatesToCQWithInvalidOnFlavors=false. This means the user cannot say RejectUpdatesToCQWithInvalidOnFlavorz=true, but this is probably ok. So, I'm not clear what cherrypicking would change there.
  
  > If we fully align with K8S compatible rules, we can not make validations strong, but we will ignore the rule here. So, at least, I think we should disable it by default for a while.
  
  Sure, so for 0.18 the feature gate is planned as Alpha, and we may consider the FG as Beta in 0.20 or later.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-13T10:47:17Z

- **pkg/features/kube_features.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-13T10:47:17Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  > But the default behavior already cherrypicked to 0.16 and 0.17 is to ignore the pre-existing validation errors, so as RejectUpdatesToCQWithInvalidOnFlavors=false. 
  
  But the commits are not released yet. So, I still think that we should cherry pick this one as alpha to released branches.
  
  > Sure, so for 0.18 the feature gate is planned as Alpha, and we may consider the FG as Beta in 0.20 or later.
  
  I think so too.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T10:51:20Z

- **pkg/features/kube_features.go:555** by [@mimowo](https://github.com/mimowo) — 2026-04-13T10:51:20Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  > But the commits are not released yet. So, I still think that we should cherry pick this one as alpha to released branches.
  
  I'm ok with that, I just wanted to clarify about the argument: "we introduced this FG to avoid breaking their production just by updating Kueue version." is not fully accurate. Avoiding the production breaking is already the default behavior that was cherrypicked. 
  
  The feature gate is introduced to allow simplifying the code in the future and to drop the code responsible for graceful handling of invalid CQs.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-13T11:14:05Z

- **pkg/features/kube_features.go:555** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-13T11:14:05Z
```diff
@@ -543,6 +550,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	FastQuotaReleaseInPodIntegration: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
 	},
+	RejectUpdatesToCQWithInvalidOnFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  > Avoiding the production breaking is already the default behavior that was cherrypicked.
  
  I am missing something here. Do you indicate https://github.com/kubernetes-sigs/kueue/pull/10336?
  If so, what "the default behavior" does that mean?
  
  10336 has not been released yet, so no default behavior yet.
  If we didn't cherry-pick this FG PR, the ClusterQueue updates sometimes will fail on an invalid ClusterQueue user env. Surely, the invalid ClusterQueue parameters are bad, but from the user PoV, they can not operate on what they have been proceeding with before Kueue update.
  
  Isn't the situation "breaking"?
