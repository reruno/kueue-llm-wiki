# PR #104: Add PriorityClass in Workload api

**Summary**: Add PriorityClass in Workload api

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/104

**Last updated**: 2022-03-17T16:31:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@denkensk](https://github.com/denkensk)
- **Branch**: `add-workload-priority` → `main`
- **Created**: 2022-03-09T12:56:16Z
- **Updated**: 2022-03-17T16:31:20Z
- **Merged**: 2022-03-17T16:31:19Z
- **Closed**: 2022-03-17T16:31:20Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: _none_
- **Changed files**: 12   **+372 / -11**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->
/kind feature

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
The first part of #82

## Discussion

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-09T12:58:49Z

/assign @ahg-g

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-09T16:09:54Z

/test pull-kueue-test-integration-main

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-10T00:26:03Z

/test pull-kueue-test-integration-main

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-15T08:09:52Z

/test pull-kueue-test-integration-main

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-15T12:48:43Z

/test pull-kueue-test-integration-main

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:17:58Z

/test pull-kueue-test-integration-main

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:28:14Z

> I would continue with the simpler approach of looking up the priority at the time we add the workload to the queue and only store it in the internal struct.

It turns out to be quite invasive (we have to pass contexts, logs, a client) all within an event handler that will be quite busy.

I preferred the older approach of having it done in the job_controller until we have a webhook :(

But I won't block this PR on that.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T20:06:48Z

> > I would continue with the simpler approach of looking up the priority at the time we add the workload to the queue and only store it in the internal struct.
> 
> It turns out to be quite invasive (we have to pass contexts, logs, a client) all within an event handler that will be quite busy.
> 
> I preferred the older approach of having it done in the job_controller until we have a webhook :(
> 
> But I won't block this PR on that.

I thought it would be simpler because we could look it up in the create/update event handler and populate it in a workload.Info struct directly which we would pass to the Add/UpdateWorkload functions (we need to change them to receive `workload.Info` instead of `kueue.Workload`).

What I think is problematic still is that there is really no setup in MVP that would allow an Admin to restrict who can use which priority classes. What do you think of the following:

1) remove Priority from QueuedWorkload completely for now (we will add it once we have a webhook)
2) add PriorityClass to Queue, workloads inherit this priority.
3) an admin now has a path with our initial MVP to a setup where it can restrict which namespaces can use which priority by restrict Queue creation to be done only by the admin.
4) this will also decouple pod priority from job priority for v1.Job

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-16T04:21:02Z

> what is populating the PriorityClassName now?

It's populated by different controller which need to construct workloads. I will add this in job-controller in follow up pr. @alculquicondor

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-16T08:02:21Z

> I thought it would be simpler because we could look it up in the create/update event handler and populate it in a workload.Info struct directly which we would pass to the Add/UpdateWorkload functions (we need to change them to receive workload.Info instead of kueue.Workload).

Yes. It would be simpler. I refactored the code as the suggestion.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-16T08:39:41Z

/retest

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T11:09:57Z

I am sorry for the back and forth, this plumbing doesn't look "right". I am pretty sure there will be places where we will miss to set `info.Priority` and so it will be dangerous to continue like this unless the priority look up is done inside NewInfo, which I think isn't going to be pretty because it means `NewInfo` will need to receive client/ctx/logger, and it is used in multiple places including the cache.

I coming back to having the priority int on the QueuedWorkload and having it populated by the job-controller. Thinking more about how to prevent users from circumventing priority classes: users shouldn't be creating QueuedWorkload instances directly, and so they really can't set the QueuedWorkload priority (at least in our MVP). 

Alex, I am pretty sure this is frustrating to you, sorry about that, I am happy to make the change myself if you don't have the time.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-16T11:39:07Z

> I coming back to having the priority int on the QueuedWorkload and having it populated by the job-controller. Thinking more about how to prevent users from circumventing priority classes: users shouldn't be creating QueuedWorkload instances directly, and so they really can't set the QueuedWorkload priority (at least in our MVP).

Shall we only have Priority in QueuedWorkload or both PriorityClass and Priority?

> Alex, I am pretty sure this is frustrating to you, sorry about that, I am happy to make the change myself if you don't have the time.

It doesn't matter. It's worth putting in some effort before making the right decision. I will try to finish refactor the code today and tomorrow.

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T11:54:31Z

> Shall we only have Priority in QueuedWorkload or both PriorityClass and Priority?

Both, the job-controller populates it just like we do for pods. Initially I was concerned that there was no path for Admins to block usage of higher priority classes or preventing users from setting directly a high priority on the workload. But that is actually not true because admins could prevent users from creating QW directly and only allow them to create v1.Job.

I still think we want to have priority class on the Queue object but we can leave that as a followup.

> It doesn't matter. It's worth putting in some effort before making the right decision. I will try to finish refactor the code today and tomorrow.

Thank you, and I am sorry again for the back and forth, but indeed this was an informative exercise, at least for me.

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-16T13:14:22Z

Since this is very similar to my first version of the implementation, so I finish it quickly 😄  pls take a look again. Thanks. @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T13:34:46Z

It would be nice if we can add a [scheduler integration test](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/scheduler/scheduler_test.go) in a separate `ginkgo.It`

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:03:31Z

> It would be nice if we can add a [scheduler integration test](https://github.com/kubernetes-sigs/kueue/blob/main/test/integration/scheduler/scheduler_test.go) in a separate ginkgo.It

Added integration test for scheduler and job-controller in the second commit @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T15:47:33Z

/require-retest

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-17T15:59:09Z

/retest-required

### Comment by [@denkensk](https://github.com/denkensk) — 2022-03-17T16:04:07Z

Updated base on the review comments. @ahg-g

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T16:18:59Z

/retest-required

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T16:30:53Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-17T16:31:12Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/104#issuecomment-1071054487" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/104#" title="Author self-approved">denkensk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-09T13:19:16Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-09T13:17:58Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's capacity-scoped and generated according to the priority class of the
```

  ```suggestion
  	// It's ClusterQueue-scoped and generated according to the priority class of the
  ```
  
  maybe?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-09T15:59:13Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@denkensk](https://github.com/denkensk) — 2022-03-09T15:59:12Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's capacity-scoped and generated according to the priority class of the
```

  nice catch. updated

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-10T01:19:10Z

- **api/v1alpha1/queuedworkload_types.go:47** by [@ahg-g](https://github.com/ahg-g) — 2022-03-10T01:19:10Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
+	// job.
```

  how will priority impact order exactly? will workloads always be ordered first by priority then the QueueingStrategy? or will priority itself be one of the queuing strategies?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-10T01:31:09Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@ahg-g](https://github.com/ahg-g) — 2022-03-10T01:31:09Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  > generated according to the priorityclass
  
  hmm, this is not really enforced by Kueue, so it can't be claimed. Remember that the job controller we have is not really part of Kueue.
  
  I think this field should be a PriorityClassName, not the priority itself; the queuedworkload-controller reads the actual priority from the class and makes it available to Kueue internals.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-10T02:25:59Z

- **api/v1alpha1/queuedworkload_types.go:47** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-10T02:25:59Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
+	// job.
```

  you got me here, I was also looking for the func re-sorting every time a new queuedWorkload was added to a queue

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-10T02:42:58Z

- **api/v1alpha1/queuedworkload_types.go:47** by [@denkensk](https://github.com/denkensk) — 2022-03-10T02:42:57Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
+	// job.
```

  > how will priority impact order exactly? will workloads always be ordered first by priority then the QueueingStrategy? or will priority itself be one of the queuing strategies?
  
  Yes. It is one of the queuing strategies. Probably similar to yarn, where the user specifies whether to sort workloads by priority.  The workloads are ordered by the priority in the ClusterQueue.
  
  > I was also looking for the func re-sorting every time a new queuedWorkload was added to a queue
  
  It's only api definition. The implementation of the `QueueingStrategy` depends on the https://github.com/kubernetes-sigs/kueue/pull/97 I will add TODO to clarify.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-10T02:59:32Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@denkensk](https://github.com/denkensk) — 2022-03-10T02:59:32Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  SG. I can store the actual priority into the internal info of workload.  However, during the workload queuing process, the user can delete and recreate the priority classes with different values. Perhaps the value of the workload can be refreshed each time re-queue.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-10T16:22:37Z

- **api/v1alpha1/queuedworkload_types.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T16:08:14Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
```

  instead of located, should we say "published" or perhaps "posted"?
  
  It makes me think that we need a glossary of terms for our user documentation.

- **api/v1alpha1/queuedworkload_types.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T16:11:32Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  I think we should indeed have the priority value here too, as we do in PodSpec. Ideally it would be loaded at creation, but since we don't have a webhook yet, we might need to do it in the queuedworkload-controller for now.

- **api/v1alpha1/queuedworkload_types.go:47** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T16:17:37Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
+	// job.
```

  We have to think of 2 things: the default strategy and options for configuration.
  
  I think the default strategy should be order by priority and then by creation time in the case of ties.
  
  As for configuration, we can defer that to #8 and #10.

- **pkg/controller/workload/job/job_controller.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-10T16:19:22Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  What will be do for workloads that have multiple pod specs?
  
  Should we do this in the queued_workload controller instead?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-11T03:54:20Z

- **pkg/controller/workload/job/job_controller.go:377** by [@denkensk](https://github.com/denkensk) — 2022-03-11T03:54:20Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  I rarely see people setting different priorities in a single workload. But in this case, we need a fixed treatment and clear understanding for the user: like choosing the highest one as the priority of the workload?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-11T03:57:42Z

- **api/v1alpha1/queuedworkload_types.go:44** by [@denkensk](https://github.com/denkensk) — 2022-03-11T03:57:42Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
```

  `associated` can also be a candidate?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T04:18:24Z

- **api/v1alpha1/queuedworkload_types.go:44** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T03:42:26Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
```

  "queued"?

- **api/v1alpha1/queuedworkload_types.go:46** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T04:05:52Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  Having the actual priority on the workload spec means we need a way to limit setting it directly to prevent circumventing priority class (this is done for pods in by the priority admission controller); so we need a webhook to enforce that.
  
  One more thing, we need to limit which namespaces can use which PriorityClasses, this is done via quota scopes in k8s at the pod level; to enforce it at the QueuedWorkload level, we need a webhook as well.

- **api/v1alpha1/queuedworkload_types.go:47** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T04:08:57Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
+	// job.
```

  > I think the default strategy should be order by priority and then by creation time in the case of ties.
  
  where does OrderStrategy fit here? priority then OrderStrategy (which would be creation time or re-queue depending on the strategy)?

- **pkg/controller/workload/job/job_controller.go:377** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T04:15:57Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  > What will be do for workloads that have multiple pod specs?
  
  It is up to the custom workloads to decide from where to set it (e.g., based on the driver).
  
  > Should we do this in the queued_workload controller instead?
  
  how? queuedworkload_controller is not aware of the custom workload CRD.
  
  
  That makes me think, since order is done at the ClusterQueue level and Queue is simply a pointer to it; what if we have the priority on the Queue, and workloads simply inherit it from the Queue they are submitted to, and so users don't need to set it on the custom workload directly and we avoid those issues altogether.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-14T13:44:35Z

- **pkg/controller/workload/job/job_controller.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-14T13:44:34Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  >> Should we do this in the queued_workload controller instead?
  >
  > how? queuedworkload_controller is not aware of the custom workload CRD.
  
  I'm thinking that workloads should have a priority independent of the pod priority, although it might be confusing. Also that would mean that users have to set an annotation for it in the Job or something like that. Having it in the Queue is certainly a cleaner option.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-14T16:23:47Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@denkensk](https://github.com/denkensk) — 2022-03-14T16:23:46Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  I find it's difficult to add the priority as the field of workload spec if we don't have an admission web-hook to populate priority from priority class. When queued-workload-controller receive the create event of workload, the workload is already created, we don't have chance to populate priority from priority class unless we update the workload again on `create func`. 
  
  In comparison, it would make more sense to pass Priority as a parameter in the initialization of workload info as an internal data. WDYT? @ahg-g @alculquicondor

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-14T16:29:09Z

- **pkg/controller/workload/job/job_controller.go:377** by [@denkensk](https://github.com/denkensk) — 2022-03-14T16:29:08Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  I also agree that workloads have a separate priority independent of the pod priority. In fact here it already is kubeflow
  https://github.com/kubeflow/common/blob/2b40c8f8991e302920ee5536c0ad49dec6724c66/pkg/apis/common/v1/types.go#L208

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-14T16:59:27Z

- **pkg/controller/workload/job/job_controller.go:377** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-14T16:59:26Z
```diff
@@ -370,8 +371,24 @@ func (r *JobReconciler) ensureAtMostOneWorkload(ctx context.Context, job *batchv
 	return match, nil
 }
 
-func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.QueuedWorkload, error) {
-	w := &kueue.QueuedWorkload{
+func ConstructWorkloadFor(ctx context.Context, client client.Client,
+	job *batchv1.Job, scheme *runtime.Scheme) (w *kueue.QueuedWorkload, err error) {
+	var p int32
+	pcName := job.Spec.Template.Spec.PriorityClassName
```

  I see. Do you know what they do with it? Perhaps insert it in the pod specs?

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-14T17:43:03Z

- **api/v1alpha1/queuedworkload_types.go:46** by [@ahg-g](https://github.com/ahg-g) — 2022-03-14T17:43:03Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and generated according to the priorityclass of the
```

  I would continue with the simpler approach of looking up the priority at the time we add the workload to the queue and only store it in the internal struct.
  
  The idea is to show how this whole thing works, but we keep the issue open to tighten the API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-14T17:57:23Z

- **api/v1alpha1/queuedworkload_types.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-14T17:57:22Z
```diff
@@ -39,6 +39,13 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is located.
```

  queued is the most simple and efficient answer :)

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-15T13:12:43Z

- **api/v1alpha1/queuedworkload_types.go:52** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:49:37Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is queued.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and is taken from the PriorityClass of the job.
```

  I would remove this last line, the one above explains already that order is always at the CQ level

- **api/v1alpha1/queuedworkload_types.go:49** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:49:51Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
```

  ```suggestion
  	// Priority determines the order of access to the resources managed by the
  ```

- **api/v1alpha1/queuedworkload_types.go:50** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:51:20Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is queued.
```

  ```suggestion
  	// ClusterQueue the workload is actually queued.
  ```

- **pkg/constants/constants.go:31** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:53:21Z
```diff
@@ -24,4 +24,9 @@ const (
 
 	ManagerName       = "kueue-manager"
 	JobControllerName = "kueue-job-controller"
+
+	// DefaultPriorityWhenNoDefaultClassExists is used to set priority of pods
+	// that do not specify any priority class and there is no priority class
+	// marked as default.
+	DefaultPriorityWhenNoDefaultClassExists = 0
```

  ```suggestion
  	DefaultPriority = 0
  ```

- **pkg/constants/constants.go:28** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:53:54Z
```diff
@@ -24,4 +24,9 @@ const (
 
 	ManagerName       = "kueue-manager"
 	JobControllerName = "kueue-job-controller"
+
+	// DefaultPriorityWhenNoDefaultClassExists is used to set priority of pods
```

  ```suggestion
  	// DefaultPriorityWhenNoDefaultClassExists is used to set priority of workloads
  ```

- **pkg/controller/core/queuedworkload_controller.go:78** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:55:58Z
```diff
@@ -68,6 +72,13 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 	log := r.log.WithValues("queuedWorkload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	log.V(2).Info("QueuedWorkload create event")
 
+	// Pre-check for PriorityClass
+	_, err := workload.GetPriorityFromPriorityClass(context.Background(), r.client, wl.Spec.PriorityClassName)
+	if err != nil {
+		log.V(2).Info("Populate priority from priority class failed %v", err.Error())
```

  ```suggestion
  		log.Error(err, "Looking up workload priority %v")
  ```

- **pkg/controller/core/queuedworkload_controller.go:75** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T12:57:22Z
```diff
@@ -68,6 +72,13 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 	log := r.log.WithValues("queuedWorkload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	log.V(2).Info("QueuedWorkload create event")
 
+	// Pre-check for PriorityClass
```

  hmm, this means the workload will be ignored even if the the priority was created after the workload.
  
  I understand that in an ideal situation we would have a webhook and the workload will be rejected completely, but until then, I am wondering if we should continue to have the workload queued using the default priority and just log a message indicating that the priority wasn't found and default priority was used.

- **pkg/queue/queue.go:124** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T13:09:32Z
```diff
@@ -109,19 +115,40 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
```

  lets use the default priority and log a message. This shouldn't happen in the future once we have a webhook that populates the priority on workload creation.

- **pkg/queue/queue.go:142** by [@ahg-g](https://github.com/ahg-g) — 2022-03-15T13:12:08Z
```diff
@@ -109,19 +115,40 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
+	}
+	info.Priority = p
+
 	heap.Push(&cq.heap, *info)
 	return true
 }
 
-func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) {
+func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) bool {
 	item := cq.heap.items[workload.Key(w)]
 	info := *workload.NewInfo(w)
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
+	}
```

  lets encapulate this in a function that return the default priority when not found and logs an error; and use the function above as well.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:15:52Z

- **api/v1alpha1/queuedworkload_types.go:52** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:15:52Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is queued.
+	// The higher the value, the higher the priority.
+	// It's ClusterQueue-scoped and is taken from the PriorityClass of the job.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:15:55Z

- **api/v1alpha1/queuedworkload_types.go:49** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:15:55Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:15:58Z

- **api/v1alpha1/queuedworkload_types.go:50** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:15:58Z
```diff
@@ -39,6 +39,19 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// priority determines the order of access to the resources in the
+	// ClusterQueue associated with the queue where the workload is queued.
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:16:03Z

- **pkg/constants/constants.go:31** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:16:03Z
```diff
@@ -24,4 +24,9 @@ const (
 
 	ManagerName       = "kueue-manager"
 	JobControllerName = "kueue-job-controller"
+
+	// DefaultPriorityWhenNoDefaultClassExists is used to set priority of pods
+	// that do not specify any priority class and there is no priority class
+	// marked as default.
+	DefaultPriorityWhenNoDefaultClassExists = 0
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:16:08Z

- **pkg/constants/constants.go:28** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:16:08Z
```diff
@@ -24,4 +24,9 @@ const (
 
 	ManagerName       = "kueue-manager"
 	JobControllerName = "kueue-job-controller"
+
+	// DefaultPriorityWhenNoDefaultClassExists is used to set priority of pods
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:16:13Z

- **pkg/controller/core/queuedworkload_controller.go:78** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:16:12Z
```diff
@@ -68,6 +72,13 @@ func (r *QueuedWorkloadReconciler) Create(e event.CreateEvent) bool {
 	log := r.log.WithValues("queuedWorkload", klog.KObj(wl), "queue", wl.Spec.QueueName, "status", status)
 	log.V(2).Info("QueuedWorkload create event")
 
+	// Pre-check for PriorityClass
+	_, err := workload.GetPriorityFromPriorityClass(context.Background(), r.client, wl.Spec.PriorityClassName)
+	if err != nil {
+		log.V(2).Info("Populate priority from priority class failed %v", err.Error())
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:16:23Z

- **pkg/queue/queue.go:124** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:16:22Z
```diff
@@ -109,19 +115,40 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
```

  updated

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-15T16:16:29Z

- **pkg/queue/queue.go:142** by [@denkensk](https://github.com/denkensk) — 2022-03-15T16:16:29Z
```diff
@@ -109,19 +115,40 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
+	}
+	info.Priority = p
+
 	heap.Push(&cq.heap, *info)
 	return true
 }
 
-func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) {
+func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) bool {
 	item := cq.heap.items[workload.Key(w)]
 	info := *workload.NewInfo(w)
+
+	// Populate priority from priority class before enqueue.
+	p, err := workload.GetPriorityFromPriorityClass(context.Background(),
+		cq.client, info.Obj.Spec.PriorityClassName)
+	// Since the client is cached, the only possible error is NotFound
+	if apierrors.IsNotFound(err) {
+		return false
+	}
```

  updated

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-15T18:25:01Z

what is populating the PriorityClassName now?

- **api/v1alpha1/queuedworkload_types.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:12:52Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
```

  where is this default defined?

- **api/v1alpha1/queuedworkload_types.go:50** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:13:21Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// Priority determines the order of access to the resources managed by the
+	// ClusterQueue the workload is actually queued.
```

  ```suggestion
  	// ClusterQueue where the workload is queued.
  ```

- **pkg/queue/queue.go:89** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:17:53Z
```diff
@@ -66,20 +71,24 @@ func (q *Queue) AddOrUpdate(w *kueue.QueuedWorkload) {
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds pending workloads.
 type ClusterQueue struct {
+	client client.Client
 	// QueueingStrategy indicates the queueing strategy of the workloads
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
-
-	heap heapImpl
+	log              logr.Logger
+	heap             heapImpl
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue) *ClusterQueue {
+func newClusterQueue(client client.Client, cq *kueue.ClusterQueue) *ClusterQueue {
 	cqImpl := &ClusterQueue{
+		client: client,
 		heap: heapImpl{
 			less:  creationFIFO,
 			items: make(map[string]*heapItem),
 		},
+		log: ctrl.Log.WithName("cluster-queue"),
```

  If anything, the log should be in the manager, not in the queues, and passed to the functions that need it

- **pkg/queue/queue.go:116** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:22:26Z
```diff
@@ -109,19 +118,36 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	log := cq.log.WithValues("workload", workload.Key(info.Obj))
+	ctx := ctrl.LoggerInto(context.Background(), log)
```

  Instead, receive a context.

- **pkg/queue/queue.go:119** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-15T18:23:24Z
```diff
@@ -109,19 +118,36 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	log := cq.log.WithValues("workload", workload.Key(info.Obj))
+	ctx := ctrl.LoggerInto(context.Background(), log)
+	// Populate priority from priority class before enqueue.
+	p := workload.GetPriorityFromPriorityClass(ctx, cq.client,
+		info.Obj.Spec.PriorityClassName)
+	info.Priority = &p
+
 	heap.Push(&cq.heap, *info)
 	return true
 }
 
-func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) {
+func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) bool {
 	item := cq.heap.items[workload.Key(w)]
 	info := *workload.NewInfo(w)
```

  why not calculate the value in `NewInfo`?

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T01:06:19Z

- **api/v1alpha1/queuedworkload_types.go:48** by [@denkensk](https://github.com/denkensk) — 2022-03-16T01:06:19Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
```

  in the constant
  ```go
  DefaultPriority = 0
  ```

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T01:16:25Z

- **pkg/queue/queue.go:119** by [@denkensk](https://github.com/denkensk) — 2022-03-16T01:16:24Z
```diff
@@ -109,19 +118,36 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	log := cq.log.WithValues("workload", workload.Key(info.Obj))
+	ctx := ctrl.LoggerInto(context.Background(), log)
+	// Populate priority from priority class before enqueue.
+	p := workload.GetPriorityFromPriorityClass(ctx, cq.client,
+		info.Obj.Spec.PriorityClassName)
+	info.Priority = &p
+
 	heap.Push(&cq.heap, *info)
 	return true
 }
 
-func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) {
+func (cq *ClusterQueue) PushOrUpdate(w *kueue.QueuedWorkload) bool {
 	item := cq.heap.items[workload.Key(w)]
 	info := *workload.NewInfo(w)
```

  I was initially hoping to calculate in NewInfo. But I find WorkloadInfo is a basic data struct which is invoked in other places like cache. It's hard and unclean if I pass the log/ctx/client to everywhere I want to invoke New.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T01:16:56Z

- **pkg/queue/queue.go:89** by [@denkensk](https://github.com/denkensk) — 2022-03-16T01:16:56Z
```diff
@@ -66,20 +71,24 @@ func (q *Queue) AddOrUpdate(w *kueue.QueuedWorkload) {
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds pending workloads.
 type ClusterQueue struct {
+	client client.Client
 	// QueueingStrategy indicates the queueing strategy of the workloads
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
-
-	heap heapImpl
+	log              logr.Logger
+	heap             heapImpl
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue) *ClusterQueue {
+func newClusterQueue(client client.Client, cq *kueue.ClusterQueue) *ClusterQueue {
 	cqImpl := &ClusterQueue{
+		client: client,
 		heap: heapImpl{
 			less:  creationFIFO,
 			items: make(map[string]*heapItem),
 		},
+		log: ctrl.Log.WithName("cluster-queue"),
```

  Ok, I will update later.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T01:17:02Z

- **pkg/queue/queue.go:116** by [@denkensk](https://github.com/denkensk) — 2022-03-16T01:17:02Z
```diff
@@ -109,19 +118,36 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	log := cq.log.WithValues("workload", workload.Key(info.Obj))
+	ctx := ctrl.LoggerInto(context.Background(), log)
```

  Ok, I will update later.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T04:21:22Z

- **pkg/queue/queue.go:116** by [@denkensk](https://github.com/denkensk) — 2022-03-16T04:21:22Z
```diff
@@ -109,19 +118,36 @@ func (cq *ClusterQueue) PushIfNotPresent(info *workload.Info) bool {
 	if item != nil {
 		return false
 	}
+
+	log := cq.log.WithValues("workload", workload.Key(info.Obj))
+	ctx := ctrl.LoggerInto(context.Background(), log)
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T04:21:25Z

- **pkg/queue/queue.go:89** by [@denkensk](https://github.com/denkensk) — 2022-03-16T04:21:25Z
```diff
@@ -66,20 +71,24 @@ func (q *Queue) AddOrUpdate(w *kueue.QueuedWorkload) {
 // ClusterQueue is the internal implementation of kueue.ClusterQueue that
 // holds pending workloads.
 type ClusterQueue struct {
+	client client.Client
 	// QueueingStrategy indicates the queueing strategy of the workloads
 	// across the queues in this ClusterQueue.
 	QueueingStrategy kueue.QueueingStrategy
-
-	heap heapImpl
+	log              logr.Logger
+	heap             heapImpl
 }
 
-func newClusterQueue(cq *kueue.ClusterQueue) *ClusterQueue {
+func newClusterQueue(client client.Client, cq *kueue.ClusterQueue) *ClusterQueue {
 	cqImpl := &ClusterQueue{
+		client: client,
 		heap: heapImpl{
 			less:  creationFIFO,
 			items: make(map[string]*heapItem),
 		},
+		log: ctrl.Log.WithName("cluster-queue"),
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T04:21:29Z

- **api/v1alpha1/queuedworkload_types.go:50** by [@denkensk](https://github.com/denkensk) — 2022-03-16T04:21:29Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
+	// Priority determines the order of access to the resources managed by the
+	// ClusterQueue the workload is actually queued.
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-16T11:10:24Z

- **pkg/workload/workload.go:105** by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T10:46:10Z
```diff
@@ -82,6 +91,64 @@ func totalRequests(spec *kueue.QueuedWorkloadSpec) []PodSetResources {
 	return res
 }
 
+// Priority returns priority of the given workload.
+func Priority(w Info) int32 {
+	if w.Priority != nil {
+		return *w.Priority
+	}
+	// When priority of a running workload is nil, it means it was created at a time
+	// that there was no global default priority class and the priority class
+	// name of the pod was empty. So, we resolve to the static default priority.
+	return constants.DefaultPriority
+}
+
+func GetPriorityFromPriorityClass(ctx context.Context, client client.Client,
```

  pls move this to `pkg/util/priority`

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-16T13:32:58Z

thanks, good for squash

- **pkg/queue/queue_test.go:101** by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T13:27:59Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
```

  ```suggestion
  			name: "w1.priority is higher than w2.priority",
  ```

- **pkg/queue/queue_test.go:141** by [@ahg-g](https://github.com/ahg-g) — 2022-03-16T13:30:20Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
+				},
+			},
+			w2: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w2",
+					CreationTimestamp: metav1.NewTime(t2),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
+				},
+			},
+			expected: "w1",
+		},
+		{
+			name: "w1.priority equals w2.priority and w1.create time is earlier than w2.create time",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+			},
+			w2: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w2",
+					CreationTimestamp: metav1.NewTime(t2),
+				},
+			},
+			expected: "w1",
+		},
+		{
+			name: "p1.priority is less than p2.priority and w1.create time is earlier than w2.create time",
```

  ```suggestion
  			name: "p1.priority is lower than p2.priority and w1.create time is earlier than w2.create time",
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-16T13:43:12Z

- **api/v1alpha1/queuedworkload_types.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:43:11Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
```

  Since it wouldn't change by external factors, you should just say that the default priority is zero.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-16T14:05:51Z

- **api/v1alpha1/queuedworkload_types.go:48** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:51:13Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
```

  btw, I'm referring to `If not specified, the queuedWorkload priority will be default`
  
  Is this referring to the default priority class defined for the cluster?

- **pkg/controller/workload/job/job_controller.go:38** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:51:58Z
```diff
@@ -35,6 +35,7 @@ import (
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/constants"
+	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
```

  nit: remove the alias. Use just `priority`

- **pkg/queue/queue.go:24** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:54:15Z
```diff
@@ -21,6 +21,7 @@ import (
 	"fmt"
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
+	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
```

  same here

- **pkg/queue/queue.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:55:43Z
```diff
@@ -140,7 +141,9 @@ func (cq *ClusterQueue) Pop() *workload.Info {
 }
 
 func creationFIFO(a, b workload.Info) bool {
-	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
+	p1 := utilpriority.Priority(a)
+	p2 := utilpriority.Priority(b)
+	return (p1 > p2) || (p1 == p2 && a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp))
```

  to improve readability and extensibility, I prefer this:
  
  ```suggestion
  	if p1 != p2 {
  		return p1 > p2
  	}
  	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
  ```

- **pkg/queue/queue_test.go:109** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:56:59Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
```

  ```suggestion
  					Priority:          pointer.Int32(highPriority),
  ```
  
  then `highPriority` can be a constant.

- **pkg/queue/queue_test.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:57:06Z
```diff
@@ -24,6 +24,10 @@ import (
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
 )
 
+var (
+	lowPriority, highPriority = int32(0), int32(1000)
```

  make it 2 lines

- **pkg/queue/queue_test.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T13:59:19Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
```

  For a follow up, we should find a way to merge the 2 tests #72

- **pkg/util/priority/priority.go:31** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T14:00:28Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package priority
+
+import (
+	"context"
+
+	schedulingv1 "k8s.io/api/scheduling/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Priority returns priority of the given workload.
+func Priority(w workload.Info) int32 {
```

  why not make this a method of `workload.Info`?

- **pkg/controller/workload/job/job_controller.go:392** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T14:04:02Z
```diff
@@ -386,6 +388,16 @@ func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.Queu
 			QueueName: queueName(job),
 		},
 	}
+
+	priorityClassName := job.Spec.Template.Spec.PriorityClassName
```

  Do we populate the priorityClassName from the default for pods too? If so, we should do the same here.

- **pkg/util/routine/wrapper.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-16T14:04:42Z
```diff
@@ -1,3 +1,19 @@
+/*
```

  Oops, I wonder if we can make a verify step for this.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T14:40:19Z

- **pkg/controller/workload/job/job_controller.go:392** by [@denkensk](https://github.com/denkensk) — 2022-03-16T14:40:19Z
```diff
@@ -386,6 +388,16 @@ func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.Queu
 			QueueName: queueName(job),
 		},
 	}
+
+	priorityClassName := job.Spec.Template.Spec.PriorityClassName
```

  yes. Will add it later.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T14:43:16Z

- **pkg/controller/workload/job/job_controller.go:38** by [@denkensk](https://github.com/denkensk) — 2022-03-16T14:43:15Z
```diff
@@ -35,6 +35,7 @@ import (
 
 	kueue "sigs.k8s.io/kueue/api/v1alpha1"
 	"sigs.k8s.io/kueue/pkg/constants"
+	utilpriority "sigs.k8s.io/kueue/pkg/util/priority"
```

  `utilpriority` is auto add by `auto import`. I suggest keeping as `utilpriority`.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T14:55:39Z

- **pkg/util/priority/priority.go:31** by [@denkensk](https://github.com/denkensk) — 2022-03-16T14:55:39Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package priority
+
+import (
+	"context"
+
+	schedulingv1 "k8s.io/api/scheduling/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Priority returns priority of the given workload.
+func Priority(w workload.Info) int32 {
```

  No special reason. Just keep it as k/k. 
  And I also think it's not suitable as a member function. But I can change the input as `kueue.workload` to make it can be used other place in the future.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-16T14:56:14Z

- **pkg/util/priority/priority.go:31** by [@denkensk](https://github.com/denkensk) — 2022-03-16T14:56:13Z
```diff
@@ -0,0 +1,87 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package priority
+
+import (
+	"context"
+
+	schedulingv1 "k8s.io/api/scheduling/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+// Priority returns priority of the given workload.
+func Priority(w workload.Info) int32 {
```

  No special reason. Just keep it as k/k. 
  And I also think it's not suitable as a member function. But I can change the input as `kueue.workload` to make it can be used other place in the future.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T03:57:45Z

- **api/v1alpha1/queuedworkload_types.go:48** by [@denkensk](https://github.com/denkensk) — 2022-03-17T03:57:45Z
```diff
@@ -39,6 +39,18 @@ type QueuedWorkloadSpec struct {
 
 	// admission holds the parameters of the admission of the workload by a ClusterQueue.
 	Admission *Admission `json:"admission,omitempty"`
+
+	// If specified, indicates the queuedWorkload's priority.
+	// "system-node-critical" and "system-cluster-critical" are two special
+	// keywords which indicate the highest priorities with the former being
+	// the highest priority. Any other name must be defined by creating a
+	// PriorityClass object with that name. If not specified, the queuedWorkload
+	// priority will be default or zero if there is no default.
```

  Yes. It's the default priority class defined for the cluster. But if there is no default. It will be 0.
  Keep the comments as
  https://github.com/kubernetes/kubernetes/blob/ca2cd3b18ef145c34311ba7fd9d389fe8233fae8/pkg/apis/core/types.go#L2902

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T05:01:46Z

- **pkg/controller/workload/job/job_controller.go:392** by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:01:45Z
```diff
@@ -386,6 +388,16 @@ func ConstructWorkloadFor(job *batchv1.Job, scheme *runtime.Scheme) (*kueue.Queu
 			QueueName: queueName(job),
 		},
 	}
+
+	priorityClassName := job.Spec.Template.Spec.PriorityClassName
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T05:01:53Z

- **pkg/queue/queue_test.go:109** by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:01:53Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T05:01:56Z

- **pkg/queue/queue.go:146** by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:01:56Z
```diff
@@ -140,7 +141,9 @@ func (cq *ClusterQueue) Pop() *workload.Info {
 }
 
 func creationFIFO(a, b workload.Info) bool {
-	return a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp)
+	p1 := utilpriority.Priority(a)
+	p2 := utilpriority.Priority(b)
+	return (p1 > p2) || (p1 == p2 && a.Obj.CreationTimestamp.Before(&b.Obj.CreationTimestamp))
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T05:02:01Z

- **pkg/queue/queue_test.go:141** by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:02:01Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
+				},
+			},
+			w2: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w2",
+					CreationTimestamp: metav1.NewTime(t2),
+				},
+				Spec: kueue.QueuedWorkloadSpec{
+					PriorityClassName: "highPriority",
+					Priority:          &highPriority,
+				},
+			},
+			expected: "w1",
+		},
+		{
+			name: "w1.priority equals w2.priority and w1.create time is earlier than w2.create time",
+			w1: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w1",
+					CreationTimestamp: metav1.NewTime(t1),
+				},
+			},
+			w2: &kueue.QueuedWorkload{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:              "w2",
+					CreationTimestamp: metav1.NewTime(t2),
+				},
+			},
+			expected: "w1",
+		},
+		{
+			name: "p1.priority is less than p2.priority and w1.create time is earlier than w2.create time",
```

  Done

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T05:02:05Z

- **pkg/queue/queue_test.go:101** by [@denkensk](https://github.com/denkensk) — 2022-03-17T05:02:05Z
```diff
@@ -83,3 +87,98 @@ func TestFIFOClusterQueue(t *testing.T) {
 		t.Errorf("Queue is not empty, poped workload %q", got.Obj.Name)
 	}
 }
+
+func TestStrictFIFO(t *testing.T) {
+	t1 := time.Now()
+	t2 := t1.Add(time.Second)
+	for _, tt := range []struct {
+		name     string
+		w1       *kueue.QueuedWorkload
+		w2       *kueue.QueuedWorkload
+		expected string
+	}{
+		{
+			name: "w1.priority is larger than w2.priority",
```

  Done

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-17T12:39:25Z

Thanks! this is looking great!

- **test/integration/scheduler/scheduler_test.go:279** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:26:47Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
```

  I think we can use the existing ones (e.g., `prodQueue` defined above), otherwise we will need to add defer func to delete it.

- **test/integration/scheduler/scheduler_test.go:282** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:27:14Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
```

  if you remove the CQ, then you could delay creating the Q until after the workloads are created.

- **test/integration/scheduler/scheduler_test.go:277** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:28:11Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
```

  ```suggestion
  	ginkgo.It("Should schedule jobs according to their priorities", func() {
  ```

- **test/integration/scheduler/scheduler_test.go:300** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:28:50Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
```

  ```suggestion
  		ginkgo.By("checking that workload1 is created with priority and priorityName")
  ```

- **test/integration/scheduler/scheduler_test.go:293** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:29:05Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
```

  ```suggestion
  		jobLowPriority := testing.MakeJob("job-low-priority", ns.Name).Queue(queue.Name).Request(
  ```

- **test/integration/scheduler/scheduler_test.go:296** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:29:42Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
```

  ```suggestion
  		jobHighPriority := testing.MakeJob("job-high-priority", ns.Name).Queue(queue.Name).Request(
  ```

- **test/integration/scheduler/scheduler_test.go:293** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:32:46Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		createdLowPriorityWorkload := &kueue.QueuedWorkload{}
  		gomega.Eventually(func() bool {
  		        lookupKey := types.NamespacedName{Name: jobLowPriority.Name, Namespace: jobLowPriority.NameSpace}
  			err := k8sClient.Get(ctx, lookupKey, createdLowPriorityWorkload)
  			return err == nil
  		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
  ```

- **test/integration/scheduler/scheduler_test.go:303** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:34:22Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload1.Spec.PriorityClassName).Should(gomega.Equal(lowPriorityClassName))
+		gomega.Expect(*createdWorkload1.Spec.Priority).Should(gomega.Equal(int32(lowPriority)))
+
+		ginkgo.By("checking the workload2 is created with priority and priorityName")
+		createdWorkload2 := &kueue.QueuedWorkload{}
+		lookupKey2 := types.NamespacedName{Name: "job2", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey2, createdWorkload2)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		ginkgo.By("checking the workload2 is created with priority and priorityName")
  		createdHighPriorityWorkload := &kueue.QueuedWorkload{}
  		gomega.Eventually(func() bool {
  		        lookupKey := types.NamespacedName{Name: jobHighPriority.Name, Namespace: jobHighPriority.Namespace}
  			err := k8sClient.Get(ctx, lookupKey, createdHighPriorityWorkload)
  			return err == nil
  		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
  ```

- **test/integration/scheduler/scheduler_test.go:326** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:35:14Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload1.Spec.PriorityClassName).Should(gomega.Equal(lowPriorityClassName))
+		gomega.Expect(*createdWorkload1.Spec.Priority).Should(gomega.Equal(int32(lowPriority)))
+
+		ginkgo.By("checking the workload2 is created with priority and priorityName")
+		createdWorkload2 := &kueue.QueuedWorkload{}
+		lookupKey2 := types.NamespacedName{Name: "job2", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey2, createdWorkload2)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload2.Spec.PriorityClassName).Should(gomega.Equal(highPriorityClassName))
+		gomega.Expect(*createdWorkload2.Spec.Priority).Should(gomega.Equal(int32(highPriority)))
+
+		// Create clusterQueue
+		gomega.Expect(k8sClient.Create(ctx, clusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQ)).ToNot(gomega.HaveOccurred())
+		}()
+
+		ginkgo.By("checking the job with low priority suspends")
```

  ```suggestion
  		ginkgo.By("checking the job with low priority continues to be suspended")
  ```

- **test/integration/scheduler/scheduler_test.go:329** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:35:49Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload1.Spec.PriorityClassName).Should(gomega.Equal(lowPriorityClassName))
+		gomega.Expect(*createdWorkload1.Spec.Priority).Should(gomega.Equal(int32(lowPriority)))
+
+		ginkgo.By("checking the workload2 is created with priority and priorityName")
+		createdWorkload2 := &kueue.QueuedWorkload{}
+		lookupKey2 := types.NamespacedName{Name: "job2", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey2, createdWorkload2)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload2.Spec.PriorityClassName).Should(gomega.Equal(highPriorityClassName))
+		gomega.Expect(*createdWorkload2.Spec.Priority).Should(gomega.Equal(int32(highPriority)))
+
+		// Create clusterQueue
+		gomega.Expect(k8sClient.Create(ctx, clusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQ)).ToNot(gomega.HaveOccurred())
+		}()
+
+		ginkgo.By("checking the job with low priority suspends")
+		createdJob1 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: "job1",
```

  ```suggestion
  			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: jobLowPriority.Name,
  ```

- **test/integration/scheduler/scheduler_test.go:337** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:36:03Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload1.Spec.PriorityClassName).Should(gomega.Equal(lowPriorityClassName))
+		gomega.Expect(*createdWorkload1.Spec.Priority).Should(gomega.Equal(int32(lowPriority)))
+
+		ginkgo.By("checking the workload2 is created with priority and priorityName")
+		createdWorkload2 := &kueue.QueuedWorkload{}
+		lookupKey2 := types.NamespacedName{Name: "job2", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey2, createdWorkload2)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload2.Spec.PriorityClassName).Should(gomega.Equal(highPriorityClassName))
+		gomega.Expect(*createdWorkload2.Spec.Priority).Should(gomega.Equal(int32(highPriority)))
+
+		// Create clusterQueue
+		gomega.Expect(k8sClient.Create(ctx, clusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQ)).ToNot(gomega.HaveOccurred())
+		}()
+
+		ginkgo.By("checking the job with low priority suspends")
+		createdJob1 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: "job1",
+				Namespace: ns.Name}, createdJob1)).Should(gomega.Succeed())
+			return createdJob1.Spec.Suspend
+		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(true)))
+
+		ginkgo.By("checking the job with high priority starts")
+		createdJob2 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: "job2",
```

  ```suggestion
  			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: jobHighPriority.Name,
  ```

- **test/integration/scheduler/scheduler_test.go:328** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:36:54Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
+		gomega.Expect(k8sClient.Create(ctx, queue)).Should(gomega.Succeed())
+
+		highPriorityClass := testing.MakePriorityClass(highPriorityClassName).
+			PriorityValue(int32(highPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).Should(gomega.Succeed())
+
+		lowPriorityClass := testing.MakePriorityClass(lowPriorityClassName).
+			PriorityValue(int32(lowPriority)).Obj()
+		gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).Should(gomega.Succeed())
+
+		job1 := testing.MakeJob("job1", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(lowPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job1)).Should(gomega.Succeed())
+		job2 := testing.MakeJob("job2", ns.Name).Queue(queue.Name).Request(
+			corev1.ResourceCPU, "1").PriorityClass(highPriorityClassName).Obj()
+		gomega.Expect(k8sClient.Create(ctx, job2)).Should(gomega.Succeed())
+
+		ginkgo.By("checking the workload1 is created with priority and priorityName")
+		createdWorkload1 := &kueue.QueuedWorkload{}
+		lookupKey1 := types.NamespacedName{Name: "job1", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey1, createdWorkload1)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload1.Spec.PriorityClassName).Should(gomega.Equal(lowPriorityClassName))
+		gomega.Expect(*createdWorkload1.Spec.Priority).Should(gomega.Equal(int32(lowPriority)))
+
+		ginkgo.By("checking the workload2 is created with priority and priorityName")
+		createdWorkload2 := &kueue.QueuedWorkload{}
+		lookupKey2 := types.NamespacedName{Name: "job2", Namespace: ns.Name}
+		gomega.Eventually(func() bool {
+			err := k8sClient.Get(ctx, lookupKey2, createdWorkload2)
+			return err == nil
+		}, framework.Timeout, framework.Interval).Should(gomega.BeTrue())
+		gomega.Expect(createdWorkload2.Spec.PriorityClassName).Should(gomega.Equal(highPriorityClassName))
+		gomega.Expect(*createdWorkload2.Spec.Priority).Should(gomega.Equal(int32(highPriority)))
+
+		// Create clusterQueue
+		gomega.Expect(k8sClient.Create(ctx, clusterQ)).Should(gomega.Succeed())
+		defer func() {
+			gomega.Expect(framework.DeleteClusterQueue(ctx, k8sClient, clusterQ)).ToNot(gomega.HaveOccurred())
+		}()
+
+		ginkgo.By("checking the job with low priority suspends")
+		createdJob1 := &batchv1.Job{}
+		gomega.Eventually(func() *bool {
```

  we need to use consistently not eventually since we expect no change in the suspend status.

- **test/integration/scheduler/scheduler_test.go:43** by [@ahg-g](https://github.com/ahg-g) — 2022-03-17T12:38:09Z
```diff
@@ -34,9 +34,13 @@ import (
 
 var _ = ginkgo.Describe("Scheduler", func() {
 	const (
-		instanceKey    = "cloud.provider.com/instance"
-		onDemandFlavor = "on-demand"
-		spotFlavor     = "spot"
+		instanceKey           = "cloud.provider.com/instance"
+		onDemandFlavor        = "on-demand"
+		spotFlavor            = "spot"
+		highPriorityClassName = "high-priority"
+		highPriority          = 100
+		lowPriorityClassName  = "low-priority"
+		lowPriority           = 10
```

  since they are only used in one test, lets define those priority constants inside the test.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T16:04:38Z

- **test/integration/scheduler/scheduler_test.go:282** by [@denkensk](https://github.com/denkensk) — 2022-03-17T16:04:38Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
+			Flavor(testing.MakeFlavor(onDemandFlavor, "1").Obj()).Obj()).Obj()
+
+		queue := testing.MakeQueue("queue", ns.Name).ClusterQueue(clusterQ.Name).Obj()
```

  Thanks. It will be simpler.

### Review by [@denkensk](https://github.com/denkensk) (commented) — 2022-03-17T16:04:47Z

- **test/integration/scheduler/scheduler_test.go:279** by [@denkensk](https://github.com/denkensk) — 2022-03-17T16:04:47Z
```diff
@@ -269,4 +273,70 @@ var _ = ginkgo.Describe("Scheduler", func() {
 			return createdJob.Spec.Suspend
 		}, framework.Timeout, framework.Interval).Should(gomega.Equal(pointer.Bool(false)))
 	})
+
+	ginkgo.It("Should schedule jobs according to the priority", func() {
+		// Initialize clusterQueue first but do not create
+		clusterQ := testing.MakeClusterQueue("cluster-queue").Resource(testing.MakeResource(corev1.ResourceCPU).
```

  Done
