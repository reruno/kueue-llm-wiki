# PR #10406: TAS: Use hierarchical LeastFreeCapacity for unconstrained workloads

**Summary**: TAS: Use hierarchical LeastFreeCapacity for unconstrained workloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10406

**Last updated**: 2026-05-20T02:33:21Z

---

## Metadata

- **State**: open
- **Author**: [@abhi179](https://github.com/abhi179)
- **Branch**: `users/abhisi/fixLeastFreeForUnconstrained` → `main`
- **Created**: 2026-04-11T08:00:12Z
- **Updated**: 2026-05-20T02:33:21Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `do-not-merge/hold`, `ok-to-test`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 5   **+153 / -1**

## Description


### **TL;DR**
Fixes rack fragmentation for unconstrained workloads by making LeastFreeCapacity topology-aware. Instead of scheduling at node level only, the algorithm now packs workloads top-down (rack → node), preserving empty racks for large jobs (e.g., NVL72).


**What changed**

When TASProfileMixed is enabled and a workload uses unconstrained topology, LeastFreeCapacity now evaluates capacity hierarchically:

- Previously: scheduling was done only at node level (lowest level)
- Now: scheduling is done top-down across topology levels
    - Select the domain with least free capacity at the highest level (e.g., rack)
    - Recursively select the tightest-fitting child domain (e.g., node)


### **Detailed Description:**
**Title: LeastFreeCapacity for unconstrained workloads should consider higher topology levels to prevent rack fragmentation**

**Issue Description :**
On a large GPU cluster with GB200 NVL72 racks (~33 racks, ~18 nodes per rack, 4 GPUs per node), single-GPU inference workloads using unconstrained-topology: "true" are being scattered across many partially-filled racks instead of being consolidated into the fewest racks possible.

The cluster topology has two levels:

 apiVersion: kueue.x-k8s.io/v1beta2
 kind: Topology
 metadata:
   name: rack-topology
 spec:
   levels:
   - nodeLabel: nvidia.com/gpu.clique   # rack
   - nodeLabel: kubernetes.io/hostname   # node

With TASProfileMixed enabled (default since v0.15), unconstrained workloads use LeastFreeCapacity. However, the algorithm only operates at the hostname (lowest) level — it picks the globally busiest node without considering rack-level grouping. This causes
single-GPU pods to land on partially-used nodes across many different racks, creating widespread rack fragmentation.

**What should have been the expected behavior:**

LeastFreeCapacity should traverse the topology tree top-down for unconstrained workloads: first select the busiest rack (least free capacity at the rack level), then select the busiest node within that rack. This would consolidate single-GPU workloads into the fewest racks possible.

Why this matters (GB200/NVL72 use case):

GB200 NVL72 workloads require an entire rack (72 GPUs across 18 nodes connected via NVLink). When single-GPU inference pods (image-encoding, proxy, etc.) are scattered across many racks — each consuming just 1 GPU per rack — it becomes impossible to find a fully empty rack for NVL72 jobs. The goal is to pack unconstrained single-GPU workloads into the fewest racks possible, keeping maximum racks entirely free for large topology-constrained jobs.

**Reproduction:**

 1. Create a topology with two levels (rack → hostname)
 2. Deploy multiple single-GPU StatefulSets with kueue.x-k8s.io/podset-unconstrained-topology: "true"
 3. Observe pods scattered across many racks instead of consolidating into the fewest racks

**Evidence from production (Kueue v0.16.1):**

 - Pod deleted from a node landed back on the same rack instead of going to the busiest rack (LeastFree)
 - TopologyAssignment shows only levels: ["kubernetes.io/hostname"] — rack level absent
 - Rack utilization ranged from 1–72 GPUs across 33 racks, confirming no rack-level consolidation

**Root cause in code:**

pkg/cache/scheduler/tas_flavor_snapshot.go:

 1. levelKey() (line 1105): For unconstrained, returns s.lowestLevel() = kubernetes.io/hostname, skipping the rack level entirely
 2. findLevelWithFitDomains() (line 1250): Upward traversal to rack level is blocked by !unconstrained guard: if levelIdx > 0 && !unconstrained {
      return s.findLevelWithFitDomains(levelIdx-1, ...)
  }
 3. LeastFreeCapacity sorts hostname-level domains only — it never groups or scores by rack

Proposed behavior:
For unconstrained + LeastFreeCapacity with multi-level topologies, the algorithm should:

 1. Score capacity at each topology level (rack, then hostname)
 2. Select the rack with the least free capacity that can fit the workload
 3. Within that rack, select the node with the least free capacity

This preserves the "no hard placement constraint" semantics of unconstrained while enabling topology-aware bin-packing to reduce fragmentation.

Environment:

 - Kueue version: v0.16.1
 - Feature gates: TASProfileMixed=true (default)
 - Kubernetes: AKS with unmanaged GPU nodes
 - Topology: 2 levels (nvidia.com/gpu.clique → kubernetes.io/hostname)

```release-note
TAS: Introduce hierarchical LeastFreeCapacity for unconstrained workloads, gated by the TASHierarchicalUnconstrained feature gate (alpha, disabled by default). When enabled alongside TASProfileMixed, the algorithm resolves topology starting from the highest level (e.g., rack) instead of the node level, packing pods into the busiest racks first to reduce fragmentation.
```

## Discussion

### Comment by [@linux-foundation-easycla[bot]](https://github.com/apps/linux-foundation-easycla) — 2026-04-11T08:00:21Z

<a href="https://easycla.lfx.linuxfoundation.org/#/?version=2"><img src="https://s3.amazonaws.com/cla-project-logo-prod/cla-signed.svg?v=2" alt="CLA Signed" align="left" height="28" width="328" ></a><br/><br />The committers listed above are authorized under a signed CLA.<ul><li>:white_check_mark: login: abhishek179 / name: abhishek  (17eb6a9fc9ca155132bd6db8d767330fc80847d9, 45fef33480c01f2a1666740598929dafa710aab0, 6de848d347ce96bb4ec18797c64b874909df1d00, 8d1051d617f6b6aaa1928726d08ebecbe7bc0cd5, d366a056c481734b3d2df7b706a49e41c0f4d1c0)</li></ul>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-11T08:00:21Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 6de848d347ce96bb4ec18797c64b874909df1d00 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f2476b44a9260008caa3db |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10406--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10406--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwNDA2LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.YaayZqI9vyUY4oW8YcGsyxmEAsx2XS-xeOdl5TA2b-0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10406--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-11T08:00:23Z

Welcome @abhi179! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://www.kubernetes.dev/docs/guide/pull-requests/) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://www.kubernetes.dev/docs/guide/pull-requests/#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://www.kubernetes.dev/docs/contributor-cheatsheet/). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-11T08:00:23Z

Hi @abhi179. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-13T07:45:06Z

/ok-to-test

### Comment by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T10:50:43Z

And please sign the CLA.

### Comment by [@abhi179](https://github.com/abhi179) — 2026-04-22T07:53:49Z

> And please sign the CLA.

done

### Comment by [@abhi179](https://github.com/abhi179) — 2026-04-28T07:03:00Z

@mimowo @kannon92 quick ping on this PR, I have addressed all the feedback. it doesn't change the current behavior, and the new flag is gated.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-28T07:25:47Z

> @mimowo @kannon92 quick ping on this PR, I have addressed all the feedback. it doesn't change the current behavior, and the new flag is gated.

Great, Adding to my ToDo and will try to review EoW. In the meanwhile it would be great if @PBundyra could give it a pass who has most context here as worked on the initial version. 

If we agree about changing the behavior then I'm thinking this FG could be Beta in 0.18, and cherrypicked as Alpha down to 0.16 and 0.17. wdyt?

Please also:
1. add a release in the release-note box in the PR description, I just injected the box with some template, feel free to adjust
2. adjust the TAS KEP where it mentions the available algorithms

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:37:00Z

@abhi179 please also make sure all comments are addressed clearly one way or another - either confirm that you applied the suggestion (then feel free to mark the comment as resolved), or explain why you didn't, so that we don't need to reverse engineer your actions and motivations. For example this comment does not have any answer: https://github.com/kubernetes-sigs/kueue/pull/10406#discussion_r3078880555

### Comment by [@abhi179](https://github.com/abhi179) — 2026-04-28T20:15:46Z

> > @mimowo @kannon92 quick ping on this PR, I have addressed all the feedback. it doesn't change the current behavior, and the new flag is gated.
> 
> Great, Adding to my ToDo and will try to review EoW. In the meanwhile it would be great if @PBundyra could give it a pass who has most context here as worked on the initial version.
> 
> If we agree about changing the behavior then I'm thinking this FG could be Beta in 0.18, and cherrypicked as Alpha down to 0.16 and 0.17. wdyt?
> 
> Please also:
> 
> 1. add a release in the release-note box in the PR description, I just injected the box with some template, feel free to adjust
> 2. adjust the TAS KEP where it mentions the available algorithms

@mimowo updated release note and TAS KEP documentation.

@mimowo That sounds like a good plan. Starting as Alpha cherry-picked to v0.16/v0.17 gives existing users access to the fix sooner, and promoting to Beta in v0.18 makes sense once it's had some validation. Happy to help with the cherry-picks once this lands on main.

### Comment by [@abhi179](https://github.com/abhi179) — 2026-04-28T20:31:32Z

/test pull-kueue-verify-main

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T11:31:47Z

Overall lgtm just a small comment regarding tests

### Comment by [@abhi179](https://github.com/abhi179) — 2026-04-29T18:19:04Z

/test pull-kueue-verify-main

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-30T14:58:11Z

I land here right now

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-30T14:59:26Z

/kind feature
/kind kep

### Comment by [@abhi179](https://github.com/abhi179) — 2026-05-01T20:34:13Z

Thanks @mimowo and @PBundyra for the thorough review! All feedback has been addressed. Let me know if I missed anything.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T13:01:22Z

/lgtm
/approve
Thank you 👍 
/hold
in case @tenzen-y has extra comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T13:01:29Z

LGTM label has been added.  <details>Git tree hash: 98f9dde81ed035a1fbc5b3315ae6aa561fc32c70</details>

### Comment by [@mwysokin](https://github.com/mwysokin) — 2026-05-05T13:22:10Z

@abhi179 Thank you for submitting the improvement 🙏 Since you have some real-life work experience with prod workloads on GB200 let me pick your brain about something.

I think in general our strategy going forward should be to have configurable placement policies e.g. in heterogenous clusters there should be an option to set default placement policy at the topology level (e.g. GB200 vs H100) but also a user should be able to override it if they feel the need to. Would this be a useful in your system? 

I'm anticipating many more placement policies in the future and treating them individually, hiding them under feature gates and exposing them as pre-configured packages of placement policies doesn't seem efficient to me. That's my thinking.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-05T13:44:07Z

> hiding them under feature gates and exposing them as pre-configured packages of placement policies doesn't seem efficient to me. That's my thinking.

Yes, for sure, [feature gates](https://github.com/kubernetes/community/blob/main/contributors/devel/sig-architecture/feature-gates.md#feature-gates-in-kubernetes) are transitive in nature, quoting from the guide:

```
Feature "gates" are how Kubernetes manages behavioral changes as they progress through the development lifecycle. This gives cluster operators a way to try new features or to disable them when something goes wrong.
```

Feature gates are useful to start with a speculative feature, or risky feature, but once we collect user feedback we should either deprecate feature gates, or graduate to GA. The most direct translation of a feature gate is global configuration knob, but in some cases we may replace them with CRD-level knobs.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-05T14:50:53Z

I read the PR again, and I think likely it is going in the right direction, but I think I found a flaw (or at least I'm not sure yet about it), so let me cancel approval for now to avoid premature merging.
/approve cancel

Also, I would like to explore more how we could confirm how it performs in practice, so that we can have some better graduation criteria. Intuitively I think the approach is good (with the caveat mentioned), but I'm wondering if we can some better experimental support for it, for example;
1. are you going to use it on prod, how would you measure the improvement?
2. do you have maybe some ideas for "simulated" measuring of the improvement?

I would also be happy to learn more about your use case, so that we can better learn how people use "unconstrained" in practice, for example:
a) what is the percentage of "unconstrained" / "preferred" / "required" workloads
b) what do you use "unconstrained" for - training / inference / auxiliary jobs
c) what is the relative priority of the "unconstained" jobs to other workloads.

The practice is super relevant here, because the algos BestFit / LeastFreeCapactiy, etc are heuristics, which we thought make sense, but proving anything is very hard.

The forums that come to my mind:
1. feel free to reach out on slack (kubernetes workspace), either DM, and wg-batch
2. maybe you could present the intended improvement on  [wg-batch meetings](https://github.com/kubernetes/community/tree/main/wg-batch)

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T14:51:06Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10406#" title="Author self-approved">abhi179</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [mimowo](https://github.com/mimowo). For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-20T02:33:21Z

@abhi179: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-priority-booster-test-integration-main | 6de848d347ce96bb4ec18797c64b874909df1d00 | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10406/pull-kueue-priority-booster-test-integration-main/2056925234077896704) | true | `/test pull-kueue-priority-booster-test-integration-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=10406). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Aabhi179). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

## Reviews

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-13T09:20:13Z

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@mszadkow](https://github.com/mszadkow) — 2026-04-13T09:20:14Z
```diff
@@ -595,10 +595,8 @@ func TestFindTopologyAssignments(t *testing.T) {
 					Levels: defaultOneLevel,
 					Domains: []tas.TopologyDomainAssignment{
 						{Count: 1, Values: []string{"x1"}},
-						{Count: 1, Values: []string{"x3"}},
+						{Count: 4, Values: []string{"x3"}},
 						{Count: 1, Values: []string{"x5"}},
-						{Count: 1, Values: []string{"x2"}},
-						{Count: 2, Values: []string{"x6"}},
 					},
```

  Those test changes are valid, but we might want to consider to add a separate test for the un-gated scenario if it should remain supported.
  wdyt @PBundyra ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-13T10:40:08Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1108** by [@mimowo](https://github.com/mimowo) — 2026-04-13T10:40:08Z
```diff
@@ -1103,6 +1103,9 @@ func (s *TASFlavorSnapshot) levelKey(topologyRequest *kueue.PodSetTopologyReques
 	case isSliceTopologyOnlyRequest(topologyRequest):
 		return ptr.To(s.highestLevel())
 	case ptr.Deref(topologyRequest.Unconstrained, false):
+		if features.Enabled(features.TASProfileMixed) {
+			return ptr.To(s.highestLevel())
+		}
```

  A couple of thoughts here:
  1. I know the idea of using the lowest level for "unconstrained" was to minimize strictly the fragmentation at the node level, which is not guaranteed in the new hierarchical mode, OTOH, as pointed out in this issue this leads to increased fragmentation at the rack level. I think it makes sense that for topologies we optimize fragmentation at different levels. Additionally, since this is "unconstrained" mode, Kueue has freedom of choice for the exact algo, from this perspective tweaking the algo as proposed sgtm
  2. since TASProfileMixed is default behavior, and this PR is changing it, so we may consider a feature gate to ensure users can bailout, something like TASHierarchicalUnconstrained
  3. we may also consider the "unconstrained" annotation to indicate the level for the top-level LeastFreeCapacity behavior, similarly as "preferred" and "required" point to a level, but I'm not clear we have enough motivation for that currently. 
  
  So I'm considering the approaches:
  A. Do as proposed in the PR, but use a feature gate TASHierarchicalUnconstrained for bailout option
  B. Allow the "unconstrained" annotation to indicate the level at each it starts, default is "node" which is used for just "true" value
  
  I'm leaning towards starting with A for simplicity, even as a bugfix, and only then do B if this is proven not enough.
  
  cc @PBundyra who worked on the initial version of the algo.

### Review by [@abhi179](https://github.com/abhi179) (commented) — 2026-04-13T21:25:30Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1108** by [@abhi179](https://github.com/abhi179) — 2026-04-13T21:25:30Z
```diff
@@ -1103,6 +1103,9 @@ func (s *TASFlavorSnapshot) levelKey(topologyRequest *kueue.PodSetTopologyReques
 	case isSliceTopologyOnlyRequest(topologyRequest):
 		return ptr.To(s.highestLevel())
 	case ptr.Deref(topologyRequest.Unconstrained, false):
+		if features.Enabled(features.TASProfileMixed) {
+			return ptr.To(s.highestLevel())
+		}
```

  good idea, let me introduce it under a feature gate TASHierarchicalUnconstrained

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-14T10:50:22Z

- **pkg/cache/scheduler/tas_cache_test.go:613** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T10:45:15Z
```diff
@@ -603,6 +603,28 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"unconstrained; 6 pods fit into hosts packed within rack; TASHierarchicalUnconstrained": {
```

  Consider adding a brief comment explaining the test, topology and domains. See above.

- **pkg/cache/scheduler/tas_cache_test.go:716** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T10:46:38Z
```diff
@@ -627,7 +649,7 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
-		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASProfileMixed": {
+		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASHierarchicalUnconstrained": {
```

  Was TasProvileMixed removed?

- **pkg/cache/scheduler/tas_cache_test.go:633** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T10:48:31Z
```diff
@@ -603,6 +603,28 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"unconstrained; 6 pods fit into hosts packed within rack; TASHierarchicalUnconstrained": {
+			nodes:  scatteredNodes,
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Unconstrained: ptr.To(true),
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count: 6,
+				wantAssignment: &tas.TopologyAssignment{
+					Levels: defaultOneLevel,
+					Domains: []tas.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"x1"}},
+						{Count: 4, Values: []string{"x3"}},
+						{Count: 1, Values: []string{"x5"}},
+					},
+				},
+			}},
+			featureGates: map[featuregate.Feature]bool{features.TASHierarchicalUnconstrained: true},
```

  To ensure robust coverage of the feature flag logic, consider adding a corresponding test case with the exact same setup but with  features.TASHierarchicalUnconstrained: false to verify the fallback behavior explicitly.

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1123** by [@mwielgus](https://github.com/mwielgus) — 2026-04-14T10:50:09Z
  Do we log what key is eventually used?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T10:09:24Z

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T10:09:24Z
```diff
@@ -595,10 +595,8 @@ func TestFindTopologyAssignments(t *testing.T) {
 					Levels: defaultOneLevel,
 					Domains: []tas.TopologyDomainAssignment{
 						{Count: 1, Values: []string{"x1"}},
-						{Count: 1, Values: []string{"x3"}},
+						{Count: 4, Values: []string{"x3"}},
 						{Count: 1, Values: []string{"x5"}},
-						{Count: 1, Values: []string{"x2"}},
-						{Count: 2, Values: []string{"x6"}},
 					},
```

  Could you elaborate on the un-gated scenario?

### Review by [@abhi179](https://github.com/abhi179) (commented) — 2026-04-28T19:40:46Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1123** by [@abhi179](https://github.com/abhi179) — 2026-04-15T06:11:55Z
  Added a V(3) log line in findTopologyAssignment right after the levelKey is resolved: s.log.V(3).Info("resolved topology level key", "topologyKey", *topologyKey, "unconstrained", unconstrained). This logs which key the scheduler selected for every assignment.

- **pkg/cache/scheduler/tas_cache_test.go:633** by [@abhi179](https://github.com/abhi179) — 2026-04-15T06:14:55Z
```diff
@@ -603,6 +603,28 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"unconstrained; 6 pods fit into hosts packed within rack; TASHierarchicalUnconstrained": {
+			nodes:  scatteredNodes,
+			levels: defaultThreeLevels,
+			podSets: []PodSetTestCase{{
+				topologyRequest: &kueue.PodSetTopologyRequest{
+					Unconstrained: ptr.To(true),
+				},
+				requests: resources.Requests{
+					corev1.ResourceCPU: 1000,
+				},
+				count: 6,
+				wantAssignment: &tas.TopologyAssignment{
+					Levels: defaultOneLevel,
+					Domains: []tas.TopologyDomainAssignment{
+						{Count: 1, Values: []string{"x1"}},
+						{Count: 4, Values: []string{"x3"}},
+						{Count: 1, Values: []string{"x5"}},
+					},
+				},
+			}},
+			featureGates: map[featuregate.Feature]bool{features.TASHierarchicalUnconstrained: true},
```

  Done — added "unconstrained; 6 pods fit into hosts scattered across blocks; TASHierarchicalUnconstrained disabled" with the same scatteredNodes setup but gate=false. It verifies the fallback: pods scatter across all nodes (matching the existing BestFit behavior).

- **pkg/cache/scheduler/tas_cache_test.go:716** by [@abhi179](https://github.com/abhi179) — 2026-04-15T06:15:51Z
```diff
@@ -627,7 +649,7 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
-		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASProfileMixed": {
+		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASHierarchicalUnconstrained": {
```

  No, TASProfileMixed was not removed. It's still the default-on gate that enables LeastFreeCapacity sorting for unconstrained workloads. This PR introduces a separate gate TASHierarchicalUnconstrained (per mimowo's suggestion) that, when enabled alongside
   TASProfileMixed, makes LeastFreeCapacity start from the highest topology level instead of just the node level. The test was renamed from TASProfileMixed → TASHierarchicalUnconstrained because the behavioral change being tested is specifically gated by the new flag.

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@abhi179](https://github.com/abhi179) — 2026-04-22T07:53:18Z
```diff
@@ -595,10 +595,8 @@ func TestFindTopologyAssignments(t *testing.T) {
 					Levels: defaultOneLevel,
 					Domains: []tas.TopologyDomainAssignment{
 						{Count: 1, Values: []string{"x1"}},
-						{Count: 1, Values: []string{"x3"}},
+						{Count: 4, Values: []string{"x3"}},
 						{Count: 1, Values: []string{"x5"}},
-						{Count: 1, Values: []string{"x2"}},
-						{Count: 2, Values: []string{"x6"}},
 					},
```

  Added in the second commit — test case "unconstrained; 6 pods fit into hosts scattered across blocks; TASHierarchicalUnconstrained disabled" verifies the un-gated fallback where pods scatter across all nodes sorted by least free capacity at node level only (no rack awareness).

- **pkg/cache/scheduler/tas_cache_test.go:613** by [@abhi179](https://github.com/abhi179) — 2026-04-28T19:26:45Z
```diff
@@ -603,6 +603,28 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
+		"unconstrained; 6 pods fit into hosts packed within rack; TASHierarchicalUnconstrained": {
```

  Added layout information + per-test capacity explanations

### Review by [@abhi179](https://github.com/abhi179) (commented) — 2026-04-28T19:46:14Z

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@abhi179](https://github.com/abhi179) — 2026-04-28T19:46:13Z
```diff
@@ -595,10 +595,8 @@ func TestFindTopologyAssignments(t *testing.T) {
 					Levels: defaultOneLevel,
 					Domains: []tas.TopologyDomainAssignment{
 						{Count: 1, Values: []string{"x1"}},
-						{Count: 1, Values: []string{"x3"}},
+						{Count: 4, Values: []string{"x3"}},
 						{Count: 1, Values: []string{"x5"}},
-						{Count: 1, Values: []string{"x2"}},
-						{Count: 2, Values: []string{"x6"}},
 					},
```

  The un-gated scenario is the current (existing) behavior: when TASHierarchicalUnconstrained is disabled, workloads with Unconstrained: true resolve their topology level at the node level and sort by
   LeastFreeCapacity only within that level. 
   
   When the gate is enabled, the topology level is resolved starting from the highest level (e.g., rack), so LeastFreeCapacity sorting first picks the busiest rack, then finds the busiest node within it —
   giving a hierarchical packing behavior.
   
   The gate-off test (TASHierarchicalUnconstrained disabled) validates that the existing node-level behavior is preserved when the feature gate is off.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T11:30:28Z

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T11:30:28Z
```diff
@@ -627,7 +682,14 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
-		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASProfileMixed": {
```

  Can we keep the old test as well to showcase how the algorithm acts without `TASHierarchicalUnconstrained`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T16:23:00Z

- **pkg/features/kube_features.go:443** by [@mimowo](https://github.com/mimowo) — 2026-04-29T16:23:00Z
```diff
@@ -431,6 +439,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
 		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHierarchicalUnconstrained: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
```

  Let's make it Beta in 0.18, and backport it as alpha only in a small follow up. I think this new approach makes more sense actually as default. Please also add a note to graduate to GA in 0.21 so that we have enough time to collect feedback in case it was breaking for someone,

### Review by [@abhi179](https://github.com/abhi179) (commented) — 2026-04-29T18:17:34Z

- **pkg/features/kube_features.go:443** by [@abhi179](https://github.com/abhi179) — 2026-04-29T18:17:34Z
```diff
@@ -431,6 +439,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
 		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHierarchicalUnconstrained: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
```

  Done — promoted to Beta (default: true) in v0.18. Added GA graduation note for v0.21 in the feature gate comment, KEP, and versioned_feature_list.yaml.

### Review by [@abhi179](https://github.com/abhi179) (commented) — 2026-04-29T18:18:21Z

- **pkg/cache/scheduler/tas_cache_test.go:630** by [@abhi179](https://github.com/abhi179) — 2026-04-29T18:18:21Z
```diff
@@ -627,7 +682,14 @@ func TestFindTopologyAssignments(t *testing.T) {
 				},
 			}},
 		},
-		"unconstrained; a single pod fits into each host; LeastFreeCapacity; TASProfileMixed": {
```

  Done. Added "unconstrained; a single pod fits into each host; LeastFreeCapacity; TASProfileMixed" (gate-off) alongside the existing gate-on test. The gate-off test shows node-level-only sorting (picks
   x3), while the gate-on test shows hierarchical rack→node sorting (picks x2).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T14:39:03Z

- **keps/2724-topology-aware-scheduling/README.md:1558** by [@mimowo](https://github.com/mimowo) — 2026-05-05T14:39:03Z
```diff
@@ -1541,11 +1542,29 @@ Since v0.15, the available feature gates are as follows:
 | TASProfileBestFit (deprecated)                   | BestFit           | BestFit           | BestFit           |
 | TASProfileLeastFreeCapacity (removed in v0.17)   | LeastFreeCapacity | LeastFreeCapacity | LeastFreeCapacity |
 | TASBalancedPlacement                             | BalancedPlacement | BestFit           | LeastFreeCapacity |
+| TASHierarchicalUnconstrained                     | BestFit           | BestFit           | LeastFreeCapacity (hierarchical) |
 
 Based on the user feedback, we decided to make `TASProfileMixed` default starting in v0.15. (The corresponding feature gate is hence obsolete; we formally keep it "deprecated" for backwards compatibility). It differs from the previous default only in the `unconstrained` case - in which Kueue should prioritize minimizing fragmentation which is provided by the `LeastFreeCapacity` algorithm.
 
 For users still preferring the "always BestFit" profile, we introduce the `TASProfileBestFit` feature gate, marking it as deprecated. We will remove it in v0.17 if we see no report indicating a need for that configuration.
 
+##### Hierarchical LeastFreeCapacity for unconstrained workloads
+
+When `TASHierarchicalUnconstrained` is enabled (alongside the default `TASProfileMixed`
+profile), the `LeastFreeCapacity` algorithm for `unconstrained` workloads resolves the
+topology level starting from the **highest level** in the hierarchy (e.g., rack) rather
+than the lowest level (host). This means the algorithm first identifies the busiest
+higher-level domain and then packs pods into the busiest nodes within that domain,
+resulting in hierarchical packing behavior.
```

  Actually, looking at this PR again I'm not sure we want to start from the highest level. Assume we have the following levels:
  - block
  - rack
  - hostname (node)
  
  And we have many blocks that could fully accomodate the workload, so we would choose the random block, and commit to place the workload in a block which is not fragmented internally.
  
  So, I'm wondering the better strategy might be to actually start from the highest level at whcih the workload cannot fully fit. Wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T14:40:39Z

- **pkg/cache/scheduler/tas_cache_test.go:613** by [@mimowo](https://github.com/mimowo) — 2026-05-05T14:40:39Z
```diff
@@ -602,6 +602,62 @@ func TestFindTopologyAssignments(t *testing.T) {
 					},
 				},
 			}},
+			featureGates: map[featuregate.Feature]bool{features.TASHierarchicalUnconstrained: false},
+		},
+		// Topology: scatteredNodes (block → rack → host)
+		//   b1/r1: x3(4cpu), x5(1cpu), x1(1cpu) = 6cpu total
+		//   b2/r1: x6(2cpu), x2(1cpu) = 3cpu total
+		// With TASHierarchicalUnconstrained, LeastFreeCapacity sorts top-down:
+		// block b1 has more used capacity than b2, so b1 is chosen first, packing
+		// 6 pods into b1's nodes (x3:4, x5:1, x1:1) instead of scattering across blocks.
+		"unconstrained; 6 pods fit into hosts packed within rack; TASHierarchicalUnconstrained": {
```

  Let's add a test cases for the scanario when we have multiple blocks, and each of them fully accomodates the workload, but choosing the random block may result in placing the workload in a block which is not much fragmented, as described in https://github.com/kubernetes-sigs/kueue/pull/10406/changes#r3189306058. I'm wondering if my understanding is correct or not.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T14:41:40Z

- **pkg/features/kube_features.go:444** by [@mimowo](https://github.com/mimowo) — 2026-05-05T14:41:40Z
```diff
@@ -431,6 +440,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 		{Version: version.MustParse("0.10"), Default: false, PreRelease: featuregate.Alpha},
 		{Version: version.MustParse("0.15"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHierarchicalUnconstrained: {
+		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
```

  After the second thought, we may need to keep it in Alpha for 0.18, especially if I'm correct about the issue in the current version: https://github.com/kubernetes-sigs/kueue/pull/10406/changes#r3189316475
