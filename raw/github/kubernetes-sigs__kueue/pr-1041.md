# PR #1041: KEP: workload priority

**Summary**: KEP: workload priority

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1041

**Last updated**: 2023-08-18T08:40:14Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Gekko0114](https://github.com/Gekko0114)
- **Branch**: `kep` → `main`
- **Created**: 2023-08-06T07:10:08Z
- **Updated**: 2023-08-18T08:40:14Z
- **Merged**: 2023-08-18T08:08:29Z
- **Closed**: 2023-08-18T08:08:29Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `release-note-none`, `size/XL`, `approved`, `kind/api-change`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Changed files**: 2   **+510 / -0**

## Description

#### What type of PR is this?
/kind documentation
/kind api-change

#### What this PR does / why we need it:
KEP for #973 

#### Special notes for your reviewer:
Thank you so much for your reviews.

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-06T07:10:15Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 5cb73280613e8251e2067696fa68728c92cbe704 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64df23f8d7559e000894b077 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-06T07:10:17Z

Hi @Gekko0114. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-06T07:10:52Z

Hi @alculquicondor,
I created a KEP based on the issue's discussion.
Could you take a look?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T11:50:14Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-16T14:26:23Z

/label tide/merge-method-squash
/approve
I'll leave the LGTM to @tenzen-y for any final comments

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-16T14:26:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1041#issuecomment-1680721098" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1041#" title="Author self-approved">Gekko0114</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T17:36:32Z

I will review this KEP within 24 hours. Thanks for driving this KEP!

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-18T05:59:09Z

Hi @tenzen-y , @alculquicondor 
Thank you so much for your insightful comments! I learned a lot from this KEP.
I've fixed all the comments.
Could you take a look again?

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-07T12:18:11Z

- **keps/973-workload-priority/README.md:67** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T11:59:18Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
```

  Mentioning that kueue priorityClass doesn't implement all features on the k8s priorityClass might be worth it.

- **keps/973-workload-priority/README.md:62** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:01:12Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
```

  Putting concrete user stories would be nice here.
  For example, please refer to other KEPs.

- **keps/973-workload-priority/README.md:66** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:05:04Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
```

  How does conflict is happens? Mentioning a concrete issue with reference to [Kante's comment](https://github.com/kubernetes-sigs/kueue/issues/973#issuecomment-1641663413) would be nice.

- **keps/973-workload-priority/README.md:74** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:08:07Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
```

  I'm thinking maybe the new API name should be named `WorkloadPriorityClass` to avoid confusion with K8s `PriorityClass`.
  
  WDYT? @alculquicondor @kerthcet  @Gekko0114

- **keps/973-workload-priority/README.md:92** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:11:42Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
```

  I hesitate to use v1beta1 as api version. To allow a period of time to improve the API, using `v1alpha1` might be better.
  WDYT? @alculquicondor @kerthcet @Gekko0114

- **keps/973-workload-priority/README.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:12:55Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
```

  ```suggestion
  
  The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
  ```

- **keps/973-workload-priority/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:15:51Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  In the following, it would be nice to write a plan for the logic for the Workload and PriorityClass.

- **keps/973-workload-priority/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:17:47Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  In the following, mentioning a future work regarding [`Rereference to another object that could be a CRD defined by an organization.`](https://github.com/kubernetes-sigs/kueue/issues/973#issuecomment-1664341233) would be great :)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-07T12:44:55Z

- **keps/973-workload-priority/README.md:82** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:44:55Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
```

  ```suggestion
  	 Description string `json:"description,omitempty"`
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-07T12:45:19Z

- **keps/973-workload-priority/kep.yaml:5** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-07T12:45:18Z
```diff
@@ -0,0 +1,31 @@
+title: workload priority
+kep-number: 973
+authors:
+  - "@Gekko0114"
+status:implementable
```

  ```suggestion
  status: implementable
  ```

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T13:28:46Z

- **keps/973-workload-priority/README.md:74** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T13:28:46Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
```

  I agree on this opinion. Either is fine for me.
  In order to avoid confusion, I think `WorkloadPriorityClass` is better

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T13:31:42Z

- **keps/973-workload-priority/README.md:92** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T13:31:42Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
```

  I used v1beta1 here without any specific reason.
  Since this feature is not yet stable, I agree that we should use v1alpha1 here

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T13:37:41Z

- **keps/973-workload-priority/README.md:67** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T13:37:40Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
```

  Thanks for all of your comments! Fixed it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T14:20:08Z

- **keps/973-workload-priority/README.md:280** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T14:20:08Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  Thanks for your comment! I added logic for this case.
  Is this you intended?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T14:20:25Z

- **keps/973-workload-priority/README.md:280** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T14:20:25Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  Thanks. Added "future work"

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T14:21:15Z

- **keps/973-workload-priority/README.md:62** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T14:21:14Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
```

  Thanks for your comments! Added user story

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-07T14:21:38Z

- **keps/973-workload-priority/README.md:66** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-07T14:21:38Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
```

  Added how the conflict might happen

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-08T20:34:00Z

- **keps/973-workload-priority/README.md:74** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-08T20:33:59Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
```

  yes, even though they belong to different API groups, it's better to avoid the confusion.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T05:08:55Z

- **keps/973-workload-priority/README.md:161** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T04:42:57Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+The Kueue's `PriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `PriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
```

  How to use this `WorkloadPriorityClass` from Jobs?

- **keps/973-workload-priority/README.md:142** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T04:44:59Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+The Kueue's `PriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `PriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are non-preempting.
```

  ```suggestion
  We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
  ```

- **keps/973-workload-priority/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T05:08:37Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  I would suggest to say more details.
  For example, I have questions:
  
  - Who does the priority of `WorkdloadPriorityClass` evaluate? flavorAssigner? preempter?
  - What happens if the Pod `PriorityClass` and `WorkloadPriorityClass` are conflicts? Which priority is respected?
  - How to expand [`Priority`](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) utility?
  - Which resources `WorkloadPriorityClass` controller needs to reconcile against? (Watch for only `WorkloadPriorityClass`?)
  - etc...

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-09T13:05:42Z

- **keps/973-workload-priority/README.md:280** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-09T13:05:42Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  Thank you for your comments. I agree with you.
  I'll explain it in a way that prevents misunderstandings and will also consider it from an implementation standpoint.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-09T13:09:07Z

- **keps/973-workload-priority/README.md:74** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-09T13:09:07Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
```

  Changed to `WorkloadPriorityClass`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-09T13:22:20Z

- **keps/973-workload-priority/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-09T13:22:19Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  Thank you for your hard work :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-10T19:27:41Z

- **keps/973-workload-priority/README.md:140** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-10T19:24:23Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Workload](#how-to-use-workloadpriorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
```

  ```suggestion
  For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
  ```

- **keps/973-workload-priority/README.md:161** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-10T19:26:44Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Workload](#how-to-use-workloadpriorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Workload
```

  Also, how to use from a Job/MPIJob/etc?

- **keps/973-workload-priority/README.md:184** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-10T19:27:37Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Workload](#how-to-use-workloadpriorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Workload
+
+The `WorkloadPriorityClass` is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
```

  there should already be a priorityValue. We should populate it from the Workload reconciler if it's not populated.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-13T05:48:15Z

- **keps/973-workload-priority/README.md:161** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-13T05:48:15Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+The Kueue's `PriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `PriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
```

  Thanks. Added

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-13T05:48:36Z

- **keps/973-workload-priority/README.md:184** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-13T05:48:36Z
```diff
@@ -0,0 +1,225 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Workload](#how-to-use-workloadpriorityclass-on-workload)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass` because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, a `WorkloadPriorityClass` will be created.
+The `Workload` will be able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: dev-workload-class
+value: 100
+description: "Dev's priority class"
+---
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: prod-workload-class
+value: 10000
+description: "Prod's priority class"
+---
+# dev-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-a
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+---        
+# dev-workload-b.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: dev-job-b
+spec:
+  priorityClassName: dev-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+# prod-workload-a.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: prod-job-b
+spec:
+  priorityClassName: prod-workload-class
+  queueName: team-b-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest        
+```
+
+
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Workload
+
+The `WorkloadPriorityClass` is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
```

  Thanks. Updated

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-13T05:49:30Z

- **keps/973-workload-priority/README.md:280** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-13T05:49:30Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  I've updated this file.
  Could you review it again?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-13T05:50:15Z

- **keps/973-workload-priority/README.md:45** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-13T05:50:15Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
```

  Is my understanding correct?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-13T05:51:00Z

- **keps/973-workload-priority/README.md:156** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-13T05:51:00Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
```

  If you have better idea, could you leave comments? Thanks

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T13:14:52Z

- **keps/973-workload-priority/README.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:01:50Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
```

  kube-scheduler

- **keps/973-workload-priority/README.md:49** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:02:20Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
```

  A motivation should explain why we want this :)

- **keps/973-workload-priority/README.md:93** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:04:47Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
```

  They shouldn't change the value in the WorkloadPriorityClass, as that would affect all Workloads. But they can change the value in the Workload spec.

- **keps/973-workload-priority/README.md:123** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:05:34Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
```

  This should be enough.
  
  ```suggestion
  The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
  ```

- **keps/973-workload-priority/README.md:156** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:09:10Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
```

  I think it's better to have a separate field indicating where is the priority coming from:
  
  `priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass`
  or
  `priorityClassSource: scheduling.k8s.io/PriorityClass`

- **keps/973-workload-priority/README.md:283** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:12:31Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassName: kueue.x-k8s.io/workload-priority-class/sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function, `workloadPriorityClass` is checked first.
```

  We should have different functions. If the user sets the priority via annotation, we should search for the WorkloadPriorityClass.
  If the user only sets pod priority, we look for the k8s PriorityClass.

- **keps/973-workload-priority/README.md:298** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:13:45Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassName: kueue.x-k8s.io/workload-priority-class/sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function, `workloadPriorityClass` is checked first.
+If `workloadPriorityClass` is not present, the k8s `priorityClass` is referred to.
+
+### Where workload's Priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+With the introduction of `workloadPriorityClass`, there is no change in the places where priority is used in Kueue.
+It just enables the usage of `workloadPriorityClass` as the priority.
+
+### Role of workloadPriorityClass controller
+
+The workloadPriorityClass Controller only reconcile the `workloadPriorityClass`.
+
+### When workloadPriorityClass's priority is modified
+
+The workload controller updates the value of `workloadPriorityClass` through reconcilation.
```

  I'm not understanding here. Isn't the user who changes the value?

- **keps/973-workload-priority/kep.yaml:31** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T13:14:46Z
```diff
@@ -0,0 +1,31 @@
+title: workload priority
+kep-number: 973
+authors:
+  - "@Gekko0114"
+status: implementable
+creation-date: 2023-08-05
+reviewers:
+  - "@alculquicondor"
+  - "@kerthcet"
+  - "@tenzen-y"
+approvers:
+  - "@alculquicondor"
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. This can be the current (upcoming) milestone, if it is being actively
+# worked on.
+latest-milestone: "v0.5"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.5"
+  beta: "v0.6"
+  stable: "v0.7"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: WorkloadPriority
```

  I'm ok not having a feature gate for this, since it's opt-in behavior

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-14T14:45:20Z

- **keps/973-workload-priority/README.md:298** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-14T14:45:20Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassName: kueue.x-k8s.io/workload-priority-class/sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function, `workloadPriorityClass` is checked first.
+If `workloadPriorityClass` is not present, the k8s `priorityClass` is referred to.
+
+### Where workload's Priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+With the introduction of `workloadPriorityClass`, there is no change in the places where priority is used in Kueue.
+It just enables the usage of `workloadPriorityClass` as the priority.
+
+### Role of workloadPriorityClass controller
+
+The workloadPriorityClass Controller only reconcile the `workloadPriorityClass`.
+
+### When workloadPriorityClass's priority is modified
+
+The workload controller updates the value of `workloadPriorityClass` through reconcilation.
```

  I misunderstood here. Thanks for your comment.
  Updated here.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-14T14:46:22Z

- **keps/973-workload-priority/README.md:93** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-14T14:46:21Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
```

  Thanks, you're right. Updated.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-14T14:46:35Z

- **keps/973-workload-priority/README.md:49** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-14T14:46:35Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
```

  Thanks. Updated. Is it enough?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-14T14:46:48Z

- **keps/973-workload-priority/README.md:156** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-14T14:46:48Z
```diff
@@ -0,0 +1,331 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Role of workloadPriorityClass controller](#role-of-workloadpriorityclass-controller)
+  - [When workloadPriorityClass's priority is modified](#when-workloadpriorityclasss-priority-is-modified)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-controller for preempting pods and jobs.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `WorkloadPriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `WorkloadPriorityClass`.
+`Workload` can utilize `WorkloadPriorityClass`.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `WorkloadPriorityClass`, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/workload-priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/workload-priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
+To distinguish, when using `workloadPriorityClass`, a prefix "kueue.x-k8s.io/workload-priority-class/" is added.
```

  Updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-14T14:54:31Z

I think this looks good.
Maybe explain if you plan to add new integration tests or want to modify the existing ones to use WorkloadPriorityClass instead of PriorityClass.
It might not be worth duplicating all tests, but also it might be worth keeping some integration tests for PriorityClass.

- **keps/973-workload-priority/README.md:121** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-14T14:51:05Z
```diff
@@ -115,12 +116,21 @@ type WorkloadPriorityClass struct {
 	Value int32   `json:"value,omitempty"`
 	Description string `json:"description,omitempty"`
 }
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.
```

  Explain what this is and possible values, or add a link to the sections that explain it.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-15T13:51:28Z

- **keps/973-workload-priority/README.md:122** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T13:50:38Z
```diff
@@ -119,6 +119,9 @@ type WorkloadPriorityClass struct {
 ```
 
 Also `priorityClassSource` field is added to `WorkloadSpec`.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` as values.
```

  ```suggestion
  The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
  ```

- **keps/973-workload-priority/README.md:339** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T13:51:26Z
```diff
@@ -330,7 +333,10 @@ This change should be covered by unit tests.
 
 #### Integration tests
 
-This change should be covered by integration tests.
+The following scenarios will be covered with integration tests where `WorkloadPriorityClass` is used:
+- Controller tests related to job interfaces such as job_controller, mpi_job_controller, etc
+- Integration tests for scheduler and webhook where the existing integration tests already cover `PriorityClass`
+- Integration tests for workloadPriorityClass_controller
```

  Will we actually need a reconciler for WorkloadPriorityClass?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-15T14:03:31Z

- **keps/973-workload-priority/README.md:339** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-15T14:03:31Z
```diff
@@ -330,7 +333,10 @@ This change should be covered by unit tests.
 
 #### Integration tests
 
-This change should be covered by integration tests.
+The following scenarios will be covered with integration tests where `WorkloadPriorityClass` is used:
+- Controller tests related to job interfaces such as job_controller, mpi_job_controller, etc
+- Integration tests for scheduler and webhook where the existing integration tests already cover `PriorityClass`
+- Integration tests for workloadPriorityClass_controller
```

  The WorkloadPriorityClass controller would only reconcile `WorkloadPriorityClass`.
  Therefore, having a reconciler might be optional.
  What do you think?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-15T14:33:06Z

- **keps/973-workload-priority/README.md:339** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-15T14:33:05Z
```diff
@@ -330,7 +333,10 @@ This change should be covered by unit tests.
 
 #### Integration tests
 
-This change should be covered by integration tests.
+The following scenarios will be covered with integration tests where `WorkloadPriorityClass` is used:
+- Controller tests related to job interfaces such as job_controller, mpi_job_controller, etc
+- Integration tests for scheduler and webhook where the existing integration tests already cover `PriorityClass`
+- Integration tests for workloadPriorityClass_controller
```

  But what does the reconciler do? I don't think we require any functionality.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-16T13:35:19Z

- **keps/973-workload-priority/README.md:339** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-16T13:35:18Z
```diff
@@ -330,7 +333,10 @@ This change should be covered by unit tests.
 
 #### Integration tests
 
-This change should be covered by integration tests.
+The following scenarios will be covered with integration tests where `WorkloadPriorityClass` is used:
+- Controller tests related to job interfaces such as job_controller, mpi_job_controller, etc
+- Integration tests for scheduler and webhook where the existing integration tests already cover `PriorityClass`
+- Integration tests for workloadPriorityClass_controller
```

  Yeah, I agree. Then I will not implement workloadPriorityClass controller.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-16T21:35:41Z

- **keps/973-workload-priority/README.md:45** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T20:33:30Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
```

  ```suggestion
  kube-scheduler for preempting pods.
  ```
  
  IIUC, the kube-scheduler doesn't manage jobs.

- **keps/973-workload-priority/README.md:168** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T20:58:42Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
```

  The camel case looks a bit weird as a `priorityClassSource`'s value like `kueue.x-k8s.io/WorkloadPriorityClass`.
  Should we use a lowercase similar to config API?
  
  https://github.com/kubernetes-sigs/kueue/blob/9efe0d713b5937ca590efe21bb4ae6f7b15bf196/config/components/manager/controller_manager_config.yaml#L31-L37
  
  WDYT? @alculquicondor @Gekko0114

- **keps/973-workload-priority/README.md:236** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:07:35Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+```yaml
```

  ```suggestion
  
  ```yaml
  ```

- **keps/973-workload-priority/README.md:219** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:13:30Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
```

  I think there are more cases. Can you add cases if the jobFrameworks, such as `MPIJob`, implement the `JobWithPriorityClass` interface? Which `PriorityClass` is used?
  
  https://github.com/kubernetes-sigs/kueue/blob/9efe0d713b5937ca590efe21bb4ae6f7b15bf196/pkg/controller/jobframework/interface.go#L69-L72

- **keps/973-workload-priority/README.md:307** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:17:35Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function on workload, first we check the `PriorityClassSource` field.
+If `PriorityClassSource` has the value `kueue.x-k8s.io/WorkloadPriorityClass`, `workloadPriorityClass` is searched.
+If `PriorityClassSource` has the value `scheduling.k8s.io/PriorityClass`, `priorityClass` is searched.
+
+### Where workload's Priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+With the introduction of `workloadPriorityClass`, there is no change in the places where priority is used in Kueue.
+It just enables the usage of `workloadPriorityClass` as the priority.
+
+### Not Implementing the workloadPriorityClass Controller
```

  Moving this to the `Non-Goals` section would be worth it.

- **keps/973-workload-priority/README.md:69** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:29:21Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
```

  Does `priority value` mean including `priorityClassSource`?

- **keps/973-workload-priority/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:33:52Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  This label is mutable only when the Job isn't yet admitted by ClusterQueue, right?
  It would be worth mentioning about conditions that the label is mutable.

- **keps/973-workload-priority/README.md:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-16T21:34:31Z
```diff
@@ -0,0 +1,143 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue PriorityClass API](#kueue-priorityclass-api)
+  - [How to use PriorityClass on Workload](#how-to-use-priorityclass-on-workload)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be a part of the workload spec and be mutable.
+
+## Motivation
+
+We want to make the priority of the workload customizable according to user's needs.
+By defining `Workload`'s `PriorityClass`, we aim to enable various cutomizations only for Kueue's `Workload`.
+
+### Goals
+
+Implement `PriorityClass` for `Workload`.
+`Workload` can utilize `Workload`'s `PriorityClass`.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for `Workload` is not recommended.
+
+## Proposal
+
+In this proposal, a `PriorityClass` for `Workload` will be created.
+The `Workload` will be able to utilize this `PriorityClass`.
+`Workload`'s `PriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature details.
+
+### Risks and Mitigations
+
+It's possible that pod's priority conflicts with workload's priority.
+We sohuld document the risks of pod preemption to uses.
+We can also point users to create PriorityClasses for their pods that are non-preempting.
+
+## Design Details
+
+### Kueue PriorityClass API
+
+We introduce the Kueue `PriorityClass` API.
+
+```golang
+type PriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+  Description string `json:"description,omitempty"`
+}
+
+```
+
+### How to use PriorityClass on Workload
+The `PriorityClass` for a Workload is defined using `PriorityClassName` field.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: PriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: sample-job
+spec:
+  priorityClassName: sample-priority
+  queueName: team-a-queue
+  podSets:
+  - count: 3
+    name: main
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          imagePullPolicy: Always
+          name: container
+```
+
```

  Thanks for the update!

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-17T12:15:46Z

- **keps/973-workload-priority/README.md:168** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-17T12:15:46Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
```

  yeah, let's be consistent.
  Out of curiosity, does the RESTMapper return lowercase as well?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T13:29:12Z

- **keps/973-workload-priority/README.md:168** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T13:29:11Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
```

  I agree, then I will use "kueue.x-k8s.io/workloadpriorityclass" and "scheduling.k8s.io/priorityclass" for each.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T13:42:41Z

- **keps/973-workload-priority/README.md:219** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T13:42:41Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
```

  I will add cases, thanks.
  
  I thought this `JobWithPriorityClass` interface is for `pod's priority`.
  If an MPIJob has both `PriorityClass` and `kueue.x-k8s.io/priority-class`, the `kueue.x-k8s.io/priority-class` is used as 'workload's priority'.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T14:06:21Z

- **keps/973-workload-priority/README.md:139** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T14:06:20Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  Thanks for your comment.
  I haven't decided whether `workloadPriorityClass` is mutable or not.
  
  > This label is mutable only when the Job isn't yet admitted by ClusterQueue, right?
  
  Sounds good, I will add this in README.md.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T14:16:38Z

- **keps/973-workload-priority/README.md:69** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T14:16:38Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
```

  Thanks for your comment.
  I don't mean `priority value` including `priorityClassSource` here, but `priorityClassSource` should be mutable when the workload isn't yet admitted by ClusterQueue.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T14:22:49Z

- **keps/973-workload-priority/README.md:69** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T14:22:49Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
```

  Updated.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-17T14:22:56Z

- **keps/973-workload-priority/README.md:139** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-17T14:22:56Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  Updated.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-17T14:43:02Z

- **keps/973-workload-priority/README.md:168** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-17T14:43:02Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
```

  > Out of curiosity, does the RESTMapper return lowercase as well?
  
  RESTMapper returns `schema.GroupVersionKind`. So it wouldn't return lowercase :)

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-17T14:45:09Z

- **keps/973-workload-priority/README.md:139** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-17T14:45:08Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  I think we should just make it mutable.
  Note that there is an open KEP to make PriorityClass mutable in k8s
  
  https://github.com/kubernetes/enhancements/pull/4129

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-17T14:46:44Z

- **keps/973-workload-priority/README.md:219** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-17T14:46:44Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/WorkloadPriorityClass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityCLass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+The same applies to CRDs other than `Job` (such as `RayJob`).
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
```

  > If an MPIJob has both PriorityClass and kueue.x-k8s.io/priority-class, the kueue.x-k8s.io/priority-class is used as 'workload's priority'.
  
  SGTM Thanks!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-17T14:55:00Z

- **keps/973-workload-priority/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-17T14:55:00Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  > I think we should just make it mutable.
  
  If `workloadPriorityClass` is always mutable, it might be useful for the preemption. Maybe, we can easily preempt the workloads that exceed the time budget for the user.
  
  So, +1 on `just make it mutable`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-17T14:55:54Z

- **keps/973-workload-priority/README.md:139** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-17T14:55:53Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  @Gekko0114 Also, can you add https://github.com/kubernetes/enhancements/pull/4129 to this KEP as an xref?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-18T05:55:18Z

- **keps/973-workload-priority/README.md:139** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-18T05:55:18Z
```diff
@@ -0,0 +1,349 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityCLass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Not Implementing the workloadPriorityClass Controller](#not-implementing-the-workloadpriorityclass-controller)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term "workload Priority" is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term "pod Priority" is used to denote the priority utilized by the
+kube-scheduler for preempting pods and jobs.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's Priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.
+The `Workload` is able to utilize this `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value will be part of the workload spec and be mutable.
+CRDs like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `priorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the "kueue.x-k8s.io/WorkloadPriorityClass" value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the "scheduling.k8s.io/PriorityClass" value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
```

  Thanks. Added

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-18T07:37:27Z

@Gekko0114 Thanks for the update! Overall LGTM!
I left a comment for a nit.

- **keps/973-workload-priority/README.md:444** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-18T07:14:02Z
```diff
@@ -0,0 +1,484 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityClass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+    - [4. A jobFramework specifies both <code>workload's priority</code> and <code>priorityClass</code>](#4-a-jobframework-specifies-both--and-)
+    - [5. A jobFramework specifies only <code>workload's priority</code>](#5-a-jobframework-specifies-only-)
+    - [6. A jobFramework specifies only <code>priorityClass</code>](#6-a-jobframework-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Workload's priority values are always mutable](#workloads-priority-values-are-always-mutable)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term `workload Priority` is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term `pod Priority` is used to denote the priority utilized by the
+kube-scheduler for preempting pods.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+JobFrameworks like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.  
+When creating a new `WorkloadPriorityClass`, there is no need to create other CRDs owned by `WorkloadPriorityClass`. Therefore, the reconcile functionality is unnecessary. The `WorkloadPriorityClass` controller will not be implemented for now.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.  
+The `Workload` is able to utilize this `WorkloadPriorityClass`.  
+`WorkloadPriorityClass` is independent from pod's priority.  
+`Priority`, `PriorityClassName` and `PriorityClassSource` fields will be part of the workload spec and always be mutable.
+JobFrameworks like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.  
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).  
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `PriorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `WorkloadPriorityClass` names as values.
+To distinguish, when using `WorkloadPriorityClass`, a `PriorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This label is always mutable because it might be useful for the preemption.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityClass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+In the case of jobFrameworks, the following scenarios are considered. For jobFrameworks, the `priorityClass` is intended to reflect the `pod's priority`. Therefore, `workloadPriorityClass` is used for the `workload's priority` if jobFramework has both `workloadPriorityClass` and `priorityClass`.
+
+4. A jobFramework specifies both `workload's priority` and `priorityClass`
+5. A jobFramework specifies only `workload's priority`
+6. A jobFramework specifies only `priorityClass`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 4. A jobFramework specifies both `workload's priority` and `priorityClass`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+    schedulingPolicy:
+      priorityClass: high-priority
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+#### 5. A jobFramework specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+#### 6. A jobFramework specifies only `priorityClass`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+    schedulingPolicy:
+      priorityClass: high-priority
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function on workload, first we check the `PriorityClassSource` field.
+If `PriorityClassSource` has the value `kueue.x-k8s.io/workloadpriorityclass`, `workloadPriorityClass` is searched.
+If `PriorityClassSource` has the value `scheduling.k8s.io/priorityclass`, `priorityClass` is searched.
+
+### Where workload's Priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+With the introduction of `workloadPriorityClass`, there is no change in the places where priority is used in Kueue.
+It just enables the usage of `workloadPriorityClass` as the priority.
+
+### Workload's priority values are always mutable
+
+`Priority`, `PriorityClassName` and `PriorityClassSource` fields is part of the workload spec and always mutable because it might be useful for the preemption.  
```

  nit
  
  ```suggestion
  `Priority`, `PriorityClassName`, and `PriorityClassSource` fields are part of the workload spec and always mutable because it might be useful for the preemption.  
  ```

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-18T07:55:50Z

- **keps/973-workload-priority/README.md:444** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-18T07:55:50Z
```diff
@@ -0,0 +1,484 @@
+# KEP-973: Workload priority
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Kueue WorkloadPriorityClass API](#kueue-workloadpriorityclass-api)
+  - [How to use WorkloadPriorityClass on Job](#how-to-use-workloadpriorityclass-on-job)
+  - [How to use WorkloadPriorityClass on MPIJob](#how-to-use-workloadpriorityclass-on-mpijob)
+  - [How workloads are created from Jobs](#how-workloads-are-created-from-jobs)
+    - [1. A job specifies both <code>workload's priority</code> and <code>pod's priority</code>](#1-a-job-specifies-both--and-)
+    - [2. A job specifies only <code>workload's priority</code>](#2-a-job-specifies-only-)
+    - [3. A job specifies only <code>pod's priority</code>](#3-a-job-specifies-only-)
+    - [4. A jobFramework specifies both <code>workload's priority</code> and <code>priorityClass</code>](#4-a-jobframework-specifies-both--and-)
+    - [5. A jobFramework specifies only <code>workload's priority</code>](#5-a-jobframework-specifies-only-)
+    - [6. A jobFramework specifies only <code>priorityClass</code>](#6-a-jobframework-specifies-only-)
+  - [How to expand Priority utility](#how-to-expand-priority-utility)
+  - [Where workload's Priority is used](#where-workloads-priority-is-used)
+  - [Workload's priority values are always mutable](#workloads-priority-values-are-always-mutable)
+  - [What happens when a user changes the priority of <code>workloadPriorityClass</code>?](#what-happens-when-a-user-changes-the-priority-of-)
+  - [Future works](#future-works)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+In this proposal, a `WorkloadPriorityClass` is created.
+The `Workload` is able to utilize `WorkloadPriorityClass`.
+`WorkloadPriorityClass` is independent from pod's priority.
+The priority value is a part of the workload spec and is mutable.  
+In this document, the term `workload Priority` is used to refer
+to the priority utilized by Kueue controller for managing the queueing
+and preemption of workloads.  
+The term `pod Priority` is used to denote the priority utilized by the
+kube-scheduler for preempting pods.
+
+## Motivation
+
+Currently, some proposals are submitted for the kueue scheduling order.
+However, under the current implementation, the priority of the `Workload` is tied to the priority of the pod. Therefore, it's not possible to change only the priority of the `Workload`. We don't want to change the pod priority because pod's priority is tied to pod preemption. We need a mechanism where users can freely modify the priority of the `Workload` alone, not affecting pod priority.
+
+### Goals
+
+Implement `WorkloadPriorityClass`. `Workload` can utilize `WorkloadPriorityClass`.  
+JobFrameworks like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.  
+Users can modify the priority of a `Workload` by changing `Workload`'s priority directly.
+
+### Non-Goals
+
+Using existing k8s `PriorityClass` for Workload's priority is not recommended.  
+`WorkloadPriorityClass` doesn't implement all the features of the k8s `PriorityClass`
+because some fields on the k8s `PriorityClass` are not relevant to Kueue.  
+When creating a new `WorkloadPriorityClass`, there is no need to create other CRDs owned by `WorkloadPriorityClass`. Therefore, the reconcile functionality is unnecessary. The `WorkloadPriorityClass` controller will not be implemented for now.
+
+## Proposal
+
+In this proposal, `WorkloadPriorityClass` is defined.  
+The `Workload` is able to utilize this `WorkloadPriorityClass`.  
+`WorkloadPriorityClass` is independent from pod's priority.  
+`Priority`, `PriorityClassName` and `PriorityClassSource` fields will be part of the workload spec and always be mutable.
+JobFrameworks like Job, MPIJob etc specify the `WorkloadPriorityClass` through labels.
+
+<!--
+This is where we get down to the specifics of what the proposal actually is.
+This should have enough detail that reviewers can understand exactly what
+you're proposing, but should not include things like API designs or
+implementation. What is the desired outcome and how do we measure success?.
+The "Design Details" section below is for the real
+nitty-gritty.
+-->
+
+### User Stories
+
+Kueue issue [973](https://github.com/kubernetes-sigs/kueue/issues/973) provides details on the initial feature implementation.
+
+#### Story 1
+
+In an organization, admins want to set a lower priority for development workloads and a higher priority for production workloads.
+In such cases, they create two `WorkloadPriorityClass` and apply each one to the respective workloads.
+
+#### Story 2
+
+An organization desires to modify the priority of workloads that remain inactive for a specific duration.
+By developing a custom controller to manage Priority value of `Workload` spec, this expectation can be met.
+
+### Risks and Mitigations
+
+It's possible that the pod's priority conflicts with the workload's priority.
+For example, a high-priority job with low-priority pods may never run to completion because it may always be preempted by kube-scheduler.
+We should document the risks of pod preemption to uses.  
+We can also point users to create `PriorityClass` for their pods that are [non-preempting](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/#non-preempting-priority-class).  
+If a workload's priority is high and pod's priority is low and the kube-scheduler initiates preemption, the pod's priority is prioritized. To prevent this behavior, non-preempting setting is needed.
+
+
+## Design Details
+
+### Kueue WorkloadPriorityClass API
+
+We introduce the `WorkloadPriorityClass` API.
+
+```golang
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value int32   `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+```
+
+Also `PriorityClassSource` field is added to `WorkloadSpec`.  
+The `PriorityClass` field can accept both k8s `PriorityClass` and `WorkloadPriorityClass` names as values.
+To distinguish, when using `WorkloadPriorityClass`, a `PriorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```golang
+type WorkloadSpec struct {
+  ...
+  PriorityClassSource string `json:"priorityClassSource,omitempty"`
+  ...
+}
+```
+
+### How to use WorkloadPriorityClass on Job
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This label is always mutable because it might be useful for the preemption.
+
+```yaml
+# sample-priority-class.yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+---
+# sample-job.yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above.
+The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values.
+To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+# sample-workload.yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb-f5982
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+In this example, since the `WorkloadPriorityClassName` of `sample-job` is set to `sample-priority`, the `priority` of the `sample-job` will be set to 10,000.
+During queuing and preemption of the workload, this priority value will be used in the calculations.
+
+### How to use WorkloadPriorityClass on MPIJob
+
+The `workloadPriorityClass` is specified through a label `kueue.x-k8s.io/priority-class`.
+This is same as other CRDs like `RayJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+### How workloads are created from Jobs
+
+There are three scenarios for creating a workload from a job.
+
+1. A job specifies both `workload's priority` and `pod's priority`
+2. A job specifies only `workload's priority`
+3. A job specifies only `pod's priority`
+
+In the case of jobFrameworks, the following scenarios are considered. For jobFrameworks, the `priorityClass` is intended to reflect the `pod's priority`. Therefore, `workloadPriorityClass` is used for the `workload's priority` if jobFramework has both `workloadPriorityClass` and `priorityClass`.
+
+4. A jobFramework specifies both `workload's priority` and `priorityClass`
+5. A jobFramework specifies only `workload's priority`
+6. A jobFramework specifies only `priorityClass`
+
+#### 1. A job specifies both `workload's priority` and `pod's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+On the other hand, the `priorityClass` high-priority is used for the `pod's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 2. A job specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 3. A job specifies only `pod's priority`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  generateName: sample-job-
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  priorityClassName: high-priority
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+#### 4. A jobFramework specifies both `workload's priority` and `priorityClass`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+    schedulingPolicy:
+      priorityClass: high-priority
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+#### 5. A jobFramework specifies only `workload's priority`
+
+When creating this yaml, the `workloadPriorityClass` sample-priority is used for the `workload's priority`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+#### 6. A jobFramework specifies only `priorityClass`
+
+When creating this yaml, the `PriorityClass` high-priority is used for the `workload's priority`.
+This is basically same as current implementation of workload.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+spec:
+  slotsPerWorker: 1
+  runPolicy:
+    cleanPodPolicy: Running
+    ttlSecondsAfterFinished: 60
+    schedulingPolicy:
+      priorityClass: high-priority
+  sshAuthMountPath: /home/mpiuser/.ssh
+  mpiReplicaSpecs:
+    Launcher:
+      replicas: 1
+      template:
+        spec:
+          containers:
+          - image: mpioperator/mpi-pi:openmpi
+            name: mpi-launcher
+            securityContext:
+              runAsUser: 1000
+            command:
+            - mpirun
+            args:
+            - -n
+            - "2"
+            - /home/mpiuser/pi
+            resources:
+              limits:
+                cpu: 1
+                memory: 1Gi
+```
+
+### How to expand Priority utility
+
+When referencing the `priorityClass` in the [Priority utility](https://github.com/kubernetes-sigs/kueue/blob/ba404ad282c35cf1d6b15d07643935fffbcc1835/pkg/util/priority/priority.go) function on workload, first we check the `PriorityClassSource` field.
+If `PriorityClassSource` has the value `kueue.x-k8s.io/workloadpriorityclass`, `workloadPriorityClass` is searched.
+If `PriorityClassSource` has the value `scheduling.k8s.io/priorityclass`, `priorityClass` is searched.
+
+### Where workload's Priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+With the introduction of `workloadPriorityClass`, there is no change in the places where priority is used in Kueue.
+It just enables the usage of `workloadPriorityClass` as the priority.
+
+### Workload's priority values are always mutable
+
+`Priority`, `PriorityClassName` and `PriorityClassSource` fields is part of the workload spec and always mutable because it might be useful for the preemption.  
```

  Thanks! Fixed

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-18T08:08:01Z

Thanks!
/lgtm
