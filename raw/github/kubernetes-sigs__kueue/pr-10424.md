# PR #10424: [Cleanup] Deduplicate e2e util functions

**Summary**: [Cleanup] Deduplicate e2e util functions

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10424

**Last updated**: 2026-04-17T06:54:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mszadkow](https://github.com/mszadkow)
- **Branch**: `cleanup/10200-dedup-e2e-helpers` → `main`
- **Created**: 2026-04-13T11:39:04Z
- **Updated**: 2026-04-17T06:54:57Z
- **Merged**: 2026-04-16T08:23:03Z
- **Closed**: 2026-04-16T08:23:03Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`, `area/testing`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 7   **+82 / -179**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
/area testing
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
As mentioned [here](https://github.com/kubernetes-sigs/kueue/pull/10356#discussion_r3062831539), we have multiple instances of the same function.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates #10200

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-13T11:39:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1c8b2c8fb70843d473209b8781d2aeec0e92c70e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69df8d085ee3d800099fa3c2 |

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-13T11:52:54Z

/cc @olekzabl

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-16T07:49:58Z

LGTM label has been added.  <details>Git tree hash: 9c10892935bf060aced01e521e1d6e6100329ab0</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-16T07:50:00Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10424#pullrequestreview-4119124612" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10424#" title="Author self-approved">mszadkow</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mszadkow](https://github.com/mszadkow) — 2026-04-17T06:53:10Z

Making CP for that before continue with https://github.com/kubernetes-sigs/kueue/pull/10268

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-17T06:53:42Z

/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-17T06:54:20Z

@tenzen-y: #10424 failed to apply on top of branch "release-0.16":
```
Applying: Deduplicate e2e util functions
Using index info to reconstruct a base tree...
M	test/e2e/multikueue/e2e_test.go
M	test/e2e/multikueue/tas_test.go
A	test/e2e/multikueuedra/dra_test.go
A	test/e2e/multikueuedra/suite_test.go
A	test/e2e/multikueuesequential/e2e_test.go
M	test/util/util.go
Falling back to patching base and 3-way merge...
Auto-merging test/util/util.go
CONFLICT (content): Merge conflict in test/util/util.go
CONFLICT (modify/delete): test/e2e/multikueuedra/suite_test.go deleted in HEAD and modified in Deduplicate e2e util functions. Version Deduplicate e2e util functions of test/e2e/multikueuedra/suite_test.go left in tree.
CONFLICT (modify/delete): test/e2e/multikueuedra/dra_test.go deleted in HEAD and modified in Deduplicate e2e util functions. Version Deduplicate e2e util functions of test/e2e/multikueuedra/dra_test.go left in tree.
Auto-merging test/e2e/multikueue/tas_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue/tas_test.go
Auto-merging test/e2e/multikueue/e2e_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue/e2e_test.go
Auto-merging test/e2e/multikueue-sequential/e2e_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Deduplicate e2e util functions

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10424#issuecomment-4266021048):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-17T06:54:57Z

@tenzen-y: new pull request created: #10545

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10424#issuecomment-4266021048):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:01:38Z

- **test/util/util.go:1518** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:01:38Z
```diff
@@ -1493,3 +1493,46 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnWorkerClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	gomega.Eventually(func(g gomega.Gomega) {
+		for _, c := range clients {
+			ExpectObjectToBeDeleted(ctx, c, obj, false)
+		}
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
+	gomega.Eventually(func(g gomega.Gomega) {
+		createdWorkload := &kueue.Workload{}
+		g.Expect(client.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+		g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+			Type:    kueue.WorkloadAdmitted,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Admitted",
+			Message: "The workload is admitted",
+		}, IgnoreConditionTimestampsAndObservedGeneration))
+	}, MediumTimeout, Interval).Should(gomega.Succeed())
```

  We already have ExpectWorkloadsToBeAdmitted(). Can we reuse it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:11:29Z

- **test/util/util.go:1501** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:11:29Z
```diff
@@ -1493,3 +1493,46 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnWorkerClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	gomega.Eventually(func(g gomega.Gomega) {
+		for _, c := range clients {
+			ExpectObjectToBeDeleted(ctx, c, obj, false)
```

  We shouldn't use Eventually inside another Eventually.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:16:20Z

- **test/e2e/multikueue/e2e_test.go:1423** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:16:20Z
```diff
@@ -1420,20 +1420,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 
 			ginkgo.By("Waiting for the appwrapper to finish", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sManagerClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-
-					g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeComparableTo(&metav1.Condition{
-						Type:    kueue.WorkloadFinished,
-						Status:  metav1.ConditionTrue,
-						Reason:  kueue.WorkloadFinishedReasonSucceeded,
-						Message: "AppWrapper finished successfully",
-					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+					checkFinishStatusCondition(g, wlLookupKey, "AppWrapper finished successfully")
```

  Can we use ExpectWorkloadToFinish()?

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-14T06:19:09Z

- **test/util/util.go:1501** by [@mszadkow](https://github.com/mszadkow) — 2026-04-14T06:19:09Z
```diff
@@ -1493,3 +1493,46 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnWorkerClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	gomega.Eventually(func(g gomega.Gomega) {
+		for _, c := range clients {
+			ExpectObjectToBeDeleted(ctx, c, obj, false)
```

  yes, thanks!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:21:41Z

- **test/e2e/dra/suite_test.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:21:41Z
```diff
@@ -58,7 +56,7 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	waitForDRAExampleDriverAvailability(ctx, k8sClient)
+	util.WaitForDRAExampleDriverAvailability(ctx, k8sClient, "example-driver")
```

  ```suggestion
  	util.WaitForDRAExampleDriverAvailability(ctx, k8sClient, "kind")
  ```
  ?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:23:59Z

- **test/e2e/dra/suite_test.go:59** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:23:59Z
```diff
@@ -58,7 +56,7 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	waitForDRAExampleDriverAvailability(ctx, k8sClient)
+	util.WaitForDRAExampleDriverAvailability(ctx, k8sClient, "example-driver")
```

  Or actually, we might be able to use KIND_CLUSTER_NAME.

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-14T06:24:29Z

- **test/e2e/multikueue/e2e_test.go:1423** by [@mszadkow](https://github.com/mszadkow) — 2026-04-14T06:24:29Z
```diff
@@ -1420,20 +1420,13 @@ var _ = ginkgo.Describe("MultiKueue", func() {
 
 			ginkgo.By("Waiting for the appwrapper to finish", func() {
 				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sManagerClient.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
-
-					g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadFinished)).To(gomega.BeComparableTo(&metav1.Condition{
-						Type:    kueue.WorkloadFinished,
-						Status:  metav1.ConditionTrue,
-						Reason:  kueue.WorkloadFinishedReasonSucceeded,
-						Message: "AppWrapper finished successfully",
-					}, util.IgnoreConditionTimestampsAndObservedGeneration))
+					checkFinishStatusCondition(g, wlLookupKey, "AppWrapper finished successfully")
```

  I think so, they are pretty similar except for the reason message, but I don't think it's that important if the condition itself is Finished

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-14T06:24:58Z

- **test/util/util.go:1518** by [@mszadkow](https://github.com/mszadkow) — 2026-04-14T06:24:58Z
```diff
@@ -1493,3 +1493,46 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnWorkerClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	gomega.Eventually(func(g gomega.Gomega) {
+		for _, c := range clients {
+			ExpectObjectToBeDeleted(ctx, c, obj, false)
+		}
+	}, Timeout, Interval).Should(gomega.Succeed())
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
+	gomega.Eventually(func(g gomega.Gomega) {
+		createdWorkload := &kueue.Workload{}
+		g.Expect(client.Get(ctx, wlLookupKey, createdWorkload)).To(gomega.Succeed())
+		g.Expect(apimeta.FindStatusCondition(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)).To(gomega.BeComparableTo(&metav1.Condition{
+			Type:    kueue.WorkloadAdmitted,
+			Status:  metav1.ConditionTrue,
+			Reason:  "Admitted",
+			Message: "The workload is admitted",
+		}, IgnoreConditionTimestampsAndObservedGeneration))
+	}, MediumTimeout, Interval).Should(gomega.Succeed())
```

  Yes, thanks, there are so many of those helper one can get lost...

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T06:33:02Z

- **test/util/util.go:1497** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T06:33:02Z
```diff
@@ -1493,3 +1493,46 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnWorkerClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
```

  I’m not sure about this naming. Could we rename it to something like ExpectObjectDeletedOnClusters()?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-04-14T07:24:50Z

- **test/e2e/multikueue-sequential/e2e_test.go:286** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-14T07:17:32Z
```diff
@@ -284,8 +282,8 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 			})
 
 			ginkgo.By("Checking no objects are left in the worker clusters and the job is completed", func() {
-				expectObjectToBeDeletedOnWorkerClusters(ctx, createdLeaderWorkload)
-				expectObjectToBeDeletedOnWorkerClusters(ctx, job)
+				util.ExpectObjectToBeDeletedOnClusters(ctx, createdLeaderWorkload)
+				util.ExpectObjectToBeDeletedOnClusters(ctx, job)
```

  ```suggestion
  				util.ExpectObjectToBeDeletedOnClusters(ctx, createdLeaderWorkload, k8sManagerClient)
  				util.ExpectObjectToBeDeletedOnClusters(ctx, job, k8sManagerClient)
  ```
  
  Shouldn't we pass manager cluster client? Otherwise, we can not check anything.

- **test/util/util.go:1497** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-14T07:18:11Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
```

  Can you emit error when the number of clients are zero?

- **test/util/util.go:1521** by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-14T07:19:06Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
+	ExpectWorkloadsToBeAdmittedByKeys(ctx, client, wlLookupKey)
+	ExpectAdmissionCheckStateWithMessage(
+		ctx, client, wlLookupKey,
+		acName,
+		kueue.CheckStateReady,
+		fmt.Sprintf(`The workload got reservation on "%s"`, workerName),
+	)
+}
+
+func WaitForDRAExampleDriverAvailability(ctx context.Context, k8sClient client.Client, clusterName string) {
+	dsKey := types.NamespacedName{Namespace: "dra-example-driver", Name: "dra-example-driver-kubeletplugin"}
```

  ```suggestion
  	ginkgo.GinkgoHelper()
  	dsKey := types.NamespacedName{Namespace: "dra-example-driver", Name: "dra-example-driver-kubeletplugin"}
  ```
  
  I'm wondering if we should warm ginkgo by helper.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T07:41:26Z

- **test/util/util.go:1504** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T07:41:26Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
```

  ```suggestion
  func ExpectWorkloadAdmittedWithCheck(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
  ```
  Maybe like this?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T07:42:15Z

- **test/util/util.go:1506** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T07:42:15Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
```

  What does it mean "and manager"?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T07:42:33Z

- **test/util/util.go:1504** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T07:42:33Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
```

  ```suggestion
  func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, clusterName string, client client.Client) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-14T07:43:45Z

- **test/e2e/dra/suite_test.go:60** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-14T07:43:45Z
```diff
@@ -58,9 +57,11 @@ var _ = ginkgo.BeforeSuite(func() {
 
 	waitForAvailableStart := time.Now()
 	util.WaitForKueueAvailability(ctx, k8sClient)
-	waitForDRAExampleDriverAvailability(ctx, k8sClient)
+	clustername := cmp.Or(os.Getenv("KIND_CLUSTER_NAME"), "kind")
```

  ```suggestion
  	clusterName := cmp.Or(os.Getenv("KIND_CLUSTER_NAME"), "kind")
  ```
  nit

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-15T12:22:16Z

- **test/e2e/multikueue-sequential/e2e_test.go:286** by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:22:16Z
```diff
@@ -284,8 +282,8 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 			})
 
 			ginkgo.By("Checking no objects are left in the worker clusters and the job is completed", func() {
-				expectObjectToBeDeletedOnWorkerClusters(ctx, createdLeaderWorkload)
-				expectObjectToBeDeletedOnWorkerClusters(ctx, job)
+				util.ExpectObjectToBeDeletedOnClusters(ctx, createdLeaderWorkload)
+				util.ExpectObjectToBeDeletedOnClusters(ctx, job)
```

  yay, thanks, I don't know how this happened

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-15T12:22:33Z

- **test/util/util.go:1497** by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:22:32Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
```

  I think so yes, good guarding idea

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-15T12:24:57Z

- **test/util/util.go:1506** by [@mszadkow](https://github.com/mszadkow) — 2026-04-15T12:24:57Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
```

  because it must be admitted in both manager and worker clusters, but yes I think the word "clusters" is missing

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-15T12:34:09Z

- **test/util/util.go:1506** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-15T12:34:09Z
```diff
@@ -1493,3 +1493,35 @@ func waitForDummyWorkloadToRunOnNode(ctx context.Context, c client.Client, node
 		}, MediumTimeout, Interval).Should(gomega.Succeed())
 	})
 }
+
+func ExpectObjectToBeDeletedOnClusters[PtrT objAsPtr[T], T any](ctx context.Context, obj PtrT, clients ...client.Client) {
+	ginkgo.GinkgoHelper()
+	for _, c := range clients {
+		ExpectObjectToBeDeleted(ctx, c, obj, false)
+	}
+}
+
+func WaitForJobAdmitted(ctx context.Context, wlLookupKey types.NamespacedName, acName, workerName string, client client.Client) {
+	ginkgo.GinkgoHelper()
+	ginkgo.By(fmt.Sprintf("Waiting to be admitted in %s and manager", workerName))
```

  Not sure I got. This is helper and I see only one client.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-16T07:49:50Z

/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16
Thanks 👍
