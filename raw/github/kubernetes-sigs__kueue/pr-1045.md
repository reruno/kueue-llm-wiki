# PR #1045: Add two stage admission support.

**Summary**: Add two stage admission support.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1045

**Last updated**: 2023-10-25T17:44:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `twosetpadmission` → `main`
- **Created**: 2023-08-07T14:48:59Z
- **Updated**: 2023-10-25T17:44:41Z
- **Merged**: 2023-08-25T13:00:53Z
- **Closed**: 2023-08-25T13:00:53Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `tide/merge-method-squash`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 22   **+1163 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Add two stage admission support.  
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #993 

#### Special notes for your reviewer:
To fully cover the needs of #993 and #994 the following followups are needed:

1. Change any references to `Admitted` and `Admission` in the current code base to `QuotaReserved` and `QuotaReservation`. Add a new `Admitted` condition that represents   `QuotaReserved==true && HasAllChecksReady()`. This is kept separated to ease the review of the current PR.

2.  Change the inactive state of a queue based on its configured checks (similar implementation to the resorceflavors check )

3.  Add a new built-in admission check controller to manage the preemption. Should be in charge of issuing evictions and set its condition to ready once is done. 

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Add support for AdmissionChecks, a mechanism for internal or external components to influence whether a Workload can be admitted.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-07T14:49:01Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-07T14:52:45Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 48017f45a137823e020ca3b3fa210f210c365066 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64e76ba9ea675d0008c49152 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T14:25:48Z

/lgtm
/approve
/label tide/merge-method-squash

/hold
for: squash and KEP to merge

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T14:25:58Z

please squash now.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-24T14:26:55Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1045#issuecomment-1691788188" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1045#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T15:09:24Z

/lgtm

### Comment by [@trasc](https://github.com/trasc) — 2023-08-25T12:48:44Z

/unhold

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:33:30Z

/kind feature

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:44:39Z

/release-note-edit
```release-note
Add support for AdmissionChecks, a mechanism for internal or external components to influence whether a Workload can be admitted.
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-21T19:57:10Z

First pass. I didn't look at tests yet.
looks good overall.

- **apis/kueue/v1beta1/admissioncheck_types.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:13:28Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
+
+const (
+	// The check cannot pass at this moment, back off (possibly
+	// allowing other to try, unblock quota) and retry.
+	// A workload having at least one check in the state,
+	// will be evicted if admitted will not be considered
+	//for admission.
+	CheckStateRetry AdmissionCheckState = "Retry"
+
+	// The check will not pass in the near future. It is not worth
+	// to retry.
+	//NOTE: The admission behaviour is currently the same as for retry,
+	// we can consider marking the workload as "Finished" with a failure
+	// description.
+	CheckStateRejected AdmissionCheckState = "Rejected"
+
+	// The state can be
+	// 1. Unknown, the condition was added by kueue and its controller was not able to evaluate it.
+	// 2. Set by its controller and reevaluated after quota is reserved.
+	CheckStatePending AdmissionCheckState = "Pending"
+
+	// The check has passed.
+	// A workload having all its checks ready, and quota reserved can begin execution.
+	CheckStateReady AdmissionCheckState = "Ready"
+)
+
+// AdmissionCheckSpec defines the desired state of AdmissionCheck
+type AdmissionCheckSpec struct {
+	// ControllerName identifies the controller managing this condition.
+	// +optional
+	ControllerName string `json:"controllerName"`
+
+	// RetryDelayMinutes specifies the duration in minutes between tow checks for the same
```

  ```suggestion
  	// RetryDelayMinutes specifies the duration in minutes between two checks for the same
  ```

- **apis/kueue/v1beta1/workload_types.go:204** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:15:52Z
```diff
@@ -189,6 +195,10 @@ const (
 	// WorkloadEvictedByPodsReadyTimeout indicates that the eviction took
 	// place due to a PodsReady timeout.
 	WorkloadEvictedByPodsReadyTimeout = "PodsReadyTimeout"
+
+	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
+	// beacuse at least one admission check transitioned to false.
```

  ```suggestion
  	// beacuse at least one admission check transitioned to False.
  ```

- **pkg/controller/core/admissioncheck_controller.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:19:08Z
  There isn't anything to Reconcile for these objects, so let's just remove the code.

- **pkg/controller/core/workload_controller.go:144** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:23:53Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
```

  ```suggestion
  				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "admissionChecks", queueAdmissionChecks)
  ```

- **pkg/controller/core/workload_controller.go:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:28:02Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
+		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			if workload.HasRertyOrRejectedChecks(&wl) {
```

  ```suggestion
  			if workload.HasRetryOrRejectedChecks(&wl) {
  ```

- **pkg/controller/core/workload_controller.go:148** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:31:38Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
+		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			if workload.HasRertyOrRejectedChecks(&wl) {
+				log.V(3).Info("Workload is evicted due to admission checks", "localQueue")
+				workload.SetEvictedCondition(&wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+				err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
 		return r.reconcileNotReadyTimeout(ctx, req, &wl)
```

  just because this is in a function, let's put the check above also in a function.

- **pkg/controller/core/workload_controller.go:142** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:35:06Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
```

  these can be one function.

- **pkg/controller/core/workload_controller.go:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:36:08Z
```diff
@@ -154,9 +181,57 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context, name string) ([]string, error) {
+	queue := kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, types.NamespacedName{Name: name}, &queue); err != nil {
+		return nil, err
+	}
+	return queue.Spec.AdmissionChecks, nil
+}
+
+func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string) ([]metav1.Condition, bool) {
+	shouldUpdate := false
+	if len(queueChecks) == 0 {
+		if len(conds) > 0 {
+			conds = nil
+			shouldUpdate = true
+		}
```

  return here

- **apis/kueue/v1beta1/admissioncheck_types.go:23** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:37:28Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
```

  Because the Reason field is not typed, there is not much motivation to type these.

- **pkg/controller/core/workload_controller.go:232** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:42:53Z
```diff
@@ -154,9 +181,57 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context, name string) ([]string, error) {
+	queue := kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, types.NamespacedName{Name: name}, &queue); err != nil {
+		return nil, err
+	}
+	return queue.Spec.AdmissionChecks, nil
+}
+
+func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string) ([]metav1.Condition, bool) {
+	shouldUpdate := false
+	if len(queueChecks) == 0 {
+		if len(conds) > 0 {
+			conds = nil
+			shouldUpdate = true
+		}
+	} else {
+		currentChecks := slices.ToRefMap(conds, func(c *metav1.Condition) string { return c.Type })
+		for _, t := range queueChecks {
+			if _, found := currentChecks[t]; !found {
+				apimeta.SetStatusCondition(&conds, metav1.Condition{
+					Type:   t,
+					Status: metav1.ConditionUnknown,
+					Reason: string(kueue.CheckStatePending),
+				})
+				shouldUpdate = true
+			}
+		}
+
+		// if the workload conditions length is bigger then some cleanup should be done
+		if len(conds) > len(queueChecks) {
+			toRemove := make([]string, 0, len(conds)-len(queueChecks))
+			queueChecksSet := sets.New(queueChecks...)
+			for i := range conds {
+				c := &conds[i]
+				if !queueChecksSet.Has(c.Type) {
+					toRemove = append(toRemove, c.Type)
+				}
+			}
+			shouldUpdate = len(toRemove) > 0
+			for _, t := range toRemove {
+				apimeta.RemoveStatusCondition(&conds, t)
+			}
```

  A simpler and more performant implementation is to build a new slice of conditions: the conditions that we need to keep.

- **pkg/controller/core/workload_controller_test.go:183** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:52:22Z
```diff
@@ -176,3 +178,127 @@ func TestAdmittedNotReadyWorkload(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncCheckConditions(t *testing.T) {
+	testStartTime := metav1.NewTime(time.Now())
```

  ```suggestion
  	now := metav1.NewTime(time.Now())
  ```
  
  for consistency with other tests

- **test/integration/framework/framework.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T19:56:48Z
```diff
@@ -61,7 +61,7 @@ func (f *Framework) Init() *rest.Config {
 	ctrl.SetLogger(zap.New(
 		zap.WriteTo(ginkgo.GinkgoWriter),
 		zap.UseDevMode(true),
-		zap.Level(zapcore.Level(-3)),
+		zap.Level(zapcore.Level(-5)),
```

  revert before merge

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-21T20:00:45Z

/hold
I want to take a look at the renaming PR before merging this.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-08-22T13:11:43Z

- **apis/kueue/v1beta1/admissioncheck_types.go:30** by [@mwielgus](https://github.com/mwielgus) — 2023-08-22T13:11:42Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
+
+const (
+	// The check cannot pass at this moment, back off (possibly
+	// allowing other to try, unblock quota) and retry.
+	// A workload having at least one check in the state,
+	// will be evicted if admitted will not be considered
+	//for admission.
```

  ```suggestion
  	// for admission.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-22T13:56:07Z

- **apis/kueue/v1beta1/admissioncheck_types.go:23** by [@trasc](https://github.com/trasc) — 2023-08-22T12:44:13Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
```

  It was at some point in the KEP, @mwielgus  any preferencea on this?

- **apis/kueue/v1beta1/admissioncheck_types.go:56** by [@trasc](https://github.com/trasc) — 2023-08-22T12:44:59Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
+
+const (
+	// The check cannot pass at this moment, back off (possibly
+	// allowing other to try, unblock quota) and retry.
+	// A workload having at least one check in the state,
+	// will be evicted if admitted will not be considered
+	//for admission.
+	CheckStateRetry AdmissionCheckState = "Retry"
+
+	// The check will not pass in the near future. It is not worth
+	// to retry.
+	//NOTE: The admission behaviour is currently the same as for retry,
+	// we can consider marking the workload as "Finished" with a failure
+	// description.
+	CheckStateRejected AdmissionCheckState = "Rejected"
+
+	// The state can be
+	// 1. Unknown, the condition was added by kueue and its controller was not able to evaluate it.
+	// 2. Set by its controller and reevaluated after quota is reserved.
+	CheckStatePending AdmissionCheckState = "Pending"
+
+	// The check has passed.
+	// A workload having all its checks ready, and quota reserved can begin execution.
+	CheckStateReady AdmissionCheckState = "Ready"
+)
+
+// AdmissionCheckSpec defines the desired state of AdmissionCheck
+type AdmissionCheckSpec struct {
+	// ControllerName identifies the controller managing this condition.
+	// +optional
+	ControllerName string `json:"controllerName"`
+
+	// RetryDelayMinutes specifies the duration in minutes between tow checks for the same
```

  done

- **apis/kueue/v1beta1/workload_types.go:204** by [@trasc](https://github.com/trasc) — 2023-08-22T12:46:00Z
```diff
@@ -189,6 +195,10 @@ const (
 	// WorkloadEvictedByPodsReadyTimeout indicates that the eviction took
 	// place due to a PodsReady timeout.
 	WorkloadEvictedByPodsReadyTimeout = "PodsReadyTimeout"
+
+	// WorkloadEvictedByAdmissionCheck indicates that the workload was evicted
+	// beacuse at least one admission check transitioned to false.
```

  done

- **pkg/controller/core/admissioncheck_controller.go:1** by [@trasc](https://github.com/trasc) — 2023-08-22T12:47:46Z
  done

- **pkg/controller/core/workload_controller.go:142** by [@trasc](https://github.com/trasc) — 2023-08-22T12:58:35Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
```

  cqName, cqOk are reused afterwards
  
  done

- **pkg/controller/core/workload_controller.go:144** by [@trasc](https://github.com/trasc) — 2023-08-22T13:02:28Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
```

  the intent was to show that the list of checks are coming from the queue. 
  done

- **pkg/controller/core/workload_controller.go:175** by [@trasc](https://github.com/trasc) — 2023-08-22T13:03:51Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
+		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			if workload.HasRertyOrRejectedChecks(&wl) {
```

  done

- **pkg/controller/core/workload_controller.go:148** by [@trasc](https://github.com/trasc) — 2023-08-22T13:25:53Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
+		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
+			if workload.HasRertyOrRejectedChecks(&wl) {
+				log.V(3).Info("Workload is evicted due to admission checks", "localQueue")
+				workload.SetEvictedCondition(&wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
+				err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
 		return r.reconcileNotReadyTimeout(ctx, req, &wl)
```

  done

- **pkg/controller/core/workload_controller.go:220** by [@trasc](https://github.com/trasc) — 2023-08-22T13:31:13Z
```diff
@@ -154,9 +181,57 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context, name string) ([]string, error) {
+	queue := kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, types.NamespacedName{Name: name}, &queue); err != nil {
+		return nil, err
+	}
+	return queue.Spec.AdmissionChecks, nil
+}
+
+func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string) ([]metav1.Condition, bool) {
+	shouldUpdate := false
+	if len(queueChecks) == 0 {
+		if len(conds) > 0 {
+			conds = nil
+			shouldUpdate = true
+		}
```

  done

- **pkg/controller/core/workload_controller.go:232** by [@trasc](https://github.com/trasc) — 2023-08-22T13:35:16Z
```diff
@@ -154,9 +181,57 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
 		return ctrl.Result{}, client.IgnoreNotFound(err)
 	}
+
 	return ctrl.Result{}, nil
 }
 
+func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context, name string) ([]string, error) {
+	queue := kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, types.NamespacedName{Name: name}, &queue); err != nil {
+		return nil, err
+	}
+	return queue.Spec.AdmissionChecks, nil
+}
+
+func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string) ([]metav1.Condition, bool) {
+	shouldUpdate := false
+	if len(queueChecks) == 0 {
+		if len(conds) > 0 {
+			conds = nil
+			shouldUpdate = true
+		}
+	} else {
+		currentChecks := slices.ToRefMap(conds, func(c *metav1.Condition) string { return c.Type })
+		for _, t := range queueChecks {
+			if _, found := currentChecks[t]; !found {
+				apimeta.SetStatusCondition(&conds, metav1.Condition{
+					Type:   t,
+					Status: metav1.ConditionUnknown,
+					Reason: string(kueue.CheckStatePending),
+				})
+				shouldUpdate = true
+			}
+		}
+
+		// if the workload conditions length is bigger then some cleanup should be done
+		if len(conds) > len(queueChecks) {
+			toRemove := make([]string, 0, len(conds)-len(queueChecks))
+			queueChecksSet := sets.New(queueChecks...)
+			for i := range conds {
+				c := &conds[i]
+				if !queueChecksSet.Has(c.Type) {
+					toRemove = append(toRemove, c.Type)
+				}
+			}
+			shouldUpdate = len(toRemove) > 0
+			for _, t := range toRemove {
+				apimeta.RemoveStatusCondition(&conds, t)
+			}
```

  not more performant... however done

- **pkg/controller/core/workload_controller_test.go:183** by [@trasc](https://github.com/trasc) — 2023-08-22T13:37:45Z
```diff
@@ -176,3 +178,127 @@ func TestAdmittedNotReadyWorkload(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncCheckConditions(t *testing.T) {
+	testStartTime := metav1.NewTime(time.Now())
```

  done

- **test/integration/framework/framework.go:64** by [@trasc](https://github.com/trasc) — 2023-08-22T13:39:01Z
```diff
@@ -61,7 +61,7 @@ func (f *Framework) Init() *rest.Config {
 	ctrl.SetLogger(zap.New(
 		zap.WriteTo(ginkgo.GinkgoWriter),
 		zap.UseDevMode(true),
-		zap.Level(zapcore.Level(-3)),
+		zap.Level(zapcore.Level(-5)),
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-22T14:01:19Z

- **apis/kueue/v1beta1/admissioncheck_types.go:30** by [@trasc](https://github.com/trasc) — 2023-08-22T14:01:19Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
+
+const (
+	// The check cannot pass at this moment, back off (possibly
+	// allowing other to try, unblock quota) and retry.
+	// A workload having at least one check in the state,
+	// will be evicted if admitted will not be considered
+	//for admission.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-22T14:29:30Z

just minor things.
I'm reviewing the other PR next.

- **pkg/controller/core/workload_controller.go:139** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T14:11:00Z
```diff
@@ -136,27 +136,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
-		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
-		if err == nil {
-			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
-			if shouldUpdate {
-				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
-				wl.Status.AdmissionChecks = newChecks
-				err := r.client.Status().Update(ctx, &wl)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated {
```

  `updated || err != nil`
  Just in case `reconcileSyncAdmissionChecks` implementation changes in the future in a way that would result in an error even if updated is false.

- **pkg/controller/core/workload_controller.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T14:13:39Z
```diff
@@ -136,27 +136,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
-		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
-		if err == nil {
-			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
-			if shouldUpdate {
-				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
-				wl.Status.AdmissionChecks = newChecks
-				err := r.client.Status().Update(ctx, &wl)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated {
+			return ctrl.Result{}, err
 		}
 	}
 
 	if workload.IsAdmitted(&wl) {
-		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			if workload.HasRertyOrRejectedChecks(&wl) {
-				log.V(3).Info("Workload is evicted due to admission checks", "localQueue")
-				workload.SetEvictedCondition(&wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-				err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if evicted, err := r.reconcileCheckBasedEviction(ctx, &wl); evicted {
```

  same suggestion as above.

- **pkg/controller/core/workload_controller.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T14:19:33Z
```diff
@@ -193,41 +191,51 @@ func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context,
 	return queue.Spec.AdmissionChecks, nil
 }
 
+func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cqName string) (bool, error) {
+	// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+	if queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName); err == nil {
```

  unroll the function `getClusterQueueAdmissionChecks` here. It's too short to be worth it's own function.
  Also, if there is an error getting the checks, we should return so that we can retry.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-08-22T14:41:00Z

- **pkg/controller/core/workload_controller.go:144** by [@mwielgus](https://github.com/mwielgus) — 2023-08-22T14:40:59Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
```

  This is confusing here. How can a workload be both admitted and rejected at the same time?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-08-22T14:59:40Z

- **pkg/controller/core/workload_controller.go:638** by [@mwielgus](https://github.com/mwielgus) — 2023-08-22T14:59:39Z
```diff
@@ -531,3 +607,75 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 		}
 	}
 }
+
+type workloadCqHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*workloadCqHandler)(nil)
+
+// Create is called in response to an create event - e.g. Pod Creation.
```

  Why the comment mentions Pods (here and below)?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-08-22T15:04:19Z

- **pkg/workload/workload.go:419** by [@mwielgus](https://github.com/mwielgus) — 2023-08-22T15:04:18Z
```diff
@@ -393,3 +393,28 @@ func ReclaimablePodsAreEqual(a, b []kueue.ReclaimablePod) bool {
 	}
 	return true
 }
+
+// Returns true if all the checks of the workload are ready.
+func HasAllChecksReady(wl *kueue.Workload) bool {
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].Status != metav1.ConditionTrue {
+			return false
+		}
+	}
+	return true
+}
+
+// Returns true if any of the workloads checks are Retry or Rejected
+func HasRertyOrRejectedChecks(wl *kueue.Workload) bool {
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].Status == metav1.ConditionFalse {
+			return true
+		}
+	}
+	return false
+}
+
+// Returns true if the workload should can execution.
+func IsAdmittedAndChecked(w *kueue.Workload) bool {
+	return IsAdmitted(w) && HasAllChecksReady(w)
```

  Can we rename isAdmitted function to PreAdmitted or QuotaBooked?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-22T15:12:01Z

- **pkg/workload/workload.go:419** by [@trasc](https://github.com/trasc) — 2023-08-22T15:12:00Z
```diff
@@ -393,3 +393,28 @@ func ReclaimablePodsAreEqual(a, b []kueue.ReclaimablePod) bool {
 	}
 	return true
 }
+
+// Returns true if all the checks of the workload are ready.
+func HasAllChecksReady(wl *kueue.Workload) bool {
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].Status != metav1.ConditionTrue {
+			return false
+		}
+	}
+	return true
+}
+
+// Returns true if any of the workloads checks are Retry or Rejected
+func HasRertyOrRejectedChecks(wl *kueue.Workload) bool {
+	for i := range wl.Status.AdmissionChecks {
+		if wl.Status.AdmissionChecks[i].Status == metav1.ConditionFalse {
+			return true
+		}
+	}
+	return false
+}
+
+// Returns true if the workload should can execution.
+func IsAdmittedAndChecked(w *kueue.Workload) bool {
+	return IsAdmitted(w) && HasAllChecksReady(w)
```

  That is done in #1075

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-08-22T15:22:24Z

- **apis/kueue/v1beta1/admissioncheck_types.go:23** by [@mwielgus](https://github.com/mwielgus) — 2023-08-22T15:22:24Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
```

  I don't have a strong opinion on that. We can use regular strings.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-22T15:26:38Z

- **pkg/controller/core/workload_controller.go:139** by [@trasc](https://github.com/trasc) — 2023-08-22T15:14:30Z
```diff
@@ -136,27 +136,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
-		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
-		if err == nil {
-			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
-			if shouldUpdate {
-				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
-				wl.Status.AdmissionChecks = newChecks
-				err := r.client.Status().Update(ctx, &wl)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated {
```

  done

- **pkg/controller/core/workload_controller.go:144** by [@trasc](https://github.com/trasc) — 2023-08-22T15:16:52Z
```diff
@@ -129,7 +133,31 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 	if apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadFinished) {
 		return ctrl.Result{}, nil
 	}
+
+	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
+	if cqOk {
+		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
+		if err == nil {
+			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
+			if shouldUpdate {
+				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
+				wl.Status.AdmissionChecks = newChecks
+				err := r.client.Status().Update(ctx, &wl)
+				return ctrl.Result{}, client.IgnoreNotFound(err)
+			}
+		}
+	}
+
 	if workload.IsAdmitted(&wl) {
```

  In case it is admitted we might request an eviction , maybe make more sense with `evictionTriggered`.

- **pkg/controller/core/workload_controller.go:145** by [@trasc](https://github.com/trasc) — 2023-08-22T15:17:27Z
```diff
@@ -136,27 +136,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 
 	cqName, cqOk := r.queues.ClusterQueueForWorkload(&wl)
 	if cqOk {
-		// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
-		queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName)
-		if err == nil {
-			newChecks, shouldUpdate := syncAdmissionCheckConditions(wl.Status.AdmissionChecks, queueAdmissionChecks)
-			if shouldUpdate {
-				log.V(3).Info("The workload needs admission checks updates", "clusterQueue", klog.KRef("", cqName), "queueChecks", queueAdmissionChecks)
-				wl.Status.AdmissionChecks = newChecks
-				err := r.client.Status().Update(ctx, &wl)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if updated, err := r.reconcileSyncAdmissionChecks(ctx, &wl, cqName); updated {
+			return ctrl.Result{}, err
 		}
 	}
 
 	if workload.IsAdmitted(&wl) {
-		if !apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadEvicted) {
-			if workload.HasRertyOrRejectedChecks(&wl) {
-				log.V(3).Info("Workload is evicted due to admission checks", "localQueue")
-				workload.SetEvictedCondition(&wl, kueue.WorkloadEvictedByAdmissionCheck, "At least one admission check is false")
-				err := workload.ApplyAdmissionStatus(ctx, r.client, &wl, true)
-				return ctrl.Result{}, client.IgnoreNotFound(err)
-			}
+		if evicted, err := r.reconcileCheckBasedEviction(ctx, &wl); evicted {
```

  done

- **pkg/controller/core/workload_controller.go:196** by [@trasc](https://github.com/trasc) — 2023-08-22T15:21:21Z
```diff
@@ -193,41 +191,51 @@ func (r *WorkloadReconciler) getClusterQueueAdmissionChecks(ctx context.Context,
 	return queue.Spec.AdmissionChecks, nil
 }
 
+func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cqName string) (bool, error) {
+	// because we need to react to API cluster queue events, the list of checks from a cache can lead to race conditions
+	if queueAdmissionChecks, err := r.getClusterQueueAdmissionChecks(ctx, cqName); err == nil {
```

  done

- **pkg/controller/core/workload_controller.go:638** by [@trasc](https://github.com/trasc) — 2023-08-22T15:23:18Z
```diff
@@ -531,3 +607,75 @@ func (h *resourceUpdatesHandler) queueReconcileForPending(ctx context.Context, _
 		}
 	}
 }
+
+type workloadCqHandler struct {
+	client client.Client
+}
+
+var _ handler.EventHandler = (*workloadCqHandler)(nil)
+
+// Create is called in response to an create event - e.g. Pod Creation.
```

  `impl` behavior,  removed

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-22T15:34:31Z

- **apis/kueue/v1beta1/admissioncheck_types.go:23** by [@trasc](https://github.com/trasc) — 2023-08-22T15:34:31Z
```diff
@@ -0,0 +1,103 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+type AdmissionCheckState string
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-22T15:36:54Z

- **test/e2e/e2e_test.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T15:33:56Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
```

  Let's do this with SSA. I expect clients to want to use SSA. (It might require force:true)

- **test/e2e/e2e_test.go:320** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T15:34:47Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+					return false
+				}
+				return workload.IsAdmitted(createdWorkload) &&
+					apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+
+			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
+		})
+
+		ginkgo.It("Should suspend a job when its checks become invalid", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+
+			ginkgo.By("setting the check as successful", func() {
```

  This message seems wrong

- **test/e2e/e2e_test.go:283** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T15:35:00Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+					return false
+				}
+				return workload.IsAdmitted(createdWorkload) &&
+					apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+
+			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
+		})
+
+		ginkgo.It("Should suspend a job when its checks become invalid", func() {
```

  Let's move this test to integration

- **test/integration/controller/core/workload_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T15:36:52Z
  Some tests from the test plan in the KEP are missing. They can probably be done for the job controller, and some just for the workload controller.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-23T07:41:22Z

- **test/e2e/e2e_test.go:265** by [@trasc](https://github.com/trasc) — 2023-08-23T06:06:26Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
```

  done

- **test/e2e/e2e_test.go:320** by [@trasc](https://github.com/trasc) — 2023-08-23T06:06:36Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+					return false
+				}
+				return workload.IsAdmitted(createdWorkload) &&
+					apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+
+			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
+		})
+
+		ginkgo.It("Should suspend a job when its checks become invalid", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+
+			ginkgo.By("setting the check as successful", func() {
```

  done

- **test/integration/controller/core/workload_controller_test.go:1** by [@trasc](https://github.com/trasc) — 2023-08-23T06:49:34Z
  The workload tests are focusing on setting and syncing the admission conditions. 
  The job specific test are focused on getting a job in and out of suspended state taking into account quotaReservation and admission checks.
  
  Since the KEP is not yet final, and is still describing the cases in terms of  `Precheck`, I think we could revisit the tests once both this PR and the KEP one are merged.

- **test/e2e/e2e_test.go:283** by [@trasc](https://github.com/trasc) — 2023-08-23T07:25:39Z
```diff
@@ -180,6 +182,169 @@ var _ = ginkgo.Describe("Kueue", func() {
 			})
 		})
 	})
+	ginkgo.When("Creating a Job In a Twostepadmission Queue", func() {
+		var (
+			onDemandRF   *kueue.ResourceFlavor
+			localQueue   *kueue.LocalQueue
+			clusterQueue *kueue.ClusterQueue
+		)
+		ginkgo.BeforeEach(func() {
+			onDemandRF = testing.MakeResourceFlavor("on-demand").
+				Label("instance-type", "on-demand").Obj()
+			gomega.Expect(k8sClient.Create(ctx, onDemandRF)).Should(gomega.Succeed())
+			clusterQueue = testing.MakeClusterQueue("cluster-queue").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("on-demand").
+						Resource(corev1.ResourceCPU, "1").
+						Resource(corev1.ResourceMemory, "1Gi").
+						Obj(),
+				).
+				AdditionalChecks("check1").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("main", ns.Name).ClusterQueue("cluster-queue").Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteLocalQueue(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+			gomega.Expect(util.DeleteAllJobsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, onDemandRF, true)
+		})
+
+		ginkgo.It("Should unsuspend a job only after all checks are cleared", func() {
+			// Use a binary that ends.
+			sampleJob = (&testingjob.JobWrapper{Job: *sampleJob}).Image("gcr.io/k8s-staging-perf-tests/sleep:v0.0.3", []string{"5s"}).Obj()
+			gomega.Expect(k8sClient.Create(ctx, sampleJob)).Should(gomega.Succeed())
+
+			createdWorkload := &kueue.Workload{}
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobKey.Name), Namespace: ns.Name}
+
+			ginkgo.By("verify the check is added to the workload", func() {
+				gomega.Eventually(func() map[string]string {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return nil
+					}
+					return slices.ToMap(createdWorkload.Status.AdmissionChecks, func(i int) (string, string) { return createdWorkload.Status.AdmissionChecks[i].Type, "" })
+
+				}, util.LongTimeout, util.Interval).Should(gomega.BeComparableTo(map[string]string{"check1": ""}))
+			})
+
+			ginkgo.By("waiting for the workload to be assigned", func() {
+				gomega.Eventually(func() bool {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("checking the job remains suspended", func() {
+				createdJob := &batchv1.Job{}
+				jobKey := client.ObjectKeyFromObject(sampleJob)
+				gomega.Consistently(func() bool {
+					if err := k8sClient.Get(ctx, jobKey, createdJob); err != nil {
+						return false
+					}
+					return ptr.Deref(createdJob.Spec.Suspend, false)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+			})
+
+			ginkgo.By("setting the check as successful", func() {
+				gomega.Eventually(func() error {
+					if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdWorkload.Status.AdmissionChecks, metav1.Condition{
+						Type:   "check1",
+						Status: metav1.ConditionTrue,
+						Reason: "ByTest",
+					})
+					return k8sClient.Status().Update(ctx, createdWorkload)
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			expectJobUnsuspendedWithNodeSelectors(jobKey, map[string]string{
+				"instance-type": "on-demand",
+			})
+			gomega.Eventually(func() bool {
+				if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+					return false
+				}
+				return workload.IsAdmitted(createdWorkload) &&
+					apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+
+			}, util.LongTimeout, util.Interval).Should(gomega.BeTrue())
+		})
+
+		ginkgo.It("Should suspend a job when its checks become invalid", func() {
```

  I'd prefer to keep it here, the job integration test don't run the workload controller.
