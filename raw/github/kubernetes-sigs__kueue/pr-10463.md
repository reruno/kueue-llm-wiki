# PR #10463: Trigger failure recovery reconciliations for unreachable nodes

**Summary**: Trigger failure recovery reconciliations for unreachable nodes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10463

**Last updated**: 2026-04-15T15:58:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kshalot](https://github.com/kshalot)
- **Branch**: `fix/pod-termination-controller-missed-updates` → `main`
- **Created**: 2026-04-14T13:32:04Z
- **Updated**: 2026-04-15T15:58:24Z
- **Merged**: 2026-04-15T15:57:03Z
- **Closed**: 2026-04-15T15:57:03Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 3   **+204 / -31**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
If the node is not tainted at the moment the timeout elapses, the pod won't be forcefully terminated and the reconcile will never be queued again, leading to the pod being stuck.

#### Which issue(s) this PR fixes:
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
FailureRecoveryPolicy: Fixed an issue where pods could remain stuck terminating if their node became unreachable only after the force-termination timeout had already elapsed.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-14T13:32:08Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-14T13:32:40Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 119af71341d70fd95861480ac7542fe20df6c653 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69dfa5d2dee46f0009a17852 |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:41:11Z

/release-note-edit
```release-note
FailureRecoveryPolicy: Fixed an issue where pods could remain stuck terminating if their node became unreachable only after the force-termination timeout had already elapsed.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-15T14:55:41Z

Thanks 👍 
/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-15T14:55:44Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10463#issuecomment-4253115162):

>Thanks 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-15T14:55:49Z

LGTM label has been added.  <details>Git tree hash: 09c38b86f1d838a91c3a5be1e285ae4879701a6f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-15T14:55:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10463#" title="Author self-approved">kshalot</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10463#issuecomment-4253115162" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@kshalot](https://github.com/kshalot) — 2026-04-15T15:02:09Z

@mimowo This also warrants a KEP update, I'll post a PR later today.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-15T15:57:46Z

@mimowo: new pull request created: #10500

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10463#issuecomment-4253115162):

>Thanks 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-15T15:58:24Z

@mimowo: new pull request created: #10501

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10463#issuecomment-4253115162):

>Thanks 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:42:50Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:253** by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:42:50Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
+		},
+		UpdateFunc: func(e event.TypedUpdateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.ObjectNew.Spec.Taints, corev1.TaintNodeUnreachable)
```

  Here we only need to trigger the reconcile when the Node is actually transitioning to Unreachable, right?
  So `utiltaints.TaintKeyExists(e.ObjectOld.Spec.Taints, corev1.TaintNodeUnreachable) &&`, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:44:17Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:75** by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:44:18Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
```

  ```suggestion
  	ginkgo.It("should forcefufully delete a pod once the hosting node becomes unreachable", func() {
  ```
  to make it more intent-oriented, rather than technical

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:50:56Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:105** by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:50:56Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
+		var pod *corev1.Pod
+		ginkgo.By("creating the pod on a reachable node", func() {
+			pod = matchingPodWrapper.Clone().NodeName(reachableNodeName).Obj()
+			util.MustCreate(ctx, k8sClient, pod)
+		})
+		ginkgo.By("marking the pod for deletion and verifying that it's not forcefully terminated", func() {
+			gomega.Expect(k8sClient.Delete(ctx, pod)).To(gomega.Succeed())
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).To(gomega.Succeed())
+			}, forcefulTerminationCheckTimeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("tainting the previously reachable node as unreachable", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = append(node.Spec.Taints, corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule})
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
+		ginkgo.By("verifying that the pod is forcefully terminated after the timeout elapses", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).
+					To(utiltesting.BeNotFoundError())
+			}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
+		})
+		ginkgo.By("removing the unreachable taint from the node", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = nil
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
```

  Actually, instead of using BeforeAll and reverting the nodes I would prefer to create those which are relevant per scenario, and then just delete. This makes the tests easier to run in parallel, which is gain for our runtime. 
  
  Since this is pre-existing I'm fine as is and refactor later, at the same time it does not seem like much work yet, so I'm considering it could go in this PR. Let me know what you think.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:51:50Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:98** by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:51:50Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
+		var pod *corev1.Pod
+		ginkgo.By("creating the pod on a reachable node", func() {
+			pod = matchingPodWrapper.Clone().NodeName(reachableNodeName).Obj()
+			util.MustCreate(ctx, k8sClient, pod)
+		})
+		ginkgo.By("marking the pod for deletion and verifying that it's not forcefully terminated", func() {
+			gomega.Expect(k8sClient.Delete(ctx, pod)).To(gomega.Succeed())
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).To(gomega.Succeed())
+			}, forcefulTerminationCheckTimeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("tainting the previously reachable node as unreachable", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = append(node.Spec.Taints, corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule})
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
+		ginkgo.By("verifying that the pod is forcefully terminated after the timeout elapses", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).
+					To(utiltesting.BeNotFoundError())
+			}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
```

  iiuc this timeout is not relevant here, we should just check using util.Timeout. It was relevant for the Consistency only - waited above.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:55:04Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:225** by [@mimowo](https://github.com/mimowo) — 2026-04-14T15:55:04Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
```

  Either move under the List for error handling, or use to log the number of Pods eligible for termination. No strong view, but I would probably log the number of pods / requests as V4

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T15:55:27Z

LGTM overall, thanks 👍

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-14T15:58:26Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:253** by [@kshalot](https://github.com/kshalot) — 2026-04-14T15:58:26Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
+		},
+		UpdateFunc: func(e event.TypedUpdateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.ObjectNew.Spec.Taints, corev1.TaintNodeUnreachable)
```

  This is what I wrote initially, but then I thought that it's the same case as this check which we decided we want to get rid of:
  ```go
  	// Pod was not marked for deletion in the update
  	if !u.ObjectOld.DeletionTimestamp.IsZero() || u.ObjectNew.DeletionTimestamp.IsZero() {
  		return false
  	}
  ```
  I was on the fence about adding it, so since you are mentioning it I'll add it back for now. I'll revert if we decide otherwise.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T16:10:59Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:253** by [@mimowo](https://github.com/mimowo) — 2026-04-14T16:10:58Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
+		},
+		UpdateFunc: func(e event.TypedUpdateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.ObjectNew.Spec.Taints, corev1.TaintNodeUnreachable)
```

  I see, my main issue with the snippet above is readability. I wouldn't mind `!podEligible(oldPod) && podEligible(newPod)` this makes it very clear we are checking the transition.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-14T16:11:34Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:98** by [@kshalot](https://github.com/kshalot) — 2026-04-14T16:11:34Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
+		var pod *corev1.Pod
+		ginkgo.By("creating the pod on a reachable node", func() {
+			pod = matchingPodWrapper.Clone().NodeName(reachableNodeName).Obj()
+			util.MustCreate(ctx, k8sClient, pod)
+		})
+		ginkgo.By("marking the pod for deletion and verifying that it's not forcefully terminated", func() {
+			gomega.Expect(k8sClient.Delete(ctx, pod)).To(gomega.Succeed())
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).To(gomega.Succeed())
+			}, forcefulTerminationCheckTimeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("tainting the previously reachable node as unreachable", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = append(node.Spec.Taints, corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule})
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
+		ginkgo.By("verifying that the pod is forcefully terminated after the timeout elapses", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).
+					To(utiltesting.BeNotFoundError())
+			}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
```

  We can reduce the timeout here, yes.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-14T16:15:34Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:225** by [@kshalot](https://github.com/kshalot) — 2026-04-14T16:15:34Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
```

  I added the log. It's always logged atm, but I'm wondering whether we want to chech len() > 0

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-14T16:36:55Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:105** by [@kshalot](https://github.com/kshalot) — 2026-04-14T16:36:55Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
+		var pod *corev1.Pod
+		ginkgo.By("creating the pod on a reachable node", func() {
+			pod = matchingPodWrapper.Clone().NodeName(reachableNodeName).Obj()
+			util.MustCreate(ctx, k8sClient, pod)
+		})
+		ginkgo.By("marking the pod for deletion and verifying that it's not forcefully terminated", func() {
+			gomega.Expect(k8sClient.Delete(ctx, pod)).To(gomega.Succeed())
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).To(gomega.Succeed())
+			}, forcefulTerminationCheckTimeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("tainting the previously reachable node as unreachable", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = append(node.Spec.Taints, corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule})
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
+		ginkgo.By("verifying that the pod is forcefully terminated after the timeout elapses", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).
+					To(utiltesting.BeNotFoundError())
+			}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
+		})
+		ginkgo.By("removing the unreachable taint from the node", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = nil
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
```

  I see I see. I can add the change in this PR later today, but if it happens to get merged in the meantime I'll make it a followup.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T16:40:47Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:105** by [@mimowo](https://github.com/mimowo) — 2026-04-14T16:40:47Z
```diff
@@ -72,6 +72,39 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 		}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
 	})
 
+	ginkgo.It("triggers reconciliation when the node becomes unreachable", func() {
+		var pod *corev1.Pod
+		ginkgo.By("creating the pod on a reachable node", func() {
+			pod = matchingPodWrapper.Clone().NodeName(reachableNodeName).Obj()
+			util.MustCreate(ctx, k8sClient, pod)
+		})
+		ginkgo.By("marking the pod for deletion and verifying that it's not forcefully terminated", func() {
+			gomega.Expect(k8sClient.Delete(ctx, pod)).To(gomega.Succeed())
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).To(gomega.Succeed())
+			}, forcefulTerminationCheckTimeout, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("tainting the previously reachable node as unreachable", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = append(node.Spec.Taints, corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule})
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
+		ginkgo.By("verifying that the pod is forcefully terminated after the timeout elapses", func() {
+			gomega.Eventually(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: pod.Name, Namespace: pod.Namespace}, pod)).
+					To(utiltesting.BeNotFoundError())
+			}, forcefulTerminationCheckTimeout, util.Interval).Should(gomega.Succeed())
+		})
+		ginkgo.By("removing the unreachable taint from the node", func() {
+			node := &corev1.Node{}
+			gomega.Expect(k8sClient.Get(ctx, types.NamespacedName{Name: reachableNodeName}, node)).To(gomega.Succeed())
+			node.Spec.Taints = nil
+			gomega.Expect(k8sClient.Update(ctx, node)).To(gomega.Succeed())
+		})
```

  Nah, I will merge tomorrow anyway

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T16:41:11Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:225** by [@mimowo](https://github.com/mimowo) — 2026-04-14T16:41:11Z
```diff
@@ -228,6 +221,46 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
```

  Yes, we can check len() > 0

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-14T16:50:19Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:255** by [@mimowo](https://github.com/mimowo) — 2026-04-14T16:50:19Z
```diff
@@ -228,6 +221,50 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+
+	log.V(4).Info("Reconciling pods affected by unreachable node", "pod_count", len(requests))
+
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
```

  I think the code here is correct, but not covered with a test and so it could be overlooked one day as not relevant, as seemingly this scenario is impossible, but in fact it may happen if Kueue is restarted in the meanwhile. I'm wondering we it would be good to add an integration tests, where we restart Kueue manager during the test, I think this could be just a slight modification of the new test case.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-15T09:02:56Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:255** by [@kshalot](https://github.com/kshalot) — 2026-04-15T09:02:56Z
```diff
@@ -228,6 +221,50 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+
+	log.V(4).Info("Reconciling pods affected by unreachable node", "pod_count", len(requests))
+
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
```

  I agree with you, I'll see how to test it without limiting our ability to run the test in parallel. I think we can just label it with `ginkgo.Serial`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-15T09:03:19Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:53** by [@mimowo](https://github.com/mimowo) — 2026-04-15T09:03:19Z
```diff
@@ -43,26 +41,24 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 
 	ginkgo.BeforeAll(func() {
 		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
-		nodes := []corev1.Node{
-			*testingnode.MakeNode(unreachableNodeName).
-				NotReady().
-				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
-				Obj(),
-			*testingnode.MakeNode(reachableNodeName).
-				Ready().
-				Obj(),
-		}
-		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
 
 		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
 			StatusPhase(corev1.PodPending).
 			TerminationGracePeriod(1).
-			NodeName(unreachableNodeName).
 			Annotation(constants.SafeToForcefullyDeleteAnnotationKey, constants.SafeToForcefullyDeleteAnnotationValue)
 	})
 
-	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
-		matchingPod := matchingPodWrapper.Clone().Obj()
+	ginkgo.It("should forcefully terminate pods that opt-in, scheduled on unreachable nodes", func() {
+		unreachableNode := testingnode.MakeNode("unreachable-node").
```

  Can you also add the ns suffix, and remove the ` ginkgo.Ordered,` from the describe?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-15T09:18:35Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:255** by [@mimowo](https://github.com/mimowo) — 2026-04-15T09:18:35Z
```diff
@@ -228,6 +221,50 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+
+	log.V(4).Info("Reconciling pods affected by unreachable node", "pod_count", len(requests))
+
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
```

  Ah good point, I'm not sure about running such a test in parallel, requires a check, but maybe not needed, because for integration tests IIRC each set of tests will run on a dedicated API server, and dedicated Kueue manager.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-15T09:20:33Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:53** by [@mimowo](https://github.com/mimowo) — 2026-04-15T09:20:33Z
```diff
@@ -43,26 +41,24 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 
 	ginkgo.BeforeAll(func() {
 		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
-		nodes := []corev1.Node{
-			*testingnode.MakeNode(unreachableNodeName).
-				NotReady().
-				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
-				Obj(),
-			*testingnode.MakeNode(reachableNodeName).
-				Ready().
-				Obj(),
-		}
-		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
 
 		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
 			StatusPhase(corev1.PodPending).
 			TerminationGracePeriod(1).
-			NodeName(unreachableNodeName).
 			Annotation(constants.SafeToForcefullyDeleteAnnotationKey, constants.SafeToForcefullyDeleteAnnotationValue)
 	})
 
-	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
-		matchingPod := matchingPodWrapper.Clone().Obj()
+	ginkgo.It("should forcefully terminate pods that opt-in, scheduled on unreachable nodes", func() {
+		unreachableNode := testingnode.MakeNode("unreachable-node").
```

  Ah, this is related to https://github.com/kubernetes-sigs/kueue/pull/10463#discussion_r3085272581, and what I suggested may actually not be needed to run the tests in parallel, because iirc the tests will anyway have their own API server and Kueue manager, but this is worth double-checking.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-15T10:21:27Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:53** by [@kshalot](https://github.com/kshalot) — 2026-04-15T10:21:27Z
```diff
@@ -43,26 +41,24 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 
 	ginkgo.BeforeAll(func() {
 		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
-		nodes := []corev1.Node{
-			*testingnode.MakeNode(unreachableNodeName).
-				NotReady().
-				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
-				Obj(),
-			*testingnode.MakeNode(reachableNodeName).
-				Ready().
-				Obj(),
-		}
-		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
 
 		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
 			StatusPhase(corev1.PodPending).
 			TerminationGracePeriod(1).
-			NodeName(unreachableNodeName).
 			Annotation(constants.SafeToForcefullyDeleteAnnotationKey, constants.SafeToForcefullyDeleteAnnotationValue)
 	})
 
-	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
-		matchingPod := matchingPodWrapper.Clone().Obj()
+	ginkgo.It("should forcefully terminate pods that opt-in, scheduled on unreachable nodes", func() {
+		unreachableNode := testingnode.MakeNode("unreachable-node").
```

  Good point. I already made the change, but I'll double check and revert if it's not needed, no point in keeping it.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-15T11:07:10Z

- **test/integration/singlecluster/controller/failurerecovery/pod_termination_controller_test.go:53** by [@kshalot](https://github.com/kshalot) — 2026-04-15T11:07:10Z
```diff
@@ -43,26 +41,24 @@ var _ = ginkgo.Describe("Pod termination controller", ginkgo.Ordered, ginkgo.Con
 
 	ginkgo.BeforeAll(func() {
 		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "pod-fr-namespace-")
-		nodes := []corev1.Node{
-			*testingnode.MakeNode(unreachableNodeName).
-				NotReady().
-				Taints(corev1.Taint{Key: corev1.TaintNodeUnreachable, Effect: corev1.TaintEffectNoSchedule}).
-				Obj(),
-			*testingnode.MakeNode(reachableNodeName).
-				Ready().
-				Obj(),
-		}
-		util.CreateNodesWithStatus(ctx, k8sClient, nodes)
 
 		matchingPodWrapper = testingpod.MakePod("matching-pod", ns.Name).
 			StatusPhase(corev1.PodPending).
 			TerminationGracePeriod(1).
-			NodeName(unreachableNodeName).
 			Annotation(constants.SafeToForcefullyDeleteAnnotationKey, constants.SafeToForcefullyDeleteAnnotationValue)
 	})
 
-	ginkgo.It("forcefully terminates pods that opt-in, scheduled on unreachable nodes", func() {
-		matchingPod := matchingPodWrapper.Clone().Obj()
+	ginkgo.It("should forcefully terminate pods that opt-in, scheduled on unreachable nodes", func() {
+		unreachableNode := testingnode.MakeNode("unreachable-node").
```

  Yeah it should be isolated anyway. I removed the node namespacing to reduce noise.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-15T12:14:21Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:255** by [@kshalot](https://github.com/kshalot) — 2026-04-15T12:14:20Z
```diff
@@ -228,6 +221,50 @@ func isPodPartiallyForcefullyTerminated(p *corev1.Pod) bool {
 	})
 }
 
+func (r *TerminatingPodReconciler) mapNodeToPods(ctx context.Context, node *corev1.Node) []ctrl.Request {
+	log := log.FromContext(ctx)
+
+	pods := &corev1.PodList{}
+	if err := r.client.List(ctx, pods, client.MatchingFields{indexer.PodNodeNameKey: node.Name}); err != nil {
+		log.Error(err, "Failed to list pods for node", "node", klog.KObj(node))
+		return nil
+	}
+
+	var requests []ctrl.Request
+	for _, pod := range pods.Items {
+		if podEligibleForTermination(&pod) {
+			requests = append(requests, ctrl.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: pod.Namespace,
+					Name:      pod.Name,
+				},
+			})
+		}
+	}
+
+	log.V(4).Info("Reconciling pods affected by unreachable node", "pod_count", len(requests))
+
+	return requests
+}
+
+func (r *TerminatingPodReconciler) nodeEventsPredicate() predicate.TypedPredicate[*corev1.Node] {
+	return predicate.TypedFuncs[*corev1.Node]{
+		CreateFunc: func(e event.TypedCreateEvent[*corev1.Node]) bool {
+			return utiltaints.TaintKeyExists(e.Object.Spec.Taints, corev1.TaintNodeUnreachable)
```

  https://github.com/kubernetes-sigs/kueue/pull/10463/commits/2774c2b5ab274e2b03728ce1296101e19364ae8f

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-15T14:37:36Z

- **pkg/controller/failurerecovery/pod_termination_controller.go:128** by [@mimowo](https://github.com/mimowo) — 2026-04-15T14:37:37Z
```diff
@@ -123,16 +125,7 @@ func (r *TerminatingPodReconciler) Create(e event.TypedCreateEvent[*corev1.Pod])
 }
 
 func (r *TerminatingPodReconciler) Update(u event.TypedUpdateEvent[*corev1.Pod]) bool {
-	if !podEligibleForTermination(u.ObjectNew) {
-		return false
-	}
-
-	// Pod was not marked for deletion in the update
-	if !u.ObjectOld.DeletionTimestamp.IsZero() || u.ObjectNew.DeletionTimestamp.IsZero() {
-		return false
-	}
-
-	return true
+	return podEligibleForTermination(u.ObjectNew)
```

  Should we actually check for the transition  ` podEligibleForTermination(u.ObjectNew) && !podEligibleForTermination(u.ObjectOld)`, wdyt? This seems natural and I cannot see any counter example.
