# PR #10485: Pending workload resource metric

**Summary**: Pending workload resource metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10485

**Last updated**: 2026-04-22T05:32:20Z

---

## Metadata

- **State**: open
- **Author**: [@amy](https://github.com/amy)
- **Branch**: `pending` → `main`
- **Created**: 2026-04-15T03:34:43Z
- **Updated**: 2026-04-22T05:32:20Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 11   **+336 / -6**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Similar to kueue_cluster_queue_resource_reservation, but I want to track the resources pending in the CQ. Today we just count the number of pending workloads. Call it kueue_cluster_queue_resource_pending.

Helps with instrumenting Kueue to alert when something is broken. You can take the diff of Pending - Unallocated Nominal Quota to roughly gauge whether there's a scheduling issue and investigation needs to happen. (ex: fragmentation, scheduling gates not initiated by Kueue, bugs, etc.) If there's enough unallocated nominal quota to admit the pending workloads, but you see that diff for a prolonged period of time (ie not a spike shape), there's potentially an issue.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #9741

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
New kueue_cluster_queue_resource_pending metric to show total resources pending.
```

---
> AI Assistance Disclosure: This pull request was created with the assistance of AI

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-15T03:34:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 75b2f29a3d319bb994555e0b2a11ab7d04aa5fc6 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e7d0655e84c700082c6530 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-15T03:34:53Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10485#" title="Author self-approved">amy</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mimowo](https://github.com/mimowo) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mimowo"]} -->

### Comment by [@amy](https://github.com/amy) — 2026-04-17T03:25:57Z

/test pull-kueue-test-integration-baseline-main

### Comment by [@amy](https://github.com/amy) — 2026-04-20T00:50:54Z

/test pull-kueue-test-e2e-main-1-33

### Comment by [@amy](https://github.com/amy) — 2026-04-20T16:50:53Z

cc/ @mbobrovskyi could you take a review pass? 🙏

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-22T04:37:48Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-22T04:37:55Z

LGTM label has been added.  <details>Git tree hash: 9c63183f88619a9a8032b82f6e7fd7bf74d07c27</details>

### Comment by [@amy](https://github.com/amy) — 2026-04-22T05:32:20Z

/assign @mimowo

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T07:11:12Z

- **pkg/cache/queue/cluster_queue.go:151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T07:11:12Z
```diff
@@ -136,6 +137,19 @@ type ClusterQueue struct {
 	localQueuesInClusterQueue map[utilqueue.LocalQueueReference]bool
 
 	sw *stickyWorkload
+
+	// pendingResourcesTotal is the incremental sum of TotalRequests across workloads
+	// in heap and inadmissibleWorkloads (not inflight). Updated at each mutation site so
+	// PendingResources() is O(1) rather than O(N).
+	// Configured resources are seeded at 0 by Update() so they appear in metrics
+	// even when no workloads are pending; stale zero entries are pruned on Update().
+	pendingResourcesTotal map[corev1.ResourceName]int64
+
+	// inflightResources is a snapshot of the current inflight workload's TotalRequests,
+	// taken at Pop time under the exclusive lock before the scheduler receives the pointer.
+	// Avoids a data race with flavorassigner.assignFlavors, which writes to the same maps.
+	// Nil when no workload is inflight.
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T07:29:08Z

- **pkg/cache/queue/cluster_queue.go:151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T07:29:08Z
```diff
@@ -136,6 +137,19 @@ type ClusterQueue struct {
 	localQueuesInClusterQueue map[utilqueue.LocalQueueReference]bool
 
 	sw *stickyWorkload
+
+	// pendingResourcesTotal is the incremental sum of TotalRequests across workloads
+	// in heap and inadmissibleWorkloads (not inflight). Updated at each mutation site so
+	// PendingResources() is O(1) rather than O(N).
+	// Configured resources are seeded at 0 by Update() so they appear in metrics
+	// even when no workloads are pending; stale zero entries are pruned on Update().
+	pendingResourcesTotal map[corev1.ResourceName]int64
+
+	// inflightResources is a snapshot of the current inflight workload's TotalRequests,
+	// taken at Pop time under the exclusive lock before the scheduler receives the pointer.
+	// Avoids a data race with flavorassigner.assignFlavors, which writes to the same maps.
+	// Nil when no workload is inflight.
```

  Or should we remove the initialization of this map in newClusterQueueImpl()?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T07:41:08Z

- **pkg/cache/queue/cluster_queue.go:151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T07:41:08Z
```diff
@@ -136,6 +137,19 @@ type ClusterQueue struct {
 	localQueuesInClusterQueue map[utilqueue.LocalQueueReference]bool
 
 	sw *stickyWorkload
+
+	// pendingResourcesTotal is the incremental sum of TotalRequests across workloads
+	// in heap and inadmissibleWorkloads (not inflight). Updated at each mutation site so
+	// PendingResources() is O(1) rather than O(N).
+	// Configured resources are seeded at 0 by Update() so they appear in metrics
+	// even when no workloads are pending; stale zero entries are pruned on Update().
+	pendingResourcesTotal map[corev1.ResourceName]int64
+
+	// inflightResources is a snapshot of the current inflight workload's TotalRequests,
+	// taken at Pop time under the exclusive lock before the scheduler receives the pointer.
+	// Avoids a data race with flavorassigner.assignFlavors, which writes to the same maps.
+	// Nil when no workload is inflight.
```

  I'm actually can't see where we checking for nil.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T07:47:59Z

- **pkg/cache/queue/metrics.go:63** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T07:48:00Z
```diff
@@ -48,7 +50,21 @@ func reportCQPendingWorkloads(m *Manager, cq *ClusterQueue) {
 		inadmissible += active
 		active = 0
 	}
-	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, m.customLabels.CQGet(cq.name), m.roleTracker)
+	cqCustomLabels := m.customLabels.CQGet(cq.name)
+	metrics.ReportPendingWorkloads(cq.name, active, inadmissible, cqCustomLabels, m.roleTracker)
+
+	var cohort kueue.CohortReference
+	if cq.HasParent() {
+		cohort = cq.Parent().GetName()
+	}
+	// pendingResourcesTotal carries 0 entries for configured resources (seeded by
+	// Update), so iterating it once covers both the zero-series and actual pending.
+	pendingResources := cq.PendingResources()
+	metrics.ClearClusterQueueResourcePendingMetrics(string(cq.name))
```

  Actually, I’m not sure we need to clear it here. I think we should clear it if the cohort or CQ is deleted, right?

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-21T17:18:38Z

- **pkg/cache/queue/cluster_queue.go:151** by [@amy](https://github.com/amy) — 2026-04-21T17:18:38Z
```diff
@@ -136,6 +137,19 @@ type ClusterQueue struct {
 	localQueuesInClusterQueue map[utilqueue.LocalQueueReference]bool
 
 	sw *stickyWorkload
+
+	// pendingResourcesTotal is the incremental sum of TotalRequests across workloads
+	// in heap and inadmissibleWorkloads (not inflight). Updated at each mutation site so
+	// PendingResources() is O(1) rather than O(N).
+	// Configured resources are seeded at 0 by Update() so they appear in metrics
+	// even when no workloads are pending; stale zero entries are pruned on Update().
+	pendingResourcesTotal map[corev1.ResourceName]int64
+
+	// inflightResources is a snapshot of the current inflight workload's TotalRequests,
+	// taken at Pop time under the exclusive lock before the scheduler receives the pointer.
+	// Avoids a data race with flavorassigner.assignFlavors, which writes to the same maps.
+	// Nil when no workload is inflight.
```

  Removed the initialization of inflightResources. Keeping the Nil comment because that reflects current inflightresources behavior
  
  InflightResources is used here: https://github.com/kubernetes-sigs/kueue/pull/10485/changes#diff-04ff27d9f9b97758172eb0674ca8040fe5ec7c2c3c427e68aadc679b075e2697R564
  So if nil, the resources aren't counted.
