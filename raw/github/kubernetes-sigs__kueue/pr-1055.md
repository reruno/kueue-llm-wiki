# PR #1055: remove pointer package and use k8s pointer

**Summary**: remove pointer package and use k8s pointer

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1055

**Last updated**: 2023-08-11T20:43:23Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@kannon92](https://github.com/kannon92)
- **Branch**: `remove-pointer-package` → `main`
- **Created**: 2023-08-11T13:02:36Z
- **Updated**: 2023-08-11T20:43:23Z
- **Merged**: 2023-08-11T20:43:22Z
- **Closed**: 2023-08-11T20:43:22Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 44   **+336 / -368**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Remove the kueue pointer package.
Quantity was used in that package so I removed the pointer alias and renamed the package to quantity.  

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1051 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-11T13:02:42Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 3a7d5aeb37fa1675410302e177800cef68a0e8bd |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64d697c0070aa10008e58c87 |

### Comment by [@kannon92](https://github.com/kannon92) — 2023-08-11T13:48:30Z

I see what you all mean now.  

pointer is deprecated and we should switch to using ptr.  Will do that now.  Thank you `make verify`!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-11T20:40:21Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1055#" title="Author self-approved">kannon92</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1055#pullrequestreview-1574344036" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T20:40:44Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T20:40:51Z

/tide merge-method-squash

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T20:41:47Z

/label tide/merge-method-squash
/hold cancel

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T13:09:19Z

- **pkg/controller/core/clusterqueue_controller_test.go:255** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T13:07:19Z
```diff
@@ -252,7 +252,7 @@ func TestRecordResourceMetrics(t *testing.T) {
 								{
 									Name:           corev1.ResourceCPU,
 									NominalQuota:   resource.MustParse("1"),
-									BorrowingLimit: pointer.Quantity(resource.MustParse("2")),
+									BorrowingLimit: quantity.Quantity(resource.MustParse("2")),
```

  ```suggestion
  									BorrowingLimit: ptr.To(resource.MustParse("2")),
  ```
  
  Let's use `k8s.io/utils/ptr`.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T13:10:44Z

- **pkg/controller/core/clusterqueue_controller_test.go:255** by [@kannon92](https://github.com/kannon92) — 2023-08-11T13:10:44Z
```diff
@@ -252,7 +252,7 @@ func TestRecordResourceMetrics(t *testing.T) {
 								{
 									Name:           corev1.ResourceCPU,
 									NominalQuota:   resource.MustParse("1"),
-									BorrowingLimit: pointer.Quantity(resource.MustParse("2")),
+									BorrowingLimit: quantity.Quantity(resource.MustParse("2")),
```

  Yep! I'm working on a fix for that.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T13:28:33Z

- **pkg/controller/core/clusterqueue_controller_test.go:255** by [@kannon92](https://github.com/kannon92) — 2023-08-11T13:28:33Z
```diff
@@ -252,7 +252,7 @@ func TestRecordResourceMetrics(t *testing.T) {
 								{
 									Name:           corev1.ResourceCPU,
 									NominalQuota:   resource.MustParse("1"),
-									BorrowingLimit: pointer.Quantity(resource.MustParse("2")),
+									BorrowingLimit: quantity.Quantity(resource.MustParse("2")),
```

  Pushed up this change.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T15:52:13Z

- **apis/config/v1beta1/defaults.go:96** by [@kannon92](https://github.com/kannon92) — 2023-08-11T15:52:13Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  @alculquicondor since you were involved with pointer -> ptr deprecating, it may be worth keeping some of these utilities.  Removing this package was quite painful as I had to typecast since inline declarations are usually a generic type and k8 uses explicit types.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:00:56Z

- **apis/config/v1beta1/defaults.go:96** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:00:56Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  ```suggestion
  		cfg.ClientConnection.QPS = ptr.To[float32](DefaultClientConnectionQPS)
  ```
  
  I think the type argument makes sense.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T16:22:57Z

- **apis/config/v1beta1/defaults.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T16:22:57Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  You can also add a type to a constant

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T16:25:30Z

- **apis/config/v1beta1/defaults.go:96** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T16:25:30Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  > You can also add a type to a constant
  
  It sounds more reasonable.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T16:42:24Z

- **apis/config/v1beta1/defaults.go:96** by [@kannon92](https://github.com/kannon92) — 2023-08-11T16:42:24Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  Yea In this case I think that is the best option.  My point is mostly that every place where `pointer.Int32(1)` was used, I guess we should change that to `ptr.To[int32](1)`.  If we want to deprecreate pointer, I think having a utility to make that migration easier than changing all these calls may help this process.  
  
  I commented on this line to call it out.  I made the changes all over because `golang-ci` started flagging that pointer was deprecated.  
  
  I know that this utility is used throughout k8s (esp in Job controller code) so I was thinking it may be worth keeping a utility in `ptr` to allow for an easier transition.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T17:05:33Z

- **apis/config/v1beta1/defaults.go:96** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T17:05:33Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  In kubernetes, `verify` is not as strict as we have it here, so the migration can be done more gradually.
  
  As for `ptr.To[int32](1)`, that will do. It's similar to `sets.New[string]`, which we have in k8s codebase already.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T17:24:44Z

- **apis/config/v1beta1/defaults.go:96** by [@kannon92](https://github.com/kannon92) — 2023-08-11T17:24:44Z
```diff
@@ -79,24 +79,24 @@ func SetDefaults_Configuration(cfg *Configuration) {
 		cfg.InternalCertManagement = &InternalCertManagement{}
 	}
 	if cfg.InternalCertManagement.Enable == nil {
-		cfg.InternalCertManagement.Enable = pointer.Bool(true)
+		cfg.InternalCertManagement.Enable = ptr.To(true)
 	}
 	if *cfg.InternalCertManagement.Enable {
 		if cfg.InternalCertManagement.WebhookServiceName == nil {
-			cfg.InternalCertManagement.WebhookServiceName = pointer.String(DefaultWebhookServiceName)
+			cfg.InternalCertManagement.WebhookServiceName = ptr.To(DefaultWebhookServiceName)
 		}
 		if cfg.InternalCertManagement.WebhookSecretName == nil {
-			cfg.InternalCertManagement.WebhookSecretName = pointer.String(DefaultWebhookSecretName)
+			cfg.InternalCertManagement.WebhookSecretName = ptr.To(DefaultWebhookSecretName)
 		}
 	}
 	if cfg.ClientConnection == nil {
 		cfg.ClientConnection = &ClientConnection{}
 	}
 	if cfg.ClientConnection.QPS == nil {
-		cfg.ClientConnection.QPS = pointer.Float32(DefaultClientConnectionQPS)
+		cfg.ClientConnection.QPS = ptr.To(float32(DefaultClientConnectionQPS))
```

  Sounds good.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T18:32:13Z

- **apis/config/v1beta1/defaults.go:35** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T18:32:13Z
```diff
@@ -23,22 +23,22 @@ import (
 
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/runtime"
-	"k8s.io/utils/pointer"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
 )
 
 const (
-	DefaultNamespace              = "kueue-system"
-	DefaultWebhookServiceName     = "kueue-webhook-service"
-	DefaultWebhookSecretName      = "kueue-webhook-server-cert"
-	DefaultWebhookPort            = 9443
-	DefaultHealthProbeBindAddress = ":8081"
-	DefaultMetricsBindAddress     = ":8080"
-	DefaultLeaderElectionID       = "c1f6bfd2.kueue.x-k8s.io"
-	DefaultClientConnectionQPS    = 20.0
-	DefaultClientConnectionBurst  = 30
-	defaultPodsReadyTimeout       = 5 * time.Minute
+	DefaultNamespace                      = "kueue-system"
+	DefaultWebhookServiceName             = "kueue-webhook-service"
+	DefaultWebhookSecretName              = "kueue-webhook-server-cert"
+	DefaultWebhookPort            int     = 9443
```

  ```suggestion
  	DefaultWebhookPort               = 9443
  ```
  
  Do we need to declare explicitly `int`?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T19:22:19Z

- **apis/config/v1beta1/defaults.go:35** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:02:46Z
```diff
@@ -23,22 +23,22 @@ import (
 
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/runtime"
-	"k8s.io/utils/pointer"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/jobs/job"
 )
 
 const (
-	DefaultNamespace              = "kueue-system"
-	DefaultWebhookServiceName     = "kueue-webhook-service"
-	DefaultWebhookSecretName      = "kueue-webhook-server-cert"
-	DefaultWebhookPort            = 9443
-	DefaultHealthProbeBindAddress = ":8081"
-	DefaultMetricsBindAddress     = ":8080"
-	DefaultLeaderElectionID       = "c1f6bfd2.kueue.x-k8s.io"
-	DefaultClientConnectionQPS    = 20.0
-	DefaultClientConnectionBurst  = 30
-	defaultPodsReadyTimeout       = 5 * time.Minute
+	DefaultNamespace                      = "kueue-system"
+	DefaultWebhookServiceName             = "kueue-webhook-service"
+	DefaultWebhookSecretName              = "kueue-webhook-server-cert"
+	DefaultWebhookPort               = 9443
```

  Maybe, we need to clean up format with `make fmt`.

- **pkg/cache/cache_test.go:104** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:03:50Z
```diff
@@ -101,7 +101,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        10_000,
-									BorrowingLimit: pointer.Int64(10_000),
+									BorrowingLimit: ptr.To(int64(10_000)),
```

  ```suggestion
  									BorrowingLimit: ptr.To[int64](10_000),
  ```
  
  Should we use type argument instead of casting?

- **pkg/cache/cache_test.go:226** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:07:26Z
```diff
@@ -223,7 +223,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        10_000,
-									BorrowingLimit: pointer.Int64(10_000),
+									BorrowingLimit: ptr.To(int64(10_000)),
```

  ```suggestion
  									BorrowingLimit: ptr.To[int64](10_000),
  ```

- **pkg/cache/cache_test.go:347** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:18:35Z
```diff
@@ -344,7 +344,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        5_000,
-									BorrowingLimit: pointer.Int64(5_000),
+									BorrowingLimit: ptr.To(int64(5_000)),
```

  ```suggestion
  									BorrowingLimit: ptr.To[int64](5_000),
  ```

- **pkg/cache/cache_test.go:393** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:18:52Z
```diff
@@ -390,7 +390,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        5_000,
-									BorrowingLimit: pointer.Int64(5_000),
+									BorrowingLimit: ptr.To(int64(5_000)),
```

  ```suggestion
  									BorrowingLimit: ptr.To[int64](5_000),
  ```

- **pkg/cache/cache_test.go:498** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:19:03Z
```diff
@@ -495,7 +495,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        10_000,
-									BorrowingLimit: pointer.Int64(10_000),
+									BorrowingLimit: ptr.To(int64(10_000)),
```

  ```suggestion
  									BorrowingLimit: ptr.To[int64](10_000),
  ```

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:925** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:19:19Z
```diff
@@ -922,7 +922,7 @@ func TestAssignFlavors(t *testing.T) {
 					Flavors: []cache.FlavorQuotas{{
 						Name: "default",
 						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourceCPU:    {Nominal: 2000, BorrowingLimit: pointer.Int64(98_000)},
+							corev1.ResourceCPU:    {Nominal: 2000, BorrowingLimit: ptr.To(int64(98_000))},
```

  ```suggestion
  							corev1.ResourceCPU:    {Nominal: 2000, BorrowingLimit: ptr.To[int64](98_000)},
  ```

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1025** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:19:34Z
```diff
@@ -1022,7 +1022,7 @@ func TestAssignFlavors(t *testing.T) {
 					Flavors: []cache.FlavorQuotas{{
 						Name: "one",
 						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourceCPU: {Nominal: 2000, BorrowingLimit: pointer.Int64(8_000)},
+							corev1.ResourceCPU: {Nominal: 2000, BorrowingLimit: ptr.To(int64(8_000))},
```

  ```suggestion
  							corev1.ResourceCPU: {Nominal: 2000, BorrowingLimit: ptr.To[int64](8_000)},
  ```

- **pkg/util/testing/wrappers.go:29** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:20:14Z
```diff
@@ -27,8 +27,9 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
+	"k8s.io/utils/ptr"
```

  ```suggestion
  	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
  	"k8s.io/utils/ptr"
  ```

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:36** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:22:13Z
```diff
@@ -23,16 +23,17 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	"sigs.k8s.io/kueue/pkg/util/pointer"
 )
 
 // MPIJobWrapper wraps a Job.
 type MPIJobWrapper struct{ kubeflow.MPIJob }
 
 // MakeMPIJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakeMPIJob(name, ns string) *MPIJobWrapper {
+	const replica int32 = 1
```

  I don't think this is needed. If we declare this, we don't need to use the `ptr` package. Just use `&replica`.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-11T19:36:49Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-11T19:36:49Z
```diff
@@ -23,16 +23,17 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	"sigs.k8s.io/kueue/pkg/util/pointer"
 )
 
 // MPIJobWrapper wraps a Job.
 type MPIJobWrapper struct{ kubeflow.MPIJob }
 
 // MakeMPIJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakeMPIJob(name, ns string) *MPIJobWrapper {
+	const replica int32 = 1
```

  I would get rid of the constant and use `ptr.To[int32](1)` where needed.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T19:54:54Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:36** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T19:54:54Z
```diff
@@ -23,16 +23,17 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	"sigs.k8s.io/kueue/pkg/util/pointer"
 )
 
 // MPIJobWrapper wraps a Job.
 type MPIJobWrapper struct{ kubeflow.MPIJob }
 
 // MakeMPIJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakeMPIJob(name, ns string) *MPIJobWrapper {
+	const replica int32 = 1
```

  It's right. I wanted to say what you say.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:14:06Z

- **pkg/cache/cache_test.go:104** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:14:06Z
```diff
@@ -101,7 +101,7 @@ func TestCacheClusterQueueOperations(t *testing.T) {
 							Resources: map[corev1.ResourceName]*ResourceQuota{
 								corev1.ResourceCPU: {
 									Nominal:        10_000,
-									BorrowingLimit: pointer.Int64(10_000),
+									BorrowingLimit: ptr.To(int64(10_000)),
```

  updated.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:17:04Z

- **pkg/util/testingjobs/mpijob/wrappers_mpijob.go:36** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:17:03Z
```diff
@@ -23,16 +23,17 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
 
 	"sigs.k8s.io/kueue/pkg/controller/constants"
-	"sigs.k8s.io/kueue/pkg/util/pointer"
 )
 
 // MPIJobWrapper wraps a Job.
 type MPIJobWrapper struct{ kubeflow.MPIJob }
 
 // MakeMPIJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakeMPIJob(name, ns string) *MPIJobWrapper {
+	const replica int32 = 1
```

  fixed.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:25:12Z

- **pkg/controller/jobs/job/job_controller.go:295** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:25:11Z
```diff
@@ -292,7 +292,7 @@ func (j *Job) podsCount() int32 {
 func (j *Job) minPodsCount() *int32 {
 	if strVal, found := j.GetAnnotations()[JobMinParallelismAnnotation]; found {
 		if iVal, err := strconv.Atoi(strVal); err == nil {
-			return pointer.Int32(int32(iVal))
+			return ptr.To[int32](int32(iVal))
```

  Not sure on generics syntax but I did try `ptr.To[int32](ival)` but this was a compile error.  I had to cast.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:27:50Z

- **pkg/util/testing/wrappers.go:29** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:27:50Z
```diff
@@ -27,8 +27,9 @@ import (
 	"k8s.io/apimachinery/pkg/api/resource"
 	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
 
+	"k8s.io/utils/ptr"
```

  applied.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2023-08-11T20:28:12Z

- **pkg/scheduler/flavorassigner/flavorassigner_test.go:1025** by [@kannon92](https://github.com/kannon92) — 2023-08-11T20:28:12Z
```diff
@@ -1022,7 +1022,7 @@ func TestAssignFlavors(t *testing.T) {
 					Flavors: []cache.FlavorQuotas{{
 						Name: "one",
 						Resources: map[corev1.ResourceName]*cache.ResourceQuota{
-							corev1.ResourceCPU: {Nominal: 2000, BorrowingLimit: pointer.Int64(8_000)},
+							corev1.ResourceCPU: {Nominal: 2000, BorrowingLimit: ptr.To(int64(8_000))},
```

  thank you.  I knew I missed some.  Applied.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T20:33:05Z

- **pkg/controller/jobs/job/job_controller.go:295** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-11T20:33:04Z
```diff
@@ -292,7 +292,7 @@ func (j *Job) podsCount() int32 {
 func (j *Job) minPodsCount() *int32 {
 	if strVal, found := j.GetAnnotations()[JobMinParallelismAnnotation]; found {
 		if iVal, err := strconv.Atoi(strVal); err == nil {
-			return pointer.Int32(int32(iVal))
+			return ptr.To[int32](int32(iVal))
```

  It's expected behavior since `iVal` typed is `int`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-11T20:40:13Z

Thanks!
/tide/merge-method-squash
/lgtm
/approve
