# PR #10610: Introduce MVP version of Concurrent Admission feature

**Summary**: Introduce MVP version of Concurrent Admission feature

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10610

**Last updated**: 2026-05-29T12:10:49Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `pca-static-mvp` → `main`
- **Created**: 2026-04-20T10:47:24Z
- **Updated**: 2026-05-29T12:10:49Z
- **Merged**: 2026-04-30T11:49:29Z
- **Closed**: 2026-04-30T11:49:29Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl), [@kshalot](https://github.com/kshalot)
- **Changed files**: 38   **+4037 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
The PR introduces MVP version of ConcurrentAdmission feature. It includes
- new API definition
- new Concurrent Admission Controller
- minor changes to scheduling logic to narrow choice of resource flavor that are allowed to be assigned to a particular Workload
- minor changes to cache
- validation webhooks

The PR introduces two sub-types of Workloads as described in #8861 : 1) Parents; 2) Variants

The high-level idea is that the new concurrent admission controller creates Variants which are "copies" of a Parent Workload but with constraints regarding scheduling on a particular flavor.

The Variants are processed by Kueue almost as if they were regular Workloads, with constraints:
- Kueue scheduler can only assign certain resource flavor to them;
- Jobframework reconciler is not aware of them, and don't process them at all - this is to keep the implementation simple, and keep the current invariant, that a job corresponds to a Workload in 1;1 manner.

Variants are keepers of quota reservation and admission checks. 

The Parents are vastly different than regular Workloads:
- They do not undergo scheduling cycle
- They are not part of the Kueue queue cache (with a small exception) and scheduling cache
- They serve as an aggregation point between the Job and its Variants

Information between Parents and Variants is synced by the variant controller, and it's done both directions: a Parent to Variants, and Variants to Parent

Synchronization from Variants to Parent:
- If a Variant gets admitted, it sets proper conditions on its Parent as well
- If a Variant gets evicted, it sets proper conditions on its Parent as well

Synchronization from Parent to Variants:
- If a Parent gets deactivated, it deactivates its Variants
- If a Parent is finished, it finishes its Variants
- If a Parent has WaitForPodsReady condition, it syncs it on its Variants

E2E tests will be added in a separate PR to not blow the size of a single PR. I've tested it manually e2e as well

A few follow-ups this MVP requires (I'm going to create GH issues for them):
- Create index for Variants https://github.com/kubernetes-sigs/kueue/pull/10921
- Include the scheduling constraints in the hashing mechanism https://github.com/kubernetes-sigs/kueue/pull/10910
- Add logic to scheduler to preempt less favorable siblings before admission the more favorable one (currently concurrent admission controller deactivates less favorable sibling) https://github.com/kubernetes-sigs/kueue/pull/10917
- Add logic to scheduler to ensure only one Variant per siblings can preempt other Workloads to prevent excessive preemption https://github.com/kubernetes-sigs/kueue/issues/10920
- Make CQ's config mutable (add listener on CQs in concurrent admission controller) https://github.com/kubernetes-sigs/kueue/issues/10912
- ConcurrentAdmission controller should react to changes to CQ's resource flavors https://github.com/kubernetes-sigs/kueue/issues/10918

Test coverage of the new concurrentadmission/controller is at 72%, with close to 100% if we ignored error paths, registering the controller, and event handlers


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Scheduling: Introduce Concurrent Admission, an alpha feature disabled by default, allowing
Kueue to migrate admitted Workloads between ResourceFlavors to chase the optimal available capacity.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-20T10:47:31Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1f9cdf30b0c61377359e2e92bc7749a3d94e99cf |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f32e477edcb40008dfaefc |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T10:49:05Z

/assign @mbobrovskyi for the first pass

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T10:49:09Z

@PBundyra: GitHub didn't allow me to assign the following users: for, the, first, pass.

Note that only [kubernetes-sigs members](https://github.com/orgs/kubernetes-sigs/people) with read permissions, repo collaborators and people who have commented on this issue/PR can be assigned. Additionally, issues/PRs can only have 10 assignees at the same time.
For more information please see [the contributor guide](https://git.k8s.io/community/contributors/guide/first-contribution.md#issue-assignment-in-github)

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10610#issuecomment-4279993709):

>/assign @mbobrovskyi for the first pass


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T10:50:07Z

/cc

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T13:27:59Z

> Could you please add integration tests?

Yes, working on it

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T20:47:16Z

> > Could you please add integration tests?
> 
> Yes, working on it

Added some basic scenarios, will add more later

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T21:11:58Z

/restest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T08:02:29Z

/release-note-edit
```release-note
Introduce Concurrent Admission feature
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:33:40Z

@PBundyra before you address my comments, could we maybe squash the commits? At the moment we have >100 commits the history starts not to be that useful imo. Ofc take a backup on another branch "just in case". wdyt?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T11:25:40Z

> @PBundyra before you address my comments, could we maybe squash the commits? At the moment we have >100 commits the history starts not to be that useful imo. Ofc take a backup on another branch "just in case". wdyt?

Absolutely, done

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T08:15:52Z

I have changed the sibling preemption into eviction as proposed above

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T12:46:13Z

/retest

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T14:22:38Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T14:25:28Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10610/pull-kueue-test-e2e-main-1-33/2049486982500847616
Let me open flake issue

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T09:57:48Z

LGTM as for alpha, left some comments to improve the separation. Let's also make sure the follow ups are addressed before 0.18 as much as possible. Those which are not addressed by 0.18 make sure to capture in follow up issues / KEP updates.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-30T11:21:07Z

LGTM label has been added.  <details>Git tree hash: 2f097be7bcd26795cbc971a0d53eea84edcd7f5c</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-30T11:21:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10610#pullrequestreview-4204673338" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10610#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [PBundyra,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-29T12:10:46Z

/release-note-edit
```release-note
Scheduling: Introduce Concurrent Admission, an alpha feature disabled by default, allowing
Kueue to migrate admitted Workloads between ResourceFlavors to chase the optimal available capacity.
```

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:29:40Z

- **apis/kueue/v1beta2/clusterqueue_types.go:146** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T12:29:40Z
```diff
@@ -142,6 +142,10 @@ type ClusterQueueSpec struct {
 	// admissionScope indicates whether ClusterQueue uses the Admission Fair Sharing
 	// +optional
 	AdmissionScope *AdmissionScope `json:"admissionScope,omitempty"`
+
+	// concurrentAdmission defines the policy for concurrent attempts.
```

  ```suggestion
  	// concurrentAdmissionPolicy defines the policy for concurrent attempts.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:31:06Z

- **apis/kueue/v1beta2/clusterqueue_types.go:191** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T12:31:06Z
```diff
@@ -183,6 +187,42 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migrationConstraints defines the constraints of Variants migration
```

  ```suggestion
  	// migration defines the constraints of Variants migration
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T12:52:48Z

Could you please add integration tests?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T13:27:08Z

- **apis/kueue/v1beta2/clusterqueue_types.go:146** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T13:27:08Z
```diff
@@ -142,6 +142,10 @@ type ClusterQueueSpec struct {
 	// admissionScope indicates whether ClusterQueue uses the Admission Fair Sharing
 	// +optional
 	AdmissionScope *AdmissionScope `json:"admissionScope,omitempty"`
+
+	// concurrentAdmission defines the policy for concurrent attempts.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T13:27:15Z

- **apis/kueue/v1beta2/clusterqueue_types.go:191** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T13:27:14Z
```diff
@@ -183,6 +187,42 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migrationConstraints defines the constraints of Variants migration
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:10:17Z

- **pkg/util/testing/v1beta2/wrappers.go:420** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:10:17Z
```diff
@@ -415,6 +417,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AlloweResourceFlavors annotation
```

  ```suggestion
  // Set WorkloadAllowedResourceFlavorAnnotation annotation
  ```
  nit

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:11:59Z

- **pkg/util/testing/v1beta2/wrappers.go:420** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:11:59Z
```diff
@@ -415,6 +417,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AlloweResourceFlavors annotation
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:14:45Z

- **pkg/controller/concurrentadmission/variants.go:37** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:14:45Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  ```suggestion
  		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:16:03Z

- **pkg/controller/concurrentadmission/variants.go:49** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:16:03Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  Can we move this to a separate helper, like `isWorkloadOwner() bool`?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:19:46Z

- **pkg/controller/concurrentadmission/variants.go:49** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:19:46Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  This loop returns the name of the Owner not only a boolean value. Are you suggesting changing signature of `isOwnedByAWorkload` so it returns `string` (name of the Owner) instead of `bool` and call it here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:19:51Z

- **pkg/controller/concurrentadmission/variants.go:41** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:19:51Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
```

  Or actually, maybe we could do it like this:
  
  ```suggestion
  	return getParentVariant(workload) != ""
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:20:22Z

- **pkg/controller/concurrentadmission/variants.go:32** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:20:22Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
```

  ```suggestion
  func isOwnedByWorkload(workload *kueue.Workload) bool {
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:21:09Z

- **pkg/controller/concurrentadmission/variants.go:28** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:21:09Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:22:13Z

- **pkg/controller/concurrentadmission/variants.go:68** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:22:13Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+func getAdmittedVariant(variants []kueue.Workload) *kueue.Workload {
+	for i := range variants {
+		v := &variants[i]
+		if workload.IsAdmitted(v) {
+			return v
+		}
+	}
+	return nil
+}
+
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string)
```

  ```suggestion
  		workload.Labels = make(map[string]string, 1)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:25:52Z

- **pkg/workload/workload.go:1911** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:25:52Z
```diff
@@ -1873,3 +1902,11 @@ func TASAssignedNodeNames(wl *kueue.Workload) []string {
 	}
 	return nodesSet.UnsortedList()
 }
+
+func IsParentVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	val, ok := workload.Labels[controllerconstants.ParentVariantLabel]
+	return ok && val == "true"
```

  ```suggestion
  	return workload != nil && workload.Labels[controllerconstants.ParentVariantLabel] == "true"
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:30:58Z

- **pkg/controller/concurrentadmission/controller.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:30:58Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
```

  Does it mean that we are handling only parent? If yes, why we allow to reconcile variants in shouldReconcile()?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:32:19Z

- **pkg/controller/concurrentadmission/controller.go:226** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:32:19Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentVariant(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(context.Background(), client.ObjectKey{Name: string(cqName)}, cq); err != nil {
```

  ```suggestion
  	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
  ```
  I believe we can propagate ctx.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:44:05Z

- **pkg/controller/concurrentadmission/variants.go:49** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:44:05Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  No, I mean to use helper like this:
  
  ```
  func isWorkloadOwner(owner metav1.OwnerReference) bool {
  	return owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String()
  }
  ```
  
  And you can use it like this:
  ```
  	for _, owner := range workload.OwnerReferences {
  		if isWorkloadOwner(owner) {
  			return owner.Name
  		}
  	}
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:44:39Z

- **pkg/controller/concurrentadmission/variants.go:68** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:44:39Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+func getAdmittedVariant(variants []kueue.Workload) *kueue.Workload {
+	for i := range variants {
+		v := &variants[i]
+		if workload.IsAdmitted(v) {
+			return v
+		}
+	}
+	return nil
+}
+
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:44:49Z

- **pkg/controller/concurrentadmission/variants.go:32** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:44:49Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:45:10Z

- **pkg/controller/concurrentadmission/variants.go:37** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:45:10Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  Fixed

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:48:44Z

- **pkg/controller/concurrentadmission/controller.go:129** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:48:44Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
```

  Is it possible len(cq.Spec.ResourceGroups) == 0?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:49:53Z

- **pkg/controller/concurrentadmission/controller.go:128** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:49:53Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
```

  ```suggestion
  	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:53:22Z

- **pkg/controller/concurrentadmission/controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:53:22Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
```

  ```suggestion
  	log = log.WithValues("clusterQueue", klog.KObj(cq), "variants", utilslices.Map(variants, func(v *kueue.Workload) []klog.ObjectRef {
  		return klog.KObj(v)
  	}))
  	ctx = ctrl.LoggerInto(ctx, log)
  
  	log.V(2).Info("Found Workload family and ClusterQueue")
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:54:27Z

- **pkg/controller/concurrentadmission/controller.go:226** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:54:27Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentVariant(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(context.Background(), client.ObjectKey{Name: string(cqName)}, cq); err != nil {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:55:07Z

- **pkg/controller/concurrentadmission/variants.go:49** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:55:07Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  I've cleaned it up a bit, PTAL if it looks better

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T14:56:13Z

- **pkg/controller/concurrentadmission/controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T14:56:13Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
```

  I think parent no need to add because it is already a part of log.Values.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T14:56:26Z

- **pkg/controller/concurrentadmission/controller.go:126** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T14:56:25Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
```

  We still reconcile only parent because of this mapping function:
  
  https://github.com/kubernetes-sigs/kueue/pull/10610/changes#diff-6fc941f3717c5f64124852f07071a6b02e3e48072b879069fc9bbb4a8d850ddbR94-R102
  
  But we do want to watch both parent and variants changes

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:01:00Z

- **pkg/controller/concurrentadmission/controller.go:126** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:00:59Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
```

  But the predicate runs after this handler, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:03:12Z

- **pkg/controller/concurrentadmission/controller.go:134** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:03:12Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
```

  Ah, actually, we can make this much simpler:
  
  ```
  	log = log.WithValues("clusterQueue", klog.KObj(cq), "variants", klog.KObjSlice(variants))
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:07:06Z

- **pkg/controller/concurrentadmission/controller.go:126** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:07:06Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
```

  Predicate runs before the handler

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:07:59Z

- **pkg/controller/concurrentadmission/controller.go:139** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:07:59Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
```

  What happens if the user changes the resource group? Should we cleanup irrelevant variants?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:20:51Z

- **pkg/controller/concurrentadmission/controller.go:128** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:20:51Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:21:20Z

- **pkg/controller/concurrentadmission/controller.go:129** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:21:20Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
```

  I've added a webhook validation to ensure a CQ that defines ConcurrentAdmissionPolicy has exactly 1 ResourceGroup

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:22:15Z

- **pkg/controller/concurrentadmission/controller.go:134** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:22:15Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
```

  Added CQ name to the logger. This logger emits logs that are specific to a particular variant so I don't want to put the whole list into it

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:24:24Z

- **pkg/controller/concurrentadmission/controller.go:139** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:24:24Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
```

  That's a good point. Yes we should do that, adding that to a follow-ups list

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:25:33Z

- **pkg/controller/concurrentadmission/controller.go:151** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:25:33Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
```

  I think it would be great to categorize workloads that should be create, delete and sync. In that case we can handle it in separate goroutines. Like we did for LeaderWorkerSet: https://github.com/kubernetes-sigs/kueue/blob/13ac77c2706a1b9a7f18a99883c4e3a675832ab3/pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go#L138.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:27:03Z

- **pkg/controller/concurrentadmission/controller.go:134** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:27:03Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentVariant(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
```

  > Ah, actually, we can make this much simpler:
  
  I've cleaned up the logger as recommended

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T15:46:35Z

- **pkg/controller/concurrentadmission/controller.go:151** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T15:46:35Z
```diff
@@ -0,0 +1,592 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utilslices "sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int)
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log.V(2).Info("Found Workload family and ClusterQueue", "parent", parent.Name, "clusterQueue", cq.Name, "variants", utilslices.Map(variants, func(v *kueue.Workload) string {
+		return v.Name
+	}))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
```

  I think it's a good idea to parallelize deletion of the Workloads; will do it in a follow-up!
  
  However I am afraid categorizing Workloads in general will lead to only more complicated code. Usually the reconciler syncs only one variant (the admitted one) with a parent, so there's not really too much to gain imo. 
  
  Maybe we could do some parallelization when deactivating/activating/finishing multiple Variants, but I treat as a perf improvement, past mvp stage

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:51:25Z

- **pkg/controller/concurrentadmission/controller.go:491** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:51:25Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", variant.Name, "parent", parent.Name, "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", variant.Name, "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", variant.Name, "flavor", flavor)
+		// TODO: Emit event here
+	}
+	return nil
+}
+
+func generateVariant(parent *kueue.Workload, flavor kueue.ResourceFlavorReference) *kueue.Workload {
+	variant := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:          jobframework.GetWorkloadNameForVariant(parent.Name, parent.UID, parent.GroupVersionKind(), string(flavor)),
+			Namespace:     parent.Namespace,
+			Labels:        parent.Labels,
+			Annotations:   parent.Annotations,
+			ManagedFields: parent.ManagedFields,
+		},
+		Spec:   parent.Spec,
+		Status: parent.Status,
+	}
+	delete(variant.Labels, constants.ParentVariantLabel)
+	metav1.SetMetaDataAnnotation(&variant.ObjectMeta, constants.WorkloadAllowedResourceFlavorAnnotation, string(flavor))
+	return variant
+}
+
+func getVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[constants.WorkloadAllowedResourceFlavorAnnotation])
+}
+
+func (r *variantReconciler) hasVariantWithFlavor(ctx context.Context, variants []kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Checking if there is a variant with the flavor", "flavor", flavor)
+	for _, v := range variants {
+		if getVariantFlavor(&v) == flavor {
+			log.V(3).Info("Found a variant with the flavor", "variant", v.Name, "flavor", flavor)
+			return true
+		}
+	}
+	return false
+}
+
+func sortVariantsByFlavorOrder(variants []kueue.Workload, flavorOrder map[kueue.ResourceFlavorReference]int) []kueue.Workload {
+	slices.SortFunc(variants, func(a, b kueue.Workload) int {
+		aFlavor := getVariantFlavor(&a) // we only support one flavor per variant for now
+		bFlavor := getVariantFlavor(&b)
+		return flavorOrder[aFlavor] - flavorOrder[bFlavor]
+	})
+	return variants
+}
+
+func (r *variantReconciler) syncVariantEvictionStatus(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Syncing eviction status of variants", "parent", parent.Name)
+	for i := range variants {
+		v := &variants[i]
+		evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted)
+		if evCond != nil && evCond.Status == metav1.ConditionTrue && workload.HasQuotaReservation(v) {
+			log.V(2).Info("The variant has been evicted, clearing parents's admission", "variant", v.Name, "parent", parent.Name)
+			if err := r.clearWorkloadAdmission(ctx, v, evCond); err != nil {
+				return fmt.Errorf("clearing variant admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) clearWorkloadAdmission(ctx context.Context, wl *kueue.Workload, evCond *metav1.Condition) error {
+	return workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(w *kueue.Workload) (bool, error) {
+		setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) ||
+			(evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+		updated := workload.SetRequeuedCondition(w, evCond.Reason, evCond.Message, setRequeued)
+		if workload.UnsetQuotaReservationWithCondition(w, "Pending", evCond.Message, r.clock.Now()) {
+			updated = true
+		}
+		return updated, nil
+	})
+}
+
+func (r *variantReconciler) deactivateVariant(ctx context.Context, v *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if err := workload.Deactivate(ctx, r.client, v); err != nil {
+		return err
+	}
+	// fetch the updated variant and unset quota
+	if err := r.client.Get(ctx, client.ObjectKeyFromObject(v), v); err != nil {
+		return err
+	}
+	if evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+		if workload.HasQuotaReservation(v) {
+			log.V(3).Info("The variant is no longer active, clear the workloads admission")
+			err := workload.PatchAdmissionStatus(ctx, r.client, v, r.clock, func(wl *kueue.Workload) (bool, error) {
+				// The requeued condition status set to true only on EvictedByPreemption
+				setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) || (evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+				updated := workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, setRequeued)
+				if workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message, r.clock.Now()) {
+					updated = true
+				}
+				return updated, nil
+			})
+			if err != nil {
+				return fmt.Errorf("clearing admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) deactivateVariants(
+	ctx context.Context,
+	parent *kueue.Workload,
+	variants []kueue.Workload,
+	cq *kueue.ClusterQueue,
+	flavorOrder map[kueue.ResourceFlavorReference]int,
+) error {
+	log := ctrl.LoggerFrom(ctx)
+	if !workload.IsActive(parent) {
+		log.V(2).Info("Parent is not active, deactivating all variants", "parent", parent.Name)
+		for i := range variants {
+			v := &variants[i]
+			if err := r.deactivateVariant(ctx, v); err != nil {
+				return err
+			}
+		}
+		return nil
+	}
+
+	admittedWl := getAdmittedVariant(variants)
+	if admittedWl == nil {
+		log.V(3).Info("No admitted variant, no need to deactivate any variant")
+		return nil
+	}
+	// deactivate Variants below minTargetFlavor if specified
+	var minTargetFlavor *kueue.ResourceFlavorReference
+	if cq.Spec.ConcurrentAdmissionPolicy != nil && cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints != nil {
+		minTargetFlavor = cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	}
+	if minTargetFlavor != nil {
+		log.V(3).Info("Deactivating variants below minTargetFlavor", "minTargetFlavor", *minTargetFlavor)
+		for i := range variants {
+			v := &variants[i]
+			if v.Name == admittedWl.Name {
+				continue
+			}
+			if flavorOrder[getVariantFlavor(v)] > flavorOrder[*minTargetFlavor] {
+				log.V(2).Info("Deactivating variant because it is below the minTargetFlavor", "variant", v.Name, "flavor", getVariantFlavor(v), "minTargetFlavor", *minTargetFlavor)
+				if err := r.deactivateVariant(ctx, v); err != nil {
+					return err
+				}
+			}
+		}
+	}
+	// also deactivate Variants below the admitted variant regardless of minTargetFlavor
+	log.V(3).Info("Deactivating variants below the admitted variant", "admittedVariant", admittedWl.Name, "admittedFlavor", getVariantFlavor(admittedWl))
+	for i := range variants {
+		v := &variants[i]
+		if flavorOrder[getVariantFlavor(v)] > flavorOrder[getVariantFlavor(admittedWl)] {
+			log.V(2).Info("Deactivating variant because it is below the admitted variant", "variant", v.Name, "flavor", getVariantFlavor(v), "admittedFlavor", getVariantFlavor(admittedWl))
+			if err := r.deactivateVariant(ctx, v); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) activateVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, cq *kueue.ClusterQueue, flavorOrder map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	if !workload.IsActive(parent) {
+		log.V(3).Info("Parent is not active, no needed to activate variants", "parent", parent.Name)
+		return nil
+	}
+	admittedVariant := getAdmittedVariant(variants)
+	if admittedVariant == nil {
+		log.V(3).Info("No admitted variant, activating all variants")
+		// no admitted variants so activate all variants if they are not active, case of preemption
+		for i := range variants {
+			v := &variants[i]
+			if err := workload.Activate(ctx, r.client, v); err != nil {
+				return err
+			}
+		}
+		return nil
+	}
+	// activate all variants that are at least at the minTargetFlavor if specificed
+	var minTargetFlavor *kueue.ResourceFlavorReference
+	if cq.Spec.ConcurrentAdmissionPolicy != nil && cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints != nil {
+		minTargetFlavor = cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	}
+	if minTargetFlavor != nil {
+		for i := range variants {
+			v := &variants[i]
+			if flavorOrder[getVariantFlavor(v)] <= flavorOrder[*minTargetFlavor] && flavorOrder[getVariantFlavor(v)] < flavorOrder[getVariantFlavor(admittedVariant)] {
+				// activate the variant, the smaller or equal the flavor order is to the minTargetFlavor, the higher the priority is
+				if err := workload.Activate(ctx, r.client, v); err != nil {
+					return err
+				}
+			}
+		}
+		return nil
+	}
+	// no minTargetFlavor specified, so activate all variants that are below the admitted variant in the flavor order
+	for i := range variants {
+		v := &variants[i]
+		if flavorOrder[getVariantFlavor(v)] < flavorOrder[getVariantFlavor(admittedVariant)] {
+			// activate the variant, the smaller the flavor order is to the admitted variant, the higher the priority is
+			if err := workload.Activate(ctx, r.client, v); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) syncFinished(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	finishCond := apimeta.FindStatusCondition(parent.Status.Conditions, kueue.WorkloadFinished)
+	for i := range variants {
+		v := &variants[i]
+		if err := workload.Finish(ctx, r.client, v, finishCond.Reason, finishCond.Message, r.clock); err != nil && !apierrors.IsNotFound(err) {
```

  ```suggestion
  		if err := workload.Finish(ctx, r.client, v, finishCond.Reason, finishCond.Message, r.clock); client.IgnoreNotFound(err) != nil {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:56:08Z

- **pkg/controller/concurrentadmission/controller.go:243** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:56:08Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
```

  ```suggestion
  		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", klog.KObj(variant))
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T15:56:15Z

- **pkg/controller/concurrentadmission/controller.go:243** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T15:56:15Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
```

  Also in other places.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T16:03:37Z

- **pkg/controller/concurrentadmission/controller.go:353** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T16:03:37Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", variant.Name, "parent", parent.Name, "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", variant.Name, "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", variant.Name, "flavor", flavor)
+		// TODO: Emit event here
+	}
+	return nil
+}
+
+func generateVariant(parent *kueue.Workload, flavor kueue.ResourceFlavorReference) *kueue.Workload {
+	variant := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:          jobframework.GetWorkloadNameForVariant(parent.Name, parent.UID, parent.GroupVersionKind(), string(flavor)),
+			Namespace:     parent.Namespace,
+			Labels:        parent.Labels,
+			Annotations:   parent.Annotations,
+			ManagedFields: parent.ManagedFields,
+		},
+		Spec:   parent.Spec,
+		Status: parent.Status,
+	}
+	delete(variant.Labels, constants.ParentVariantLabel)
+	metav1.SetMetaDataAnnotation(&variant.ObjectMeta, constants.WorkloadAllowedResourceFlavorAnnotation, string(flavor))
+	return variant
+}
+
+func getVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[constants.WorkloadAllowedResourceFlavorAnnotation])
+}
+
+func (r *variantReconciler) hasVariantWithFlavor(ctx context.Context, variants []kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Checking if there is a variant with the flavor", "flavor", flavor)
+	for _, v := range variants {
+		if getVariantFlavor(&v) == flavor {
+			log.V(3).Info("Found a variant with the flavor", "variant", v.Name, "flavor", flavor)
+			return true
+		}
+	}
+	return false
+}
+
+func sortVariantsByFlavorOrder(variants []kueue.Workload, flavorOrder map[kueue.ResourceFlavorReference]int) []kueue.Workload {
+	slices.SortFunc(variants, func(a, b kueue.Workload) int {
+		aFlavor := getVariantFlavor(&a) // we only support one flavor per variant for now
+		bFlavor := getVariantFlavor(&b)
+		return flavorOrder[aFlavor] - flavorOrder[bFlavor]
+	})
+	return variants
+}
+
+func (r *variantReconciler) syncVariantEvictionStatus(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Syncing eviction status of variants", "parent", parent.Name)
+	for i := range variants {
+		v := &variants[i]
+		evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted)
+		if evCond != nil && evCond.Status == metav1.ConditionTrue && workload.HasQuotaReservation(v) {
+			log.V(2).Info("The variant has been evicted, clearing parents's admission", "variant", v.Name, "parent", parent.Name)
+			if err := r.clearWorkloadAdmission(ctx, v, evCond); err != nil {
+				return fmt.Errorf("clearing variant admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) clearWorkloadAdmission(ctx context.Context, wl *kueue.Workload, evCond *metav1.Condition) error {
+	return workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(w *kueue.Workload) (bool, error) {
+		setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) ||
+			(evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+		updated := workload.SetRequeuedCondition(w, evCond.Reason, evCond.Message, setRequeued)
+		if workload.UnsetQuotaReservationWithCondition(w, "Pending", evCond.Message, r.clock.Now()) {
+			updated = true
+		}
+		return updated, nil
+	})
+}
+
+func (r *variantReconciler) deactivateVariant(ctx context.Context, v *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if err := workload.Deactivate(ctx, r.client, v); err != nil {
+		return err
+	}
+	// fetch the updated variant and unset quota
+	if err := r.client.Get(ctx, client.ObjectKeyFromObject(v), v); err != nil {
+		return err
+	}
+	if evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+		if workload.HasQuotaReservation(v) {
+			log.V(3).Info("The variant is no longer active, clear the workloads admission")
+			err := workload.PatchAdmissionStatus(ctx, r.client, v, r.clock, func(wl *kueue.Workload) (bool, error) {
+				// The requeued condition status set to true only on EvictedByPreemption
+				setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) || (evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+				updated := workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, setRequeued)
+				if workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message, r.clock.Now()) {
+					updated = true
+				}
+				return updated, nil
+			})
+			if err != nil {
+				return fmt.Errorf("clearing admission: %w", err)
+			}
```

  I see this logic duplicated in multiple places. Can we extract it into a helper? We have the same logic in the jobframework reconciler as well.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-20T16:08:27Z

- **pkg/controller/concurrentadmission/variants.go:49** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-20T16:08:26Z
```diff
@@ -0,0 +1,71 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	return isOwnedByAWorkload(workload)
+}
+
+func isOwnedByAWorkload(workload *kueue.Workload) bool {
+	if workload == nil {
+		return false
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
+			return true
+		}
+	}
+	return false
+}
+
+func getParentVariant(workload *kueue.Workload) string {
+	if workload == nil {
+		return ""
+	}
+	for _, owner := range workload.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == "kueue.x-k8s.io/v1beta2" {
```

  LGTM. Thank you!

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T19:35:03Z

- **pkg/controller/concurrentadmission/controller.go:243** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T19:35:03Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
```

  Done in all places

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T19:35:20Z

- **pkg/controller/concurrentadmission/controller.go:491** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T19:35:20Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", variant.Name, "parent", parent.Name, "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", variant.Name, "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", variant.Name, "flavor", flavor)
+		// TODO: Emit event here
+	}
+	return nil
+}
+
+func generateVariant(parent *kueue.Workload, flavor kueue.ResourceFlavorReference) *kueue.Workload {
+	variant := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:          jobframework.GetWorkloadNameForVariant(parent.Name, parent.UID, parent.GroupVersionKind(), string(flavor)),
+			Namespace:     parent.Namespace,
+			Labels:        parent.Labels,
+			Annotations:   parent.Annotations,
+			ManagedFields: parent.ManagedFields,
+		},
+		Spec:   parent.Spec,
+		Status: parent.Status,
+	}
+	delete(variant.Labels, constants.ParentVariantLabel)
+	metav1.SetMetaDataAnnotation(&variant.ObjectMeta, constants.WorkloadAllowedResourceFlavorAnnotation, string(flavor))
+	return variant
+}
+
+func getVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[constants.WorkloadAllowedResourceFlavorAnnotation])
+}
+
+func (r *variantReconciler) hasVariantWithFlavor(ctx context.Context, variants []kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Checking if there is a variant with the flavor", "flavor", flavor)
+	for _, v := range variants {
+		if getVariantFlavor(&v) == flavor {
+			log.V(3).Info("Found a variant with the flavor", "variant", v.Name, "flavor", flavor)
+			return true
+		}
+	}
+	return false
+}
+
+func sortVariantsByFlavorOrder(variants []kueue.Workload, flavorOrder map[kueue.ResourceFlavorReference]int) []kueue.Workload {
+	slices.SortFunc(variants, func(a, b kueue.Workload) int {
+		aFlavor := getVariantFlavor(&a) // we only support one flavor per variant for now
+		bFlavor := getVariantFlavor(&b)
+		return flavorOrder[aFlavor] - flavorOrder[bFlavor]
+	})
+	return variants
+}
+
+func (r *variantReconciler) syncVariantEvictionStatus(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Syncing eviction status of variants", "parent", parent.Name)
+	for i := range variants {
+		v := &variants[i]
+		evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted)
+		if evCond != nil && evCond.Status == metav1.ConditionTrue && workload.HasQuotaReservation(v) {
+			log.V(2).Info("The variant has been evicted, clearing parents's admission", "variant", v.Name, "parent", parent.Name)
+			if err := r.clearWorkloadAdmission(ctx, v, evCond); err != nil {
+				return fmt.Errorf("clearing variant admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) clearWorkloadAdmission(ctx context.Context, wl *kueue.Workload, evCond *metav1.Condition) error {
+	return workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(w *kueue.Workload) (bool, error) {
+		setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) ||
+			(evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+		updated := workload.SetRequeuedCondition(w, evCond.Reason, evCond.Message, setRequeued)
+		if workload.UnsetQuotaReservationWithCondition(w, "Pending", evCond.Message, r.clock.Now()) {
+			updated = true
+		}
+		return updated, nil
+	})
+}
+
+func (r *variantReconciler) deactivateVariant(ctx context.Context, v *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if err := workload.Deactivate(ctx, r.client, v); err != nil {
+		return err
+	}
+	// fetch the updated variant and unset quota
+	if err := r.client.Get(ctx, client.ObjectKeyFromObject(v), v); err != nil {
+		return err
+	}
+	if evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+		if workload.HasQuotaReservation(v) {
+			log.V(3).Info("The variant is no longer active, clear the workloads admission")
+			err := workload.PatchAdmissionStatus(ctx, r.client, v, r.clock, func(wl *kueue.Workload) (bool, error) {
+				// The requeued condition status set to true only on EvictedByPreemption
+				setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) || (evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+				updated := workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, setRequeued)
+				if workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message, r.clock.Now()) {
+					updated = true
+				}
+				return updated, nil
+			})
+			if err != nil {
+				return fmt.Errorf("clearing admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) deactivateVariants(
+	ctx context.Context,
+	parent *kueue.Workload,
+	variants []kueue.Workload,
+	cq *kueue.ClusterQueue,
+	flavorOrder map[kueue.ResourceFlavorReference]int,
+) error {
+	log := ctrl.LoggerFrom(ctx)
+	if !workload.IsActive(parent) {
+		log.V(2).Info("Parent is not active, deactivating all variants", "parent", parent.Name)
+		for i := range variants {
+			v := &variants[i]
+			if err := r.deactivateVariant(ctx, v); err != nil {
+				return err
+			}
+		}
+		return nil
+	}
+
+	admittedWl := getAdmittedVariant(variants)
+	if admittedWl == nil {
+		log.V(3).Info("No admitted variant, no need to deactivate any variant")
+		return nil
+	}
+	// deactivate Variants below minTargetFlavor if specified
+	var minTargetFlavor *kueue.ResourceFlavorReference
+	if cq.Spec.ConcurrentAdmissionPolicy != nil && cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints != nil {
+		minTargetFlavor = cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	}
+	if minTargetFlavor != nil {
+		log.V(3).Info("Deactivating variants below minTargetFlavor", "minTargetFlavor", *minTargetFlavor)
+		for i := range variants {
+			v := &variants[i]
+			if v.Name == admittedWl.Name {
+				continue
+			}
+			if flavorOrder[getVariantFlavor(v)] > flavorOrder[*minTargetFlavor] {
+				log.V(2).Info("Deactivating variant because it is below the minTargetFlavor", "variant", v.Name, "flavor", getVariantFlavor(v), "minTargetFlavor", *minTargetFlavor)
+				if err := r.deactivateVariant(ctx, v); err != nil {
+					return err
+				}
+			}
+		}
+	}
+	// also deactivate Variants below the admitted variant regardless of minTargetFlavor
+	log.V(3).Info("Deactivating variants below the admitted variant", "admittedVariant", admittedWl.Name, "admittedFlavor", getVariantFlavor(admittedWl))
+	for i := range variants {
+		v := &variants[i]
+		if flavorOrder[getVariantFlavor(v)] > flavorOrder[getVariantFlavor(admittedWl)] {
+			log.V(2).Info("Deactivating variant because it is below the admitted variant", "variant", v.Name, "flavor", getVariantFlavor(v), "admittedFlavor", getVariantFlavor(admittedWl))
+			if err := r.deactivateVariant(ctx, v); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) activateVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, cq *kueue.ClusterQueue, flavorOrder map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	if !workload.IsActive(parent) {
+		log.V(3).Info("Parent is not active, no needed to activate variants", "parent", parent.Name)
+		return nil
+	}
+	admittedVariant := getAdmittedVariant(variants)
+	if admittedVariant == nil {
+		log.V(3).Info("No admitted variant, activating all variants")
+		// no admitted variants so activate all variants if they are not active, case of preemption
+		for i := range variants {
+			v := &variants[i]
+			if err := workload.Activate(ctx, r.client, v); err != nil {
+				return err
+			}
+		}
+		return nil
+	}
+	// activate all variants that are at least at the minTargetFlavor if specificed
+	var minTargetFlavor *kueue.ResourceFlavorReference
+	if cq.Spec.ConcurrentAdmissionPolicy != nil && cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints != nil {
+		minTargetFlavor = cq.Spec.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	}
+	if minTargetFlavor != nil {
+		for i := range variants {
+			v := &variants[i]
+			if flavorOrder[getVariantFlavor(v)] <= flavorOrder[*minTargetFlavor] && flavorOrder[getVariantFlavor(v)] < flavorOrder[getVariantFlavor(admittedVariant)] {
+				// activate the variant, the smaller or equal the flavor order is to the minTargetFlavor, the higher the priority is
+				if err := workload.Activate(ctx, r.client, v); err != nil {
+					return err
+				}
+			}
+		}
+		return nil
+	}
+	// no minTargetFlavor specified, so activate all variants that are below the admitted variant in the flavor order
+	for i := range variants {
+		v := &variants[i]
+		if flavorOrder[getVariantFlavor(v)] < flavorOrder[getVariantFlavor(admittedVariant)] {
+			// activate the variant, the smaller the flavor order is to the admitted variant, the higher the priority is
+			if err := workload.Activate(ctx, r.client, v); err != nil {
+				return err
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) syncFinished(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	finishCond := apimeta.FindStatusCondition(parent.Status.Conditions, kueue.WorkloadFinished)
+	for i := range variants {
+		v := &variants[i]
+		if err := workload.Finish(ctx, r.client, v, finishCond.Reason, finishCond.Message, r.clock); err != nil && !apierrors.IsNotFound(err) {
```

  Done in all occurrences

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-20T19:49:41Z

- **pkg/controller/concurrentadmission/controller.go:353** by [@PBundyra](https://github.com/PBundyra) — 2026-04-20T19:49:41Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	"k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: getParentWorkload(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      ptr.To(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	if err := r.syncVariantEvictionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "name", req.Name, "namespace", req.Namespace)
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if getParentWorkload(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", variant.Name, "parent", parent.Name, "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", variant.Name, "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", variant.Name, "flavor", flavor)
+		// TODO: Emit event here
+	}
+	return nil
+}
+
+func generateVariant(parent *kueue.Workload, flavor kueue.ResourceFlavorReference) *kueue.Workload {
+	variant := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:          jobframework.GetWorkloadNameForVariant(parent.Name, parent.UID, parent.GroupVersionKind(), string(flavor)),
+			Namespace:     parent.Namespace,
+			Labels:        parent.Labels,
+			Annotations:   parent.Annotations,
+			ManagedFields: parent.ManagedFields,
+		},
+		Spec:   parent.Spec,
+		Status: parent.Status,
+	}
+	delete(variant.Labels, constants.ParentVariantLabel)
+	metav1.SetMetaDataAnnotation(&variant.ObjectMeta, constants.WorkloadAllowedResourceFlavorAnnotation, string(flavor))
+	return variant
+}
+
+func getVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[constants.WorkloadAllowedResourceFlavorAnnotation])
+}
+
+func (r *variantReconciler) hasVariantWithFlavor(ctx context.Context, variants []kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Checking if there is a variant with the flavor", "flavor", flavor)
+	for _, v := range variants {
+		if getVariantFlavor(&v) == flavor {
+			log.V(3).Info("Found a variant with the flavor", "variant", v.Name, "flavor", flavor)
+			return true
+		}
+	}
+	return false
+}
+
+func sortVariantsByFlavorOrder(variants []kueue.Workload, flavorOrder map[kueue.ResourceFlavorReference]int) []kueue.Workload {
+	slices.SortFunc(variants, func(a, b kueue.Workload) int {
+		aFlavor := getVariantFlavor(&a) // we only support one flavor per variant for now
+		bFlavor := getVariantFlavor(&b)
+		return flavorOrder[aFlavor] - flavorOrder[bFlavor]
+	})
+	return variants
+}
+
+func (r *variantReconciler) syncVariantEvictionStatus(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(3).Info("Syncing eviction status of variants", "parent", parent.Name)
+	for i := range variants {
+		v := &variants[i]
+		evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted)
+		if evCond != nil && evCond.Status == metav1.ConditionTrue && workload.HasQuotaReservation(v) {
+			log.V(2).Info("The variant has been evicted, clearing parents's admission", "variant", v.Name, "parent", parent.Name)
+			if err := r.clearWorkloadAdmission(ctx, v, evCond); err != nil {
+				return fmt.Errorf("clearing variant admission: %w", err)
+			}
+		}
+	}
+	return nil
+}
+
+func (r *variantReconciler) clearWorkloadAdmission(ctx context.Context, wl *kueue.Workload, evCond *metav1.Condition) error {
+	return workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(w *kueue.Workload) (bool, error) {
+		setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) ||
+			(evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+		updated := workload.SetRequeuedCondition(w, evCond.Reason, evCond.Message, setRequeued)
+		if workload.UnsetQuotaReservationWithCondition(w, "Pending", evCond.Message, r.clock.Now()) {
+			updated = true
+		}
+		return updated, nil
+	})
+}
+
+func (r *variantReconciler) deactivateVariant(ctx context.Context, v *kueue.Workload) error {
+	log := ctrl.LoggerFrom(ctx)
+	if err := workload.Deactivate(ctx, r.client, v); err != nil {
+		return err
+	}
+	// fetch the updated variant and unset quota
+	if err := r.client.Get(ctx, client.ObjectKeyFromObject(v), v); err != nil {
+		return err
+	}
+	if evCond := apimeta.FindStatusCondition(v.Status.Conditions, kueue.WorkloadEvicted); evCond != nil && evCond.Status == metav1.ConditionTrue {
+		if workload.HasQuotaReservation(v) {
+			log.V(3).Info("The variant is no longer active, clear the workloads admission")
+			err := workload.PatchAdmissionStatus(ctx, r.client, v, r.clock, func(wl *kueue.Workload) (bool, error) {
+				// The requeued condition status set to true only on EvictedByPreemption
+				setRequeued := (evCond.Reason == kueue.WorkloadEvictedByPreemption) || (evCond.Reason == kueue.WorkloadEvictedDueToNodeFailures)
+				updated := workload.SetRequeuedCondition(wl, evCond.Reason, evCond.Message, setRequeued)
+				if workload.UnsetQuotaReservationWithCondition(wl, "Pending", evCond.Message, r.clock.Now()) {
+					updated = true
+				}
+				return updated, nil
+			})
+			if err != nil {
+				return fmt.Errorf("clearing admission: %w", err)
+			}
```

  Used a helper function here

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:20:08Z

- **apis/kueue/v1beta2/clusterqueue_types.go:194** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:20:08Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitempty,omitzero"`
```

  If it is required why we omit it?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:24:20Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:785** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:24:20Z
```diff
@@ -777,6 +777,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	annotations := a.wl.Obj.GetAnnotations()
+	if annotations == nil {
+		return true
+	}
+	allowedFlavors, ok := annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"]
```

  ```suggestion
  	allowedFlavors, ok := annotations[constants.WorkloadAllowedResourceFlavorAnnotation]
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:27:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:785** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:27:59Z
```diff
@@ -777,6 +777,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	annotations := a.wl.Obj.GetAnnotations()
+	if annotations == nil {
+		return true
+	}
+	allowedFlavors, ok := annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"]
```

  ```suggestion
  	allowedFlavors, ok := a.wl.Obj.GetAnnotations()[constants.WorkloadAllowedResourceFlavorAnnotation]
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:33:51Z

- **pkg/util/testing/v1beta2/wrappers.go:428** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:33:51Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
+		annotations = make(map[string]string)
```

  ```suggestion
  		annotations = make(map[string]string, 1)
  ```
  nit

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:34:35Z

- **pkg/util/testing/v1beta2/wrappers.go:431** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:34:35Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
+		annotations = make(map[string]string)
+	}
+	allowedFlavors := strings.Join(utilslices.Map(flavors, func(f *kueue.ResourceFlavorReference) string { return string(*f) }), ",")
+	annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"] = allowedFlavors
```

  ```suggestion
  	annotations[constants.WorkloadAllowedResourceFlavorAnnotation] = allowedFlavors
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:35:39Z

- **pkg/util/testing/v1beta2/wrappers.go:427** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:35:39Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
```

  ```suggestion
  	if w.Workload.Annotations == nil {
  ```
  I think we can use w.Workload.Annotations instead of annotations.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:42:05Z

- **pkg/webhooks/clusterqueue_webhook_test.go:388** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:42:05Z
```diff
@@ -383,6 +383,80 @@ func TestValidateClusterQueue(t *testing.T) {
 			wantDetail:   `preference "PreemptionOverBorrowing" requires both whenCanBorrow and whenCanPreempt to be TryNextFlavor`,
 			wantBadValue: string(kueue.PreemptionOverBorrowing),
 		},
+		{
+			name: "valid ConcurrentAdmissionPolicy",
+			clusterQueue: &kueue.ClusterQueue{
```

  Can we use ClusterQueueWrapper to build ClusterQueue?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:51:43Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:78** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:51:43Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
```

  ```suggestion
  			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, cq)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:51:57Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:83** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:51:57Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
```

  ```suggestion
  			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, lq)
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:52:56Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:86** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:52:56Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
```

  ```suggestion
  ```
  
  We should delete it in DeleteWorkloadsInNamespace().

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-21T08:54:08Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:76** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-21T08:54:08Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
```

  ```suggestion
  					*utiltestingapi.MakeFlavorQuotas(flavorReservation.Name).Resource(corev1.ResourceCPU, "5").Obj(),
  					*utiltestingapi.MakeFlavorQuotas(flavorSpot.Name).Resource(corev1.ResourceCPU, "5").Obj(),
  ```

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:56:51Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:76** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:56:51Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:57:06Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:83** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:57:06Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:58:19Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:78** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:58:19Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:58:37Z

- **pkg/webhooks/clusterqueue_webhook_test.go:388** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:58:37Z
```diff
@@ -383,6 +383,80 @@ func TestValidateClusterQueue(t *testing.T) {
 			wantDetail:   `preference "PreemptionOverBorrowing" requires both whenCanBorrow and whenCanPreempt to be TryNextFlavor`,
 			wantBadValue: string(kueue.PreemptionOverBorrowing),
 		},
+		{
+			name: "valid ConcurrentAdmissionPolicy",
+			clusterQueue: &kueue.ClusterQueue{
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:58:50Z

- **pkg/util/testing/v1beta2/wrappers.go:427** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:58:49Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
```

  Cleaned up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:59:00Z

- **pkg/util/testing/v1beta2/wrappers.go:431** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:58:59Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
+		annotations = make(map[string]string)
+	}
+	allowedFlavors := strings.Join(utilslices.Map(flavors, func(f *kueue.ResourceFlavorReference) string { return string(*f) }), ",")
+	annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"] = allowedFlavors
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T07:59:35Z

- **pkg/util/testing/v1beta2/wrappers.go:428** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T07:59:34Z
```diff
@@ -415,6 +421,18 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	annotations := w.GetAnnotations()
+	if annotations == nil {
+		annotations = make(map[string]string)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T08:00:05Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:785** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T08:00:04Z
```diff
@@ -777,6 +777,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	annotations := a.wl.Obj.GetAnnotations()
+	if annotations == nil {
+		return true
+	}
+	allowedFlavors, ok := annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"]
```

  Cleaned up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T08:00:18Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:785** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T08:00:18Z
```diff
@@ -777,6 +777,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	annotations := a.wl.Obj.GetAnnotations()
+	if annotations == nil {
+		return true
+	}
+	allowedFlavors, ok := annotations["kueue.x-k8s.io/workload-allowed-resource-flavors"]
```

  Cleaned up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T08:03:08Z

- **test/integration/singlecluster/controller/concurrentadmission/concurrent_admission_test.go:86** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T08:03:07Z
```diff
@@ -0,0 +1,432 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+var _ = ginkgo.Describe("Concurrent Admission", ginkgo.Ordered, ginkgo.ContinueOnFailure, func() {
+	var (
+		ns *corev1.Namespace
+	)
+
+	ginkgo.BeforeEach(func() {
+		features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.ConcurrentAdmission, true)
+		ns = util.CreateNamespaceFromPrefixWithLog(ctx, k8sClient, "concurrent-")
+	})
+
+	ginkgo.AfterEach(func() {
+		gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+	})
+
+	ginkgo.BeforeAll(func() {
+		fwk.StartManager(ctx, cfg, managerAndSchedulerSetup(&configapi.Configuration{}))
+	})
+
+	ginkgo.AfterAll(func() {
+		fwk.StopManager(ctx)
+	})
+
+	ginkgo.When("Should assign flavors with respect to constraints", func() {
+		var cq *kueue.ClusterQueue
+		var lq *kueue.LocalQueue
+		var flavorReservation *kueue.ResourceFlavor
+		var flavorSpot *kueue.ResourceFlavor
+
+		ginkgo.BeforeEach(func() {
+			flavorReservation = utiltestingapi.MakeResourceFlavor("reservation").Obj()
+			util.MustCreate(ctx, k8sClient, flavorReservation)
+
+			flavorSpot = utiltestingapi.MakeResourceFlavor("spot").Obj()
+			util.MustCreate(ctx, k8sClient, flavorSpot)
+
+			cq = utiltestingapi.MakeClusterQueue("cq-constraints").
+				ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas("reservation").Resource(corev1.ResourceCPU, "5").Obj(),
+					*utiltestingapi.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+				).Obj()
+			util.MustCreate(ctx, k8sClient, cq)
+
+			lq = utiltestingapi.MakeLocalQueue("lq", ns.Name).ClusterQueue(cq.Name).Obj()
+			util.MustCreate(ctx, k8sClient, lq)
+
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, cq)
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, lq, true)
```

  Can you elaborate? IIUC `DeleteWorkloadsInNamespace` deletes only Workloads and omits LQs

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-27T17:26:17Z

- **apis/kueue/v1beta2/clusterqueue_types.go:194** by [@PBundyra](https://github.com/PBundyra) — 2026-04-27T17:26:17Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitempty,omitzero"`
```

  When it doesn't have the tag, the linter fails: https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10610/pull-kueue-verify-main/2048742985826308096

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T08:11:44Z

- **pkg/controller/concurrentadmission/controller.go:25** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T08:11:44Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
```

  ```suggestion
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T08:13:24Z

- **pkg/controller/concurrentadmission/controller.go:113** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T08:13:24Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
```

  I think we need more actions, right?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T08:18:28Z

- **pkg/controller/concurrentadmission/controller.go:210** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T08:18:29Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
```

  We fetch all workloads in the namespace, which isn’t optimized. Can we add an index?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T08:23:21Z

- **pkg/controller/concurrentadmission/controller.go:255** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T08:23:21Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if workload.GetParentWorkloadName(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", klog.KObj(variant), "parent", klog.KObj(parent), "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", klog.KObj(variant), "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", klog.KObj(variant), "flavor", flavor)
+		// TODO: Emit event here
```

  Could you please implement this?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T09:14:59Z

- **pkg/controller/concurrentadmission/controller.go:210** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T09:14:59Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
```

  Absolutely, this is listed as a TODO and will be added in a follow-up PR

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T09:15:45Z

- **pkg/controller/concurrentadmission/controller.go:255** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T09:15:45Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if workload.GetParentWorkloadName(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", klog.KObj(variant), "parent", klog.KObj(parent), "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", klog.KObj(variant), "error", err)
+			return err
+		}
+		log.V(3).Info("Variant created", "variant", klog.KObj(variant), "flavor", flavor)
+		// TODO: Emit event here
```

  Yes, this will be implemented, but I want to keep the size of this PR minimal to make it easier to review. Will add it in a follow-up PR

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T09:21:14Z

- **pkg/controller/concurrentadmission/controller.go:113** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T09:21:14Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=clusterqueues,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch
```

  Fixed, thanks

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T09:21:25Z

- **pkg/controller/concurrentadmission/controller.go:25** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T09:21:25Z
```diff
@@ -0,0 +1,583 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	// "k8s.io/apimachinery/pkg/api/equality"
```

  Fixed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T10:31:43Z

First pass for the API . In the next phase I will review the new code isolation from the previous, and then the actual logic changes.

- **apis/kueue/v1beta2/clusterqueue_types.go:146** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:08:05Z
```diff
@@ -142,6 +142,10 @@ type ClusterQueueSpec struct {
 	// admissionScope indicates whether ClusterQueue uses the Admission Fair Sharing
 	// +optional
 	AdmissionScope *AdmissionScope `json:"admissionScope,omitempty"`
+
+	// concurrentAdmissionPolicy defines the policy for concurrent attempts.
```

  Let's expand the comment a bit with the main capabilitiies of the feature. I would mention that setting this field allows to upgrade the initially selected flavor during runtime. Allowing to cut down costs running the Job on a costly autoscaled flavor.

- **apis/kueue/v1beta2/clusterqueue_types.go:191** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:08:41Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
```

  Expand the comment to introduce what the variants are.

- **apis/kueue/v1beta2/clusterqueue_types.go:238** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:09:46Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
+	// +required
+	Mode ConcurrentAdmissionMigrationMode `json:"mode,omitempty"`
+
+	// constraints defines the constraints of Workload's migration.
+	//
+	// +optional
+	Constraints *ConcurrentAdmissionConstraints `json:"constraints,omitempty"`
+}
+
+type ConcurrentAdmissionConstraints struct {
+	// minPreferredFlavorName defines the minimal flavor a Workload can migrate to.
+	// The order is based on the order of flavors in ClusterQueue.
+	// It can only be used if the Mode is `UpgradeOnly` and `ExplicitVariants` is not specified.
+	// If the Mode is `UpgradeOnly` and MinPreferredFlavorName is not specified, then there's
+	// no constraints on what flavors a Workload can migrate to.
+	//
+	// +optional
+	MinPreferredFlavorName *ResourceFlavorReference `json:"minPreferredFlavorName,omitempty"`
+}
+
+// +kubebuilder:validation:MaxLength=253
+// +kubebuilder:validation:Enum=TryPreferredFlavors
+type ConcurrentAdmissionMigrationMode string
+
+const (
+	ConcurrentAdmissionTryPreferredFlavors ConcurrentAdmissionMigrationMode = "TryPreferredFlavors"
```

  Add a comment describing what this does.

- **apis/kueue/v1beta2/clusterqueue_types.go:211** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:10:28Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
```

  List the possible values, say "Possible values are: " and describe their semantics briefly.

- **apis/kueue/v1beta2/clusterqueue_types.go:214** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:11:49Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
+	// +required
+	Mode ConcurrentAdmissionMigrationMode `json:"mode,omitempty"`
+
+	// constraints defines the constraints of Workload's migration.
+	//
+	// +optional
+	Constraints *ConcurrentAdmissionConstraints `json:"constraints,omitempty"`
+}
+
+type ConcurrentAdmissionConstraints struct {
+	// minPreferredFlavorName defines the minimal flavor a Workload can migrate to.
+	// The order is based on the order of flavors in ClusterQueue.
+	// It can only be used if the Mode is `UpgradeOnly` and `ExplicitVariants` is not specified.
+	// If the Mode is `UpgradeOnly` and MinPreferredFlavorName is not specified, then there's
+	// no constraints on what flavors a Workload can migrate to.
```

  Let's avoid referencing modes which are not supported in the initial version. We can iterate on the comment as we expand the set of supported modes.
  
  Am I right that UpgradeOnly = TryPreferredFlavors ?

- **apis/kueue/v1beta2/workload_types.go:1002** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:20:43Z
```diff
@@ -996,6 +996,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// ConcurrentAdmissionReason indicates the Workload was preempted due to
+	// admission of more favorable Variant of the same Parent Workload.
+	ConcurrentAdmissionReason string = "ConcurrentAdmission"
```

  I would prefer not to overload the term "Preempted" for ConcurrentAdmission. the reason is that I expect most users consider Preemption to be mean "eviction to accomodate another workload". As such preemption is always initiated by the Preemptor Workload which needs to be admitted. This is not the case here.  Here we have "eviction to migrate the workload from previous flavor to the new one".
  
  Also, for the name of the reason I would suggest "FlavorMigration", or "MigrationToNewFlavor".

- **apis/kueue/v1beta2/workload_types.go:1002** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:21:43Z
```diff
@@ -996,6 +996,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// ConcurrentAdmissionReason indicates the Workload was preempted due to
+	// admission of more favorable Variant of the same Parent Workload.
+	ConcurrentAdmissionReason string = "ConcurrentAdmission"
```

  Let's also make sure the comment has the context at least briefly introducing what the "Variant" is and "Parent Workload".

- **pkg/controller/constants/constants.go:72** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:27:13Z
```diff
@@ -67,4 +67,13 @@ const (
 	// external controllers to adjust a workload's effective priority.
 	// Positive values increase priority; negative values decrease it.
 	PriorityBoostAnnotationKey = "kueue.x-k8s.io/priority-boost"
+
+	// WorkloadAllowedResourceFlavorAnnotation is an annotation used with ConcurrentAdmission feature
+	// It's set on a Workload level that defines which ResourceFlavors can be assigned to this Workload by Kueue scheduler
```

  Expand the comment to clarify this is comma separeted, similarly as we do for the [AdmissionGatedBy](https://github.com/kubernetes-sigs/kueue/blob/32c4babf8f793c3e19415e17fffabc2155d5d037/pkg/constants/constants.go#L70-L76)

- **pkg/controller/constants/constants.go:78** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:30:18Z
```diff
@@ -67,4 +67,13 @@ const (
 	// external controllers to adjust a workload's effective priority.
 	// Positive values increase priority; negative values decrease it.
 	PriorityBoostAnnotationKey = "kueue.x-k8s.io/priority-boost"
+
+	// WorkloadAllowedResourceFlavorAnnotation is an annotation used with ConcurrentAdmission feature
+	// It's set on a Workload level that defines which ResourceFlavors can be assigned to this Workload by Kueue scheduler
+	WorkloadAllowedResourceFlavorAnnotation = "kueue.x-k8s.io/workload-allowed-resource-flavors"
+
+	// ParentVariantLabel is the label key in the Workload that is a parent of Variants
+	// The value of this label is boolean, and it is set to "true" if the Workload is a parent of Variants.
+	// The label is used with ConcurrentAdmission feature.
+	ParentVariantLabel = "kueue.x-k8s.io/parent-variant"
```

  I think the name "ParentVariant" is a bit misleading, because it suggests the parent is one of the variants. However, in fact the parent workload is rather "coordinator" for the variants.
  
  I'm thinking of names like:  "kueue.x-k8s.io/concurrent-admission-parent". I don't have a strong view here, or don't claim the new name is necessarily better, but something catches my attention here as suboptimal. Let's see if we could iron this out.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T11:22:11Z

Ok I left comments for the changes on the boundary of the new and old codes to ensure good separation and minimize risks of introducing the new feature.

I will check the new controller itself later, probably tomorrow, but for Alpha the most important thing for me is to ensure we are not breaking what we have.

- **pkg/cache/queue/cluster_queue.go:255** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:43:10Z
```diff
@@ -250,6 +252,7 @@ func (c *ClusterQueue) Update(apiCQ *kueue.ClusterQueue) error {
 	}
 	c.namespaceSelector = nsSelector
 	c.active = apimeta.IsStatusConditionTrue(apiCQ.Status.Conditions, kueue.ClusterQueueActive)
+	c.ConcurrentAdmissionPolicy = apiCQ.Spec.ConcurrentAdmissionPolicy
```

  Wrap this with the feature gate check

- **pkg/cache/queue/cluster_queue.go:270** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:43:20Z
```diff
@@ -261,6 +264,10 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	defer c.rwm.Unlock()
 	added := false
 	for _, info := range q.items {
+		if workload.IsParentVariant(info.Obj) {
+			// Parent Workloads are not pushed onto heap
+			continue
+		}
```

  Wrap in the FG check

- **pkg/cache/queue/cluster_queue.go:286** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:43:42Z
```diff
@@ -272,9 +279,19 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	return added
 }
 
+func (c *ClusterQueue) ConcurrentAdmissionEnabled() bool {
```

  short circuit if the FG is disabled.

- **pkg/cache/queue/cluster_queue.go:291** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:43:51Z
```diff
@@ -272,9 +279,19 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	return added
 }
 
+func (c *ClusterQueue) ConcurrentAdmissionEnabled() bool {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	return c.ConcurrentAdmissionPolicy != nil
+}
+
 // PushOrUpdate pushes the workload to ClusterQueue.
 // If the workload is already present, updates with the new one.
 func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
+	if workload.IsParentVariant(wInfo.Obj) {
```

  Add the FG check

- **pkg/cache/queue/manager.go:507** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:44:11Z
```diff
@@ -504,7 +519,6 @@ func (m *Manager) PendingWorkloads(q *kueue.LocalQueue) (int32, error) {
 	if !ok {
 		return 0, ErrLocalQueueDoesNotExistOrInactive
 	}
-
```

  nit, revert

- **pkg/cache/scheduler/cache.go:680** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:48:35Z
```diff
@@ -674,7 +677,7 @@ func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Worklo
 	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
 
 	// Finished or deactivated workloads should not keep ClusterQueues in-use in the cache.
-	if !workload.HasActiveQuotaReservation(wl) {
+	if !workload.HasActiveQuotaReservation(wl) || workload.IsParentVariant(wl) {
```

  This looks fishy to check this condition here. Is it expected that the parent workloads are added to the `workloadAssignedQueues` in the first place? The HasActiveQuotaReservation I can understand, because QuotaReservetion is something that changes with the lifetime of a workload, but being Parent should not change with the lifetime.

- **pkg/controller/concurrentadmission/variants.go:32** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:50:14Z
```diff
@@ -0,0 +1,47 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func IsVariant(wl *kueue.Workload) bool {
+	if wl == nil {
+		return false
+	}
+	return workload.GetParentWorkloadName(wl) != ""
+}
+
+func getAdmittedVariant(variants []kueue.Workload) *kueue.Workload {
```

  Is the invariant of the feature to have only one Admitted workload at any given moment (I would assume so). If this is the case let's pass logger and log high if there are multiple workloads. Let's also change the function name to `getOnlyAdmittedVariant`

- **pkg/controller/core/workload_controller.go:251** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:50:35Z
```diff
@@ -238,6 +239,17 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
+	cqName, found := r.queues.ClusterQueueForWorkload(&wl)
+	if found {
+		enabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+		if enabled && !concurrentadmission.IsVariant(&wl) && !workload.IsParentVariant(&wl) {
+			// Workload is a Parent without set Parent annotation yet
+			concurrentadmission.SetParentVariantLabel(&wl)
+			err := r.client.Update(ctx, &wl)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	}
```

  Wrap the code in FG

- **pkg/workload/workload.go:1944** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:51:24Z
```diff
@@ -1873,3 +1938,7 @@ func TASAssignedNodeNames(wl *kueue.Workload) []string {
 	}
 	return nodesSet.UnsortedList()
 }
+
+func IsParentVariant(workload *kueue.Workload) bool {
+	return workload != nil && workload.Labels[controllerconstants.ParentVariantLabel] == "true"
+}
```

  Make sure all uses of IsParentVariant are wrapped in FG check, or check it here inside as short-circuit check.

- **pkg/controller/core/workload_controller.go:389** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:52:49Z
```diff
@@ -369,13 +381,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if workload.IsAdmissible(&wl) {
-			if err := r.queues.AddOrUpdateWorkload(log, wl.DeepCopy(), queueOptions...); err != nil {
-				log.V(2).Info("Failed to add DRA workload to queue", "error", err)
-				return ctrl.Result{}, err
+			if !workload.IsParentVariant(&wl) {
+				// at this point Workload Parent are for sure labeled
+				if err := r.queues.AddOrUpdateWorkload(log, wl.DeepCopy(), queueOptions...); err != nil {
+					log.V(2).Info("Failed to add DRA workload to queue", "error", err)
+					return ctrl.Result{}, err
+				}
```

  I see this pattern repeated `if !ParentVariant() {.queues.AddOrUpdateWorkload}`.
  
  Let's either:
  1. short cirtuit inside AddOrUpdateWorkload (preferred)
  2. introduce a helper functio to wrap that logic

- **pkg/controller/core/workload_controller.go:758** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:54:01Z
```diff
@@ -738,6 +755,10 @@ func buildAdmissionChecksMessage(checks []kueue.AdmissionCheckState, state kueue
 // reconcileCheckBasedEviction evicts or deactivates the given Workload if any admission checks have failed.
 // Returns true if the Workload was rejected or deactivated, and false otherwise.
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  Make sure the check for `IsParentVariant` is standardized - either wrap all uses with FG check (as here), or check it inside the function only. I'm ok with both approaches.

- **pkg/controller/core/workload_controller.go:791** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:55:30Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  Why is workload.IsParentVariant, while concurrentadmission.IsVariant. Let's move them both to  concurrentadmission package.

- **pkg/controller/core/workload_controller.go:791** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:55:50Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  So this will read concurrentadmission.IsVariant, and concurrentadmission.IsParent

- **pkg/controller/core/workload_controller.go:1084** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:56:14Z
```diff
@@ -1051,7 +1081,10 @@ func (r *WorkloadReconciler) Create(e event.TypedCreateEvent[*kueue.Workload]) b
 	log.V(2).Info("Workload create event")
 
 	if status == workload.StatusFinished {
-		r.queues.AddFinishedWorkload(e.Object)
+		if !concurrentadmission.IsVariant(e.Object) {
```

  Make sure the FG is checked here, either outside of inside.

- **pkg/controller/core/workload_controller.go:1104** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:57:27Z
```diff
@@ -1065,8 +1098,12 @@ func (r *WorkloadReconciler) Create(e event.TypedCreateEvent[*kueue.Workload]) b
 	}
 
 	if workload.IsAdmissible(e.Object) {
-		if err := r.queues.AddOrUpdateWorkload(log, wlCopy); err != nil {
-			log.V(2).Info("ignored an error for now", "error", err)
+		cqName, _ := r.queues.ClusterQueueForWorkload(e.Object)
+		concurrentAdmissionEnabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+		if !concurrentAdmissionEnabled || (concurrentAdmissionEnabled && concurrentadmission.IsVariant(wlCopy)) {
+			if err := r.queues.AddOrUpdateWorkload(log, wlCopy); err != nil {
```

  These checks are very verbose, can we move them inside  r.queues.AddOrUpdateWorkload, to avoid changing the code here?

- **pkg/controller/core/workload_controller.go:1176** by [@mimowo](https://github.com/mimowo) — 2026-04-28T10:58:18Z
```diff
@@ -1134,6 +1171,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
+	concurrentAdmissionEnabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+	// we need to prevent ParentVariants from being added to caches now as the Parent Workload may not have the label yet
```

  here also the diff is busy, can we move down the checks under the responsible functions, like AddFinishedWorkload

- **pkg/controller/jobframework/workload_names.go:70** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:00:14Z
```diff
@@ -65,6 +65,16 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	return GenerateWorkloadNameWithExtra(ownerName, ownerUID, ownerGVK, "")
 }
 
+func GetWorkloadNameForVariant(parentName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, flavor string) string {
+	// delete the existing hash from parentName
+	prefix := parentName[:strings.LastIndex(parentName, "-")]
```

  Extract this as a helper function getWorkloadBaseName

- **pkg/controller/jobframework/workload_names.go:75** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:04:24Z
```diff
@@ -65,6 +65,16 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	return GenerateWorkloadNameWithExtra(ownerName, ownerUID, ownerGVK, "")
 }
 
+func GetWorkloadNameForVariant(parentName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, flavor string) string {
+	// delete the existing hash from parentName
+	prefix := parentName[:strings.LastIndex(parentName, "-")]
+	// add flavor
+	prefixWithFlavor := truncate(fmt.Sprintf("%s-variant-%s", prefix, flavor), maxPrefixLength())
+	// get unique hash - parentName has already hash that includes Job's GVK so it won't colide in case different kinds of Job have the same name
+	hash := getHash(parentName, ownerUID, ownerGVK, flavor)[:hashLength]
+	return fmt.Sprintf("%s-%s", prefixWithFlavor, hash)
```

  deduplicate this part with https://github.com/kubernetes-sigs/kueue/pull/10610/changes#diff-dffb8328676ffbfbee72eb47d2348b292215cf5143a407a645019a28f951123dR89-R93 in a dedicated helper function, like `generateName(prefix, ownerUID, ownerGVK, extra)`

- **pkg/controller/tas/topology_ungater.go:297** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:05:41Z
```diff
@@ -285,15 +286,15 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 }
 
 func (r *topologyUngater) Create(event event.TypedCreateEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.Object)
+	return workload.IsAdmittedByTAS(event.Object) && !concurrentadmission.IsVariant(event.Object)
 }
 
 func (r *topologyUngater) Delete(event event.TypedDeleteEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.Object)
+	return workload.IsAdmittedByTAS(event.Object) && !concurrentadmission.IsVariant(event.Object)
 }
 
 func (r *topologyUngater) Update(event event.TypedUpdateEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.ObjectNew)
+	return workload.IsAdmittedByTAS(event.ObjectNew) && !concurrentadmission.IsVariant(event.ObjectNew)
```

  Let's introduce a helper to avoid duplicating the chcks, something like `shouldReconcile` that would internally check `workload.IsAdmittedByTAS(event.ObjectNew) && !concurrentadmission.IsVariant(event.ObjectNew)`

- **pkg/scheduler/flavorassigner/flavorassigner.go:831** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:06:08Z
```diff
@@ -818,6 +828,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !a.isFlavorAllowed(fName) {
```

  Make sure the FG is checked, either outside or inside the function

- **pkg/scheduler/scheduler.go:423** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:09:00Z
```diff
@@ -420,7 +420,7 @@ func (s *Scheduler) processEntry(
 	// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
 	preemptionTargets, oldWorkloadSlice := workloadslicing.FindReplacedSliceTarget(e.Obj, e.preemptionTargets)
 
-	if mode == flavorassigner.Preempt {
+	if mode == flavorassigner.Preempt || len(preemptionTargets) > 0 {
```

  I would prefer not to use `IssuePreemptions` for this use-case. I don't think what we are doing is preemption, and all the logs and events will be misleading (IMO). 
  
  Preemption in my mind is to make room for incoming workload. This is not whar we are doing here. 
  
  This use case is more similar to eviction due to Multiple Unhealthy Nodes, or Finishing due to NodeReplacement for Workload Slices.

- **pkg/scheduler/scheduler.go:703** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:09:46Z
```diff
@@ -697,6 +700,51 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSiblings(log logr.Logger, wl *workload.Info, assignment flavorassigner.Assignment, snap *schdcache.Snapshot) []*preemption.Target {
```

  Why / when multiple eviciton targets could exist? Or is it just defensive programming? IIUC we should ensure invariant that at most one workload is admitted at any given time.

- **pkg/scheduler/scheduler.go:666** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:10:46Z
```diff
@@ -662,6 +662,9 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, fairsharing.Enabled(s.fairSharing), preemption.NewOracle(s.preemptor, snap), replaceableWorkloadSlice)
 	fullAssignment := flvAssigner.Assign(log, nil)
 
+	// Add less favorable siblings to preemption targets once here.
+	preemptionTargets = append(preemptionTargets, s.getLessFavorableSiblings(log, wl, fullAssignment, snap)...)
```

  As mentioned in other comments, don't use preemption here. I understand this is similar, but I think this is just Eviction, with the reason "FlavorMigration",or  "MigrationToNewFlavor".

- **pkg/scheduler/scheduler.go:703** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:11:12Z
```diff
@@ -697,6 +700,51 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSiblings(log logr.Logger, wl *workload.Info, assignment flavorassigner.Assignment, snap *schdcache.Snapshot) []*preemption.Target {
```

  Make sure the new code is guarded by FG. I couldn't find it in any obvious way.

- **pkg/webhooks/clusterqueue_webhook.go:230** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:11:36Z
```diff
@@ -221,6 +227,25 @@ func validateFlavorResourceCombinations(resourceGroups []kueue.ResourceGroup, pa
 	return allErrs
 }
 
+func validateConcurrentAdmissionPolicy(cq *kueue.ClusterQueue, path *field.Path) field.ErrorList {
```

  Make sure this is short-circuit by FG check.

- **pkg/workload/workload.go:1369** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:12:12Z
```diff
@@ -1319,6 +1333,41 @@ func IsAdmissible(w *kueue.Workload) bool {
 	return !HasAdmissionGate(w) && !IsFinished(w) && IsActive(w) && !HasQuotaReservation(w)
 }
 
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+}
```

  I think all these functions rather belong to the new concurrentadmission package

- **pkg/workload/workload.go:1746** by [@mimowo](https://github.com/mimowo) — 2026-04-28T11:20:03Z
```diff
@@ -1680,6 +1729,22 @@ func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, ms
 	return nil
 }
 
+func Activate(ctx context.Context, c client.Client, wl *kueue.Workload) error {
+	if wl == nil || IsActive(wl) {
+		return nil
+	}
+	wl.Spec.Active = new(true)
+	return c.Update(ctx, wl)
+}
+
+func Deactivate(ctx context.Context, c client.Client, wl *kueue.Workload) error {
+	if wl == nil || !IsActive(wl) {
+		return nil
+	}
+	wl.Spec.Active = new(false)
+	return c.Update(ctx, wl)
+}
```

  I'm not yet sure how these are generic, because in most cases Kueue would deactivate  workloads using DeactivationTarget. So I would rather move the functions into the controller for now, becasue I'm not clear what is the potential for reuse.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T11:42:31Z

- **pkg/cache/scheduler/cache.go:680** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T11:42:31Z
```diff
@@ -674,7 +677,7 @@ func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Worklo
 	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
 
 	// Finished or deactivated workloads should not keep ClusterQueues in-use in the cache.
-	if !workload.HasActiveQuotaReservation(wl) {
+	if !workload.HasActiveQuotaReservation(wl) || workload.IsParentVariant(wl) {
```

  This is to handle the case where the ClusterQueue is added after the Workload was created and reserved quota, e.g. on the Kueue restart. More explicitly, if
  1) the Parent has quota reserved condition (which would be perfectly valid if its Variant has been admitted) 
  2) Kueue restarts
  3) ClusterQueue is being added to cache
  Then this function will be called, and we don't want to add the Workload to cache. Alternatively I can move this check a bit earlier, e.g. here: https://github.com/PBundyra/kueue/blob/pca-static-mvp/pkg/cache/scheduler/cache.go#L460
  
  >  Is it expected that the parent workloads are added to the workloadAssignedQueues in the first place?
  
   It shouldn't be present in the `workloadAssignedQueues` but the code that adds the Workload to the cache doesn't rely on that presence. It's only important from the perspective of deleting the Workload from cache.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T11:47:24Z

- **pkg/controller/core/workload_controller.go:791** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T11:47:23Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  The `IsParentVariant` is put in `workload` package to prevent dependency cycles

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T11:48:56Z

- **pkg/controller/core/workload_controller.go:791** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T11:48:56Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  E.g. concurrentadmission -> jobframework -> scheduler (which uses `IsParentVariant`)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T12:54:07Z

- **pkg/controller/core/workload_controller.go:791** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T12:54:06Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  I'm happy to put both of them in the workload package so they are easy to use across the codebase

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:01:24Z

- **pkg/cache/queue/cluster_queue.go:255** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:01:24Z
```diff
@@ -250,6 +252,7 @@ func (c *ClusterQueue) Update(apiCQ *kueue.ClusterQueue) error {
 	}
 	c.namespaceSelector = nsSelector
 	c.active = apimeta.IsStatusConditionTrue(apiCQ.Status.Conditions, kueue.ClusterQueueActive)
+	c.ConcurrentAdmissionPolicy = apiCQ.Spec.ConcurrentAdmissionPolicy
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:02:01Z

- **pkg/cache/queue/cluster_queue.go:270** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:02:01Z
```diff
@@ -261,6 +264,10 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	defer c.rwm.Unlock()
 	added := false
 	for _, info := range q.items {
+		if workload.IsParentVariant(info.Obj) {
+			// Parent Workloads are not pushed onto heap
+			continue
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:02:17Z

- **pkg/cache/queue/cluster_queue.go:286** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:02:17Z
```diff
@@ -272,9 +279,19 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	return added
 }
 
+func (c *ClusterQueue) ConcurrentAdmissionEnabled() bool {
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:02:32Z

- **pkg/cache/queue/cluster_queue.go:291** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:02:31Z
```diff
@@ -272,9 +279,19 @@ func (c *ClusterQueue) AddFromLocalQueue(q *LocalQueue, roleTracker *roletracker
 	return added
 }
 
+func (c *ClusterQueue) ConcurrentAdmissionEnabled() bool {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+	return c.ConcurrentAdmissionPolicy != nil
+}
+
 // PushOrUpdate pushes the workload to ClusterQueue.
 // If the workload is already present, updates with the new one.
 func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
+	if workload.IsParentVariant(wInfo.Obj) {
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:03:19Z

- **pkg/controller/core/workload_controller.go:251** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:03:19Z
```diff
@@ -238,6 +239,17 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
+	cqName, found := r.queues.ClusterQueueForWorkload(&wl)
+	if found {
+		enabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+		if enabled && !concurrentadmission.IsVariant(&wl) && !workload.IsParentVariant(&wl) {
+			// Workload is a Parent without set Parent annotation yet
+			concurrentadmission.SetParentVariantLabel(&wl)
+			err := r.client.Update(ctx, &wl)
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+	}
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:03:52Z

- **pkg/controller/core/workload_controller.go:758** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:03:52Z
```diff
@@ -738,6 +755,10 @@ func buildAdmissionChecksMessage(checks []kueue.AdmissionCheckState, state kueue
 // reconcileCheckBasedEviction evicts or deactivates the given Workload if any admission checks have failed.
 // Returns true if the Workload was rejected or deactivated, and false otherwise.
 func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl *kueue.Workload) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  Wrapped every call

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:04:32Z

- **pkg/controller/core/workload_controller.go:791** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:04:32Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  Moved, so both funcs are in `workload` package

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:05:59Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:831** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:05:59Z
```diff
@@ -818,6 +828,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !a.isFlavorAllowed(fName) {
```

  Added check inside

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:06:19Z

- **pkg/webhooks/clusterqueue_webhook.go:230** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:06:18Z
```diff
@@ -221,6 +227,25 @@ func validateFlavorResourceCombinations(resourceGroups []kueue.ResourceGroup, pa
 	return allErrs
 }
 
+func validateConcurrentAdmissionPolicy(cq *kueue.ClusterQueue, path *field.Path) field.ErrorList {
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:06:53Z

- **pkg/workload/workload.go:1944** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:06:53Z
```diff
@@ -1873,3 +1938,7 @@ func TASAssignedNodeNames(wl *kueue.Workload) []string {
 	}
 	return nodesSet.UnsortedList()
 }
+
+func IsParentVariant(workload *kueue.Workload) bool {
+	return workload != nil && workload.Labels[controllerconstants.ParentVariantLabel] == "true"
+}
```

  Wrapped every use in FG check

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:12:33Z

- **pkg/controller/core/workload_controller.go:1176** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:12:33Z
```diff
@@ -1134,6 +1171,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
+	concurrentAdmissionEnabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+	// we need to prevent ParentVariants from being added to caches now as the Parent Workload may not have the label yet
```

  I pushed down most of the checks, but we cannot do it everywhere elegantly. We recognize if a Workload is a Parent Workload by presence of the parent-variant label. This label is added in the Reconcile function, so in some interleavings it may happen that e.g. `Update` event handler code is called before Reconcile function managed to label the Parent Workload. Hence we need to fallback to a check that depends on if a CQ has ConcurrentAdmission feature is enabled, if the Workload is Variant. I've added relevant comments to signal such possibility

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:42:23Z

- **pkg/workload/workload.go:1746** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:42:23Z
```diff
@@ -1680,6 +1729,22 @@ func Finish(ctx context.Context, c client.Client, wl *kueue.Workload, reason, ms
 	return nil
 }
 
+func Activate(ctx context.Context, c client.Client, wl *kueue.Workload) error {
+	if wl == nil || IsActive(wl) {
+		return nil
+	}
+	wl.Spec.Active = new(true)
+	return c.Update(ctx, wl)
+}
+
+func Deactivate(ctx context.Context, c client.Client, wl *kueue.Workload) error {
+	if wl == nil || !IsActive(wl) {
+		return nil
+	}
+	wl.Spec.Active = new(false)
+	return c.Update(ctx, wl)
+}
```

  Moved

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:43:18Z

- **pkg/controller/core/workload_controller.go:389** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:43:18Z
```diff
@@ -369,13 +381,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		if workload.IsAdmissible(&wl) {
-			if err := r.queues.AddOrUpdateWorkload(log, wl.DeepCopy(), queueOptions...); err != nil {
-				log.V(2).Info("Failed to add DRA workload to queue", "error", err)
-				return ctrl.Result{}, err
+			if !workload.IsParentVariant(&wl) {
+				// at this point Workload Parent are for sure labeled
+				if err := r.queues.AddOrUpdateWorkload(log, wl.DeepCopy(), queueOptions...); err != nil {
+					log.V(2).Info("Failed to add DRA workload to queue", "error", err)
+					return ctrl.Result{}, err
+				}
```

  Refactored

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:43:31Z

- **pkg/controller/core/workload_controller.go:1084** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:43:31Z
```diff
@@ -1051,7 +1081,10 @@ func (r *WorkloadReconciler) Create(e event.TypedCreateEvent[*kueue.Workload]) b
 	log.V(2).Info("Workload create event")
 
 	if status == workload.StatusFinished {
-		r.queues.AddFinishedWorkload(e.Object)
+		if !concurrentadmission.IsVariant(e.Object) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:43:48Z

- **pkg/controller/core/workload_controller.go:1104** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:43:48Z
```diff
@@ -1065,8 +1098,12 @@ func (r *WorkloadReconciler) Create(e event.TypedCreateEvent[*kueue.Workload]) b
 	}
 
 	if workload.IsAdmissible(e.Object) {
-		if err := r.queues.AddOrUpdateWorkload(log, wlCopy); err != nil {
-			log.V(2).Info("ignored an error for now", "error", err)
+		cqName, _ := r.queues.ClusterQueueForWorkload(e.Object)
+		concurrentAdmissionEnabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+		if !concurrentAdmissionEnabled || (concurrentAdmissionEnabled && concurrentadmission.IsVariant(wlCopy)) {
+			if err := r.queues.AddOrUpdateWorkload(log, wlCopy); err != nil {
```

  Moved most of them, see my other comment for more details

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:43:58Z

- **pkg/controller/jobframework/workload_names.go:70** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:43:58Z
```diff
@@ -65,6 +65,16 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	return GenerateWorkloadNameWithExtra(ownerName, ownerUID, ownerGVK, "")
 }
 
+func GetWorkloadNameForVariant(parentName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, flavor string) string {
+	// delete the existing hash from parentName
+	prefix := parentName[:strings.LastIndex(parentName, "-")]
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:44:04Z

- **pkg/controller/jobframework/workload_names.go:75** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:44:04Z
```diff
@@ -65,6 +65,16 @@ func GetWorkloadNameForOwnerWithGVK(ownerName string, ownerUID types.UID, ownerG
 	return GenerateWorkloadNameWithExtra(ownerName, ownerUID, ownerGVK, "")
 }
 
+func GetWorkloadNameForVariant(parentName string, ownerUID types.UID, ownerGVK schema.GroupVersionKind, flavor string) string {
+	// delete the existing hash from parentName
+	prefix := parentName[:strings.LastIndex(parentName, "-")]
+	// add flavor
+	prefixWithFlavor := truncate(fmt.Sprintf("%s-variant-%s", prefix, flavor), maxPrefixLength())
+	// get unique hash - parentName has already hash that includes Job's GVK so it won't colide in case different kinds of Job have the same name
+	hash := getHash(parentName, ownerUID, ownerGVK, flavor)[:hashLength]
+	return fmt.Sprintf("%s-%s", prefixWithFlavor, hash)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:44:11Z

- **pkg/controller/tas/topology_ungater.go:297** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:44:11Z
```diff
@@ -285,15 +286,15 @@ func (r *topologyUngater) Reconcile(ctx context.Context, req reconcile.Request)
 }
 
 func (r *topologyUngater) Create(event event.TypedCreateEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.Object)
+	return workload.IsAdmittedByTAS(event.Object) && !concurrentadmission.IsVariant(event.Object)
 }
 
 func (r *topologyUngater) Delete(event event.TypedDeleteEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.Object)
+	return workload.IsAdmittedByTAS(event.Object) && !concurrentadmission.IsVariant(event.Object)
 }
 
 func (r *topologyUngater) Update(event event.TypedUpdateEvent[*kueue.Workload]) bool {
-	return workload.IsAdmittedByTAS(event.ObjectNew)
+	return workload.IsAdmittedByTAS(event.ObjectNew) && !concurrentadmission.IsVariant(event.ObjectNew)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:45:35Z

- **pkg/controller/constants/constants.go:78** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:45:34Z
```diff
@@ -67,4 +67,13 @@ const (
 	// external controllers to adjust a workload's effective priority.
 	// Positive values increase priority; negative values decrease it.
 	PriorityBoostAnnotationKey = "kueue.x-k8s.io/priority-boost"
+
+	// WorkloadAllowedResourceFlavorAnnotation is an annotation used with ConcurrentAdmission feature
+	// It's set on a Workload level that defines which ResourceFlavors can be assigned to this Workload by Kueue scheduler
+	WorkloadAllowedResourceFlavorAnnotation = "kueue.x-k8s.io/workload-allowed-resource-flavors"
+
+	// ParentVariantLabel is the label key in the Workload that is a parent of Variants
+	// The value of this label is boolean, and it is set to "true" if the Workload is a parent of Variants.
+	// The label is used with ConcurrentAdmission feature.
+	ParentVariantLabel = "kueue.x-k8s.io/parent-variant"
```

  Aligned with the comment

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:45:42Z

- **pkg/controller/constants/constants.go:72** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:45:42Z
```diff
@@ -67,4 +67,13 @@ const (
 	// external controllers to adjust a workload's effective priority.
 	// Positive values increase priority; negative values decrease it.
 	PriorityBoostAnnotationKey = "kueue.x-k8s.io/priority-boost"
+
+	// WorkloadAllowedResourceFlavorAnnotation is an annotation used with ConcurrentAdmission feature
+	// It's set on a Workload level that defines which ResourceFlavors can be assigned to this Workload by Kueue scheduler
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:46:16Z

- **apis/kueue/v1beta2/clusterqueue_types.go:214** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:46:16Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
+	// +required
+	Mode ConcurrentAdmissionMigrationMode `json:"mode,omitempty"`
+
+	// constraints defines the constraints of Workload's migration.
+	//
+	// +optional
+	Constraints *ConcurrentAdmissionConstraints `json:"constraints,omitempty"`
+}
+
+type ConcurrentAdmissionConstraints struct {
+	// minPreferredFlavorName defines the minimal flavor a Workload can migrate to.
+	// The order is based on the order of flavors in ClusterQueue.
+	// It can only be used if the Mode is `UpgradeOnly` and `ExplicitVariants` is not specified.
+	// If the Mode is `UpgradeOnly` and MinPreferredFlavorName is not specified, then there's
+	// no constraints on what flavors a Workload can migrate to.
```

  > Am I right that UpgradeOnly = TryPreferredFlavors ?
  
  Exactly. Improved the comment

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:46:24Z

- **apis/kueue/v1beta2/clusterqueue_types.go:211** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:46:23Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
```

  Added

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:46:30Z

- **apis/kueue/v1beta2/clusterqueue_types.go:238** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:46:30Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
+	//
+	// +required
+	Migration ConcurrentAdmissionMigration `json:"migration,omitzero"`
+}
+
+type ConcurrentAdmissionMigration struct {
+	// mode defines the mode of Workload's migration.
+	//
+	// +required
+	Mode ConcurrentAdmissionMigrationMode `json:"mode,omitempty"`
+
+	// constraints defines the constraints of Workload's migration.
+	//
+	// +optional
+	Constraints *ConcurrentAdmissionConstraints `json:"constraints,omitempty"`
+}
+
+type ConcurrentAdmissionConstraints struct {
+	// minPreferredFlavorName defines the minimal flavor a Workload can migrate to.
+	// The order is based on the order of flavors in ClusterQueue.
+	// It can only be used if the Mode is `UpgradeOnly` and `ExplicitVariants` is not specified.
+	// If the Mode is `UpgradeOnly` and MinPreferredFlavorName is not specified, then there's
+	// no constraints on what flavors a Workload can migrate to.
+	//
+	// +optional
+	MinPreferredFlavorName *ResourceFlavorReference `json:"minPreferredFlavorName,omitempty"`
+}
+
+// +kubebuilder:validation:MaxLength=253
+// +kubebuilder:validation:Enum=TryPreferredFlavors
+type ConcurrentAdmissionMigrationMode string
+
+const (
+	ConcurrentAdmissionTryPreferredFlavors ConcurrentAdmissionMigrationMode = "TryPreferredFlavors"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:46:37Z

- **apis/kueue/v1beta2/clusterqueue_types.go:191** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:46:37Z
```diff
@@ -183,6 +187,44 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints of Variants migration
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T14:47:32Z

- **apis/kueue/v1beta2/clusterqueue_types.go:146** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T14:47:32Z
```diff
@@ -142,6 +142,10 @@ type ClusterQueueSpec struct {
 	// admissionScope indicates whether ClusterQueue uses the Admission Fair Sharing
 	// +optional
 	AdmissionScope *AdmissionScope `json:"admissionScope,omitempty"`
+
+	// concurrentAdmissionPolicy defines the policy for concurrent attempts.
```

  Expanded

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T15:04:58Z

- **pkg/controller/core/workload_controller.go:1166** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T15:04:59Z
```diff
@@ -1134,6 +1163,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
```

  It looks like we should execute this only if the feature is enabled. Can we wrap it in an if?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T15:05:56Z

- **pkg/controller/core/workload_controller.go:1203** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T15:05:56Z
```diff
@@ -1161,7 +1200,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	case prevStatus == workload.StatusPending && status == workload.StatusPending:
 		if dra.NeedsDRAReconcile(e.ObjectNew) {
 			log.V(2).Info("Skipping queue update for DRA workload - handled in Reconcile")
-		} else {
+		} else if !concurrentAdmissionEnabled || (concurrentAdmissionEnabled && workload.IsVariant(wlCopy)) {
```

  ```suggestion
  		} else if !concurrentAdmissionEnabled || workload.IsVariant(wlCopy) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T15:15:13Z

- **pkg/controller/core/workload_controller.go:1166** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T15:15:13Z
```diff
@@ -1134,6 +1163,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
```

  Or we could create a helper function like ConcurrentAdmissionEnabledFor and use it in both cases:
  
  ```
  if !ConcurrentAdmissionEnabledFor(wlCopy) || workload.IsVariant(wlCopy) {
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T15:18:24Z

- **pkg/controller/core/workload_controller.go:1166** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T15:18:24Z
```diff
@@ -1134,6 +1163,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
```

  I think like this:
  
  ```go
  func (m *Manager) ConcurrentAdmissionEnabledFor(wl *kueue.Workload) bool {
  	if !features.Enabled(features.ConcurrentAdmission) {
  		return false
  	}
  	cqName, _ := m.ClusterQueueForWorkload(wl)
  	return m.ConcurrentAdmissionEnabled(cqName)
  }
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-28T15:22:22Z

- **pkg/controller/core/workload_controller.go:1166** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-28T15:22:22Z
```diff
@@ -1134,6 +1163,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
```

  Probably we also can merge ConcurrentAdmissionEnabled() and ConcurrentAdmissionEnabledFor() and optimize other places.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T15:56:45Z

- **pkg/cache/queue/manager.go:578** by [@mimowo](https://github.com/mimowo) — 2026-04-28T15:56:45Z
```diff
@@ -546,6 +571,12 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && workload.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
```

  This looks wrong, so you check some condisions, and if they are satisfied, then call `m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)`. If the conditions are not satisfied then... the same function is called with the same parameters.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T16:00:19Z

- **pkg/cache/scheduler/cache.go:680** by [@mimowo](https://github.com/mimowo) — 2026-04-28T16:00:19Z
```diff
@@ -674,7 +677,7 @@ func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Worklo
 	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
 
 	// Finished or deactivated workloads should not keep ClusterQueues in-use in the cache.
-	if !workload.HasActiveQuotaReservation(wl) {
+	if !workload.HasActiveQuotaReservation(wl) || workload.IsParentVariant(wl) {
```

  Hm, I'm still confused why we delete the parent from cache rather than preventing its addition in the first place.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:06:31Z

- **pkg/cache/queue/manager.go:578** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:06:31Z
```diff
@@ -546,6 +571,12 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && workload.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
```

  Good point, I've missed the return. Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:06:37Z

- **pkg/controller/core/workload_controller.go:1203** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:06:37Z
```diff
@@ -1161,7 +1200,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	case prevStatus == workload.StatusPending && status == workload.StatusPending:
 		if dra.NeedsDRAReconcile(e.ObjectNew) {
 			log.V(2).Info("Skipping queue update for DRA workload - handled in Reconcile")
-		} else {
+		} else if !concurrentAdmissionEnabled || (concurrentAdmissionEnabled && workload.IsVariant(wlCopy)) {
```

  Good point, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:08:41Z

- **pkg/controller/core/workload_controller.go:1166** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:08:40Z
```diff
@@ -1134,6 +1163,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
```

  Added the helper func

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:09:07Z

- **pkg/scheduler/scheduler.go:703** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:09:07Z
```diff
@@ -697,6 +700,51 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSiblings(log logr.Logger, wl *workload.Info, assignment flavorassigner.Assignment, snap *schdcache.Snapshot) []*preemption.Target {
```

  Added a guard

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:09:19Z

- **pkg/cache/queue/manager.go:507** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:09:19Z
```diff
@@ -504,7 +519,6 @@ func (m *Manager) PendingWorkloads(q *kueue.LocalQueue) (int32, error) {
 	if !ok {
 		return 0, ErrLocalQueueDoesNotExistOrInactive
 	}
-
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:10:51Z

- **pkg/cache/scheduler/cache.go:680** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:10:51Z
```diff
@@ -674,7 +677,7 @@ func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Worklo
 	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
 
 	// Finished or deactivated workloads should not keep ClusterQueues in-use in the cache.
-	if !workload.HasActiveQuotaReservation(wl) {
+	if !workload.HasActiveQuotaReservation(wl) || workload.IsParentVariant(wl) {
```

  Right, we do prevent its addition in the first place. I can move this if statement to the very top of this function and exit early as well

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T16:14:13Z

- **pkg/workload/workload.go:1369** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T16:14:12Z
```diff
@@ -1319,6 +1333,41 @@ func IsAdmissible(w *kueue.Workload) bool {
 	return !HasAdmissionGate(w) && !IsFinished(w) && IsActive(w) && !HasQuotaReservation(w)
 }
 
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	annotations := wl.GetAnnotations()
+	if annotations == nil {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(annotations[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+}
```

  The reason those functions are in the workload package is scheduler also calls them, and scheduler cannot import concurrentadmission package due to dependency cycles

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T16:35:40Z

- **pkg/controller/concurrentadmission/controller_test.go:1045** by [@mimowo](https://github.com/mimowo) — 2026-04-28T16:35:40Z
```diff
@@ -0,0 +1,1062 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	testingclock "k8s.io/utils/clock/testing"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+var (
+	workloadCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion", "ObjectMeta.UID", "Status.AccumulatedPastExecutionTimeSeconds", "Status.SchedulingStats",
+		),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.SortSlices(func(a, b kueue.Workload) bool { return a.Name < b.Name }),
+		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	defaultCQ := utiltestingapi.MakeClusterQueue("cq").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	defaultLQ := utiltestingapi.MakeLocalQueue("lq", "default").ClusterQueue("cq").Obj()
+
+	migrationCQ := utiltestingapi.MakeClusterQueue("cq-migration").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		MinPreferredFlavorName("reservation").
+		Obj()
+	migrationLQ := utiltestingapi.MakeLocalQueue("lq-migration", "default").ClusterQueue("cq-migration").Obj()
+
+	migrationCQNoConstraint := utiltestingapi.MakeClusterQueue("cq-migration-no-constraint").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	migrationLQNoConstraint := utiltestingapi.MakeLocalQueue("lq-migration-no-constraint", "default").ClusterQueue("cq-migration-no-constraint").Obj()
+
+	testCases := map[string]struct {
+		parentWorkload       *kueue.Workload
+		variantWorkloads     []kueue.Workload
+		wantParentWorkload   *kueue.Workload
+		wantVariantWorkloads []kueue.Workload
+		req                  reconcile.Request
+		wantResult           reconcile.Result
+		wantErr              bool
+	}{
+		"workload not found": {
+			req: reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: "default",
+					Name:      "non-existing",
+				},
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"workload is not parent or variant": {
+			parentWorkload:     utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantResult:         reconcile.Result{},
+			wantErr:            false,
+		},
+		"parent workload without variants creates them": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot-545ad", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"parent workload with missing variants; creates missing": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant syncs admission to parent": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has quota; evict parent and wait": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadEvicted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "VariantEvicted",
+					Message: "Admitted variant was evicted",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has no quota reservation; clear variant reservation": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Preempted",
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(true).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"with minTargetFlavor=reservation, variant admitted on spot, deactivate on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					AllowedFlavors("reservation").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on spot, nothing deactivated": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"parent marked finished, mark all variants finished": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+			},
+		},
+		"parent is not active, propagating deactivation to all variants": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.ConcurrentAdmission, true)
+			var objects []client.Object
+			if tc.parentWorkload != nil {
+				objects = append(objects, tc.parentWorkload)
+			}
+			for i := range tc.variantWorkloads {
+				objects = append(objects, &tc.variantWorkloads[i])
+			}
+			cl := utiltesting.NewClientBuilder().
+				WithObjects(objects...).
+				WithStatusSubresource(objects...).
+				Build()
+			preemptionExpectations := preemptexpectations.New()
+			qManager := qcache.NewManagerForUnitTests(cl, nil, qcache.WithPreemptionExpectations(preemptionExpectations))
+			roleTracker := roletracker.NewFakeRoleTracker(roletracker.RoleLeader)
+
+			cqs := []*kueue.ClusterQueue{defaultCQ.DeepCopy(), migrationCQ.DeepCopy(), migrationCQNoConstraint.DeepCopy()}
+			lqs := []*kueue.LocalQueue{defaultLQ.DeepCopy(), migrationLQ.DeepCopy(), migrationLQNoConstraint.DeepCopy()}
+
+			for _, cq := range cqs {
+				if err := cl.Create(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddClusterQueue(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for _, lq := range lqs {
+				if err := cl.Create(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddLocalQueue(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for i := range tc.variantWorkloads {
+				_ = qManager.AddOrUpdateWorkload(ctrl.Log, tc.variantWorkloads[i].DeepCopy())
+			}
+
+			r := &variantReconciler{
+				logName:     ConcurrentAdmissionController,
+				client:      cl,
+				queues:      qManager,
+				roleTracker: roleTracker,
+				clock:       testingclock.NewFakeClock(metav1.Now().Time),
+				recorder:    record.NewFakeRecorder(10),
+			}
+
+			req := tc.req
+			if req.Name == "" && tc.parentWorkload != nil {
+				req = reconcile.Request{
+					NamespacedName: types.NamespacedName{
+						Namespace: tc.parentWorkload.Namespace,
+						Name:      tc.parentWorkload.Name,
+					},
+				}
+			}
+
+			got, err := r.Reconcile(t.Context(), req)
+			if (err != nil) != tc.wantErr {
+				t.Errorf("Reconcile() error = %v, wantErr %v", err, tc.wantErr)
+				return
+			}
+			if !cmp.Equal(got, tc.wantResult) {
+				t.Errorf("Reconcile() got = %v, want %v", got, tc.wantResult)
+			}
+
+			if tc.wantParentWorkload != nil {
+				var gotParent kueue.Workload
+				err := cl.Get(t.Context(), types.NamespacedName{Namespace: tc.wantParentWorkload.Namespace, Name: tc.wantParentWorkload.Name}, &gotParent)
+				if err != nil {
+					t.Fatal(err)
+				}
+				if diff := cmp.Diff(tc.wantParentWorkload, &gotParent, workloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected parent workload (-want +got):\n%s", diff)
+				}
+			}
+
+			var gotVariants kueue.WorkloadList
```

  So `gotVariants` isn't variants, it is allWorkloads?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T16:35:56Z

- **pkg/controller/concurrentadmission/controller_test.go:1054** by [@mimowo](https://github.com/mimowo) — 2026-04-28T16:35:57Z
```diff
@@ -0,0 +1,1062 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	testingclock "k8s.io/utils/clock/testing"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+var (
+	workloadCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion", "ObjectMeta.UID", "Status.AccumulatedPastExecutionTimeSeconds", "Status.SchedulingStats",
+		),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.SortSlices(func(a, b kueue.Workload) bool { return a.Name < b.Name }),
+		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	defaultCQ := utiltestingapi.MakeClusterQueue("cq").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	defaultLQ := utiltestingapi.MakeLocalQueue("lq", "default").ClusterQueue("cq").Obj()
+
+	migrationCQ := utiltestingapi.MakeClusterQueue("cq-migration").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		MinPreferredFlavorName("reservation").
+		Obj()
+	migrationLQ := utiltestingapi.MakeLocalQueue("lq-migration", "default").ClusterQueue("cq-migration").Obj()
+
+	migrationCQNoConstraint := utiltestingapi.MakeClusterQueue("cq-migration-no-constraint").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	migrationLQNoConstraint := utiltestingapi.MakeLocalQueue("lq-migration-no-constraint", "default").ClusterQueue("cq-migration-no-constraint").Obj()
+
+	testCases := map[string]struct {
+		parentWorkload       *kueue.Workload
+		variantWorkloads     []kueue.Workload
+		wantParentWorkload   *kueue.Workload
+		wantVariantWorkloads []kueue.Workload
+		req                  reconcile.Request
+		wantResult           reconcile.Result
+		wantErr              bool
+	}{
+		"workload not found": {
+			req: reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: "default",
+					Name:      "non-existing",
+				},
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"workload is not parent or variant": {
+			parentWorkload:     utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantResult:         reconcile.Result{},
+			wantErr:            false,
+		},
+		"parent workload without variants creates them": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot-545ad", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"parent workload with missing variants; creates missing": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant syncs admission to parent": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has quota; evict parent and wait": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadEvicted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "VariantEvicted",
+					Message: "Admitted variant was evicted",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has no quota reservation; clear variant reservation": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Preempted",
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(true).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"with minTargetFlavor=reservation, variant admitted on spot, deactivate on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					AllowedFlavors("reservation").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on spot, nothing deactivated": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"parent marked finished, mark all variants finished": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+			},
+		},
+		"parent is not active, propagating deactivation to all variants": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.ConcurrentAdmission, true)
+			var objects []client.Object
+			if tc.parentWorkload != nil {
+				objects = append(objects, tc.parentWorkload)
+			}
+			for i := range tc.variantWorkloads {
+				objects = append(objects, &tc.variantWorkloads[i])
+			}
+			cl := utiltesting.NewClientBuilder().
+				WithObjects(objects...).
+				WithStatusSubresource(objects...).
+				Build()
+			preemptionExpectations := preemptexpectations.New()
+			qManager := qcache.NewManagerForUnitTests(cl, nil, qcache.WithPreemptionExpectations(preemptionExpectations))
+			roleTracker := roletracker.NewFakeRoleTracker(roletracker.RoleLeader)
+
+			cqs := []*kueue.ClusterQueue{defaultCQ.DeepCopy(), migrationCQ.DeepCopy(), migrationCQNoConstraint.DeepCopy()}
+			lqs := []*kueue.LocalQueue{defaultLQ.DeepCopy(), migrationLQ.DeepCopy(), migrationLQNoConstraint.DeepCopy()}
+
+			for _, cq := range cqs {
+				if err := cl.Create(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddClusterQueue(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for _, lq := range lqs {
+				if err := cl.Create(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddLocalQueue(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for i := range tc.variantWorkloads {
+				_ = qManager.AddOrUpdateWorkload(ctrl.Log, tc.variantWorkloads[i].DeepCopy())
+			}
+
+			r := &variantReconciler{
+				logName:     ConcurrentAdmissionController,
+				client:      cl,
+				queues:      qManager,
+				roleTracker: roleTracker,
+				clock:       testingclock.NewFakeClock(metav1.Now().Time),
+				recorder:    record.NewFakeRecorder(10),
+			}
+
+			req := tc.req
+			if req.Name == "" && tc.parentWorkload != nil {
+				req = reconcile.Request{
+					NamespacedName: types.NamespacedName{
+						Namespace: tc.parentWorkload.Namespace,
+						Name:      tc.parentWorkload.Name,
+					},
+				}
+			}
+
+			got, err := r.Reconcile(t.Context(), req)
+			if (err != nil) != tc.wantErr {
+				t.Errorf("Reconcile() error = %v, wantErr %v", err, tc.wantErr)
+				return
+			}
+			if !cmp.Equal(got, tc.wantResult) {
+				t.Errorf("Reconcile() got = %v, want %v", got, tc.wantResult)
+			}
+
+			if tc.wantParentWorkload != nil {
+				var gotParent kueue.Workload
+				err := cl.Get(t.Context(), types.NamespacedName{Namespace: tc.wantParentWorkload.Namespace, Name: tc.wantParentWorkload.Name}, &gotParent)
+				if err != nil {
+					t.Fatal(err)
+				}
+				if diff := cmp.Diff(tc.wantParentWorkload, &gotParent, workloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected parent workload (-want +got):\n%s", diff)
+				}
+			}
+
+			var gotVariants kueue.WorkloadList
+			if err := cl.List(t.Context(), &gotVariants, client.InNamespace(tc.req.Namespace)); err != nil {
+				t.Fatal(err)
+			}
+
+			var variants []kueue.Workload
+			for _, wl := range gotVariants.Items {
+				if wl.Name != tc.wantParentWorkload.Name {
+					variants = append(variants, wl)
+				}
```

  Can we use the workload.IsVariant for filtering here?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T16:37:35Z

- **pkg/controller/concurrentadmission/controller.go:251** by [@mimowo](https://github.com/mimowo) — 2026-04-28T16:37:35Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if workload.IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if workload.GetParentWorkloadName(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", klog.KObj(variant), "parent", klog.KObj(parent), "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", klog.KObj(variant), "error", err)
```

  for error logging user log.Error

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T17:01:00Z

- **pkg/controller/core/workload_controller.go:1185** by [@mimowo](https://github.com/mimowo) — 2026-04-28T17:01:01Z
```diff
@@ -1147,7 +1175,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 		// The workload could have been in the queues if we missed an event.
 		r.queues.DeleteWorkload(log, wlKey)
-		r.queues.AddFinishedWorkload(wlCopy)
+
+		if features.Enabled(features.ConcurrentAdmission) && workload.IsVariant(wlCopy) {
+			// Finished metric should account for Parent Workloads, not Variants
+			log.V(2).Info("Skipping finished workload metric update for Variant workload")
+		} else {
+			// Finished metric should account for Variants
+			r.queues.AddFinishedWorkload(wlCopy)
+		}
```

  Try to move the checks inside AddFinishedWorkload

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T17:03:32Z

- **pkg/controller/core/workload_controller.go:1201** by [@mimowo](https://github.com/mimowo) — 2026-04-28T17:03:32Z
```diff
@@ -1161,7 +1196,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	case prevStatus == workload.StatusPending && status == workload.StatusPending:
 		if dra.NeedsDRAReconcile(e.ObjectNew) {
 			log.V(2).Info("Skipping queue update for DRA workload - handled in Reconcile")
-		} else {
+		} else if !concurrentAdmissionEnabled || workload.IsVariant(wlCopy) {
+			// Concurrent Admission: Here there's no guarantee the Parent is properly labeled because it may have not gone through Reconcile yet
+			// We need to check if the Workload is has Concurrent Admission enabled for its CQ and is Variant
```

  try to move the checks inside  r.queues.UpdateWorkload

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T17:04:51Z

- **pkg/controller/core/workload_controller.go:1264** by [@mimowo](https://github.com/mimowo) — 2026-04-28T17:04:52Z
```diff
@@ -1219,7 +1256,12 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	default:
 		// Workload update in the cache is handled here; however, some fields are immutable
 		// and are not supposed to actually change anything.
-		r.cache.AddOrUpdateWorkload(log, wlCopy)
+
+		// Concurrent Admission: Here there's no guarantee the Parent is properly labeled because it may have not gone through Reconcile yet
+		// We need to check if the Workload is has Concurrent Admission enabled for its CQ and is Variant
+		if !concurrentAdmissionEnabled || workload.IsVariant(wlCopy) {
+			r.cache.AddOrUpdateWorkload(log, wlCopy)
+		}
```

  Try to move the checks inside `r.cache.AddOrUpdateWorkload`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T17:06:10Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:788** by [@mimowo](https://github.com/mimowo) — 2026-04-28T17:06:10Z
```diff
@@ -777,6 +778,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	if annotations := a.wl.Obj.GetAnnotations(); annotations != nil {
+		if allowedFlavors, ok := annotations[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]; ok {
+			return slices.Contains(strings.Split(allowedFlavors, ","), string(fName))
+		}
```

  Introduce helpers to Parse and Serialize the annotatation. Similarly as we do for AdmissionGatedBy.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-28T17:14:19Z

- **pkg/controller/core/workload_controller.go:791** by [@mimowo](https://github.com/mimowo) — 2026-04-28T17:14:18Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  We have synced with @PBundyra and the idea to resolve the cycle it to create a dedicaed `workload/concurrentadmission` so that when imported we will have concurrentadmission.IsParent, and concurrentadmission.IsVariant, so that the functions read nicely. 
  
  The other concurrentadmission package (under controller/) willl most likely not need to be imported except for the setup code.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T19:59:13Z

- **pkg/controller/core/workload_controller.go:791** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T19:59:13Z
```diff
@@ -767,6 +788,10 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 }
 
 func (r *WorkloadReconciler) reconcileSyncAdmissionChecks(ctx context.Context, wl *kueue.Workload, cq *kueue.ClusterQueue) (bool, error) {
+	if features.Enabled(features.ConcurrentAdmission) && workload.IsParentVariant(wl) {
```

  Added as described above

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T19:59:38Z

- **pkg/controller/concurrentadmission/controller.go:251** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T19:59:38Z
```diff
@@ -0,0 +1,591 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"context"
+	"fmt"
+	"slices"
+
+	"github.com/go-logr/logr"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/builder"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/controller"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/predicate"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+	"sigs.k8s.io/controller-runtime/pkg/source"
+
+	configapi "sigs.k8s.io/kueue/apis/config/v1beta2"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	ConcurrentAdmissionController = "concurrent-admission-controller"
+)
+
+type variantReconciler struct {
+	logName     string
+	queues      *qcache.Manager
+	client      client.Client
+	recorder    record.EventRecorder
+	roleTracker *roletracker.RoleTracker
+	clock       clock.Clock
+}
+
+var _ reconcile.Reconciler = (*variantReconciler)(nil)
+var _ predicate.TypedPredicate[*kueue.Workload] = (*variantReconciler)(nil)
+
+func newVariantReconciler(c client.Client, queues *qcache.Manager, recorder record.EventRecorder, roleTracker *roletracker.RoleTracker) *variantReconciler {
+	return &variantReconciler{
+		logName:     ConcurrentAdmissionController,
+		client:      c,
+		queues:      queues,
+		recorder:    recorder,
+		roleTracker: roleTracker,
+		clock:       clock.RealClock{},
+	}
+}
+
+func SetupControllers(mgr ctrl.Manager, queues *qcache.Manager, cfg *configapi.Configuration, roleTracker *roletracker.RoleTracker) (string, error) {
+	recorder := mgr.GetEventRecorderFor(ConcurrentAdmissionController)
+	variantRec := newVariantReconciler(mgr.GetClient(), queues, recorder, roleTracker)
+	if ctrlName, err := variantRec.setupWithManager(mgr, cfg); err != nil {
+		return ctrlName, err
+	}
+	return "", nil
+}
+
+func (r *variantReconciler) setupWithManager(mgr ctrl.Manager, cfg *configapi.Configuration) (string, error) {
+	return ConcurrentAdmissionController, builder.TypedControllerManagedBy[reconcile.Request](mgr).
+		Named(ConcurrentAdmissionController).
+		WatchesRawSource(source.TypedKind(
+			mgr.GetCache(),
+			&kueue.Workload{},
+			handler.TypedEnqueueRequestsFromMapFunc(func(_ context.Context, obj *kueue.Workload) []reconcile.Request {
+				if workload.IsParentVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKeyFromObject(obj)}}
+				}
+				if workload.IsVariant(obj) {
+					return []reconcile.Request{{NamespacedName: client.ObjectKey{Namespace: obj.Namespace, Name: workload.GetParentWorkloadName(obj)}}}
+				}
+				return nil
+			}),
+			r,
+		)).
+		WithOptions(controller.Options{
+			NeedLeaderElection:      new(false),
+			MaxConcurrentReconciles: mgr.GetControllerOptions().GroupKindConcurrency[kueue.GroupVersion.WithKind("Workload").GroupKind().String()],
+		}).
+		WithLogConstructor(roletracker.NewLogConstructor(r.roleTracker, ConcurrentAdmissionController)).
+		Complete(r)
+}
+
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;create;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
+
+func (r *variantReconciler) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Result, error) {
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile Workload")
+
+	parent, variants, cq, err := r.getFamilyAndClusterQueue(ctx, req)
+	if err != nil {
+		return ctrl.Result{}, err
+	}
+	if parent == nil {
+		return ctrl.Result{}, nil
+	}
+
+	flavorOrder := make(map[kueue.ResourceFlavorReference]int, len(cq.Spec.ResourceGroups[0].Flavors))
+	for i, flavor := range cq.Spec.ResourceGroups[0].Flavors {
+		flavorOrder[flavor.Name] = i
+	}
+	variants = sortVariantsByFlavorOrder(variants, flavorOrder)
+
+	log = log.WithValues("clusterQueue", cq.Name)
+	log.V(2).Info("Found Workload family and ClusterQueue", "variants", klog.KObjSlice(variants))
+
+	// TODO: If ConcurrentAdmission is no longer enabled for this CQ, delete parent and variants.
+
+	if len(variants) < len(flavorOrder) {
+		log.V(3).Info("Too few variants, creating new ones", "desired", len(flavorOrder), "actual", len(variants))
+		if err := r.createVariants(ctx, parent, variants, flavorOrder); err != nil {
+			return ctrl.Result{}, err
+		}
+	}
+	if len(variants) == len(flavorOrder) {
+		log.V(3).Info("Desired number of variants, no action needed", "desired", len(flavorOrder), "actual", len(variants))
+	}
+
+	log.V(3).Info("Syncing variants if needed")
+	parentEvicted, err := r.syncVariantEvictionStatus(ctx, parent, variants)
+	if err != nil {
+		log.V(3).Info("Failed to sync variant eviction status", "error", err)
+		return ctrl.Result{}, err
+	}
+	if parentEvicted {
+		return ctrl.Result{}, nil
+	}
+
+	log.V(3).Info("Deactivating variants if needed")
+	if err := r.deactivateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to deactivate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	log.V(3).Info("Activating variants if needed")
+	if err := r.activateVariants(ctx, parent, variants, cq, flavorOrder); err != nil {
+		log.V(3).Info("Failed to activate variants", "error", err)
+		return ctrl.Result{}, err
+	}
+
+	if err := r.syncAdmissionStatus(ctx, parent, variants); err != nil {
+		log.V(3).Info("Failed to sync admission status", "error", err)
+		return ctrl.Result{}, err
+	}
+	return ctrl.Result{}, nil
+}
+
+func (r *variantReconciler) getFamilyAndClusterQueue(ctx context.Context, req ctrl.Request) (*kueue.Workload, []kueue.Workload, *kueue.ClusterQueue, error) {
+	log := ctrl.LoggerFrom(ctx)
+	wl := &kueue.Workload{}
+	if err := r.client.Get(ctx, req.NamespacedName, wl); err != nil {
+		if apierrors.IsNotFound(err) {
+			log.V(3).Info("Workload not found, might have been deleted", "workload", klog.KRef(req.Namespace, req.Name))
+		}
+		return nil, nil, nil, client.IgnoreNotFound(err)
+	}
+
+	if !workload.IsParentVariant(wl) {
+		return nil, nil, nil, nil
+	}
+
+	variants, err := r.getVariantsForParent(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	cq, err := r.getClusterQueue(ctx, wl)
+	if err != nil {
+		return nil, nil, nil, err
+	}
+
+	return wl, variants, cq, nil
+}
+
+func (r *variantReconciler) getVariantsForParent(ctx context.Context, parent *kueue.Workload) ([]kueue.Workload, error) {
+	if !workload.IsParentVariant(parent) {
+		return nil, fmt.Errorf("workload %s/%s is not a parent variant", parent.Namespace, parent.Name)
+	}
+	list := &kueue.WorkloadList{}
+	if err := r.client.List(ctx, list, client.InNamespace(parent.Namespace)); err != nil {
+		// TODO: Index variants
+		return nil, err
+	}
+	variants := make([]kueue.Workload, 0)
+	for i := range list.Items {
+		if workload.GetParentWorkloadName(&list.Items[i]) == parent.Name {
+			variants = append(variants, list.Items[i])
+		}
+	}
+	return variants, nil
+}
+
+func (r *variantReconciler) getClusterQueue(ctx context.Context, wl *kueue.Workload) (*kueue.ClusterQueue, error) {
+	cqName, ok := r.queues.ClusterQueueForWorkload(wl)
+	if !ok {
+		return nil, fmt.Errorf("could not find cluster queue for workload %s/%s", wl.Namespace, wl.Name)
+	}
+	cq := &kueue.ClusterQueue{}
+	if err := r.client.Get(ctx, client.ObjectKey{Name: string(cqName)}, cq); err != nil {
+		return nil, err
+	}
+	return cq, nil
+}
+
+func (r *variantReconciler) createVariants(ctx context.Context, parent *kueue.Workload, variants []kueue.Workload, resourceFlavors map[kueue.ResourceFlavorReference]int) error {
+	log := ctrl.LoggerFrom(ctx)
+	for flavor := range resourceFlavors {
+		if r.hasVariantWithFlavor(ctx, variants, flavor) {
+			continue
+		}
+
+		variant := generateVariant(parent, flavor)
+		log.V(3).Info("Creating variant for flavor", "flavor", flavor, "variant", variant.Name)
+
+		// Set the owner reference to the parent workload
+		if err := ctrl.SetControllerReference(parent, variant, r.client.Scheme()); err != nil {
+			log.V(3).Info("Failed to set owner reference for variant", "variant", klog.KObj(variant), "parent", klog.KObj(parent), "error", err)
+			return err
+		}
+		if err := r.client.Create(ctx, variant); err != nil {
+			log.V(3).Info("Failed to create variant", "variant", klog.KObj(variant), "error", err)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T20:23:00Z

- **pkg/controller/core/workload_controller.go:1264** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T20:23:00Z
```diff
@@ -1219,7 +1256,12 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	default:
 		// Workload update in the cache is handled here; however, some fields are immutable
 		// and are not supposed to actually change anything.
-		r.cache.AddOrUpdateWorkload(log, wlCopy)
+
+		// Concurrent Admission: Here there's no guarantee the Parent is properly labeled because it may have not gone through Reconcile yet
+		// We need to check if the Workload is has Concurrent Admission enabled for its CQ and is Variant
+		if !concurrentAdmissionEnabled || workload.IsVariant(wlCopy) {
+			r.cache.AddOrUpdateWorkload(log, wlCopy)
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T20:23:08Z

- **pkg/controller/core/workload_controller.go:1201** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T20:23:08Z
```diff
@@ -1161,7 +1196,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	case prevStatus == workload.StatusPending && status == workload.StatusPending:
 		if dra.NeedsDRAReconcile(e.ObjectNew) {
 			log.V(2).Info("Skipping queue update for DRA workload - handled in Reconcile")
-		} else {
+		} else if !concurrentAdmissionEnabled || workload.IsVariant(wlCopy) {
+			// Concurrent Admission: Here there's no guarantee the Parent is properly labeled because it may have not gone through Reconcile yet
+			// We need to check if the Workload is has Concurrent Admission enabled for its CQ and is Variant
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T20:23:16Z

- **pkg/controller/core/workload_controller.go:1185** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T20:23:16Z
```diff
@@ -1147,7 +1175,14 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 
 		// The workload could have been in the queues if we missed an event.
 		r.queues.DeleteWorkload(log, wlKey)
-		r.queues.AddFinishedWorkload(wlCopy)
+
+		if features.Enabled(features.ConcurrentAdmission) && workload.IsVariant(wlCopy) {
+			// Finished metric should account for Parent Workloads, not Variants
+			log.V(2).Info("Skipping finished workload metric update for Variant workload")
+		} else {
+			// Finished metric should account for Variants
+			r.queues.AddFinishedWorkload(wlCopy)
+		}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T20:31:14Z

- **pkg/cache/scheduler/cache.go:680** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T20:31:13Z
```diff
@@ -674,7 +677,7 @@ func (c *Cache) addOrUpdateWorkloadWithoutLock(log logr.Logger, wl *kueue.Worklo
 	assignedCqName, assigned := c.workloadAssignedQueues[wlKey]
 
 	// Finished or deactivated workloads should not keep ClusterQueues in-use in the cache.
-	if !workload.HasActiveQuotaReservation(wl) {
+	if !workload.HasActiveQuotaReservation(wl) || workload.IsParentVariant(wl) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-28T20:31:39Z

- **pkg/controller/core/workload_controller.go:1176** by [@PBundyra](https://github.com/PBundyra) — 2026-04-28T20:31:39Z
```diff
@@ -1134,6 +1171,9 @@ func (r *WorkloadReconciler) Update(e event.TypedUpdateEvent[*kueue.Workload]) b
 	wlKey := workload.Key(e.ObjectNew)
 	// We do not handle old workload here as it will be deleted or replaced by new one anyway.
 	workload.AdjustResources(ctrl.LoggerInto(ctx, log), r.client, wlCopy)
+	cqName, _ := r.queues.ClusterQueueForWorkload(wlCopy)
+	concurrentAdmissionEnabled := r.queues.ConcurrentAdmissionEnabled(cqName)
+	// we need to prevent ParentVariants from being added to caches now as the Parent Workload may not have the label yet
```

  Improved even more, fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T08:16:02Z

- **pkg/scheduler/scheduler.go:666** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T08:16:02Z
```diff
@@ -662,6 +662,9 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, fairsharing.Enabled(s.fairSharing), preemption.NewOracle(s.preemptor, snap), replaceableWorkloadSlice)
 	fullAssignment := flvAssigner.Assign(log, nil)
 
+	// Add less favorable siblings to preemption targets once here.
+	preemptionTargets = append(preemptionTargets, s.getLessFavorableSiblings(log, wl, fullAssignment, snap)...)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T08:16:18Z

- **pkg/scheduler/scheduler.go:423** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T08:16:18Z
```diff
@@ -420,7 +420,7 @@ func (s *Scheduler) processEntry(
 	// Note: it is valid for either or both preemptionTargets and oldWorkloadSlice to be nil.
 	preemptionTargets, oldWorkloadSlice := workloadslicing.FindReplacedSliceTarget(e.Obj, e.preemptionTargets)
 
-	if mode == flavorassigner.Preempt {
+	if mode == flavorassigner.Preempt || len(preemptionTargets) > 0 {
```

  Addressed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T08:16:36Z

- **pkg/scheduler/scheduler.go:703** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T08:16:36Z
```diff
@@ -697,6 +700,51 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSiblings(log logr.Logger, wl *workload.Info, assignment flavorassigner.Assignment, snap *schdcache.Snapshot) []*preemption.Target {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T08:17:02Z

- **apis/kueue/v1beta2/workload_types.go:1002** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T08:17:02Z
```diff
@@ -996,6 +996,10 @@ const (
 	// InCohortReclaimWhileBorrowingReason indicates the Workload was preempted
 	// due to reclamation within the cohort while borrowing.
 	InCohortReclaimWhileBorrowingReason string = "InCohortReclaimWhileBorrowing"
+
+	// ConcurrentAdmissionReason indicates the Workload was preempted due to
+	// admission of more favorable Variant of the same Parent Workload.
+	ConcurrentAdmissionReason string = "ConcurrentAdmission"
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T09:21:59Z

- **pkg/controller/concurrentadmission/controller_test.go:1045** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T09:21:59Z
```diff
@@ -0,0 +1,1062 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	testingclock "k8s.io/utils/clock/testing"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+var (
+	workloadCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion", "ObjectMeta.UID", "Status.AccumulatedPastExecutionTimeSeconds", "Status.SchedulingStats",
+		),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.SortSlices(func(a, b kueue.Workload) bool { return a.Name < b.Name }),
+		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	defaultCQ := utiltestingapi.MakeClusterQueue("cq").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	defaultLQ := utiltestingapi.MakeLocalQueue("lq", "default").ClusterQueue("cq").Obj()
+
+	migrationCQ := utiltestingapi.MakeClusterQueue("cq-migration").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		MinPreferredFlavorName("reservation").
+		Obj()
+	migrationLQ := utiltestingapi.MakeLocalQueue("lq-migration", "default").ClusterQueue("cq-migration").Obj()
+
+	migrationCQNoConstraint := utiltestingapi.MakeClusterQueue("cq-migration-no-constraint").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	migrationLQNoConstraint := utiltestingapi.MakeLocalQueue("lq-migration-no-constraint", "default").ClusterQueue("cq-migration-no-constraint").Obj()
+
+	testCases := map[string]struct {
+		parentWorkload       *kueue.Workload
+		variantWorkloads     []kueue.Workload
+		wantParentWorkload   *kueue.Workload
+		wantVariantWorkloads []kueue.Workload
+		req                  reconcile.Request
+		wantResult           reconcile.Result
+		wantErr              bool
+	}{
+		"workload not found": {
+			req: reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: "default",
+					Name:      "non-existing",
+				},
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"workload is not parent or variant": {
+			parentWorkload:     utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantResult:         reconcile.Result{},
+			wantErr:            false,
+		},
+		"parent workload without variants creates them": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot-545ad", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"parent workload with missing variants; creates missing": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant syncs admission to parent": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has quota; evict parent and wait": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadEvicted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "VariantEvicted",
+					Message: "Admitted variant was evicted",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has no quota reservation; clear variant reservation": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Preempted",
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(true).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"with minTargetFlavor=reservation, variant admitted on spot, deactivate on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					AllowedFlavors("reservation").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on spot, nothing deactivated": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"parent marked finished, mark all variants finished": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+			},
+		},
+		"parent is not active, propagating deactivation to all variants": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.ConcurrentAdmission, true)
+			var objects []client.Object
+			if tc.parentWorkload != nil {
+				objects = append(objects, tc.parentWorkload)
+			}
+			for i := range tc.variantWorkloads {
+				objects = append(objects, &tc.variantWorkloads[i])
+			}
+			cl := utiltesting.NewClientBuilder().
+				WithObjects(objects...).
+				WithStatusSubresource(objects...).
+				Build()
+			preemptionExpectations := preemptexpectations.New()
+			qManager := qcache.NewManagerForUnitTests(cl, nil, qcache.WithPreemptionExpectations(preemptionExpectations))
+			roleTracker := roletracker.NewFakeRoleTracker(roletracker.RoleLeader)
+
+			cqs := []*kueue.ClusterQueue{defaultCQ.DeepCopy(), migrationCQ.DeepCopy(), migrationCQNoConstraint.DeepCopy()}
+			lqs := []*kueue.LocalQueue{defaultLQ.DeepCopy(), migrationLQ.DeepCopy(), migrationLQNoConstraint.DeepCopy()}
+
+			for _, cq := range cqs {
+				if err := cl.Create(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddClusterQueue(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for _, lq := range lqs {
+				if err := cl.Create(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddLocalQueue(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for i := range tc.variantWorkloads {
+				_ = qManager.AddOrUpdateWorkload(ctrl.Log, tc.variantWorkloads[i].DeepCopy())
+			}
+
+			r := &variantReconciler{
+				logName:     ConcurrentAdmissionController,
+				client:      cl,
+				queues:      qManager,
+				roleTracker: roleTracker,
+				clock:       testingclock.NewFakeClock(metav1.Now().Time),
+				recorder:    record.NewFakeRecorder(10),
+			}
+
+			req := tc.req
+			if req.Name == "" && tc.parentWorkload != nil {
+				req = reconcile.Request{
+					NamespacedName: types.NamespacedName{
+						Namespace: tc.parentWorkload.Namespace,
+						Name:      tc.parentWorkload.Name,
+					},
+				}
+			}
+
+			got, err := r.Reconcile(t.Context(), req)
+			if (err != nil) != tc.wantErr {
+				t.Errorf("Reconcile() error = %v, wantErr %v", err, tc.wantErr)
+				return
+			}
+			if !cmp.Equal(got, tc.wantResult) {
+				t.Errorf("Reconcile() got = %v, want %v", got, tc.wantResult)
+			}
+
+			if tc.wantParentWorkload != nil {
+				var gotParent kueue.Workload
+				err := cl.Get(t.Context(), types.NamespacedName{Namespace: tc.wantParentWorkload.Namespace, Name: tc.wantParentWorkload.Name}, &gotParent)
+				if err != nil {
+					t.Fatal(err)
+				}
+				if diff := cmp.Diff(tc.wantParentWorkload, &gotParent, workloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected parent workload (-want +got):\n%s", diff)
+				}
+			}
+
+			var gotVariants kueue.WorkloadList
```

  Cleaned up

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T09:22:05Z

- **pkg/controller/concurrentadmission/controller_test.go:1054** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T09:22:05Z
```diff
@@ -0,0 +1,1062 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/tools/record"
+	testingclock "k8s.io/utils/clock/testing"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	qcache "sigs.k8s.io/kueue/pkg/cache/queue"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+	preemptexpectations "sigs.k8s.io/kueue/pkg/scheduler/preemption/expectations"
+	"sigs.k8s.io/kueue/pkg/util/roletracker"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+)
+
+var (
+	workloadCmpOpts = cmp.Options{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreFields(
+			kueue.Workload{}, "TypeMeta", "ObjectMeta.ResourceVersion", "ObjectMeta.UID", "Status.AccumulatedPastExecutionTimeSeconds", "Status.SchedulingStats",
+		),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.SortSlices(func(a, b kueue.Workload) bool { return a.Name < b.Name }),
+		cmpopts.SortSlices(func(a, b metav1.Condition) bool { return a.Type < b.Type }),
+	}
+)
+
+func TestReconcile(t *testing.T) {
+	defaultCQ := utiltestingapi.MakeClusterQueue("cq").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	defaultLQ := utiltestingapi.MakeLocalQueue("lq", "default").ClusterQueue("cq").Obj()
+
+	migrationCQ := utiltestingapi.MakeClusterQueue("cq-migration").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		MinPreferredFlavorName("reservation").
+		Obj()
+	migrationLQ := utiltestingapi.MakeLocalQueue("lq-migration", "default").ClusterQueue("cq-migration").Obj()
+
+	migrationCQNoConstraint := utiltestingapi.MakeClusterQueue("cq-migration-no-constraint").
+		ResourceGroup(
+			*utiltestingapi.MakeFlavorQuotas("reservation").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("on-demand").Obj(),
+			*utiltestingapi.MakeFlavorQuotas("spot").Obj(),
+		).
+		ConcurrentAdmissionPolicy(kueue.ConcurrentAdmissionTryPreferredFlavors).
+		Obj()
+	migrationLQNoConstraint := utiltestingapi.MakeLocalQueue("lq-migration-no-constraint", "default").ClusterQueue("cq-migration-no-constraint").Obj()
+
+	testCases := map[string]struct {
+		parentWorkload       *kueue.Workload
+		variantWorkloads     []kueue.Workload
+		wantParentWorkload   *kueue.Workload
+		wantVariantWorkloads []kueue.Workload
+		req                  reconcile.Request
+		wantResult           reconcile.Result
+		wantErr              bool
+	}{
+		"workload not found": {
+			req: reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: "default",
+					Name:      "non-existing",
+				},
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"workload is not parent or variant": {
+			parentWorkload:     utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("wl", "default").Obj(),
+			wantResult:         reconcile.Result{},
+			wantErr:            false,
+		},
+		"parent workload without variants creates them": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot-545ad", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"parent workload with missing variants; creates missing": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", ""). // UID is ignored in cmp
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand-39893", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant syncs admission to parent": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has quota; evict parent and wait": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+				AdmittedAt(true, metav1.Now().Time).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadEvicted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "VariantEvicted",
+					Message: "Admitted variant was evicted",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"admitted variant evicted; parent has no quota reservation; clear variant reservation": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Request(corev1.ResourceCPU, "1").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadQuotaReserved,
+						Status:  metav1.ConditionFalse,
+						Reason:  "Pending",
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadAdmitted,
+						Status:  metav1.ConditionFalse,
+						Reason:  "NoReservation",
+						Message: "The workload has no reservation",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadEvicted,
+						Status:  metav1.ConditionTrue,
+						Reason:  kueue.WorkloadEvictedByPreemption,
+						Message: "Evicted by preemption",
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadRequeued,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Preempted",
+						Message: "Evicted by preemption",
+					}).
+					Active(true).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(true).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+		"with minTargetFlavor=reservation, variant admitted on spot, deactivate on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					AllowedFlavors("reservation").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"with minTargetFlavor=reservation, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on spot, nothing deactivated": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "spot",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-spot is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "spot", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on on-demand, deactivate spot": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "on-demand",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-on-demand is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "on-demand", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"without minTargetFlavor, variant admitted on reservation, deactivate spot and on-demand": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq-migration-no-constraint").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Admission(utiltestingapi.MakeAdmission("cq-migration-no-constraint", "main").
+					PodSets(kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: "reservation",
+						},
+						Count:         ptr.To[int32](1),
+						ResourceUsage: corev1.ResourceList{corev1.ResourceCPU: resource.MustParse("1")},
+					}).Obj()).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadAdmitted,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Admitted",
+					Message: "The variant parent-variant-reservation is admitted",
+				}).
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadQuotaReserved,
+					Status:  metav1.ConditionTrue,
+					Reason:  "QuotaReserved",
+					Message: "Quota reserved in ClusterQueue cq-migration-no-constraint",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-reservation", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("reservation").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					SimpleReserveQuota("cq-migration-no-constraint", "reservation", metav1.Now().Time).
+					AdmittedAt(true, metav1.Now().Time).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq-migration-no-constraint").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+		},
+		"parent marked finished, mark all variants finished": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Request(corev1.ResourceCPU, "1").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Condition(metav1.Condition{
+					Type:    kueue.WorkloadFinished,
+					Status:  metav1.ConditionTrue,
+					Reason:  "Succeeded",
+					Message: "Job finished successfully",
+				}).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("on-demand").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					Request(corev1.ResourceCPU, "1").
+					AllowedFlavors("spot").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "Succeeded",
+						Message: "Job finished successfully",
+					}).
+					Obj(),
+			},
+		},
+		"parent is not active, propagating deactivation to all variants": {
+			parentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			variantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Obj(),
+			},
+			wantParentWorkload: utiltestingapi.MakeWorkload("parent-12345", "default").
+				Queue("lq").
+				Label(constants.ConcurrentAdmissionParentLabelKey, "true").
+				Request(corev1.ResourceCPU, "1").
+				Active(false).
+				Obj(),
+			wantVariantWorkloads: []kueue.Workload{
+				*utiltestingapi.MakeWorkload("parent-variant-spot", "default").
+					Queue("lq").
+					AllowedFlavors("spot").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+				*utiltestingapi.MakeWorkload("parent-variant-on-demand", "default").
+					Queue("lq").
+					AllowedFlavors("on-demand").
+					Request(corev1.ResourceCPU, "1").
+					ControllerReference(kueue.GroupVersion.WithKind("Workload"), "parent-12345", "").
+					Active(false).
+					Obj(),
+			},
+			wantResult: reconcile.Result{},
+			wantErr:    false,
+		},
+	}
+
+	for name, tc := range testCases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.ConcurrentAdmission, true)
+			var objects []client.Object
+			if tc.parentWorkload != nil {
+				objects = append(objects, tc.parentWorkload)
+			}
+			for i := range tc.variantWorkloads {
+				objects = append(objects, &tc.variantWorkloads[i])
+			}
+			cl := utiltesting.NewClientBuilder().
+				WithObjects(objects...).
+				WithStatusSubresource(objects...).
+				Build()
+			preemptionExpectations := preemptexpectations.New()
+			qManager := qcache.NewManagerForUnitTests(cl, nil, qcache.WithPreemptionExpectations(preemptionExpectations))
+			roleTracker := roletracker.NewFakeRoleTracker(roletracker.RoleLeader)
+
+			cqs := []*kueue.ClusterQueue{defaultCQ.DeepCopy(), migrationCQ.DeepCopy(), migrationCQNoConstraint.DeepCopy()}
+			lqs := []*kueue.LocalQueue{defaultLQ.DeepCopy(), migrationLQ.DeepCopy(), migrationLQNoConstraint.DeepCopy()}
+
+			for _, cq := range cqs {
+				if err := cl.Create(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddClusterQueue(t.Context(), cq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for _, lq := range lqs {
+				if err := cl.Create(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+				if err := qManager.AddLocalQueue(t.Context(), lq); err != nil {
+					t.Fatal(err)
+				}
+			}
+
+			for i := range tc.variantWorkloads {
+				_ = qManager.AddOrUpdateWorkload(ctrl.Log, tc.variantWorkloads[i].DeepCopy())
+			}
+
+			r := &variantReconciler{
+				logName:     ConcurrentAdmissionController,
+				client:      cl,
+				queues:      qManager,
+				roleTracker: roleTracker,
+				clock:       testingclock.NewFakeClock(metav1.Now().Time),
+				recorder:    record.NewFakeRecorder(10),
+			}
+
+			req := tc.req
+			if req.Name == "" && tc.parentWorkload != nil {
+				req = reconcile.Request{
+					NamespacedName: types.NamespacedName{
+						Namespace: tc.parentWorkload.Namespace,
+						Name:      tc.parentWorkload.Name,
+					},
+				}
+			}
+
+			got, err := r.Reconcile(t.Context(), req)
+			if (err != nil) != tc.wantErr {
+				t.Errorf("Reconcile() error = %v, wantErr %v", err, tc.wantErr)
+				return
+			}
+			if !cmp.Equal(got, tc.wantResult) {
+				t.Errorf("Reconcile() got = %v, want %v", got, tc.wantResult)
+			}
+
+			if tc.wantParentWorkload != nil {
+				var gotParent kueue.Workload
+				err := cl.Get(t.Context(), types.NamespacedName{Namespace: tc.wantParentWorkload.Namespace, Name: tc.wantParentWorkload.Name}, &gotParent)
+				if err != nil {
+					t.Fatal(err)
+				}
+				if diff := cmp.Diff(tc.wantParentWorkload, &gotParent, workloadCmpOpts); diff != "" {
+					t.Errorf("Unexpected parent workload (-want +got):\n%s", diff)
+				}
+			}
+
+			var gotVariants kueue.WorkloadList
+			if err := cl.List(t.Context(), &gotVariants, client.InNamespace(tc.req.Namespace)); err != nil {
+				t.Fatal(err)
+			}
+
+			var variants []kueue.Workload
+			for _, wl := range gotVariants.Items {
+				if wl.Name != tc.wantParentWorkload.Name {
+					variants = append(variants, wl)
+				}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T11:23:07Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:788** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T11:23:07Z
```diff
@@ -777,6 +778,18 @@ func (a *Assignment) findOldPodSetRequest(psName kueue.PodSetReference, resource
 	return 0
 }
 
+func (a *FlavorAssigner) isFlavorAllowed(fName kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	if annotations := a.wl.Obj.GetAnnotations(); annotations != nil {
+		if allowedFlavors, ok := annotations[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]; ok {
+			return slices.Contains(strings.Split(allowedFlavors, ","), string(fName))
+		}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T15:04:06Z

- **apis/kueue/v1beta2/clusterqueue_types.go:195** by [@mimowo](https://github.com/mimowo) — 2026-04-29T14:29:26Z
```diff
@@ -183,6 +191,53 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints Workload's migration.
```

  ```suggestion
  	// migration defines the constraints on Workload's migration.
  ```
  nit?

- **pkg/cache/queue/manager.go:592** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:03:04Z
```diff
@@ -546,6 +583,13 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && concurrentadmission.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
+		return nil
+	}
```

  ```suggestion
  	if features.Enabled(features.ConcurrentAdmission) && m.IsConcurrentAdmissionParent(w) {
  		return nil
  	}
  ```
  same in UpdateWorkload

- **pkg/controller/concurrentadmission/controller.go:2** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:04:04Z
```diff
@@ -0,0 +1,605 @@
+/*
+Copyright The Kubernetes Authors.
```

  What is the unit test coverage for this file? What are the places where this is no complete?Summarize in PR descirption

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T15:06:37Z

- **pkg/cache/queue/manager.go:592** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T15:06:36Z
```diff
@@ -546,6 +583,13 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && concurrentadmission.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
+		return nil
+	}
```

  The motivation behind why I wrote it this way is that we cannot assume that the Parent label is present at the time of calling this function. So we need to check if the workload is a Variant (+ if CQ defines ConcurrentAdmission)

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-29T15:16:16Z

- **pkg/controller/concurrentadmission/controller.go:2** by [@PBundyra](https://github.com/PBundyra) — 2026-04-29T15:16:16Z
```diff
@@ -0,0 +1,605 @@
+/*
+Copyright The Kubernetes Authors.
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T15:45:10Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:822** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:45:11Z
```diff
@@ -818,6 +819,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !concurrentadmission.IsFlavorAllowedForVariant(a.wl.Obj, fName) {
```

  Check the FG here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T15:49:59Z

- **pkg/scheduler/scheduler.go:446** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:49:59Z
```diff
@@ -433,6 +439,13 @@ func (s *Scheduler) processEntry(
 		}
 	}
 
+	if features.Enabled(features.ConcurrentAdmission) && migrationVictim != nil {
+		if err := s.issueMigration(ctx, log, e, migrationVictim); err != nil {
+			return
+		}
+		return
```

  Either do not return the err here, or log it here, and do not log inside the helper

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T15:57:53Z

- **pkg/scheduler/scheduler_test.go:1583** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:57:53Z
```diff
@@ -1580,6 +1580,104 @@ func TestSchedule(t *testing.T) {
 					Obj(),
 			},
 		},
+		"concurrent admission: more favorable variant preempts less favorable sibling": {
```

  This file is getting too long, so I would suggest separate suite like scheduler_concurrentadmission_test.go, similarly as we do for TAS and AFS

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T15:58:38Z

- **pkg/util/testing/v1beta2/wrappers.go:432** by [@mimowo](https://github.com/mimowo) — 2026-04-29T15:58:38Z
```diff
@@ -415,6 +421,16 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	if w.ObjectMeta.Annotations == nil {
+		w.ObjectMeta.Annotations = make(map[string]string, 1)
+	}
+	allowedFlavors := strings.Join(utilslices.Map(flavors, func(f *kueue.ResourceFlavorReference) string { return string(*f) }), ",")
+	w.ObjectMeta.Annotations[constants.WorkloadAllowedResourceFlavorAnnotation] = allowedFlavors
+	return w
+}
```

  Use the helpers for parsing the comma-separated annotation

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T16:03:31Z

- **pkg/workload/concurrentadmission/concurrentadmission.go:109** by [@mimowo](https://github.com/mimowo) — 2026-04-29T16:03:31Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"slices"
+	"strings"
+
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// SetParentVariantLabel sets the label indicating the workload is a parent variant.
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string, 1)
+	}
+	workload.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] = "true"
+}
+
+// IsParent returns true if the workload is a parent variant.
+func IsParent(wl *kueue.Workload) bool {
+	return wl != nil && wl.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] == "true"
+}
+
+// IsVariant returns true if the workload is a variant.
+func IsVariant(wl *kueue.Workload) bool {
+	if wl == nil {
+		return false
+	}
+	return GetParentWorkloadName(wl) != ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	flavors := parseAllowedFlavorsString(wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+	if len(flavors) == 0 {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(flavors[0])
+}
+
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+func IsFlavorAllowedForVariant(wl *kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	val, ok := wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]
+	if !ok {
+		return true
+	}
+	allowedFlavors := parseAllowedFlavorsString(val)
+	return slices.Contains(allowedFlavors, string(flavor))
+}
+
+// parseAllowedFlavorsString parses a comma-separated string of flavor names
+// and returns a slice of flavor names with leading and trailing spaces removed.
+func parseAllowedFlavorsString(s string) []string {
+	if strings.TrimSpace(s) == "" {
+		return []string{}
+	}
+	parts := strings.Split(s, ",")
+	for i := range parts {
+		parts[i] = strings.TrimSpace(parts[i])
+	}
+	return parts
+}
```

  This is duplicate of https://github.com/kubernetes-sigs/kueue/blob/main/pkg/util/admissiongates/admissiongates.go
  
  
  Let's instead rename the pre-existing code as `util/csv/csv.go`, and call it from the new places

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T16:11:30Z

- **pkg/scheduler/scheduler.go:712** by [@mimowo](https://github.com/mimowo) — 2026-04-29T16:11:31Z
```diff
@@ -662,6 +706,10 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, fairsharing.Enabled(s.fairSharing), preemption.NewOracle(s.preemptor, snap), replaceableWorkloadSlice)
 	fullAssignment := flvAssigner.Assign(log, nil)
 
+	// Add less favorable siblings to preemption targets once here.
+	if lessFavorableSibling := s.getLessFavorableSibling(log, wl, fullAssignment, snap); lessFavorableSibling != nil {
+		preemptionTargets = append(preemptionTargets, lessFavorableSibling)
+	}
```

  I'm not a fan that we mix preemptionTargets with migrationTargets. I think we should either:
  1. have some structure which encapsulates both sets, like EvictionTargets which would have functions like .appendPreemptionTarget, or appendMigrationTarget etc; or
  2. only determine the migrationTargets later. I'm not clear what is the benefit of determining them here, combining into preemptionTargets, and then unfolding into two lists again. I'm wondering if we can just determine the list later.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T07:10:21Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:822** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T07:10:21Z
```diff
@@ -818,6 +819,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !concurrentadmission.IsFlavorAllowedForVariant(a.wl.Obj, fName) {
```

  It's checked inside the function

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T07:18:27Z

- **pkg/workload/concurrentadmission/concurrentadmission.go:109** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T07:18:26Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"slices"
+	"strings"
+
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// SetParentVariantLabel sets the label indicating the workload is a parent variant.
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string, 1)
+	}
+	workload.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] = "true"
+}
+
+// IsParent returns true if the workload is a parent variant.
+func IsParent(wl *kueue.Workload) bool {
+	return wl != nil && wl.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] == "true"
+}
+
+// IsVariant returns true if the workload is a variant.
+func IsVariant(wl *kueue.Workload) bool {
+	if wl == nil {
+		return false
+	}
+	return GetParentWorkloadName(wl) != ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	flavors := parseAllowedFlavorsString(wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+	if len(flavors) == 0 {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(flavors[0])
+}
+
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+func IsFlavorAllowedForVariant(wl *kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	val, ok := wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]
+	if !ok {
+		return true
+	}
+	allowedFlavors := parseAllowedFlavorsString(val)
+	return slices.Contains(allowedFlavors, string(flavor))
+}
+
+// parseAllowedFlavorsString parses a comma-separated string of flavor names
+// and returns a slice of flavor names with leading and trailing spaces removed.
+func parseAllowedFlavorsString(s string) []string {
+	if strings.TrimSpace(s) == "" {
+		return []string{}
+	}
+	parts := strings.Split(s, ",")
+	for i := range parts {
+		parts[i] = strings.TrimSpace(parts[i])
+	}
+	return parts
+}
```

  I wanted to minimize interference with existing code so I planned to do it in a follow-up

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T07:21:24Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:822** by [@mimowo](https://github.com/mimowo) — 2026-04-30T07:21:24Z
```diff
@@ -818,6 +819,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !concurrentadmission.IsFlavorAllowedForVariant(a.wl.Obj, fName) {
```

  I think it should be checked outside here too. Otherwise this is confusing due to the negation `!concurrentadmission.IsFlavorAllowedForVariant`.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:30:28Z

- **apis/kueue/v1beta2/clusterqueue_types.go:195** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:30:28Z
```diff
@@ -183,6 +191,53 @@ const (
 	BestEffortFIFO QueueingStrategy = "BestEffortFIFO"
 )
 
+type ConcurrentAdmissionPolicy struct {
+	// migration defines the constraints Workload's migration.
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:31:02Z

- **pkg/scheduler/scheduler.go:446** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:31:02Z
```diff
@@ -433,6 +439,13 @@ func (s *Scheduler) processEntry(
 		}
 	}
 
+	if features.Enabled(features.ConcurrentAdmission) && migrationVictim != nil {
+		if err := s.issueMigration(ctx, log, e, migrationVictim); err != nil {
+			return
+		}
+		return
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T08:35:20Z

- **pkg/cache/queue/manager.go:592** by [@mimowo](https://github.com/mimowo) — 2026-04-30T08:35:20Z
```diff
@@ -546,6 +583,13 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && concurrentadmission.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
+		return nil
+	}
```

  I see but the helper function Im proposing is on the managwr so it will have acceess to the CQ. it is just to minimize the code duplication, not to change the mechanics

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:38:18Z

- **pkg/util/testing/v1beta2/wrappers.go:432** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:38:18Z
```diff
@@ -415,6 +421,16 @@ func (w *WorkloadWrapper) PreemptionGateStates(preemptionGateStates ...kueue.Pre
 	return w
 }
 
+// Set AllowedResourceFlavors annotation
+func (w *WorkloadWrapper) AllowedFlavors(flavors ...kueue.ResourceFlavorReference) *WorkloadWrapper {
+	if w.ObjectMeta.Annotations == nil {
+		w.ObjectMeta.Annotations = make(map[string]string, 1)
+	}
+	allowedFlavors := strings.Join(utilslices.Map(flavors, func(f *kueue.ResourceFlavorReference) string { return string(*f) }), ",")
+	w.ObjectMeta.Annotations[constants.WorkloadAllowedResourceFlavorAnnotation] = allowedFlavors
+	return w
+}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:38:27Z

- **pkg/scheduler/scheduler_test.go:1583** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:38:26Z
```diff
@@ -1580,6 +1580,104 @@ func TestSchedule(t *testing.T) {
 					Obj(),
 			},
 		},
+		"concurrent admission: more favorable variant preempts less favorable sibling": {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:38:35Z

- **pkg/scheduler/flavorassigner/flavorassigner.go:822** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:38:35Z
```diff
@@ -818,6 +819,10 @@ func (a *FlavorAssigner) findFlavorForPodSets(
 	for ; idx < len(resourceGroup.Flavors); idx++ {
 		attemptedFlavorIdx = idx
 		fName := resourceGroup.Flavors[idx]
+		if !concurrentadmission.IsFlavorAllowedForVariant(a.wl.Obj, fName) {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:39:39Z

- **pkg/cache/queue/manager.go:592** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:39:38Z
```diff
@@ -546,6 +583,13 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && concurrentadmission.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
+		return nil
+	}
```

  Oh right, yes it makes sense. I didn't catch it at first

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T08:40:03Z

- **pkg/workload/concurrentadmission/concurrentadmission.go:109** by [@mimowo](https://github.com/mimowo) — 2026-04-30T08:40:03Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"slices"
+	"strings"
+
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// SetParentVariantLabel sets the label indicating the workload is a parent variant.
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string, 1)
+	}
+	workload.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] = "true"
+}
+
+// IsParent returns true if the workload is a parent variant.
+func IsParent(wl *kueue.Workload) bool {
+	return wl != nil && wl.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] == "true"
+}
+
+// IsVariant returns true if the workload is a variant.
+func IsVariant(wl *kueue.Workload) bool {
+	if wl == nil {
+		return false
+	}
+	return GetParentWorkloadName(wl) != ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	flavors := parseAllowedFlavorsString(wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+	if len(flavors) == 0 {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(flavors[0])
+}
+
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+func IsFlavorAllowedForVariant(wl *kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	val, ok := wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]
+	if !ok {
+		return true
+	}
+	allowedFlavors := parseAllowedFlavorsString(val)
+	return slices.Contains(allowedFlavors, string(flavor))
+}
+
+// parseAllowedFlavorsString parses a comma-separated string of flavor names
+// and returns a slice of flavor names with leading and trailing spaces removed.
+func parseAllowedFlavorsString(s string) []string {
+	if strings.TrimSpace(s) == "" {
+		return []string{}
+	}
+	parts := strings.Split(s, ",")
+	for i := range parts {
+		parts[i] = strings.TrimSpace(parts[i])
+	}
+	return parts
+}
```

  I see, ok.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T08:45:49Z

- **pkg/workload/concurrentadmission/concurrentadmission.go:109** by [@mimowo](https://github.com/mimowo) — 2026-04-30T08:45:49Z
```diff
@@ -0,0 +1,109 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package concurrentadmission
+
+import (
+	"slices"
+	"strings"
+
+	"k8s.io/apimachinery/pkg/types"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	controllerconstants "sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/features"
+)
+
+// SetParentVariantLabel sets the label indicating the workload is a parent variant.
+func SetParentVariantLabel(workload *kueue.Workload) {
+	if workload.Labels == nil {
+		workload.Labels = make(map[string]string, 1)
+	}
+	workload.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] = "true"
+}
+
+// IsParent returns true if the workload is a parent variant.
+func IsParent(wl *kueue.Workload) bool {
+	return wl != nil && wl.Labels[controllerconstants.ConcurrentAdmissionParentLabelKey] == "true"
+}
+
+// IsVariant returns true if the workload is a variant.
+func IsVariant(wl *kueue.Workload) bool {
+	if wl == nil {
+		return false
+	}
+	return GetParentWorkloadName(wl) != ""
+}
+
+// GetVariantFlavor returns the allowed flavor for a variant from annotations.
+func GetVariantFlavor(wl *kueue.Workload) kueue.ResourceFlavorReference {
+	flavors := parseAllowedFlavorsString(wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation])
+	if len(flavors) == 0 {
+		return ""
+	}
+	return kueue.ResourceFlavorReference(flavors[0])
+}
+
+// GetParentWorkloadName returns the name of the parent workload from owner references.
+func GetParentWorkloadName(wl *kueue.Workload) string {
+	if wl == nil {
+		return ""
+	}
+	for _, owner := range wl.OwnerReferences {
+		if owner.Kind == "Workload" && owner.APIVersion == kueue.GroupVersion.String() {
+			return owner.Name
+		}
+	}
+	return ""
+}
+
+// GetParentWorkloadUID returns the UID of the parent workload from owner references.
+func GetParentWorkloadUID(wl *kueue.Workload) types.UID {
+	if wl == nil {
+		return ""
+	}
+	for _, ref := range wl.OwnerReferences {
+		if ref.Kind == "Workload" && ref.APIVersion == kueue.GroupVersion.String() {
+			return ref.UID
+		}
+	}
+	return ""
+}
+
+func IsFlavorAllowedForVariant(wl *kueue.Workload, flavor kueue.ResourceFlavorReference) bool {
+	if !features.Enabled(features.ConcurrentAdmission) {
+		return true
+	}
+	val, ok := wl.GetAnnotations()[controllerconstants.WorkloadAllowedResourceFlavorAnnotation]
+	if !ok {
+		return true
+	}
+	allowedFlavors := parseAllowedFlavorsString(val)
+	return slices.Contains(allowedFlavors, string(flavor))
+}
+
+// parseAllowedFlavorsString parses a comma-separated string of flavor names
+// and returns a slice of flavor names with leading and trailing spaces removed.
+func parseAllowedFlavorsString(s string) []string {
+	if strings.TrimSpace(s) == "" {
+		return []string{}
+	}
+	parts := strings.Split(s, ",")
+	for i := range parts {
+		parts[i] = strings.TrimSpace(parts[i])
+	}
+	return parts
+}
```

  Still, I would suggest this as one of the first follow ups to avoid keeping duplicated code for long.
  
  And add it to the list of follow ups in the PR description.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T08:48:16Z

- **pkg/cache/queue/manager.go:592** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T08:48:15Z
```diff
@@ -546,6 +583,13 @@ func (m *Manager) ClusterQueueForWorkload(wl *kueue.Workload) (kueue.ClusterQueu
 func (m *Manager) AddOrUpdateWorkload(log logr.Logger, w *kueue.Workload, opts ...workload.InfoOption) error {
 	m.Lock()
 	defer m.Unlock()
+	if features.Enabled(features.ConcurrentAdmission) {
+		cqName, _ := m.ClusterQueueForWorkloadWithoutLock(w)
+		if m.ConcurrentAdmissionEnabledWithoutLock(cqName) && concurrentadmission.IsVariant(w) {
+			return m.AddOrUpdateWorkloadWithoutLock(log, w, opts...)
+		}
+		return nil
+	}
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T09:15:56Z

- **pkg/scheduler/scheduler.go:712** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T09:15:55Z
```diff
@@ -662,6 +706,10 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	flvAssigner := flavorassigner.New(wl, cq, snap.ResourceFlavors, fairsharing.Enabled(s.fairSharing), preemption.NewOracle(s.preemptor, snap), replaceableWorkloadSlice)
 	fullAssignment := flvAssigner.Assign(log, nil)
 
+	// Add less favorable siblings to preemption targets once here.
+	if lessFavorableSibling := s.getLessFavorableSibling(log, wl, fullAssignment, snap); lessFavorableSibling != nil {
+		preemptionTargets = append(preemptionTargets, lessFavorableSibling)
+	}
```

  I agree, after the changes to not use issuePreemptions there is not much benefits out of it. Cleaned up following proposal 2)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T09:54:26Z

- **pkg/scheduler/scheduler.go:440** by [@mimowo](https://github.com/mimowo) — 2026-04-30T09:54:26Z
```diff
@@ -433,6 +434,11 @@ func (s *Scheduler) processEntry(
 		}
 	}
 
+	if lessFavorableSibling := s.getLessFavorableSibling(log, &e.Info, snapshot); features.Enabled(features.ConcurrentAdmission) && lessFavorableSibling != nil {
+		s.issueMigration(ctx, log, e, lessFavorableSibling)
+		return
+	}
```

  ```suggestion
      if features.Enabled(features.ConcurrentAdmission)  {
  	if lessFavorableSibling := s.getLessFavorableSibling(log, &e.Info, snapshot); && lessFavorableSibling != nil {
  		s.issueMigration(ctx, log, e, lessFavorableSibling)
  		return
  	}
  	}
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T09:55:31Z

- **pkg/scheduler/scheduler.go:724** by [@mimowo](https://github.com/mimowo) — 2026-04-30T09:55:31Z
```diff
@@ -697,6 +721,53 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  Let's move the new functions to the bottom, the seem injected in a random place, or move the a dedicated file in the scheduler's package.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T10:26:33Z

- **pkg/scheduler/scheduler.go:724** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T10:26:33Z
```diff
@@ -697,6 +721,53 @@ func (s *Scheduler) getInitialAssignments(log logr.Logger, wl *workload.Info, sn
 	return fullAssignment, nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-04-30T10:26:44Z

- **pkg/scheduler/scheduler.go:440** by [@PBundyra](https://github.com/PBundyra) — 2026-04-30T10:26:44Z
```diff
@@ -433,6 +434,11 @@ func (s *Scheduler) processEntry(
 		}
 	}
 
+	if lessFavorableSibling := s.getLessFavorableSibling(log, &e.Info, snapshot); features.Enabled(features.ConcurrentAdmission) && lessFavorableSibling != nil {
+		s.issueMigration(ctx, log, e, lessFavorableSibling)
+		return
+	}
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T11:20:59Z

Thank you for working on the epic feature 👍 Let's make sure we address as much follow ups as possible before 0.18.

/lgtm
/approve
