# PR #10623: Improve eviction message for AdmissionChecks in Retry state to includ…

**Summary**: Improve eviction message for AdmissionChecks in Retry state to includ…

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10623

**Last updated**: 2026-04-22T09:50:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@reruno](https://github.com/reruno)
- **Branch**: `feature/10618` → `main`
- **Created**: 2026-04-20T12:48:07Z
- **Updated**: 2026-04-22T09:50:03Z
- **Merged**: 2026-04-21T10:31:53Z
- **Closed**: 2026-04-21T10:31:53Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@olekzabl](https://github.com/olekzabl), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+168 / -32**

## Description

…e per-check name and reason

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:
Improve eviction message for AdmissionChecks in Retry state to include per-check name and reason
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10618 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Improve eviction message for AdmissionChecks in Retry state to include per-check name and reason
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-20T12:48:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 479dc9be37eb827d34121839fa152dcd6e8385ed |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69e748aa6c03110008318b33 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T12:48:18Z

Hi @reruno. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-20T12:49:34Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:26:34Z

LGTM overall, let me also ask @pajakd for another pass, especially as he is working also on observability improvements in the area.

### Comment by [@pajakd](https://github.com/pajakd) — 2026-04-21T09:56:18Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-21T09:56:27Z

LGTM label has been added.  <details>Git tree hash: 948c17d3c6dbee0aebb92827408738e8f91e2b05</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-21T10:30:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10623#pullrequestreview-4146956607" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10623#" title="Author self-approved">reruno</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-04-20T13:27:09Z

- **pkg/workload/admissionchecks.go:163** by [@kshalot](https://github.com/kshalot) — 2026-04-20T13:01:48Z
```diff
@@ -151,6 +151,18 @@ func RejectedChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
 	return rejectedChecks
 }
 
+// RetryChecks returns the list of Retry admission checks
+func RetryChecks(wl *kueue.Workload) []kueue.AdmissionCheckState {
+	retryChecks := make([]kueue.AdmissionCheckState, 0, len(wl.Status.AdmissionChecks))
+	for i := range wl.Status.AdmissionChecks {
+		ac := wl.Status.AdmissionChecks[i]
+		if ac.State == kueue.CheckStateRetry {
+			retryChecks = append(retryChecks, ac)
+		}
+	}
+	return retryChecks
```

  Nit: Since this is pretty much identical to the existing `RejectedChecks`, we could de-duplicate with a helper: 
  ```go
  func matchingChecks(wl *kueue.Workload, s CheckState) []kueue.AdmissionCheckState
  ```

- **pkg/controller/core/workload_controller.go:760** by [@kshalot](https://github.com/kshalot) — 2026-04-20T13:21:13Z
```diff
@@ -745,7 +745,19 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 		return true, nil
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
-	message := "At least one admission check is false"
+	retryChecks := workload.RetryChecks(wl)
+	slices.SortFunc(retryChecks, func(a, b kueue.AdmissionCheckState) int {
+		return cmp.Compare(a.Name, b.Name)
+	})
+	parts := make([]string, 0, len(retryChecks))
+	for _, ac := range retryChecks {
+		if ac.Message != "" {
+			parts = append(parts, fmt.Sprintf("%q %s", ac.Name, ac.Message))
+		} else {
+			parts = append(parts, fmt.Sprintf("%q", ac.Name))
+		}
+	}
+	message := fmt.Sprintf("There are %d admission check(s) in Retry state: %s", len(retryChecks), stringsutils.Join(parts, "; "))
```

  Nit: I realize this format is taken from the issue description, but I'm wondering whether sth like:
  ```
  Retrying %d admission check(s): %q (%s), %q (%s), %q (%s)
  ```
  wouldn't sound more natural and be a little easier to parse with your eyes. But I don't think we are using some strict convention for messages like these. I took a quick look at the [api-conventions](https://github.com/kubernetes/community/blob/main/contributors/devel/sig-architecture/api-conventions.md), but I could only find some for error messages.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T13:55:48Z

- **pkg/controller/core/workload_controller.go:760** by [@mimowo](https://github.com/mimowo) — 2026-04-20T13:55:48Z
```diff
@@ -745,7 +745,19 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 		return true, nil
 	}
 	// at this point we know a Workload has at least one Retry AdmissionCheck
-	message := "At least one admission check is false"
+	retryChecks := workload.RetryChecks(wl)
+	slices.SortFunc(retryChecks, func(a, b kueue.AdmissionCheckState) int {
+		return cmp.Compare(a.Name, b.Name)
+	})
+	parts := make([]string, 0, len(retryChecks))
+	for _, ac := range retryChecks {
+		if ac.Message != "" {
+			parts = append(parts, fmt.Sprintf("%q (%s)", ac.Name, ac.Message))
+		} else {
+			parts = append(parts, fmt.Sprintf("%q", ac.Name))
+		}
+	}
+	message := fmt.Sprintf("There are %d admission check(s) in Retry state: %s", len(retryChecks), stringsutils.Join(parts, ", "))
```

  Wrap it into a helper like buildRetryMessage, 
  
  Also, I would like commonize the message building for Rejected (above), so that there we also have more details.
  
  Maybe something like "Deactivating workload, because there are %d admission check(s) in Rejected state: %s".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T17:01:08Z

- **pkg/controller/core/workload_controller.go:758** by [@mimowo](https://github.com/mimowo) — 2026-04-20T17:01:08Z
```diff
@@ -729,23 +746,21 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
 	if workload.HasRejectedChecks(wl) {
-		var rejectedCheckNames []kueue.AdmissionCheckReference
-		for _, check := range workload.RejectedChecks(wl) {
-			rejectedCheckNames = append(rejectedCheckNames, check.Name)
-		}
+		rejectedChecks := workload.RejectedChecks(wl)
+		message := buildAdmissionChecksMessage(rejectedChecks, kueue.CheckStateRejected)
 		err := workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(wl *kueue.Workload) (bool, error) {
-			return workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, fmt.Sprintf("Admission check(s): %v, were rejected", stringsutils.Join(rejectedCheckNames, ","))), nil
+			return workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, message), nil
 		})
 		if err != nil {
 			return false, err
 		}
-		log.V(3).Info("Workload is evicted due to rejected admission checks", "workload", klog.KObj(wl), "rejectedChecks", rejectedCheckNames)
-		rejectedCheck := workload.RejectedChecks(wl)[0]
-		r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v was Rejected: %s", rejectedCheck.Name, rejectedCheck.Message)
+		log.V(3).Info("Workload is evicted due to rejected admission checks", "workload", klog.KObj(wl), "rejectedChecks", rejectedChecks)
+		r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload, because there are %s", message)
```

  Let's reuse the same detailed message for the Event and the condition, for consistency, similar to what workload.Evict(...) does. 
  
  I would also prefer to:
  - drop the explicit count, as in 99% there will be only one, and they are all listed anyway
  - differentiate based on singular / plural so that it reads more naturally, in 99% there will be only one
  
  *Rejected*: 
  singular: Deactivated due to AdmissionCheck in Rejected state: (...)
  plural: Deactivated due to AdmissionChecks in Rejected state: (...)
  * Retry*: 
  singular: Evicted due to AdmissionCheck in Retry state: (...)
  plural: Evicted due to AdmissionChecks in Retry state: (...)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T17:02:17Z

- **pkg/controller/core/workload_controller_test.go:872** by [@mimowo](https://github.com/mimowo) — 2026-04-20T17:02:18Z
```diff
@@ -819,17 +819,19 @@ func TestReconcile(t *testing.T) {
 				AdmittedAt(true, now).
 				ControllerReference(batchv1.SchemeGroupVersion.WithKind("Job"), "ownername", "owneruid").
 				AdmissionCheck(kueue.AdmissionCheckState{
-					Name:  "check",
-					State: kueue.CheckStateRejected,
+					Name:    "check",
+					State:   kueue.CheckStateRejected,
+					Message: "quota exceeded",
```

  Let's add a new test case "with message" rather than modidying the existing one.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-20T17:02:41Z

- **pkg/controller/core/workload_controller_test.go:1021** by [@mimowo](https://github.com/mimowo) — 2026-04-20T17:02:42Z
  Let's add a new test case rather than modifying the existing one

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T08:57:33Z

- **pkg/controller/core/workload_controller.go:476** by [@mimowo](https://github.com/mimowo) — 2026-04-21T08:57:33Z
```diff
@@ -465,7 +466,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if !workload.IsEvicted(&wl) {
 			if dtCond != nil {
 				underlyingCause = kueue.EvictionUnderlyingCause(dtCond.Reason)
-				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				if strings.HasPrefix(dtCond.Message, "Deactivated due to ") {
+					// dtCond.Message is already a complete sentence (e.g. produced by
+					// buildAdmissionChecksMessage); avoid duplicating the "deactivated due to"
+					// fragment by prepending "The workload is " and lowercasing the first letter.
+					message = "The workload is " + strings.ToLower(dtCond.Message[:1]) + dtCond.Message[1:]
+				} else {
+					message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				}
```

  I'm not sure why this is changed. I thought the message would be taken from the DeactivationTarget which has the complete message. 
  
  Can you provide what would be the message without / with the change in the discussion thread.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T09:00:44Z

- **pkg/controller/core/workload_controller.go:476** by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:00:44Z
```diff
@@ -465,7 +466,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if !workload.IsEvicted(&wl) {
 			if dtCond != nil {
 				underlyingCause = kueue.EvictionUnderlyingCause(dtCond.Reason)
-				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				if strings.HasPrefix(dtCond.Message, "Deactivated due to ") {
+					// dtCond.Message is already a complete sentence (e.g. produced by
+					// buildAdmissionChecksMessage); avoid duplicating the "deactivated due to"
+					// fragment by prepending "The workload is " and lowercasing the first letter.
+					message = "The workload is " + strings.ToLower(dtCond.Message[:1]) + dtCond.Message[1:]
+				} else {
+					message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				}
```

  I basically don't like the string manupulations like that `"The workload is " + strings.ToLower(dtCond.Message[:1]) + dtCond.Message[1:]`. I think we need to rethink how to approach it. I gave some proposals for the messages, but if they require this kind of manipulations I'm ok to adjust them, but I need to understand why is happening.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-04-21T09:01:21Z

- **pkg/controller/core/workload_controller.go:476** by [@reruno](https://github.com/reruno) — 2026-04-21T09:01:21Z
```diff
@@ -465,7 +466,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if !workload.IsEvicted(&wl) {
 			if dtCond != nil {
 				underlyingCause = kueue.EvictionUnderlyingCause(dtCond.Reason)
-				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				if strings.HasPrefix(dtCond.Message, "Deactivated due to ") {
+					// dtCond.Message is already a complete sentence (e.g. produced by
+					// buildAdmissionChecksMessage); avoid duplicating the "deactivated due to"
+					// fragment by prepending "The workload is " and lowercasing the first letter.
+					message = "The workload is " + strings.ToLower(dtCond.Message[:1]) + dtCond.Message[1:]
+				} else {
+					message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				}
```

  Without this change message in condition will look like this 
  `The workload is deactivated due to Deactivated due to AdmissionCheck in Rejected state: "check"`
  
  I think it is best to revert this change completely and make different message in condition and event in `buildAdmissionChecksMessage`, I am working on it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T09:04:44Z

- **pkg/controller/core/workload_controller.go:476** by [@mimowo](https://github.com/mimowo) — 2026-04-21T09:04:44Z
```diff
@@ -465,7 +466,14 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		if !workload.IsEvicted(&wl) {
 			if dtCond != nil {
 				underlyingCause = kueue.EvictionUnderlyingCause(dtCond.Reason)
-				message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				if strings.HasPrefix(dtCond.Message, "Deactivated due to ") {
+					// dtCond.Message is already a complete sentence (e.g. produced by
+					// buildAdmissionChecksMessage); avoid duplicating the "deactivated due to"
+					// fragment by prepending "The workload is " and lowercasing the first letter.
+					message = "The workload is " + strings.ToLower(dtCond.Message[:1]) + dtCond.Message[1:]
+				} else {
+					message = fmt.Sprintf("%s due to %s", message, dtCond.Message)
+				}
```

  Maybe the message for conditions could be just `AdmissionCheck in Rejected state: "check"`, and `AdmissionCheck in Retry state: "check"`, because `Evicted` / `Deactivated` is anyway redundant given the condition type.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-21T09:46:37Z

- **pkg/controller/core/workload_controller.go:761** by [@pajakd](https://github.com/pajakd) — 2026-04-21T09:46:37Z
```diff
@@ -729,23 +750,21 @@ func (r *WorkloadReconciler) reconcileCheckBasedEviction(ctx context.Context, wl
 	log := ctrl.LoggerFrom(ctx)
 	log.V(3).Info("Workload is evicted due to admission checks")
 	if workload.HasRejectedChecks(wl) {
-		var rejectedCheckNames []kueue.AdmissionCheckReference
-		for _, check := range workload.RejectedChecks(wl) {
-			rejectedCheckNames = append(rejectedCheckNames, check.Name)
-		}
+		rejectedChecks := workload.RejectedChecks(wl)
+		message := buildAdmissionChecksMessage(rejectedChecks, kueue.CheckStateRejected)
 		err := workload.PatchAdmissionStatus(ctx, r.client, wl, r.clock, func(wl *kueue.Workload) (bool, error) {
-			return workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, fmt.Sprintf("Admission check(s): %v, were rejected", stringsutils.Join(rejectedCheckNames, ","))), nil
+			return workload.SetDeactivationTarget(wl, kueue.WorkloadEvictedByAdmissionCheck, message), nil
 		})
 		if err != nil {
 			return false, err
 		}
-		log.V(3).Info("Workload is evicted due to rejected admission checks", "workload", klog.KObj(wl), "rejectedChecks", rejectedCheckNames)
-		rejectedCheck := workload.RejectedChecks(wl)[0]
-		r.recorder.Eventf(wl, corev1.EventTypeWarning, "AdmissionCheckRejected", "Deactivating workload because AdmissionCheck for %v was Rejected: %s", rejectedCheck.Name, rejectedCheck.Message)
+		log.V(3).Info("Workload is evicted due to rejected admission checks", "workload", klog.KObj(wl), "rejectedChecks", rejectedChecks)
```

  The log should probably say "Workload is deactivated", right?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-21T10:30:16Z

Thanks 👍 
/approve
/cherrypick release-0.16
/cherrypick release-0.17
