# PR #10633: Add metrics for time to preemption

**Summary**: Add metrics for time to preemption

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10633

**Last updated**: 2026-05-07T09:09:23Z

---

## Metadata

- **State**: open
- **Author**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Branch**: `PendingWaitMetricsWorker` → `main`
- **Created**: 2026-04-20T22:50:14Z
- **Updated**: 2026-05-07T09:09:23Z
- **Merged**: —
- **Closed**: —
- **Labels**: `release-note`, `size/XL`, `needs-rebase`, `do-not-merge/hold`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@mszadkow](https://github.com/mszadkow), [@mykysha](https://github.com/mykysha), [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y), [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 17   **+624 / -2**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #10124

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Observability: introduce a guage metric, called pending_workload_wait_time_seconds, which allows to track
the current waiting time for the pending workloads. This metric is currently available behind the 
PendingWorkloadsMetrics feature gate (alpha, disabled by default).
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T22:50:17Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-20T22:50:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bda4d000ac50942fe845aa3f1710b122b959bc6c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f34c6a01a8cc000808454f |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10633--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10633--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwNjMzLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.Wye7HlkYrqpy8rcNgQyBO09le9gFzEBkbcvyRTJ82c0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10633--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-20T22:50:26Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10633#" title="Author self-approved">vladikkuzn</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [mbobrovskyi](https://github.com/mbobrovskyi), [mimowo](https://github.com/mimowo) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["mbobrovskyi","mimowo"]} -->

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-20T22:50:32Z

/ok-to-test
/assign

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-29T16:19:58Z

/cc @mbobrovskyi @mykysha @mszadkow

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-29T18:57:36Z

@vladikkuzn could you please rebase?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:18:08Z

/release-note-edit
```release-note
Observability: introduce a guage metric, called pending_workload_wait_time_seconds, which allows to track
the current waiting time for the pending workloads. This metric is currently available behind the 
PendingWorkloadsMetrics feature gate (alpha, disabled by default).
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:39:33Z

cc @amy would you like to give it a pass?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-30T17:39:43Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T16:26:42Z

/hold
Thank you for the PR - it is close to being ready, and it shows us what is the complexity added by the metric - it is significant, but not a blocker if we have a really good use case. 

However, I'm still not clear if the use cases can or cannot be covered by the more idiomatic approaches first, posted here: https://github.com/kubernetes-sigs/kueue/issues/10124#issuecomment-4372642534. Let's discuss to determine that first.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-07T09:09:23Z

@vladikkuzn: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-large-scale-scheduling-perf-main | bda4d000ac50942fe845aa3f1710b122b959bc6c | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10633/pull-kueue-test-large-scale-scheduling-perf-main/2052314679782412288) | true | `/test pull-kueue-test-large-scale-scheduling-perf-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=10633). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Avladikkuzn). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-21T20:39:23Z

- **pkg/cache/queue/pending_wait_metrics.go:38** by [@mwielgus](https://github.com/mwielgus) — 2026-04-21T20:31:58Z
```diff
@@ -0,0 +1,219 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+type queuedWaitAgg struct {
```

  Can you add some commends here and below?

- **pkg/cache/queue/pending_wait_metrics.go:208** by [@mwielgus](https://github.com/mwielgus) — 2026-04-21T20:33:32Z
```diff
@@ -0,0 +1,219 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+type queuedWaitAgg struct {
+	n   int
+	sum float64
+	max float64
+}
+
+func (a *queuedWaitAgg) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	a.n++
+	a.sum += sec
+	if a.n == 1 || sec > a.max {
+		a.max = sec
+	}
+}
+
+func (a queuedWaitAgg) finalize() (max, mean float64) {
+	if a.n == 0 {
+		return 0, 0
+	}
+	return a.max, a.sum / float64(a.n)
+}
+
+func maxQueuedWaitAcross(a, b queuedWaitAgg) float64 {
+	switch {
+	case a.n == 0:
+		return b.max
+	case b.n == 0:
+		return a.max
+	default:
+		return math.Max(a.max, b.max)
+	}
+}
+
+// pendingWaitAggregates computes max/mean queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitAggregates(clk clock.Clock, cqSchedulingActive bool) (activeMax, activeMean float64, activeN int, inadmMax, inadmMean float64, inadmN int) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var active, inadm queuedWaitAgg
+	for _, wi := range c.heap.List() {
+		active.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		active.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadm.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		totalN := active.n + inadm.n
+		if totalN == 0 {
+			return 0, 0, 0, 0, 0, 0
+		}
+		sum := active.sum + inadm.sum
+		inadmMean = sum / float64(totalN)
+		inadmMax = maxQueuedWaitAcross(active, inadm)
+		return 0, 0, 0, inadmMax, inadmMean, totalN
+	}
+
+	amx, amn := active.finalize()
+	imx, imn := inadm.finalize()
+	return amx, amn, active.n, imx, imn, inadm.n
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("pending_wait_metrics_worker")
+	ctx = ctrl.LoggerInto(ctx, log)
+	go func() {
+		<-ctx.Done()
+		w.queue.ShutDown()
+	}()
+
+	w.enqueueAllClusterQueues()
+
+	ticker := time.NewTicker(w.tickPeriod)
+	go func() {
+		defer ticker.Stop()
+		for {
+			select {
+			case <-ctx.Done():
+				return
+			case <-ticker.C:
+				w.enqueueAllClusterQueues()
+			}
+		}
+	}()
+
+	for {
+		item, shutdown := w.queue.Get()
+		if shutdown {
+			return nil
+		}
+		w.reconcile(ctx, item)
+		w.queue.Done(item)
+	}
+}
+
+func (w *PendingWaitMetricsWorker) enqueueAllClusterQueues() {
```

  Can you introduce some minor jitter to evaluation, so not all CQ are processed at the very same time?

- **pkg/cache/queue/pending_wait_metrics.go:218** by [@mwielgus](https://github.com/mwielgus) — 2026-04-21T20:35:02Z
```diff
@@ -0,0 +1,219 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+type queuedWaitAgg struct {
+	n   int
+	sum float64
+	max float64
+}
+
+func (a *queuedWaitAgg) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	a.n++
+	a.sum += sec
+	if a.n == 1 || sec > a.max {
+		a.max = sec
+	}
+}
+
+func (a queuedWaitAgg) finalize() (max, mean float64) {
+	if a.n == 0 {
+		return 0, 0
+	}
+	return a.max, a.sum / float64(a.n)
+}
+
+func maxQueuedWaitAcross(a, b queuedWaitAgg) float64 {
+	switch {
+	case a.n == 0:
+		return b.max
+	case b.n == 0:
+		return a.max
+	default:
+		return math.Max(a.max, b.max)
+	}
+}
+
+// pendingWaitAggregates computes max/mean queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitAggregates(clk clock.Clock, cqSchedulingActive bool) (activeMax, activeMean float64, activeN int, inadmMax, inadmMean float64, inadmN int) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var active, inadm queuedWaitAgg
+	for _, wi := range c.heap.List() {
+		active.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		active.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadm.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		totalN := active.n + inadm.n
+		if totalN == 0 {
+			return 0, 0, 0, 0, 0, 0
+		}
+		sum := active.sum + inadm.sum
+		inadmMean = sum / float64(totalN)
+		inadmMax = maxQueuedWaitAcross(active, inadm)
+		return 0, 0, 0, inadmMax, inadmMean, totalN
+	}
+
+	amx, amn := active.finalize()
+	imx, imn := inadm.finalize()
+	return amx, amn, active.n, imx, imn, inadm.n
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("pending_wait_metrics_worker")
+	ctx = ctrl.LoggerInto(ctx, log)
+	go func() {
+		<-ctx.Done()
+		w.queue.ShutDown()
+	}()
+
+	w.enqueueAllClusterQueues()
+
+	ticker := time.NewTicker(w.tickPeriod)
+	go func() {
+		defer ticker.Stop()
+		for {
+			select {
+			case <-ctx.Done():
+				return
+			case <-ticker.C:
+				w.enqueueAllClusterQueues()
+			}
+		}
+	}()
+
+	for {
+		item, shutdown := w.queue.Get()
+		if shutdown {
+			return nil
+		}
+		w.reconcile(ctx, item)
+		w.queue.Done(item)
+	}
+}
+
+func (w *PendingWaitMetricsWorker) enqueueAllClusterQueues() {
+	if w.manager == nil {
+		return
+	}
+	for _, name := range w.manager.GetClusterQueueNames() {
+		w.notify(name)
+	}
+}
+
+func (w *PendingWaitMetricsWorker) reconcile(ctx context.Context, cqName kueue.ClusterQueueReference) {
+	_ = ctx
+	m := w.manager
+	if m == nil {
+		return
+	}
+	cq := m.getClusterQueue(cqName)
+	if cq == nil {
+		metrics.ClearPendingWorkloadWaitTimes(cqName)
+		return
+	}
+	cqSchedulingActive := m.statusChecker == nil || m.statusChecker.ClusterQueueActive(cqName)
+	activeMax, activeMean, activeN, inadmMax, inadmMean, inadmN := cq.pendingWaitAggregates(m.clock, cqSchedulingActive)
+	metrics.ReportPendingWorkloadWaitTimes(cqName, activeMax, activeMean, activeN, inadmMax, inadmMean, inadmN, m.customLabels.CQGet(cq.name), m.roleTracker)
```

  Is cq.name the same as cqName?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T17:12:04Z

- **pkg/metrics/metrics.go:373** by [@mimowo](https://github.com/mimowo) — 2026-04-23T17:12:04Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
```

  I think for this metric it would be very helpful to include the longest pending workload, so that the admin can react. Maybe a dedicated label like "workload", which would be namespace/name format.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T17:12:49Z

- **pkg/metrics/metrics.go:385** by [@mimowo](https://github.com/mimowo) — 2026-04-23T17:12:50Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
+	)
+	trackGaugeVec(PendingWorkloadMaxWaitTimeSeconds, gaugeCleanupScopeClusterQueue)
+
+	PendingWorkloadMeanWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_mean_wait_time_seconds",
+			Help: `The mean queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Semantics match kueue_pending_workload_max_wait_time_seconds.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
+	)
+	trackGaugeVec(PendingWorkloadMeanWaitTimeSeconds, gaugeCleanupScopeClusterQueue)
```

  For this metric I would rather have `PendingWorkloadWaitTimeSeconds{quantile=0.5|0.95|0.99}`, I suppose this will not complicate the code that much.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T17:15:10Z

- **pkg/metrics/metrics.go:363** by [@mimowo](https://github.com/mimowo) — 2026-04-23T17:15:10Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
```

  Let's keep both metrics under a feature gate, like PendingWorkloadsMetrics, because they introduce new mechanism which may have non trivial consequences. I think we could start with Alpha in 0.18

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-23T22:11:05Z

- **pkg/metrics/metrics.go:373** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-23T22:11:05Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
```

  > I think for this metric it would be very helpful to include the longest pending workload, so that the admin can react.
  
  Following the same pattern as [kube_pod_start_time](https://github.com/kubernetes/kube-state-metrics/blob/main/docs/metrics/workload/pod-metrics.md), age is computed in PromQL (time() - metric), I think we can add:
  ```
  # HELP kueue_pending_workload_oldest_start_time_seconds Unix timestamp of the oldest pending workload in a ClusterQueue, by status.
  # TYPE kueue_pending_workload_oldest_start_time_seconds gauge
  kueue_pending_workload_oldest_start_time_seconds{cluster_queue, status, replica_role}
  ```
  And the same for LQ:
  ```
  kueue_local_queue_pending_workload_oldest_start_time_seconds{name, namespace, status, replica_role}
  ```
  This could be potential alerts based on those:
  ```
  - alert: KueueWorkloadPendingTooLong
    expr: time() - kueue_pending_workload_oldest_start_time_seconds > 1800
    for: 5m
    labels:
      severity: warning
    annotations:
      summary: "Workload pending >30m in ClusterQueue {{ $labels.cluster_queue }} (status={{ $labels.status }})"
  
  - alert: KueueWorkloadInadmissibleTooLong
    expr: time() - kueue_pending_workload_oldest_start_time_seconds{status="inadmissible"} > 3600
    for: 10m
    ```
  
  > Maybe a dedicated label like "workload", which would be namespace/name format.
  
  We need to avoid workload and priority_class labels and as it blow-ups cardinality, or make it opt-in

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-23T22:28:28Z

- **pkg/metrics/metrics.go:373** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-23T22:28:28Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
```

  I can also propose to add traces per-workload via OpenTelemetry as a longer term goal. This would enable true per-workload analytics for power users

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-29T02:01:02Z

- **pkg/metrics/metrics.go:373** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-29T02:01:02Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
```

  @mimowo quick demo is available here: https://github.com/kubernetes-sigs/kueue/pull/10818

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:14:26Z

- **pkg/metrics/metrics.go:373** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:14:26Z
```diff
@@ -352,6 +360,30 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadMaxWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_max_wait_time_seconds",
+			Help: `The maximum queued wait time in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue' and 'status'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.`,
+		}, append([]string{"cluster_queue", "status", "replica_role"}, extraLabels...),
```

  This is really cool, but let's discuss that on the PR. Let's discuss the OpenTelemetry stuff under the PR/issue.
  
  As for the metric for the longest waiting workload, if we could include the workload name / namespace in the guage metric it would be very useful for admins to setup alerting on, and look at such workloads. We can discuss details in the follow up PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:23:48Z

- **pkg/cache/queue/pending_wait_metrics.go:76** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:23:49Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
```

  The comment does not match the  implementation. The comment says ceiling while the impl is using rounding as I see int(  x +0.5). I think this looks correct `idx := int(math.Ceil(q*float64(len(sorted)))) - 1`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:38:58Z

- **pkg/metrics/metrics.go:370** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:38:58Z
```diff
@@ -352,6 +356,21 @@ The label 'result' can have the following values:
 	)
 	trackGaugeVec(PendingWorkloads, gaugeCleanupScopeClusterQueue)
 
+	PendingWorkloadWaitTimeSeconds = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "pending_workload_wait_time_seconds",
+			Help: `Queued wait time quantiles in seconds among pending workloads in this ClusterQueue bucket,
+per 'cluster_queue', 'status', and 'quantile'. Queued wait time is from workload creation or last requeue until now.
+'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),
+"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.
+When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.
+No time series is reported for a bucket with zero pending workloads.
+Requires feature gate PendingWorkloadsMetrics (alpha in 0.18).`,
+		}, append([]string{"cluster_queue", "status", "quantile", "replica_role"}, extraLabels...),
```

  I would suggest the status to a follow up PR, because:
  
  For the foundation PR, I think the metric should answer the core request from the issue: “what is the pending wait time distribution for this ClusterQueue?” Splitting active vs inadmissible may be useful, but it is not requested in the issue and it complicates both the implementation and the metric surface.
  
  Since this is alpha, we can still evolve it. If we add breakdowns later, I would prefer to design that deliberately around multiple possible dimensions, for example `status` and `priority_class`, rather than hard-coding the implementation around only the active/inadmissible split.
  
  So my preference would be: one aggregated vector for now, and discuss status/priority breakdowns as a follow-up.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:50:39Z

- **pkg/cache/queue/pending_wait_metrics.go:219** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:50:39Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("pending_wait_metrics_worker")
+	ctx = ctrl.LoggerInto(ctx, log)
+	go func() {
+		<-ctx.Done()
+		w.queue.ShutDown()
+	}()
+
+	w.enqueueAllClusterQueues()
+
+	ticker := time.NewTicker(w.tickPeriod)
+	go func() {
+		defer ticker.Stop()
+		for {
+			select {
+			case <-ctx.Done():
+				return
+			case <-ticker.C:
+				w.enqueueAllClusterQueues()
+			}
+		}
+	}()
+
+	for {
+		item, shutdown := w.queue.Get()
+		if shutdown {
+			return nil
+		}
+		w.reconcile(ctx, item)
+		w.queue.Done(item)
+	}
+}
+
+// enqueueAllClusterQueues schedules all known ClusterQueues for metrics re-evaluation.
+// A random jitter of up to 50% of the tick period is added per CQ to spread reconcile
+// work and avoid processing all queues at the same instant.
+func (w *PendingWaitMetricsWorker) enqueueAllClusterQueues() {
+	if w.manager == nil {
+		return
+	}
+	maxJitter := int64(w.tickPeriod / 2)
+	for _, name := range w.manager.GetClusterQueueNames() {
+		jitter := time.Duration(rand.Int64N(maxJitter))
+		w.queue.AddAfter(name, w.batchPeriod+jitter)
+	}
+}
+
+func (w *PendingWaitMetricsWorker) reconcile(ctx context.Context, cqName kueue.ClusterQueueReference) {
```

  Seems like ctx is unused, let's use it for logging at V4

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:51:36Z

- **pkg/cache/queue/manager.go:214** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:51:36Z
```diff
@@ -200,10 +209,20 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		option(m)
 	}
 	m.requeuer.setManager(m)
+	if m.pendingWaitMetrics != nil {
+		m.pendingWaitMetrics.setManager(m)
+	}
```

  hide it behind the FG, just in case

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:52:01Z

- **pkg/cache/queue/manager.go:220** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:52:01Z
```diff
@@ -200,10 +209,20 @@ func NewManager(client client.Client, checker StatusChecker, requeuer inadmissib
 		option(m)
 	}
 	m.requeuer.setManager(m)
+	if m.pendingWaitMetrics != nil {
+		m.pendingWaitMetrics.setManager(m)
+	}
 	m.cond.L = &m.RWMutex
 	return m
 }
 
+func (m *Manager) notifyPendingWaitMetrics(cqName kueue.ClusterQueueReference) {
+	if m.pendingWaitMetrics == nil {
```

  short cirtuit if the FG is disabled

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:54:11Z

- **pkg/cache/queue/pending_wait_metrics_test.go:31** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:54:11Z
```diff
@@ -0,0 +1,144 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"testing"
+	"time"
+
+	testingclock "k8s.io/utils/clock/testing"
+
+	"sigs.k8s.io/kueue/pkg/metrics"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+	utiltestingapi "sigs.k8s.io/kueue/pkg/util/testing/v1beta2"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+func TestPendingWaitQuantiles_ActiveHeap(t *testing.T) {
```

  Instead of adding many tests, could you consolidate with fewer, but table-driven?
  
  This will save us code on the setup / asserting which repeats across some of the tests

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:56:34Z

- **pkg/cache/queue/pending_wait_metrics.go:180** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:56:34Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
+	log := ctrl.LoggerFrom(ctx).WithName("pending_wait_metrics_worker")
+	ctx = ctrl.LoggerInto(ctx, log)
+	go func() {
+		<-ctx.Done()
+		w.queue.ShutDown()
+	}()
+
+	w.enqueueAllClusterQueues()
```

  Why is this added? Asking cause this should also happen inside the goroutine.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T18:01:22Z

- **site/content/en/docs/reference/metrics.md:48** by [@amy](https://github.com/amy) — 2026-04-30T18:01:22Z
```diff
@@ -45,6 +45,7 @@ Use the following metrics to monitor the status of your ClusterQueues:
 | `kueue_evicted_workloads_total` | Counter | The number of evicted workloads per 'cluster_queue',<br>The label 'reason' can have the following values:<br>- "Preempted" means that the workload was evicted in order to free resources for a workload with a higher priority or reclamation of nominal quota.<br>- "PodsReadyTimeout" means that the eviction took place due to a PodsReady timeout.<br>- "AdmissionCheck" means that the workload was evicted because at least one admission check transitioned to False.<br>- "ClusterQueueStopped" means that the workload was evicted because the ClusterQueue is stopped.<br>- "LocalQueueStopped" means that the workload was evicted because the LocalQueue is stopped.<br>- "NodeFailures" means that the workload was evicted due to node failures when using TopologyAwareScheduling.<br>- "Deactivated" means that the workload was evicted because spec.active is set to false.<br>The label 'underlying_cause' can have the following values:<br>- "" means that the value in 'reason' label is the root cause for eviction.<br>- "AdmissionCheck" means that the workload was evicted by Kueue due to a rejected admission check.<br>- "MaximumExecutionTimeExceeded" means that the workload was evicted by Kueue due to maximum execution time exceeded.<br>- "RequeuingLimitExceeded" means that the workload was evicted by Kueue due to requeuing limit exceeded. | `cluster_queue`: the name of the ClusterQueue<br> `reason`: eviction or preemption reason<br> `underlying_cause`: root cause for eviction<br> `priority_class`: the priority class name<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_finished_workloads` | Gauge | The number of finished workloads per 'cluster_queue'. | `cluster_queue`: the name of the ClusterQueue<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
 | `kueue_finished_workloads_total` | Counter | The total number of finished workloads per 'cluster_queue' | `cluster_queue`: the name of the ClusterQueue<br> `priority_class`: the priority class name<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
+| `kueue_pending_workload_wait_time_seconds` | Gauge | Queued wait time quantiles in seconds among pending workloads in this ClusterQueue bucket,<br>per 'cluster_queue', 'status', and 'quantile'. Queued wait time is from workload creation or last requeue until now.<br>'status' matches kueue_pending_workloads: "active" for workloads in the admission heap (plus inflight scheduling),<br>"inadmissible" for workloads waiting for cluster conditions after a failed admission attempt.<br>When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket.<br>No time series is reported for a bucket with zero pending workloads.<br>Requires feature gate PendingWorkloadsMetrics (alpha in 0.18). | `cluster_queue`: the name of the ClusterQueue<br> `status`: one of `active` or `inadmissible` (same as kueue_pending_workloads)<br> `quantile`: one of `0.5`, `0.95`, or `0.99`<br> `replica_role`: one of `leader`, `follower`, or `standalone` |
```

  Queued wait time is from workload creation or last requeue until now. -> the "now" word should be changed to be more clear. 
  
  Queued wait time is from workload creation or last requeue until ... something about while the workload is still pending.
  
  When the ClusterQueue is inactive for scheduling, active workloads are folded into the inadmissible bucket. -> 
  Users dont need to know what inadmissible is. What does this mean for the metric.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T18:04:03Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@amy](https://github.com/amy) — 2026-04-30T18:04:04Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  Sorry to make this annoying comment bc I think it would fundamentally change your entire PR... but... 
  
  Do you need pending to update every second? Like I think people use this retroactively. To analyze how long a workload was pending for. So you can just capture the pending time when it changes state to something like admitted.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T18:09:51Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@amy](https://github.com/amy) — 2026-04-30T18:09:51Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  Clarifying with the issue creator here: https://github.com/kubernetes-sigs/kueue/issues/10124#issuecomment-4354985487

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T18:31:09Z

- **pkg/metrics/metrics.go:1296** by [@amy](https://github.com/amy) — 2026-04-30T18:31:09Z
```diff
@@ -1229,6 +1293,7 @@ func Register() {
 		admissionAttemptDuration,
 		AdmissionCyclePreemptionSkips,
 		PendingWorkloads,
+		PendingWorkloadWaitTimeSeconds,
```

  You should copy how localQueue registers if the featuregate is on only. See: https://github.com/epam/kubernetes-kueue/blob/bda4d000ac50942fe845aa3f1710b122b959bc6c/pkg/metrics/metrics.go#L1325

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T18:34:38Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@mimowo](https://github.com/mimowo) — 2026-04-30T18:34:37Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  That is a very good point, I was also thinking about this, because making it retroactively is so much easier to implement. 
  
  At the same time it feels that this specific metric maybe be very helpful for alerting ASAP.
  
  In particular, we had incidents where users were calling "why my job hasn't scheduled for 2 or 3h". At that point seeing the increase in the waiting time gives admins a potential for faster alerting, before a user calls. 
  
  Making the metric retrospective may not show any increase for a long time if there is an outage actually ongoing.
  
  wdyt?

### Review by [@vladikkuzn](https://github.com/vladikkuzn) (commented) — 2026-04-30T18:49:22Z

- **pkg/metrics/metrics.go:1296** by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-04-30T18:49:22Z
```diff
@@ -1229,6 +1293,7 @@ func Register() {
 		admissionAttemptDuration,
 		AdmissionCyclePreemptionSkips,
 		PendingWorkloads,
+		PendingWorkloadWaitTimeSeconds,
```

  My initial design intended to hide reporting behind FG, the metric is registered but not reported, but I guess I can hide it as well

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T19:09:29Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@amy](https://github.com/amy) — 2026-04-30T19:09:29Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  @mimowo Alternatively you could alert on "guarantee violation" using: https://github.com/kubernetes-sigs/kueue/pull/10485
  
  Its a different alert responsibility. But... alerting per workload on pending time is maybe the wrong layer to alert on. Like what if all the workloads that are pending are supposed to be pending. That seems like a noisy alert to proactively page on. When actually you sorta just need point in time debugging on whether or not that workload is supposed to be pending or not.
  
  I'd want to know what rough criteria you're filtering out the noise to make this sorta thing useful vs. just "workloads that exceed 2hrs pending". (not opposed & willing to change my mind. Just slightly skeptical about the practically of hooking this up to a page.)

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T19:13:47Z

- **pkg/metrics/metrics.go:1296** by [@amy](https://github.com/amy) — 2026-04-30T19:13:47Z
```diff
@@ -1229,6 +1293,7 @@ func Register() {
 		admissionAttemptDuration,
 		AdmissionCyclePreemptionSkips,
 		PendingWorkloads,
+		PendingWorkloadWaitTimeSeconds,
```

  Yeah its just nice if it follows existing patterns.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T19:18:07Z

- **pkg/cache/queue/pending_wait_metrics.go:88** by [@amy](https://github.com/amy) — 2026-04-30T19:18:07Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
```

  the integration test only covers the happy-path active bucket. Missing coverage for the inadmissible bucket, inadmissible→active transition, and the inactive CQ folding behavior.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T19:28:24Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@mimowo](https://github.com/mimowo) — 2026-04-30T19:28:24Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  Good point I'm not sure either this should page by itself, probably not. 
  
  However I had a time in the past I was paged exactly like this: my job didn't schedule for 3h.
  
  So my first thought: is the user outlier who is just unhappy or in 30min, or I will soonget a wave of many more pages.
  
  I was back then thinking such a metric would tell me on the spot about the scale of the issue.
  
  Still I'm not the creator of the issue, and it is right thing to ask, but seeing the issue I just correlated that with my past experience and thought this metric might be useful if done live, but this interpretation is not necessarily MVP for the issue. 
  
  Because the waiting time is very much correlated with ux.

### Review by [@amy](https://github.com/amy) (commented) — 2026-04-30T22:30:11Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@amy](https://github.com/amy) — 2026-04-30T22:30:11Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  Waiting for sundar to get back. He said he would think about it over dm: https://github.com/kubernetes-sigs/kueue/issues/10124#issuecomment-4356310720
  
  > So my first thought: is the user outlier who is just unhappy or in 30min, or I will soon get a wave of many more pages. I was back then thinking such a metric would tell me on the spot about the scale of the issue.
  
  yeah... chicken and egg. Also I think that it depends on per ClusterQueue usage patterns. Regardless, I'm not blocking if y'all end up deciding you want to merge.

### Review by [@amy](https://github.com/amy) (commented) — 2026-05-02T15:38:50Z

- **pkg/cache/queue/pending_wait_metrics.go:172** by [@amy](https://github.com/amy) — 2026-05-02T15:38:50Z
```diff
@@ -0,0 +1,233 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package queue
+
+import (
+	"context"
+	"math/rand/v2"
+	"slices"
+	"time"
+
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/clock"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta2"
+	"sigs.k8s.io/kueue/pkg/metrics"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	pendingWaitMetricsBatchPeriod = time.Second
+	pendingWaitMetricsTickPeriod  = 15 * time.Second
+)
+
+// queuedWaitSamples accumulates individual queued-wait durations (in seconds)
+// for a single bucket (active or inadmissible) within a ClusterQueue.
+// After collection, the samples are sorted and quantiles are computed.
+type queuedWaitSamples struct {
+	samples []float64
+}
+
+// add records one workload's queued-wait duration from the given clock.
+func (s *queuedWaitSamples) add(clk clock.Clock, wl *kueue.Workload) {
+	sec := workload.QueuedWaitTime(wl, clk).Seconds()
+	s.samples = append(s.samples, sec)
+}
+
+// quantiles computes p50, p95, p99 from the collected samples using the nearest-rank method.
+// Returns zero values if no samples were collected.
+func (s *queuedWaitSamples) quantiles() metrics.PendingWorkloadWaitQuantiles {
+	n := len(s.samples)
+	if n == 0 {
+		return metrics.PendingWorkloadWaitQuantiles{}
+	}
+	slices.Sort(s.samples)
+	return metrics.PendingWorkloadWaitQuantiles{
+		P50: quantileFromSorted(s.samples, 0.50),
+		P95: quantileFromSorted(s.samples, 0.95),
+		P99: quantileFromSorted(s.samples, 0.99),
+		N:   n,
+	}
+}
+
+// quantileFromSorted returns the q-th quantile from a pre-sorted slice
+// using the nearest-rank (ceiling) method.
+func quantileFromSorted(sorted []float64, q float64) float64 {
+	n := len(sorted)
+	if n == 0 {
+		return 0
+	}
+	// Nearest-rank: rank = ceil(q * n) - 1 (0-indexed).
+	rank := max(int(q*float64(n)+0.5)-1, 0)
+	if rank >= n {
+		rank = n - 1
+	}
+	return sorted[rank]
+}
+
+// pendingWaitQuantiles computes quantile-based queued wait times for active vs inadmissible pending workloads.
+// When cqSchedulingActive is false (ClusterQueue inactive for scheduling), active workloads are folded into the
+// inadmissible bucket — matching ReportPendingWorkloads counts semantics.
+//
+// Caller must NOT hold ClusterQueue locks; this method acquires RLock internally.
+func (c *ClusterQueue) pendingWaitQuantiles(clk clock.Clock, cqSchedulingActive bool) (active, inadm metrics.PendingWorkloadWaitQuantiles) {
+	c.rwm.RLock()
+	defer c.rwm.RUnlock()
+
+	var activeSamples, inadmSamples queuedWaitSamples
+	for _, wi := range c.heap.List() {
+		activeSamples.add(clk, wi.Obj)
+	}
+	if c.inflight != nil {
+		activeSamples.add(clk, c.inflight.Obj)
+	}
+	for _, wi := range c.inadmissibleWorkloads {
+		inadmSamples.add(clk, wi.Obj)
+	}
+
+	if !cqSchedulingActive {
+		// Fold all samples into the inadmissible bucket.
+		merged := queuedWaitSamples{
+			samples: append(activeSamples.samples, inadmSamples.samples...),
+		}
+		return metrics.PendingWorkloadWaitQuantiles{}, merged.quantiles()
+	}
+
+	return activeSamples.quantiles(), inadmSamples.quantiles()
+}
+
+type pendingWaitMetricsOptions struct {
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// PendingWaitMetricsOption configures the pending wait metrics worker.
+type PendingWaitMetricsOption func(*pendingWaitMetricsOptions)
+
+// WithPendingWaitMetricsBatchPeriod sets the delay-queue batching window (default 1s).
+func WithPendingWaitMetricsBatchPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.batchPeriod = d
+	}
+}
+
+// WithPendingWaitMetricsTickPeriod sets how often all ClusterQueues are refreshed for monotonic wait time (default 15s).
+func WithPendingWaitMetricsTickPeriod(d time.Duration) PendingWaitMetricsOption {
+	return func(o *pendingWaitMetricsOptions) {
+		o.tickPeriod = d
+	}
+}
+
+// PendingWaitMetricsWorker batches ClusterQueue notifications and periodically refreshes all queues,
+// mirroring workqueueRequeuer. Heavy aggregation runs only in this worker's goroutine.
+type PendingWaitMetricsWorker struct {
+	manager     *Manager
+	queue       workqueue.TypedDelayingInterface[kueue.ClusterQueueReference]
+	batchPeriod time.Duration
+	tickPeriod  time.Duration
+}
+
+// NewPendingWaitMetricsWorker constructs a Runnable that updates pending wait time gauges.
+func NewPendingWaitMetricsWorker(opts ...PendingWaitMetricsOption) *PendingWaitMetricsWorker {
+	options := pendingWaitMetricsOptions{
+		batchPeriod: pendingWaitMetricsBatchPeriod,
+		tickPeriod:  pendingWaitMetricsTickPeriod,
+	}
+	for _, opt := range opts {
+		opt(&options)
+	}
+	return &PendingWaitMetricsWorker{
+		queue:       workqueue.NewTypedDelayingQueue[kueue.ClusterQueueReference](),
+		batchPeriod: options.batchPeriod,
+		tickPeriod:  options.tickPeriod,
+	}
+}
+
+func (w *PendingWaitMetricsWorker) setManager(manager *Manager) {
+	w.manager = manager
+}
+
+// notify schedules a single ClusterQueue for metrics re-evaluation after the batch period.
+// Used on event-driven paths (workload add/remove); no jitter is applied here.
+func (w *PendingWaitMetricsWorker) notify(cqName kueue.ClusterQueueReference) {
+	w.queue.AddAfter(cqName, w.batchPeriod)
+}
+
+// Start implements manager.Runnable.
+func (w *PendingWaitMetricsWorker) Start(ctx context.Context) error {
```

  Update: spoke with sundar offline. He said that he agrees that age of pending workload is maybe the wrong metric.
  
  I'll leave it to him to comment on the original issue with a clarification.
  
  I think guarantee violation is the direction we should go in. (Total Pending <= remaining nominal) https://github.com/kubernetes-sigs/kueue/pull/10485
  
  For more granularity... we could also emit the specific quota for the head of queue workload. Where in grafana you could do a query on if capacity for head of queue workload is <= remaining nominal.
