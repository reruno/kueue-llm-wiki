# PR #10723: hack/releasing: add upgrade note to patch releases

**Summary**: hack/releasing: add upgrade note to patch releases

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10723

**Last updated**: 2026-05-05T20:56:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ShaanveerS](https://github.com/ShaanveerS)
- **Branch**: `relnotes-10489` → `main`
- **Created**: 2026-04-23T05:18:58Z
- **Updated**: 2026-05-05T20:56:24Z
- **Merged**: 2026-05-05T20:56:22Z
- **Closed**: 2026-05-05T20:56:22Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot)
- **Changed files**: 1   **+99 / -2**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
Add patch upgrade policy to `hack/releasing/sync-notes.sh` for patch releases under "Urgent Upgrade Notes" section.

Before:
```md
## v0.17.1

Changes since `v0.17.0`:

## Urgent Upgrade Notes

### (No, really, you MUST read this before you upgrade)

- AdmissionChecks: Add the alpha `RejectUpdatesToCQWithInvalidOnFlavors` ...

## Changes by Kind
```
  After:
```md
## v0.17.1

Changes since `v0.17.0`:

## Actions Required Before Upgrading

### (No, really, you MUST read this before you upgrade)

- **Minor releases:** Review the `.0` release notes for each new minor version you cross; see: [`v0.16.0`](https://github.com/kubernetes-sigs/kueue/releases/tag/v0.16.0), [`v0.17.0`](https://github.com/kubernetes-sigs/kueue/releases/tag/v0.17.0)
- AdmissionChecks: Add the alpha `RejectUpdatesToCQWithInvalidOnFlavors` ...

## Changes by Kind
```
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10489 (second part)

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-23T05:19:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 785468661fe7e02350efde84cade8bbca89b2c7f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f42d1a5de0ce0008e6e8d1 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-23T05:19:09Z

Hi @ShaanveerS. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-23T05:57:49Z

/ok-to-test

### Comment by [@amy](https://github.com/amy) — 2026-04-23T17:00:32Z

To clarify. Lets say I'm upgrading from 0.17.0 to 0.17.3, I should look at the release notes for ..0, ..1, ..2, and ..3 right? (actually asking. I think... that should be the case but should confirm.) 
If this generates that, then LGTM!

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-23T17:03:47Z

> To clarify. Lets say I'm upgrading from 0.17.0 to 0.17.3, I should look at the release notes for ..0, ..1, ..2, and ..3 right? (actually asking. I think... that should be the case but should confirm.)

Yeah, this is a tricky question. To know all differences, yes, but from the upgrade PoV upgrading 0.17.0 -> 0.17.3 should not require release note reading assuming we don't break compatibilities, which we very occasionally do, so for safety we may include the interim releases too. 

This is currently not included in the message.

wdyt?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-23T17:05:04Z

cc @mwysokin

### Comment by [@amy](https://github.com/amy) — 2026-04-23T18:07:56Z

> Yeah, this is a tricky question. To know all differences, yes, but from the upgrade PoV upgrading 0.17.0 -> 0.17.3 should not require release note reading assuming we don't break compatibilities, which we very occasionally do, so for safety we may include the interim releases too.

Ah okay. If thats the case then current message LGTM!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-24T14:00:41Z

> > To clarify. Lets say I'm upgrading from 0.17.0 to 0.17.3, I should look at the release notes for ..0, ..1, ..2, and ..3 right? (actually asking. I think... that should be the case but should confirm.)
> 
> Yeah, this is a tricky question. To know all differences, yes, but from the upgrade PoV upgrading 0.17.0 -> 0.17.3 should not require release note reading assuming we don't break compatibilities, which we very occasionally do, so for safety we may include the interim releases too.
> 
> This is currently not included in the message.
> 
> wdyt?

I absolutely agree that we should recommend users should check all patch release notes. We sometimes refine some specifications. Especially, we often aggressively break alpha feature functionalities even in patch version.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-24T15:40:00Z

> I absolutely agree that we should recommend users should check all patch release notes. We sometimes refine some specifications. Especially, we often aggressively break alpha feature functionalities even in patch version.

Thank you, in that case I will propose small tweaks to this PR and the other PRs to capture that explicitly👍

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-24T16:23:12Z

Ok, after the next round of thinking I would propose:

## v0.17.0

Changes since `v0.16.0`:

## Urgent Upgrade Notes

### (No, really, you MUST read this before you upgrade)

- When upgrading from an earlier minor release, review the upgrade notes for each
  minor `.0` release after your current version, up to and including this minor
  release; see: 0.15.0, 0.16.0
- ...

-------

## Changes by Kind

## v0.17.1

Changes since `v0.17.1`:

## Urgent Upgrade Notes

### (No, really, you MUST read this before you upgrade)

- When upgrading from an earlier minor release, review the upgrade notes for each
  minor `.0` release after your current version, up to and including this minor
  release; see: 0.15.0, 0.16.0, 0.17.0
- ...

## Changes by Kind

-------

## v0.17.2

Changes since `v0.17.2`:

## Urgent Upgrade Notes

### (No, really, you MUST read this before you upgrade)

- When upgrading from an earlier minor release, review the upgrade notes for each
  minor `.0` release after your current version, up to and including this minor
  release; see: 0.16.0, 0.17.0
- When upgrading to this patch release, also review the upgrade notes for earlier
  patch releases in this minor line; see: 0.17.1.
- ...

## Changes by Kind
-----

Here the versions after "see" would be links actually.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T16:26:51Z

@ShaanveerS how do you test the code? Are you able to maybe test it on your for e2e, when the "v0.18.0" release issue is already open?

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-04-28T04:02:29Z

Tested in https://github.com/ShaanveerS/kueue/issues/1 (0.18.0) and https://github.com/ShaanveerS/kueue/issues/2 (0.17.2)

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-28T09:38:13Z

Thank you, one more remark, might be a follow up, I would like to rename the title "Urgent Upgrade Notes" to  "Upgrade Notes" or "Important Upgrade Notes".

 I'm not clear how they are "Urgent", that suggests to me "upgrade as soon as possible because there is a critical bug (say security)" . 
 
 However, the way we are using this section is more like "When you decide to upgrade please be aware of this potential extra steps or complications", but we don't say "you need to upgrade today".
 
 wdyt @tenzen-y @amy @mwysokin ?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T11:58:30Z

Or more generally I think we could split this section "Urgent Upgrade Steps" into:
1. "New Upgrade Steps"  - here goes what was so far under "Urgent Upgrade Steps"
2. "Upgrade Prerequisites" - here go the new points about checking the previous versions

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-04-30T12:06:50Z

> Thank you, one more remark, might be a follow up, I would like to rename the title "Urgent Upgrade Notes" to "Upgrade Notes" or "Important Upgrade Notes".
> 
> I'm not clear how they are "Urgent", that suggests to me "upgrade as soon as possible because there is a critical bug (say security)" .
> 
> However, the way we are using this section is more like "When you decide to upgrade please be aware of this potential extra steps or complications", but we don't say "you need to upgrade today".
> 
> wdyt @tenzen-y @amy @mwysokin ?

I think that "Urgent" is not weird. That's the same pattern as Kubernetes: https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.36.md#urgent-upgrade-notes

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T12:10:48Z

> > Thank you, one more remark, might be a follow up, I would like to rename the title "Urgent Upgrade Notes" to "Upgrade Notes" or "Important Upgrade Notes".
> > I'm not clear how they are "Urgent", that suggests to me "upgrade as soon as possible because there is a critical bug (say security)" .
> > However, the way we are using this section is more like "When you decide to upgrade please be aware of this potential extra steps or complications", but we don't say "you need to upgrade today".
> > wdyt @tenzen-y @amy @mwysokin ?
> 
> I think that "Urgent" is not weird. That's the same pattern as Kubernetes: https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.36.md#urgent-upgrade-notes

I know it is the same wording in k/k because the same script is used underneath. To my reading:
1. "urgent steps" suggests to me: "do these steps "ASAP". 
2. "Important steps" means: "don't forget about this step". 
And we use it for (2.), conceptually.

However, if you prefer to keep "urgent" for consistency with k/k then I'm totally ok with it.

EDIT: maybe I could also open an issue for the repo hosting the script so that we can discuss the reasons for "urgent" vs "important" there and hear from maintainers, and for now stick to what we have. So that the decisions are de-coupled.

### Comment by [@mwysokin](https://github.com/mwysokin) — 2026-04-30T13:26:55Z

> > > Thank you, one more remark, might be a follow up, I would like to rename the title "Urgent Upgrade Notes" to "Upgrade Notes" or "Important Upgrade Notes".
> > > I'm not clear how they are "Urgent", that suggests to me "upgrade as soon as possible because there is a critical bug (say security)" .
> > > However, the way we are using this section is more like "When you decide to upgrade please be aware of this potential extra steps or complications", but we don't say "you need to upgrade today".
> > > wdyt @tenzen-y @amy @mwysokin ?
> > 
> > 
> > I think that "Urgent" is not weird. That's the same pattern as Kubernetes: https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.36.md#urgent-upgrade-notes
> 
> I know it is the same wording in k/k because the same script is used underneath. To my reading:
> 
> 1. "urgent steps" suggests to me: "do these steps "ASAP".
> 2. "Important steps" means: "don't forget about this step".
>    And we use it for (2.), conceptually.
> 
> However, if you prefer to keep "urgent" for consistency with k/k then I'm totally ok with it.
> 
> EDIT: maybe I could also open an issue for the repo hosting the script so that we can discuss the reasons for "urgent" vs "important" there and hear from maintainers, and for now stick to what we have. So that the decisions are de-coupled.

+1 I don't think urgent fits here. Urgent gives some sense of urgency here we should give some sense of importance. I'd propose either:
- **Action Required Before Upgrading**: I think it is the most common phras I've seen on Github
- **Breaking Changes and Upgrade Instructions**: Plain, simple and direct

@mimowo @ShaanveerS WDYT?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T15:59:46Z

I like both proposals, but slightly leaning to "Actions Required Before Upgrading" - this calls for action and doesn't have the negative connotation of "Breaking Changes", even if the actions are due to breaking changes :)  

It also feels better to describe the section in its current form.

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-01T04:33:57Z

Done, switched to `Actions Required Before Upgrading`. Thanks both!
[v.0.18.0 test](https://github.com/ShaanveerS/kueue/issues/1)

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T12:07:44Z

LGTM, thank you 👍

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T12:10:13Z

> I think that "Urgent" is not weird. That's the same pattern as Kubernetes: https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.36.md#urgent-upgrade-notes

@tenzen-y are you ok with the latest proposal, and "Action Required Before Upgrading" for the section name?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-04T13:05:46Z

@ShaanveerS could you update the PR description with the latest version of the release-notes?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-05T16:59:52Z

> > I think that "Urgent" is not weird. That's the same pattern as Kubernetes: https://github.com/kubernetes/kubernetes/blob/master/CHANGELOG/CHANGELOG-1.36.md#urgent-upgrade-notes
> 
> @tenzen-y are you ok with the latest proposal, and "Action Required Before Upgrading" for the section name?

I'm fine with that 👍 Thank you for discussing some representations!

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-05T20:29:10Z

Thank you, let's see how this works in practice
/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T20:29:25Z

LGTM label has been added.  <details>Git tree hash: 537bfd10a5704c98a7e6561557ff28d17c57e049</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T20:29:27Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10723#issuecomment-4382783839" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10723#" title="Author self-approved">ShaanveerS</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T06:00:42Z

- **hack/releasing/sync-notes.sh:108** by [@mimowo](https://github.com/mimowo) — 2026-04-23T06:00:42Z
```diff
@@ -101,6 +101,40 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function add_patch_upgrade_note() {
+  local input_file="$1"
+  local output_file="$2"
+
+  awk -v current_release="${3}" \
```

  This awk is a bit tricky to follow, could you explain how it works? 
  
  Also, provide some examples for the release notes adjusted by the script for 0.17.1 and 0.17.0. I think for 0.17.0 we don't really need to inject the text.

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-04-23T06:36:11Z

- **hack/releasing/sync-notes.sh:108** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-04-23T06:36:11Z
```diff
@@ -101,6 +101,40 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function add_patch_upgrade_note() {
+  local input_file="$1"
+  local output_file="$2"
+
+  awk -v current_release="${3}" \
```

  added a comment and updated the description with an example. Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T06:52:26Z

LGTM, thank you 👍 

but before we merge let me folks who might be interested in the change, maybe there are some good ideas how to improve it even more.

cc @mwysokin @tenzen-y @gabesaba @kannon92 @amy

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-23T06:52:39Z

- **hack/releasing/sync-notes.sh:108** by [@mimowo](https://github.com/mimowo) — 2026-04-23T06:52:39Z
```diff
@@ -101,6 +101,40 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function add_patch_upgrade_note() {
+  local input_file="$1"
+  local output_file="$2"
+
+  awk -v current_release="${3}" \
```

  lgtm, thanks 👍

### Review by [@mwysokin](https://github.com/mwysokin) (commented) — 2026-04-30T13:19:07Z

Submitted 4 comments: 2 are regarding bugs which I think need need to be fixed and 2 are suggestions to make the content more concise.

- **hack/releasing/sync-notes.sh:153** by [@mwysokin](https://github.com/mwysokin) — 2026-04-30T12:58:55Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  local end_minor=$((patch == 0 ? minor - 1 : minor))
+  local start_minor=$((end_minor > 0 ? end_minor - 1 : 0))
+
+  release_note_links "v${major}." "${start_minor}" "${end_minor}" ".0"
+}
+
+# Returns links to earlier patch release notes in the same minor line.
+function patch_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  release_note_links "v${major}.${minor}." 1 "$((patch - 1))" ""
+}
+
+function add_upgrade_notes() {
+  local input_file="$1"
+  local output_file="$2"
+
+  # Add the upgrade notes under the existing "Urgent Upgrade Notes" section,
+  # or create that section immediately before "## Changes by Kind" if it is missing.
+  awk -v minor_release_links="$3" \
+      -v patch_release_links="$4" '
+    function print_upgrade_notes() {
+      if (minor_release_links != "") {
+        printf "- When upgrading from an earlier minor release, review the upgrade notes for each minor `.0` release after your current version, up to and including this minor release; see: %s.\n", minor_release_links
```

  I'd try to make it simpler and more direct. Something like in the suggestion but don't feel to required to apply the suggestion. Just an idea.
  
  ```suggestion
          printf "- **Minor releases:** Review the `.0` release notes for each new minor version you cross; see: %s.\n", minor_release_links
  ```

- **hack/releasing/sync-notes.sh:156** by [@mwysokin](https://github.com/mwysokin) — 2026-04-30T12:59:58Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  local end_minor=$((patch == 0 ? minor - 1 : minor))
+  local start_minor=$((end_minor > 0 ? end_minor - 1 : 0))
+
+  release_note_links "v${major}." "${start_minor}" "${end_minor}" ".0"
+}
+
+# Returns links to earlier patch release notes in the same minor line.
+function patch_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  release_note_links "v${major}.${minor}." 1 "$((patch - 1))" ""
+}
+
+function add_upgrade_notes() {
+  local input_file="$1"
+  local output_file="$2"
+
+  # Add the upgrade notes under the existing "Urgent Upgrade Notes" section,
+  # or create that section immediately before "## Changes by Kind" if it is missing.
+  awk -v minor_release_links="$3" \
+      -v patch_release_links="$4" '
+    function print_upgrade_notes() {
+      if (minor_release_links != "") {
+        printf "- When upgrading from an earlier minor release, review the upgrade notes for each minor `.0` release after your current version, up to and including this minor release; see: %s.\n", minor_release_links
+      }
+      if (patch_release_links != "") {
+        printf "- When upgrading to this patch release, also review the upgrade notes for earlier patch releases in this minor line; see: %s.\n", patch_release_links
```

  Same as above. Just a suggestion to make it simpler and consistent with the language used in the documentation.
  
  ```suggestion
          printf "- **Patch releases:** Review the patch release notes leading up to this version, but *only* within this minor release line; see: %s.\n", patch_release_links
  ```

- **hack/releasing/sync-notes.sh:126** by [@mwysokin](https://github.com/mwysokin) — 2026-04-30T13:12:00Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
```

  Great logic here, but there is a bug that will crash the script during pre-releases.
  
  Justification: If we ever generate release notes for a tag with a suffix (e.g., `v0.8.2-rc.1`), the current `IFS` parsing will assign `2-rc.1` to the `patch` variable. When bash tries to evaluate `$((patch == 0))`, it will throw a syntax error (`invalid arithmetic operator`). We need to strip out any pre-release suffix before performing math operations on the patch number.
  
  ```suggestion
    local major minor patch raw_patch
    IFS='.' read -r major minor raw_patch <<< "${release_version#v}"
    patch="${raw_patch%%-*}"
  ```

- **hack/releasing/sync-notes.sh:138** by [@mwysokin](https://github.com/mwysokin) — 2026-04-30T13:15:17Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  local end_minor=$((patch == 0 ? minor - 1 : minor))
+  local start_minor=$((end_minor > 0 ? end_minor - 1 : 0))
+
+  release_note_links "v${major}." "${start_minor}" "${end_minor}" ".0"
+}
+
+# Returns links to earlier patch release notes in the same minor line.
+function patch_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
```

  Same minor bug as above.
  
  Justification: We need to apply the exact same suffix-stripping logic here to ensure that `patch` is strictly a number. Otherwise, generating notes for alphas, betas, or RCs will result in a bash syntax crash on line 140 when evaluating `$((patch - 1))`.
  
  ```suggestion
    local major minor patch raw_patch
    IFS='.' read -r major minor raw_patch <<< "${release_version#v}"
    patch="${raw_patch%%-*}"
  ```

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-01T04:11:32Z

- **hack/releasing/sync-notes.sh:126** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-01T04:11:32Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
```

  Thanks for catching this. I think this bug is not reachable today since pre-releases are rejected before we parse the version:
  https://github.com/kubernetes-sigs/kueue/blob/ba7c512d557dd2571025dad68983d606f180934b/hack/releasing/sync-notes.sh#L52-L54

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-01T04:34:07Z

- **hack/releasing/sync-notes.sh:153** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-01T04:34:07Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  local end_minor=$((patch == 0 ? minor - 1 : minor))
+  local start_minor=$((end_minor > 0 ? end_minor - 1 : 0))
+
+  release_note_links "v${major}." "${start_minor}" "${end_minor}" ".0"
+}
+
+# Returns links to earlier patch release notes in the same minor line.
+function patch_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  release_note_links "v${major}.${minor}." 1 "$((patch - 1))" ""
+}
+
+function add_upgrade_notes() {
+  local input_file="$1"
+  local output_file="$2"
+
+  # Add the upgrade notes under the existing "Urgent Upgrade Notes" section,
+  # or create that section immediately before "## Changes by Kind" if it is missing.
+  awk -v minor_release_links="$3" \
+      -v patch_release_links="$4" '
+    function print_upgrade_notes() {
+      if (minor_release_links != "") {
+        printf "- When upgrading from an earlier minor release, review the upgrade notes for each minor `.0` release after your current version, up to and including this minor release; see: %s.\n", minor_release_links
```

  done!

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-01T04:34:15Z

- **hack/releasing/sync-notes.sh:156** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-01T04:34:15Z
```diff
@@ -101,6 +101,84 @@ function find_head_branch() {
   echo "$head_branch"
 }
 
+function release_note_links() {
+  local release_prefix="$1"
+  local start="$2"
+  local end="$3"
+  local release_suffix="$4"
+  local joined=""
+  local current release_version
+
+  for ((current = start; current <= end; current++)); do
+    release_version="${release_prefix}${current}${release_suffix}"
+    if [[ -n "${joined}" ]]; then
+      joined+=", "
+    fi
+    joined+="[\`${release_version}\`](https://github.com/${MAIN_REPO_ORG}/${MAIN_REPO_NAME}/releases/tag/${release_version})"
+  done
+  printf "%s" "${joined}"
+}
+
+# Returns links to the two latest minor `.0` release notes.
+function minor_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  local end_minor=$((patch == 0 ? minor - 1 : minor))
+  local start_minor=$((end_minor > 0 ? end_minor - 1 : 0))
+
+  release_note_links "v${major}." "${start_minor}" "${end_minor}" ".0"
+}
+
+# Returns links to earlier patch release notes in the same minor line.
+function patch_upgrade_release_links() {
+  local release_version="$1"
+  local major minor patch
+  IFS='.' read -r major minor patch <<< "${release_version#v}"
+
+  release_note_links "v${major}.${minor}." 1 "$((patch - 1))" ""
+}
+
+function add_upgrade_notes() {
+  local input_file="$1"
+  local output_file="$2"
+
+  # Add the upgrade notes under the existing "Urgent Upgrade Notes" section,
+  # or create that section immediately before "## Changes by Kind" if it is missing.
+  awk -v minor_release_links="$3" \
+      -v patch_release_links="$4" '
+    function print_upgrade_notes() {
+      if (minor_release_links != "") {
+        printf "- When upgrading from an earlier minor release, review the upgrade notes for each minor `.0` release after your current version, up to and including this minor release; see: %s.\n", minor_release_links
+      }
+      if (patch_release_links != "") {
+        printf "- When upgrading to this patch release, also review the upgrade notes for earlier patch releases in this minor line; see: %s.\n", patch_release_links
```

  done!
