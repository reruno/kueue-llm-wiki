# PR #1075: Change Admission semantics to better cover two stage admission.

**Summary**: Change Admission semantics to better cover two stage admission.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1075

**Last updated**: 2023-10-25T17:35:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `rename-admission` → `main`
- **Created**: 2023-08-21T11:55:53Z
- **Updated**: 2023-10-25T17:35:57Z
- **Merged**: 2023-08-25T18:02:54Z
- **Closed**: 2023-08-25T18:02:54Z
- **Labels**: `lgtm`, `tide/merge-method-squash`, `release-note-none`, `size/XL`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 35   **+513 / -384**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Change Admission semantics to fully cover two stage admission.  
- The old `Admission` condition is renamed to `QuotaReservation`
- The new `Admission` condition covers both `QuotaResrvation` and `AdmissionChecks`

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #993

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-21T11:55:55Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-21T11:56:01Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1b2d7d331c3c8185ca748726500c5c92bc841f3b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/64e8db6dd39f43000883e197 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T19:59:16Z

A bit hard to see which commits are not part of #1045.

Could you squash all the commits that don't belong to #1045 in one commit, matching the commit message with the PR title?

After we review, the follow up changes can go in new commits.

### Comment by [@trasc](https://github.com/trasc) — 2023-08-23T09:08:43Z

> A bit hard to see which commits are not part of #1045.
> 
> Could you squash all the commits that don't belong to #1045 in one commit, matching the commit message with the PR title?
> 
> After we review, the follow up changes can go in new commits.

done

### Comment by [@trasc](https://github.com/trasc) — 2023-08-25T12:01:24Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2023-08-25T15:40:05Z

/label tide/merge-method-squash

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-25T15:49:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1075#pullrequestreview-1595999324" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1075#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-25T18:01:27Z

/lgtm

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:35:35Z

/kind cleanup
/release-note-edit
```
NONE
````

Just because this is just a continuation to #1045

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-25T17:35:37Z

@alculquicondor: /release-note-edit must be used with a release note block.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1075#issuecomment-1779748324):

>/kind cleanup
>/release-note-edit
>```
>NONE
>````
>
>Just because this is just a continuation to #1045


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:35:53Z

/release-note-edit
```release-note
NONE
```

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-21T13:08:11Z

- **PROJECT:56** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-21T13:08:11Z
```diff
@@ -53,4 +53,12 @@ resources:
   kind: KueueConfiguration
   path: sigs.k8s.io/kueue/apis/config/v1alpha1
   version: v1alpha1
+- api:
```

  Based on the title, it sounds like this and the rest of API files shouldn't be in this PR?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-21T13:11:27Z

- **PROJECT:56** by [@trasc](https://github.com/trasc) — 2023-08-21T13:11:27Z
```diff
@@ -53,4 +53,12 @@ resources:
   kind: KueueConfiguration
   path: sigs.k8s.io/kueue/apis/config/v1alpha1
   version: v1alpha1
+- api:
```

  That is in fact part of #1045, this pr is still draft

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-22T20:12:27Z

I'll stop my review here, since there will a significant reduction of changes with the comments below.

- **apis/kueue/v1beta1/workload_types.go:137** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T20:04:43Z
```diff
@@ -128,10 +128,13 @@ type PodSet struct {
 
 // WorkloadStatus defines the observed state of Workload
 type WorkloadStatus struct {
-	// admission holds the parameters of the admission of the workload by a
-	// ClusterQueue. admission can be set back to null, but its fields cannot be
+	// quotaReservation holds the parameters of the admission of the workload by a
+	// ClusterQueue. quotaReservation can be set back to null, but its fields cannot be
 	// changed once set.
-	Admission *Admission `json:"admission,omitempty"`
+	QuotaReservation *QuotaReservation `json:"quotaReservation,omitempty"`
+
+	// the same as quotaReservation, used for backwards compatibility
+	Admission *QuotaReservation `json:"admission,omitempty"`
```

  If find this rename (the API field change) unnecessary.
  
  Admission is still a valid name: it contains the details about admission. It doesn't mean that the workload is admitted. That's what the condition is for.

- **apis/kueue/v1beta1/workload_types.go:178** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T20:06:53Z
```diff
@@ -172,9 +175,12 @@ type ReclaimablePod struct {
 }
 
 const (
-	// WorkloadAdmitted means that the Workload was admitted by a ClusterQueue.
+	// WorkloadAdmitted means that the Workload is ready to run.
```

  ```suggestion
  	// WorkloadAdmitted means that the Workload has reserved quota and all the admissionChecks defined in the ClusterQueue are satisfied.
  ```

- **pkg/workload/workload.go:370** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-22T20:11:48Z
```diff
@@ -362,9 +362,9 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 	return &w.CreationTimestamp
 }
 
-// IsAdmitted checks if workload is admitted based on conditions
-func IsAdmitted(w *kueue.Workload) bool {
-	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
+// HasQuotaReservation checks if workload is admitted based on conditions
+func HasQuotaReservation(w *kueue.Workload) bool {
+	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadQuotaReserved)
```

  just to be clear that my comment above is not misinterpreted:
  This rename is fine.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-23T09:12:06Z

- **pkg/workload/workload.go:370** by [@trasc](https://github.com/trasc) — 2023-08-23T09:09:46Z
```diff
@@ -362,9 +362,9 @@ func GetQueueOrderTimestamp(w *kueue.Workload) *metav1.Time {
 	return &w.CreationTimestamp
 }
 
-// IsAdmitted checks if workload is admitted based on conditions
-func IsAdmitted(w *kueue.Workload) bool {
-	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadAdmitted)
+// HasQuotaReservation checks if workload is admitted based on conditions
+func HasQuotaReservation(w *kueue.Workload) bool {
+	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadQuotaReserved)
```

  ok

- **apis/kueue/v1beta1/workload_types.go:137** by [@trasc](https://github.com/trasc) — 2023-08-23T09:11:35Z
```diff
@@ -128,10 +128,13 @@ type PodSet struct {
 
 // WorkloadStatus defines the observed state of Workload
 type WorkloadStatus struct {
-	// admission holds the parameters of the admission of the workload by a
-	// ClusterQueue. admission can be set back to null, but its fields cannot be
+	// quotaReservation holds the parameters of the admission of the workload by a
+	// ClusterQueue. quotaReservation can be set back to null, but its fields cannot be
 	// changed once set.
-	Admission *Admission `json:"admission,omitempty"`
+	QuotaReservation *QuotaReservation `json:"quotaReservation,omitempty"`
+
+	// the same as quotaReservation, used for backwards compatibility
+	Admission *QuotaReservation `json:"admission,omitempty"`
```

  I can't say I agree ... however, done

- **apis/kueue/v1beta1/workload_types.go:178** by [@trasc](https://github.com/trasc) — 2023-08-23T09:11:46Z
```diff
@@ -172,9 +175,12 @@ type ReclaimablePod struct {
 }
 
 const (
-	// WorkloadAdmitted means that the Workload was admitted by a ClusterQueue.
+	// WorkloadAdmitted means that the Workload is ready to run.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-23T15:51:57Z

- **apis/kueue/v1beta1/workload_types.go:180** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-23T15:51:56Z
```diff
@@ -166,9 +172,13 @@ type ReclaimablePod struct {
 }
 
 const (
-	// WorkloadAdmitted means that the Workload was admitted by a ClusterQueue.
+	// WorkloadAdmitted means that the Workload has reserved quota and all the admissionChecks
+	// defined in the ClusterQueue are satisfied.
 	WorkloadAdmitted = "Admitted"
 
+	// WorkloadQuotaReserved means that the Workload has reserved quota a ClusterQueue.
+	WorkloadQuotaReserved = "QuotaReserved"
```

  Let's rename this to AdmissionPrecheck, as the KEP suggests.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-23T17:01:15Z

- **apis/kueue/v1beta1/workload_types.go:180** by [@trasc](https://github.com/trasc) — 2023-08-23T17:01:15Z
```diff
@@ -166,9 +172,13 @@ type ReclaimablePod struct {
 }
 
 const (
-	// WorkloadAdmitted means that the Workload was admitted by a ClusterQueue.
+	// WorkloadAdmitted means that the Workload has reserved quota and all the admissionChecks
+	// defined in the ClusterQueue are satisfied.
 	WorkloadAdmitted = "Admitted"
 
+	// WorkloadQuotaReserved means that the Workload has reserved quota a ClusterQueue.
+	WorkloadQuotaReserved = "QuotaReserved"
```

  There are two reasons why I don't think it's a good idea:
  
  1. This can imply that is done before the checks.
  2. I think, the Precheck, as described in the KEP is not blocking the quota

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-23T17:09:19Z

- **apis/kueue/v1beta1/workload_types.go:180** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-23T17:09:19Z
```diff
@@ -166,9 +172,13 @@ type ReclaimablePod struct {
 }
 
 const (
-	// WorkloadAdmitted means that the Workload was admitted by a ClusterQueue.
+	// WorkloadAdmitted means that the Workload has reserved quota and all the admissionChecks
+	// defined in the ClusterQueue are satisfied.
 	WorkloadAdmitted = "Admitted"
 
+	// WorkloadQuotaReserved means that the Workload has reserved quota a ClusterQueue.
+	WorkloadQuotaReserved = "QuotaReserved"
```

  Let's discuss in the KEP.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-24T19:22:54Z

- **pkg/controller/core/workload_controller.go:162** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:07:28Z
```diff
@@ -149,7 +149,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
-	if workload.IsAdmitted(&wl) {
+	hasReservation := workload.HasQuotaReservation(&wl)
+	hasAllChecksReady := workload.HasAllChecksReady(&wl)
+	isAdmitted := workload.IsAdmitted(&wl)
+	if isAdmitted != (hasReservation && hasAllChecksReady) {
+		newStatus := metav1.ConditionTrue
+		newReason := "Admitted"
+		if isAdmitted {
+			newStatus = metav1.ConditionFalse
+			switch {
+			case !hasReservation && !hasAllChecksReady:
+				newReason = "NoReservatioNoChecks"
```

  ```suggestion
  				newReason = "NoReservationNoChecks"
  ```

- **pkg/controller/core/workload_controller.go:164** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:07:42Z
```diff
@@ -149,7 +149,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
-	if workload.IsAdmitted(&wl) {
+	hasReservation := workload.HasQuotaReservation(&wl)
+	hasAllChecksReady := workload.HasAllChecksReady(&wl)
+	isAdmitted := workload.IsAdmitted(&wl)
+	if isAdmitted != (hasReservation && hasAllChecksReady) {
+		newStatus := metav1.ConditionTrue
+		newReason := "Admitted"
+		if isAdmitted {
+			newStatus = metav1.ConditionFalse
+			switch {
+			case !hasReservation && !hasAllChecksReady:
+				newReason = "NoReservatioNoChecks"
+			case !hasReservation:
+				newReason = "NoReservatio"
```

  ```suggestion
  				newReason = "NoReservation"
  ```

- **pkg/controller/core/workload_controller.go:258** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:09:11Z
```diff
@@ -234,7 +255,7 @@ func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
-	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
+	countingTowardsTimeout, recheckAfter := r.quotaReservedNotReadyWorkload(wl, realClock)
```

  NotReadyTimeout should actually count since Admitted=True (the new Admitted condition)

- **pkg/controller/jobframework/reconciler.go:269** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:12:52Z
```diff
@@ -266,10 +266,10 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if err := r.stopJob(ctx, job, object, wl, evCond.Message); err != nil {
 			return ctrl.Result{}, err
 		}
-		if workload.IsAdmitted(wl) {
+		if workload.HasQuotaReservation(wl) {
```

  shouldn't we unset both conditions in this case? Or is your intention that updating this one will trigger updating the Admitted condition in the next sync?
  
  I would prefer updating both in one API call, just to be efficient in the number of API calls.

- **pkg/controller/jobframework/reconciler.go:602** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:15:13Z
```diff
@@ -599,6 +599,11 @@ type PodSetInfo struct {
 
 // getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
 func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+	// this chan happen when the eviction finished and the workload controller did not yet changed the admitted condition
```

  Another reason to update both conditions together.

- **pkg/controller/jobframework/reconciler.go:671** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:15:46Z
```diff
@@ -663,7 +668,7 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 	// Ready to Completed. As pods finish, they transition first into the
 	// uncountedTerminatedPods staging area, before passing to the
 	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
+	if workload.HasQuotaReservation(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
```

  IsAdmitted seems right here too

- **pkg/scheduler/scheduler.go:430** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:17:24Z
```diff
@@ -427,7 +427,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetAdmission(newWorkload, admission)
+	workload.SetQuotaReservation(newWorkload, admission)
```

  We should probably change the name of the `admit` function too, but let's defer that after we have settled on the name.

- **pkg/util/testing/wrappers.go:122** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:17:54Z
```diff
@@ -114,6 +114,21 @@ func (w *WorkloadWrapper) Admit(a *kueue.Admission) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) Admitted(a bool) *WorkloadWrapper {
+	cond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "AdmittedByTest",
```

  ```suggestion
  		Reason:             "ByTest",
  ```
  
  since it could be false.

- **test/util/util.go:340** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-24T19:19:34Z
```diff
@@ -327,16 +327,28 @@ func ExpectCQResourceUsage(cq *kueue.ClusterQueue, flavor, resource string, v fl
 	}, Timeout, Interval).Should(gomega.Equal(v))
 }
 
-func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
+func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
 	wl = wl.DeepCopy()
 	if admission == nil {
-		workload.UnsetAdmissionWithCondition(wl, "EvictedByTest", "Evicted By Test")
+		workload.UnsetQuotaReservationWithCondition(wl, "EvictedByTest", "Evicted By Test")
 	} else {
-		workload.SetAdmission(wl, admission)
+		workload.SetQuotaReservation(wl, admission)
 	}
 	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false)
 }
 
+func SyncAdmissionFofWorkloads(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
```

  add a comment for what this does and where it should be used (I suppose, in cases where the workload controller isn't running).
  
  Maybe `SyncAdmittedConditionForWorkloads`

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-25T12:01:37Z

- **test/util/util.go:340** by [@trasc](https://github.com/trasc) — 2023-08-25T09:28:11Z
```diff
@@ -327,16 +327,28 @@ func ExpectCQResourceUsage(cq *kueue.ClusterQueue, flavor, resource string, v fl
 	}, Timeout, Interval).Should(gomega.Equal(v))
 }
 
-func SetAdmission(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
+func SetQuotaReservation(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, admission *kueue.Admission) error {
 	wl = wl.DeepCopy()
 	if admission == nil {
-		workload.UnsetAdmissionWithCondition(wl, "EvictedByTest", "Evicted By Test")
+		workload.UnsetQuotaReservationWithCondition(wl, "EvictedByTest", "Evicted By Test")
 	} else {
-		workload.SetAdmission(wl, admission)
+		workload.SetQuotaReservation(wl, admission)
 	}
 	return workload.ApplyAdmissionStatus(ctx, k8sClient, wl, false)
 }
 
+func SyncAdmissionFofWorkloads(ctx context.Context, k8sClient client.Client, wls ...*kueue.Workload) {
```

  done

- **pkg/util/testing/wrappers.go:122** by [@trasc](https://github.com/trasc) — 2023-08-25T09:29:14Z
```diff
@@ -114,6 +114,21 @@ func (w *WorkloadWrapper) Admit(a *kueue.Admission) *WorkloadWrapper {
 	return w
 }
 
+func (w *WorkloadWrapper) Admitted(a bool) *WorkloadWrapper {
+	cond := metav1.Condition{
+		Type:               kueue.WorkloadAdmitted,
+		Status:             metav1.ConditionTrue,
+		LastTransitionTime: metav1.Now(),
+		Reason:             "AdmittedByTest",
```

  done

- **pkg/controller/jobframework/reconciler.go:269** by [@trasc](https://github.com/trasc) — 2023-08-25T10:02:57Z
```diff
@@ -266,10 +266,10 @@ func (r *JobReconciler) ReconcileGenericJob(ctx context.Context, req ctrl.Reques
 		if err := r.stopJob(ctx, job, object, wl, evCond.Message); err != nil {
 			return ctrl.Result{}, err
 		}
-		if workload.IsAdmitted(wl) {
+		if workload.HasQuotaReservation(wl) {
```

  We could, it was done like this with separation of concerns in mind.

- **pkg/controller/jobframework/reconciler.go:671** by [@trasc](https://github.com/trasc) — 2023-08-25T11:08:15Z
```diff
@@ -663,7 +668,7 @@ func generatePodsReadyCondition(job GenericJob, wl *kueue.Workload) metav1.Condi
 	// Ready to Completed. As pods finish, they transition first into the
 	// uncountedTerminatedPods staging area, before passing to the
 	// succeeded/failed counters.
-	if workload.IsAdmitted(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
+	if workload.HasQuotaReservation(wl) && (job.PodsReady() || apimeta.IsStatusConditionTrue(wl.Status.Conditions, kueue.WorkloadPodsReady)) {
```

  done

- **pkg/controller/core/workload_controller.go:162** by [@trasc](https://github.com/trasc) — 2023-08-25T11:09:48Z
```diff
@@ -149,7 +149,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
-	if workload.IsAdmitted(&wl) {
+	hasReservation := workload.HasQuotaReservation(&wl)
+	hasAllChecksReady := workload.HasAllChecksReady(&wl)
+	isAdmitted := workload.IsAdmitted(&wl)
+	if isAdmitted != (hasReservation && hasAllChecksReady) {
+		newStatus := metav1.ConditionTrue
+		newReason := "Admitted"
+		if isAdmitted {
+			newStatus = metav1.ConditionFalse
+			switch {
+			case !hasReservation && !hasAllChecksReady:
+				newReason = "NoReservatioNoChecks"
```

  done

- **pkg/controller/core/workload_controller.go:164** by [@trasc](https://github.com/trasc) — 2023-08-25T11:09:56Z
```diff
@@ -149,7 +149,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 	}
 
-	if workload.IsAdmitted(&wl) {
+	hasReservation := workload.HasQuotaReservation(&wl)
+	hasAllChecksReady := workload.HasAllChecksReady(&wl)
+	isAdmitted := workload.IsAdmitted(&wl)
+	if isAdmitted != (hasReservation && hasAllChecksReady) {
+		newStatus := metav1.ConditionTrue
+		newReason := "Admitted"
+		if isAdmitted {
+			newStatus = metav1.ConditionFalse
+			switch {
+			case !hasReservation && !hasAllChecksReady:
+				newReason = "NoReservatioNoChecks"
+			case !hasReservation:
+				newReason = "NoReservatio"
```

  done

- **pkg/controller/core/workload_controller.go:258** by [@trasc](https://github.com/trasc) — 2023-08-25T11:12:33Z
```diff
@@ -234,7 +255,7 @@ func syncAdmissionCheckConditions(conds []metav1.Condition, queueChecks []string
 
 func (r *WorkloadReconciler) reconcileNotReadyTimeout(ctx context.Context, req ctrl.Request, wl *kueue.Workload) (ctrl.Result, error) {
 	log := ctrl.LoggerFrom(ctx)
-	countingTowardsTimeout, recheckAfter := r.admittedNotReadyWorkload(wl, realClock)
+	countingTowardsTimeout, recheckAfter := r.quotaReservedNotReadyWorkload(wl, realClock)
```

  indeed, done

- **pkg/controller/jobframework/reconciler.go:602** by [@trasc](https://github.com/trasc) — 2023-08-25T11:13:56Z
```diff
@@ -599,6 +599,11 @@ type PodSetInfo struct {
 
 // getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
 func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+	// this chan happen when the eviction finished and the workload controller did not yet changed the admitted condition
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-25T13:59:07Z

- **pkg/workload/workload.go:321** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-25T13:57:44Z
```diff
@@ -315,6 +315,11 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+
+	// sync Admitted
+	if inSync, newCond := IsAdmittedInSync(w); !inSync {
+		apimeta.SetStatusCondition(&w.Status.Conditions, *newCond)
```

  Every time you call `IsAdmittedInSync` you also call `SetStatusCondition`.
  
  I would change the function to be `SyncAdmittedCondition` that does both operations, returning True if something changed.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-25T14:04:18Z

- **pkg/scheduler/scheduler.go:430** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-25T14:04:17Z
```diff
@@ -427,7 +427,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetAdmission(newWorkload, admission)
+	workload.SetQuotaReservation(newWorkload, admission)
```

  Let's set the Admitted condition here if the ClusterQueue doesn't have any checks. I guess it might be enough to call SyncAdmittedCondition.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-08-25T15:33:43Z

- **pkg/workload/workload.go:321** by [@trasc](https://github.com/trasc) — 2023-08-25T15:31:05Z
```diff
@@ -315,6 +315,11 @@ func SetQuotaReservation(w *kueue.Workload, admission *kueue.Admission) {
 		evictedCond.Status = metav1.ConditionFalse
 		evictedCond.LastTransitionTime = metav1.Now()
 	}
+
+	// sync Admitted
+	if inSync, newCond := IsAdmittedInSync(w); !inSync {
+		apimeta.SetStatusCondition(&w.Status.Conditions, *newCond)
```

  done

- **pkg/scheduler/scheduler.go:430** by [@trasc](https://github.com/trasc) — 2023-08-25T15:32:22Z
```diff
@@ -427,7 +427,7 @@ func (s *Scheduler) admit(ctx context.Context, e *entry) error {
 		PodSetAssignments: e.assignment.ToAPI(),
 	}
 
-	workload.SetAdmission(newWorkload, admission)
+	workload.SetQuotaReservation(newWorkload, admission)
```

  it's done in `SetQuotaReservation`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-25T15:49:20Z

- **pkg/workload/workload.go:376** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-25T15:49:19Z
```diff
@@ -372,37 +370,40 @@ func HasQuotaReservation(w *kueue.Workload) bool {
 	return apimeta.IsStatusConditionTrue(w.Status.Conditions, kueue.WorkloadQuotaReserved)
 }
 
-func IsAdmittedInSync(w *kueue.Workload) (bool, *metav1.Condition) {
+// SyncAdmittedCondition sync the state of the Admitted condition
+// with the state of QuoataReserved and AdmissionChecks.
+// Return true if any change was done.
+func SyncAdmittedCondition(w *kueue.Workload) bool {
```

  Let's add a unit test for this function in a follow up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-25T15:49:26Z

/lgtm
/approve
