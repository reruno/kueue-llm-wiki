# PR #10752: Fix TopologyAwareScheduling when Creating a JobSet requesting TAS should admit a JobSet via TAS test.

**Summary**: Fix TopologyAwareScheduling when Creating a JobSet requesting TAS should admit a JobSet via TAS test.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10752

**Last updated**: 2026-04-27T18:28:10Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Branch**: `flake/should-admit-a-JobSet-via-TAS` → `main`
- **Created**: 2026-04-24T07:25:13Z
- **Updated**: 2026-04-27T18:28:10Z
- **Merged**: 2026-04-27T18:26:50Z
- **Closed**: 2026-04-27T18:26:50Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `kind/failing-test`, `kind/flake`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mszadkow](https://github.com/mszadkow), [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 5   **+33 / -75**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
/kind failing-test
/kind flake

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->

#### What this PR does / why we need it:
Fix TopologyAwareScheduling when Creating a JobSet requesting TAS should admit a JobSet via TAS test.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10751

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-24T07:25:20Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | be2a4a0ad3105151a2d9687590a3661bfbb26223 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69ef94eb6decd40008a2e425 |

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-24T08:34:55Z

/cc @mszadkow

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T18:25:48Z

/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16
Thank you

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-27T18:25:54Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10752#issuecomment-4329469810):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Thank you


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-27T18:25:59Z

LGTM label has been added.  <details>Git tree hash: 55f7040e8be528edcdc2d00bb7993700e21ff7fe</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-27T18:26:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10752#" title="Author self-approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10752#issuecomment-4329469810" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mbobrovskyi,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-27T18:27:32Z

@mimowo: new pull request created: #10803

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10752#issuecomment-4329469810):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Thank you


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-27T18:28:09Z

@mimowo: new pull request created: #10804

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10752#issuecomment-4329469810):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Thank you


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-24T08:37:26Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mimowo](https://github.com/mimowo) — 2026-04-24T08:37:23Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  Should we use the same helper also in other places? I would assume so, or this spot is somehow special

### Review by [@mszadkow](https://github.com/mszadkow) (commented) — 2026-04-24T12:17:41Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mszadkow](https://github.com/mszadkow) — 2026-04-24T12:17:41Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  I wonder if checking quota reservation along side finished state brings any value?
  If not, then we might do what @mimowo mentioned and replace those check pairs with the wrapper

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T07:24:51Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mimowo](https://github.com/mimowo) — 2026-04-27T07:24:50Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  > I wonder if checking quota reservation along side finished state brings any value?
  
  It could be useful if this was a separate assert, separete ginkgo.By. So we could keep it as part of the previous ginkgo.By: ginkgo.By(fmt.Sprintf("await for admission of workload %q and verify TopologyAssignment", wlLookupKey), func() {

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T07:26:46Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mimowo](https://github.com/mimowo) — 2026-04-27T07:26:46Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  > If not, then we might do what @mimowo mentioned and replace those check pairs with the wrapper
  
  I don't have a strong view about the QuotaReservation check - eiither a separate ginkgo.By, or merge with previous step. 
  
  Still, I think we should use the extended timeout for all places where we are waiting for workload finished in e2e tests.
  
  We don't need to cover all tests in this PR, but as I checked there are more similar checks, so I'm wondering if we could extend the scope of the PR slightly, at least for the analogous cases.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T15:55:19Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T15:55:19Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  > I wonder if checking quota reservation along side finished state brings any value?
  
  I'm not sure we need to check this here. We already know the workload is admitted in the "waiting for the JobSet to be unsuspended" case.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T16:19:00Z

- **test/e2e/singlecluster/tas_test.go:267** by [@mimowo](https://github.com/mimowo) — 2026-04-27T16:18:59Z
```diff
@@ -270,11 +270,7 @@ var _ = ginkgo.Describe("TopologyAwareScheduling", ginkgo.Label("area:singleclus
 			})
 
 			ginkgo.By(fmt.Sprintf("verify the workload %q gets finished", wlLookupKey), func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, wlLookupKey, createdWorkload)).Should(gomega.Succeed())
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  Exactly, I'm ok to drop this check. I'm just thinking it might be a good idea to also cover other places doing the smilar check in this file

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T16:38:15Z

- **test/util/util.go:573** by [@mimowo](https://github.com/mimowo) — 2026-04-27T16:38:15Z
```diff
@@ -569,6 +570,15 @@ func expectWorkloadsWithPriority(
 	}, Timeout, Interval).Should(gomega.Succeed(), AssertMsg("Workload priority does not match expected", createdWl))
 }
 
+func ExpectWorkloadFinalized(ctx context.Context, k8sClient client.Client, wlKey client.ObjectKey) {
```

  It does not seem used

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T16:39:19Z

- **test/util/util.go:573** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T16:39:19Z
```diff
@@ -569,6 +570,15 @@ func expectWorkloadsWithPriority(
 	}, Timeout, Interval).Should(gomega.Succeed(), AssertMsg("Workload priority does not match expected", createdWl))
 }
 
+func ExpectWorkloadFinalized(ctx context.Context, k8sClient client.Client, wlKey client.ObjectKey) {
```

  Removed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T16:40:41Z

- **test/e2e/singlecluster/job_test.go:406** by [@mimowo](https://github.com/mimowo) — 2026-04-27T16:40:41Z
```diff
@@ -402,18 +400,10 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("Wait for the workload to finish", func() {
-				createdWorkload := &kueue.Workload{}
-				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
-				gomega.Eventually(func(g gomega.Gomega) {
-					err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
-					if apierrors.IsNotFound(err) {
-						return
-					}
-					g.Expect(err).To(gomega.Not(gomega.HaveOccurred()))
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+			ginkgo.By(fmt.Sprintf("wait for the workload %q to finish", wlLookupKey), func() {
+				util.ExpectWorkloadToFinishOrGoneWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  I see why you add the function to reflect the previous code, but do we really need to pass the test when the workload is deleted? I'm not sure what could delete the workload, iiuc the workload should exist after the job is finshed, so we could simplify this case. Maybe we should check which PRs added this code, but this looks unnecessary, wdyt?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T16:48:41Z

- **test/e2e/singlecluster/job_test.go:406** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T16:48:41Z
```diff
@@ -402,18 +400,10 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("Wait for the workload to finish", func() {
-				createdWorkload := &kueue.Workload{}
-				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
-				gomega.Eventually(func(g gomega.Gomega) {
-					err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
-					if apierrors.IsNotFound(err) {
-						return
-					}
-					g.Expect(err).To(gomega.Not(gomega.HaveOccurred()))
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+			ginkgo.By(fmt.Sprintf("wait for the workload %q to finish", wlLookupKey), func() {
+				util.ExpectWorkloadToFinishOrGoneWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  Actually, here we are checking that the finalizers are empty after the Job finishes. I think we should keep this to cover that case. We may not need to check for Gone, but we should still verify that the finalizers are empty.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T16:50:52Z

- **test/e2e/singlecluster/job_test.go:406** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T16:50:52Z
```diff
@@ -402,18 +400,10 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 				}, util.Timeout, util.Interval).Should(gomega.Succeed())
 			})
 
-			ginkgo.By("Wait for the workload to finish", func() {
-				createdWorkload := &kueue.Workload{}
-				wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
-				gomega.Eventually(func(g gomega.Gomega) {
-					err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
-					if apierrors.IsNotFound(err) {
-						return
-					}
-					g.Expect(err).To(gomega.Not(gomega.HaveOccurred()))
-					g.Expect(workload.HasQuotaReservation(createdWorkload)).Should(gomega.BeTrue())
-					g.Expect(createdWorkload.Status.Conditions).Should(utiltesting.HaveConditionStatusTrue(kueue.WorkloadFinished))
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+			wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(job.Name, job.UID), Namespace: ns.Name}
+
+			ginkgo.By(fmt.Sprintf("wait for the workload %q to finish", wlLookupKey), func() {
+				util.ExpectWorkloadToFinishOrGoneWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
```

  Ah, wait. We are talking about another case. Yes, you right here we can simplify.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T16:51:43Z

- **test/e2e/singlecluster/job_test.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T16:51:43Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  I told about this case (https://github.com/kubernetes-sigs/kueue/pull/10752/changes#r3148888419).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T17:13:23Z

- **test/e2e/singlecluster/job_test.go:299** by [@mimowo](https://github.com/mimowo) — 2026-04-27T17:13:22Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  Hmm, this assert is very weird actually, because it is checking that the workload is finished, but if the workload is deleted quickly, then the assert will fail. So IIUC it is passing only because typically the GC collector takes time. IIUC, the workload gets deleted eventually by GC anyway.
  
  If this is true, when I think we should not be really checking if the workload is finsished. I would rather just check `ExpectWorkloadsFinalizedOrGone` - I think checking if finished is flaky,

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T18:08:42Z

- **test/e2e/singlecluster/job_test.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T18:08:42Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  I think we should first check for the finished status, and then verify the finalizers or whether the workload is already gone. This is what I’ve already implemented.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T18:12:06Z

- **test/e2e/singlecluster/job_test.go:299** by [@mimowo](https://github.com/mimowo) — 2026-04-27T18:12:06Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  I know you have implemented it, and i know this is how it was.
  
  However I think this is not correct and may flake. If the workload is GCed immediately after finishing then we may not have time to obsrrve it is finished. 
  
  Sure GC works in 20s interval so probably this is very unlikely but I think possible
  
  Let me know what im missing

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-27T18:25:04Z

- **test/e2e/singlecluster/job_test.go:299** by [@mimowo](https://github.com/mimowo) — 2026-04-27T18:25:04Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  ok in any case we can revisit this as a follow up, this is not a regression, just something that caught my attention

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-04-27T18:26:24Z

- **test/e2e/singlecluster/job_test.go:299** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-04-27T18:26:24Z
```diff
@@ -294,11 +292,11 @@ var _ = ginkgo.Describe("Kueue", ginkgo.Label("area:singlecluster", "feature:job
 			})
 
 			ginkgo.By("Await for jobs completion", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), createdWorkload)).To(gomega.Succeed())
-					g.Expect(createdWorkload.Finalizers).NotTo(gomega.ContainElement(kueue.ResourceInUseFinalizerName))
```

  So, are you propose to check `OR finished OR finalized OR gone` together? Because currently we have:
  
  ```
  			ginkgo.By("Await for jobs completion", func() {
  				util.ExpectWorkloadToFinishWithTimeout(ctx, k8sClient, wlLookupKey, util.LongTimeout)
  			})
  
  			ginkgo.By("Await workload finalized", func() {
  				util.ExpectWorkloadsFinalizedOrGone(ctx, k8sClient, wlLookupKey)
  			})
  ```
  
  This means we first wait for the workload to finish, and only then check whether it’s finalized or already gone. I think this approach is correct and should work as expected.
