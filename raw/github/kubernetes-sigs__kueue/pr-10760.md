# PR #10760: TAS: NodeHotSwap should not add "late pods" to UnhealthyNodes

**Summary**: TAS: NodeHotSwap should not add "late pods" to UnhealthyNodes

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10760

**Last updated**: 2026-04-30T12:17:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `reproduce-late-pods-bug-v2` → `main`
- **Created**: 2026-04-24T13:01:55Z
- **Updated**: 2026-04-30T12:17:20Z
- **Merged**: 2026-04-29T17:57:26Z
- **Closed**: 2026-04-29T17:57:26Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@olekzabl](https://github.com/olekzabl), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+334 / -26**

## Description

#### What type of PR is this?
/kind bug
/area tas

#### What this PR does / why we need it:
A workload's UnhealthyNodes list may be polluted by nodes that no longer belong to the workload if 'late pods' are still present on them.

#### Which issue(s) this PR fixes:
Fixes #10750

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
TAS: Refine the NodeHotSwap logic to ensure that UnhealthyNodes are only updated for workloads currently assigned to a Node via a topology topology assignment. This prevents "late pods" from stale topologies from triggering inaccurate health reporting.
```

> AI Assistance Disclosure: This pull request was created with the assistance of AI

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-24T13:01:58Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-24T13:02:02Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9caaa013229495b3af0d5dc613684e912683e709 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f1c3c8858f2d000866b46a |

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-24T13:02:19Z

/test all

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-24T13:07:17Z

/test all

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T05:39:16Z

/test pull-kueue-test-integration-baseline-main

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T05:42:11Z

/test pull-kueue-test-integration-extended-main

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:28:36Z

/test pull-kueue-test-integration-extended-main
/test pull-kueue-test-unit-main

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:50:17Z

/test pull-kueue-test-integration-extended-main

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T07:39:50Z

/test pull-kueue-test-integration-extended-main

### Comment by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T07:52:20Z

/test all

### Comment by [@pajakd](https://github.com/pajakd) — 2026-04-29T08:35:54Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-29T08:36:02Z

LGTM label has been added.  <details>Git tree hash: dc62efb60985c896e442a53e4492a7662d2936d4</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T17:27:58Z

Thank you!
/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T17:28:01Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10760#issuecomment-4346013102):

>Thank you!
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-29T17:28:06Z

LGTM label has been added.  <details>Git tree hash: bae525530adfded93dcc0c1e22791935d4346727</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-29T17:28:08Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10760#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10760#issuecomment-4346013102" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T17:58:07Z

@mimowo: new pull request created: #10837

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10760#issuecomment-4346013102):

>Thank you!
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T17:58:45Z

@mimowo: new pull request created: #10838

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10760#issuecomment-4346013102):

>Thank you!
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T12:17:18Z

/release-note-edit
```release-note
TAS: Refine the NodeHotSwap logic to ensure that UnhealthyNodes are only updated for workloads currently assigned to a Node via a topology topology assignment. This prevents "late pods" from stale topologies from triggering inaccurate health reporting.
```

## Reviews

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-27T06:05:20Z

- **test/integration/singlecluster/tas/tas_test.go:1539** by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:05:20Z
```diff
@@ -1535,6 +1535,110 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
```

  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10760/pull-kueue-test-integration-extended-main/2048638833209118720
  
  ```bash
  { failed [FAILED] Failed after 0.262s.
  The function passed to Consistently failed at /home/prow/go/src/sigs.k8s.io/kueue/test/integration/singlecluster/tas/tas_test.go:1627 with:
  Expected
      <[]v1beta2.UnhealthyNode | len:1, cap:1>: [{Name: "x3"}]
  to be empty
  In [It] at: /home/prow/go/src/sigs.k8s.io/kueue/test/integration/singlecluster/tas/tas_test.go:1628 @ 04/27/26 06:03:55.181
  }
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-27T06:52:14Z

- **pkg/controller/tas/node_controller_test.go:369** by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:52:14Z
```diff
@@ -317,6 +339,55 @@ func TestNodeFailureReconciler(t *testing.T) {
 			reconcileRequests:  []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
 			wantUnhealthyNodes: []kueue.UnhealthyNode{{Name: nodeName}},
 		},
+		"Node Deleted, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+		},
+		"NodeReady missing, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				testingnode.MakeNode(nodeName).Obj(),
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+		},
```

  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10760/pull-kueue-test-unit-main/2048650511183253504
  ```bash
  {Failed  === RUN   TestNodeFailureReconciler/NodeReady_missing,_late_pod_exists_for_reassigned_workload_->_UnhealthyNodes_not_polluted
      node_controller.go:133: "level"=2 "msg"="Reconcile Node"
      node_controller.go:173: "level"=3 "msg"="NodeReady condition is missing. Marking as failed immediately"
      node_controller_test.go:1035: Unexpected unhealthyNodes in status (-want/+got):
            []v1beta2.UnhealthyNode(
          - 	nil,
          + 	{{Name: "test-node-name"}},
            )
  --- FAIL: TestNodeFailureReconciler/NodeReady_missing,_late_pod_exists_for_reassigned_workload_->_UnhealthyNodes_not_polluted (0.06s)
  }
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-27T06:52:49Z

- **pkg/controller/tas/node_controller_test.go:389** by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:52:50Z
```diff
@@ -317,6 +339,55 @@ func TestNodeFailureReconciler(t *testing.T) {
 			reconcileRequests:  []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
 			wantUnhealthyNodes: []kueue.UnhealthyNode{{Name: nodeName}},
 		},
+		"Node Deleted, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+		},
+		"NodeReady missing, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				testingnode.MakeNode(nodeName).Obj(),
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+		},
+		"Node NotReady, timer expired, patch fails during removal -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				baseNode.Clone().StatusConditions(corev1.NodeCondition{
+					Type:               corev1.NodeReady,
+					Status:             corev1.ConditionFalse,
+					LastTransitionTime: earlierTime}).Obj(),
+				workloadReassignedWithUnhealthyNode,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+			injectPatchError:     true,
+			wantError:            false,
+		},
```

  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10760/pull-kueue-test-unit-main/2048650511183253504
  ```bash
  {Failed  === RUN   TestNodeFailureReconciler/Node_NotReady,_timer_expired,_patch_fails_during_removal_->_UnhealthyNodes_not_polluted
      node_controller.go:133: "level"=2 "msg"="Reconcile Node"
      node_controller.go:179: "level"=3 "msg"="Node is not ready and NodeFailureDelay timer expired, marking as failed"
      node_controller.go:584: "msg"="Failed to patch workload status" "error"="injected patch error on removal" "workload"={"name"="test-workload" "namespace"="default"}
      node_controller_test.go:1025: Reconcile() error = injected patch error on removal, wantError false for request /test-node-name
      node_controller_test.go:1035: Unexpected unhealthyNodes in status (-want/+got):
            []v1beta2.UnhealthyNode(
          - 	nil,
          + 	{{Name: "test-node-name"}},
            )
  --- FAIL: TestNodeFailureReconciler/Node_NotReady,_timer_expired,_patch_fails_during_removal_->_UnhealthyNodes_not_polluted (0.04s)
  }
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-27T06:53:20Z

- **pkg/controller/tas/node_controller_test.go:368** by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T06:53:20Z
```diff
@@ -317,6 +339,55 @@ func TestNodeFailureReconciler(t *testing.T) {
 			reconcileRequests:  []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
 			wantUnhealthyNodes: []kueue.UnhealthyNode{{Name: nodeName}},
 		},
+		"Node Deleted, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
+		},
+		"NodeReady missing, late pod exists for reassigned workload -> UnhealthyNodes not polluted": {
+			initObjs: []client.Object{
+				testingnode.MakeNode(nodeName).Obj(),
+				workloadReassigned,
+				testingpod.MakePod("late-pod", nsName).
+					Annotation(kueue.WorkloadAnnotation, wlName).
+					Annotation(kueue.PodSetUnconstrainedTopologyAnnotation, "true").
+					NodeSelector(corev1.LabelHostname, nodeName).
+					StatusPhase(corev1.PodPending).
+					Obj(),
+			},
+			reconcileRequests:    []reconcile.Request{{NamespacedName: types.NamespacedName{Name: nodeName}}},
+			wantUnhealthyNodes:   nil,
+			ignoreUnhealthyNodes: false,
```

  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10760/pull-kueue-test-unit-main/2048650511183253504
  ```bash
  {Failed  === RUN   TestNodeFailureReconciler/Node_Deleted,_late_pod_exists_for_reassigned_workload_->_UnhealthyNodes_not_polluted
      node_controller.go:133: "level"=2 "msg"="Reconcile Node"
      node_controller.go:167: "level"=3 "msg"="Node not found. Marking as failed immediately"
      node_controller_test.go:1035: Unexpected unhealthyNodes in status (-want/+got):
            []v1beta2.UnhealthyNode(
          - 	nil,
          + 	{{Name: "test-node-name"}},
            )
  --- FAIL: TestNodeFailureReconciler/Node_Deleted,_late_pod_exists_for_reassigned_workload_->_UnhealthyNodes_not_polluted (0.07s)
  }
  ```

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-27T07:50:14Z

- **test/integration/singlecluster/tas/tas_test.go:1749** by [@j-skiba](https://github.com/j-skiba) — 2026-04-27T07:50:14Z
```diff
@@ -1535,6 +1535,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify UnhealthyNodes becomes empty after Hot Swap", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete node object entirely", func() {
+					nodeToDelete := &corev1.Node{ObjectMeta: metav1.ObjectMeta{Name: nodeName}}
+					gomega.Expect(k8sClient.Delete(ctx, nodeToDelete)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
+					podToCleanup := &corev1.Pod{}
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: terminatingPodName, Namespace: ns.Name}, podToCleanup)).To(gomega.Succeed())
+						podToCleanup.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, podToCleanup)).Should(gomega.Succeed())
+						g.Expect(client.IgnoreNotFound(k8sClient.Delete(ctx, podToCleanup))).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.It("should not add unhealthy node when Ready condition is missing but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("make node Ready first to trigger reconcile on subsequent update", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionTrue,
+						LastTransitionTime: metav1.NewTime(time.Now()),
+					})
+				})
+
+				ginkgo.By("remove NodeReady condition completely", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Status.Conditions = nil
+					gomega.Expect(k8sClient.Status().Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
+					podToCleanup := &corev1.Pod{}
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: terminatingPodName, Namespace: ns.Name}, podToCleanup)).To(gomega.Succeed())
+						podToCleanup.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, podToCleanup)).Should(gomega.Succeed())
+						g.Expect(client.IgnoreNotFound(k8sClient.Delete(ctx, podToCleanup))).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
```

  https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10760/pull-kueue-test-integration-extended-main/2048668443435077632
  ```bash
  { failed [FAILED] Failed after 0.261s.
  The function passed to Consistently failed at /home/prow/go/src/sigs.k8s.io/kueue/test/integration/singlecluster/tas/tas_test.go:1735 with:
  Expected
      <[]v1beta2.UnhealthyNode | len:1, cap:1>: [{Name: "x3"}]
  to be empty
  In [It] at: /home/prow/go/src/sigs.k8s.io/kueue/test/integration/singlecluster/tas/tas_test.go:1736 @ 04/27/26 07:48:54.612
  }
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T07:40:34Z

- **pkg/controller/tas/node_controller.go:309** by [@pajakd](https://github.com/pajakd) — 2026-04-29T07:40:34Z
```diff
@@ -304,25 +306,19 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 	return result
 }
 
-// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
-// or have "late" pods assigned to this node via nodeSelector.
-func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var workloadsOnNode kueue.WorkloadList
-	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.AdmittedWorkloadNodesKey: nodeName}); err != nil {
-		return nil, fmt.Errorf("failed to list workloads: %w", err)
-	}
-	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range workloadsOnNode.Items {
-		wl := &workloadsOnNode.Items[i]
-		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+func (r *nodeReconciler) getWorkloadsFromLatePods(
```

  Lets add a comment what this function does.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T07:47:08Z

- **pkg/controller/tas/node_controller.go:337** by [@pajakd](https://github.com/pajakd) — 2026-04-29T07:47:08Z
```diff
@@ -331,9 +327,23 @@ func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string
 			continue
 		}
 		if !workload.IsFinished(&wl) && !workload.IsEvicted(&wl) {
-			tasWorkloadsOnNode.Insert(wlKey)
+			latePodWorkloads.Insert(wlKey)
 		}
 	}
+	return latePodWorkloads, nodeSelectorPodsByWorkload, nil
+}
+
+// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
+func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
```

  I'm thinking about renames to include the method of selecting the workloads on the node. The names will be long but perhaps will make the code easier to follow? WDYT?
  
  getWorkloadsOnNode -> getWorkloadsOnNodeFromTASAssignment
  getWorkloadsFromLatePods -> getWorkloadsOnNodeFromLatePods

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T07:57:38Z

- **pkg/controller/tas/node_controller_test.go:96** by [@pajakd](https://github.com/pajakd) — 2026-04-29T07:57:39Z
```diff
@@ -74,6 +77,24 @@ func TestNodeFailureReconciler(t *testing.T) {
 		AdmittedAt(true, testStartTime).
 		Obj()
 
+	workloadReassigned := utiltestingapi.MakeWorkload(wlName, nsName).
+		Finalizers(kueue.ResourceInUseFinalizerName).
+		PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+		ReserveQuotaAt(
+			utiltestingapi.MakeAdmission("cq").
+				PodSets(utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName).
+					Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+					TopologyAssignment(utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+						Domains(utiltestingapi.MakeTopologyDomainAssignment([]string{nodeName2}, 1).Obj()).
+						Obj()).
+					Obj()).
+				Obj(), testStartTime,
+		).
+		AdmittedAt(true, testStartTime).
+		Obj()
+	workloadReassignedWithUnhealthyNode := workloadReassigned.DeepCopy()
+	workloadReassignedWithUnhealthyNode.Status.UnhealthyNodes = []kueue.UnhealthyNode{{Name: nodeName}}
```

  do we use this variable somewhere? we should not see this state, right?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:03:26Z

- **pkg/controller/tas/node_controller.go:337** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:03:26Z
```diff
@@ -331,9 +327,23 @@ func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string
 			continue
 		}
 		if !workload.IsFinished(&wl) && !workload.IsEvicted(&wl) {
-			tasWorkloadsOnNode.Insert(wlKey)
+			latePodWorkloads.Insert(wlKey)
 		}
 	}
+	return latePodWorkloads, nodeSelectorPodsByWorkload, nil
+}
+
+// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
+func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
```

  sounds good. Actually I think that `getWorkloads*LatePods` might be even a little misleading, practically the function doesn't necessarily get workloads from "late pods", it gets workloads from any pods that have specific nodeSelector, the "lateness" of the pod is decided later. I would propose something like `getWorkloadsFromPodsOnNode` and the second one `getWorkloadsOnNodeFromTASAssignment`. wdyt?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T08:05:19Z

- **test/integration/singlecluster/tas/tas_test.go:1539** by [@pajakd](https://github.com/pajakd) — 2026-04-29T08:05:19Z
```diff
@@ -1535,6 +1535,110 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
```

  I assume this is a result of test run prior to code change.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:06:14Z

- **pkg/controller/tas/node_controller_test.go:96** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:06:14Z
```diff
@@ -74,6 +77,24 @@ func TestNodeFailureReconciler(t *testing.T) {
 		AdmittedAt(true, testStartTime).
 		Obj()
 
+	workloadReassigned := utiltestingapi.MakeWorkload(wlName, nsName).
+		Finalizers(kueue.ResourceInUseFinalizerName).
+		PodSets(*utiltestingapi.MakePodSet(kueue.DefaultPodSetName, 1).Request(corev1.ResourceCPU, "1").Obj()).
+		ReserveQuotaAt(
+			utiltestingapi.MakeAdmission("cq").
+				PodSets(utiltestingapi.MakePodSetAssignment(kueue.DefaultPodSetName).
+					Assignment(corev1.ResourceCPU, "unit-test-flavor", "1").
+					TopologyAssignment(utiltestingapi.MakeTopologyAssignment([]string{corev1.LabelHostname}).
+						Domains(utiltestingapi.MakeTopologyDomainAssignment([]string{nodeName2}, 1).Obj()).
+						Obj()).
+					Obj()).
+				Obj(), testStartTime,
+		).
+		AdmittedAt(true, testStartTime).
+		Obj()
+	workloadReassignedWithUnhealthyNode := workloadReassigned.DeepCopy()
+	workloadReassignedWithUnhealthyNode.Status.UnhealthyNodes = []kueue.UnhealthyNode{{Name: nodeName}}
```

  it was unused, removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:09:01Z

- **test/integration/singlecluster/tas/tas_test.go:1539** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:09:01Z
```diff
@@ -1535,6 +1535,110 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
```

  correct

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T08:14:05Z

- **test/integration/singlecluster/tas/tas_test.go:1739** by [@pajakd](https://github.com/pajakd) — 2026-04-29T08:14:06Z
```diff
@@ -1535,6 +1535,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify UnhealthyNodes becomes empty after Hot Swap", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete node object entirely", func() {
+					nodeToDelete := &corev1.Node{ObjectMeta: metav1.ObjectMeta{Name: nodeName}}
+					gomega.Expect(k8sClient.Delete(ctx, nodeToDelete)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
+					podToCleanup := &corev1.Pod{}
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: terminatingPodName, Namespace: ns.Name}, podToCleanup)).To(gomega.Succeed())
+						podToCleanup.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, podToCleanup)).Should(gomega.Succeed())
+						g.Expect(client.IgnoreNotFound(k8sClient.Delete(ctx, podToCleanup))).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.It("should not add unhealthy node when Ready condition is missing but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("make node Ready first to trigger reconcile on subsequent update", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionTrue,
+						LastTransitionTime: metav1.NewTime(time.Now()),
+					})
+				})
+
+				ginkgo.By("remove NodeReady condition completely", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Status.Conditions = nil
+					gomega.Expect(k8sClient.Status().Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
```

  do we need it? I guess the test could stop after verifying that we don't unnecessarily add to unhealthy nodes.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:34:24Z

- **test/integration/singlecluster/tas/tas_test.go:1739** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:34:24Z
```diff
@@ -1535,6 +1535,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify UnhealthyNodes becomes empty after Hot Swap", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("delete node object entirely", func() {
+					nodeToDelete := &corev1.Node{ObjectMeta: metav1.ObjectMeta{Name: nodeName}}
+					gomega.Expect(k8sClient.Delete(ctx, nodeToDelete)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
+					podToCleanup := &corev1.Pod{}
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: terminatingPodName, Namespace: ns.Name}, podToCleanup)).To(gomega.Succeed())
+						podToCleanup.Finalizers = nil
+						g.Expect(k8sClient.Update(ctx, podToCleanup)).Should(gomega.Succeed())
+						g.Expect(client.IgnoreNotFound(k8sClient.Delete(ctx, podToCleanup))).To(gomega.Succeed())
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+			})
+
+			ginkgo.It("should not add unhealthy node when Ready condition is missing but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionFalse,
+						LastTransitionTime: metav1.NewTime(time.Now().Add(-tas.NodeFailureDelay)),
+					})
+				})
+
+				ginkgo.By("verify Hot Swap completes", func() {
+					gomega.Eventually(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+							utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+								Levels: []string{corev1.LabelHostname},
+								Domains: []utiltas.TopologyDomainAssignment{
+									{Count: 1, Values: []string{"x1"}},
+									{Count: 1, Values: []string{"x4"}},
+								},
+							}),
+						))
+					}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("make node Ready first to trigger reconcile on subsequent update", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					util.SetNodeCondition(ctx, k8sClient, nodeToUpdate, &corev1.NodeCondition{
+						Type:               corev1.NodeReady,
+						Status:             corev1.ConditionTrue,
+						LastTransitionTime: metav1.NewTime(time.Now()),
+					})
+				})
+
+				ginkgo.By("remove NodeReady condition completely", func() {
+					nodeToUpdate := &corev1.Node{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: nodeName}, nodeToUpdate)).Should(gomega.Succeed())
+					nodeToUpdate.Status.Conditions = nil
+					gomega.Expect(k8sClient.Status().Update(ctx, nodeToUpdate)).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("verify unhealthy nodes is not polluted", func() {
+					gomega.Consistently(func(g gomega.Gomega) {
+						g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+						g.Expect(wl.Status.UnhealthyNodes).Should(gomega.BeEmpty())
+					}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+				})
+
+				ginkgo.By("cleanup pod finalizer", func() {
```

  As I understand it, the pod with the finalizer will persist until all tests finish unless we explicitly remove that finalizer.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-29T08:34:29Z

- **test/integration/singlecluster/tas/tas_test.go:1587** by [@pajakd](https://github.com/pajakd) — 2026-04-29T08:34:29Z
```diff
@@ -1535,6 +1535,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
```

  "make node x3 ..."

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:37:42Z

- **pkg/controller/tas/node_controller.go:337** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:37:42Z
```diff
@@ -331,9 +327,23 @@ func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string
 			continue
 		}
 		if !workload.IsFinished(&wl) && !workload.IsEvicted(&wl) {
-			tasWorkloadsOnNode.Insert(wlKey)
+			latePodWorkloads.Insert(wlKey)
 		}
 	}
+	return latePodWorkloads, nodeSelectorPodsByWorkload, nil
+}
+
+// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
+func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string) (sets.Set[types.NamespacedName], error) {
```

  done.
  
  I also moved `listPodsAssignedByNodeSelector` call outside of `getWorkloadsFromPodsOnNode` - I think it makes more sense this way because we don't have to return `nodeSelectorPodsByWorkload` from `getWorkloadsFromPodsOnNode`.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:37:52Z

- **pkg/controller/tas/node_controller.go:309** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:37:52Z
```diff
@@ -304,25 +306,19 @@ func groupPodsByWorkload(pods []corev1.Pod) map[types.NamespacedName][]*corev1.P
 	return result
 }
 
-// getWorkloadsOnNode gets all workloads that have the given node assigned in TAS topology assignment
-// or have "late" pods assigned to this node via nodeSelector.
-func (r *nodeReconciler) getWorkloadsOnNode(ctx context.Context, nodeName string, nodeSelectorPodsByWorkload map[types.NamespacedName][]*corev1.Pod) (sets.Set[types.NamespacedName], error) {
-	var workloadsOnNode kueue.WorkloadList
-	if err := r.client.List(ctx, &workloadsOnNode, client.MatchingFields{indexer.AdmittedWorkloadNodesKey: nodeName}); err != nil {
-		return nil, fmt.Errorf("failed to list workloads: %w", err)
-	}
-	tasWorkloadsOnNode := sets.New[types.NamespacedName]()
-	for i := range workloadsOnNode.Items {
-		wl := &workloadsOnNode.Items[i]
-		tasWorkloadsOnNode.Insert(types.NamespacedName{Name: wl.Name, Namespace: wl.Namespace})
+func (r *nodeReconciler) getWorkloadsFromLatePods(
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-04-29T08:39:48Z

- **test/integration/singlecluster/tas/tas_test.go:1587** by [@j-skiba](https://github.com/j-skiba) — 2026-04-29T08:39:47Z
```diff
@@ -1535,6 +1535,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 					util.ExpectWorkloadsToBePending(ctx, k8sClient, wl2)
 				})
 			})
+
+			ginkgo.It("should not add unhealthy node when node is deleted but late pods exist", framework.SlowSpec, func() {
+				var wl *kueue.Workload
+				nodeName := nodes[0].Name
+				var terminatingPodName string
+
+				ginkgo.By("creating a workload that gets admitted", func() {
+					wl = utiltestingapi.MakeWorkload("wl", ns.Name).
+						PodSets(*utiltestingapi.MakePodSet("worker", 2).
+							PreferredTopologyRequest(utiltesting.DefaultBlockTopologyLevel).
+							Obj()).
+						Queue(kueue.LocalQueueName(localQueue.Name)).Request(corev1.ResourceCPU, "1").Obj()
+					util.MustCreate(ctx, k8sClient, wl)
+				})
+
+				ginkgo.By("verify the workload is admitted to x3 and x1", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, wl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), wl)).To(gomega.Succeed())
+					gomega.Expect(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{Count: 1, Values: []string{"x3"}},
+								{Count: 1, Values: []string{"x1"}},
+							},
+						}),
+					))
+				})
+
+				createPodsForWorkload(wl, ns.Name, true, false)
+
+				ginkgo.By("make one pod terminating on x3", func() {
+					pod0 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-0", wl.Name), Namespace: ns.Name}, pod0)).Should(gomega.Succeed())
+					pod1 := &corev1.Pod{}
+					gomega.Expect(k8sClient.Get(ctx, apitypes.NamespacedName{Name: fmt.Sprintf("%s-1", wl.Name), Namespace: ns.Name}, pod1)).Should(gomega.Succeed())
+
+					for _, podCopy := range []*corev1.Pod{pod0, pod1} {
+						if podCopy.Spec.NodeSelector[corev1.LabelHostname] == nodeName {
+							terminatingPodName = podCopy.Name
+							podCopy.Finalizers = append(podCopy.Finalizers, "kueue.x-k8s.io/dummy-finalizer")
+							gomega.Expect(k8sClient.Update(ctx, podCopy)).Should(gomega.Succeed())
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						} else {
+							gomega.Expect(k8sClient.Delete(ctx, podCopy)).Should(gomega.Succeed())
+						}
+					}
+				})
+
+				ginkgo.By("make node NotReady to trigger node replacement", func() {
```

  updated
