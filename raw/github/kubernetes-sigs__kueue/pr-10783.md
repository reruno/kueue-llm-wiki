# PR #10783: fix: Fix TAS stateWithLeader aggregation for grouped podsets

**Summary**: fix: Fix TAS stateWithLeader aggregation for grouped podsets

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10783

**Last updated**: 2026-04-30T14:20:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yuluo-yx](https://github.com/yuluo-yx)
- **Branch**: `0426-yuluo/fix` → `main`
- **Created**: 2026-04-26T02:44:18Z
- **Updated**: 2026-04-30T14:20:54Z
- **Merged**: 2026-04-29T09:19:25Z
- **Closed**: 2026-04-29T09:19:25Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Changed files**: 3   **+253 / -11**

## Description

  ## What this PR does

  For grouped podSets with a leader, parent topology domains were computing the `stateWithLeader` penalty across all children, including children that cannot host the leader. Empty or fully occupied children have `state=0`,
  `stateWithLeader=0`, and `leaderState=0`, so they contributed a zero penalty. This could make the parent domain report `stateWithLeader == state`, even when the only leader-capable child could not also fit the workers after placing
  the leader.

  In practice, this caused the scheduler to select a smaller topology domain that looked like it could fit the grouped workload during phase 1, then fail during assignment generation and produce an empty/malformed
  `TopologyAssignment`.

  This PR changes the parent aggregation logic so that, when a leader is required, only leader-capable children contribute to the `stateWithLeader` and `sliceStateWithLeader` penalty calculation. If no child can host the leader, the
  parent with-leader capacity is set to zero.

  The balanced placement pruning path was updated to preserve the same leader-aware aggregation behavior when recomputing domain counts.

  ## Tests

  Added regression coverage in `TestFindTopologyAssignments` for:
  - equal-size grouped leader/worker podSets where the small domain must be skipped
  - mixed-size grouped podSets where workers fit without the leader but not after leader placement
  - a positive case where leader and workers do fit together on the tight domain and should still be assigned there

  Fixes #10778.
  
  ```release-note
TAS: Fix handling of PodSet groups which could lead in some scenarios to empty topologyAssignment.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-26T02:44:25Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 1b74154fc1b59d792c16dc32aec65cd3c317b7e7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69f0c6e1e0cf9d00089e8bf5 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-26T02:44:29Z

Hi @yuluo-yx. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T07:10:52Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T07:14:15Z

I injected a tentative release note, @yuluo-yx ptal. Next time please keep the release-note in PR template as our automation is using the field.\

> In practice, this caused the scheduler to select a smaller topology domain that looked like it could fit the grouped workload during phase 1, then fail during assignment generation and produce an empty/malformed
TopologyAssignment.

@yuluo-yx question here: what would happen in this scenario: the workload would be admitted with empty topologyAssignment, or admission is rejected?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T07:15:42Z

Let me see if we can have reviews from folks familiar with this code, cc @pajakd @PBundyra

### Comment by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-28T14:36:00Z

/retest

### Comment by [@pajakd](https://github.com/pajakd) — 2026-04-29T07:36:16Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-29T07:36:24Z

LGTM label has been added.  <details>Git tree hash: ff3c3937785ca9d8f0a9829d6c0e390f47824515</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T08:51:23Z

/kind bug

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T09:09:12Z

/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T09:09:15Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10783#issuecomment-4342293671):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-29T09:17:53Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10783#pullrequestreview-4195564229" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10783#" title="Author self-approved">yuluo-yx</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T09:20:19Z

@mimowo: #10783 failed to apply on top of branch "release-0.17":
```
Applying: fix: Fix TAS stateWithLeader aggregation for grouped podsets
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_balanced_placement.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_balanced_placement.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_balanced_placement.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 fix: Fix TAS stateWithLeader aggregation for grouped podsets

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10783#issuecomment-4342293671):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T09:20:54Z

@mimowo: #10783 failed to apply on top of branch "release-0.16":
```
Applying: fix: Fix TAS stateWithLeader aggregation for grouped podsets
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_balanced_placement.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_balanced_placement.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_balanced_placement.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 fix: Fix TAS stateWithLeader aggregation for grouped podsets

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10783#issuecomment-4342293671):

>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T10:05:51Z

@yuluo-yx could you check if we could cherrypick without conflicts if we cherrypicked first https://github.com/kubernetes-sigs/kueue/pull/10506 and https://github.com/kubernetes-sigs/kueue/pull/10537? 

If this is the case then I'm happy to CP then. Otherwise we can use ./hack/cherry_pick_pull.sh to prepare the CPs.

### Comment by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-29T14:13:59Z

> @yuluo-yx could you check if we could cherrypick without conflicts if we cherrypicked first #10506 and #10537?
> 
> If this is the case then I'm happy to CP then. Otherwise we can use ./hack/cherry_pick_pull.sh to prepare the CPs.

got it, I have verified this locally:

For `release-0.17`, even after cherry-picking #10506 and #10537, #10783 still results in a conflict within `pkg/cache/scheduler/tas_balanced_placement.go`. The reason is that #10783 depends on the splitting of the "topology assignment state" introduced in #10598. By applying the patches in the sequence #10506 -> #10537 -> #10598 -> #10783, #10783 can be applied to `release-0.17` without any conflicts.

For `release-0.16`, #10506 itself generates a conflict in `pkg/cache/scheduler/tas_flavor_snapshot.go`; therefore, simply backporting #10506 and #10537 beforehand does not yield a conflict-free path.

The conclusion is: this approach works for `release-0.17` only if #10598 is also backported; for `release-0.16`, manual preparation is still required to perform the cherry-pick.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T16:16:06Z

@yuluo-yx thank you for the summary. Okay let me proceed with 0.17 path. All of the PRs are straighforward cleanups.

> For release-0.16, https://github.com/kubernetes-sigs/kueue/pull/10506 itself generates a conflict in pkg/cache/scheduler/tas_flavor_snapshot.go; therefore, simply backporting https://github.com/kubernetes-sigs/kueue/pull/10506 and https://github.com/kubernetes-sigs/kueue/pull/10537 beforehand does not yield a conflict-free path.

would it be feasible to backport this one manually, to unblock backporting this PR easier for 0.16?

I'm thinking for 0.16 we could put all the chain as a single PR, just different commits, wdyt?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-29T18:06:48Z

/cherrypick release-0.17
This should be clean now

Edit: failed, ptal

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-04-29T18:07:25Z

@mimowo: #10783 failed to apply on top of branch "release-0.17":
```
Applying: fix: Fix TAS stateWithLeader aggregation for grouped podsets
Using index info to reconstruct a base tree...
M	pkg/cache/scheduler/tas_balanced_placement.go
M	pkg/cache/scheduler/tas_cache_test.go
M	pkg/cache/scheduler/tas_flavor_snapshot.go
Falling back to patching base and 3-way merge...
Auto-merging pkg/cache/scheduler/tas_flavor_snapshot.go
Auto-merging pkg/cache/scheduler/tas_cache_test.go
Auto-merging pkg/cache/scheduler/tas_balanced_placement.go
CONFLICT (content): Merge conflict in pkg/cache/scheduler/tas_balanced_placement.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 fix: Fix TAS stateWithLeader aggregation for grouped podsets

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10783#issuecomment-4346280294):

>/cherrypick release-0.17
>This should be clean now


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-30T12:25:17Z

/release-note-edit
```release-note
TAS: Fix handling of PodSet groups which could lead in some scenarios to empty topologyAssignment.
```

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-27T09:39:50Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1711** by [@pajakd](https://github.com/pajakd) — 2026-04-27T09:39:50Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
+		domain.stateWithLeader = 0
+	} else {
+		domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	}
 	domain.leaderState = leaderState
-	sliceStateWithLeader := sliceCapacity - minSliceStateWithLeaderDifference
+	sliceStateWithLeader := int32(0)
+	if minSliceStateWithLeaderDifference != math.MaxInt32 {
+		sliceStateWithLeader = sliceCapacity - minSliceStateWithLeaderDifference
+	}
```

  Cannot we merge the two ifs into one? The condition is the same and the line in between is not interfering, right?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-27T10:19:56Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1695** by [@pajakd](https://github.com/pajakd) — 2026-04-27T10:19:56Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
```

  it is counter-intuitive that we enter this code branch if the leader is not required. Why do we have to do it?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-27T11:07:34Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1702** by [@pajakd](https://github.com/pajakd) — 2026-04-27T11:07:34Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
```

  This works now, but looks brittle (what if we change `minStateWithLeaderDifference` type or initial value). I would prefer an explicit flag.

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-04-27T14:39:06Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1711** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-27T14:39:06Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
+		domain.stateWithLeader = 0
+	} else {
+		domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	}
 	domain.leaderState = leaderState
-	sliceStateWithLeader := sliceCapacity - minSliceStateWithLeaderDifference
+	sliceStateWithLeader := int32(0)
+	if minSliceStateWithLeaderDifference != math.MaxInt32 {
+		sliceStateWithLeader = sliceCapacity - minSliceStateWithLeaderDifference
+	}
```

  yep, done

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-04-27T14:48:46Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1695** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-27T14:48:46Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
```

  In PodSetGrouping with a leader, `stateWithLeader` indicates "how many workers can be added to a domain after adding the leader," so only children with `child.leaderState > 0` can participate in penalty calculation. Otherwise, the occupied child will only have a penalty of 0, as mentioned in the bug report.
  
  However, in a normal TAS without a leader, `stateWithLeader` will still be read by subsequent general paths. In this case, it should degenerate into normal worker capacity, meaning all children participate in the calculation. If this branch is not entered when `!leaderRequired` is called, `minStateWithLeaderDifference` will remain `math.MaxInt32`, the parent domain's `stateWithLeader` will be set to 0, and worker-only scheduling will be judged as unavailable.

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-04-27T14:55:46Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1702** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-27T14:55:46Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
```

  How about using bool instead? 👀 
  
  such as:
  
  ```shell
  hasWithLeaderCapacityContributor = false
  
  if !leaderRequired || child.leaderState > 0 {
  	hasWithLeaderCapacityContributor = true
  	.....
  }
  
  if hasWithLeaderCapacityContributor {
  	// do
  }
  ```

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-28T08:32:01Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1702** by [@pajakd](https://github.com/pajakd) — 2026-04-28T08:32:00Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
```

  Yes, exactly

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-04-28T08:35:34Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1695** by [@pajakd](https://github.com/pajakd) — 2026-04-28T08:35:34Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
```

  OK, Makes sense. It would be nice (in a future PR) to decouple so that TAS without leader does not depend on any of the "withLeader" fields or variables.

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-04-28T14:26:29Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1702** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-28T14:26:28Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
+			minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
+			minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		}
 		leaderState = max(child.leaderState, leaderState)
 	}
 	domain.state = childrenCapacity
-	domain.stateWithLeader = childrenCapacity - minStateWithLeaderDifference
+	if minStateWithLeaderDifference == math.MaxInt32 {
```

  done

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-04-28T14:35:26Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1695** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-04-28T14:35:26Z
```diff
@@ -1692,14 +1692,23 @@ func (s *TASFlavorSnapshot) fillInCountsHelper(domain *domain, sliceSize int32,
 
 		childrenCapacity += childState
 		sliceCapacity += child.sliceState
-		minStateWithLeaderDifference = min(childState-childStateWithLeader, minStateWithLeaderDifference)
-		minSliceStateWithLeaderDifference = min(child.sliceState-child.sliceStateWithLeader, minSliceStateWithLeaderDifference)
+		if !leaderRequired || child.leaderState > 0 {
```

  got it. I created #10812 to trac it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-29T09:17:44Z

Thank you 👍 
/lgtm
/approve
