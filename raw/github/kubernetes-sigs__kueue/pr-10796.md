# PR #10796: KEP-10765: WorkloadPriorityClass defaulting

**Summary**: KEP-10765: WorkloadPriorityClass defaulting

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10796

**Last updated**: 2026-05-15T12:15:20Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@atosatto](https://github.com/atosatto)
- **Branch**: `workload-priority-class-defaulting-kep` → `main`
- **Created**: 2026-04-27T08:34:53Z
- **Updated**: 2026-05-15T12:15:20Z
- **Merged**: 2026-05-15T09:14:28Z
- **Closed**: 2026-05-15T09:14:29Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`, `kind/kep`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@pajakd](https://github.com/pajakd), [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 2   **+356 / -0**

## Description

#### What type of PR is this?

/kind kep

#### What this PR does / why we need it:

Adds KEP-10765 for WorkloadPriorityClassDefaulting, a new feature gate that automatically assigns a WorkloadPriorityClass named default to workloads that don't specify one via the `kueue.x-k8s.io/priority-class` label.

In clusters where only a subset of workloads are managed by Kueue, this avoids conflating pod scheduling priority with workload queueing priority for jobs that omit the label, without requiring custom mutating webhooks or per-job labeling by users.

The design follows the same convention-based pattern as LocalQueueDefaulting.

#### Which issue(s) this PR fixes:

Related #10765 

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-04-27T08:35:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 781932491442371a8c81b95950457d80f820455f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a056c87cb69140009e84f0e |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-04-27T08:35:04Z

Hi @atosatto. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@atosatto](https://github.com/atosatto) — 2026-04-27T08:35:47Z

/cc @tenzen-y @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2026-04-27T08:50:23Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T12:38:05Z

Checking the latest version now

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-11T10:06:06Z

LGTM, let me give some time for other reviewers in case they have additional comments

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T04:09:29Z

> LGTM, let me give some time for other reviewers in case they have additional comments

ACK

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:31:25Z

@atosatto Thank you for updating this KEP!
The continuing threads are https://github.com/kubernetes-sigs/kueue/pull/10796#discussion_r3228120659 and https://github.com/kubernetes-sigs/kueue/pull/10796#discussion_r3228141281

### Comment by [@atosatto](https://github.com/atosatto) — 2026-05-13T22:59:12Z

> @atosatto Thank you for updating this KEP! The continuing threads are [#10796 (comment)](https://github.com/kubernetes-sigs/kueue/pull/10796#discussion_r3228120659) and [#10796 (comment)](https://github.com/kubernetes-sigs/kueue/pull/10796#discussion_r3228141281)

Thank you @tenzen-y for your review! I should have addressed all of the remaining feedback in the KEP. 🤞

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T08:31:01Z

LGTM label has been added.  <details>Git tree hash: 06a3518f6c291101c53e4ff35eca8247bc3e5e3d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T08:31:03Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10796#" title="Author self-approved">atosatto</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10796#pullrequestreview-4296617424" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-04-27T09:15:38Z

- **keps/10765-workload-priority-class-defaulting/README.md:74** by [@mwielgus](https://github.com/mwielgus) — 2026-04-27T09:12:35Z
```diff
@@ -0,0 +1,179 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Priority resolution](#priority-resolution)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [Configuration-based default](#configuration-based-default)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Defaulting the `kueue.x-k8s.io/priority-class` label on the Job object at
+  webhook time. The defaulting happens during workload construction in the
+  reconciler.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
```

  What happens if the default WorkloadPriorityClass is not there?

- **keps/10765-workload-priority-class-defaulting/README.md:121** by [@mwielgus](https://github.com/mwielgus) — 2026-04-27T09:15:31Z
```diff
@@ -0,0 +1,179 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Priority resolution](#priority-resolution)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [Configuration-based default](#configuration-based-default)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Defaulting the `kueue.x-k8s.io/priority-class` label on the Job object at
+  webhook time. The defaulting happens during workload construction in the
+  reconciler.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Priority resolution
+
+The current priority resolution chain in `ExtractPriority()` is:
+
+1. Explicit `WorkloadPriorityClass` via `kueue.x-k8s.io/priority-class` label
+2. Custom priority class from the job framework (e.g., MPIJob's `schedulingPolicy.priorityClass`)
+3. Kubernetes PodSpec `priorityClassName`
+4. Kubernetes `globalDefault` PriorityClass
+5. Hardcoded `0`
+
+With this KEP, a new step is inserted between step 1 and step 2 when the
+`WorkloadPriorityClassDefaulting` feature gate is enabled:
```

  Does Kueue provide any visibility for the user how the workload ended up with a specific priority? If not, can we have it added?

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-27T09:29:18Z

- **keps/10765-workload-priority-class-defaulting/README.md:74** by [@atosatto](https://github.com/atosatto) — 2026-04-27T09:29:17Z
```diff
@@ -0,0 +1,179 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Priority resolution](#priority-resolution)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [Configuration-based default](#configuration-based-default)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Defaulting the `kueue.x-k8s.io/priority-class` label on the Job object at
+  webhook time. The defaulting happens during workload construction in the
+  reconciler.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
```

  In that case, we will fall-back to the Kubernetes PodSpec `priorityClassName`.
  The intended behaviour is captured in the " Design Details" section.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-27T09:35:35Z

- **keps/10765-workload-priority-class-defaulting/README.md:121** by [@atosatto](https://github.com/atosatto) — 2026-04-27T09:35:35Z
```diff
@@ -0,0 +1,179 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Priority resolution](#priority-resolution)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [Configuration-based default](#configuration-based-default)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Defaulting the `kueue.x-k8s.io/priority-class` label on the Job object at
+  webhook time. The defaulting happens during workload construction in the
+  reconciler.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Priority resolution
+
+The current priority resolution chain in `ExtractPriority()` is:
+
+1. Explicit `WorkloadPriorityClass` via `kueue.x-k8s.io/priority-class` label
+2. Custom priority class from the job framework (e.g., MPIJob's `schedulingPolicy.priorityClass`)
+3. Kubernetes PodSpec `priorityClassName`
+4. Kubernetes `globalDefault` PriorityClass
+5. Hardcoded `0`
+
+With this KEP, a new step is inserted between step 1 and step 2 when the
+`WorkloadPriorityClassDefaulting` feature gate is enabled:
```

  Similar to LocalQueueDefaulting the idea is to set the `kueue.x-k8s.io/priority-class` on the Job if the `default` WorkloadPriorityClass exists and the label has not been set by the Job owner. Is there any other any other mechanism you would suggest adopting in this case?

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-27T10:07:35Z

- **keps/10765-workload-priority-class-defaulting/README.md:121** by [@atosatto](https://github.com/atosatto) — 2026-04-27T10:07:35Z
```diff
@@ -0,0 +1,179 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Priority resolution](#priority-resolution)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [Configuration-based default](#configuration-based-default)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Defaulting the `kueue.x-k8s.io/priority-class` label on the Job object at
+  webhook time. The defaulting happens during workload construction in the
+  reconciler.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Priority resolution
+
+The current priority resolution chain in `ExtractPriority()` is:
+
+1. Explicit `WorkloadPriorityClass` via `kueue.x-k8s.io/priority-class` label
+2. Custom priority class from the job framework (e.g., MPIJob's `schedulingPolicy.priorityClass`)
+3. Kubernetes PodSpec `priorityClassName`
+4. Kubernetes `globalDefault` PriorityClass
+5. Hardcoded `0`
+
+With this KEP, a new step is inserted between step 1 and step 2 when the
+`WorkloadPriorityClassDefaulting` feature gate is enabled:
```

  I have pushed an update that hopefully should capture better the intended behaviour in the KEP document.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T15:38:15Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@mimowo](https://github.com/mimowo) — 2026-04-30T15:38:15Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  I'm not sure which approach is better, but the alternative one is to default the WPC at the moment of Workload creation. I remember this was discussed at the LocalQeueDefaulting, and we didn't do that for some reasons I cannot now recall. 
  
  The good things about workload-level defaulting:
  1. one place agnostic of the actual Job
  2. not a hot path like webhooks, so we can easily afford to send an event for debuggability
  
  Let me know your thoughts. Maybe for Alpha we could start at workload level and re-evaluate if we need webhooks.
  
  cc @yaroslava-serdiuk do you maybe remember why LocalQueueDefaulting was done at the Job level?

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-30T17:12:52Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-04-30T17:12:52Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  > I'm not sure which approach is better, but the alternative one is to default the WPC at the moment of Workload creation. I remember this was discussed at the LocalQeueDefaulting, and we didn't do that for some reasons I cannot now recall.
  > Let me know your thoughts. Maybe for Alpha we could start at workload level and re-evaluate if we need webhooks.
  
  I think workload level defaulting is a good alternative, but we would be changing the behaviour from what we have done with LocalQueue. Is there any specific concern with the webhook? Is this about scalability?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T17:41:46Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@mimowo](https://github.com/mimowo) — 2026-04-30T17:41:46Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  > I think workload level defaulting is a good alternative, but we would be changing the behaviour from what we have done with LocalQueue. Is there any specific concern with the webhook? Is this about scalability?
  
  Scalability (performance) is not my concern, because this check will be just cache-based. Rather the amount of testing and code duplication to ensure all integrations have it.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-30T17:50:03Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-04-30T17:50:02Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  There is an implementation here already - I feel it is reasonably self-contained which makes me comfortable with the webhook approach being ok.
  
  https://github.com/kubernetes-sigs/kueue/pull/10798
  
  Let me know what you think!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T18:20:39Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@mimowo](https://github.com/mimowo) — 2026-04-30T18:20:39Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  lgtm thank you. We could also consider as a follow up 
  
  ```golang
  jobframework.ApplyDefaultLocalQueue(job.Object(), w.queues.DefaultLocalQueueExist)
  jobframework.ApplyDefaultWorkloadPriorityClass(ctx, w.client, job.Object())
  ```
  In that case I'm ok with this approach, because the code is simple, already exercised by LocalQueueDefaulting.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-04-30T19:04:21Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-04-30T19:04:21Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  Are you suggesting any specific change to the proposed PR? Feel free to comment there whenever you get the chance. I will take care of implementing them as I rebase the code.
  
  Also @mimowo could it be ok for me to add you as reviewer and approver for this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-04-30T19:11:46Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@mimowo](https://github.com/mimowo) — 2026-04-30T19:11:46Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  let's mention the alternative in the Alternatives section and add apoint to graduation criteria to re evaluate the approach before going to beta based on user feedback. So that we make it clear it may change likely it will not.
  
  yes add me as an approver. add as reviewers other folks participating in to review too

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-01T04:37:22Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-05-01T04:37:22Z
```diff
@@ -0,0 +1,187 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  Thanks! Added the changes we discussed in e62c558. Let me know if you have any other feedback 🙏

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-06T12:47:45Z

LGTM thank you 👍 I will give a bit more time to review by @tenzen-y, but green light from me. We have exercised the same pattern with LocalQueue defaulting and haven't encountered user issues.

- **keps/10765-workload-priority-class-defaulting/README.md:103** by [@mimowo](https://github.com/mimowo) — 2026-05-06T12:45:39Z
```diff
@@ -0,0 +1,210 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
```

  Maybe let's rephase that slightly to emphasize:
  - the semantics is changed in case the environment already contains WPC named "default" - other environemnts are not affected
  To mitigate:
  - feature gate will allow admins to disable the mechanism while in Beta in case their environments are affected negatively
  - we will add information about this change in the release notes

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-06T23:21:34Z

- **keps/10765-workload-priority-class-defaulting/README.md:103** by [@atosatto](https://github.com/atosatto) — 2026-05-06T23:21:34Z
```diff
@@ -0,0 +1,210 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature changes the priority of workloads that previously fell
+through to the Kubernetes PriorityClass default. Admins should set the `default`
+WorkloadPriorityClass value carefully before enabling the feature gate to avoid
+unexpected changes in queueing and preemption behavior.
```

  Sounds good, addressed this in c56cc9695

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-07T16:41:53Z

@atosatto Thank you for working on this KEP, and apologies for delaying my review.
I left some comments to clarify this feature specifications.

- **keps/10765-workload-priority-class-defaulting/README.md:130** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T16:29:25Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
```

  Just checking about `custom webhooks`. Do we call this new `ApplyDefaultWorkloadPriorityClass` in Pod integration as well? That's why I am asking this is that Pod integration doesn't use `BaseWebhook`, AFAIK.

- **keps/10765-workload-priority-class-defaulting/README.md:181** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T16:35:03Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
```

  AFAIK, admins can remove WorkloadPriorityClass (WPC) even when any Workload points to the WPC.
  
  It's ok to align with K8S PriorityClass behavior, but I want to check if Workload creation will fail in the same way as today when `default` WPC doesn't exist during Workload creation (default WPC is removed after being stamped by webhooks).

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T16:36:44Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  What if Job podTemplateSpec has Pod PriorityClass?

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T16:39:22Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  Do we stamp `kueue.x-k8s.io/queue-name: default` label only in managed namespaces or all namespaces? And does this WorkloadPriorityClass defaulting have compatibility with manageJobsWithoutQueueName?

- **keps/10765-workload-priority-class-defaulting/kep.yaml:14** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-07T16:41:09Z
```diff
@@ -0,0 +1,30 @@
+title: WorkloadPriorityClass defaulting
+kep-number: 10765
+authors:
+  - "@atosatto"
+status: implementable
+creation-date: 2026-04-27
+reviewers:
+  - "@vladikkuzn"
+  - "@mwielgus"
+  - "@tenzen-y"
+approvers:
+  - "@mimowo"
+
```

  ```suggestion
    - "@mimowo"
    - "@tenzen-y"
    
  ```
  
  Just adding me to hew :)

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-07T20:46:08Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@kannon92](https://github.com/kannon92) — 2026-05-07T20:46:08Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  In Slurm, they have a default setting at the partition level (ClusterQueue equivalent).
  
  https://slurm.schedmd.com/qos.html#priority
  
  I've been interested in having Kueue maybe consider ClusterQueue defaults or LocalQueue defaults for some of these values. Slurm has a concept of QoS which sets resources, priority, wall time limits.
  
  Have we considered this in this design?
  
  https://github.com/kubernetes-sigs/kueue/issues/7029

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T04:59:45Z

- **keps/10765-workload-priority-class-defaulting/README.md:130** by [@atosatto](https://github.com/atosatto) — 2026-05-08T04:59:45Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
```

  Good catch, thanks! For the Pod integration the call to `ApplyDefaultWorkloadPriorityClass` is wired explicitly from `PodWebhook.Default`, same as the other custom-webhook integrations (Deployment, Job, JobSet, LeaderWorkerSet, MPIJob, RayCluster, RayJob, RayService, SparkApplication, StatefulSet, TrainJob), as done already for `LocalQueueDefaulting`.
  
  I've updated the KEP to enumerate which integrations inherit the call via `BaseWebhook.Default` and which invoke it from their own `Default` method, so it's explicit for future readers.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T05:52:49Z

- **keps/10765-workload-priority-class-defaulting/README.md:181** by [@atosatto](https://github.com/atosatto) — 2026-05-08T05:52:49Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
```

  Yes, this aligns with today's behavior. The `default` WorkloadPriorityClass label is resolved at Job-creation time. When default WPC doesn't exist during Workload creation, the Get returns NotFound, the error propagates up, and the reconciler fails to create the Workload (and retries until the WPC is recreated or the label is removed).
  
  That's identical to what happens today if a user sets `kueue.x-k8s.io/priority-class: foo` manually and an admin later deletes `foo`. This means that removing any WorkloadPriorityClass, including `default`, has the risk of impacting multiple Workloads within a cluster. In order to do this operation safely the operator would have to first disable the feature-flag and then remove the `default` WPC. This does not seem great if we intend to remove this flag ad part of the GA promotion criteria.
  
  Following up on @kannon92 's comment here - one of the alternatives we might want to consider is switching the defaulting to the `ClusterQueue`. Will add this to the alternatives considered so that we can explore whether it's a much more desirable approach.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T06:40:23Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@atosatto](https://github.com/atosatto) — 2026-05-08T06:40:23Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  That's a great feedback! I have added this to the alternatives considered for now (see 40a6a07d7) but I feel this could be the way we deliver this feature.
  
  @kannon92  / @tenzen-y  / @mimowo any feedback on making defaulting at the ClusterQueue vs LocalQueue level? I can see merits for both approaches. I am curious on whether there are specific use-cases that would be supported by one approach but not the other that you might be aware of.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T06:44:43Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-05-08T06:44:43Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  Yes, when defaulting is enabled and a `default` WPC exists the webhook still stamps the `kueue.x-k8s.io/priority-class: default` label. Defaulting at the ClusterQueue or LocalQueue (as described in 40a6a07d7) gives us a more flexible approach to enable this behaviour on a more granular basis vs cluster-wide.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T06:46:58Z

- **keps/10765-workload-priority-class-defaulting/kep.yaml:14** by [@atosatto](https://github.com/atosatto) — 2026-05-08T06:46:57Z
```diff
@@ -0,0 +1,30 @@
+title: WorkloadPriorityClass defaulting
+kep-number: 10765
+authors:
+  - "@atosatto"
+status: implementable
+creation-date: 2026-04-27
+reviewers:
+  - "@vladikkuzn"
+  - "@mwielgus"
+  - "@tenzen-y"
+approvers:
+  - "@mimowo"
+
```

  Done in e7f121cab! 🙇

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T09:45:11Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-05-08T09:45:11Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  > Do we stamp kueue.x-k8s.io/queue-name: default label only in managed namespaces or all namespaces?
  
  The current LocalQueue defaulting logic is set independently from the `managedJobsNamespaceSelector` as the label is set in the webhook before before `ApplyDefaultForSuspend`, which is where the namespace-selector check happens. For WorkloadPriorityClass defaulting we are following the same pattern.
  
  > And does this WorkloadPriorityClass defaulting have compatibility with manageJobsWithoutQueueName?
  
  The default WorkloadPriorityClass is set regardless on the `kueue.x-k8s.io/queue-name` label. so Jobs admitted under `manageJobsWithoutQueueName=true` should be defaulted as expected.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-08T12:57:13Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@kannon92](https://github.com/kannon92) — 2026-05-08T12:57:13Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  Thanks for adding that!

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-08T13:26:48Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@atosatto](https://github.com/atosatto) — 2026-05-08T13:26:47Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  @kannon92  do you have any suggestion on whether this should be a LocalQueue or ClusterQueue level setting?

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-08T14:26:41Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@kannon92](https://github.com/kannon92) — 2026-05-08T14:26:41Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  Let me ask the slurm folks. cc @catblade

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-08T14:27:56Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@kannon92](https://github.com/kannon92) — 2026-05-08T14:27:56Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  I'm not sure if priority should be namespaced setting or a global setting. I think slurm provides options for both but not sure what is most commonly used.
  
  Based on feedback I got for time-limits I think ClusterQueue as that is where we put most of our configuration for ClusterQueues. LocalQueues are primarly a link to a CQ.
  
  But I want to get some input on slurm community what they find the most used/useful.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T10:03:00Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@mimowo](https://github.com/mimowo) — 2026-05-11T10:03:00Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  I think both global and CQ-level have some use-cases and are not mutually exclusive. 
  
  Global default can be helpful for the stories which we already have, and corresponds to the `globalDefault` setting in the core k8s. 
  
  Also, doing it global is no API commitment, whereas CQ-level entails new APIs. In other words: if we make global setting as Alpha, we can easily change our approach in the next release. However, if we choose to introduce new CQ level API fields, then the cost of deprecating the approach is much higher as it requires v1beta3 to drop the fields.
  
  So I would suggest to follow YAGNI and start with global for now, then only work on CQ-level when we discover that this is needed.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-11T14:39:22Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@atosatto](https://github.com/atosatto) — 2026-05-11T14:39:22Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  > Also, doing it global is no API commitment, whereas CQ-level entails new APIs. In other words: if we make global setting as Alpha, we can easily change our approach in the next release. However, if we choose to introduce new CQ level API fields, then the cost of deprecating the approach is much higher as it requires v1beta3 to drop the fields.
  
  That's reasonable. I can definitely see us doing this at the CQ-level eventually. However I do agree that a less involved alpha phase will provide us with the use-cases and understanding to design that API properly, including the decision on whether we surface this at the CQ or LQ level.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-12T16:15:50Z

- **keps/10765-workload-priority-class-defaulting/README.md:130** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:15:50Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
```

  The updated description looks great, thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-12T16:17:55Z

- **keps/10765-workload-priority-class-defaulting/README.md:181** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:17:54Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
```

  That sounds good, let's see a more comprehensive or complementary way in the next phase (beta).

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-12T16:26:24Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:26:24Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  I think that the core problem is that users typically have permissions to specify Pod PriorityClass.
  So, I'm wondering if we should stamp `kueue.x-k8s.io/priority-class: default` only when Jobs don't have Pod PriorityClass.
  
  But, I can understand that admins sometimes hope to enforce the default "WPC" at all.
  So, can you add a new alternative section to describe the enforcement mode which has "strong" enforcement (your demand) and soft enforcement (only when the Job doesn't have Pod PriorityClass).
  
  I don't request to describe a concrete design for the mode at this time, but I want to leave consideration to this KEP even at this time.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-12T16:29:06Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:29:06Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  That sounds good to me, can you add explanations for that to this KEP.
  I think we can expand `Notes/Constraints/Caveats` section.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-12T16:30:31Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-12T16:30:31Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  +1 on @mimowo 
  We can revisit here at another time.

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-12T16:50:14Z

- **keps/10765-workload-priority-class-defaulting/README.md:227** by [@kannon92](https://github.com/kannon92) — 2026-05-12T16:50:14Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
+
+This follows the same pattern as `LocalQueueDefaulting`, which sets
+`kueue.x-k8s.io/queue-name: default` via `ApplyDefaultLocalQueue` in the
+webhook. The new `ApplyDefaultWorkloadPriorityClass` function is called from all
+job-type webhooks (both `BaseWebhook` and custom webhooks) alongside
+`ApplyDefaultLocalQueue`.
+
+The label is set on the job at admission time, so the existing priority
+resolution chain in `ExtractPriority()` picks it up naturally during workload
+construction — no changes to the reconciler are needed.
+
+The defaulting is skipped when:
+- The feature gate is disabled.
+- The job already has a `kueue.x-k8s.io/priority-class` label.
+- The job's owner is already managed by Kueue (to avoid double-defaulting).
+- No `WorkloadPriorityClass` named `default` exists.
+
+When the `default` WorkloadPriorityClass does not exist, the webhook makes no
+changes to the job and the existing priority resolution chain applies: the
+workload priority falls through to the Kubernetes PodSpec `priorityClassName`,
+then to the Kubernetes `globalDefault` PriorityClass, and finally to the
+hardcoded value `0`. This is the same behavior as when the feature gate is
+disabled.
+
+### Test Plan
+
+[x] I/we understand the owners of the involved components may require updates to
+existing tests to make this code solid enough prior to committing the changes
+necessary to implement this enhancement.
+
+#### Unit Tests
+
+- `pkg/util/priority/priority_test.go` — `DefaultWorkloadPriorityClassExist`:
+  - `default` WorkloadPriorityClass exists: returns `true`.
+  - `default` WorkloadPriorityClass does not exist (different name): returns
+    `false`.
+  - No WorkloadPriorityClasses exist: returns `false`.
+
+- `pkg/controller/jobframework/defaults_test.go` —
+  `ApplyDefaultWorkloadPriorityClass`:
+  - Feature gate enabled, no label, `default` WPC exists: label
+    `kueue.x-k8s.io/priority-class: default` is set on the job.
+  - Feature gate disabled: label is not set.
+  - Label already set: existing label is preserved unchanged.
+  - `default` WPC does not exist: label is not set.
+  - Job's owner is managed by Kueue: label is not set.
+
+#### Integration Tests
+
+- Webhook/controller integration tests verifying that workloads created from
+  Jobs without a `kueue.x-k8s.io/priority-class` label receive the `default`
+  WPC priority when the feature gate is enabled.
+
+### Graduation Criteria
+
+**Alpha (v0.18):**
+- Feature gate `WorkloadPriorityClassDefaulting` disabled by default.
+- Unit and integration tests.
+
+**Beta:**
+- Feature gate enabled by default.
+- Address feedback from Alpha usage.
+- Re-evaluate whether defaulting should move from the Job webhook to the
+  Workload level based on user feedback and integration complexity.
+
+**GA:**
+- Feature gate locked to true.
+
+## Alternatives
```

  Yea, I'm happy with this for alpha or even GA. Just wanted to add an alternative.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-13T22:38:33Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-05-13T22:38:33Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  > I think that the core problem is that users typically have permissions to specify Pod PriorityClass.
  So, I'm wondering if we should stamp kueue.x-k8s.io/priority-class: default only when Jobs don't have Pod PriorityClass.
  
  Added a new "Soft enforcement when a Pod PriorityClass is set" section under Alternatives that describes both enforcement modes and the rationale for starting with strong enforcement: in clusters with a mix of batch and non-batch workloads, Pod PriorityClass is typically used cluster-wide for quota and scheduling priority, and batch Jobs often still set one to participate in that system — those are exactly the Jobs where decoupling Kueue queueing priority from scheduler priority matters most, so skipping defaulting for them would undermine the feature.
  
  I think this is totally something we should revisit based on user feedback as part of the Beta graduation criteria.

### Review by [@atosatto](https://github.com/atosatto) (commented) — 2026-05-13T22:57:50Z

- **keps/10765-workload-priority-class-defaulting/README.md:151** by [@atosatto](https://github.com/atosatto) — 2026-05-13T22:57:49Z
```diff
@@ -0,0 +1,222 @@
+# KEP-10765: WorkloadPriorityClass defaulting
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories](#user-stories)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Defaulting mechanism](#defaulting-mechanism)
+  - [Test Plan](#test-plan)
+    - [Unit Tests](#unit-tests)
+    - [Integration Tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Alternatives](#alternatives)
+  - [globalDefault field on WorkloadPriorityClass](#globaldefault-field-on-workloadpriorityclass)
+  - [Defaulting in the Workload rather than in the Job webhook](#defaulting-in-the-workload-rather-than-in-the-job-webhook)
+<!-- /toc -->
+
+## Summary
+
+This KEP introduces a `WorkloadPriorityClassDefaulting` feature gate. When
+enabled, workloads that do not specify a `WorkloadPriorityClass` via the
+`kueue.x-k8s.io/priority-class` label automatically receive the priority value
+from a `WorkloadPriorityClass` named `default`, if one exists.
+
+## Motivation
+
+In clusters where only a subset of workloads are managed by Kueue, admins need a
+way to assign a default `WorkloadPriorityClass` to Kueue-managed workloads
+without requiring every job author to set the `kueue.x-k8s.io/priority-class`
+label.
+
+Today, if a Kueue-managed job omits that label, `ExtractPriority()` falls
+through to the Kubernetes `scheduling.k8s.io/v1` PriorityClass chain. This
+conflates pod scheduling priority with workload queueing priority, which are
+intentionally separate concerns in clusters that use `WorkloadPriorityClass`.
+
+The only workarounds are requiring every job author to set the label (fragile and
+hard to enforce across teams) or deploying a custom mutating webhook (operational
+overhead for injecting a default that Kueue could handle natively).
+
+### Goals
+
+- When the `WorkloadPriorityClassDefaulting` feature gate is enabled, workloads
+  without an explicit `WorkloadPriorityClass` label automatically use a
+  `WorkloadPriorityClass` named `default` for their queueing priority, if it
+  exists.
+
+### Non-Goals
+
+- Automatically creating a default `WorkloadPriorityClass`. The administrator
+  must create it manually.
+- Modifying the `WorkloadPriorityClass` API.
+
+## Proposal
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a workload
+does not specify a `WorkloadPriorityClass`, the priority resolution chain looks
+for a `WorkloadPriorityClass` named `default` before falling through to the
+Kubernetes PriorityClass chain. This follows the same convention-based pattern as
+`LocalQueueDefaulting`, which looks for a LocalQueue named `default`.
+
+The `default` WorkloadPriorityClass itself must be created manually by the
+administrator.
+
+### User Stories
+
+#### Story 1
+
+An organization runs a shared cluster where only batch workloads are managed by
+Kueue. The admin creates a `WorkloadPriorityClass` named `default` with value
+`1000`. Users submit batch Jobs without needing to know about
+`WorkloadPriorityClass` — their workloads automatically receive priority `1000`
+for queueing and preemption, keeping them in the `WorkloadPriorityClass` priority
+domain.
+
+#### Story 2
+
+An admin wants to ensure that all Kueue-managed workloads have a baseline
+queueing priority that is independent of their pod scheduling priority. By
+creating a `default` WorkloadPriorityClass with a low value, new workloads that
+forget to set a priority class get a well-defined baseline rather than inheriting
+an unrelated value from the Kubernetes PriorityClass chain.
+
+### Notes/Constraints/Caveats (Optional)
+
+The name `default` is a convention, consistent with how `LocalQueueDefaulting`
+works. Administrators who want a descriptive name can create a
+`WorkloadPriorityClass` with their preferred name and instruct users to reference
+it via label; this feature only covers the case where no label is set at all.
+
+### Risks and Mitigations
+
+Enabling this feature only changes semantics in environments that already
+contain a `WorkloadPriorityClass` named `default`: workloads submitted without a
+`kueue.x-k8s.io/priority-class` label, which previously fell through to the
+Kubernetes PriorityClass chain, will instead inherit the priority of the
+`default` WorkloadPriorityClass. Environments without a `default`
+WorkloadPriorityClass are not affected.
+
+To mitigate the risk for affected environments:
+
+- The `WorkloadPriorityClassDefaulting` feature gate allows admins to disable
+  the mechanism while in Beta in case their environments are negatively
+  affected.
+- The behavior change will be called out in the release notes so that admins
+  upgrading Kueue can review their existing `WorkloadPriorityClass` resources
+  and decide whether to rename the `default` one or adjust its value before
+  enabling the feature gate.
+
+## Design Details
+
+### Defaulting mechanism
+
+When the `WorkloadPriorityClassDefaulting` feature gate is enabled and a job is
+submitted without a `kueue.x-k8s.io/priority-class` label, the mutating webhook
+checks whether a `WorkloadPriorityClass` named `default` exists. If it does, the
+webhook sets the label `kueue.x-k8s.io/priority-class: default` on the job
+object.
```

  Done. I have expanded Notes/Constraints/Caveats with two new subsections:
  
  - Interaction with `managedJobsNamespaceSelector` — capturing the fact that the webhook sets the `kueue.x-k8s.io/priority-class: default` label on every Job that passes through it (including Jobs in excluded namespaces), mirroring `LocalQueueDefaulting` which runs at the same point in `BaseWebhook.Default` (before `ApplyDefaultForSuspend`).
  - Compatibility with `manageJobsWithoutQueueName` — clarifying that defaulting does not depend on the `kueue.x-k8s.io/queue-name` label, so Jobs admitted under `manageJobsWithoutQueueName=true` receive the default WPC similarly to any other Kueue-managed Job.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-15T08:30:53Z

@atosatto Thank you for finalizing this KEP! This looks really great!
/lgtm
/approve
