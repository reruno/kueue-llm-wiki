# PR #1081: KEP 973 workload-Priority

**Summary**: KEP 973 workload-Priority

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1081

**Last updated**: 2023-10-03T07:58:01Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Gekko0114](https://github.com/Gekko0114)
- **Branch**: `priority` → `main`
- **Created**: 2023-08-27T14:34:48Z
- **Updated**: 2023-10-03T07:58:01Z
- **Merged**: 2023-09-24T02:20:56Z
- **Closed**: 2023-09-24T02:20:56Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 65   **+1905 / -80**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:
Add support for workload Priority

#### Which issue(s) this PR fixes:
Fixes #973 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Add support for workload Priority
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-08-27T14:34:55Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2c000f09a7cec69ebe19bb4ec1e8e6bbb0e1abeb |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/650e94d865f4b300080f614e |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-08-27T14:34:56Z

Hi @Gekko0114. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-29T17:25:01Z

Is this ready for review or still a Draft?

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-02T15:12:02Z

Hi @alculquicondor, @tenzen-y 

Could you review this PR ?
Though I will add other tests, the basic implementation has done.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:39:07Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T10:52:25Z

Sorry for the late. I will review this PR today.

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-07T10:55:52Z

> Sorry for the late. I will review this PR today.
> 
> 

Np, thank you so much!

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-10T06:43:12Z

Hi @alculquicondor @tenzen-y,

Thank you so much for your review comments.
I have made the requested changes. Could you review it again?

Additionally, I haven't written tests for preemption and the queue. Should I write tests for them as well?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:29:49Z

/approve-cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:30:36Z

/approve cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-13T05:30:12Z

> Hi @alculquicondor @tenzen-y,
> 
> Thank you so much for your review comments. I have made the requested changes. Could you review it again?
> 
> Additionally, I haven't written tests for preemption and the queue. Should I write tests for them as well?

I'm ok with not adding teets for the scheduler and preemptor since they depend only on priority values and priorityClassSources don't directly affect them.

WDYT? @alculquicondor

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-13T08:32:29Z

> > Hi @alculquicondor @tenzen-y,
> > Thank you so much for your review comments. I have made the requested changes. Could you review it again?
> > Additionally, I haven't written tests for preemption and the queue. Should I write tests for them as well?
> 
> I'm ok with not adding teets for the scheduler and preemptor since they depend only on priority values and priorityClassSources don't directly affect them.
> 
> WDYT? @alculquicondor

However, expanding integration tests for the job with the scheduler might be worth it.

https://github.com/kubernetes-sigs/kueue/blob/fd90aad36c52300493b1c980611946c134f13cde/test/integration/controller/jobs/job/job_controller_test.go#L706

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-17T06:49:04Z

Hi @alculquicondor , @tenzen-y ,

I've fixed all comments and added e2e and integration test cases. Could you review it again?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T23:09:42Z

Also, it would be better to squash all commits into one to avoid a huge commit message.

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-23T07:36:57Z

Hi @tenzen-y , @alculquicondor 

Thank you so much for your detailed review! I've learned a lot from this PR.
I've fixed all comments, could you review it again?

> Could you open a PR to add documentation for WorkloadPriorityClass?

Sure, do you mean I should add docs under [here](https://github.com/kubernetes-sigs/kueue/tree/main/site) ?

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-24T02:15:13Z

> > Could you open a PR to add documentation for WorkloadPriorityClass?
> 
> Sure, do you mean I should add docs under [here](https://github.com/kubernetes-sigs/kueue/tree/main/site) ?

Yes.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-24T02:18:49Z

LGTM label has been added.  <details>Git tree hash: 92c89dd9222d35070d9f6595565cac6ae742b070</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-24T02:18:52Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1081#" title="Author self-approved">Gekko0114</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1081#pullrequestreview-1640966065" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-08-27T14:35:51Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:17** by [@Gekko0114](https://github.com/Gekko0114) — 2023-08-27T14:35:51Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
```

  Question: v1alpha1 should be used?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-08-29T17:24:52Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:17** by [@alculquicondor](https://github.com/alculquicondor) — 2023-08-29T17:24:52Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
```

  beta should be ok.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-08-29T17:48:59Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:17** by [@tenzen-y](https://github.com/tenzen-y) — 2023-08-29T17:48:59Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
```

  I'm also ok with beta api.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-02T05:41:03Z

- **pkg/util/priority/priority.go:40** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-02T05:41:03Z
```diff
@@ -36,32 +36,58 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
```

  add ut

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-05T18:19:00Z

- **apis/kueue/v1beta1/workload_types.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:42:35Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
```

  ```suggestion
  	// Workload's PriorityClass can accept the name of a k8s priorityClass or a workloadPriorityClass.
  ```

- **apis/kueue/v1beta1/workload_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:44:09Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  Prefer to start the comment with the name of the field.
  
  Something like:
  
  ```
  priorityClassSource determines whether the priorityClass field refers to a kubernetes PriorityClass or
  kueue.x-k8s.io/workloadpriorityclass
  ```

- **apis/kueue/v1beta1/workload_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:45:26Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  Also, use `+kubebuilder:validation:Enum` to restrict the possible values and `+kubebuilder:default`

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:49:11Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster,shortName={wpc}
+//+kubebuilder:printcolumn:name="Value",JSONPath=".value",type=integer,description="Value of workloadPriorityClass's Priority"
+
+// Workload is the Schema for the workloads API
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value       int32  `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
```

  Add comments to explain how to use these fields.
  
  Also document that changing the value doesn't affect the jobs that were already created.

- **pkg/controller/core/workload_controller.go:362** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:53:19Z
```diff
@@ -357,6 +359,21 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// Update the priority of workload if source or priorityclass is modified. PriorityClassName and PriorityClassSource should be mutable.
```

  While we said that the priority value should be mutable, we didn't say anything about the class.
  
  For simplicity, I think this should be left immutable.

- **pkg/controller/jobframework/interface.go:92** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:55:29Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
+	object := job.Object()
+	if workloadPriorityClassLabel := object.GetLabels()[constants.WorkloadPriorityClassLabel]; workloadPriorityClassLabel != "" {
```

  add webhook validation that this annotation doesn't change.

- **pkg/controller/jobframework/interface.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T17:59:14Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
```

  this function is only used once in `extractPriority`. I would just put it there.

- **pkg/util/priority/priority.go:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:01:28Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
+	if source == constants.WorkloadPriorityClassSource {
+		_, priority, _, err := GetPriorityFromWorkloadPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	// priority belongs k8s priorityClass
+	if source == constants.K8sPriorityClassSource {
+		_, priority, _, err := GetPriorityFromPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	err := fmt.Errorf("failed to find priorityClass: %s, source: %s", priorityClassName, source)
+	return 0, err
+}
+
 // GetPriorityFromPriorityClass returns the priority populated from
 // priority class. If not specified, priority will be default or
 // zero if there is no default.
 func GetPriorityFromPriorityClass(ctx context.Context, client client.Client,
-	priorityClass string) (string, int32, error) {
+	priorityClass string) (string, int32, string, error) {
 	if len(priorityClass) == 0 {
 		return getDefaultPriority(ctx, client)
 	}
 
 	pc := &schedulingv1.PriorityClass{}
 	if err := client.Get(ctx, types.NamespacedName{Name: priorityClass}, pc); err != nil {
-		return "", 0, err
+		return "", 0, "", err
 	}
 
-	return pc.Name, pc.Value, nil
+	return pc.Name, pc.Value, constants.K8sPriorityClassSource, nil
+}
+
+// GetPriorityFromWorkloadPriorityClass returns the priority populated from
+// workload priority class. If not specified, returns 0.
+// DefaultPriority is not called within this function
+// because k8s priority class should be  checked next.
+func GetPriorityFromWorkloadPriorityClass(ctx context.Context, client client.Client,
```

  why should this return the source? It's always going to be WorkloadPriorityClassSource

- **pkg/controller/jobs/job/job_controller.go:285** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:03:36Z
```diff
@@ -275,6 +275,15 @@ func (j *Job) Finished() (metav1.Condition, bool) {
 	return condition, finished
 }
 
+// PriorityClass calculates the priorityClass name needed for workload according to the following priorities:
+//  1. .Spec.Template.Spec.PriorityClassName
+func (j *Job) PriorityClass() string {
+	if len(j.Spec.Template.Spec.PriorityClassName) != 0 {
+		return j.Spec.Template.Spec.PriorityClassName
+	}
+	return ""
+}
```

  Why add this? It works without it.

- **pkg/controller/jobs/job/job_controller_test.go:370** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:07:40Z
```diff
@@ -349,13 +351,21 @@ func TestReconciler(t *testing.T) {
 		Request(corev1.ResourceCPU, "1").
 		Image("", nil)
 
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		focus             bool
 		reconcilerOptions []jobframework.Option
 		job               batchv1.Job
 		wantJob           batchv1.Job
 		workloads         []kueue.Workload
 		wantWorkloads     []kueue.Workload
+		wpc               kueue.WorkloadPriorityClass
+		pc                schedulingv1.PriorityClass
```

  maybe they can be in a single place `extraObjs []client.Object`
  
  Also, try to put all the inputs separate from the outputs (`wantWorkloads`, `wantErr`)

- **pkg/controller/jobs/jobset/jobset_controller_test.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:08:53Z
  No need to duplicate the same test for every job, unless they have special logic.

- **pkg/util/priority/priority.go:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:10:12Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
```

  Why add this function? I only see it being used in tests.

- **pkg/util/priority/priority.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:10:27Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
+	if source == constants.WorkloadPriorityClassSource {
+		_, priority, _, err := GetPriorityFromWorkloadPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	// priority belongs k8s priorityClass
+	if source == constants.K8sPriorityClassSource {
+		_, priority, _, err := GetPriorityFromPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	err := fmt.Errorf("failed to find priorityClass: %s, source: %s", priorityClassName, source)
+	return 0, err
+}
+
 // GetPriorityFromPriorityClass returns the priority populated from
 // priority class. If not specified, priority will be default or
 // zero if there is no default.
 func GetPriorityFromPriorityClass(ctx context.Context, client client.Client,
```

  Revert this

- **pkg/util/priority/priority_test.go:220** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:16:28Z
```diff
@@ -157,6 +162,203 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 				t.Errorf("unexpected name: got: %s, expected: %s", name, tt.wantPriorityClassName)
 			}
 
+			if source != tt.wantSource {
+				t.Errorf("unexpected source: got: %s, expected: %s", source, tt.wantSource)
+			}
+
+			if value != tt.wantPriorityClassValue {
+				t.Errorf("unexpected value: got: %d, expected: %d", value, tt.wantPriorityClassValue)
+			}
+		})
+	}
+}
+
+func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+
+	tests := map[string]struct {
+		workloadPriorityClassList      *kueue.WorkloadPriorityClassList
+		workloadPriorityClassName      string
+		wantWorkloadPriorityClassName  string
+		wantWorkloadPriorityClassValue int32
+		wantSource                     string
+		wantErr                        string
+	}{
+		"workloadPriorityClass is specified and it exists": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{
+					{
+						ObjectMeta: v1.ObjectMeta{Name: "test"},
+						Value:      50,
+					},
+				},
+			},
+			workloadPriorityClassName:      "test",
+			wantWorkloadPriorityClassName:  "test",
+			wantSource:                     constants.WorkloadPriorityClassSource,
+			wantWorkloadPriorityClassValue: 50,
+		},
+		"workloadPriorityClass is specified and it does not exist": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{},
+			},
+			workloadPriorityClassName: "test",
+			wantSource:                "",
+			wantErr:                   `workloadpriorityclasses.kueue.x-k8s.io "test" not found`,
+		},
+	}
+
+	for desc, tt := range tests {
+		tt := tt
+		t.Run(desc, func(t *testing.T) {
+			t.Parallel()
+
+			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.workloadPriorityClassList)
+			client := builder.Build()
+
+			name, value, source, err := GetPriorityFromWorkloadPriorityClass(context.Background(), client, tt.workloadPriorityClassName)
+			if tt.wantErr != "" {
+				if err == nil {
+					t.Fatalf("expected an error")
+				}
+
+				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
+					t.Errorf("unexpected error (-want,+got):\n%s", diff)
+				}
+				return
+			}
```

  try to get this working with a single `cmp.Diff(tt.wantErr, err)`
  
  you can use:
  
  ```golang
  wantErr: apierrors.NewNotFound(..., ...)
  ```
  
  https://github.com/kubernetes/apimachinery/blob/0d057e543013c79e20abb000df19bc16d286b777/pkg/api/errors/errors.go#L144

- **test/integration/controller/core/workload_controller_test.go:219** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:17:56Z
```diff
@@ -209,4 +215,158 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("the priorityClass and priorityClassSource are mutable", func() {
```

  just the priority

- **test/integration/controller/core/workload_controller_test.go:336** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-05T18:18:58Z
```diff
@@ -209,4 +215,158 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("the priorityClass and priorityClassSource are mutable", func() {
+		ginkgo.BeforeEach(func() {
+			highPriorityClass = testing.MakePriorityClass("high-priority-class").PriorityValue(100).Obj()
+			lowPriorityClass = testing.MakePriorityClass("low-priority-class").PriorityValue(10).Obj()
+			highWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("high-priority-class").WorkloadPriorityValue(200).Obj()
+			lowWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("low-priority-class").WorkloadPriorityValue(20).Obj()
+			gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("the workloadpriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(20)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from k8sPriorityClass to workloadPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.WorkloadPriorityClassSource
+			updatedPriority := int32(200)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from workloadPriorityClass to k8sPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedPriority := int32(100)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource and priorityClass name are mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+	})
+
+	// We ignore priorityClass because currently priorityClass is immutable.
```

  what did you mean here?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-06T06:49:40Z

- **pkg/controller/core/workload_controller.go:362** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-06T06:49:40Z
```diff
@@ -357,6 +359,21 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// Update the priority of workload if source or priorityclass is modified. PriorityClassName and PriorityClassSource should be mutable.
```

  It was documented in [KEP](https://github.com/kubernetes-sigs/kueue/tree/main/keps/973-workload-priority#workloads-priority-values-are-always-mutable) that classname and source are mutable as well.
  However, I agree with your suggestion to make classname and source immutable for simplicity.
  If many users want to make these immutable, we can implement it in the future.
  CC: @tenzen-y

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-06T06:57:56Z

- **pkg/util/priority/priority.go:60** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-06T06:57:55Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
+	if source == constants.WorkloadPriorityClassSource {
+		_, priority, _, err := GetPriorityFromWorkloadPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	// priority belongs k8s priorityClass
+	if source == constants.K8sPriorityClassSource {
+		_, priority, _, err := GetPriorityFromPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	err := fmt.Errorf("failed to find priorityClass: %s, source: %s", priorityClassName, source)
+	return 0, err
+}
+
 // GetPriorityFromPriorityClass returns the priority populated from
 // priority class. If not specified, priority will be default or
 // zero if there is no default.
 func GetPriorityFromPriorityClass(ctx context.Context, client client.Client,
-	priorityClass string) (string, int32, error) {
+	priorityClass string) (string, int32, string, error) {
 	if len(priorityClass) == 0 {
 		return getDefaultPriority(ctx, client)
 	}
 
 	pc := &schedulingv1.PriorityClass{}
 	if err := client.Get(ctx, types.NamespacedName{Name: priorityClass}, pc); err != nil {
-		return "", 0, err
+		return "", 0, "", err
 	}
 
-	return pc.Name, pc.Value, nil
+	return pc.Name, pc.Value, constants.K8sPriorityClassSource, nil
+}
+
+// GetPriorityFromWorkloadPriorityClass returns the priority populated from
+// workload priority class. If not specified, returns 0.
+// DefaultPriority is not called within this function
+// because k8s priority class should be  checked next.
+func GetPriorityFromWorkloadPriorityClass(ctx context.Context, client client.Client,
```

  Because I thought the case source is ""  when the priorityClass doesn't exist.
  But I agree with your opinion.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-06T07:01:19Z

- **pkg/util/priority/priority.go:41** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-06T07:01:19Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
```

  I am using this function in workload_controller.go.
  However, classname and source won't be mutable, so I will remove this function.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-06T07:22:06Z

- **pkg/controller/jobframework/interface.go:92** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-06T07:22:05Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
+	object := job.Object()
+	if workloadPriorityClassLabel := object.GetLabels()[constants.WorkloadPriorityClassLabel]; workloadPriorityClassLabel != "" {
```

  @alculquicondor 
  Just to clarify,
  are you suggesting making the 'source' name in the workload CRD immutable by workload webhook? or are you proposing making this field immutable for each Job's webhook?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-06T07:38:41Z

- **test/integration/controller/core/workload_controller_test.go:336** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-06T07:38:41Z
```diff
@@ -209,4 +215,158 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("the priorityClass and priorityClassSource are mutable", func() {
+		ginkgo.BeforeEach(func() {
+			highPriorityClass = testing.MakePriorityClass("high-priority-class").PriorityValue(100).Obj()
+			lowPriorityClass = testing.MakePriorityClass("low-priority-class").PriorityValue(10).Obj()
+			highWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("high-priority-class").WorkloadPriorityValue(200).Obj()
+			lowWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("low-priority-class").WorkloadPriorityValue(20).Obj()
+			gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("the workloadpriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(20)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from k8sPriorityClass to workloadPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.WorkloadPriorityClassSource
+			updatedPriority := int32(200)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from workloadPriorityClass to k8sPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedPriority := int32(100)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource and priorityClass name are mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+	})
+
+	// We ignore priorityClass because currently priorityClass is immutable.
```

  I mean that we don't need to test priorityClass cases here because priority value of priorityClass is currently immutable.
  I will remove this test case.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-06T15:04:33Z

- **pkg/controller/core/workload_controller.go:362** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-06T15:04:33Z
```diff
@@ -357,6 +359,21 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// Update the priority of workload if source or priorityclass is modified. PriorityClassName and PriorityClassSource should be mutable.
```

  oh
  
  > Also there is an [open KEP](https://github.com/kubernetes/enhancements/pull/4129) to make PriorityClass mutable in k8s. This workload's design aligns with the direction of k8s PriorityClass.
  
  This refers to the PriorityClass object, not Pod.spec.priorityClass

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-06T18:51:33Z

- **test/integration/controller/core/workload_controller_test.go:336** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-06T18:51:33Z
```diff
@@ -209,4 +215,158 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("the priorityClass and priorityClassSource are mutable", func() {
+		ginkgo.BeforeEach(func() {
+			highPriorityClass = testing.MakePriorityClass("high-priority-class").PriorityValue(100).Obj()
+			lowPriorityClass = testing.MakePriorityClass("low-priority-class").PriorityValue(10).Obj()
+			highWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("high-priority-class").WorkloadPriorityValue(200).Obj()
+			lowWorkloadPriorityClass = testing.MakeWorkloadPriorityClass("low-priority-class").WorkloadPriorityValue(20).Obj()
+			gomega.Expect(k8sClient.Create(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, highWorkloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, lowWorkloadPriorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("the workloadpriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(20)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClass name is mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from k8sPriorityClass to workloadPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.K8sPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.WorkloadPriorityClassSource
+			updatedPriority := int32(200)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource is mutable (from workloadPriorityClass to k8sPriorityClass)", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedPriority := int32(100)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+		ginkgo.It("the PriorityClassSource and priorityClass name are mutable", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("high-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+
+			updatedQueueWorkload.Spec.PriorityClassSource = constants.K8sPriorityClassSource
+			updatedQueueWorkload.Spec.PriorityClassName = "low-priority-class"
+			updatedPriority := int32(10)
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() *int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(&updatedPriority))
+		})
+	})
+
+	// We ignore priorityClass because currently priorityClass is immutable.
```

  Ah, you mean that there is no test for a changing k8s Priority class, only for a changing WorkloadPriorityClass. Correct?
  
  Yeah, I would not add such test, but let's keep the one for WorkloadPriorityClass.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-06T18:52:54Z

- **pkg/controller/jobframework/interface.go:92** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-06T18:52:54Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
+	object := job.Object()
+	if workloadPriorityClassLabel := object.GetLabels()[constants.WorkloadPriorityClassLabel]; workloadPriorityClassLabel != "" {
```

  We probably need both: immutable annotation in each Job and immutable source in the Workload CRD.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-07T23:09:29Z

I left a comment for the first pass. I will review this PR again once you address @alculquicondor's comments regarding to immutable priorityClass.

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:26** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T22:49:16Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster,shortName={wpc}
```

  ```suggestion
  //+kubebuilder:resource:scope=Cluster
  ```
  
  I don't think this `shortName`  is appropriate since we previously removed cryptic shorName: https://github.com/kubernetes-sigs/kueue/pull/532#discussion_r1099065187

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T22:51:50Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  It seems that client-go library for this object isn't generated. Did you run `make generate` on your local?

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:29** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T22:52:40Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster,shortName={wpc}
+//+kubebuilder:printcolumn:name="Value",JSONPath=".value",type=integer,description="Value of workloadPriorityClass's Priority"
+
+// Workload is the Schema for the workloads API
```

  ```suggestion
  // WorkloadPriorityClass is the Schema for the workloadPriorityClass API
  ```

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:40** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T22:53:22Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster,shortName={wpc}
+//+kubebuilder:printcolumn:name="Value",JSONPath=".value",type=integer,description="Value of workloadPriorityClass's Priority"
+
+// Workload is the Schema for the workloads API
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Value       int32  `json:"value,omitempty"`
+	Description string `json:"description,omitempty"`
+}
+
+//+kubebuilder:object:root=true
+
+// WorkloadPriorityClassList contains a list of ResourceClaim
```

  ```suggestion
  // WorkloadPriorityClassList contains a list of WorkloadPriorityClass
  ```

- **pkg/constants/constants.go:40** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T22:56:59Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
+
+	K8sPriorityClassSource = "scheduling.k8s.io/priorityclass"
```

  ```suggestion
  	PodPriorityClassSource = "scheduling.k8s.io/priorityclass"
  ```
  
  The `K8s Priority` sounds a bit weird to me since the `WorkloadPriorityClass` also indicates the K8s object.
  @alculquicondor  WDYT?

- **pkg/controller/core/workload_controller.go:362** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T23:00:14Z
```diff
@@ -357,6 +359,21 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// Update the priority of workload if source or priorityclass is modified. PriorityClassName and PriorityClassSource should be mutable.
```

  I agree with @alculquicondor 
  
  @Gekko0114 Could you update the KEP?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-07T23:12:21Z

- **pkg/controller/jobs/job/job_controller.go:285** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-07T23:12:21Z
```diff
@@ -275,6 +275,15 @@ func (j *Job) Finished() (metav1.Condition, bool) {
 	return condition, finished
 }
 
+// PriorityClass calculates the priorityClass name needed for workload according to the following priorities:
+//  1. .Spec.Template.Spec.PriorityClassName
+func (j *Job) PriorityClass() string {
+	if len(j.Spec.Template.Spec.PriorityClassName) != 0 {
+		return j.Spec.Template.Spec.PriorityClassName
+	}
+	return ""
+}
```

  @Gekko0114 The `JobWithPriorityClass` interface is optional. So, all jobs don't need to implement the interface.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-08T15:04:22Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-08T15:04:22Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  I ran `make generate` and got the log `Generating clientset for kueue:v1beta1 at sigs.k8s.io/kueue/client-go/clientset`, but isn't generated..
  I'll investigate on it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-08T15:13:14Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-08T15:13:14Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  When I remove client-go dir and run `make generate`, client-go dir will not generated. My env might have trouble.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-08T15:37:29Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-08T15:37:28Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  I found the root cause.
  I have cloned this repository to a location outside of gopath, but files were generated in gopath.
  It might be useful if it works outside of gopath.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T15:43:15Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T15:43:15Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  We previously supported outside of GOPATH 🤔 
  
  https://github.com/kubernetes-sigs/kueue/pull/882

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-08T15:44:01Z

- **pkg/controller/jobframework/reconciler.go:584** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-08T15:44:01Z
```diff
@@ -560,27 +560,37 @@ func (r *JobReconciler) constructWorkload(ctx context.Context, job GenericJob, o
 		)
 	}
 
-	priorityClassName, p, err := r.extractPriority(ctx, podSets, job)
+	priorityClassName, p, source, err := r.extractPriority(ctx, podSets, job)
 	if err != nil {
 		return nil, err
 	}
 
 	wl.Spec.PriorityClassName = priorityClassName
 	wl.Spec.Priority = &p
+	wl.Spec.PriorityClassSource = source
 
 	if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 		return nil, err
 	}
 	return wl, nil
 }
 
-func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, error) {
+func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, string, error) {
+	if workloadPriorityClass, isWorkloadPriorityClass := WorkloadPriorityClassName(job); isWorkloadPriorityClass {
+		priorityClassName, p, err := utilpriority.GetPriorityFromWorkloadPriorityClass(ctx, r.client, workloadPriorityClass)
+		source := constants.WorkloadPriorityClassSource
+		return priorityClassName, p, source, err
+	}
+	source := constants.K8sPriorityClassSource
```

  @alculquicondor , @tenzen-y 
  If priorityClass is not workloadPriorityClass, can we label it as `k8sPriorityClassSource`(aka` podPriorityClassSource`) ?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-08T15:47:46Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:23** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-08T15:47:46Z
```diff
@@ -0,0 +1,49 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
```

  Umm... I will create a patch if possible

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-08T16:29:05Z

- **pkg/controller/jobframework/reconciler.go:584** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-08T16:29:05Z
```diff
@@ -560,27 +560,37 @@ func (r *JobReconciler) constructWorkload(ctx context.Context, job GenericJob, o
 		)
 	}
 
-	priorityClassName, p, err := r.extractPriority(ctx, podSets, job)
+	priorityClassName, p, source, err := r.extractPriority(ctx, podSets, job)
 	if err != nil {
 		return nil, err
 	}
 
 	wl.Spec.PriorityClassName = priorityClassName
 	wl.Spec.Priority = &p
+	wl.Spec.PriorityClassSource = source
 
 	if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 		return nil, err
 	}
 	return wl, nil
 }
 
-func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, error) {
+func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, string, error) {
+	if workloadPriorityClass, isWorkloadPriorityClass := WorkloadPriorityClassName(job); isWorkloadPriorityClass {
+		priorityClassName, p, err := utilpriority.GetPriorityFromWorkloadPriorityClass(ctx, r.client, workloadPriorityClass)
+		source := constants.WorkloadPriorityClassSource
+		return priorityClassName, p, source, err
+	}
+	source := constants.K8sPriorityClassSource
```

  Do you mean that kueue will set `kueue.x-k8s.io/workloadpriorityclass` to `.spec.priorityClassSource` in the Workload if jobs don't have `kueue.x-k8s.io/priority-class` label?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T11:35:22Z

- **apis/kueue/v1beta1/workload_types.go:57** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T11:35:22Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  @alculquicondor 
  Which value should be default? `k8s priorityClass`, `workloadPriorityClass` or ""?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T11:50:03Z

- **pkg/controller/jobframework/reconciler.go:584** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T11:50:03Z
```diff
@@ -560,27 +560,37 @@ func (r *JobReconciler) constructWorkload(ctx context.Context, job GenericJob, o
 		)
 	}
 
-	priorityClassName, p, err := r.extractPriority(ctx, podSets, job)
+	priorityClassName, p, source, err := r.extractPriority(ctx, podSets, job)
 	if err != nil {
 		return nil, err
 	}
 
 	wl.Spec.PriorityClassName = priorityClassName
 	wl.Spec.Priority = &p
+	wl.Spec.PriorityClassSource = source
 
 	if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 		return nil, err
 	}
 	return wl, nil
 }
 
-func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, error) {
+func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, string, error) {
+	if workloadPriorityClass, isWorkloadPriorityClass := WorkloadPriorityClassName(job); isWorkloadPriorityClass {
+		priorityClassName, p, err := utilpriority.GetPriorityFromWorkloadPriorityClass(ctx, r.client, workloadPriorityClass)
+		source := constants.WorkloadPriorityClassSource
+		return priorityClassName, p, source, err
+	}
+	source := constants.K8sPriorityClassSource
```

  My question is about what should be set in the `PrirorityClassSource` field of `workload` if a job does not have a `priorityClassName`.
  Within the `GetPriorityFromPriorityClass` function, if there is no default `priorityClass`, it returns a priority of 0. In this case, I am considering what value should be assigned to the `source` field of `workload`. Can I set `scheduling.k8s.io/priorityclass` in this case?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T12:55:13Z

- **pkg/controller/core/workload_controller.go:362** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T12:55:13Z
```diff
@@ -357,6 +359,21 @@ func (r *WorkloadReconciler) Update(e event.UpdateEvent) bool {
 	}
 	log.V(2).Info("Workload update event")
 
+	// Update the priority of workload if source or priorityclass is modified. PriorityClassName and PriorityClassSource should be mutable.
```

  Thanks for your comments! Also updated the KEP

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T12:56:06Z

- **pkg/controller/jobs/job/job_controller.go:285** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T12:56:06Z
```diff
@@ -275,6 +275,15 @@ func (j *Job) Finished() (metav1.Condition, bool) {
 	return condition, finished
 }
 
+// PriorityClass calculates the priorityClass name needed for workload according to the following priorities:
+//  1. .Spec.Template.Spec.PriorityClassName
+func (j *Job) PriorityClass() string {
+	if len(j.Spec.Template.Spec.PriorityClassName) != 0 {
+		return j.Spec.Template.Spec.PriorityClassName
+	}
+	return ""
+}
```

  I misunderstood. I've removed this function. Thank you.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T13:11:38Z

- **pkg/controller/jobs/jobset/jobset_controller_test.go:1** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T13:11:38Z
  Sure.
  Regarding jobset and mpijob, since there was already a TestReconciler, I added a few test cases within TestReconciler function. I added test cases because functions like PriorityClass differ between Job types. 
  
  If they seem unnecessary, please let me know, and I will remove them.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-09T14:03:18Z

- **apis/kueue/v1beta1/workload_types.go:57** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-09T14:03:18Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  I set `workloadPriorityClass` as default. If you have any concerns, please let me know

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-10T06:14:46Z

- **pkg/controller/jobframework/interface.go:92** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-10T06:14:46Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
+	object := job.Object()
+	if workloadPriorityClassLabel := object.GetLabels()[constants.WorkloadPriorityClassLabel]; workloadPriorityClassLabel != "" {
```

  added webhook for jobs and workloads

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-10T06:15:31Z

- **pkg/controller/jobframework/interface.go:90** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-10T06:15:31Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
```

  I keep it here because it is used within webhook as well

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-11T13:20:21Z

- **apis/kueue/v1beta1/workload_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-11T13:20:21Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  I think it should be empty, given that there might not be any priority class.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-11T17:52:48Z

- **pkg/controller/jobframework/reconciler.go:584** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-11T17:52:48Z
```diff
@@ -560,27 +560,37 @@ func (r *JobReconciler) constructWorkload(ctx context.Context, job GenericJob, o
 		)
 	}
 
-	priorityClassName, p, err := r.extractPriority(ctx, podSets, job)
+	priorityClassName, p, source, err := r.extractPriority(ctx, podSets, job)
 	if err != nil {
 		return nil, err
 	}
 
 	wl.Spec.PriorityClassName = priorityClassName
 	wl.Spec.Priority = &p
+	wl.Spec.PriorityClassSource = source
 
 	if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 		return nil, err
 	}
 	return wl, nil
 }
 
-func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, error) {
+func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, string, error) {
+	if workloadPriorityClass, isWorkloadPriorityClass := WorkloadPriorityClassName(job); isWorkloadPriorityClass {
+		priorityClassName, p, err := utilpriority.GetPriorityFromWorkloadPriorityClass(ctx, r.client, workloadPriorityClass)
+		source := constants.WorkloadPriorityClassSource
+		return priorityClassName, p, source, err
+	}
+	source := constants.K8sPriorityClassSource
```

  Ah, I see. Thanks for the clarification.
  Setting an empty source there would be better since there are situations not to set priority and we should keep backward compatibility.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-12T14:17:14Z

- **pkg/util/priority/priority.go:60** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-12T14:17:13Z
```diff
@@ -36,32 +37,60 @@ func Priority(w *kueue.Workload) int32 {
 	return ptr.Deref(w.Spec.Priority, constants.DefaultPriority)
 }
 
+// GetPriorityFromPriorityClassAndSource returns the priority from priorityClassName and source.
+func GetPriorityFromPriorityClassAndSource(ctx context.Context, client client.Client, priorityClassName string, source string) (int32, error) {
+	if source == constants.WorkloadPriorityClassSource {
+		_, priority, _, err := GetPriorityFromWorkloadPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	// priority belongs k8s priorityClass
+	if source == constants.K8sPriorityClassSource {
+		_, priority, _, err := GetPriorityFromPriorityClass(ctx, client, priorityClassName)
+		return priority, err
+	}
+	err := fmt.Errorf("failed to find priorityClass: %s, source: %s", priorityClassName, source)
+	return 0, err
+}
+
 // GetPriorityFromPriorityClass returns the priority populated from
 // priority class. If not specified, priority will be default or
 // zero if there is no default.
 func GetPriorityFromPriorityClass(ctx context.Context, client client.Client,
-	priorityClass string) (string, int32, error) {
+	priorityClass string) (string, int32, string, error) {
 	if len(priorityClass) == 0 {
 		return getDefaultPriority(ctx, client)
 	}
 
 	pc := &schedulingv1.PriorityClass{}
 	if err := client.Get(ctx, types.NamespacedName{Name: priorityClass}, pc); err != nil {
-		return "", 0, err
+		return "", 0, "", err
 	}
 
-	return pc.Name, pc.Value, nil
+	return pc.Name, pc.Value, constants.K8sPriorityClassSource, nil
+}
+
+// GetPriorityFromWorkloadPriorityClass returns the priority populated from
+// workload priority class. If not specified, returns 0.
+// DefaultPriority is not called within this function
+// because k8s priority class should be  checked next.
+func GetPriorityFromWorkloadPriorityClass(ctx context.Context, client client.Client,
```

  source is returned in this function.
  
  Within the GetPriorityFromPriorityClass function, if there is no default priorityClass, it returns a priority of 0. In this case, we want to set souce "".

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-12T14:18:11Z

- **pkg/controller/jobframework/reconciler.go:584** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-12T14:18:11Z
```diff
@@ -560,27 +560,37 @@ func (r *JobReconciler) constructWorkload(ctx context.Context, job GenericJob, o
 		)
 	}
 
-	priorityClassName, p, err := r.extractPriority(ctx, podSets, job)
+	priorityClassName, p, source, err := r.extractPriority(ctx, podSets, job)
 	if err != nil {
 		return nil, err
 	}
 
 	wl.Spec.PriorityClassName = priorityClassName
 	wl.Spec.Priority = &p
+	wl.Spec.PriorityClassSource = source
 
 	if err := ctrl.SetControllerReference(object, wl, r.client.Scheme()); err != nil {
 		return nil, err
 	}
 	return wl, nil
 }
 
-func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, error) {
+func (r *JobReconciler) extractPriority(ctx context.Context, podSets []kueue.PodSet, job GenericJob) (string, int32, string, error) {
+	if workloadPriorityClass, isWorkloadPriorityClass := WorkloadPriorityClassName(job); isWorkloadPriorityClass {
+		priorityClassName, p, err := utilpriority.GetPriorityFromWorkloadPriorityClass(ctx, r.client, workloadPriorityClass)
+		source := constants.WorkloadPriorityClassSource
+		return priorityClassName, p, source, err
+	}
+	source := constants.K8sPriorityClassSource
```

  Thanks, fixed it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-12T14:18:27Z

- **pkg/constants/constants.go:40** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-12T14:18:26Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
+
+	K8sPriorityClassSource = "scheduling.k8s.io/priorityclass"
```

  Fixed it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-12T14:19:09Z

- **apis/kueue/v1beta1/workload_types.go:57** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-12T14:19:09Z
```diff
@@ -52,6 +52,11 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept both k8s priorityClass and workloadPriorityClass.
+	// To distinguish, when using workloadPriorityClass, a PriorityClassSource field has the kueue.x-k8s.io/workloadpriorityclass value.
```

  I set "" as a default value

### Review by [@tenzen-y](https://github.com/tenzen-y) (approved) — 2023-09-12T18:29:32Z

I don't yet review integration tests. I will review it afterward.
Also, can you update e2e?

- **apis/kueue/v1beta1/workload_types.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T16:51:00Z
```diff
@@ -52,6 +52,13 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept the name of a k8s priorityClass or a workloadPriorityClass.
```

  ```suggestion
  	// Workload's PriorityClass can accept the name of a pod priorityClass or a workloadPriorityClass.
  ```
  
  Let's also replace `k8s piorityClass` with `pod priorityClass` in other places.

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T16:58:34Z
```diff
@@ -0,0 +1,56 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster
+//+kubebuilder:printcolumn:name="Value",JSONPath=".value",type=integer,description="Value of workloadPriorityClass's Priority"
+
+// WorkloadPriorityClass is the Schema for the workloadPriorityClass API
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	// value represents the integer value of this workloadPriorityClass. This is the actual priority that workloads
+	// receive when jobs have the name of this class in their workloadPriorityClass label.
+	// Changing the value of workloadPriorityClass doesn't affect the priority of workloads that were already created.
+	Value int32 `json:"value,omitempty"`
```

  ```suggestion
  	Value int32 `json:"value"`
  ```
  
  Should we make `value` the required field? I don't think workloadPriorityClasses without value is valuable in creation.

- **keps/973-workload-priority/README.md:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:00:38Z
  Should we update the KEP on another small PR? It helps to track changes.

- **pkg/constants/constants.go:39** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:01:24Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
+
```

  ```suggestion
  ```

- **pkg/controller/jobframework/interface.go:90** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:06:58Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) (string, bool) {
```

  I think it is enough to return only priorityClassName (string). The callers could verify by `if pririotyClassName != ""` whether `workloadPriorityClass` exists or not.

- **pkg/controller/jobframework/validation.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:28:07Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  Let's just use `apivalidation.ValidateImmutableField`.

- **pkg/controller/jobs/job/job_controller_test.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:45:44Z
```diff
@@ -352,11 +353,18 @@ func TestReconciler(t *testing.T) {
 		Request(corev1.ResourceCPU, "1").
 		Image("", nil)
 
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               batchv1.Job
-		wantJob           batchv1.Job
 		workloads         []kueue.Workload
+		extraObjs         []client.Object
```

  ```suggestion
  		podPriorityClass         schedulingv1.PriorityClass
  		workloadPriorityClass kueue.WorkloadPriorityClass	
  ```
  
  I think it would be better to set priorityClasses as a separate field since it will allow us to declare more clear test cases.

- **pkg/controller/jobs/jobset/jobset_controller_test.go:218** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:52:54Z
```diff
@@ -206,9 +207,15 @@ var (
 )
 
 func TestReconciler(t *testing.T) {
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               *jobset.JobSet
+		extraObjs         []client.Object
```

  Ditto.

- **pkg/controller/jobs/jobset/jobset_controller_test.go:280** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:58:07Z
```diff
@@ -254,6 +261,147 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload is created with podsets and workloadPriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
```

  ```suggestion
  				testingjobset.ReplicatedJobRequirements{
  					Name:        "replicated-job-1",
  					Replicas:    1,
  					Completions: 1,
  					Parallelism: 1,
  				},
  ```
  
  I think that we don't need multiple replicatedJobs to verify the behavior of priorityClasses.

- **pkg/controller/jobs/jobset/jobset_controller_test.go:327** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:58:23Z
```diff
@@ -254,6 +261,147 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload is created with podsets and workloadPriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).WorkloadPriorityClass("test-wpc").Obj(),
+			extraObjs: []client.Object{
+				baseWPCWrapper.Obj(),
+			},
+			wantJob: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).WorkloadPriorityClass("test-wpc").Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("jobset", "ns").
+					PriorityClass("test-wpc").
+					Priority(100).
+					PriorityClassSource(constants.WorkloadPriorityClassSource).
+					PodSets(
+						*utiltesting.MakePodSet("replicated-job-1", 1).Obj(),
+						*utiltesting.MakePodSet("replicated-job-2", 4).Obj(),
+					).
+					Obj(),
+			},
+		},
+		"workload is created with podsets and PriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
```

  Ditto.

- **pkg/controller/jobs/jobset/jobset_controller_test.go:374** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:58:36Z
```diff
@@ -254,6 +261,147 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"workload is created with podsets and workloadPriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).WorkloadPriorityClass("test-wpc").Obj(),
+			extraObjs: []client.Object{
+				baseWPCWrapper.Obj(),
+			},
+			wantJob: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).WorkloadPriorityClass("test-wpc").Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("jobset", "ns").
+					PriorityClass("test-wpc").
+					Priority(100).
+					PriorityClassSource(constants.WorkloadPriorityClassSource).
+					PodSets(
+						*utiltesting.MakePodSet("replicated-job-1", 1).Obj(),
+						*utiltesting.MakePodSet("replicated-job-2", 4).Obj(),
+					).
+					Obj(),
+			},
+		},
+		"workload is created with podsets and PriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).PriorityClass("test-pc").Obj(),
+			extraObjs: []client.Object{
+				basePCWrapper.Obj(),
+			},
+			wantJob: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
+			).PriorityClass("test-pc").Obj(),
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("jobset", "ns").
+					PriorityClass("test-pc").
+					Priority(200).
+					PriorityClassSource(constants.PodPriorityClassSource).
+					PodSets(
+						*utiltesting.MakePodSet("replicated-job-1", 1).Obj(),
+						*utiltesting.MakePodSet("replicated-job-2", 4).Obj(),
+					).
+					Obj(),
+			},
+		},
+		"workload is created with podsets, workloadPriorityClass and PriorityClass": {
+			reconcilerOptions: []jobframework.Option{
+				jobframework.WithManageJobsWithoutQueueName(true),
+			},
+			job: testingjobset.MakeJobSet("jobset", "ns").ReplicatedJobs(
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-1",
+					Replicas:    1,
+					Completions: 1,
+					Parallelism: 1,
+				},
+				testingjobset.ReplicatedJobRequirements{
+					Name:        "replicated-job-2",
+					Replicas:    2,
+					Completions: 2,
+					Parallelism: 2,
+				},
```

  Ditto.

- **pkg/controller/jobs/mpijob/mpijob_controller_test.go:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T17:59:27Z
```diff
@@ -195,9 +196,14 @@ var (
 )
 
 func TestReconciler(t *testing.T) {
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               *kubeflow.MPIJob
+		extraObjs         []client.Object
```

  Ditto.

- **pkg/util/priority/priority_test.go:29** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:07:01Z
```diff
@@ -26,6 +26,8 @@ import (
 	"k8s.io/apimachinery/pkg/runtime"
 	"sigs.k8s.io/controller-runtime/pkg/client/fake"
 
```

  ```suggestion
  ```

- **pkg/util/testing/wrappers.go:578** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:14:32Z
```diff
@@ -550,3 +560,28 @@ func (lr *LimitRangeWrapper) WithValue(member string, t corev1.ResourceName, q s
 func (lr *LimitRangeWrapper) Obj() *corev1.LimitRange {
 	return &lr.LimitRange
 }
+
+// WorkloadPriorityClassWrapper wraps a WorkloadPriorityClass.
+type WorkloadPriorityClassWrapper struct {
+	kueue.WorkloadPriorityClass
+}
+
+// MakeWorkloadPriorityClass creates a wrapper for a WorkloadPriorityClass.
+func MakeWorkloadPriorityClass(name string) *WorkloadPriorityClassWrapper {
+	return &WorkloadPriorityClassWrapper{kueue.WorkloadPriorityClass{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+// WorkloadPriorityValue update value of WorkloadPriorityClass。
```

  ```suggestion
  // WorkloadPriorityValue update value of WorkloadPriorityClass.
  ```
  
  This is a Japanese custom :upside_down_face:

- **pkg/util/priority/priority_test.go:146** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:20:52Z
```diff
@@ -137,29 +143,87 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.priorityClassList)
 			client := builder.Build()
 
-			name, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
-			if tt.wantErr != "" {
-				if err == nil {
-					t.Fatalf("expected an error")
-				}
-
-				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
-					t.Errorf("unexpected error (-want,+got):\n%s", diff)
-				}
-				return
-			}
-
-			if err != nil {
-				t.Fatalf("unexpected error: %v", err)
+			name, source, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
```

  ```suggestion
  			if diff := cmp.Diff(tt.wantErr, err, cmpopts.EquateErrors()); diff != "" {
  ```

- **pkg/util/priority/priority_test.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-12T18:21:22Z
```diff
@@ -137,29 +143,87 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.priorityClassList)
 			client := builder.Build()
 
-			name, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
-			if tt.wantErr != "" {
-				if err == nil {
-					t.Fatalf("expected an error")
-				}
-
-				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
-					t.Errorf("unexpected error (-want,+got):\n%s", diff)
-				}
-				return
-			}
-
-			if err != nil {
-				t.Fatalf("unexpected error: %v", err)
+			name, source, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
+				t.Errorf("unexpected error (-want,+got):\n%s", diff)
 			}
 
 			if name != tt.wantPriorityClassName {
 				t.Errorf("unexpected name: got: %s, expected: %s", name, tt.wantPriorityClassName)
 			}
 
+			if source != tt.wantPriorityClassSource {
+				t.Errorf("unexpected source: got: %s, expected: %s", source, tt.wantPriorityClassSource)
+			}
+
 			if value != tt.wantPriorityClassValue {
 				t.Errorf("unexpected value: got: %d, expected: %d", value, tt.wantPriorityClassValue)
 			}
 		})
 	}
 }
+
+func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+
+	tests := map[string]struct {
+		workloadPriorityClassList       *kueue.WorkloadPriorityClassList
+		workloadPriorityClassName       string
+		wantWorkloadPriorityClassName   string
+		wantWorkloadPriorityClassSource string
+		wantWorkloadPriorityClassValue  int32
+		wantErr                         error
+	}{
+		"workloadPriorityClass is specified and it exists": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{
+					{
+						ObjectMeta: v1.ObjectMeta{Name: "test"},
+						Value:      50,
+					},
+				},
+			},
+			workloadPriorityClassName:       "test",
+			wantWorkloadPriorityClassSource: constants.WorkloadPriorityClassSource,
+			wantWorkloadPriorityClassName:   "test",
+			wantWorkloadPriorityClassValue:  50,
+		},
+		"workloadPriorityClass is specified and it does not exist": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{},
+			},
+			workloadPriorityClassName: "test",
+			wantErr:                   apierrors.NewNotFound(kueue.Resource("workloadpriorityclasses"), "test"),
+		},
+	}
+
+	for desc, tt := range tests {
+		tt := tt
+		t.Run(desc, func(t *testing.T) {
+			t.Parallel()
+
+			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.workloadPriorityClassList)
+			client := builder.Build()
+
+			name, source, value, err := GetPriorityFromWorkloadPriorityClass(context.Background(), client, tt.workloadPriorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
```

  ```suggestion
  			if diff := cmp.Diff(tt.wantErr, err, cmpopts.EquateErrors()); diff != "" {
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-13T08:32:52Z

- **test/integration/controller/core/workload_controller_test.go:268** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-13T08:23:30Z
```diff
@@ -209,4 +215,88 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Workload's priority is mutable", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			priorityClass = testing.MakePriorityClass("priority-class").PriorityValue(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, priorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, priorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+		ginkgo.It("case of PriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("priority-class").PriorityClassSource(constants.PodPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(50)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+	})
```

  It looks like these tests verify workload webhooks. So can we move these to https://github.com/kubernetes-sigs/kueue/blob/025384a6d4d118538233d1603214b32a2a30b4bd/test/integration/webhook/workload_test.go?

- **test/integration/controller/core/workload_controller_test.go:297** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-13T08:28:08Z
```diff
@@ -209,4 +215,88 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Workload's priority is mutable", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			priorityClass = testing.MakePriorityClass("priority-class").PriorityValue(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, priorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, priorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+		ginkgo.It("case of PriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("priority-class").PriorityClassSource(constants.PodPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(50)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+	})
+
+	ginkgo.When("changing the priority value of PriorityClass doesn't affect the priority of the workload", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedWorkloadPriorityClass = workloadPriorityClass.DeepCopy()
+			workloadPriorityClass.Value = updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(workloadPriorityClass), updatedWorkloadPriorityClass)).To(gomega.Succeed())
+				return updatedWorkloadPriorityClass.Value
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
```

  ```suggestion
  			}, util.Timeout, util.Interval).Should(gomega.Equal(updatedPriority))
  ```

- **test/integration/controller/core/workload_controller_test.go:299** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-13T08:28:51Z
```diff
@@ -209,4 +215,88 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Workload's priority is mutable", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			priorityClass = testing.MakePriorityClass("priority-class").PriorityValue(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, priorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, priorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+		ginkgo.It("case of PriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("priority-class").PriorityClassSource(constants.PodPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(50)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+	})
+
+	ginkgo.When("changing the priority value of PriorityClass doesn't affect the priority of the workload", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedWorkloadPriorityClass = workloadPriorityClass.DeepCopy()
+			workloadPriorityClass.Value = updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(workloadPriorityClass), updatedWorkloadPriorityClass)).To(gomega.Succeed())
+				return updatedWorkloadPriorityClass.Value
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
```

  ```suggestion
  			gomega.Expect(finalQueueWorkload.Spec.Priority).To(gomega.BeEqual(&initialPriority))
  ```

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T14:06:18Z

- **pkg/controller/jobs/job/job_controller_test.go:366** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T14:06:18Z
```diff
@@ -352,11 +353,18 @@ func TestReconciler(t *testing.T) {
 		Request(corev1.ResourceCPU, "1").
 		Image("", nil)
 
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               batchv1.Job
-		wantJob           batchv1.Job
 		workloads         []kueue.Workload
+		extraObjs         []client.Object
```

  This is the suggestion from @alculquicondor  :)
  
  https://github.com/kubernetes-sigs/kueue/pull/1081#discussion_r1316230676

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T14:08:13Z

- **pkg/util/testing/wrappers.go:578** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T14:08:13Z
```diff
@@ -550,3 +560,28 @@ func (lr *LimitRangeWrapper) WithValue(member string, t corev1.ResourceName, q s
 func (lr *LimitRangeWrapper) Obj() *corev1.LimitRange {
 	return &lr.LimitRange
 }
+
+// WorkloadPriorityClassWrapper wraps a WorkloadPriorityClass.
+type WorkloadPriorityClassWrapper struct {
+	kueue.WorkloadPriorityClass
+}
+
+// MakeWorkloadPriorityClass creates a wrapper for a WorkloadPriorityClass.
+func MakeWorkloadPriorityClass(name string) *WorkloadPriorityClassWrapper {
+	return &WorkloadPriorityClassWrapper{kueue.WorkloadPriorityClass{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+// WorkloadPriorityValue update value of WorkloadPriorityClass。
```

  Oh... Thanks for your review!

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T14:34:39Z

- **apis/kueue/v1beta1/workloadpriorityclass_types.go:37** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T14:34:39Z
```diff
@@ -0,0 +1,56 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster
+//+kubebuilder:printcolumn:name="Value",JSONPath=".value",type=integer,description="Value of workloadPriorityClass's Priority"
+
+// WorkloadPriorityClass is the Schema for the workloadPriorityClass API
+type WorkloadPriorityClass struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	// value represents the integer value of this workloadPriorityClass. This is the actual priority that workloads
+	// receive when jobs have the name of this class in their workloadPriorityClass label.
+	// Changing the value of workloadPriorityClass doesn't affect the priority of workloads that were already created.
+	Value int32 `json:"value,omitempty"`
```

  Agree. Fixed it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T14:48:44Z

- **pkg/util/priority/priority_test.go:211** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T14:48:43Z
```diff
@@ -137,29 +143,87 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.priorityClassList)
 			client := builder.Build()
 
-			name, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
-			if tt.wantErr != "" {
-				if err == nil {
-					t.Fatalf("expected an error")
-				}
-
-				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
-					t.Errorf("unexpected error (-want,+got):\n%s", diff)
-				}
-				return
-			}
-
-			if err != nil {
-				t.Fatalf("unexpected error: %v", err)
+			name, source, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
+				t.Errorf("unexpected error (-want,+got):\n%s", diff)
 			}
 
 			if name != tt.wantPriorityClassName {
 				t.Errorf("unexpected name: got: %s, expected: %s", name, tt.wantPriorityClassName)
 			}
 
+			if source != tt.wantPriorityClassSource {
+				t.Errorf("unexpected source: got: %s, expected: %s", source, tt.wantPriorityClassSource)
+			}
+
 			if value != tt.wantPriorityClassValue {
 				t.Errorf("unexpected value: got: %d, expected: %d", value, tt.wantPriorityClassValue)
 			}
 		})
 	}
 }
+
+func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+
+	tests := map[string]struct {
+		workloadPriorityClassList       *kueue.WorkloadPriorityClassList
+		workloadPriorityClassName       string
+		wantWorkloadPriorityClassName   string
+		wantWorkloadPriorityClassSource string
+		wantWorkloadPriorityClassValue  int32
+		wantErr                         error
+	}{
+		"workloadPriorityClass is specified and it exists": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{
+					{
+						ObjectMeta: v1.ObjectMeta{Name: "test"},
+						Value:      50,
+					},
+				},
+			},
+			workloadPriorityClassName:       "test",
+			wantWorkloadPriorityClassSource: constants.WorkloadPriorityClassSource,
+			wantWorkloadPriorityClassName:   "test",
+			wantWorkloadPriorityClassValue:  50,
+		},
+		"workloadPriorityClass is specified and it does not exist": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{},
+			},
+			workloadPriorityClassName: "test",
+			wantErr:                   apierrors.NewNotFound(kueue.Resource("workloadpriorityclasses"), "test"),
+		},
+	}
+
+	for desc, tt := range tests {
+		tt := tt
+		t.Run(desc, func(t *testing.T) {
+			t.Parallel()
+
+			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.workloadPriorityClassList)
+			client := builder.Build()
+
+			name, source, value, err := GetPriorityFromWorkloadPriorityClass(context.Background(), client, tt.workloadPriorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
```

  When I added `cmpopts.EquateErrors`, I got an error below.
  Seems like if error object's pointer is different it returns error. This is not expected.
  
  > 
  >           (*errors.StatusError)(
  >         - 	&⟪0xc000448f00⟫{
  >         - 		ErrStatus: "k8s.io/apimachinery/pkg/apis/meta/v1".Status{
  >         - 			Status:  "Failure",
  >         - 			Message: `priorityclasses.scheduling.k8s.io "test" not found`,
  >         - 			Reason:  "NotFound",
  >         - 			Details: &⟪0xc00003e3c0⟫"k8s.io/apimachinery/pkg/apis/meta/v1".StatusDetails{Name: "test", Group: "scheduling.k8s.io", Kind: "priorityclasses"},
  >         - 			Code:    404,
  >         - 		},
  >         - 	},
  >         + 	&⟪0xc0002f60a0⟫{
  >         + 		ErrStatus: "k8s.io/apimachinery/pkg/apis/meta/v1".Status{
  >         + 			Status:  "Failure",
  >         + 			Message: `priorityclasses.scheduling.k8s.io "test" not found`,
  >         + 			Reason:  "NotFound",
  >         + 			Details: &⟪0xc00012e540⟫"k8s.io/apimachinery/pkg/apis/meta/v1".StatusDetails{Name: "test", Group: "scheduling.k8s.io", Kind: "priorityclasses"},
  >         + 			Code:    404,
  >         + 		},
  >         + 	},
  >           )

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T14:49:21Z

- **pkg/util/priority/priority_test.go:211** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T14:49:21Z
```diff
@@ -137,29 +143,87 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.priorityClassList)
 			client := builder.Build()
 
-			name, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
-			if tt.wantErr != "" {
-				if err == nil {
-					t.Fatalf("expected an error")
-				}
-
-				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
-					t.Errorf("unexpected error (-want,+got):\n%s", diff)
-				}
-				return
-			}
-
-			if err != nil {
-				t.Fatalf("unexpected error: %v", err)
+			name, source, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
+				t.Errorf("unexpected error (-want,+got):\n%s", diff)
 			}
 
 			if name != tt.wantPriorityClassName {
 				t.Errorf("unexpected name: got: %s, expected: %s", name, tt.wantPriorityClassName)
 			}
 
+			if source != tt.wantPriorityClassSource {
+				t.Errorf("unexpected source: got: %s, expected: %s", source, tt.wantPriorityClassSource)
+			}
+
 			if value != tt.wantPriorityClassValue {
 				t.Errorf("unexpected value: got: %d, expected: %d", value, tt.wantPriorityClassValue)
 			}
 		})
 	}
 }
+
+func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+
+	tests := map[string]struct {
+		workloadPriorityClassList       *kueue.WorkloadPriorityClassList
+		workloadPriorityClassName       string
+		wantWorkloadPriorityClassName   string
+		wantWorkloadPriorityClassSource string
+		wantWorkloadPriorityClassValue  int32
+		wantErr                         error
+	}{
+		"workloadPriorityClass is specified and it exists": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{
+					{
+						ObjectMeta: v1.ObjectMeta{Name: "test"},
+						Value:      50,
+					},
+				},
+			},
+			workloadPriorityClassName:       "test",
+			wantWorkloadPriorityClassSource: constants.WorkloadPriorityClassSource,
+			wantWorkloadPriorityClassName:   "test",
+			wantWorkloadPriorityClassValue:  50,
+		},
+		"workloadPriorityClass is specified and it does not exist": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{},
+			},
+			workloadPriorityClassName: "test",
+			wantErr:                   apierrors.NewNotFound(kueue.Resource("workloadpriorityclasses"), "test"),
+		},
+	}
+
+	for desc, tt := range tests {
+		tt := tt
+		t.Run(desc, func(t *testing.T) {
+			t.Parallel()
+
+			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.workloadPriorityClassList)
+			client := builder.Build()
+
+			name, source, value, err := GetPriorityFromWorkloadPriorityClass(context.Background(), client, tt.workloadPriorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
```

  https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1081/pull-kueue-test-unit-main/1702329955154661376

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-14T15:08:56Z

- **pkg/controller/jobframework/validation.go:94** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-14T15:08:55Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  Do you mean like this?
  `allErrs = append(allErrs, apivalidation.ValidateImmutableField(oldName, newName, workloadPriorityClassNamePath)...)`
  
  BTW, in validation.go, other functions use `field.Forbidden`. 
  How do we differentiate between `field.Forbidden` and `apivalidation.ValidateImmutableField` ?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-14T16:32:35Z

- **pkg/controller/jobs/job/job_controller_test.go:366** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-14T16:32:35Z
```diff
@@ -352,11 +353,18 @@ func TestReconciler(t *testing.T) {
 		Request(corev1.ResourceCPU, "1").
 		Image("", nil)
 
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               batchv1.Job
-		wantJob           batchv1.Job
 		workloads         []kueue.Workload
+		extraObjs         []client.Object
```

  Ah, I see.
  However, `extraObjs` sounds much more abstract to me. How about `priorityClasses []client.Object`? @Gekko0114 @alculquicondor

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-14T16:39:25Z

- **pkg/util/priority/priority_test.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-14T16:39:24Z
```diff
@@ -137,29 +143,87 @@ func TestGetPriorityFromPriorityClass(t *testing.T) {
 			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.priorityClassList)
 			client := builder.Build()
 
-			name, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
-			if tt.wantErr != "" {
-				if err == nil {
-					t.Fatalf("expected an error")
-				}
-
-				if diff := cmp.Diff(tt.wantErr, err.Error()); diff != "" {
-					t.Errorf("unexpected error (-want,+got):\n%s", diff)
-				}
-				return
-			}
-
-			if err != nil {
-				t.Fatalf("unexpected error: %v", err)
+			name, source, value, err := GetPriorityFromPriorityClass(context.Background(), client, tt.priorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
+				t.Errorf("unexpected error (-want,+got):\n%s", diff)
 			}
 
 			if name != tt.wantPriorityClassName {
 				t.Errorf("unexpected name: got: %s, expected: %s", name, tt.wantPriorityClassName)
 			}
 
+			if source != tt.wantPriorityClassSource {
+				t.Errorf("unexpected source: got: %s, expected: %s", source, tt.wantPriorityClassSource)
+			}
+
 			if value != tt.wantPriorityClassValue {
 				t.Errorf("unexpected value: got: %d, expected: %d", value, tt.wantPriorityClassValue)
 			}
 		})
 	}
 }
+
+func TestGetPriorityFromWorkloadPriorityClass(t *testing.T) {
+	scheme := runtime.NewScheme()
+	if err := kueue.AddToScheme(scheme); err != nil {
+		t.Fatalf("Failed adding kueue scheme: %v", err)
+	}
+
+	tests := map[string]struct {
+		workloadPriorityClassList       *kueue.WorkloadPriorityClassList
+		workloadPriorityClassName       string
+		wantWorkloadPriorityClassName   string
+		wantWorkloadPriorityClassSource string
+		wantWorkloadPriorityClassValue  int32
+		wantErr                         error
+	}{
+		"workloadPriorityClass is specified and it exists": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{
+					{
+						ObjectMeta: v1.ObjectMeta{Name: "test"},
+						Value:      50,
+					},
+				},
+			},
+			workloadPriorityClassName:       "test",
+			wantWorkloadPriorityClassSource: constants.WorkloadPriorityClassSource,
+			wantWorkloadPriorityClassName:   "test",
+			wantWorkloadPriorityClassValue:  50,
+		},
+		"workloadPriorityClass is specified and it does not exist": {
+			workloadPriorityClassList: &kueue.WorkloadPriorityClassList{
+				Items: []kueue.WorkloadPriorityClass{},
+			},
+			workloadPriorityClassName: "test",
+			wantErr:                   apierrors.NewNotFound(kueue.Resource("workloadpriorityclasses"), "test"),
+		},
+	}
+
+	for desc, tt := range tests {
+		tt := tt
+		t.Run(desc, func(t *testing.T) {
+			t.Parallel()
+
+			builder := fake.NewClientBuilder().WithScheme(scheme).WithLists(tt.workloadPriorityClassList)
+			client := builder.Build()
+
+			name, source, value, err := GetPriorityFromWorkloadPriorityClass(context.Background(), client, tt.workloadPriorityClassName)
+			if diff := cmp.Diff(tt.wantErr, err); diff != "" {
```

  I see. Ideally, wrapping an error in https://github.com/kubernetes-sigs/kueue/blob/540551609352332c8d5877a6e1a025030102facb/pkg/util/priority/priority.go#L50, then we compare the unwrapped error. However, in this case, the way might be overkill.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-14T16:53:55Z

- **pkg/controller/jobframework/validation.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-14T16:53:54Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  > Do you mean like this?
  allErrs = append(allErrs, apivalidation.ValidateImmutableField(oldName, newName, workloadPriorityClassNamePath)...)
  
  Yes, I meant what you say.
  
  > BTW, in validation.go, other functions use field.Forbidden.
  How do we differentiate between field.Forbidden and apivalidation.ValidateImmutableField ?
  
  Uhm, I see. Although, I think we should use `apivalidation.ValidateImmutableField` in the below places:
  
  https://github.com/kubernetes-sigs/kueue/blob/91d5fa44b6b06c15778700f7126578816de3cb79/pkg/controller/jobframework/validation.go#L60-L68
  
  https://github.com/kubernetes-sigs/kueue/blob/91d5fa44b6b06c15778700f7126578816de3cb79/pkg/controller/jobframework/validation.go#L70-L76
  
  and here.
  
  @mimowo Do you remember why you use `field.Forbidden` instead of `apivalidation.ValidateImmutableField` in 
  [pkg/controller/jobframework/validation.go](https://github.com/kubernetes-sigs/kueue/pull/633/files#diff-5c2c085d3754c50f6e899f937ef8113a2fc81685d7130b5bf88d1a37d2f26d67): https://github.com/kubernetes-sigs/kueue/pull/633?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-16T05:52:21Z

- **pkg/controller/jobframework/validation.go:94** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-16T05:52:21Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  Thanks. Fixed it

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-16T05:52:55Z

- **pkg/controller/jobs/job/job_controller_test.go:366** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-16T05:52:55Z
```diff
@@ -352,11 +353,18 @@ func TestReconciler(t *testing.T) {
 		Request(corev1.ResourceCPU, "1").
 		Image("", nil)
 
+	baseWPCWrapper := utiltesting.MakeWorkloadPriorityClass("test-wpc").
+		WorkloadPriorityValue(100)
+
+	basePCWrapper := utiltesting.MakePriorityClass("test-pc").
+		PriorityValue(200)
+
 	cases := map[string]struct {
 		reconcilerOptions []jobframework.Option
 		job               batchv1.Job
-		wantJob           batchv1.Job
 		workloads         []kueue.Workload
+		extraObjs         []client.Object
```

  I agree. This name is more clear what this variable is. Fixed it.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-16T06:31:49Z

- **test/integration/controller/core/workload_controller_test.go:268** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-16T06:31:49Z
```diff
@@ -209,4 +215,88 @@ var _ = ginkgo.Describe("Workload controller", func() {
 			})
 		})
 	})
+
+	ginkgo.When("Workload's priority is mutable", func() {
+		ginkgo.BeforeEach(func() {
+			workloadPriorityClass = testing.MakeWorkloadPriorityClass("workload-priority-class").WorkloadPriorityValue(200).Obj()
+			priorityClass = testing.MakePriorityClass("priority-class").PriorityValue(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Create(ctx, priorityClass)).To(gomega.Succeed())
+
+		})
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, workloadPriorityClass)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Delete(ctx, priorityClass)).To(gomega.Succeed())
+		})
+		ginkgo.It("case of WorkloadPriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+		ginkgo.It("case of PriorityClass", func() {
+			wl = testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("priority-class").PriorityClassSource(constants.PodPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.BeComparableTo(&initialPriority))
+			updatedPriority := int32(50)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.BeComparableTo(updatedPriority))
+		})
+	})
```

  I see. This test confirms that workload webhooks wouldn't stop updating the priority values of workload. So it might be the test case for webhook instead of controller.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-20T20:54:13Z

Looking good overall.
I'll leave the final say to @tenzen-y.

- **apis/kueue/v1beta1/workload_types.go:57** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-20T18:43:11Z
```diff
@@ -52,6 +52,13 @@ type WorkloadSpec struct {
 	// The higher the value, the higher the priority.
 	// If priorityClassName is specified, priority must not be null.
 	Priority *int32 `json:"priority,omitempty"`
+
+	// Workload's PriorityClass can accept the name of a pod priorityClass or a workloadPriorityClass.
+	// priorityClassSource determines whether the priorityClass field refers to a pod PriorityClass or kueue.x-k8s.io/workloadpriorityclass
```

  this should be the first line. Please move it above.

- **keps/973-workload-priority/README.md:433** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-20T18:53:13Z
```diff
@@ -429,9 +432,8 @@ spec:
 
 ### How to expand Priority utility
```

  This section just contains implementation details. You could remove it.

- **pkg/controller/jobframework/interface.go:90** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-20T18:58:54Z
```diff
@@ -86,3 +86,11 @@ func QueueNameForObject(object client.Object) string {
 	// fallback to the annotation (deprecated)
 	return object.GetAnnotations()[constants.QueueAnnotation]
 }
+
+func WorkloadPriorityClassName(job GenericJob) string {
```

  If no other package uses this, you can name it `workloadPriorityClassName`.

- **pkg/controller/jobframework/validation.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-20T19:01:50Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
```

  add a unit test

- **pkg/util/testing/wrappers.go:595** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-20T20:52:43Z
```diff
@@ -566,3 +576,28 @@ func MakeAdmissionCheck(name string) *AdmissionCheckWrapper {
 func (ac *AdmissionCheckWrapper) Obj() *kueue.AdmissionCheck {
 	return &ac.AdmissionCheck
 }
+
+// WorkloadPriorityClassWrapper wraps a WorkloadPriorityClass.
+type WorkloadPriorityClassWrapper struct {
+	kueue.WorkloadPriorityClass
+}
+
+// MakeWorkloadPriorityClass creates a wrapper for a WorkloadPriorityClass.
+func MakeWorkloadPriorityClass(name string) *WorkloadPriorityClassWrapper {
+	return &WorkloadPriorityClassWrapper{kueue.WorkloadPriorityClass{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+// WorkloadPriorityValue update value of WorkloadPriorityClass.
+func (p *WorkloadPriorityClassWrapper) WorkloadPriorityValue(v int32) *WorkloadPriorityClassWrapper {
```

  ```suggestion
  func (p *WorkloadPriorityClassWrapper) Value(v int32) *WorkloadPriorityClassWrapper {
  ```

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-21T11:28:51Z

- **pkg/util/testing/wrappers.go:595** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-21T11:28:51Z
```diff
@@ -566,3 +576,28 @@ func MakeAdmissionCheck(name string) *AdmissionCheckWrapper {
 func (ac *AdmissionCheckWrapper) Obj() *kueue.AdmissionCheck {
 	return &ac.AdmissionCheck
 }
+
+// WorkloadPriorityClassWrapper wraps a WorkloadPriorityClass.
+type WorkloadPriorityClassWrapper struct {
+	kueue.WorkloadPriorityClass
+}
+
+// MakeWorkloadPriorityClass creates a wrapper for a WorkloadPriorityClass.
+func MakeWorkloadPriorityClass(name string) *WorkloadPriorityClassWrapper {
+	return &WorkloadPriorityClassWrapper{kueue.WorkloadPriorityClass{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+// WorkloadPriorityValue update value of WorkloadPriorityClass.
+func (p *WorkloadPriorityClassWrapper) WorkloadPriorityValue(v int32) *WorkloadPriorityClassWrapper {
```

  Value couldn't be used as a function name because it is already being used in the "workloadPriorityClass" field. Instead, I renamed it "PriorityValue."

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-21T11:34:07Z

- **pkg/controller/jobframework/validation.go:88** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-21T11:34:06Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
```

  Since `validation.go` doesn't have test file like `validation_test.go`, I added test cases to `job_webhook_test.go` etc instead. This is consistent with other validation functions like `ValidateUpdateForParentWorkload`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-22T23:08:17Z

@Gekko0114 Otherwise LGTM
Could you open a PR to add documentation for WorkloadPriorityClass?

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:55** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T22:55:10Z
```diff
@@ -52,6 +52,7 @@ func init() {
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloadpriorityclasses,verbs=get;list;watch
```

  Could you add this marker to the XGBoostJob and the PaddleJob?
  
  https://github.com/kubernetes-sigs/kueue/blob/f9273bef71e136c0eed659ae19fce178512a6f6a/pkg/controller/jobs/kubeflow/jobs/xgboostjob/xgboostjob_controller.go#L47-L54
  
  https://github.com/kubernetes-sigs/kueue/blob/f9273bef71e136c0eed659ae19fce178512a6f6a/pkg/controller/jobs/kubeflow/jobs/paddlejob/paddlejob_controller.go#L47-L54

- **pkg/util/testing/wrappers.go:594** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T22:57:41Z
```diff
@@ -566,3 +576,28 @@ func MakeAdmissionCheck(name string) *AdmissionCheckWrapper {
 func (ac *AdmissionCheckWrapper) Obj() *kueue.AdmissionCheck {
 	return &ac.AdmissionCheck
 }
+
+// WorkloadPriorityClassWrapper wraps a WorkloadPriorityClass.
+type WorkloadPriorityClassWrapper struct {
+	kueue.WorkloadPriorityClass
+}
+
+// MakeWorkloadPriorityClass creates a wrapper for a WorkloadPriorityClass.
+func MakeWorkloadPriorityClass(name string) *WorkloadPriorityClassWrapper {
+	return &WorkloadPriorityClassWrapper{kueue.WorkloadPriorityClass{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: name,
+		}},
+	}
+}
+
+// PriorityValue update value of WorkloadPriorityClass.
```

  ```suggestion
  // PriorityValue updates value of WorkloadPriorityClass.
  ```

- **test/integration/webhook/workload_test.go:238** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T23:02:58Z
```diff
@@ -194,5 +215,49 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 			gomega.Expect(err).Should(gomega.HaveOccurred())
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
+
+		ginkgo.It("workload's priority should be mutable when referencing WorkloadPriorityClass", func() {
+			ginkgo.By("creating workload")
+			wl := testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
+
+			ginkgo.By("Updating workload's priority")
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.Equal(updatedPriority))
```

  ```suggestion
  				return finalQueueWorkload.Spec.Priority
  			}, util.Timeout, util.Interval).Should(gomega.Equal(&updatedPriority))
  ```
  
  I think there is a potential nil pointer error.

- **test/integration/webhook/workload_test.go:260** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T23:03:44Z
```diff
@@ -194,5 +215,49 @@ var _ = ginkgo.Describe("Workload validating webhook", func() {
 			gomega.Expect(err).Should(gomega.HaveOccurred())
 			gomega.Expect(errors.IsForbidden(err)).Should(gomega.BeTrue(), "error: %v", err)
 		})
+
+		ginkgo.It("workload's priority should be mutable when referencing WorkloadPriorityClass", func() {
+			ginkgo.By("creating workload")
+			wl := testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("workload-priority-class").PriorityClassSource(constants.WorkloadPriorityClassSource).Priority(200).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(200)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
+
+			ginkgo.By("Updating workload's priority")
+			updatedPriority := int32(150)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.Equal(updatedPriority))
+		})
+
+		ginkgo.It("workload's priority should be mutable when referencing PriorityClass", func() {
+			ginkgo.By("creating workload")
+			wl := testing.MakeWorkload("wl", ns.Name).Queue("lq").Request(corev1.ResourceCPU, "1").
+				PriorityClass("priority-class").PriorityClassSource(constants.PodPriorityClassSource).Priority(100).Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			gomega.Eventually(func() []metav1.Condition {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedQueueWorkload)).To(gomega.Succeed())
+				return updatedQueueWorkload.Status.Conditions
+			}, util.Timeout, util.Interval).ShouldNot(gomega.BeNil())
+			initialPriority := int32(100)
+			gomega.Expect(updatedQueueWorkload.Spec.Priority).To(gomega.Equal(&initialPriority))
+
+			ginkgo.By("Updating workload's priority")
+			updatedPriority := int32(50)
+			updatedQueueWorkload.Spec.Priority = &updatedPriority
+			gomega.Expect(k8sClient.Update(ctx, &updatedQueueWorkload)).To(gomega.Succeed())
+			gomega.Eventually(func() int32 {
+				gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(&updatedQueueWorkload), &finalQueueWorkload)).To(gomega.Succeed())
+				return *finalQueueWorkload.Spec.Priority
+			}, util.Timeout, util.Interval).Should(gomega.Equal(updatedPriority))
```

  ```suggestion
  				return finalQueueWorkload.Spec.Priority
  			}, util.Timeout, util.Interval).Should(gomega.Equal(&updatedPriority))
  ```
  
  ditto.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-09-23T07:34:07Z

- **pkg/controller/jobs/kubeflow/jobs/pytorchjob/pytorchjob_controller.go:55** by [@Gekko0114](https://github.com/Gekko0114) — 2023-09-23T07:34:07Z
```diff
@@ -52,6 +52,7 @@ func init() {
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/finalizers,verbs=update
 // +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=resourceflavors,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloadpriorityclasses,verbs=get;list;watch
```

  Thanks, added

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-24T02:18:43Z

@Gekko0114 Thank you for the hard work! I am really looking forward to this new feature :)

/lgtm
/approve

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-02T17:11:00Z

- **pkg/controller/jobframework/validation.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T17:11:00Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  it probably just slipped. We should prefer to use `ValidateImmutableField` to reduce code repetion.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-10-03T07:58:01Z

- **pkg/controller/jobframework/validation.go:94** by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-03T07:58:01Z
```diff
@@ -83,3 +84,13 @@ func ValidateUpdateForParentWorkload(oldJob, newJob GenericJob) field.ErrorList
 	}
 	return allErrs
 }
+
+func ValidateUpdateForWorkloadPriorityClassName(oldJob, newJob GenericJob) field.ErrorList {
+	var allErrs field.ErrorList
+	oldName, _ := WorkloadPriorityClassName(oldJob)
+	newName, _ := WorkloadPriorityClassName(newJob)
+	if oldName != newName {
+		allErrs = append(allErrs, field.Forbidden(workloadPriorityClassNamePath, "must not update workloadPriorityClass name"))
+	}
```

  Sure, will create an issue.
