# PR #10877: KEP-9270: Introduce configuration for the MultiKueue Incremental Dispatcher

**Summary**: KEP-9270: Introduce configuration for the MultiKueue Incremental Dispatcher

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10877

**Last updated**: 2026-05-08T13:42:01Z

---

## Metadata

- **State**: open
- **Author**: [@Mostafahassen1](https://github.com/Mostafahassen1)
- **Branch**: `kep-9270-multikueue-step-size` → `main`
- **Created**: 2026-05-01T00:20:53Z
- **Updated**: 2026-05-08T13:42:01Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `cncf-cla: yes`, `area/multikueue`, `kind/kep`
- **Assignees**: _none_
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+176 / -1**

## Description

#### What type of PR is this?

/kind kep
/area multikueue

#### What this PR does / why we need it:
This PR proposes a KEP to introduce the `stepSize` configuration parameter for the MultiKueue Incremental Dispatcher. 

Currently, the Incremental Dispatcher can either query worker clusters one-by-one (which is slow) or all-at-once (which creates a heavy API load and risks race conditions). This KEP outlines a design to allow administrators to batch these queries by specifying a `stepSize`, finding the optimal balance between dispatch speed and system stability.

#### Which issue(s) this PR fixes:
Fixes #9270

#### Special notes for your reviewer:
This PR contains the initial KEP document (`README.md` and `kep.yaml`) for the feature proposed in #9270. 

Once this KEP is reviewed and approved (status: implementable), I will open follow-up PRs containing the API additions to `MultiKueueConfig` and the core logic updates to the Workload Controller.

#### Does this PR introduce a user-facing change?
```release-note
NONE

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-01T00:20:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 038f1eb6dd7ce0874efdb3efa30c29005e3cd807 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fde8183a2e3a000816d3a8 |

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-01T15:06:37Z

CC /  @mimowo , @olekzabl

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T23:00:24Z

1. Once the plan gets approved (which I hope to happen soon), we'll probably need to update the "main" KEP for MultiKueue, KEP-693. (Search for "incremental" there, and you'll see).
  OTOH, AFAIU, the good practice is to keep such old KEPs universally valid for all versions - which is achieved by saying "until v..." or "since v..." (for inspiration, search for "since v" in KEP-693, or e.g. in KEP-2724).

2. Given the above - my apologies for coming to this thought so late! - I'm now not sure if this new KEP should even exist.
  So far, this feature feels quite simple, and not involving any major controversy. So maybe just updating KEP-693 could be enough.
  OTOH, Kueue already has some tiny KEPs (e.g. 2936, or 3122). So perhaps having this new one won't hurt :)

@mimowo You know the customs better; what would you say?
Should KEP-693 be updated? Should it happen in this PR? Should we still keep this new KEP?

_(P.S. I think a separate KEP makes much sense **if** we want to have a new dedicated feature gate - but do we? see [below](https://github.com/kubernetes-sigs/kueue/pull/10877#issuecomment-4383899103))_

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-05T23:16:14Z

@olekzabl 
>Given the above - my apologies for coming to this thought so late! - I'm now not sure if this new KEP should even exist.
So far, this feature feels quite simple,

No problem at all! I’m completely open to following the Kueue standards here. If you think it’s better to update KEP-693 instead of having a new one

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T23:25:04Z

> @olekzabl
> 
> > Given the above - my apologies for coming to this thought so late! - I'm now not sure if this new KEP should even exist.
> > So far, this feature feels quite simple,
> 
> No problem at all! I’m completely open to following the Kueue standards here. If you think it’s better to update KEP-693 instead of having a new one

I'm sorry, I'm puzzled by this question (i.e. "update old or write new or both").
I'm increasingly feeling that the answer depends on whether we want to have a dedicated feature gate for this - which is also not obvious. (After all, your only API change is introducing a new field, which does not change _much_ in the implementation when set, and should change _nothing_ in it when unset).

For me, right now, this feels a delicate judgement call... so for now I'd keep it as is and wait to see what the other reviewers will say.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-06T15:10:09Z

/release-note-none

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-06T15:13:53Z

@olekzabl @mimowo I am just wondering if we should add new capabilities to the dispatcher mechanism since we were planning to replace it with another approach in https://github.com/kubernetes-sigs/kueue/issues/8607

But if you want to postpone https://github.com/kubernetes-sigs/kueue/issues/8607 to a later date for now, I'm ok with that.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T15:27:11Z

@tenzen-y my intention in https://github.com/kubernetes-sigs/kueue/issues/8607 is not to replace or decommission the built-in dispatches that we have (AllAtOnce, Incremental). The intention of that Issue is to make it easier to work with external dispatchers - currently they need to pretend they are core Kueue when sending requests.

So regardless if we do https://github.com/kubernetes-sigs/kueue/issues/8607 it is useful to add new capabilities to the built-in dispatchers.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T15:33:12Z

As discussed with @olekzabl today - I would prefer to generalize the KEP as I think there is a need to extend the configuration in the future - to add other fields like "timeout", or maybe "stepSizeAsPercent". So, I would suggest to name the KEP as Introduce configuration for the MultiKueue Incremental Dispatcher. For now the configuration will be just one field, but it will also give a paved way for future configuration of built-in dispatchers, I'm thinking about

```yaml
multiKueue:
  dispatcherName: "kueue.x-k8s.io/multikueue-dispatcher-incremental"
  inrementalDispatcherConfig:
    stepSize: 1
```
cc @olekzabl @tenzen-y @Mostafahassen1

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-06T15:40:23Z

> @tenzen-y my intention in #8607 is not to replace or decommission the built-in dispatches that we have (AllAtOnce, Incremental). The intention of that Issue is to make it easier to work with external dispatchers - currently they need to pretend they are core Kueue when sending requests.
> 
> So regardless if we do #8607 it is useful to add new capabilities to the built-in dispatchers.

That sounds good to me, thank you. In that case, let's see this KEP.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T15:48:40Z

> As discussed with @olekzabl today - I would prefer to generalize the KEP as I think there is a need to extend the configuration in the future - to add other fields like "timeout", or maybe "stepSizeAsPercent".

+1 (and thank you for posting this info before I managed to do it).

Additional points:

1. We think your new feature gate can stay, particularly given the Beta graduation criterion on gathering user feedback.

2. This KEP can also stay as it is.
   Personally, I'd be happy if you add a short "Possible Follow-ups" section, covering @mimowo 's ideas listed above (timeout, stepSizeAsPercent). This way we'll ensure good ideas aren't lost. This may also facilitate receiving feedback.

3. In KEP-693, currently mentioning "set of 3 clusters", let's make sure this doesn't become incorrect.
   This can be a minimal change, e.g. at the bottom of the description of the incremental dispatcher, add 1 sentence:
   "There are plans to make this behavior configurable; see [a link to your new KEP]".

   * FYI, one clarification I got from @mimowo : our custom is to allow KEPs get out of date once their feature becomes stable. However, this is not the case for KEP-693, where MultiKueue is still in Beta. So, AFAIU, it's still "fresh enough" to be kept accurate.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:26:00Z

/retitle KEP-9270: Introduce configuration for the MultiKueue Incremental Dispatcher

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-07T11:01:04Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10877#" title="Author self-approved">Mostafahassen1</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10877#pullrequestreview-4243358790" title="Approved">olekzabl</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

## Reviews

### Review by [@olekzabl](https://github.com/olekzabl) (changes_requested) — 2026-05-05T11:49:29Z

Hi, thank you for your PR!

This is my first round of comments; I plan to come back soon.

- **keps/9270-multikueue-incremental-step-size/kep.yaml:23** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:26:09Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.9"
+  beta: "v0.10"
+  stable: "v1.0"
```

  This will need updating. I guess alpha 0.19, beta 0.20, stable 0.21?

- **keps/9270-multikueue-incremental-step-size/README.md:29** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:32:42Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
```

  Nit: I feel this paragraph is somewhat confusing.
  
  I think you captured the _tradeoff_ well (between slow dispatching & high resource usage).
  However, that tradeoff _does not yet exist_, as we currently hard-code `batchSize := 3`.
  I think this paragraph sounds as if this was already variable.
  
  Also, when you write "the Incremental Dispatcher has a limitation", it remains unclear what's the "limitation" specifically.

- **keps/9270-multikueue-incremental-step-size/README.md:37** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:35:25Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
```

  1. Why only in that batch?
  2. Isn't this already done, [here](https://github.com/kubernetes-sigs/kueue/blob/3a3d405ff0596aa4d3f433c90a73b570b59b2e94/pkg/controller/admissionchecks/multikueue/workload.go#L460-L469)?
  
  (I'm not sure if it's worth mentioning as a goal).

- **keps/9270-multikueue-incremental-step-size/README.md:52** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:36:46Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
```

  Nit: please separate them by an empty line, for better rendering.

- **keps/9270-multikueue-incremental-step-size/README.md:52** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:39:50Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
```

  > (which deletes duplicate proxy workloads once one is admitted)
  
  Nit: I'd remove this part because this is not the only race condition threatening us.
  See e.g. [KEP-8303](https://github.com/kubernetes-sigs/kueue/tree/main/keps/8303-multikueue-orchestrated-preemption) which deals with another kind of race condition.
  
  (Still, the rest of the sentence makes sense: whatever prevention mechanisms we have, they will still act).

- **keps/9270-multikueue-incremental-step-size/README.md:52** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:42:36Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
```

  > regarding best practices
  
  Out of curiosity, what "best practices" do you have in mind?
  Is there any number (constant, or dependent on N = number of clusters) which you wouldn't recommend exceeding?
  
  Asking because I don't know. Maybe N/2 is still reasonable for some use cases? Maybe even more? (Presumably, some users are happy even with `AllAtOnce`).

- **keps/9270-multikueue-incremental-step-size/README.md:57** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:45:06Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
```

  > defaulting the step size to `1` (which matches the current one-by-one behavior).
  
  Hmm, is the current behavior one-by-one?
  
  https://github.com/kubernetes-sigs/kueue/blob/3a3d405ff0596aa4d3f433c90a73b570b59b2e94/pkg/controller/workloaddispatcher/incrementaldispatcher.go#L180

- **keps/9270-multikueue-incremental-step-size/README.md:74** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T11:48:46Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change will take place inside the MultiKueue Workload Controller's reconciliation loop:
```

  Hmm, it seems that we need less changes than that.
  IIUC the batching logic already exists in [this file](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/workloaddispatcher/incrementaldispatcher.go); we just need to replace the hard-coded `batchSize := 3` with your proposed custom parameter.
  
  Unless I'm confusing things.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-05T13:14:40Z

- **keps/9270-multikueue-incremental-step-size/README.md:119** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T12:43:41Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change will take place inside the MultiKueue Workload Controller's reconciliation loop:
+1. When a workload is ready for dispatch, the controller reads the `stepSize` from the `MultiKueueConfig`.
+2. The controller takes the full list of available worker clusters and divides them into groups (batches) equal to the `stepSize`.
+3. The controller loops through the first batch, creating proxy workloads on those specific worker clusters simultaneously.
+4. If a cluster in the batch admits the job, the controller immediately deletes the proxy workloads from the other clusters in that same batch and stops the loop.
+5. If no cluster in the batch admits the job, the controller moves on to the next batch of clusters.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the default value remains `1` if the user does not specify a `stepSize`.
+
+#### Integration tests
+
+- `TestIncrementalDispatcherStepSize`: Simulate a Manager Cluster with 5 Worker Clusters. Set `stepSize` to `2`. Submit a workload and verify via the API server that exactly 2 proxy workloads are created in the first step. Simulate a rejection on the first 2 clusters, and verify that the controller correctly creates the next 2 proxy workloads in the second step.
+
+#### e2e tests
+
+Standard E2E multi-cluster tests will be updated to include an iteration where `MultiKueueConfig` is deployed with a `stepSize > 1` to ensure workloads execute successfully end-to-end without duplication.
+
+### Graduation Criteria
+
+* **Alpha:** * API fields added.
+    * Workload Controller updated to support batching.
+    * Unit tests and EnvTest integration tests passing.
+* **Beta:** * Gather feedback from the community.
+    * E2E tests added and passing consistently.
+* **Stable:**
+    * Feature has been in Beta for at least one release cycle with no major bugs reported.
+
+## Implementation History
+
+- 2026-05-01: KEP proposed and initial draft created.
+
+## Drawbacks
+
+This adds slight code complexity to the existing Incremental Dispatcher loop and introduces one additional configuration parameter for administrators to manage and tune.
+```
```

  ```suggestion
  ```

- **keps/9270-multikueue-incremental-step-size/README.md:104** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T12:45:41Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change will take place inside the MultiKueue Workload Controller's reconciliation loop:
+1. When a workload is ready for dispatch, the controller reads the `stepSize` from the `MultiKueueConfig`.
+2. The controller takes the full list of available worker clusters and divides them into groups (batches) equal to the `stepSize`.
+3. The controller loops through the first batch, creating proxy workloads on those specific worker clusters simultaneously.
+4. If a cluster in the batch admits the job, the controller immediately deletes the proxy workloads from the other clusters in that same batch and stops the loop.
+5. If no cluster in the batch admits the job, the controller moves on to the next batch of clusters.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the default value remains `1` if the user does not specify a `stepSize`.
+
+#### Integration tests
+
+- `TestIncrementalDispatcherStepSize`: Simulate a Manager Cluster with 5 Worker Clusters. Set `stepSize` to `2`. Submit a workload and verify via the API server that exactly 2 proxy workloads are created in the first step. Simulate a rejection on the first 2 clusters, and verify that the controller correctly creates the next 2 proxy workloads in the second step.
+
+#### e2e tests
+
+Standard E2E multi-cluster tests will be updated to include an iteration where `MultiKueueConfig` is deployed with a `stepSize > 1` to ensure workloads execute successfully end-to-end without duplication.
+
+### Graduation Criteria
+
+* **Alpha:** * API fields added.
```

  ```suggestion
  * **Alpha:**
      * API fields added.
  ```
  
  and same for "Beta" below.

- **keps/9270-multikueue-incremental-step-size/README.md:92** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T12:46:31Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change will take place inside the MultiKueue Workload Controller's reconciliation loop:
+1. When a workload is ready for dispatch, the controller reads the `stepSize` from the `MultiKueueConfig`.
+2. The controller takes the full list of available worker clusters and divides them into groups (batches) equal to the `stepSize`.
+3. The controller loops through the first batch, creating proxy workloads on those specific worker clusters simultaneously.
+4. If a cluster in the batch admits the job, the controller immediately deletes the proxy workloads from the other clusters in that same batch and stops the loop.
+5. If no cluster in the batch admits the job, the controller moves on to the next batch of clusters.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the default value remains `1` if the user does not specify a `stepSize`.
```

  ```suggestion
  - `pkg/controller/core/workload_controller_test.go`: Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
  - `pkg/controller/core/workload_controller_test.go`: Verify that the default value remains `1` if the user does not specify a `stepSize`.
  ```

- **keps/9270-multikueue-incremental-step-size/README.md:69** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T13:13:49Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
```

  With this proposal, if a user wants to use incremental dispatcher with step 4, they'll have to say:
  
  ```yaml
  # in the global Configuration object for Kueue
  multiKueue:
    dispatcherName: kueue.x-k8s.io/multikueue-dispatcher-incremental
  
  # in the MultiKueueConfig (referenced by the MK AC, referenced by the manager CQ)
  spec:
    stepSize: 4
  ```
  
  This _might_ be best - but I'd like to discuss:
  
  1. Do we really want step size configured in a very distant place from the choice of the dispatcher?
     (This would be justified if we needed to support different step sizes for different CQs.
     However, note that the choice "Incremental or AllAtOnce or Custom" is already global.
     So maybe we have no reason to stand out?)
  
  2. Regardless of the answer to (1), I think I'd prefer the field name to be more self-explaining.
     E.g. `IncrementalDispatcherStepSize`.
  
  BTW, if we choose to keep both fields next to each other, I'd also add validation that "step size" can be set _only if_ "incremental" is actually chosen. (This can be done with kubebuilder rules).

### Review by [@Mostafahassen1](https://github.com/Mostafahassen1) (commented) — 2026-05-05T20:17:41Z

- **keps/9270-multikueue-incremental-step-size/README.md:52** by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-05T20:17:41Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
```

  > Out of curiosity, what "best practices" do you have in mind?
  
  Since you are providing them with a new configuration, they need to know how to use it safely. The 'best practices' that you will document will typically include the following:
  
  **Avoiding extreme values:**  Warning them against setting the `stepSize` to a massive number (such as the total number of clusters they have). You will clarify that doing so defeats the purpose of this feature and recreates the exact issues of high API load and race conditions

### Review by [@Mostafahassen1](https://github.com/Mostafahassen1) (commented) — 2026-05-05T20:36:08Z

- **keps/9270-multikueue-incremental-step-size/README.md:57** by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-05T20:36:08Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
```

  You're right. I knew it was currently 3, but I incorrectly assumed we should just default to the minimum value. Sticking with 3 makes much more sense so we maintain the exact same behavior for users when they upgrade.

### Review by [@Mostafahassen1](https://github.com/Mostafahassen1) (commented) — 2026-05-05T22:24:58Z

- **keps/9270-multikueue-incremental-step-size/README.md:69** by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-05T22:24:58Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
```

   I agree with these points. Sticking the step size next to the dispatcher globally makes much more sense.
  
  I'll update the KEP to:
  
  1-Move the field to the global Configuration struct.
  
  2-Rename it to `IncrementalDispatcherStepSize`.
  
  3-Add kubebuilder validation so it can only be used with the incremental dispatcher.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-05T23:00:40Z

- **keps/9270-multikueue-incremental-step-size/README.md:49** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T22:39:19Z
```diff
@@ -0,0 +1,127 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, the MultiKueue Incremental Dispatcher has a hardcoded batch size `batchSize := 3` when attempting to schedule a workload across multiple worker clusters. The primary limitation is that this value is not configurable. For administrators managing a massive fleet of worker clusters, a fixed batch size of 3 is too slow and delays job execution. Conversely, if administrators were to theoretically increase this to query all clusters simultaneously to speed things up, it would generate heavy API traffic and increase the risk of race conditions. By making this step size configurable, administrators can find the optimal balance between dispatch speed and Manager Cluster resource usage for their specific environment.
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:**An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
```

  `**Risk**` misrenders, probably due to a missing space after that.

- **keps/9270-multikueue-incremental-step-size/README.md:104** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T22:39:29Z
```diff
@@ -0,0 +1,119 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, when MultiKueue attempts to schedule a workload across multiple worker clusters, the Incremental Dispatcher has a limitation. If it checks clusters one by one, the process is too slow and delays job execution. If it attempts to dispatch to all clusters simultaneously, it generates unnecessary API traffic, increases the load on the Manager Cluster, and can lead to race conditions where multiple clusters try to admit the same job at the same time.
+
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+* Ensure that if a workload is admitted in a batch, the remaining pending proxy workloads in that batch are cleanly removed.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic (which deletes duplicate proxy workloads once one is admitted) will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy. 
+
+## Design Details
+
+### API Changes
+We will update the `MultiKueueConfigSpec` struct to include the new field. This change will be backwards compatible by defaulting the step size to `1` (which matches the current one-by-one behavior).
+
+```go
+type MultiKueueConfigSpec struct {
+    // Other existing fields...
+
+    // StepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query at the same time. 
+    // Minimum value is 1. If not set, it defaults to 1.
+    // +optional
+    // +kubebuilder:default=1
+    // +kubebuilder:validation:Minimum=1
+    StepSize *int32 `json:"stepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change will take place inside the MultiKueue Workload Controller's reconciliation loop:
+1. When a workload is ready for dispatch, the controller reads the `stepSize` from the `MultiKueueConfig`.
+2. The controller takes the full list of available worker clusters and divides them into groups (batches) equal to the `stepSize`.
+3. The controller loops through the first batch, creating proxy workloads on those specific worker clusters simultaneously.
+4. If a cluster in the batch admits the job, the controller immediately deletes the proxy workloads from the other clusters in that same batch and stops the loop.
+5. If no cluster in the batch admits the job, the controller moves on to the next batch of clusters.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: `<date>` - Verify that the default value remains `1` if the user does not specify a `stepSize`.
+
+#### Integration tests
+
+- `TestIncrementalDispatcherStepSize`: Simulate a Manager Cluster with 5 Worker Clusters. Set `stepSize` to `2`. Submit a workload and verify via the API server that exactly 2 proxy workloads are created in the first step. Simulate a rejection on the first 2 clusters, and verify that the controller correctly creates the next 2 proxy workloads in the second step.
+
+#### e2e tests
+
+Standard E2E multi-cluster tests will be updated to include an iteration where `MultiKueueConfig` is deployed with a `stepSize > 1` to ensure workloads execute successfully end-to-end without duplication.
+
+### Graduation Criteria
+
+* **Alpha:** * API fields added.
```

  Thanks for fixing here - but it's still there for "Beta".

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-05T23:12:47Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:28** by [@olekzabl](https://github.com/olekzabl) — 2026-05-05T23:11:07Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.19"
+  beta: "v0.20"
+  stable: "v0.21"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: MultiKueueStepSize
```

  I'm not sure if it's strictly required, but how about explicitly mentioning this FG anywhere in the KEP text?
  
  Perhaps the best place would be the `//` comment above your new field - disclaiming that the field is going to be ignored (right?) when the FG is disabled.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T12:43:53Z

LGTM; I just have one more round of tiny comments.

Now I'm feeling we're just blocked by [this thread](https://github.com/kubernetes-sigs/kueue/pull/10877#issuecomment-4383767843).

- **keps/9270-multikueue-incremental-step-size/kep.yaml:35** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:35:24Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.9"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.19"
+  beta: "v0.20"
+  stable: "v0.21"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: MultiKueueStepSize
+    components:
+      - kueue-controller-manager
+disable-supported: true
+
+# The following PRR answers are required at beta release
+metrics:
+  - kueue_multikueue_dispatched_workloads_total
```

  I'm not sure if we need it; AFAICS the last KEP featuring a metric here was KEP-1282 (>30 KEPs ago).
  
  (If, however, you claim we should have it, then I think it should be somehow described in the KEP - which I don't see)

- **keps/9270-multikueue-incremental-step-size/kep.yaml:17** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:35:33Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - TBD
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.9"
```

  This probably needs an update.

- **keps/9270-multikueue-incremental-step-size/kep.yaml:8** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:36:12Z
```diff
@@ -0,0 +1,35 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - TBD
```

  ```suggestion
    - olekzabl
  ```
  
  :)

- **keps/9270-multikueue-incremental-step-size/README.md:61** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:38:38Z
```diff
@@ -0,0 +1,128 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, the MultiKueue Incremental Dispatcher has a hardcoded batch size `batchSize := 3` when attempting to schedule a workload across multiple worker clusters. The primary limitation is that this value is not configurable. For administrators managing a massive fleet of worker clusters, a fixed batch size of 3 is too slow and delays job execution. Conversely, if administrators were to theoretically increase this to query all clusters simultaneously to speed things up, it would generate heavy API traffic and increase the risk of race conditions. By making this step size configurable, administrators can find the optimal balance between dispatch speed and Manager Cluster resource usage for their specific environment.
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy.
+
+## Design Details
+
+### API Changes
+
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+
+The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+
+We will also use Kubebuilder validation markers (e.g., `CEL` rules or `+union` discriminators depending on the struct setup) to enforce that this field can only be populated if `DispatcherName` is set to the incremental dispatcher.
```

  Nit: I think the "struct setup" is already known.

- **keps/9270-multikueue-incremental-step-size/README.md:101** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:39:11Z
```diff
@@ -0,0 +1,128 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, the MultiKueue Incremental Dispatcher has a hardcoded batch size `batchSize := 3` when attempting to schedule a workload across multiple worker clusters. The primary limitation is that this value is not configurable. For administrators managing a massive fleet of worker clusters, a fixed batch size of 3 is too slow and delays job execution. Conversely, if administrators were to theoretically increase this to query all clusters simultaneously to speed things up, it would generate heavy API traffic and increase the risk of race conditions. By making this step size configurable, administrators can find the optimal balance between dispatch speed and Manager Cluster resource usage for their specific environment.
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy.
+
+## Design Details
+
+### API Changes
+
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+
+The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+
+We will also use Kubebuilder validation markers (e.g., `CEL` rules or `+union` discriminators depending on the struct setup) to enforce that this field can only be populated if `DispatcherName` is set to the incremental dispatcher.
+
+```go
+type MultiKueue struct {
+    // Other existing fields...
+    // DispatcherName defines the type of dispatcher to use.
+    DispatcherName *string `json:"dispatcherName,omitempty"`
+
+    // IncrementalDispatcherStepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query simultaneously. 
+    // This field is only valid when DispatcherName is set to the incremental dispatcher.
+	// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
+    // Minimum value is 1. If not set, it defaults to 3.
+    // +optional
+    // +kubebuilder:default=3
+    // +kubebuilder:validation:Minimum=1
+    IncrementalDispatcherStepSize *int32 `json:"incrementalDispatcherStepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change is minimal because the batching mechanism is already implemented in the Incremental Dispatcher (`pkg/controller/workloaddispatcher/incrementaldispatcher.go`).
+
+The update will simply modify the existing logic to:
+1. Read the `stepSize` value from the `MultiKueueConfig`.
+2. Replace the hardcoded `batchSize := 3` variable with this dynamically configured `stepSize` parameter.
+
+The existing loop and conflict resolution mechanisms will naturally handle the rest of the execution based on this new size, meaning no major architectural changes are required to the dispatching loop itself.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: Verify that the default value remains `1` if the user does not specify a `stepSize`.
```

  ```suggestion
  - `pkg/controller/core/workload_controller_test.go`: Verify that the default value remains `3` if the user does not specify a `stepSize`.
  ```

- **keps/9270-multikueue-incremental-step-size/README.md:123** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T12:39:58Z
```diff
@@ -0,0 +1,128 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, the MultiKueue Incremental Dispatcher has a hardcoded batch size `batchSize := 3` when attempting to schedule a workload across multiple worker clusters. The primary limitation is that this value is not configurable. For administrators managing a massive fleet of worker clusters, a fixed batch size of 3 is too slow and delays job execution. Conversely, if administrators were to theoretically increase this to query all clusters simultaneously to speed things up, it would generate heavy API traffic and increase the risk of race conditions. By making this step size configurable, administrators can find the optimal balance between dispatch speed and Manager Cluster resource usage for their specific environment.
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy.
+
+## Design Details
+
+### API Changes
+
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+
+The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+
+We will also use Kubebuilder validation markers (e.g., `CEL` rules or `+union` discriminators depending on the struct setup) to enforce that this field can only be populated if `DispatcherName` is set to the incremental dispatcher.
+
+```go
+type MultiKueue struct {
+    // Other existing fields...
+    // DispatcherName defines the type of dispatcher to use.
+    DispatcherName *string `json:"dispatcherName,omitempty"`
+
+    // IncrementalDispatcherStepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query simultaneously. 
+    // This field is only valid when DispatcherName is set to the incremental dispatcher.
+	// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
+    // Minimum value is 1. If not set, it defaults to 3.
+    // +optional
+    // +kubebuilder:default=3
+    // +kubebuilder:validation:Minimum=1
+    IncrementalDispatcherStepSize *int32 `json:"incrementalDispatcherStepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change is minimal because the batching mechanism is already implemented in the Incremental Dispatcher (`pkg/controller/workloaddispatcher/incrementaldispatcher.go`).
+
+The update will simply modify the existing logic to:
+1. Read the `stepSize` value from the `MultiKueueConfig`.
+2. Replace the hardcoded `batchSize := 3` variable with this dynamically configured `stepSize` parameter.
+
+The existing loop and conflict resolution mechanisms will naturally handle the rest of the execution based on this new size, meaning no major architectural changes are required to the dispatching loop itself.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: Verify that the default value remains `1` if the user does not specify a `stepSize`.
+
+#### Integration tests
+
+- `TestIncrementalDispatcherStepSize`: Simulate a Manager Cluster with 5 Worker Clusters. Set `stepSize` to `2`. Submit a workload and verify via the API server that exactly 2 proxy workloads are created in the first step. Simulate a rejection on the first 2 clusters, and verify that the controller correctly creates the next 2 proxy workloads in the second step.
+
+#### e2e tests
+
+Standard E2E multi-cluster tests will be updated to include an iteration where `MultiKueueConfig` is deployed with a `stepSize > 1` to ensure workloads execute successfully end-to-end without duplication.
+
+### Graduation Criteria
+
+* **Alpha:**
+    * API fields added.
+    * Workload Controller updated to support batching.
+    * Unit tests and EnvTest integration tests passing.
+* **Beta:** * Gather feedback from the community.
```

  ```suggestion
  * **Beta:**
      * Gather feedback from the community.
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-06T15:50:19Z

- **keps/9270-multikueue-incremental-step-size/README.md:123** by [@olekzabl](https://github.com/olekzabl) — 2026-05-06T15:50:14Z
```diff
@@ -0,0 +1,128 @@
+ 
+# KEP-9270: MultiKueue Incremental Dispatcher Step Size
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [API Changes](#api-changes)
+  - [Execution Loop](#execution-loop)
+  - [Test Plan](#test-plan)
+    - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+    - [e2e tests](#e2e-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+<!-- /toc -->
+
+## Summary
+
+This proposal introduces a new configuration field called `stepSize` to the MultiKueue Incremental Dispatcher. This setting will control the number of worker clusters that the Manager Cluster will attempt to dispatch a workload to concurrently.
+
+## Motivation
+
+Currently, the MultiKueue Incremental Dispatcher has a hardcoded batch size `batchSize := 3` when attempting to schedule a workload across multiple worker clusters. The primary limitation is that this value is not configurable. For administrators managing a massive fleet of worker clusters, a fixed batch size of 3 is too slow and delays job execution. Conversely, if administrators were to theoretically increase this to query all clusters simultaneously to speed things up, it would generate heavy API traffic and increase the risk of race conditions. By making this step size configurable, administrators can find the optimal balance between dispatch speed and Manager Cluster resource usage for their specific environment.
+Introducing a `stepSize` allows cluster administrators to configure a "batch size" for this process, finding the perfect balance between speed and system stability.
+
+### Goals
+
+* Add a `stepSize` parameter to the `MultiKueueConfig` API.
+* Update the Workload Controller's dispatching loop to process worker clusters in batches defined by the `stepSize`.
+
+### Non-Goals
+
+* We will not change the way worker clusters are scored or prioritized.
+* We will not alter the `AllAtOnce` dispatcher behavior, as this proposal strictly targets the Incremental Dispatcher.
+
+## Proposal
+
+We propose updating the `MultiKueueConfig` API to accept an optional integer for the step size, and updating the Workload Controller to respect this batch limit when iterating through the list of available worker clusters.
+
+
+### Risks and Mitigations
+
+**Risk:** An administrator might set the `stepSize` to an extremely high number (e.g., equal to the total number of clusters), which effectively recreates the heavy load issues of the `AllAtOnce` dispatcher and increases the chance of race conditions where multiple clusters admit the job at the exact same time.
+
+**Mitigation:** Provide clear API documentation regarding best practices. The Workload Controller's conflict resolution logic will naturally handle race conditions, just as it already does for the `AllAtOnce` strategy.
+
+## Design Details
+
+### API Changes
+
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+
+The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+
+We will also use Kubebuilder validation markers (e.g., `CEL` rules or `+union` discriminators depending on the struct setup) to enforce that this field can only be populated if `DispatcherName` is set to the incremental dispatcher.
+
+```go
+type MultiKueue struct {
+    // Other existing fields...
+    // DispatcherName defines the type of dispatcher to use.
+    DispatcherName *string `json:"dispatcherName,omitempty"`
+
+    // IncrementalDispatcherStepSize defines the number of worker clusters the Incremental 
+    // Dispatcher will query simultaneously. 
+    // This field is only valid when DispatcherName is set to the incremental dispatcher.
+	// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
+    // Minimum value is 1. If not set, it defaults to 3.
+    // +optional
+    // +kubebuilder:default=3
+    // +kubebuilder:validation:Minimum=1
+    IncrementalDispatcherStepSize *int32 `json:"incrementalDispatcherStepSize,omitempty"`
+}
+```
+
+### Execution Loop
+The core logic change is minimal because the batching mechanism is already implemented in the Incremental Dispatcher (`pkg/controller/workloaddispatcher/incrementaldispatcher.go`).
+
+The update will simply modify the existing logic to:
+1. Read the `stepSize` value from the `MultiKueueConfig`.
+2. Replace the hardcoded `batchSize := 3` variable with this dynamically configured `stepSize` parameter.
+
+The existing loop and conflict resolution mechanisms will naturally handle the rest of the execution based on this new size, meaning no major architectural changes are required to the dispatching loop itself.
+
+### Test Plan
+
+ I/we understand the owners of the involved components may require updates to existing tests to make this code solid enough prior to committing the changes necessary to implement this enhancement.
+
+#### Prerequisite testing updates
+
+No major prerequisite testing updates are required. Current mock worker cluster frameworks in EnvTest are sufficient.
+
+#### Unit tests
+
+- `pkg/controller/core/workload_controller_test.go`: Verify that the worker cluster list is correctly divided into chunks matching the `stepSize`.
+- `pkg/controller/core/workload_controller_test.go`: Verify that the default value remains `1` if the user does not specify a `stepSize`.
+
+#### Integration tests
+
+- `TestIncrementalDispatcherStepSize`: Simulate a Manager Cluster with 5 Worker Clusters. Set `stepSize` to `2`. Submit a workload and verify via the API server that exactly 2 proxy workloads are created in the first step. Simulate a rejection on the first 2 clusters, and verify that the controller correctly creates the next 2 proxy workloads in the second step.
+
+#### e2e tests
+
+Standard E2E multi-cluster tests will be updated to include an iteration where `MultiKueueConfig` is deployed with a `stepSize > 1` to ensure workloads execute successfully end-to-end without duplication.
+
+### Graduation Criteria
+
+* **Alpha:**
+    * API fields added.
+    * Workload Controller updated to support batching.
+    * Unit tests and EnvTest integration tests passing.
+* **Beta:** * Gather feedback from the community.
```

  This still misrenders; perhaps you have some auto-formatter doing it?
  (If so, consider adding blank lines to prevent this).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:24:35Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:1** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:24:35Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
```

  Align the title with KEP title

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:24:42Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:11** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:24:42Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - olekzabl
+approvers:
+  - TBD
```

  add me here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:24:49Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:17** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:24:49Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - olekzabl
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.17"
```

  0.18 or 0.19

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:25:09Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:22** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:25:09Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - olekzabl
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.17"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.19"
```

  align with latest-version

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:25:23Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:23** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:25:24Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - olekzabl
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.17"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.19"
+  beta: "v0.20"
+  stable: "v0.21"
```

  replace with TBD for now

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-07T10:25:48Z

- **keps/9270-multikueue-incremental-step-size/kep.yaml:28** by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:25:48Z
```diff
@@ -0,0 +1,32 @@
+title: MultiKueue Incremental Dispatcher Step Size
+kep-number: 9270
+authors:
+  - "@Mostafahassen1"
+status: provisional
+creation-date: "2026-05-01"
+reviewers:
+  - olekzabl
+approvers:
+  - TBD
+
+# The target maturity stage in the current dev cycle for this KEP.
+stage: alpha
+
+# The most recent milestone for which work toward delivery of this KEP has been
+# done. Kueue typically uses 0.x versioning.
+latest-milestone: "v0.17"
+
+# The milestone at which this feature was, or is targeted to be, at each stage.
+milestone:
+  alpha: "v0.19"
+  beta: "v0.20"
+  stable: "v0.21"
+
+# The following PRR answers are required at alpha release
+# List the feature gate name and the components for which it must be enabled
+feature-gates:
+  - name: MultiKueueStepSize
```

  Update the FG name to MultiKueueIncrementalDispatcherConfig

### Review by [@olekzabl](https://github.com/olekzabl) (approved) — 2026-05-07T11:00:55Z

LGTM!

- **keps/9270-multikueue-incremental-step-size/README.md:73** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T10:59:33Z
```diff
@@ -54,27 +54,33 @@ We propose updating the `MultiKueueConfig` API to accept an optional integer for
 
 ### API Changes
 
-We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include a new configuration struct, rather than placing it in `MultiKueueConfigSpec`. This keeps the incremental dispatcher configuration co-located with the `DispatcherName` selection.
 
-The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+The field will be named `IncrementalDispatcherConfig` and will contain a `StepSize` field. To ensure backwards compatibility and preserve existing system behavior, the default value for `StepSize` will be `3`.
 
-We will also use Kubebuilder validation markers (e.g., CEL rules) to enforce that this field can only be populated if DispatcherName is set to the incremental dispatcher.
+We will also use Kubebuilder validation markers (e.g., CEL rules) to enforce that this struct can only be populated if `DispatcherName` is set to the incremental dispatcher.
 
 ```go
 type MultiKueue struct {
-    // Other existing fields...
-    // DispatcherName defines the type of dispatcher to use.
-    DispatcherName *string `json:"dispatcherName,omitempty"`
-
-    // IncrementalDispatcherStepSize defines the number of worker clusters the Incremental 
-    // Dispatcher will query simultaneously. 
-    // This field is only valid when DispatcherName is set to the incremental dispatcher.
-	// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
-    // Minimum value is 1. If not set, it defaults to 3.
-    // +optional
-    // +kubebuilder:default=3
-    // +kubebuilder:validation:Minimum=1
-    IncrementalDispatcherStepSize *int32 `json:"incrementalDispatcherStepSize,omitempty"`
+// Other existing fields...
+// DispatcherName defines the type of dispatcher to use.
+DispatcherName *string `json:"dispatcherName,omitempty"`
+
+// IncrementalDispatcherConfig contains the configuration for the incremental dispatcher.
+// This field is only valid when DispatcherName is set to the incremental dispatcher.
+// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
+// +optional
+IncrementalDispatcherConfig *IncrementalDispatcherConfig `json:"incrementalDispatcherConfig,omitempty"`
```

  Nit: restore the indent

- **keps/9270-multikueue-incremental-step-size/README.md:71** by [@olekzabl](https://github.com/olekzabl) — 2026-05-07T11:00:11Z
```diff
@@ -54,27 +54,33 @@ We propose updating the `MultiKueueConfig` API to accept an optional integer for
 
 ### API Changes
 
-We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include the new field, rather than placing it in `MultiKueueConfigSpec`. This keeps the step size configuration co-located with the `DispatcherName` selection.
+We will update the global `Configuration` API (specifically the `MultiKueue` struct) to include a new configuration struct, rather than placing it in `MultiKueueConfigSpec`. This keeps the incremental dispatcher configuration co-located with the `DispatcherName` selection.
 
-The field will be named `IncrementalDispatcherStepSize` for clarity. To ensure backwards compatibility and preserve existing system behavior, the default value will be `3`.
+The field will be named `IncrementalDispatcherConfig` and will contain a `StepSize` field. To ensure backwards compatibility and preserve existing system behavior, the default value for `StepSize` will be `3`.
 
-We will also use Kubebuilder validation markers (e.g., CEL rules) to enforce that this field can only be populated if DispatcherName is set to the incremental dispatcher.
+We will also use Kubebuilder validation markers (e.g., CEL rules) to enforce that this struct can only be populated if `DispatcherName` is set to the incremental dispatcher.
 
 ```go
 type MultiKueue struct {
-    // Other existing fields...
-    // DispatcherName defines the type of dispatcher to use.
-    DispatcherName *string `json:"dispatcherName,omitempty"`
-
-    // IncrementalDispatcherStepSize defines the number of worker clusters the Incremental 
-    // Dispatcher will query simultaneously. 
-    // This field is only valid when DispatcherName is set to the incremental dispatcher.
-	// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
-    // Minimum value is 1. If not set, it defaults to 3.
-    // +optional
-    // +kubebuilder:default=3
-    // +kubebuilder:validation:Minimum=1
-    IncrementalDispatcherStepSize *int32 `json:"incrementalDispatcherStepSize,omitempty"`
+// Other existing fields...
+// DispatcherName defines the type of dispatcher to use.
+DispatcherName *string `json:"dispatcherName,omitempty"`
+
+// IncrementalDispatcherConfig contains the configuration for the incremental dispatcher.
+// This field is only valid when DispatcherName is set to the incremental dispatcher.
+// Note: This field is going to be ignored when the MultiKueueStepSize feature gate is disabled.
```

  Don't forget to update the FG name also here
