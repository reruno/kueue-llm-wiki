# PR #10903: Add prefferedDuringScheduling node affinity support for TAS

**Summary**: Add prefferedDuringScheduling node affinity support for TAS

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10903

**Last updated**: 2026-05-18T06:18:21Z

---

## Metadata

- **State**: open
- **Author**: [@j-skiba](https://github.com/j-skiba)
- **Branch**: `add-preferred-during-scheduling-support-tas` → `main`
- **Created**: 2026-05-04T09:04:22Z
- **Updated**: 2026-05-18T06:18:21Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note`, `size/XXL`, `approved`, `do-not-merge/hold`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd)
- **Changed files**: 12   **+1323 / -8**

## Description

#### What type of PR is this?
/kind feature
/area tas

#### What this PR does / why we need it:
This PR adds support for `preferredDuringSchedulingIgnoredDuringExecution` node affinity in TAS.
The preferred node affinity takes precedence over capacity fitness.
The feature is hidden behind `TASPreferredSchedulingAffinity` feature gate.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of #10902

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: Added support for `preferredDuringSchedulingIgnoredDuringExecution` node affinity. This feature is currently in alpha and is guarded by the `TASPreferredSchedulingAffinity` feature gate.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-04T09:04:25Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-04T09:04:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d8403de29be773e9038d0c37ed9f71e6a6832584 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0aaef2811fed0008e8364f |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-12T15:02:05Z

/test pull-kueue-test-e2e-tas-extended-main

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-13T09:20:59Z

LGTM, left remaining comments. 

cc @tenzen-y in case you would like to review the KEP updates here

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T11:02:34Z

LGTM label has been added.  <details>Git tree hash: 9d6608a63f3720439104d494e33b698b28c4d13b</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T11:02:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10903#" title="Author self-approved">j-skiba</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10903#pullrequestreview-4297570917" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T06:17:24Z

New changes are detected. LGTM label has been removed.

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T10:47:15Z

- **pkg/features/kube_features.go:390** by [@mimowo](https://github.com/mimowo) — 2026-05-12T10:47:15Z
```diff
@@ -382,6 +382,12 @@ const (
 
 	// Enable recording of WorkloadCreationLatency metric.
 	MetricForWorkloadCreationLatency featuregate.Feature = "MetricForWorkloadCreationLatency"
+
+	// owner: @j-skiba
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/10902
+	// Enable evaluation of preferredDuringSchedulingIgnoredDuringExecution node affinities during scheduling in TAS.
+	// The preferred affinity will take precedence over capacity fitness of a domain.
+	TASPreferredSchedulingAffinity featuregate.Feature = "TASPreferredSchedulingAffinity"
```

  As I synced with @j-skiba and @mwielgus this solution might be in the future generalized by the score mixing coming from the domain affinity and placement policy.
  
  We are currently designing the more generic solution, and this FG is the easy approach to collect early feedback from users.
  
  I would like to have this FG reflected in the KEP and the plan about its graduation or decomissioning

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T11:44:14Z

- **pkg/features/kube_features.go:390** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T11:44:14Z
```diff
@@ -382,6 +382,12 @@ const (
 
 	// Enable recording of WorkloadCreationLatency metric.
 	MetricForWorkloadCreationLatency featuregate.Feature = "MetricForWorkloadCreationLatency"
+
+	// owner: @j-skiba
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/10902
+	// Enable evaluation of preferredDuringSchedulingIgnoredDuringExecution node affinities during scheduling in TAS.
+	// The preferred affinity will take precedence over capacity fitness of a domain.
+	TASPreferredSchedulingAffinity featuregate.Feature = "TASPreferredSchedulingAffinity"
```

  updated the KEP

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T11:55:59Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1607** by [@mimowo](https://github.com/mimowo) — 2026-05-12T11:55:59Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
+	var compiledTerms []compiledPreferredTerm
+	if features.Enabled(features.TASPreferredSchedulingAffinity) {
+		compiledTerms = make([]compiledPreferredTerm, 0, len(requirements.preferredNodeAffinity))
+		for _, term := range requirements.preferredNodeAffinity {
+			if term.Preference.MatchExpressions == nil && term.Preference.MatchFields == nil {
+				continue
+			}
+			if nodeSelector, err := nodeaffinity.NewNodeSelector(&corev1.NodeSelector{
+				NodeSelectorTerms: []corev1.NodeSelectorTerm{term.Preference},
+			}); err == nil {
```

  Do not silence the compilation errors, preferably return them up, or at least log

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T11:56:24Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1597** by [@mimowo](https://github.com/mimowo) — 2026-05-12T11:56:24Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
```

  Is there no structure in the core k8s that could be used to represent the terms with weights?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T11:56:52Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1597** by [@mimowo](https://github.com/mimowo) — 2026-05-12T11:56:52Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
```

  If not then at least move the struct decalation out of the function

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T11:57:15Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1613** by [@mimowo](https://github.com/mimowo) — 2026-05-12T11:57:16Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
+	var compiledTerms []compiledPreferredTerm
+	if features.Enabled(features.TASPreferredSchedulingAffinity) {
+		compiledTerms = make([]compiledPreferredTerm, 0, len(requirements.preferredNodeAffinity))
+		for _, term := range requirements.preferredNodeAffinity {
+			if term.Preference.MatchExpressions == nil && term.Preference.MatchFields == nil {
+				continue
+			}
+			if nodeSelector, err := nodeaffinity.NewNodeSelector(&corev1.NodeSelector{
+				NodeSelectorTerms: []corev1.NodeSelectorTerm{term.Preference},
+			}); err == nil {
+				compiledTerms = append(compiledTerms, compiledPreferredTerm{
+					selector: nodeSelector,
+					weight:   int64(term.Weight),
+				})
+			}
+		}
```

  Extract to a helper function to offload the main function with technical details

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T11:57:51Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1671** by [@mimowo](https://github.com/mimowo) — 2026-05-12T11:57:51Z
```diff
@@ -1603,14 +1641,22 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 			}
 
 			// 3. Check Node against Affinity Node Selector
-			if requirements.affinitySelector != nil && !requirements.affinitySelector.Match(leaf.node.toNode()) {
+			nodeObj := leaf.node.toNode()
+			if requirements.affinitySelector != nil && !requirements.affinitySelector.Match(nodeObj) {
 				s.log.V(5).Info("excluding node that doesn't match requiredDuringSchedulingIgnoredDuringExecution affinity", "domainID", leaf.id)
 				state.stats.Affinity++
 				continue
 			}
+
+			// 4. Calculate Affinity Score
+			for _, term := range compiledTerms {
+				if term.selector.Match(nodeObj) {
+					leaf.affinityScore += term.weight
+				}
+			}
```

  wrap that in the new feature gate

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T12:00:55Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:909** by [@mimowo](https://github.com/mimowo) — 2026-05-12T12:00:56Z
```diff
@@ -737,3 +740,460 @@ func TestTruncateAssignment(t *testing.T) {
 		})
 	}
 }
+
+func TestFillInCountsAndSorting(t *testing.T) {
+	cases := map[string]struct {
+		levels            []string
+		nodes             []corev1.Node
+		preferredAffinity []corev1.PreferredSchedulingTerm
+		requests          resources.Requests
+		leaderRequests    *resources.Requests
+		testLeaderSort    bool
+		wantOrder         []string
+	}{
+		"sorted domains with preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"node-preferred", "node-other"},
+		},
+		"sorted domains with leader and preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:       resources.Requests{corev1.ResourceCPU: 1},
+			leaderRequests: &resources.Requests{corev1.ResourceCPU: 1},
+			testLeaderSort: true,
+			wantOrder:      []string{"node-preferred", "node-other"},
+		},
+		"affinity score propagation": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"rack-preferred", "rack-other"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.TASPreferredSchedulingAffinity, true)
+			_, log := utiltesting.ContextWithLog(t)
+			s := newTASFlavorSnapshot(log, "dummy", tc.levels, nil)
+			for _, node := range tc.nodes {
+				s.addNode(newNodeInfo(&node))
+			}
+			s.initialize()
+
+			s.fillInCounts(
+				&topologyAssignmentPodRequirements{
+					requests:              tc.requests,
+					leaderRequests:        tc.leaderRequests,
+					selector:              labels.Everything(),
+					preferredNodeAffinity: tc.preferredAffinity,
+				},
+				&findTopologyAssignmentState{
+					topologyAssignmentParameters: topologyAssignmentParameters{
+						count:     1,
+						sliceSize: 1,
+					},
+					stats: newExclusionStats(),
+				},
+			)
+
+			domains := make([]*domain, 0, len(s.domainsPerLevel[0]))
+			for _, d := range s.domainsPerLevel[0] {
+				domains = append(domains, d)
+			}
+
+			var gotDomains []*domain
+			if tc.testLeaderSort {
+				gotDomains = s.sortedDomainsWithLeader(domains, false)
+			} else {
+				gotDomains = s.sortedDomains(domains, false)
+			}
+
+			gotValues := make([]string, len(gotDomains))
+			for i, d := range gotDomains {
+				gotValues[i] = d.levelValues[0]
+			}
+
+			if diff := cmp.Diff(tc.wantOrder, gotValues); diff != "" {
+				t.Errorf("unexpected sorted domains (-want,+got): %s", diff)
+			}
+		})
+	}
+}
+
+func TestTASFindTopologyAssignments(t *testing.T) {
```

  Any reason to add the new test instead of extending the pre-existing one which it seems could cover that feature as well ;TestFindTopologyAssignments in tas_cache_test.go

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T12:01:51Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:1007** by [@mimowo](https://github.com/mimowo) — 2026-05-12T12:01:51Z
```diff
@@ -737,3 +740,460 @@ func TestTruncateAssignment(t *testing.T) {
 		})
 	}
 }
+
+func TestFillInCountsAndSorting(t *testing.T) {
+	cases := map[string]struct {
+		levels            []string
+		nodes             []corev1.Node
+		preferredAffinity []corev1.PreferredSchedulingTerm
+		requests          resources.Requests
+		leaderRequests    *resources.Requests
+		testLeaderSort    bool
+		wantOrder         []string
+	}{
+		"sorted domains with preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"node-preferred", "node-other"},
+		},
+		"sorted domains with leader and preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:       resources.Requests{corev1.ResourceCPU: 1},
+			leaderRequests: &resources.Requests{corev1.ResourceCPU: 1},
+			testLeaderSort: true,
+			wantOrder:      []string{"node-preferred", "node-other"},
+		},
+		"affinity score propagation": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"rack-preferred", "rack-other"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.TASPreferredSchedulingAffinity, true)
+			_, log := utiltesting.ContextWithLog(t)
+			s := newTASFlavorSnapshot(log, "dummy", tc.levels, nil)
+			for _, node := range tc.nodes {
+				s.addNode(newNodeInfo(&node))
+			}
+			s.initialize()
+
+			s.fillInCounts(
+				&topologyAssignmentPodRequirements{
+					requests:              tc.requests,
+					leaderRequests:        tc.leaderRequests,
+					selector:              labels.Everything(),
+					preferredNodeAffinity: tc.preferredAffinity,
+				},
+				&findTopologyAssignmentState{
+					topologyAssignmentParameters: topologyAssignmentParameters{
+						count:     1,
+						sliceSize: 1,
+					},
+					stats: newExclusionStats(),
+				},
+			)
+
+			domains := make([]*domain, 0, len(s.domainsPerLevel[0]))
+			for _, d := range s.domainsPerLevel[0] {
+				domains = append(domains, d)
+			}
+
+			var gotDomains []*domain
+			if tc.testLeaderSort {
+				gotDomains = s.sortedDomainsWithLeader(domains, false)
+			} else {
+				gotDomains = s.sortedDomains(domains, false)
+			}
+
+			gotValues := make([]string, len(gotDomains))
+			for i, d := range gotDomains {
+				gotValues[i] = d.levelValues[0]
+			}
+
+			if diff := cmp.Diff(tc.wantOrder, gotValues); diff != "" {
+				t.Errorf("unexpected sorted domains (-want,+got): %s", diff)
+			}
+		})
+	}
+}
+
+func TestTASFindTopologyAssignments(t *testing.T) {
+	cases := map[string]struct {
+		levels     []string
+		nodes      []corev1.Node
+		podSet     kueue.PodSet
+		requests   resources.Requests
+		wantDomain string
+	}{
+		"with preferred affinity": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+			},
+			podSet: kueue.PodSet{
+				Name: "main",
+				TopologyRequest: &kueue.PodSetTopologyRequest{
+					Preferred: new("rack"),
+				},
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Affinity: &corev1.Affinity{
+							NodeAffinity: &corev1.NodeAffinity{
+								PreferredDuringSchedulingIgnoredDuringExecution: []corev1.PreferredSchedulingTerm{
+									{
+										Weight: 10,
+										Preference: corev1.NodeSelectorTerm{
+											MatchExpressions: []corev1.NodeSelectorRequirement{
+												{
+													Key:      "region",
+													Operator: corev1.NodeSelectorOpIn,
+													Values:   []string{"us-west"},
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			requests:   resources.Requests{corev1.ResourceCPU: 1},
+			wantDomain: "node-preferred",
+		},
+		"with required and preferred affinity": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					Label("zone", "us").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					Label("zone", "us").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-excluded").
+					Label("rack", "rack-excluded").
+					Label("kubernetes.io/hostname", "node-excluded").
+					Label("region", "us-west").
+					Label("zone", "eu").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+			},
+			podSet: kueue.PodSet{
+				Name: "main",
+				TopologyRequest: &kueue.PodSetTopologyRequest{
+					Preferred: new("rack"),
+				},
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Affinity: &corev1.Affinity{
+							NodeAffinity: &corev1.NodeAffinity{
```

  Try to avoid such verbose declarations with the wrappers

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:42:30Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1607** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:42:30Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
+	var compiledTerms []compiledPreferredTerm
+	if features.Enabled(features.TASPreferredSchedulingAffinity) {
+		compiledTerms = make([]compiledPreferredTerm, 0, len(requirements.preferredNodeAffinity))
+		for _, term := range requirements.preferredNodeAffinity {
+			if term.Preference.MatchExpressions == nil && term.Preference.MatchFields == nil {
+				continue
+			}
+			if nodeSelector, err := nodeaffinity.NewNodeSelector(&corev1.NodeSelector{
+				NodeSelectorTerms: []corev1.NodeSelectorTerm{term.Preference},
+			}); err == nil {
```

  added v(5) error log

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:43:23Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1597** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:43:22Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
```

  changed to `*nodeaffinity.PreferredSchedulingTerms`

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:43:39Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1613** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:43:38Z
```diff
@@ -1574,7 +1588,31 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 		domain.sliceState = 0
 		domain.sliceStateWithLeader = 0
 		domain.leaderState = 0
+		domain.affinityScore = 0
 	}
+
+	type compiledPreferredTerm struct {
+		selector *nodeaffinity.NodeSelector
+		weight   int64
+	}
+	var compiledTerms []compiledPreferredTerm
+	if features.Enabled(features.TASPreferredSchedulingAffinity) {
+		compiledTerms = make([]compiledPreferredTerm, 0, len(requirements.preferredNodeAffinity))
+		for _, term := range requirements.preferredNodeAffinity {
+			if term.Preference.MatchExpressions == nil && term.Preference.MatchFields == nil {
+				continue
+			}
+			if nodeSelector, err := nodeaffinity.NewNodeSelector(&corev1.NodeSelector{
+				NodeSelectorTerms: []corev1.NodeSelectorTerm{term.Preference},
+			}); err == nil {
+				compiledTerms = append(compiledTerms, compiledPreferredTerm{
+					selector: nodeSelector,
+					weight:   int64(term.Weight),
+				})
+			}
+		}
```

  removed completely after moving to `*nodeaffinity.PreferredSchedulingTerms`

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:43:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1671** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:43:53Z
```diff
@@ -1603,14 +1641,22 @@ func (s *TASFlavorSnapshot) fillInCounts(requirements *topologyAssignmentPodRequ
 			}
 
 			// 3. Check Node against Affinity Node Selector
-			if requirements.affinitySelector != nil && !requirements.affinitySelector.Match(leaf.node.toNode()) {
+			nodeObj := leaf.node.toNode()
+			if requirements.affinitySelector != nil && !requirements.affinitySelector.Match(nodeObj) {
 				s.log.V(5).Info("excluding node that doesn't match requiredDuringSchedulingIgnoredDuringExecution affinity", "domainID", leaf.id)
 				state.stats.Affinity++
 				continue
 			}
+
+			// 4. Calculate Affinity Score
+			for _, term := range compiledTerms {
+				if term.selector.Match(nodeObj) {
+					leaf.affinityScore += term.weight
+				}
+			}
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:45:01Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:909** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:45:01Z
```diff
@@ -737,3 +740,460 @@ func TestTruncateAssignment(t *testing.T) {
 		})
 	}
 }
+
+func TestFillInCountsAndSorting(t *testing.T) {
+	cases := map[string]struct {
+		levels            []string
+		nodes             []corev1.Node
+		preferredAffinity []corev1.PreferredSchedulingTerm
+		requests          resources.Requests
+		leaderRequests    *resources.Requests
+		testLeaderSort    bool
+		wantOrder         []string
+	}{
+		"sorted domains with preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"node-preferred", "node-other"},
+		},
+		"sorted domains with leader and preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:       resources.Requests{corev1.ResourceCPU: 1},
+			leaderRequests: &resources.Requests{corev1.ResourceCPU: 1},
+			testLeaderSort: true,
+			wantOrder:      []string{"node-preferred", "node-other"},
+		},
+		"affinity score propagation": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"rack-preferred", "rack-other"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.TASPreferredSchedulingAffinity, true)
+			_, log := utiltesting.ContextWithLog(t)
+			s := newTASFlavorSnapshot(log, "dummy", tc.levels, nil)
+			for _, node := range tc.nodes {
+				s.addNode(newNodeInfo(&node))
+			}
+			s.initialize()
+
+			s.fillInCounts(
+				&topologyAssignmentPodRequirements{
+					requests:              tc.requests,
+					leaderRequests:        tc.leaderRequests,
+					selector:              labels.Everything(),
+					preferredNodeAffinity: tc.preferredAffinity,
+				},
+				&findTopologyAssignmentState{
+					topologyAssignmentParameters: topologyAssignmentParameters{
+						count:     1,
+						sliceSize: 1,
+					},
+					stats: newExclusionStats(),
+				},
+			)
+
+			domains := make([]*domain, 0, len(s.domainsPerLevel[0]))
+			for _, d := range s.domainsPerLevel[0] {
+				domains = append(domains, d)
+			}
+
+			var gotDomains []*domain
+			if tc.testLeaderSort {
+				gotDomains = s.sortedDomainsWithLeader(domains, false)
+			} else {
+				gotDomains = s.sortedDomains(domains, false)
+			}
+
+			gotValues := make([]string, len(gotDomains))
+			for i, d := range gotDomains {
+				gotValues[i] = d.levelValues[0]
+			}
+
+			if diff := cmp.Diff(tc.wantOrder, gotValues); diff != "" {
+				t.Errorf("unexpected sorted domains (-want,+got): %s", diff)
+			}
+		})
+	}
+}
+
+func TestTASFindTopologyAssignments(t *testing.T) {
```

  oh, I thought just to simply have it there as changes were made in tas_flavor_snapshot.go.
  
  I moved the tests to tas_cache_test.go now

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T13:46:09Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:1007** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T13:46:09Z
```diff
@@ -737,3 +740,460 @@ func TestTruncateAssignment(t *testing.T) {
 		})
 	}
 }
+
+func TestFillInCountsAndSorting(t *testing.T) {
+	cases := map[string]struct {
+		levels            []string
+		nodes             []corev1.Node
+		preferredAffinity []corev1.PreferredSchedulingTerm
+		requests          resources.Requests
+		leaderRequests    *resources.Requests
+		testLeaderSort    bool
+		wantOrder         []string
+	}{
+		"sorted domains with preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"node-preferred", "node-other"},
+		},
+		"sorted domains with leader and preferred affinity": {
+			levels: []string{"kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("2"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:       resources.Requests{corev1.ResourceCPU: 1},
+			leaderRequests: &resources.Requests{corev1.ResourceCPU: 1},
+			testLeaderSort: true,
+			wantOrder:      []string{"node-preferred", "node-other"},
+		},
+		"affinity score propagation": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU: resource.MustParse("1"),
+					}).Obj(),
+			},
+			preferredAffinity: []corev1.PreferredSchedulingTerm{
+				{
+					Weight: 10,
+					Preference: corev1.NodeSelectorTerm{
+						MatchExpressions: []corev1.NodeSelectorRequirement{
+							{
+								Key:      "region",
+								Operator: corev1.NodeSelectorOpIn,
+								Values:   []string{"us-west"},
+							},
+						},
+					},
+				},
+			},
+			requests:  resources.Requests{corev1.ResourceCPU: 1},
+			wantOrder: []string{"rack-preferred", "rack-other"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			features.SetFeatureGateDuringTest(t, features.TASPreferredSchedulingAffinity, true)
+			_, log := utiltesting.ContextWithLog(t)
+			s := newTASFlavorSnapshot(log, "dummy", tc.levels, nil)
+			for _, node := range tc.nodes {
+				s.addNode(newNodeInfo(&node))
+			}
+			s.initialize()
+
+			s.fillInCounts(
+				&topologyAssignmentPodRequirements{
+					requests:              tc.requests,
+					leaderRequests:        tc.leaderRequests,
+					selector:              labels.Everything(),
+					preferredNodeAffinity: tc.preferredAffinity,
+				},
+				&findTopologyAssignmentState{
+					topologyAssignmentParameters: topologyAssignmentParameters{
+						count:     1,
+						sliceSize: 1,
+					},
+					stats: newExclusionStats(),
+				},
+			)
+
+			domains := make([]*domain, 0, len(s.domainsPerLevel[0]))
+			for _, d := range s.domainsPerLevel[0] {
+				domains = append(domains, d)
+			}
+
+			var gotDomains []*domain
+			if tc.testLeaderSort {
+				gotDomains = s.sortedDomainsWithLeader(domains, false)
+			} else {
+				gotDomains = s.sortedDomains(domains, false)
+			}
+
+			gotValues := make([]string, len(gotDomains))
+			for i, d := range gotDomains {
+				gotValues[i] = d.levelValues[0]
+			}
+
+			if diff := cmp.Diff(tc.wantOrder, gotValues); diff != "" {
+				t.Errorf("unexpected sorted domains (-want,+got): %s", diff)
+			}
+		})
+	}
+}
+
+func TestTASFindTopologyAssignments(t *testing.T) {
+	cases := map[string]struct {
+		levels     []string
+		nodes      []corev1.Node
+		podSet     kueue.PodSet
+		requests   resources.Requests
+		wantDomain string
+	}{
+		"with preferred affinity": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+			},
+			podSet: kueue.PodSet{
+				Name: "main",
+				TopologyRequest: &kueue.PodSetTopologyRequest{
+					Preferred: new("rack"),
+				},
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Affinity: &corev1.Affinity{
+							NodeAffinity: &corev1.NodeAffinity{
+								PreferredDuringSchedulingIgnoredDuringExecution: []corev1.PreferredSchedulingTerm{
+									{
+										Weight: 10,
+										Preference: corev1.NodeSelectorTerm{
+											MatchExpressions: []corev1.NodeSelectorRequirement{
+												{
+													Key:      "region",
+													Operator: corev1.NodeSelectorOpIn,
+													Values:   []string{"us-west"},
+												},
+											},
+										},
+									},
+								},
+							},
+						},
+					},
+				},
+			},
+			requests:   resources.Requests{corev1.ResourceCPU: 1},
+			wantDomain: "node-preferred",
+		},
+		"with required and preferred affinity": {
+			levels: []string{"rack", "kubernetes.io/hostname"},
+			nodes: []corev1.Node{
+				*node.MakeNode("node-preferred").
+					Label("rack", "rack-preferred").
+					Label("kubernetes.io/hostname", "node-preferred").
+					Label("region", "us-west").
+					Label("zone", "us").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-other").
+					Label("rack", "rack-other").
+					Label("kubernetes.io/hostname", "node-other").
+					Label("region", "us-east").
+					Label("zone", "us").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+				*node.MakeNode("node-excluded").
+					Label("rack", "rack-excluded").
+					Label("kubernetes.io/hostname", "node-excluded").
+					Label("region", "us-west").
+					Label("zone", "eu").
+					StatusAllocatable(corev1.ResourceList{
+						corev1.ResourceCPU:  resource.MustParse("1"),
+						corev1.ResourcePods: resource.MustParse("10"),
+					}).Obj(),
+			},
+			podSet: kueue.PodSet{
+				Name: "main",
+				TopologyRequest: &kueue.PodSetTopologyRequest{
+					Preferred: new("rack"),
+				},
+				Template: corev1.PodTemplateSpec{
+					Spec: corev1.PodSpec{
+						Affinity: &corev1.Affinity{
+							NodeAffinity: &corev1.NodeAffinity{
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:12:38Z

- **keps/2724-topology-aware-scheduling/README.md:1760** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:12:38Z
```diff
@@ -1747,6 +1749,25 @@ For a 3-level topology (block, rack, hostname), in the TopologyRequest, the user
 | [[10, 5], [5, 5, 5]] | 15|1 |  [[0, 0], [5, 5, 5]] | prefer more balanced rack
 | [[15],[15]] [[15, 15]] | 25 | 1 | [[0],[0]] [[13, 12]] | prefer block where the request fits in a single rack
 | [[15], [15], [15, 15]] | 25|5 |  [[0], [0], [15, 10]] | `podset-slice-required-topology = hostname`
+
+### Support for Preferred Node Affinity
+
+TAS supports evaluating `preferredDuringSchedulingIgnoredDuringExecution`
+node affinities during the scheduling cycle. When the
+`TASPreferredSchedulingAffinity` feature gate is enabled, the preferred
+affinity scores are evaluated when selecting candidate topology domains,
+allowing affinity preferences to guide placement decisions. Preferred affinity
+takes precedence over capacity fitness of a domain.
```

  ```suggestion
  In 0.18 we introduce the pilot support for domain affinity based on `preferredDuringSchedulingIgnoredDuringExecution`, behind the 
  `TASRespectNodeAffinityPreferred` feature gate (alpha).
  
  When the feature gate is enabled, we compute the "domain affinity scores" at all levels by
  aggregating (summing) the scores from child topology domains.
  
  Then, the "domain affinity scores" takes precedence over standard placement ordering of domains (based on BestFit or LeastFreeCapacity policies).
  
  This feature only works when the lowest topology level specified in the Topology CRD is `kubernetes.io/hostname`.
  ```
  To include the key informations:
  - propose to rename as TASRespectNodeAffinityPreferred to reflect we are "respecting" api which is already there in API
  - the version when the new semantic is introduced
  - the mechanism of aggregation and evaluation in a bit more detail so that someone can imagine impl just by text
  - make it clear the feature only works when "hostname" is the lowest level.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:18:19Z

- **keps/2724-topology-aware-scheduling/README.md:1782** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:18:20Z
```diff
@@ -1747,6 +1749,25 @@ For a 3-level topology (block, rack, hostname), in the TopologyRequest, the user
 | [[10, 5], [5, 5, 5]] | 15|1 |  [[0, 0], [5, 5, 5]] | prefer more balanced rack
 | [[15],[15]] [[15, 15]] | 25 | 1 | [[0],[0]] [[13, 12]] | prefer block where the request fits in a single rack
 | [[15], [15], [15, 15]] | 25|5 |  [[0], [0], [15, 10]] | `podset-slice-required-topology = hostname`
+
+### Support for Preferred Node Affinity
+
+TAS supports evaluating `preferredDuringSchedulingIgnoredDuringExecution`
+node affinities during the scheduling cycle. When the
+`TASPreferredSchedulingAffinity` feature gate is enabled, the preferred
+affinity scores are evaluated when selecting candidate topology domains,
+allowing affinity preferences to guide placement decisions. Preferred affinity
+takes precedence over capacity fitness of a domain.
+
+#### Future Vision
+The long-term goal is to decommission this strict precedence model and its
+associated `TASPreferredSchedulingAffinity` feature gate in favor of a generic
+weighted scoring model of domains. Similar to the kube-scheduler scheduling
+plugins scoring model, candidate topology domains will be evaluated using
+a configurable list of individual scoring components (such as affinity
+preferences and capacity packing efficiency) that contribute to a normalized,
+unified final score to determine the globally optimal placement.
```

  ```suggestion
  #### Future Vision
  One drawback of the proposed solution is that it introduces a strict precedence between the 
  "domain affinity score" and the "placement policy". 
  
  A more generic approach using weighted scoring model of domains is considered, similar to
  the kube-scheduler scheduling plugins scoring model, candidate topology domains will be
  evaluated using a configurable list of individual scoring components (such as affinity
  preferences and capacity packing efficiency) that contribute to a normalized,
  unified final score to determine the globally optimal placement.
  
  We are going to re-evaluate the next steps based on the user and community feedback.
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:19:21Z

- **keps/2724-topology-aware-scheduling/README.md:1942** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:19:21Z
```diff
@@ -1916,6 +1937,9 @@ Consider the following improvements and implement if feasible:
 - perform full scheduling simulation rather than just capacity counting
  (including pod affinities and anti-affinities)
 - drop `TASLeastAllocated` feature gate
+- drop `TASPreferredSchedulingAffinity` feature gate upon decommissioning the
+ strict precedence affinity model in favor of the Weighted Scoring Model of
+ domains (see [Future Vision](#future-vision))
```

  ```suggestion
  - re-evaluate the status of the `TASPreferredSchedulingAffinity` feature gate - either graduate, or decommission with the more generic model of weighted scores (see [Future Vision](#future-vision))
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:19:59Z

- **keps/2724-topology-aware-scheduling/kep.yaml:50** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:19:59Z
```diff
@@ -47,6 +47,7 @@ feature-gates:
   - name: TASBalancedPlacement
   - name: TASReplaceNodeOnNodeTaints
   - name: TASMultiLayerTopology
+  - name: TASPreferredSchedulingAffinity
```

  I would propose TASRespectNodeAffinityPreferred to reflect the fact this is focused on respecting the exting k8s api, rather than entirely new feature  / api.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:21:50Z

- **test/integration/singlecluster/tas/tas_test.go:3648** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:21:51Z
```diff
@@ -3586,6 +3586,129 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 
+			ginkgo.When("preferred affinity is used", func() {
+				var (
+					nodes []corev1.Node
+				)
+
+				ginkgo.BeforeEach(func() {
+					features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASPreferredSchedulingAffinity, true)
+					gomega.Expect(k8sClient.DeleteAllOf(ctx, &corev1.Node{}, client.MatchingLabels{"node-group": "tas"})).Should(gomega.Succeed())
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("node-best").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-best").
+							Label("region", "us-west").
+							Label("zone", "us").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+						*testingnode.MakeNode("node-good").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-good").
+							Label("region", "us-east").
+							Label("zone", "us").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+						*testingnode.MakeNode("node-bad").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-bad").
+							Label("region", "us-west").
+							Label("zone", "eu").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.AfterEach(func() {
+					for _, node := range nodes {
+						util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+					}
+				})
```

  Hm, I remember some of the When / Descibe sections already had the code to cleanup the nodes created by the suite. Ptal maybe the nodes are already cleared at the higher level

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T08:23:32Z

- **pkg/features/kube_features.go:389** by [@mimowo](https://github.com/mimowo) — 2026-05-13T08:23:32Z
```diff
@@ -382,6 +382,12 @@ const (
 
 	// Enable recording of WorkloadCreationLatency metric.
 	MetricForWorkloadCreationLatency featuregate.Feature = "MetricForWorkloadCreationLatency"
+
+	// owner: @j-skiba
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/10902
+	// Enable evaluation of preferredDuringSchedulingIgnoredDuringExecution node affinities during scheduling in TAS.
+	// The preferred affinity will take precedence over capacity fitness of a domain.
```

  ```suggestion
  	// The preferred affinity will take precedence over the placement policy (eg. BestFit , LeastFreeCapacity).
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T09:11:14Z

- **keps/2724-topology-aware-scheduling/README.md:1368** by [@mimowo](https://github.com/mimowo) — 2026-05-13T09:11:14Z
```diff
@@ -1365,7 +1365,7 @@ The annotation contains the name of (a single) failed node to replace.
 We propose to remove the Annotation and replace it with new field in Workload status that will it will contain
 a list of failed nodes:
 
-```golang
+```golangS
```

  typo to add "S"?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:11:44Z

- **keps/2724-topology-aware-scheduling/README.md:1760** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:11:43Z
```diff
@@ -1747,6 +1749,25 @@ For a 3-level topology (block, rack, hostname), in the TopologyRequest, the user
 | [[10, 5], [5, 5, 5]] | 15|1 |  [[0, 0], [5, 5, 5]] | prefer more balanced rack
 | [[15],[15]] [[15, 15]] | 25 | 1 | [[0],[0]] [[13, 12]] | prefer block where the request fits in a single rack
 | [[15], [15], [15, 15]] | 25|5 |  [[0], [0], [15, 10]] | `podset-slice-required-topology = hostname`
+
+### Support for Preferred Node Affinity
+
+TAS supports evaluating `preferredDuringSchedulingIgnoredDuringExecution`
+node affinities during the scheduling cycle. When the
+`TASPreferredSchedulingAffinity` feature gate is enabled, the preferred
+affinity scores are evaluated when selecting candidate topology domains,
+allowing affinity preferences to guide placement decisions. Preferred affinity
+takes precedence over capacity fitness of a domain.
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:11:50Z

- **keps/2724-topology-aware-scheduling/README.md:1782** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:11:50Z
```diff
@@ -1747,6 +1749,25 @@ For a 3-level topology (block, rack, hostname), in the TopologyRequest, the user
 | [[10, 5], [5, 5, 5]] | 15|1 |  [[0, 0], [5, 5, 5]] | prefer more balanced rack
 | [[15],[15]] [[15, 15]] | 25 | 1 | [[0],[0]] [[13, 12]] | prefer block where the request fits in a single rack
 | [[15], [15], [15, 15]] | 25|5 |  [[0], [0], [15, 10]] | `podset-slice-required-topology = hostname`
+
+### Support for Preferred Node Affinity
+
+TAS supports evaluating `preferredDuringSchedulingIgnoredDuringExecution`
+node affinities during the scheduling cycle. When the
+`TASPreferredSchedulingAffinity` feature gate is enabled, the preferred
+affinity scores are evaluated when selecting candidate topology domains,
+allowing affinity preferences to guide placement decisions. Preferred affinity
+takes precedence over capacity fitness of a domain.
+
+#### Future Vision
+The long-term goal is to decommission this strict precedence model and its
+associated `TASPreferredSchedulingAffinity` feature gate in favor of a generic
+weighted scoring model of domains. Similar to the kube-scheduler scheduling
+plugins scoring model, candidate topology domains will be evaluated using
+a configurable list of individual scoring components (such as affinity
+preferences and capacity packing efficiency) that contribute to a normalized,
+unified final score to determine the globally optimal placement.
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:11:58Z

- **keps/2724-topology-aware-scheduling/README.md:1942** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:11:58Z
```diff
@@ -1916,6 +1937,9 @@ Consider the following improvements and implement if feasible:
 - perform full scheduling simulation rather than just capacity counting
  (including pod affinities and anti-affinities)
 - drop `TASLeastAllocated` feature gate
+- drop `TASPreferredSchedulingAffinity` feature gate upon decommissioning the
+ strict precedence affinity model in favor of the Weighted Scoring Model of
+ domains (see [Future Vision](#future-vision))
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:12:12Z

- **keps/2724-topology-aware-scheduling/kep.yaml:50** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:12:12Z
```diff
@@ -47,6 +47,7 @@ feature-gates:
   - name: TASBalancedPlacement
   - name: TASReplaceNodeOnNodeTaints
   - name: TASMultiLayerTopology
+  - name: TASPreferredSchedulingAffinity
```

  renamed to `TASRespectNodeAffinityPreferred`

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:16:07Z

- **test/integration/singlecluster/tas/tas_test.go:3648** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:16:07Z
```diff
@@ -3586,6 +3586,129 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 
+			ginkgo.When("preferred affinity is used", func() {
+				var (
+					nodes []corev1.Node
+				)
+
+				ginkgo.BeforeEach(func() {
+					features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASPreferredSchedulingAffinity, true)
+					gomega.Expect(k8sClient.DeleteAllOf(ctx, &corev1.Node{}, client.MatchingLabels{"node-group": "tas"})).Should(gomega.Succeed())
+					nodes = []corev1.Node{
+						*testingnode.MakeNode("node-best").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-best").
+							Label("region", "us-west").
+							Label("zone", "us").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+						*testingnode.MakeNode("node-good").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-good").
+							Label("region", "us-east").
+							Label("zone", "us").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+						*testingnode.MakeNode("node-bad").
+							Label("node-group", "tas").
+							Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+							Label(utiltesting.DefaultRackTopologyLevel, "r1").
+							Label(corev1.LabelHostname, "node-bad").
+							Label("region", "us-west").
+							Label("zone", "eu").
+							StatusAllocatable(corev1.ResourceList{
+								corev1.ResourceCPU:    resource.MustParse("1"),
+								corev1.ResourceMemory: resource.MustParse("1Gi"),
+								corev1.ResourcePods:   resource.MustParse("10"),
+							}).
+							Ready().
+							Obj(),
+					}
+					util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+				})
+
+				ginkgo.AfterEach(func() {
+					for _, node := range nodes {
+						util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+					}
+				})
```

  now I moved the whole `When` block outside of `When("Node is mutated during test cases")` as it was not the best place for the tests.
  
  Also added richer `AfterEach` cleanup procedures (the same as other `When` blocks) and added creation of topology, rf, cq etc to `BeforeEach` as it was necessary after moving out `When("Node is mutated during test cases")` block.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:16:45Z

- **pkg/features/kube_features.go:389** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:16:45Z
```diff
@@ -382,6 +382,12 @@ const (
 
 	// Enable recording of WorkloadCreationLatency metric.
 	MetricForWorkloadCreationLatency featuregate.Feature = "MetricForWorkloadCreationLatency"
+
+	// owner: @j-skiba
+	// issue: https://github.com/kubernetes-sigs/kueue/issues/10902
+	// Enable evaluation of preferredDuringSchedulingIgnoredDuringExecution node affinities during scheduling in TAS.
+	// The preferred affinity will take precedence over capacity fitness of a domain.
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:16:56Z

- **keps/2724-topology-aware-scheduling/README.md:1368** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:16:56Z
```diff
@@ -1365,7 +1365,7 @@ The annotation contains the name of (a single) failed node to replace.
 We propose to remove the Annotation and replace it with new field in Workload status that will it will contain
 a list of failed nodes:
 
-```golang
+```golangS
```

  removed

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T09:19:02Z

- **pkg/cache/scheduler/tas_cache_test.go:50** by [@mimowo](https://github.com/mimowo) — 2026-05-13T09:19:02Z
```diff
@@ -40,16 +40,18 @@ import (
 
 // PodSetTestCase defines a test case for a single podset in the consolidated test.
 type PodSetTestCase struct {
-	podSetName         string
-	topologyRequest    *kueue.PodSetTopologyRequest
-	requests           resources.Requests
-	count              int32
-	tolerations        []corev1.Toleration
-	nodeSelector       map[string]string
-	podSetGroupName    *string
-	previousAssignment *kueue.TopologyAssignment
-	wantAssignment     *tas.TopologyAssignment
-	wantReason         string
+	podSetName                string
+	topologyRequest           *kueue.PodSetTopologyRequest
+	requests                  resources.Requests
+	count                     int32
+	tolerations               []corev1.Toleration
+	nodeSelector              map[string]string
+	requiredNodeSelectorTerms []corev1.NodeSelectorTerm
+	preferredSchedulingTerms  []corev1.PreferredSchedulingTerm
```

  nit, maybe we could encapsulate both just by `nodeAffinity`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T09:20:03Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:906** by [@mimowo](https://github.com/mimowo) — 2026-05-13T09:20:04Z
```diff
@@ -894,6 +899,15 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && len(preferredAffinity) > 0 {
+			prefTerms, err := nodeaffinity.NewPreferredSchedulingTerms(preferredAffinity)
+			if err != nil {
+				s.log.V(5).Info("ignoring invalid preferred node affinity terms", "err", err)
```

  Let's rather `return` here as for the errors when parsing `requiredAffinity`.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:32:18Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:906** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:32:18Z
```diff
@@ -894,6 +899,15 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && len(preferredAffinity) > 0 {
+			prefTerms, err := nodeaffinity.NewPreferredSchedulingTerms(preferredAffinity)
+			if err != nil {
+				s.log.V(5).Info("ignoring invalid preferred node affinity terms", "err", err)
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-13T09:32:32Z

- **pkg/cache/scheduler/tas_cache_test.go:50** by [@j-skiba](https://github.com/j-skiba) — 2026-05-13T09:32:32Z
```diff
@@ -40,16 +40,18 @@ import (
 
 // PodSetTestCase defines a test case for a single podset in the consolidated test.
 type PodSetTestCase struct {
-	podSetName         string
-	topologyRequest    *kueue.PodSetTopologyRequest
-	requests           resources.Requests
-	count              int32
-	tolerations        []corev1.Toleration
-	nodeSelector       map[string]string
-	podSetGroupName    *string
-	previousAssignment *kueue.TopologyAssignment
-	wantAssignment     *tas.TopologyAssignment
-	wantReason         string
+	podSetName                string
+	topologyRequest           *kueue.PodSetTopologyRequest
+	requests                  resources.Requests
+	count                     int32
+	tolerations               []corev1.Toleration
+	nodeSelector              map[string]string
+	requiredNodeSelectorTerms []corev1.NodeSelectorTerm
+	preferredSchedulingTerms  []corev1.PreferredSchedulingTerm
```

  yeah, it looks better. Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T16:05:01Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:906** by [@mimowo](https://github.com/mimowo) — 2026-05-13T16:05:01Z
```diff
@@ -894,6 +899,16 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && len(preferredAffinity) > 0 {
+			prefTerms, err := nodeaffinity.NewPreferredSchedulingTerms(preferredAffinity)
+			if err != nil {
+				s.log.V(5).Info("ignoring invalid preferred node affinity terms", "err", err)
```

  Let's drop the log - it should be logged already in the higher levels as we are returning error.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-13T16:05:23Z

Thank you for working on that.
I left a small but critical algorithm change comment.

- **keps/2724-topology-aware-scheduling/kep.yaml:50** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-13T16:01:23Z
```diff
@@ -47,6 +47,7 @@ feature-gates:
   - name: TASBalancedPlacement
   - name: TASReplaceNodeOnNodeTaints
   - name: TASMultiLayerTopology
+  - name: TASRespectNodeAffinityPreferred
```

  Which FG names `TASRespectPreferredSchedulingAffinity` vs `TASRespectNodeAffinityPreferred` are correct?
  
  I see that you use separate FG names in KEP and implementation.

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-13T16:04:27Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  Why do we need to use the preferred affinity score evaluation as a first-step tiebreak?
  I don't see any context in KEP.
  
  Doesn't this break the existing topology assignment for the leader on multiple domains?
  
  I'm wondering if we should use this as the second tiebreak.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T16:08:17Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:902** by [@mimowo](https://github.com/mimowo) — 2026-05-13T16:08:17Z
```diff
@@ -894,6 +899,16 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
```

  Do not read it outside the FG, we can do 
  
  ```golang
  if features.Enabled(features.TASRespectPreferredSchedulingAffinity) {
    preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
    if len(preferredAffinity) == 0 {
      continue
    }
    ...
  }
  ```
  just to make it very clear to users that we are not getting any nil pointer

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T16:18:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1533** by [@mimowo](https://github.com/mimowo) — 2026-05-13T16:18:42Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
+
```

  I would like to also make sure we have tests for the scenario (maybe we already have):
  `shoud choose the second domain (ordering by  `affinityScore`), because the first domain does not satisfy the "required" constraint`; 
  
  This is in order to make sure the code does not give up on the search too early if the first domain does not satisfy the request. 
  
  Now the largest (and only domain satisfying the "required" constaint) may not be first.
  
  Historically the first was the largest, so I think we might have done such an optimization. For example we have some special hanling about the topDomain here: https://github.com/kubernetes-sigs/kueue/blob/2edaef59111e7a06a5b55488a9c8d15e39eae668/pkg/cache/scheduler/tas_flavor_snapshot.go#L1246

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T16:24:54Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@mimowo](https://github.com/mimowo) — 2026-05-13T16:24:54Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  First step tie-break is what we need for our use case.
  
  Basically we have a performance costly procedure for preparing infrastructure (in an AdmissionCheck) on some "racks", and so we want to prioritize the racks with already prepared infrastucture as much as possible.
  
  If we cannot schedule on the racks with "hot" infrastructure we may fallback to another "rack" and warm it up, but this is costly, so is avoided, even at the cost of fragmentation.
  
  Stiil, in a follow up we may need a more weighted approach when we can say that nodeAffinity is 90% important, but fragmentation 10%. However, we are not able to deliver that for 0.18.  
  
  This is partly already mentioned in the KEP, but we can extend the "User stories" section with this case, where the node preference is much more important.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-13T17:53:03Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-13T17:53:03Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  I see. In that case, I'm ok with using affinity as the first tiebreak.
  OTOH, this will change LWS TAS behavior. Before this feature, the leader would be placed in the best topology even though the scheduled place didn't match the affinity.
  
  But, after this feature, the leader doesn't get an assignment when the assigned topology doesn't match the affinity.
  
  So, I would propose mentioning this behavior change in the KEP and user documentation.
  Also, I'm wondering if we can expand notFitReason so that we can emphasize affinity is the no fit reason.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T18:11:53Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@mimowo](https://github.com/mimowo) — 2026-05-13T18:11:52Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  Let me think tomorrow, maybe there are such cases

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T07:49:03Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@mimowo](https://github.com/mimowo) — 2026-05-14T07:49:03Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  > OTOH, this will change LWS TAS behavior. Before this feature, the leader would be placed in the best topology even though the scheduled place didn't match the affinity.
  > But, after this feature, the leader doesn't get an assignment when the assigned topology doesn't match the affinity.
  
  After reading this again, I'm not sure I understand the concern. Yes, the domain for the leader might be different than before, but:
  1. users who previously set ` nodeAffinity.preferred` it was completely ignored for them so equally well they could just skip it
  2. users who don't use nodeAffinity.preferred are not affected
  3. for users who now use `nodeAffinity.preferred` the domain for leader may change, but if the domain could be found before, then it will also be found now. We only change soft preference. So, the feature should not result in `noFit` now.
  
  > So, I would propose mentioning this behavior change in the KEP and user documentation.
  Also, I'm wondering if we can expand notFitReason so that we can emphasize affinity is the no fit reason.
  
  Given my point above (3.) this should never happen with this feature that we found a domain before, but cannot find any domain now. We may just find an alternative / different placement.
  
  So, I think we may:
  1. clarify that in the text of the KEP
  2. extend testing to demonstrate the tricky cases are covered, which is also my suggestion here: https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3235820573
  
  @j-skiba correct me if I'm missing something.
  
  @tenzen-y let me also know if you meant something different, cause I'm not totally sure if I captured your concern well

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T08:39:00Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T08:38:59Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  yes, I agree with these statements

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T09:12:18Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:906** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T09:12:18Z
```diff
@@ -894,6 +899,16 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && len(preferredAffinity) > 0 {
+			prefTerms, err := nodeaffinity.NewPreferredSchedulingTerms(preferredAffinity)
+			if err != nil {
+				s.log.V(5).Info("ignoring invalid preferred node affinity terms", "err", err)
```

  removed

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T09:13:30Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:902** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T09:13:30Z
```diff
@@ -894,6 +899,16 @@ func (s *TASFlavorSnapshot) findTopologyAssignment(
 			}
 			requirements.affinitySelector = affinitySelector
 		}
+		preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
```

  :+1: done like this
  ```go
  if features.Enabled(features.TASRespectPreferredSchedulingAffinity) {
    preferredAffinity := info.Affinity.NodeAffinity.PreferredDuringSchedulingIgnoredDuringExecution
    if len(preferredAffinity) > 0 {
      ...
    }
  }
  ```
  because there's no loop

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T09:15:08Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1533** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T09:15:08Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
+
```

  added two unit test cases:
  - `affinity ordering with preferred inner topology level bubbles up to parent level to avoid splitting across parent domains`
  - `affinity ordering with required topology level chooses secondary domain if primary is too small`
  
  and I also had to adjust the code a little to handle them

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T09:15:43Z

- **keps/2724-topology-aware-scheduling/kep.yaml:50** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T09:15:43Z
```diff
@@ -47,6 +47,7 @@ feature-gates:
   - name: TASBalancedPlacement
   - name: TASReplaceNodeOnNodeTaints
   - name: TASMultiLayerTopology
+  - name: TASRespectNodeAffinityPreferred
```

  good catch, changed to `TASRespectNodeAffinityPreferred` everywhere

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:40:43Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1533** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:40:43Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
+
```

  I would like to add another test case - looking at the code I'm not sure if this works well. 
  
  
  1.  flat topology (only hostname), workload with the "preferred" mode, size 5 pods x 1
  - preferred nodes with sizes: [2, 1]
  - non-preferred nodes with sizes: [2, 1]
  2.  flat topology (only hostname), workload with the "unconstrained" mode, size 5 pods x 1:
  - prefferred nodes with sizes: [2, 1]
  - non-preferred nodes with sizes: [2, 1, 1]
  3.  flat topology (only hostname), workload with the "preferred" mode, size 2 pods x 2
  - preferred nodes with sizes: [2, 1]
  - non-preferred nodes with sizes: [2, 2, 1]
  4.  flat topology (only hostname), workload with the "unconstrained" mode, size 2 pods x 2
  - preferred nodes with sizes: [2, 1]
  - non-preferred nodes with sizes: [2, 2, 1]
  
  My expecations:
  - in (1.) we choose use two node from preferred, and one node from non-preferred of size 2
  - in (2.) we should use two nodes from preferred, and two of size "1" from the non-preferred
  - in (3.) we should use one node from preferred, and one of size "2" from the non-preferred
  - in (4.) we should use one node from preferred, and one of size "2" from the non-preferred

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:41:19Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1297** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:41:20Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
+			for i := 1; i < len(sortedDomain); i++ {
+				d := sortedDomain[i]
+				if d.sliceStateWithLeader >= sliceCount && d.leaderState >= state.leaderCount {
+					candidates := topAffinityTierDomains(sortedDomain[i:])
+					return searchLevelIdx, []*domain{findBestFitDomainForSlices(candidates, sliceCount, state.leaderCount)}, ""
+				}
+			}
```

  Is the new code fully covered by unit tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:41:29Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1289** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:41:29Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
```

  Wrap the code in the FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:41:38Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1299** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:41:38Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
+			for i := 1; i < len(sortedDomain); i++ {
+				d := sortedDomain[i]
+				if d.sliceStateWithLeader >= sliceCount && d.leaderState >= state.leaderCount {
+					candidates := topAffinityTierDomains(sortedDomain[i:])
+					return searchLevelIdx, []*domain{findBestFitDomainForSlices(candidates, sliceCount, state.leaderCount)}, ""
+				}
+			}
 			return 0, nil, notFitReason(topDomain.sliceState, sliceCount)
 		}
+
```

  nit, revert line changes

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:42:13Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:92** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:42:13Z
```diff
@@ -86,6 +86,10 @@ type domain struct {
 	// levelValues stores the mapping from domain ID back to the
 	// ordered list of values
 	levelValues []string
+
+	// affinityScore is the sum of weights of all preferred affinity terms that match the node.
+	// For non-leaf domains, it is the sum of affinity scores of all children.
+	affinityScore int64
```

  Check code coverage with some tooling for hew newly added code to confirm 100% coverage

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T10:46:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T10:46:42Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  this part doesn't have any coverage and I think it's dead code.
  
  here 
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1271
  let's say we have workload with `leaderCount = 1` and `sliceCount = 3` and `topDomain` can fit the leader pod but doing so leaves enough space for only 2 worker slices.
  
  then we move here
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1279
  and we assign
  ```go
  remainingSliceCount := 3
  remainingLeaderCount := 1 // this is always 1 or 0
  ```
  
  then we enter `for` loop
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1287
  and this `sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount` will always be false because the first iteration always has `idx = 0` so we use `sortedDomains[0]` but here
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1271
  we entered the condition because of the exact opposite reason (`topDomain.sliceStateWithLeader < sliceCount`) so it evaluates to false.
  
  Note: If `topDomain.leaderState` is 0 here
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1271
  then `for` loop condition fails to even enter for first iteration
  
  Then 
  https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148L1295
  here we subtract and get 0 and the loop ends
  
  Am I right?

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T11:35:17Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T11:35:17Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  cc @mimowo

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T11:35:42Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1289** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T11:35:42Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T11:35:51Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1299** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T11:35:50Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
+			for i := 1; i < len(sortedDomain); i++ {
+				d := sortedDomain[i]
+				if d.sliceStateWithLeader >= sliceCount && d.leaderState >= state.leaderCount {
+					candidates := topAffinityTierDomains(sortedDomain[i:])
+					return searchLevelIdx, []*domain{findBestFitDomainForSlices(candidates, sliceCount, state.leaderCount)}, ""
+				}
+			}
 			return 0, nil, notFitReason(topDomain.sliceState, sliceCount)
 		}
+
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T11:36:03Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1533** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T11:36:03Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
+
```

  done, passes without additional changes

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T11:37:34Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1297** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T11:37:34Z
```diff
@@ -1270,8 +1286,17 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 	}
 	if topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount {
 		if state.required {
+			// Scan remaining domains to support preferred affinity before failing
+			for i := 1; i < len(sortedDomain); i++ {
+				d := sortedDomain[i]
+				if d.sliceStateWithLeader >= sliceCount && d.leaderState >= state.leaderCount {
+					candidates := topAffinityTierDomains(sortedDomain[i:])
+					return searchLevelIdx, []*domain{findBestFitDomainForSlices(candidates, sliceCount, state.leaderCount)}, ""
+				}
+			}
```

  yes except for this https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3240806355 but it wasn't covered even before

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T11:51:36Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@mimowo](https://github.com/mimowo) — 2026-05-14T11:51:36Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  Let me check with @pajakd first

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-14T13:10:52Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@pajakd](https://github.com/pajakd) — 2026-05-14T13:10:51Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  I synced with @j-skiba and I also think its dead code (at least now when the number of leaders is at most 1).
  
  https://github.com/kubernetes-sigs/kueue/blob/b2a2dd8336aed252bc9fea7a0fbec3c4366fe8c5/pkg/cache/scheduler/tas_flavor_snapshot.go#L1287-L1301
  
   - `sortedDomain[idx].leaderState > 0` so in each iteration of this loop we only consider domains that can host a leader.
   - `remainingLeaderCount -= domain.leaderState` so in each iteration of this loop we decrease the `remainingLeaderCount`.
   - we stop the loop when  `remainingLeaderCount` is 0 so this loop can have at most one iteration.
   - if in this first iteration we would have `sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount`, this would mean that this single domain can host the leader and all the slices. This cannot happen as we are in the if `topDomain.sliceStateWithLeader < sliceCount || topDomain.leaderState < state.leaderCount` so the topDomain cannot fit everything.
  
  So, basically I would consider removing this code (in a separate PR).

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-14T13:15:50Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@pajakd](https://github.com/pajakd) — 2026-05-14T13:15:49Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  But, this is a complicated logic. So I would like to see some tests. For example. If the preferred affinity would move the domain that cannot host a leader to be the first domain, we will not execute this loop at all.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:35:48Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1317** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:35:48Z
```diff
@@ -1288,7 +1313,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceStateWithLeader >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, remainingLeaderCount)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, remainingLeaderCount)
 			}
```

  Thank you for the investigation folks 👍 
  
  Let's leave this PR as is, but maybe open another PR which turns the code into os.Exit(-1) to test if this is maybe tested in integration /e2e tests. If we don't have any coverage signal I'm ok to drop in a follow up PR.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-14T13:45:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:92** by [@j-skiba](https://github.com/j-skiba) — 2026-05-14T13:45:21Z
```diff
@@ -86,6 +86,10 @@ type domain struct {
 	// levelValues stores the mapping from domain ID back to the
 	// ordered list of values
 	levelValues []string
+
+	// affinityScore is the sum of weights of all preferred affinity terms that match the node.
+	// For non-leaf domains, it is the sum of affinity scores of all children.
+	affinityScore int64
```

  apart from [this](https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3240806355) and - https://github.com/kubernetes-sigs/kueue/pull/10903/changes#diff-015ac0ccc6a319e922161cdd677487236e5d4e7edc11d0376fd59aebf14ae148R1415  - (which doesn't have coverage too) everything is covered by tests.
  
  Should I add short unit test to just cover this `else` path?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:54:18Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1337** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:54:18Z
```diff
@@ -1306,7 +1333,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceState >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, 0)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, 0)
```

  One more question, these two lines `topAffinityTierDomains` and `findBestFitDomainForSlices` seem to always go together, could we put `topAffinityTierDomains` inside `findBestFitDomainForSlices` to avoid duplicating the code?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:55:37Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1337** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:55:37Z
```diff
@@ -1306,7 +1333,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceState >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, 0)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, 0)
```

  If there are some counter-examples them let's make them explictig here, and let's ponder if we have enough of them to justify the duplication, or we could maybe add a param to indicate if `topAffinityTierDomains` should be used

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T14:00:55Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:92** by [@mimowo](https://github.com/mimowo) — 2026-05-14T14:00:54Z
```diff
@@ -86,6 +86,10 @@ type domain struct {
 	// levelValues stores the mapping from domain ID back to the
 	// ordered list of values
 	levelValues []string
+
+	// affinityScore is the sum of weights of all preferred affinity terms that match the node.
+	// For non-leaf domains, it is the sum of affinity scores of all children.
+	affinityScore int64
```

  > Should I add short unit test to just cover this else path?
  
  It would be great as you are now fresh with understanding of the code.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-14T17:42:22Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-14T17:42:22Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  @j-skiba @mimowo Let's imagine the following Cross PodSchedulig LWS and cluster capacities.
  
  ```
  - us-west
    - number of host: 8
    - each host allocatable resources: CPU=8, memory=32Gi, GPU=8
  - us-east
    - number of host: 4
    - each host allocatable resources: CPU=32, memory=128Gi, GPU=8
  ```
  
  If we respect worker preferred affinity by this FG enabling, a whole of LWS will not be scheduled because they specify us-west as preferred affinity.
  
  In algorithm PoV, before this PR, we could select us-east because the `leaderState` is satisfied.
  But, after this PR, we can not select us-east due to affinityScore even though leader can not be accommodated by us-west.
  
  As a result, a whole LWS doesn't get any assignment due to PodSetGroup constraints.
  
  ```
  - topolotgy: us-west
      leaderStete: 0 # because each us-west machine has only 8 core / 32 Gi mem.
      affinityScore: 10
  - topology: us-east
      leaderState: 1
      affinityScore: 0
  ```
  
  I think that this is critical behavior changes. In this situation, the worker's preferred affinity works as strong constraints even though it is just preference which means we will face no-fit.
  
  ```yaml
  apiVersion: leaderworkerset.x-k8s.io/v1
  kind: LeaderWorkerSet
  metadata:
    name: llm-serving
    labels:
      kueue.x-k8s.io/queue-name: tas-queue
  spec:
    replicas: 1
    leaderWorkerTemplate:
      size: 9
      leaderTemplate:
        metadata:
          annotations:
            kueue.x-k8s.io/podset-group-name: "llm-group"
            kueue.x-k8s.io/podset-preferred-topology: "topology.kubernetes.io/zone"
        spec:
          containers:
          - name: router
            resources:
              requests:
                cpu: "16"
                memory: "64Gi"
      workerTemplate:
        metadata:
          annotations:
            kueue.x-k8s.io/podset-group-name: "llm-group"
            kueue.x-k8s.io/podset-preferred-topology: "topology.kubernetes.io/zone"
        spec:
          affinity:
            nodeAffinity:
              preferredDuringSchedulingIgnoredDuringExecution:
              - weight: 10
                preference:
                  matchExpressions:
                  - key: topology.kubernetes.io/zone
                    operator: In
                    values: ["us-west"]
          containers:
          - name: model-shard
            resources:
              requests:
                cpu: "4"
                memory: "32Gi"
                nvidia.com/gpu: "8"
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T18:06:26Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@mimowo](https://github.com/mimowo) — 2026-05-14T18:06:26Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  @tenzen-y  I see, this is an excellent example 👍 
  
  This shows that indeed we may turn "fit" into "nofit" with `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution` + `kueue.x-k8s.io/podset-group-name` used.
  
  In that case, the approaches I can see:
  1. just keep as is, but document the behavior, because this is a very special case, users just need to know the risk
  2. ignore  `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution `  when `kueue.x-k8s.io/podset-group-name`  is used (as before basically, just respect `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution ` for no-group scenarios
  3. if we hit "noFit" then re-run the whole algorithm ignoring  `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution` , so that the "fit" solution is found
  4. add validation against using `nodeAffinity.preferredDuringSchedulingIgnoredDuringExecution ` with `kueue.x-k8s.io/podset-group-name` 
  
  I'm leaning towards (1.), because:
  - the case is rather specially crafted, typically the leader will have the same resources( is one of workers), or small (just small CPU to coordinate), so this case is rather theoretical IMO
  - (2.) may restrict user options - if they want to ignore, then they can just not set the nodeAffinity.preferred
  - (3.) may mean excessive performance hit, and perforamnce of TAS is very important
  - (4.) is sensible, but still restricting the use-case for the case which many users will consider theoretical
  - note that even in the baseline TAS we sometimes say "nofit" even though we could say "fit" if we considered PodSets in a different order (even without `kueue.x-k8s.io/podset-group-name`), so the algorithm is intrinsically heuristic anyway already
  
  So, I would suggest (1.), just make sure the documentation is mentioning this possible case.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-15T06:58:41Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@j-skiba](https://github.com/j-skiba) — 2026-05-15T06:58:41Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  ok so actually moving the affinity check to the second position (after `leaderState` check) should be ok even for https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3235862873 this scenario I think.
  
  The `leaderState` is a binary value so even when domains are sorted then the affinity tie-breaker will guarantee the leader-capable domains sublist of all domains will be sorted by their affinity score.
  
  This way we have the scenario @tenzen-y described handled as before and it also satisfies the scenario from the comment https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3235862873

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T07:11:43Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@mimowo](https://github.com/mimowo) — 2026-05-15T07:11:43Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  cool in that case lets try to fix this scenario

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-15T07:50:59Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T07:50:58Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  @mimowo @j-skiba Thank you for moving this discussion forward.
  @jackfrancis 's new approach sounds good.
  
  As I check the algorithm again, even when LWS leader replicas are multiple, the `leaderSate` will be binary.
  So, I believe that both my scenario and @mimowo 's customer request (https://github.com/kubernetes-sigs/kueue/pull/10903#discussion_r3235862873) will be satisfied.
  
  Awesome, thank you.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-15T08:16:38Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1532** by [@j-skiba](https://github.com/j-skiba) — 2026-05-15T08:16:38Z
```diff
@@ -1512,6 +1527,10 @@ func (s *TASFlavorSnapshot) sortedDomainsWithLeader(domains []*domain, unconstra
 	isLeastFreeCapacity := useLeastFreeCapacityAlgorithm(unconstrained)
 	result := slices.Clone(domains)
 	slices.SortFunc(result, func(a, b *domain) int {
+		if features.Enabled(features.TASRespectPreferredSchedulingAffinity) && a.affinityScore != b.affinityScore {
+			return cmp.Compare(b.affinityScore, a.affinityScore)
+		}
```

  I moved the affinity score check as the second fallback, also removed section from README added before

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-15T08:16:46Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:92** by [@j-skiba](https://github.com/j-skiba) — 2026-05-15T08:16:46Z
```diff
@@ -86,6 +86,10 @@ type domain struct {
 	// levelValues stores the mapping from domain ID back to the
 	// ordered list of values
 	levelValues []string
+
+	// affinityScore is the sum of weights of all preferred affinity terms that match the node.
+	// For non-leaf domains, it is the sum of affinity scores of all children.
+	affinityScore int64
```

  done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-15T08:18:39Z

- **pkg/cache/scheduler/tas_flavor_snapshot.go:1337** by [@j-skiba](https://github.com/j-skiba) — 2026-05-15T08:18:39Z
```diff
@@ -1306,7 +1333,8 @@ func (s *TASFlavorSnapshot) findLevelWithFitDomains(
 			domain := sortedDomain[idx]
 			if useBestFitAlgorithm(state.unconstrained) && sortedDomain[idx].sliceState >= remainingSliceCount {
 				// optimize the last domain
-				domain = findBestFitDomainForSlices(sortedDomain[idx:], remainingSliceCount, 0)
+				candidates := topAffinityTierDomains(sortedDomain[idx:])
+				domain = findBestFitDomainForSlices(candidates, remainingSliceCount, 0)
```

  `findBestFitDomainForSlices` and `findBestFitDomain` call `findBestFitDomainBy` inside. I moved the `topAffinityTierDomains` into `findBestFitDomainBy`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T11:02:27Z

/lgtm
/approve

Thank you 👍 
Leaving on hold in case @tenzen-y has extra comments
/hold

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-15T15:09:34Z

Almost LGTM, thank you!

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:438** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T14:57:56Z
  Can we add `TestSrotedDomains(t *testing.T)` which has affinity cases same as `TestSortedDomainsWithLeader`?

- **test/integration/singlecluster/tas/tas_test.go:3470** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T15:09:17Z
```diff
@@ -3416,6 +3416,152 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 
+		ginkgo.When("Workloads use preferred node affinity and TASRespectNodeAffinityPreferred is enabled", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASRespectNodeAffinityPreferred, true)
+				gomega.Expect(k8sClient.DeleteAllOf(ctx, &corev1.Node{}, client.MatchingLabels{"node-group": "tas"})).Should(gomega.Succeed())
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("node-best").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-best").
+						Label("region", "us-west").
+						Label("zone", "us").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("node-good").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-good").
+						Label("region", "us-east").
+						Label("zone", "us").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("node-bad").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-bad").
+						Label("region", "us-west").
+						Label("zone", "eu").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+				}
```

  Currently, we have "node-best", "node-good", and "node-bad". The only "node-best" and "node-bad" have "region=us-west," which is the preferred affinity in the "should admit workload to the node matching preferred affinity" case.
  
  So, both "node-best" and "node-bad" match with the preferred affinity, then "node-bad" is selected due to alphabetical order.
  
  It would be better to prepare Nodes so that exactly one Node matches the preferred affinity.
  So, I would recommend using the `region=us-east` in `node-bad`.

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-18T06:18:06Z

- **test/integration/singlecluster/tas/tas_test.go:3470** by [@j-skiba](https://github.com/j-skiba) — 2026-05-18T06:18:06Z
```diff
@@ -3416,6 +3416,152 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 			})
 		})
 
+		ginkgo.When("Workloads use preferred node affinity and TASRespectNodeAffinityPreferred is enabled", func() {
+			var (
+				nodes []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				features.SetFeatureGateDuringTest(ginkgo.GinkgoTB(), features.TASRespectNodeAffinityPreferred, true)
+				gomega.Expect(k8sClient.DeleteAllOf(ctx, &corev1.Node{}, client.MatchingLabels{"node-group": "tas"})).Should(gomega.Succeed())
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("node-best").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-best").
+						Label("region", "us-west").
+						Label("zone", "us").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("node-good").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-good").
+						Label("region", "us-east").
+						Label("zone", "us").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("node-bad").
+						Label("node-group", "tas").
+						Label(utiltesting.DefaultBlockTopologyLevel, "b1").
+						Label(utiltesting.DefaultRackTopologyLevel, "r1").
+						Label(corev1.LabelHostname, "node-bad").
+						Label("region", "us-west").
+						Label("zone", "eu").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("1"),
+							corev1.ResourceMemory: resource.MustParse("1Gi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Ready().
+						Obj(),
+				}
```

  sounds good, done

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-18T06:18:21Z

- **pkg/cache/scheduler/tas_flavor_snapshot_test.go:438** by [@j-skiba](https://github.com/j-skiba) — 2026-05-18T06:18:21Z
  done
