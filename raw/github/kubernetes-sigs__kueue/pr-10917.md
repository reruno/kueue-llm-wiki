# PR #10917: [ConcurrentAdmission] Skip less favorable Variants from being admitted

**Summary**: [ConcurrentAdmission] Skip less favorable Variants from being admitted

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10917

**Last updated**: 2026-05-08T08:19:28Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `sched-skip-lower-variant` → `main`
- **Created**: 2026-05-04T11:13:28Z
- **Updated**: 2026-05-08T08:19:28Z
- **Merged**: 2026-05-08T08:19:26Z
- **Closed**: 2026-05-08T08:19:26Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: _none_
- **Changed files**: 2   **+157 / -44**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->

#### What this PR does / why we need it:
Skips less favorable Variants from being admitted, if the more favorable Variant is running. Eventually the less favorable Variant is deactivated by the concurrentadmission controller.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #10913

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-04T11:13:35Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 58d5ace30ec77169c4418089c1156fad66728754 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fc9abee9082c00089bf9ee |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10917--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10917--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwOTE3LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.88sCeN2FG_eV5uXUkEys82ZFm-1dseIYfsDUhOtegr0)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10917--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T14:26:20Z

/hold
Until https://github.com/kubernetes-sigs/kueue/pull/10954 is merged

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T14:28:08Z

Made some refactor, let me know wdyt @pajakd @mimowo

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-06T08:03:50Z

/unhold

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-06T09:50:30Z

/hold

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-07T12:39:40Z

> /hold

@PBundyra why is that put on hold?

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-07T13:59:28Z

/hold cancel
I put it on hold to slightly refactor it after out offline discussion. Now it's ready for review

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-08T07:50:20Z

LGTM label has been added.  <details>Git tree hash: f79ef9271530c8b376f6b007b2fd27ce4be3a7e5</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-08T07:50:22Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10917#pullrequestreview-4250553431" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10917#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [PBundyra,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T11:22:56Z

- **pkg/scheduler/scheduler.go:1111** by [@mimowo](https://github.com/mimowo) — 2026-05-04T11:22:56Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  Please order the functions so that the caller is always higher, it is currently very jumpy when reading the code. I think `getSiblingVariant` should be below `getLessFavorableSibling`.
  
  I know that some languages have the opposite listing convention, just trying to make sure there is some natural order.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T11:26:37Z

- **pkg/scheduler/scheduler.go:1123** by [@mimowo](https://github.com/mimowo) — 2026-05-04T11:26:37Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+func (s *Scheduler) getMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
```

  The semantics of this function is unclear to me, same for getLessFavorableSibling, because there might be more than one more/less preferreable sibling. Are you choosing the "most preffrable" / least preferrable? If so then let's capture the intention in naming. If we choose random then it is a code smell. Let's also add comments documenting the semantics for the input and ouput of these functions.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-04T11:43:39Z

- **pkg/scheduler/scheduler.go:1123** by [@PBundyra](https://github.com/PBundyra) — 2026-05-04T11:43:39Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+func (s *Scheduler) getMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
```

  The assumption of this function is that there's only one more/less favorable sibling admitted. In those functions we iterate over Workloads stored in Snapshot, which iirc stores only admitted Workloads. Let me clarify that in naming/comments

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-04T11:58:06Z

- **pkg/scheduler/scheduler.go:1111** by [@PBundyra](https://github.com/PBundyra) — 2026-05-04T11:58:06Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-04T11:58:35Z

- **pkg/scheduler/scheduler.go:1123** by [@PBundyra](https://github.com/PBundyra) — 2026-05-04T11:58:35Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+func (s *Scheduler) getMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
```

  Renamed and added comments

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T12:12:15Z

- **pkg/scheduler/scheduler.go:1123** by [@mimowo](https://github.com/mimowo) — 2026-05-04T12:12:15Z
```diff
@@ -1092,18 +1100,32 @@ func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info,
 		}
 		if concurrentadmission.GetParentWorkloadUID(otherWl.Obj) == parentUID && workload.IsAdmitted(otherWl.Obj) {
 			siblingFlavor := concurrentadmission.GetVariantFlavor(otherWl.Obj)
-			if siblingFlavor != "" && isLessFavorable(candidateFlavor, siblingFlavor, flavors) {
-				return &preemption.Target{
-					WorkloadInfo: otherWl,
-					WorkloadCq:   cq,
-					Reason:       kueue.WorkloadEvictedByFlavorMigration,
-				}
+			if siblingFlavor != "" && matchFunc(candidateFlavor, siblingFlavor, flavors) {
+				return otherWl
 			}
 		}
 	}
 	return nil
 }
 
+func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+func (s *Scheduler) getMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
```

  > The assumption of this function is that there's only one more/less favorable sibling admitted.
  
  Hm, if we look for "admitted", then shouldn't we guarantee there is only one globally anyway? IN that case we could have a single function findAdmittedSiblling / findAdmittedVariant?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T12:15:58Z

- **pkg/scheduler/scheduler.go:1083** by [@mimowo](https://github.com/mimowo) — 2026-05-04T12:15:58Z
```diff
@@ -1066,7 +1074,39 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+// getAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  nit, let's use "find" prefixes, get suggests a simple get operation as if the value was preconputed. This is typical prefix used in the getter/setter pattern. Here we do some computations, so better to reflect that.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-04T12:18:51Z

- **pkg/scheduler/scheduler.go:1109** by [@mimowo](https://github.com/mimowo) — 2026-05-04T12:18:51Z
```diff
@@ -1066,7 +1074,39 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+// getAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+// getAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+	return s.getSiblingVariant(wl, snap, func(candidate, sibling kueue.ResourceFlavorReference, flavors []kueue.ResourceFlavorReference) bool {
+		// the order of the arguments is reversed in comparsinon to getAdmittedLessFavorableSibling
+		return isLessFavorable(sibling, candidate, flavors)
+	})
+}
+
+func (s *Scheduler) getSiblingVariant(wl *workload.Info, snap *schdcache.Snapshot,
+	matchFunc func(candidate, sibling kueue.ResourceFlavorReference, flavors []kueue.ResourceFlavorReference) bool) *workload.Info {
```

  Can we rename `matchFunc` to `lessFunc`? It seems to play this role, it would be much more flamilar naming. `match` to me suggests as "are some criteria matching", here we use it for comparision.
  
  For comparing there are two popular patterns: `lessFunc` , or `compareFunc`. Compare returns  -1/0/1.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T09:29:42Z

- **pkg/scheduler/scheduler.go:1108** by [@pajakd](https://github.com/pajakd) — 2026-05-05T09:29:42Z
```diff
@@ -1066,7 +1074,39 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+// getAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+// getAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+	return s.getSiblingVariant(wl, snap, func(candidate, sibling kueue.ResourceFlavorReference, flavors []kueue.ResourceFlavorReference) bool {
+		// the order of the arguments is reversed in comparsinon to getAdmittedLessFavorableSibling
+		return isLessFavorable(sibling, candidate, flavors)
+	})
+}
+
+func (s *Scheduler) getSiblingVariant(wl *workload.Info, snap *schdcache.Snapshot,
```

  The name of this function should perhaps specify that it returns the admitted sibling variant.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T09:43:21Z

- **pkg/scheduler/scheduler.go:388** by [@pajakd](https://github.com/pajakd) — 2026-05-05T09:43:21Z
```diff
@@ -372,6 +372,14 @@ func (s *Scheduler) processEntry(
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Attempting to schedule workload")
 
+	if features.Enabled(features.ConcurrentAdmission) {
+		if moreFavorableSibling := s.getAdmittedMoreFavorableSibling(log, &e.Info, snapshot); moreFavorableSibling != nil {
+			log.V(3).Info("Skipping workload as a more favorable variant is already admitted", "moreFavorableVariant", klog.KObj(moreFavorableSibling.Obj))
```

  A general question: can there happen that there is one variant running and then multiple more favorable siblings get admitted in parallel?
  
  getAdmittedMoreFavorableSibling / getAdmittedLessFavorableSibling assume that there is only a single one so I'm wondering if there is anything blocking that from happening.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-05T12:57:22Z

- **pkg/scheduler/scheduler.go:388** by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T12:57:22Z
```diff
@@ -372,6 +372,14 @@ func (s *Scheduler) processEntry(
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Attempting to schedule workload")
 
+	if features.Enabled(features.ConcurrentAdmission) {
+		if moreFavorableSibling := s.getAdmittedMoreFavorableSibling(log, &e.Info, snapshot); moreFavorableSibling != nil {
+			log.V(3).Info("Skipping workload as a more favorable variant is already admitted", "moreFavorableVariant", klog.KObj(moreFavorableSibling.Obj))
```

  The goal of this change is to make only one Variant running.
  
  Before there was a short window between admission of Variant-A and deactivation of Variant-B when both of them could be running. However if we prevented admission in scheduler of Variant-B (before it gets deactivated) then we have guarantee only one Variant is running

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T13:32:07Z

- **pkg/scheduler/scheduler.go:388** by [@pajakd](https://github.com/pajakd) — 2026-05-05T13:32:06Z
```diff
@@ -372,6 +372,14 @@ func (s *Scheduler) processEntry(
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Attempting to schedule workload")
 
+	if features.Enabled(features.ConcurrentAdmission) {
+		if moreFavorableSibling := s.getAdmittedMoreFavorableSibling(log, &e.Info, snapshot); moreFavorableSibling != nil {
+			log.V(3).Info("Skipping workload as a more favorable variant is already admitted", "moreFavorableVariant", klog.KObj(moreFavorableSibling.Obj))
```

  Ok, if Variant-A is running, it can happen that more favorable Variant-B gets admitted and start to replace Variant-A? Its possible, right? I was just wondering if at some point of this process an even more favorable Variant-C gets admitted -- will it cause some problems or will it be prevented at some point?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-05T13:56:44Z

- **pkg/scheduler/scheduler.go:388** by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T13:56:43Z
```diff
@@ -372,6 +372,14 @@ func (s *Scheduler) processEntry(
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Attempting to schedule workload")
 
+	if features.Enabled(features.ConcurrentAdmission) {
+		if moreFavorableSibling := s.getAdmittedMoreFavorableSibling(log, &e.Info, snapshot); moreFavorableSibling != nil {
+			log.V(3).Info("Skipping workload as a more favorable variant is already admitted", "moreFavorableVariant", klog.KObj(moreFavorableSibling.Obj))
```

  The hierarchy I had in mind is that Variant-A > Variant-B > Variant-C, with Variant-A as the most favorable Variant. 
  
  To answer your question, yes more favorable Variant can evict less favorable one,  but during this eviction process more favorable Variant waits until the less favorable one finishes eviction admitting, so there's no risk of having two Variants running at the same time because of that

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-05T14:26:29Z

- **pkg/scheduler/scheduler.go:1108** by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T14:26:28Z
```diff
@@ -1066,7 +1074,39 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+// getAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+	sibling := s.getSiblingVariant(wl, snap, isLessFavorable)
+	if sibling == nil {
+		return nil
+	}
+	return &preemption.Target{
+		WorkloadInfo: sibling,
+		WorkloadCq:   snap.ClusterQueue(wl.ClusterQueue),
+		Reason:       kueue.WorkloadEvictedByFlavorMigration,
+	}
+}
+
+// getAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedMoreFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+	return s.getSiblingVariant(wl, snap, func(candidate, sibling kueue.ResourceFlavorReference, flavors []kueue.ResourceFlavorReference) bool {
+		// the order of the arguments is reversed in comparsinon to getAdmittedLessFavorableSibling
+		return isLessFavorable(sibling, candidate, flavors)
+	})
+}
+
+func (s *Scheduler) getSiblingVariant(wl *workload.Info, snap *schdcache.Snapshot,
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-05T14:26:39Z

- **pkg/scheduler/scheduler.go:1083** by [@PBundyra](https://github.com/PBundyra) — 2026-05-05T14:26:38Z
```diff
@@ -1066,7 +1074,39 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
+// getAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue.
+func (s *Scheduler) getAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) *preemption.Target {
```

  Done

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-06T08:39:25Z

- **pkg/scheduler/scheduler.go:1096** by [@pajakd](https://github.com/pajakd) — 2026-05-06T08:39:25Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
```

  log <- unused variable?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-06T08:42:11Z

- **pkg/scheduler/scheduler.go:1107** by [@pajakd](https://github.com/pajakd) — 2026-05-06T08:42:11Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return siblingIdx > wlIdx // larger index means less favorable
+	})
+}
+
+// findAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
```

  This part of the comment is repeated above the `findAdmittedLessFavorableSibling` function. 
  
  Perhaps we can just have it only once above`findAdmittedSibling`?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-06T08:51:52Z

- **pkg/scheduler/scheduler.go:1117** by [@pajakd](https://github.com/pajakd) — 2026-05-06T08:51:52Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return siblingIdx > wlIdx // larger index means less favorable
+	})
+}
+
+// findAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedMoreFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return wlIdx > siblingIdx
+	})
+}
+
+// findAdmittedSibling is a helper that finds an admitted sibling workload matching the comparison criteria.
+func (s *Scheduler) findAdmittedSibling(wl *workload.Info, snap *schdcache.Snapshot, compareFunc func(siblingIdx, wlIdx int) bool) (*workload.Info, error) {
 	if !features.Enabled(features.ConcurrentAdmission) {
-		return nil
+		return nil, nil
```

  (At least in the current implementation) we always check that this featuregate is on before calling findAdmittedMoreFavorableSibling/findAdmittedLessFavorableSibling so perhaps this is not needed. Is it left intentionally for safety?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-06T09:17:29Z

- **pkg/scheduler/scheduler.go:1117** by [@PBundyra](https://github.com/PBundyra) — 2026-05-06T09:17:29Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return siblingIdx > wlIdx // larger index means less favorable
+	})
+}
+
+// findAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedMoreFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return wlIdx > siblingIdx
+	})
+}
+
+// findAdmittedSibling is a helper that finds an admitted sibling workload matching the comparison criteria.
+func (s *Scheduler) findAdmittedSibling(wl *workload.Info, snap *schdcache.Snapshot, compareFunc func(siblingIdx, wlIdx int) bool) (*workload.Info, error) {
 	if !features.Enabled(features.ConcurrentAdmission) {
-		return nil
+		return nil, nil
```

  Yes I left it intentionally in case in the future somebody called it without checking the fg

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-06T09:17:35Z

- **pkg/scheduler/scheduler.go:1096** by [@PBundyra](https://github.com/PBundyra) — 2026-05-06T09:17:35Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
```

  Fixed

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-06T09:18:26Z

- **pkg/scheduler/scheduler.go:1107** by [@PBundyra](https://github.com/PBundyra) — 2026-05-06T09:18:26Z
```diff
@@ -1066,45 +1087,90 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedLessFavorableSibling(log logr.Logger, wl *workload.Info, snap *schdcache.Snapshot) (*workload.Info, error) {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return siblingIdx > wlIdx // larger index means less favorable
+	})
+}
+
+// findAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T07:47:10Z

- **pkg/scheduler/scheduler.go:1159** by [@mimowo](https://github.com/mimowo) — 2026-05-08T07:47:10Z
```diff
@@ -1099,45 +1108,80 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+func (s *Scheduler) findAdmittedLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return siblingIdx > wlIdx // larger index means less favorable
+	})
+}
+
+// findAdmittedMoreFavorableSibling returns an admitted Sibling Workload that has a
+// more favorable Resource Flavor assignment than the provided Workload.
+func (s *Scheduler) findAdmittedMoreFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+	return s.findAdmittedSibling(wl, snap, func(siblingIdx, wlIdx int) bool {
+		return wlIdx > siblingIdx
+	})
+}
+
+// findAdmittedSibling is a helper that finds an admitted Sibling Workload matching the comparison criteria.
+//
+// A Sibling is a Workload belonging to the same Parent Workload (part of the
+// Concurrent Admission feature). Favorability is determined by the order of
+// Resource Flavors defined in the ClusterQueue
+func (s *Scheduler) findAdmittedSibling(wl *workload.Info, snap *schdcache.Snapshot, compareFunc func(siblingIdx, wlIdx int) bool) *workload.Info {
 	if !features.Enabled(features.ConcurrentAdmission) {
 		return nil
 	}
 	parentUID := concurrentadmission.GetParentWorkloadUID(wl.Obj)
 	if parentUID == "" {
 		return nil
 	}
-
 	cq := snap.ClusterQueue(wl.ClusterQueue)
 	if cq == nil || len(cq.ResourceGroups) == 0 {
 		return nil
 	}
 	flavors := cq.ResourceGroups[0].Flavors
-
-	candidateFlavor := concurrentadmission.GetVariantFlavor(wl.Obj)
-	if candidateFlavor == "" {
+	wlFlavorIdx, err := getFlavorIndex(wl, flavors)
+	if err != nil {
 		return nil
 	}
+	return s.findAdmittedSiblingMatching(wl, cq, parentUID, func(sibling *workload.Info) bool {
+		siblingFlavorIdx, err := getFlavorIndex(sibling, flavors)
+		if err != nil {
+			return false
+		}
+		return compareFunc(siblingFlavorIdx, wlFlavorIdx)
+	})
+}
 
+func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache.ClusterQueueSnapshot, parentUID types.UID, match func(sibling *workload.Info) bool) *workload.Info {
 	for _, otherWl := range cq.Workloads {
```

  This is pre-existing code, but it is a range over all Workloads in CQ, so I think we should optimize that, is this on the list of follow ups?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T07:50:03Z

- **pkg/scheduler/scheduler.go:1113** by [@mimowo](https://github.com/mimowo) — 2026-05-08T07:50:03Z
```diff
@@ -1099,45 +1108,80 @@ func (s *Scheduler) updateEntryPenalty(log logr.Logger, e *entry, op usageOp) {
 	}
 }
 
-func (s *Scheduler) getLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
+// findAdmittedLessFavorableSibling returns an admitted Sibling Workload that has a
+// less favorable Resource Flavor assignment than the provided Workload.
+func (s *Scheduler) findAdmittedLessFavorableSibling(wl *workload.Info, snap *schdcache.Snapshot) *workload.Info {
```

  Something to consider, outside the PR - I'm wondering if we should move such helper functions to dedicated files, like scheduler_concurrent_admission.go, next to scheduler.go, just to offload the reading of the main scheduler.go. 
  
  We have done that with tas_flavorassigner.go, but not everywhere, and I'm wondering if this is a good pattern or not. In any case, this is for follow up, just exploring what is your view here.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-08T07:50:13Z

/lgtm
/approve
Thank you 👍
