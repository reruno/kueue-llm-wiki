# PR #10948: tas: improve LWS balanced placement

**Summary**: tas: improve LWS balanced placement

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10948

**Last updated**: 2026-05-11T08:29:58Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ShaanveerS](https://github.com/ShaanveerS)
- **Branch**: `tas-lws-bp` → `main`
- **Created**: 2026-05-05T09:05:44Z
- **Updated**: 2026-05-11T08:29:58Z
- **Merged**: 2026-05-11T08:29:56Z
- **Closed**: 2026-05-11T08:29:56Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd)
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+309 / -16**

## Description

#### What type of PR is this?
/kind cleanup
/area tas

#### What this PR does / why we need it:
Improves TAS Balanced Placement for LeaderWorkerSet by avoiding leader reservation on each node.
Domains that cannot meet the threshold while hosting the leader now lose only leader capacity and stay usable, instead of being pruned from worker placement.

For example with `threshold=5` and `leaderRequired=true`:
before pruning:
```
# [state, sliceState, stateWithLeader, sliceStateWithLeader, leaderState]
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [5, 5, 4, 4, 1]
  ```
after pruning (old):
  ```
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [0, 0, 0, 0, 0]  ← pruned, even though sliceState=5 meets threshold
parent-domain      [6, 6, 5, 5, 1]
  ```
after pruning (new):
  ```
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [5, 5, 0, 0, 0]  ← kept for workers with leader capacity cleared 
parent-domain      [11, 11, 10, 10, 1]
  ```
#### Which issue(s) this PR fixes:
Part of #7494

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Improved TAS balanced placement for LeaderWorkerSet: domains that meet the worker threshold stay usable even when they can't host the leader.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-05T09:05:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | f4be3fdbcd7d94c60c042991668096900b80b02e |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fb230ea6562300086958c3 |

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-06T05:55:03Z

> It might be hard to understand what this change really does. Could you please provide more information? Something like state before/ state after would be great.

Sure! For example with `threshold=5` and `leaderRequired=true`:
before pruning:
```
# [state, sliceState, stateWithLeader, sliceStateWithLeader, leaderState]
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [5, 5, 4, 4, 1]
  ```
after pruning (old):
  ```
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [0, 0, 0, 0, 0]  ← pruned, even though sliceState=5 meets threshold
parent-domain      [6, 6, 5, 5, 1]
  ```
after pruning (new):
  ```
leader-domain      [6, 6, 5, 5, 1]
worker-only-domain [5, 5, 0, 0, 0]  ← kept for workers with leader capacity cleared 
parent-domain      [11, 11, 10, 10, 1]
  ```

### Comment by [@pajakd](https://github.com/pajakd) — 2026-05-07T09:50:58Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-07T09:51:06Z

LGTM label has been added.  <details>Git tree hash: 5f37948ab97de1cba46e9aaf863c31070dbe2538</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-07T10:05:44Z

@ShaanveerS @pajakd should we categorize the issue as a bugfix or feature? The release note is there because there is a user-observable impact, but the PR categorization is cleanup. So, I suspect this should be categorized as bugfix? But the user impact remains unclear to me, on the surface - I would need to check the code and tests. Please think if we could formulate the release-note to indicate the user-observable change.

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-07T11:29:33Z

i am leaning feature rather than bug. the old behavior was conservative, but not necessarily a correctness bug imo.
maybe something like:
```
TAS Balanced Placement for LeaderWorkerSet can now handle placements where only the leader domain has extra capacity and the other selected domains have just enough capacity for workers.
``` 
wdyt?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-11T07:52:38Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10948#pullrequestreview-4261633832" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10948#" title="Author self-approved">ShaanveerS</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-11T08:09:17Z

/remove-kind cleanup
/kind feature

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T09:35:18Z

cc @pajakd @mwysokin

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T11:16:39Z

- **pkg/cache/scheduler/tas_balanced_placement.go:347** by [@pajakd](https://github.com/pajakd) — 2026-05-05T11:16:39Z
```diff
@@ -318,18 +317,52 @@ func clearState(d *domain) {
 	}
 }
 
+func clearLeaderCapacity(d *domain) {
+	d.stateWithLeader = int32(0)
+	d.sliceStateWithLeader = int32(0)
+	d.leaderState = int32(0)
+	for _, child := range d.children {
+		clearLeaderCapacity(child)
+	}
+}
+
+func cloneDomains(domains []*domain) []*domain {
+	result := make([]*domain, len(domains))
+	for i, d := range domains {
+		result[i] = cloneDomain(d, nil)
+	}
+	return result
+}
+
+func cloneDomain(d *domain, parent *domain) *domain {
+	clone := *d
+	clone.parent = parent
+	clone.children = make([]*domain, len(d.children))
+	for i, child := range d.children {
+		clone.children[i] = cloneDomain(child, &clone)
+	}
+	return &clone
+}
+
+func pruneDomainBelowThreshold(d *domain, threshold int32, leaderRequired bool) {
```

  Lets give this function a different name so that its not confused with the one below.
  
  This function is pruning a single node, while the one below is going over a list of domains.

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-05T11:33:10Z

- **pkg/cache/scheduler/tas_balanced_placement.go:347** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-05T11:33:10Z
```diff
@@ -318,18 +317,52 @@ func clearState(d *domain) {
 	}
 }
 
+func clearLeaderCapacity(d *domain) {
+	d.stateWithLeader = int32(0)
+	d.sliceStateWithLeader = int32(0)
+	d.leaderState = int32(0)
+	for _, child := range d.children {
+		clearLeaderCapacity(child)
+	}
+}
+
+func cloneDomains(domains []*domain) []*domain {
+	result := make([]*domain, len(domains))
+	for i, d := range domains {
+		result[i] = cloneDomain(d, nil)
+	}
+	return result
+}
+
+func cloneDomain(d *domain, parent *domain) *domain {
+	clone := *d
+	clone.parent = parent
+	clone.children = make([]*domain, len(d.children))
+	for i, child := range d.children {
+		clone.children[i] = cloneDomain(child, &clone)
+	}
+	return &clone
+}
+
+func pruneDomainBelowThreshold(d *domain, threshold int32, leaderRequired bool) {
```

  changed to `pruneDomainNodeBelowThreshold`. Thanks!

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-05-05T12:26:57Z

It might be hard to understand what this change really does. Could you please provide more information? Something like state before/ state after would be great.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T12:47:41Z

- **pkg/cache/scheduler/tas_balanced_placement.go:72** by [@pajakd](https://github.com/pajakd) — 2026-05-05T12:47:41Z
```diff
@@ -69,12 +69,7 @@ func balanceThresholdValue(sliceCount int32, selectedDomainsCount int32, lastDom
 		threshold = min(threshold, lastDomainWithLeader.sliceStateWithLeader)
 	}
 	if lastDomain != nil {
-		// TODO: https://github.com/kubernetes-sigs/kueue/issues/7494
-		// we don't have min(threshold, lastDomain.sliceState) here because
-		// later we prune all nodes with sliceStateWithLeader < threshold
-		// while pruning we don't know which node will host the leader
-		// so we have to leave space on each node
-		threshold = min(threshold, lastDomain.sliceStateWithLeader)
+		threshold = min(threshold, lastDomain.sliceState)
```

  the PR is looking good
  
  But we need some more testing. Currently it only has unit tests for the pruning but since its changing a some important evaluation I would like to see some (integration?) tests for the LWS with balanced placement.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-05T12:54:40Z

- **pkg/cache/scheduler/tas_balanced_placement.go:279** by [@pajakd](https://github.com/pajakd) — 2026-05-05T12:54:40Z
```diff
@@ -257,19 +252,23 @@ func findBestDomainsForBalancedPlacement(s *TASFlavorSnapshot, params *topologyA
 	var currFitDomain []*domain
 
 	for _, requestedLevelSiblingDomains := range requestedLevelDomainsToConsider {
-		lowerLevelDomains := getLowerLevelDomains(s, requestedLevelSiblingDomains, params.requestedLevelIdx, params.sliceLevelIdx)
+		candidateDomains := cloneDomains(requestedLevelSiblingDomains)
+		lowerLevelDomains := getLowerLevelDomains(s, candidateDomains, params.requestedLevelIdx, params.sliceLevelIdx)
 		fits, selectedDomainsCount, lastDomainWithLeader, lastDomain := evaluateGreedyAssignment(s, lowerLevelDomains, sliceCount, params.leaderCount)
 		if !fits {
 			continue
 		}
 		threshold := balanceThresholdValue(sliceCount, selectedDomainsCount, lastDomainWithLeader, lastDomain)
 		if threshold >= bestThreshold {
-			s.pruneDomainsBelowThreshold(requestedLevelSiblingDomains, threshold, params.sliceSize, params.sliceLevelIdx, params.requestedLevelIdx, params.leaderCount > 0)
-			_, requestedLevelDomainCount, _, _ := evaluateGreedyAssignment(s, requestedLevelSiblingDomains, sliceCount, params.leaderCount)
+			s.pruneDomainsBelowThreshold(candidateDomains, threshold, params.sliceSize, params.sliceLevelIdx, params.requestedLevelIdx, params.leaderCount > 0)
+			fitsAfterPruning, requestedLevelDomainCount, _, _ := evaluateGreedyAssignment(s, candidateDomains, sliceCount, params.leaderCount)
+			if !fitsAfterPruning {
```

  Please add some testcase that enters the "!fitsAfterPruning" code branch. Also, I thought, that if we enter this branch -- meaning that we don't fit after pruning, we should also consider the lower value of the threshold (like calculated with the old code). Isn't it needed?

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-06T05:54:03Z

- **pkg/cache/scheduler/tas_balanced_placement.go:72** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-06T05:54:03Z
```diff
@@ -69,12 +69,7 @@ func balanceThresholdValue(sliceCount int32, selectedDomainsCount int32, lastDom
 		threshold = min(threshold, lastDomainWithLeader.sliceStateWithLeader)
 	}
 	if lastDomain != nil {
-		// TODO: https://github.com/kubernetes-sigs/kueue/issues/7494
-		// we don't have min(threshold, lastDomain.sliceState) here because
-		// later we prune all nodes with sliceStateWithLeader < threshold
-		// while pruning we don't know which node will host the leader
-		// so we have to leave space on each node
-		threshold = min(threshold, lastDomain.sliceStateWithLeader)
+		threshold = min(threshold, lastDomain.sliceState)
```

  Done. added a cache-level test.

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-06T05:54:25Z

- **pkg/cache/scheduler/tas_balanced_placement.go:279** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-06T05:54:25Z
```diff
@@ -257,19 +252,23 @@ func findBestDomainsForBalancedPlacement(s *TASFlavorSnapshot, params *topologyA
 	var currFitDomain []*domain
 
 	for _, requestedLevelSiblingDomains := range requestedLevelDomainsToConsider {
-		lowerLevelDomains := getLowerLevelDomains(s, requestedLevelSiblingDomains, params.requestedLevelIdx, params.sliceLevelIdx)
+		candidateDomains := cloneDomains(requestedLevelSiblingDomains)
+		lowerLevelDomains := getLowerLevelDomains(s, candidateDomains, params.requestedLevelIdx, params.sliceLevelIdx)
 		fits, selectedDomainsCount, lastDomainWithLeader, lastDomain := evaluateGreedyAssignment(s, lowerLevelDomains, sliceCount, params.leaderCount)
 		if !fits {
 			continue
 		}
 		threshold := balanceThresholdValue(sliceCount, selectedDomainsCount, lastDomainWithLeader, lastDomain)
 		if threshold >= bestThreshold {
-			s.pruneDomainsBelowThreshold(requestedLevelSiblingDomains, threshold, params.sliceSize, params.sliceLevelIdx, params.requestedLevelIdx, params.leaderCount > 0)
-			_, requestedLevelDomainCount, _, _ := evaluateGreedyAssignment(s, requestedLevelSiblingDomains, sliceCount, params.leaderCount)
+			s.pruneDomainsBelowThreshold(candidateDomains, threshold, params.sliceSize, params.sliceLevelIdx, params.requestedLevelIdx, params.leaderCount > 0)
+			fitsAfterPruning, requestedLevelDomainCount, _, _ := evaluateGreedyAssignment(s, candidateDomains, sliceCount, params.leaderCount)
+			if !fitsAfterPruning {
```

  sorry, missed that. added the fallback and tests.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-06T10:46:42Z

- **pkg/cache/scheduler/tas_balanced_placement_test.go:210** by [@pajakd](https://github.com/pajakd) — 2026-05-06T10:46:42Z
```diff
@@ -170,3 +170,110 @@ func TestPlaceSlicesOnDomainsBalanced(t *testing.T) {
 		})
 	}
 }
+
+func TestPruneDomainsBelowThresholdKeepsWorkerOnlyDomain(t *testing.T) {
+	leaderLeaf := &domain{id: "leader-leaf", state: 6, sliceState: 6, leaderState: 1, stateWithLeader: 5, sliceStateWithLeader: 5}
+	leaderDomain := &domain{id: "leader-domain", state: 6, sliceState: 6, leaderState: 1, stateWithLeader: 5, sliceStateWithLeader: 5, children: []*domain{leaderLeaf}}
+	leaderLeaf.parent = leaderDomain
+	workerOnlyLeaf := &domain{id: "worker-only-leaf", state: 5, sliceState: 5, leaderState: 1, stateWithLeader: 4, sliceStateWithLeader: 4}
+	workerOnlyDomain := &domain{id: "worker-only-domain", state: 5, sliceState: 5, leaderState: 1, stateWithLeader: 4, sliceStateWithLeader: 4, children: []*domain{workerOnlyLeaf}}
+	workerOnlyLeaf.parent = workerOnlyDomain
+	parentDomain := &domain{id: "parent-domain", children: []*domain{leaderDomain, workerOnlyDomain}}
+	leaderDomain.parent = parentDomain
+	workerOnlyDomain.parent = parentDomain
+
+	_, log := utiltesting.ContextWithLog(t)
+	s := newTASFlavorSnapshot(log, "dummy", []string{}, nil)
+
+	s.pruneDomainsBelowThreshold([]*domain{parentDomain}, 5, 1, 2, 0, true)
+
+	domainState := func(d *domain) [5]int32 {
+		return [5]int32{d.state, d.sliceState, d.stateWithLeader, d.sliceStateWithLeader, d.leaderState}
+	}
+	got := map[string][5]int32{
+		"leaderDomain":     domainState(leaderDomain),
+		"parentDomain":     domainState(parentDomain),
+		"workerOnlyDomain": domainState(workerOnlyDomain),
+		"workerOnlyLeaf":   domainState(workerOnlyLeaf),
+	}
+	want := map[string][5]int32{
+		"leaderDomain":     {6, 6, 5, 5, 1},
+		"parentDomain":     {11, 11, 10, 10, 1},
+		"workerOnlyDomain": {5, 5, 0, 0, 0},
+		"workerOnlyLeaf":   {5, 5, 0, 0, 0},
+	}
+	if diff := cmp.Diff(want, got); diff != "" {
+		t.Errorf("Unexpected domain state (-want,+got):\n%s", diff)
+	}
+}
+
+func TestFindBestDomainsForBalancedPlacementFallsBackAfterPruning(t *testing.T) {
```

  Lets refactor these testcases to align with the rest of the codebase. Define `TestFindBestDomainsForBalancedPlacement` in which we define testcases and then run them in a loop (just check other files in our repo). It'll be easier to add more tests.
  
  EDIT: same for `pruneDomainsBelowThreshold`

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-06T11:16:54Z

- **pkg/cache/scheduler/tas_balanced_placement_test.go:210** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-06T11:16:53Z
```diff
@@ -170,3 +170,110 @@ func TestPlaceSlicesOnDomainsBalanced(t *testing.T) {
 		})
 	}
 }
+
+func TestPruneDomainsBelowThresholdKeepsWorkerOnlyDomain(t *testing.T) {
+	leaderLeaf := &domain{id: "leader-leaf", state: 6, sliceState: 6, leaderState: 1, stateWithLeader: 5, sliceStateWithLeader: 5}
+	leaderDomain := &domain{id: "leader-domain", state: 6, sliceState: 6, leaderState: 1, stateWithLeader: 5, sliceStateWithLeader: 5, children: []*domain{leaderLeaf}}
+	leaderLeaf.parent = leaderDomain
+	workerOnlyLeaf := &domain{id: "worker-only-leaf", state: 5, sliceState: 5, leaderState: 1, stateWithLeader: 4, sliceStateWithLeader: 4}
+	workerOnlyDomain := &domain{id: "worker-only-domain", state: 5, sliceState: 5, leaderState: 1, stateWithLeader: 4, sliceStateWithLeader: 4, children: []*domain{workerOnlyLeaf}}
+	workerOnlyLeaf.parent = workerOnlyDomain
+	parentDomain := &domain{id: "parent-domain", children: []*domain{leaderDomain, workerOnlyDomain}}
+	leaderDomain.parent = parentDomain
+	workerOnlyDomain.parent = parentDomain
+
+	_, log := utiltesting.ContextWithLog(t)
+	s := newTASFlavorSnapshot(log, "dummy", []string{}, nil)
+
+	s.pruneDomainsBelowThreshold([]*domain{parentDomain}, 5, 1, 2, 0, true)
+
+	domainState := func(d *domain) [5]int32 {
+		return [5]int32{d.state, d.sliceState, d.stateWithLeader, d.sliceStateWithLeader, d.leaderState}
+	}
+	got := map[string][5]int32{
+		"leaderDomain":     domainState(leaderDomain),
+		"parentDomain":     domainState(parentDomain),
+		"workerOnlyDomain": domainState(workerOnlyDomain),
+		"workerOnlyLeaf":   domainState(workerOnlyLeaf),
+	}
+	want := map[string][5]int32{
+		"leaderDomain":     {6, 6, 5, 5, 1},
+		"parentDomain":     {11, 11, 10, 10, 1},
+		"workerOnlyDomain": {5, 5, 0, 0, 0},
+		"workerOnlyLeaf":   {5, 5, 0, 0, 0},
+	}
+	if diff := cmp.Diff(want, got); diff != "" {
+		t.Errorf("Unexpected domain state (-want,+got):\n%s", diff)
+	}
+}
+
+func TestFindBestDomainsForBalancedPlacementFallsBackAfterPruning(t *testing.T) {
```

  done!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T07:52:29Z

Ack, thank you 👍 
/lgtm
/approve
