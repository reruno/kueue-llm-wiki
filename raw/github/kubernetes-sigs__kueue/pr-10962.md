# PR #10962: [website]: add documentation for Concurrent Admission feature

**Summary**: [website]: add documentation for Concurrent Admission feature

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10962

**Last updated**: 2026-05-20T10:31:47Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yuluo-yx](https://github.com/yuluo-yx)
- **Branch**: `0505-yuluo/docs-1` → `main`
- **Created**: 2026-05-05T14:30:22Z
- **Updated**: 2026-05-20T10:31:47Z
- **Merged**: 2026-05-20T09:37:49Z
- **Closed**: 2026-05-20T09:37:49Z
- **Labels**: `lgtm`, `release-note-none`, `size/XL`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`, `area/website`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@windsonsea](https://github.com/windsonsea)
- **Changed files**: 9   **+769 / -0**

## Description

#### What type of PR is this?

/kind documentation
/area website

#### What this PR does / why we need it:

Fixes #10953

#### Does this PR introduce a user-facing change?

```release-note
None
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-05T14:30:30Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9a4e17a11ac2c4fc87dc1881670d13ae3011f7c9 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0c7d8245e1830008b8dfd6 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10962--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10962--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwOTYyLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.hHEs3QVIvHwNN5RuAfngOoMH-NQuzmTBJ0cUzN5MXO4)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10962--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T14:30:34Z

Hi @yuluo-yx. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-05T14:32:09Z

/ok-to-test
cc @PBundyra

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-06T06:51:02Z

/approve
Leaving lgtm to @PBundyra

### Comment by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-05-16T08:50:13Z

ping @PBundyra

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-19T11:46:32Z

Can we also align the documentation with this recent PR https://github.com/kubernetes-sigs/kueue/pull/11317?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-19T13:57:39Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10962#pullrequestreview-4319636898" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10962#" title="Author self-approved">yuluo-yx</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-20T09:09:52Z

/lgtm
Thank you @yuluo-yx!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-20T09:10:02Z

LGTM label has been added.  <details>Git tree hash: b56bbe737e930f5bbfe30eb9c8a580a102e27600</details>

## Reviews

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2026-05-18T10:02:12Z

- **site/content/en/docs/concepts/workload.md:26** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T09:53:15Z
```diff
@@ -20,6 +20,12 @@ Workload objects that represent the resource requirements of an arbitrary
 workload. Kueue automatically creates a Workload for each Job object and syncs
 the decisions and statuses.
 
+When [Concurrent Admission](/docs/tasks/manage/setup_concurrent_admission) is
+enabled for the Workload's ClusterQueue, Kueue can represent one job with a
+parent Workload and multiple variant Workloads. The parent Workload aggregates
+the admission status for the job integration, and each variant is an internal
```

  ```suggestion
  the admission status for the Job integration, and each variant is an internal
  ```

- **site/content/en/docs/concepts/workload.md:24** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T09:53:22Z
```diff
@@ -20,6 +20,12 @@ Workload objects that represent the resource requirements of an arbitrary
 workload. Kueue automatically creates a Workload for each Job object and syncs
 the decisions and statuses.
 
+When [Concurrent Admission](/docs/tasks/manage/setup_concurrent_admission) is
+enabled for the Workload's ClusterQueue, Kueue can represent one job with a
```

  ```suggestion
  enabled for the Workload's ClusterQueue, Kueue can represent one Job with a
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:1** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T09:55:15Z
  I think this belongs more to `concepts` directory rather than `tasks/manage`

- **site/content/en/docs/concepts/concurrent_admission.md:44** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T09:57:29Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
+for the same [Workload](/docs/concepts/workload) at the same time. A Workload can
+start on the first flavor that leads to admission, while Kueue can continue
+trying more preferred flavors and migrate the Workload if a better flavor later
+becomes available.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement or for migration to a preferred
+flavor, such as a reservation.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta2
+kind: Configuration
+featureGates:
+  ConcurrentAdmission: true
+```
```

  In Kueue we don't configure feature gates via Configuration but `--feature-gates` flag passed to binary

- **site/static/examples/admin/concurrent-admission-setup.yaml:33** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T09:58:20Z
```diff
@@ -0,0 +1,60 @@
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: reservation
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: on-demand
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: spot
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: team-cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "pods"]
+    flavors:
+    # Flavors are ordered from the most preferred to the least preferred.
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+      - name: pods
+        nominalQuota: 4
```

  I believe pods are redundant and it introduces unnecessary complexity

- **site/content/en/docs/tasks/manage/setup_concurrent_admission.md:12** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:00:47Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
```

  Concurrent Admission feature introduces two main functionalities:
  1) Migration to a more preferred flavor
  2) Trying to run an admission checks configured for various flavors at the same time - saying Kueue tries multiple flavors at the same time is a bit confusing as it doesn't contain that messaging. 
  
  I'd like to shift the weight of the description to better reflect 1) and 2)

- **site/content/en/docs/concepts/concurrent_admission.md:147** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:01:34Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
+for the same [Workload](/docs/concepts/workload) at the same time. A Workload can
+start on the first flavor that leads to admission, while Kueue can continue
+trying more preferred flavors and migrate the Workload if a better flavor later
+becomes available.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement or for migration to a preferred
+flavor, such as a reservation.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta2
+kind: Configuration
+featureGates:
+  ConcurrentAdmission: true
+```
+
+Follow the
+[custom configuration installation instructions](/docs/installation#install-a-custom-configured-released-version)
+for details on changing the Kueue configuration.
+
+## Configure flavors and queues
+
+Create a `ClusterQueue` with multiple flavors in one `resourceGroup` and enable
+`.spec.concurrentAdmissionPolicy`. The flavor order matters: list flavors from
+the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, Kueue keeps trying `reservation` and `on-demand`. If a
+more preferred flavor is admitted later, Kueue migrates the Workload to that
+flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
+
+Find the variants for a parent Workload by looking for Workloads with an owner
+reference to the parent:
+
+```shell
+kubectl get workloads -o yaml
+```
+
+The parent Workload is the object that job integrations watch for admission.
+Variant Workloads are internal admission attempts. Do not create or edit parent
+labels or variant annotations manually.
+
+## Constraints
+
+Concurrent Admission currently has the following constraints:
```

  It's missing constraint on CQ's queueing strategy

- **site/content/en/docs/concepts/cluster_queue.md:74** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:02:07Z
```diff
@@ -70,6 +70,11 @@ Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroups[*].fl
 list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
+When [Concurrent Admission](/docs/tasks/manage/setup_concurrent_admission) is
+enabled for a ClusterQueue, Kueue can try more than one flavor at the same time.
```

  See my other comment: https://github.com/kubernetes-sigs/kueue/pull/10962/changes#r3257979536

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:11:32Z

- **site/content/en/docs/tasks/manage/setup_concurrent_admission.md:71** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:11:32Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
+for the same [Workload](/docs/concepts/workload) at the same time. A Workload can
+start on the first flavor that leads to admission, while Kueue can continue
+trying more preferred flavors and migrate the Workload if a better flavor later
+becomes available.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement or for migration to a preferred
+flavor, such as a reservation.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta2
+kind: Configuration
+featureGates:
+  ConcurrentAdmission: true
+```
+
+Follow the
+[custom configuration installation instructions](/docs/installation#install-a-custom-configured-released-version)
+for details on changing the Kueue configuration.
+
+## Configure flavors and queues
+
+Create a `ClusterQueue` with multiple flavors in one `resourceGroup` and enable
+`.spec.concurrentAdmissionPolicy`. The flavor order matters: list flavors from
+the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, Kueue keeps trying `reservation` and `on-demand`. If a
```

  Can we mention that if Workload starts on `spot`, it's because there's no quota on the `reservation`/`on-demand` or there are pending admission checks?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:13:39Z

- **site/content/en/docs/tasks/manage/setup_concurrent_admission.md:47** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:13:39Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
+for the same [Workload](/docs/concepts/workload) at the same time. A Workload can
+start on the first flavor that leads to admission, while Kueue can continue
+trying more preferred flavors and migrate the Workload if a better flavor later
+becomes available.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement or for migration to a preferred
+flavor, such as a reservation.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta2
+kind: Configuration
+featureGates:
+  ConcurrentAdmission: true
+```
+
+Follow the
+[custom configuration installation instructions](/docs/installation#install-a-custom-configured-released-version)
```

  This link doesn't exist

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:15:28Z

- **site/content/en/docs/concepts/concurrent_admission.md:75** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:15:29Z
```diff
@@ -0,0 +1,143 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to admit workloads by trying multiple resource flavors concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue try multiple [ResourceFlavors](/docs/concepts/resource_flavor)
+for the same [Workload](/docs/concepts/workload) at the same time. A Workload can
+start on the first flavor that leads to admission, while Kueue can continue
+trying more preferred flavors and migrate the Workload if a better flavor later
+becomes available.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement or for migration to a preferred
+flavor, such as a reservation.
+
+The intended audience for this page are [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```yaml
+apiVersion: config.kueue.x-k8s.io/v1beta2
+kind: Configuration
+featureGates:
+  ConcurrentAdmission: true
+```
+
+Follow the
+[custom configuration installation instructions](/docs/installation#install-a-custom-configured-released-version)
+for details on changing the Kueue configuration.
+
+## Configure flavors and queues
+
+Create a `ClusterQueue` with multiple flavors in one `resourceGroup` and enable
+`.spec.concurrentAdmissionPolicy`. The flavor order matters: list flavors from
+the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, Kueue keeps trying `reservation` and `on-demand`. If a
+more preferred flavor is admitted later, Kueue migrates the Workload to that
+flavor.
+
+## Limit migration to a minimum preferred flavor
```

  Can we also add example reflecting [story 3 from KEP](https://github.com/kubernetes-sigs/kueue/tree/main/keps/8691-concurrent-admission#story-3-reservation--homogenous-flavors)?

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-05-18T14:27:48Z

- **site/static/examples/admin/concurrent-admission-setup.yaml:33** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-05-18T14:27:48Z
```diff
@@ -0,0 +1,60 @@
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: reservation
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: on-demand
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: spot
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: team-cluster-queue
+spec:
+  namespaceSelector: {}
+  resourceGroups:
+  - coveredResources: ["cpu", "memory", "pods"]
+    flavors:
+    # Flavors are ordered from the most preferred to the least preferred.
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+      - name: pods
+        nominalQuota: 4
```

  got

### Review by [@PBundyra](https://github.com/PBundyra) (changes_requested) — 2026-05-18T15:23:32Z

- **site/content/en/docs/concepts/concurrent_admission.md:67** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:41:14Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
```

  ```suggestion
  Create a `ClusterQueue` with `.spec.concurrentAdmissionPolicy` defined multiple Resource Flavors in one ResourceGroup. The order of flavors matters - list flavors from the most preferred to the least preferred.
  ```
  
  BestEffortFIFO is a default mode, so I believe we don't have to mention it explicitly. Let's just mention StrictFIFO is unsupported in the constraints list

- **site/static/examples/admin/concurrent-admission-setup.yaml:22** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:41:25Z
```diff
@@ -0,0 +1,55 @@
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: reservation
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: on-demand
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: spot
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: team-cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
```

  ```suggestion
  ```

- **site/static/examples/admin/concurrent-admission-setup.yaml:47** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:43:59Z
```diff
@@ -0,0 +1,55 @@
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: reservation
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: on-demand
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: spot
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: team-cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    # Flavors are ordered from the most preferred to the least preferred.
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: on-demand
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: spot
+      resources:
+      - name: cpu
+        nominalQuota: 16
+      - name: memory
+        nominalQuota: 64Gi
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
```

  Can we move it above resourceGroups?

- **site/content/en/docs/concepts/concurrent_admission.md:202** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:44:43Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Use a reservation with homogeneous flavors
+
+Use `minPreferredFlavorName` when you have one preferred reservation flavor and
+several less preferred homogeneous flavors. This lets workloads migrate to the
+reservation if it becomes available, without migrating between homogeneous
+fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        minPreferredFlavorName: reservation
+```
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
+
+Find the variants for a parent Workload by looking for Workloads with an owner
+reference to the parent:
+
+```shell
+kubectl get workloads -o yaml
+```
+
+The parent Workload is the object that job integrations watch for admission.
+Variant Workloads are internal admission attempts. Do not create or edit parent
+labels or variant annotations manually.
+
+## Constraints
+
+Concurrent Admission currently has the following constraints:
+
+- The feature is available on the `v1beta2` ClusterQueue API.
+- The `ConcurrentAdmission` feature gate must be enabled.
+- A `ClusterQueue` with `.spec.concurrentAdmissionPolicy` must use
+  `BestEffortFIFO`. `StrictFIFO` is not supported.
```

  ```suggestion
    `BestEffortFIFO` queueing strategy. `StrictFIFO` is not supported.
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:189** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:49:34Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Use a reservation with homogeneous flavors
+
+Use `minPreferredFlavorName` when you have one preferred reservation flavor and
+several less preferred homogeneous flavors. This lets workloads migrate to the
+reservation if it becomes available, without migrating between homogeneous
+fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        minPreferredFlavorName: reservation
+```
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
+
+Find the variants for a parent Workload by looking for Workloads with an owner
+reference to the parent:
+
+```shell
+kubectl get workloads -o yaml
+```
```

  Can we add an example how this owner reference can look like?

- **site/content/en/docs/concepts/concurrent_admission.md:182** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T14:51:11Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Use a reservation with homogeneous flavors
+
+Use `minPreferredFlavorName` when you have one preferred reservation flavor and
+several less preferred homogeneous flavors. This lets workloads migrate to the
+reservation if it becomes available, without migrating between homogeneous
+fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        minPreferredFlavorName: reservation
+```
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
```

  Can we add that a user should look for a the parent label `"kueue.x-k8s.io/concurrent-admission-parent"` and add example output with metadata of parent workload?

- **site/content/en/docs/concepts/concurrent_admission.md:22** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:11:24Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
```

  ```suggestion
  Concurrent Admission consists of two main components:
  - **Event-driven migration:** Moving a running Workload to a more preferred ResourceFlavor as soon as that flavor becomes available.
  - **Concurrent multi-flavor pursuit:** Allowing a Workload to independently pursue multiple ResourceFlavors at the same time
  
  The feature implements this by creating so-called Variants of the original Workload (referred to as the Parent Workload). Each Variant is a copy of the Parent Workload assigned to a specific ResourceFlavor, allowing them to try to schedule concurrently and independently on their respective flavors.
  
  The order of Flavors within a ClusterQueue's ResourceGroups dictates their preference, with the first Flavor being the most preferred.
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:87** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:13:50Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
```

  ```suggestion
  `on-demand`. If a Variant assigned to more preferred flavor is admitted later, Kueue migrates the
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:93** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:17:20Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
```

  ```suggestion
  If you want to limit migration, only to ResourceFlavors above a certain threshold, use `minPreferredFlavorName` API. It lets the user define what is the minimal flavor a Workload can migrate to. 
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:96** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:18:29Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
```

  ```suggestion
  For example, using this policy a Workload can migrate from `spot` to `reservation`, and from `on-demand` to `reservation`, but not from `spot` to `on-demand`
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:107** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:21:29Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
```

  ```suggestion
  ```

- **site/content/en/docs/concepts/concurrent_admission.md:109** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:23:03Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Use a reservation with homogeneous flavors
```

  ```suggestion
  ## Reservation with homogeneous flavors setup
  ```

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-05-18T15:56:45Z

- **site/static/examples/admin/concurrent-admission-setup.yaml:47** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-05-18T15:56:45Z
```diff
@@ -0,0 +1,55 @@
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: reservation
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: on-demand
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ResourceFlavor
+metadata:
+  name: spot
+---
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: team-cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    # Flavors are ordered from the most preferred to the least preferred.
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: on-demand
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: spot
+      resources:
+      - name: cpu
+        nominalQuota: 16
+      - name: memory
+        nominalQuota: 64Gi
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
```

  done

### Review by [@yuluo-yx](https://github.com/yuluo-yx) (commented) — 2026-05-18T15:58:08Z

- **site/content/en/docs/concepts/concurrent_admission.md:189** by [@yuluo-yx](https://github.com/yuluo-yx) — 2026-05-18T15:58:08Z
```diff
@@ -0,0 +1,216 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets a [Workload](/docs/concepts/workload) start on an
+admitted [ResourceFlavor](/docs/concepts/resource_flavor), while Kueue keeps
+admission attempts for more preferred flavors.
+
+Concurrent Admission has two main effects:
+
+- Kueue can migrate a running Workload to a more preferred ResourceFlavor when
+  that flavor becomes available.
+- Kueue can run admission checks for variants constrained to different
+  ResourceFlavors concurrently, instead of waiting for one flavor's admission
+  check before trying another flavor.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `BestEffortFIFO` `ClusterQueue` with multiple flavors in one
+`resourceGroup` and enable `.spec.concurrentAdmissionPolicy`. The flavor order
+matters: list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+Use `minPreferredFlavorName` when you want migration only to a specific flavor or
+to a flavor that is more preferred than it.
+
+For example, this policy allows migration to `reservation`, but not from `spot`
+to `on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      minPreferredFlavorName: reservation
+```
+
+Kueue compares `minPreferredFlavorName` using the order of
+`.spec.resourceGroups[*].flavors` in the `ClusterQueue`.
+
+## Use a reservation with homogeneous flavors
+
+Use `minPreferredFlavorName` when you have one preferred reservation flavor and
+several less preferred homogeneous flavors. This lets workloads migrate to the
+reservation if it becomes available, without migrating between homogeneous
+fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  queueingStrategy: BestEffortFIFO
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        minPreferredFlavorName: reservation
+```
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
+
+Find the variants for a parent Workload by looking for Workloads with an owner
+reference to the parent:
+
+```shell
+kubectl get workloads -o yaml
+```
```

  done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-19T11:45:36Z

- **site/content/en/docs/concepts/cluster_queue.md:76** by [@PBundyra](https://github.com/PBundyra) — 2026-05-19T11:45:36Z
```diff
@@ -70,6 +70,12 @@ Kueue assigns the first flavor in the ClusterQueue's `.spec.resourceGroups[*].fl
 list that has enough unused `nominalQuota` quota in the ClusterQueue or the
 ClusterQueue's [cohort](#cohort).
 
+When [Concurrent Admission](/docs/concepts/concurrent_admission) is enabled for
+a ClusterQueue, the order of `.spec.resourceGroups[*].flavors` also defines
+flavor preference: the first flavor is the most preferred flavor for migration.
+Kueue can run admission checks for variants constrained to different flavors
```

  ```suggestion
  Kueue can concurrently pursue admission on multiple flavors independently
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-19T12:01:04Z

- **site/content/en/docs/concepts/concurrent_admission.md:53** by [@mimowo](https://github.com/mimowo) — 2026-05-19T12:01:04Z
```diff
@@ -0,0 +1,220 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission consists of two main components:
+- **Event-driven migration:** Moving a running Workload to a more preferred ResourceFlavor as soon as that flavor becomes available.
+- **Concurrent multi-flavor pursuit:** Allowing a Workload to independently pursue multiple ResourceFlavors at the same time
+
+The feature implements this by creating so-called Variants of the original Workload (referred to as the Parent Workload). Each Variant is a copy of the Parent Workload assigned to a specific ResourceFlavor, allowing them to try to schedule concurrently and independently on their respective flavors.
+
+The order of Flavors within a ClusterQueue's ResourceGroups dictates their preference, with the first Flavor being the most preferred.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
```

  no need for the snippet imo, referring to the page which says how to enable FG is enough. Details will also depend if by args (like here) or config API.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-19T12:02:17Z

- **site/content/en/docs/concepts/concurrent_admission.md:86** by [@mimowo](https://github.com/mimowo) — 2026-05-19T12:02:17Z
```diff
@@ -0,0 +1,220 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission consists of two main components:
+- **Event-driven migration:** Moving a running Workload to a more preferred ResourceFlavor as soon as that flavor becomes available.
+- **Concurrent multi-flavor pursuit:** Allowing a Workload to independently pursue multiple ResourceFlavors at the same time
+
+The feature implements this by creating so-called Variants of the original Workload (referred to as the Parent Workload). Each Variant is a copy of the Parent Workload assigned to a specific ResourceFlavor, allowing them to try to schedule concurrently and independently on their respective flavors.
+
+The order of Flavors within a ClusterQueue's ResourceGroups dictates their preference, with the first Flavor being the most preferred.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+- The `ConcurrentAdmission` feature gate is enabled in the Kueue controller manager.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Install or reconfigure Kueue with the feature gate enabled:
+
+```diff
+kind: Deployment
+...
+spec:
+  ...
+  template:
+    ...
+    spec:
+      containers:
+      - name: manager
+        args:
++       - --feature-gates=ConcurrentAdmission=true
+```
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+for details.
+
+## Configure flavors and queues
+
+Create a `ClusterQueue` with `.spec.concurrentAdmissionPolicy` defined multiple Resource Flavors in one ResourceGroup. The order of flavors matters - list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a Variant assigned to more preferred flavor is admitted later, Kueue migrates the
+Workload to that flavor.
+
+## Limit migration to a minimum preferred flavor
+
+If you want to limit migration, only to ResourceFlavors above a certain threshold, use `minPreferredFlavorName` API. It lets the user define what is the minimal flavor a Workload can migrate to. 
```

  This param is now renamed to lastAcceptableFlavorName, please rename consistently in the doc

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-19T12:05:12Z

- **site/content/en/docs/concepts/concurrent_admission.md:26** by [@mimowo](https://github.com/mimowo) — 2026-05-19T12:05:12Z
```diff
@@ -0,0 +1,220 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission consists of two main components:
+- **Event-driven migration:** Moving a running Workload to a more preferred ResourceFlavor as soon as that flavor becomes available.
+- **Concurrent multi-flavor pursuit:** Allowing a Workload to independently pursue multiple ResourceFlavors at the same time
+
+The feature implements this by creating so-called Variants of the original Workload (referred to as the Parent Workload). Each Variant is a copy of the Parent Workload assigned to a specific ResourceFlavor, allowing them to try to schedule concurrently and independently on their respective flavors.
+
+The order of Flavors within a ClusterQueue's ResourceGroups dictates their preference, with the first Flavor being the most preferred.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+The intended audience for this page is [batch administrators](/docs/tasks#batch-administrator).
+
+## Before you begin
```

  Actually, the first part is the concept, but this looks more like a tutorial / how to. 
  
  For that we have the tasks page, I would probably move this section to here: https://kueue.sigs.k8s.io/docs/tasks/manage/
  
  So I propose to split the page into two. Concepts and Tasks, both should x-reference.
  
  The concepts page could still benefit from a small snippet, but without the whole "hands on" experience.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-19T13:57:27Z

/approve
Leaving lgtm to @PBundyra 
Thank you for adding documentation for the important feature
/cherrypick website

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-19T14:07:14Z

- **site/content/en/docs/concepts/concurrent_admission.md:118** by [@PBundyra](https://github.com/PBundyra) — 2026-05-19T14:07:14Z
```diff
@@ -0,0 +1,160 @@
+---
+title: "Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+Concurrent Admission lets Kueue keep several admission attempts for the same
+[Workload](/docs/concepts/workload), each constrained to a different
+[ResourceFlavor](/docs/concepts/resource_flavor). This lets a Workload start on
+an admitted flavor while Kueue continues pursuing more preferred flavors.
+
+Concurrent Admission consists of two main components:
+
+- **Event-driven migration:** Kueue can move a running Workload to a more
+  preferred ResourceFlavor as soon as that flavor becomes available.
+- **Concurrent multi-flavor pursuit:** A Workload can independently pursue
+  multiple ResourceFlavors at the same time.
+
+Kueue implements this by creating variants of the original Workload, which is
+called the parent Workload. Each variant is a copy of the parent Workload
+assigned to a specific ResourceFlavor, allowing variants to try scheduling
+concurrently and independently on their respective flavors. The order of flavors
+within a ClusterQueue's resource groups dictates their preference, with the first
+flavor being the most preferred.
+
+Use Concurrent Admission when workloads can tolerate disruption and you want to
+trade extra scheduling work for faster placement, concurrent admission checks,
+or migration to a preferred flavor, such as a reservation.
+
+## Flavor preference and migration
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on a less preferred flavor, Kueue keeps pursuing variants for
+more preferred flavors. If a variant assigned to a more preferred flavor is
+admitted later, Kueue migrates the Workload to that flavor.
+
+If you want to limit migration to flavors above a certain preference threshold,
+use the `lastAcceptableFlavorName` API. It defines the last acceptable flavor a
+Workload can migrate to.
+
+For example, using this policy a Workload can migrate from `spot` to
+`reservation`, and from `on-demand` to `reservation`, but not from `spot` to
+`on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      lastAcceptableFlavorName: reservation
+```
+
+In a reservation with homogeneous fallback flavors setup,
+`lastAcceptableFlavorName` lets workloads migrate to the reservation if it
+becomes available, without migrating between homogeneous fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        lastAcceptableFlavorName: reservation
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+```
+
+## Parent and variant Workloads
+
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+Variant Workloads are labeled with the parent label
+`kueue.x-k8s.io/concurrent-admission-parent`, which stores the parent Workload
+name. A variant Workload can have metadata like this:
```

  This is incorrect. It's actually Parent Workloads that have this label and it's of boolean type. Parent Workloads have it set to `true`

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-19T14:08:10Z

- **site/content/en/docs/tasks/manage/setup_concurrent_admission.md:155** by [@PBundyra](https://github.com/PBundyra) — 2026-05-19T14:08:10Z
```diff
@@ -0,0 +1,187 @@
+---
+title: "Set Up Concurrent Admission"
+linkTitle: "Concurrent Admission"
+date: 2026-05-05
+weight: 8
+description: >
+  Configure Kueue to migrate admitted workloads to more preferred ResourceFlavors and run flavor-scoped admission checks concurrently.
+---
+
+{{< feature-state state="alpha" for_version="v0.18" >}}
+
+This page shows how to set up [Concurrent Admission](/docs/concepts/concurrent_admission)
+for a ClusterQueue.
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation) in version 0.18 or later.
+
+## Enable the feature gate
+
+`ConcurrentAdmission` is an alpha feature and is disabled by default.
+
+Follow the
+[feature gates configuration instructions](/docs/installation/#change-the-feature-gates-configuration)
+to enable the `ConcurrentAdmission` feature gate in the Kueue controller manager.
+
+## Configure flavors and queues
+
+Create a `ClusterQueue` with `.spec.concurrentAdmissionPolicy` defined and
+multiple ResourceFlavors in one ResourceGroup. The order of flavors matters:
+list flavors from the most preferred to the least preferred.
+
+{{< include "examples/admin/concurrent-admission-setup.yaml" "yaml" >}}
+
+Create the setup:
+
+```shell
+kubectl apply -f https://kueue.sigs.k8s.io/examples/admin/concurrent-admission-setup.yaml
+```
+
+With this configuration, Kueue can create one admission attempt for each flavor:
+
+- `reservation` is the most preferred flavor.
+- `on-demand` is less preferred than `reservation`.
+- `spot` is the least preferred flavor.
+
+The only supported migration mode is `TryPreferredFlavors`. In this mode, if a
+Workload starts on `spot`, it means `reservation` and `on-demand` did not admit
+the Workload at that time because quota was unavailable or the required
+admission checks were still pending. Kueue keeps trying `reservation` and
+`on-demand`. If a variant assigned to a more preferred flavor is admitted later,
+Kueue migrates the Workload to that flavor.
+
+## Limit migration to a last acceptable flavor
+
+If you want to limit migration to ResourceFlavors above a certain threshold, use
+the `lastAcceptableFlavorName` API. It lets you define the last acceptable flavor
+a Workload can migrate to.
+
+For example, using this policy a Workload can migrate from `spot` to
+`reservation`, and from `on-demand` to `reservation`, but not from `spot` to
+`on-demand`:
+
+```yaml
+concurrentAdmissionPolicy:
+  migration:
+    mode: TryPreferredFlavors
+    constraints:
+      lastAcceptableFlavorName: reservation
+```
+
+## Reservation with homogeneous flavors setup
+
+Use `lastAcceptableFlavorName` when you have one preferred reservation flavor and
+several less preferred homogeneous flavors. This lets workloads migrate to the
+reservation if it becomes available, without migrating between homogeneous
+fallback flavors.
+
+For example, a Workload running on `zone-b` can migrate to `reservation`, but it
+doesn't migrate to `zone-a`:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta2
+kind: ClusterQueue
+metadata:
+  name: cluster-queue
+spec:
+  namespaceSelector: {}
+  concurrentAdmissionPolicy:
+    migration:
+      mode: TryPreferredFlavors
+      constraints:
+        lastAcceptableFlavorName: reservation
+  resourceGroups:
+  - coveredResources: ["cpu", "memory"]
+    flavors:
+    - name: reservation
+      resources:
+      - name: cpu
+        nominalQuota: 4
+      - name: memory
+        nominalQuota: 16Gi
+    - name: zone-a
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-b
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+    - name: zone-c
+      resources:
+      - name: cpu
+        nominalQuota: 8
+      - name: memory
+        nominalQuota: 32Gi
+  admissionChecksStrategy:
+    admissionChecks:
+    - name: capacity-check
+      onFlavors: [zone-a, zone-b, zone-c]
+```
+
+## Observe Concurrent Admission
+
+Submit workloads to a `LocalQueue` that points to the configured `ClusterQueue`.
+When Concurrent Admission is enabled for the `ClusterQueue`, Kueue marks the
+original Workload as a parent and creates variant Workloads owned by that parent.
+Each variant is constrained to one ResourceFlavor.
+
+List the Workloads:
+
+```shell
+kubectl get workloads
+```
+
+Inspect a parent Workload:
+
+```shell
+kubectl describe workload WORKLOAD_NAME
+```
+
+Find variants by looking for Workloads with the parent label
+`kueue.x-k8s.io/concurrent-admission-parent`:
+
+```shell
+kubectl get workloads -l kueue.x-k8s.io/concurrent-admission-parent=WORKLOAD_NAME
+```
```

  ditto

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-19T14:09:39Z

Can we also use camel case when referring to Parents or Variants, to make it consistent with e.g. comments in the codebase?
