# PR #10966: Simplify  TAS validation webhook

**Summary**: Simplify  TAS validation webhook

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10966

**Last updated**: 2026-05-06T06:36:27Z

---

## Metadata

- **State**: open
- **Author**: [@kaisoz](https://github.com/kaisoz)
- **Branch**: `7297-simlify-tas-validation-wh` → `main`
- **Created**: 2026-05-05T15:54:09Z
- **Updated**: 2026-05-06T06:36:27Z
- **Merged**: —
- **Closed**: —
- **Labels**: `release-note-none`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 3   **+28 / -49**

## Description


#### What type of PR is this?

/kind cleanup


#### What this PR does / why we need it:
TAS validation in the job's webhook is too complex. This PR simplifies it based on the implementation in the TrainJob webhook

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #7297

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-05T15:54:17Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c56c5a63c778f6ddcbb5c72c6613882df190afaa |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/69fa12a461875400088cec54 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-10966--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-10966--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEwOTY2LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.cgs8hOdYE3jRLQzJNmCxqkKpddRg58wtDxATvAQYmgM)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-10966--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-05T15:54:20Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10966#" title="Author self-approved">kaisoz</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T19:41:50Z

- **pkg/controller/jobs/mpijob/mpijob_webhook.go:183** by [@mimowo](https://github.com/mimowo) — 2026-05-05T19:41:50Z
```diff
@@ -175,21 +178,13 @@ func (w *MpiJobWebhook) validateTopologyRequest(ctx context.Context, mpiJob *MPI
 	var allErrs field.ErrorList
 
 	podSets, podSetsErr := jobframework.JobPodSets(ctx, mpiJob)
-
 	if podSetsErr == nil {
-		allErrs = append(allErrs, jobframework.ValidatePodSetGroupingTopology(podSets, podSetAnnotationsPathByName)...)
-	}
-
-	for replicaType, replicaSpec := range mpiJob.Spec.MPIReplicaSpecs {
-		replicaMetaPath := mpiReplicaSpecsPath.Key(string(replicaType)).Child("template", "metadata")
-		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(replicaMetaPath, &replicaSpec.Template.ObjectMeta)...)
-
-		if podSetsErr != nil {
-			continue
+		for _, p := range podSets {
+			replicaMetaPath := podSetMetadataPathByName[p.Name]
```

  Can we be sure there are no other types? I'm wondering if we could generalize that so that we would not panic if another worker type is introduced or used

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-05T19:44:32Z

LGTM, thanks 👍 I have a small doubt about the mpi operator if we can assume the set of replica types is closed.

### Review by [@kaisoz](https://github.com/kaisoz) (commented) — 2026-05-05T22:00:41Z

- **pkg/controller/jobs/mpijob/mpijob_webhook.go:183** by [@kaisoz](https://github.com/kaisoz) — 2026-05-05T22:00:41Z
```diff
@@ -175,21 +178,13 @@ func (w *MpiJobWebhook) validateTopologyRequest(ctx context.Context, mpiJob *MPI
 	var allErrs field.ErrorList
 
 	podSets, podSetsErr := jobframework.JobPodSets(ctx, mpiJob)
-
 	if podSetsErr == nil {
-		allErrs = append(allErrs, jobframework.ValidatePodSetGroupingTopology(podSets, podSetAnnotationsPathByName)...)
-	}
-
-	for replicaType, replicaSpec := range mpiJob.Spec.MPIReplicaSpecs {
-		replicaMetaPath := mpiReplicaSpecsPath.Key(string(replicaType)).Child("template", "metadata")
-		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(replicaMetaPath, &replicaSpec.Template.ObjectMeta)...)
-
-		if podSetsErr != nil {
-			continue
+		for _, p := range podSets {
+			replicaMetaPath := podSetMetadataPathByName[p.Name]
```

  The replica types are defined by the Kubeflow MPI operator API and currently [those are the only ones available](https://pkg.go.dev/github.com/kubeflow/mpi-operator@v0.8.0/pkg/apis/kubeflow/v2beta1#MPIReplicaType) and [the only ones we consider](https://github.com/kubernetes-sigs/kueue/blob/b4deeaf4ed3e4816c733039a233761b21a0d63f5/pkg/controller/jobs/mpijob/mpijob_controller.go#L221). Adding a new replica type would definitely require some changes to the controller and therefore to the webhook, as issues would likely surface during development.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-06T06:36:27Z

- **pkg/controller/jobs/mpijob/mpijob_webhook.go:183** by [@mimowo](https://github.com/mimowo) — 2026-05-06T06:36:27Z
```diff
@@ -175,21 +178,13 @@ func (w *MpiJobWebhook) validateTopologyRequest(ctx context.Context, mpiJob *MPI
 	var allErrs field.ErrorList
 
 	podSets, podSetsErr := jobframework.JobPodSets(ctx, mpiJob)
-
 	if podSetsErr == nil {
-		allErrs = append(allErrs, jobframework.ValidatePodSetGroupingTopology(podSets, podSetAnnotationsPathByName)...)
-	}
-
-	for replicaType, replicaSpec := range mpiJob.Spec.MPIReplicaSpecs {
-		replicaMetaPath := mpiReplicaSpecsPath.Key(string(replicaType)).Child("template", "metadata")
-		allErrs = append(allErrs, jobframework.ValidateTASPodSetRequest(replicaMetaPath, &replicaSpec.Template.ObjectMeta)...)
-
-		if podSetsErr != nil {
-			continue
+		for _, p := range podSets {
+			replicaMetaPath := podSetMetadataPathByName[p.Name]
```

  Yes, we certainly don't support other replicaTypes, but I'm wondering more about creating maliciously mpi job with a custom replica type, rather than about proper support.
  
  Just for sanity check as we are on this - could you check experimentally what would happen on a real cluster on a release, and on this PR if one tries to create an MPI job with an unrecognized replicaType. I'm wondering if this could be a vector of attack to make Kueue panic. 
  
  If this panic does not exist - great. If it existed prior to the PR, then also ok, but I would like to follow up with a bugfix to eliminate this volnurability.
