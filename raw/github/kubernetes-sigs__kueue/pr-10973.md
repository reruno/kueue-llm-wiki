# PR #10973: Add event-driven DeviceClass tracking for extended resources

**Summary**: Add event-driven DeviceClass tracking for extended resources

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/10973

**Last updated**: 2026-05-29T12:20:57Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PannagaRao](https://github.com/PannagaRao)
- **Branch**: `graduate-extended-resources-beta` → `main`
- **Created**: 2026-05-06T04:54:17Z
- **Updated**: 2026-05-29T12:20:57Z
- **Merged**: 2026-05-27T20:26:54Z
- **Closed**: 2026-05-27T20:26:54Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XL`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@sohankunkerkar](https://github.com/sohankunkerkar), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 14   **+933 / -18**

## Description

Signed-off-by: Pannaga Rao Bhoja Ramamanohara

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->

#### What this PR does / why we need it:

Adds event-driven DeviceClass tracking with a DRABackedResources cache and workload index by extended resource names, ensuring only DRA-backed workloads enter the DRA path and only affected workloads are requeued on DeviceClass changes

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #https://github.com/kubernetes-sigs/kueue/pull/11552

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
DRA: Fix the performance of determining if a Workload should be handled using DRA Extended Resources, 
using an event-driven maintained cache of DRA Extended Resources.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-06T04:54:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | ba94cc82b66274ee378a311d9e0858cc5196918b |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a174b06fc5aa300072e0c46 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-06T04:54:27Z

Hi @PannagaRao. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-06T13:57:33Z

/ok-to-test

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-06T18:35:54Z

/test pull-kueue-test-e2e-main-1-35

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-07T18:53:35Z

/test pull-kueue-test-integration-extended-main

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-07T19:09:24Z

/test pull-kueue-test-e2e-main-1-33

### Comment by [@kannon92](https://github.com/kannon92) — 2026-05-07T19:43:52Z

@sohankunkerkar @PannagaRao 

should we close https://github.com/kubernetes-sigs/kueue/pull/10996?

It seems you carried that commit into this PR.

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-07T19:57:55Z

> @sohankunkerkar @PannagaRao
> 
> should we close #10996?
> 
> It seems you carried that commit into this PR.

Maybe I could add #10996 as a dependency for this PR.  Just to make sure #10996 isn't blocked coz of the changes in this PR.

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-18T21:33:00Z

JFYI: https://github.com/kubernetes-sigs/kueue/pull/10996#issuecomment-4427350328

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-22T12:09:39Z

/reopen

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-22T12:09:46Z

@sohankunkerkar: Reopened this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/10973#issuecomment-4518517804):

>/reopen


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-22T13:40:38Z

@PannagaRao @sohankunkerkar please de-couple the ER mechanism changes from the actual graduation. As I propose here: https://github.com/kubernetes-sigs/kueue/pull/10996#issuecomment-4519109275 we may consider staying with the ER in Alpha for 0.18, at the same time we would like to merge the mechanism improvements.

Staying in Alpha for 0.18 I consider is still not determined, but I want to basically de-couple the two decisions.

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-22T17:24:14Z

@mimowo  @tenzen-y Thanks for the feedback. I've decoupled the two — this PR now contains only the mechanism improvements and the  feature gate stays at Alpha (default false).

  - DeviceClass watcher on the workload controller for create/update/delete events
  - DRABackedResources cache — ensures workloads with domain-qualified resources that aren't DRA-backed (no corresponding DeviceClass) skip DRA processing entirely
  - Workload index by extended resource names — targeted requeue of only affected workloads on DeviceClass changes, instead of listing all pending workloads

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-22T17:42:36Z

cc @sohankunkerkar ptal

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-26T16:22:02Z

LGTM label has been added.  <details>Git tree hash: b9098962ffb709aea679674efec25bcd55133f96</details>

### Comment by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T16:22:10Z

/cc @mimowo

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T19:31:04Z

@PannagaRao: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-36 | 1c92ba88f6e6acd47a609e73f09b30896248680f | [link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10973/pull-kueue-test-e2e-main-1-36/2057933169541255168) | true | `/test pull-kueue-test-e2e-main-1-36`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=10973). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3APannagaRao). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T19:43:06Z

/test pull-kueue-test-e2e-extended-main-1-36

that is unrelated https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/10973/pull-kueue-test-e2e-extended-main-1-36/2059712946472226816

I will open issue

### Comment by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T19:51:44Z

> LGTM, I left only small comments (I think), thank you for the PR 👍

I believe to have addressed all of them. PTAL! Thanks!

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T20:25:59Z

LGTM label has been added.  <details>Git tree hash: 8c2b1277f4081852b699f86073e6d21cc1045505</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T20:26:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/10973#pullrequestreview-4375807714" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10973#" title="Author self-approved">PannagaRao</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/10973#pullrequestreview-4365554535" title="LGTM">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-28T20:45:18Z

/kind cleanup

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-29T12:18:16Z

/remove-kind cleanup
/kind bug
I think the previous design was actually a bug

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-29T12:20:55Z

/release-note-edit
```release-note
DRA: Fix the performance of determining if a Workload should be handled using DRA Extended Resources, 
using an event-driven maintained cache of DRA Extended Resources.
```

## Reviews

### Review by [@kannon92](https://github.com/kannon92) (commented) — 2026-05-06T13:13:20Z

- **pkg/features/kube_features.go:483** by [@kannon92](https://github.com/kannon92) — 2026-05-06T13:13:20Z
```diff
@@ -480,6 +480,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	DRAExtendedResources: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  Its a little odd to me to graduate DRAExtendedResources to beta while we leave DynamicResourceAllocation in alpha.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-06T19:23:30Z

- **pkg/features/kube_features.go:483** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T19:23:30Z
```diff
@@ -480,6 +480,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	DRAExtendedResources: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  https://github.com/kubernetes-sigs/kueue/pull/10996

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-06T22:34:24Z

- **pkg/features/kube_features.go:483** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T20:49:39Z
```diff
@@ -486,6 +480,7 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	},
 	DRAExtendedResources: {
 		{Version: version.MustParse("0.17"), Default: false, PreRelease: featuregate.Alpha},
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  ```suggestion
  		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
  ```

- **keps/2941-DRA/README.md:1022** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T21:13:49Z
```diff
@@ -1008,12 +1016,25 @@ Use existing dra-example-driver or Kubernetes test driver for e2e testing.
 
 #### Beta
 
+##### DRAExtendedResources (v0.18)
+
+- the `DRAExtendedResources` feature gate in Beta
+- event-driven DeviceClass tracking for late DeviceClass creation: implemented via
```

  This criterion was moved from the DynamicResourceAllocation beta section to DRAExtendedResources beta. But the DeviceClass watcher uses `NeedsDRAReconcile` which catches both RCT  workloads (`HasDRA`) and extended resource workloads. When a DeviceClass is created, it re-reconciles workloads from both paths. This is general DRA infrastructure being pulled into an extended-resources-specific graduation. If the watcher is gated behind `DRAExtendedResources` only, RCT-only users don't benefit from event-driven DeviceClass tracking. I think it should this stay under `DynamicResourceAllocation` beta criteria instead?

- **pkg/controller/core/workload_controller.go:1636** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T21:34:18Z
```diff
@@ -1598,3 +1603,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
+	var count int
+	for _, w := range lst.Items {
+		if dra.NeedsDRAReconcile(&w) {
```

  nit: There's no startup validation that `DRAExtendedResources` requires `DynamicResourceAllocation`. If both default to true at beta this is less of a concern, but for correctness consider adding the dependency in `config/validation.go` (same pattern as `ElasticJobsViaWorkloadSlicesWithTAS`).

- **test/integration/singlecluster/controller/dra/dra_test.go:1359** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-06T22:34:12Z
```diff
@@ -1046,4 +1034,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourceapi.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Have you tried using `util.ShortInterval` instead Interval? did you see any deviations?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-07T04:08:30Z

- **pkg/controller/core/workload_controller.go:1636** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-07T04:08:30Z
```diff
@@ -1598,3 +1603,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
+	var count int
+	for _, w := range lst.Items {
+		if dra.NeedsDRAReconcile(&w) {
```

  There is validation present https://github.com/kubernetes-sigs/kueue/blob/main/pkg/config/validation.go#L563. Is this what you were referring or am I missing something here?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-07T18:37:11Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1359** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-07T18:37:11Z
```diff
@@ -1046,4 +1034,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourceapi.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourceapi.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  ShortInterval works fine. Updated it

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-20T17:44:37Z

- **keps/2941-DRA/README.md:1541** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-20T17:16:34Z
```diff
@@ -1030,13 +1036,19 @@ Use existing dra-example-driver or Kubernetes test driver for e2e testing.
 - e2e tests
 - CEL expression validation against ResourceSlice devices
 
-##### KueueDRAIntegrationExtendedResource
+- event-driven DeviceClass tracking for late DeviceClass creation: implemented via
+  workload controller watching DeviceClass create/update/delete events and re-reconciling
+  pending DRA workloads
+
+##### KueueDRAIntegrationExtendedResource (v0.18)
 
-- user adoption feedback
-- re-evaluate event-driven DeviceClass tracking for late DeviceClass creation
-- re-evaluate post-scheduling quota reconciliation for DeviceClass drift
+- feature gate enabled by default
+- re-evaluated post-scheduling quota reconciliation for DeviceClass drift: not applicable
+  for extended resources as the quota key is `extendedResourceName` itself, making
+  DeviceClass-level drift invisible to quota accounting
 - re-evaluate the need for indexing of resourceSlices for CEL performance lookups
 - re-evaluate event-driven ResourceSlice tracking for late ResourceSlice creation
+- integration and e2e tests for DeviceClass lifecycle scenarios
 
 #### GA
```

  Can you add some GA criteria here like:
    - the feature gate in stable                                                                                                                                               
    - user adoption feedback confirms stability                                                                                                                                
    - re-evaluate DeviceClass watcher performance at scale

- **pkg/cache/queue/cluster_queue.go:366** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-20T17:24:46Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

   Good catch! This is important now that `SchedulingEquivalenceHashing` is default-true in 0.18. Without this, a DRA workload that gets NoFit before DeviceClass creation would be permanently stuck and the equivalence hash persists in noFitSchedulingHashes and sends it straight back to inadmissible even after DRA translation changes TotalRequests.

- **pkg/controller/core/workload_controller.go:1358** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-20T17:26:42Z
```diff
@@ -1344,8 +1353,13 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Watches(&corev1.LimitRange{}, ruh).
 		Watches(&nodev1.RuntimeClass{}, ruh).
 		Watches(&kueue.ClusterQueue{}, wqh).
-		Watches(&kueue.LocalQueue{}, wqh).
-		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+		Watches(&kueue.LocalQueue{}, wqh)
+	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
+		if _, err := mgr.GetRESTMapper().RESTMapping(resourcev1.SchemeGroupVersion.WithKind("DeviceClass").GroupKind()); err == nil {
```

  The indexer fix in this same PR  uses `!apimeta.IsNoMatchError(err)` to distinguish "API doesn't exist" from "API server error." Here it silently disables the DeviceClass watcher for any error, including transient API failures. Could we use the same `IsNoMatchError` pattern for consistency?

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-20T18:29:24Z

- **keps/2941-DRA/README.md:1541** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-20T18:29:24Z
```diff
@@ -1030,13 +1036,19 @@ Use existing dra-example-driver or Kubernetes test driver for e2e testing.
 - e2e tests
 - CEL expression validation against ResourceSlice devices
 
-##### KueueDRAIntegrationExtendedResource
+- event-driven DeviceClass tracking for late DeviceClass creation: implemented via
+  workload controller watching DeviceClass create/update/delete events and re-reconciling
+  pending DRA workloads
+
+##### KueueDRAIntegrationExtendedResource (v0.18)
 
-- user adoption feedback
-- re-evaluate event-driven DeviceClass tracking for late DeviceClass creation
-- re-evaluate post-scheduling quota reconciliation for DeviceClass drift
+- feature gate enabled by default
+- re-evaluated post-scheduling quota reconciliation for DeviceClass drift: not applicable
+  for extended resources as the quota key is `extendedResourceName` itself, making
+  DeviceClass-level drift invisible to quota accounting
 - re-evaluate the need for indexing of resourceSlices for CEL performance lookups
 - re-evaluate event-driven ResourceSlice tracking for late ResourceSlice creation
+- integration and e2e tests for DeviceClass lifecycle scenarios
 
 #### GA
```

  Added

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-20T18:29:36Z

- **pkg/controller/core/workload_controller.go:1358** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-20T18:29:35Z
```diff
@@ -1344,8 +1353,13 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Watches(&corev1.LimitRange{}, ruh).
 		Watches(&nodev1.RuntimeClass{}, ruh).
 		Watches(&kueue.ClusterQueue{}, wqh).
-		Watches(&kueue.LocalQueue{}, wqh).
-		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+		Watches(&kueue.LocalQueue{}, wqh)
+	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
+		if _, err := mgr.GetRESTMapper().RESTMapping(resourcev1.SchemeGroupVersion.WithKind("DeviceClass").GroupKind()); err == nil {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T12:57:04Z

- **pkg/cache/queue/cluster_queue.go:366** by [@mimowo](https://github.com/mimowo) — 2026-05-21T12:57:04Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

  Hmm, but the fix looks suspicious. I have some questions: 
  1, When we see `draRequestsChanged`, do we have a new schedulingHash basically, so that we could compute `UpdateSchedulingHash`?
  2. Why not rely on updating the scheduling hash in some already existing code paths, I'm not clear what makes DRA special here

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:00:16Z

- **pkg/cache/queue/manager.go:492** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:00:17Z
```diff
@@ -485,7 +485,11 @@ func (m *Manager) addLocalQueueLocked(ctx context.Context, q *kueue.LocalQueue)
 		}
 
 		log := ctrl.LoggerFrom(ctx).WithValues("workload", klog.KObj(&w))
-		if dra.NeedsDRAReconcile(&w) {
+		// Only defer DRA workloads to the reconcile channel if the channel
+		// is set up. Without this check, workloads would be silently dropped
+		// from the queue when the channel is not configured (e.g., in unit
+		// tests that don't run the workload controller).
+		if dra.NeedsDRAReconcile(&w) && m.draReconcileChannel != nil {
```

  I would rather register the channel in unit tests rather than add the non-trivial skip code. It is rally tricky to evaluate correctness of the skip.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:02:25Z

- **pkg/controller/core/workload_controller.go:377** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:02:26Z
```diff
@@ -368,21 +369,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			}
 		}
 
-		quotaReservedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-		requeuedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued)
+		// Only clear stale conditions when DRA processing actually resolved
+		// resources. Workloads that enter this block due to domain-qualified
+		// resource names but have no matching DeviceClass produce empty
+		// draResources and should not have their conditions cleared, as
+		// downstream requeue logic needs Requeued=False to set Requeued=True.
+		if len(draResources) > 0 {
```

  Is this a bugfix? If so could we make it as a separate PR to make that clear? 
  
  Cc @sohankunkerkar

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:02:46Z

- **pkg/controller/core/indexer/indexer.go:273** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:02:47Z
```diff
@@ -268,8 +268,9 @@ func Setup(ctx context.Context, indexer client.FieldIndexer) error {
 		}
 	}
 	// Index DeviceClasses by extendedResourceName for fast lookup during extended resource translation.
+	// The index is skipped if the DeviceClass API is not available (e.g., k8s < 1.35).
 	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
-		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil {
+		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil && !apimeta.IsNoMatchError(err) {
```

  Please submit this as a separate bugfix PR

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:06:06Z

- **pkg/controller/core/workload_controller.go:1667** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:06:07Z
```diff
@@ -1619,3 +1637,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
```

  Listing all workloads unconditionally (or just by the generic key) seems quite excessive, especially if we are making this feature Beta. I think we should at least restrict that to relevant changes in the DeviceClass. Is there a way to find quickly the workloads which are affected by deviceClass?
  
  I think we could make a more narrowing indexer basically.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:07:20Z

- **pkg/controller/core/workload_controller.go:1667** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:07:19Z
```diff
@@ -1619,3 +1637,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
```

  IIUC the issue is that by looking at the workload status we cannot tell if it was using DRA, right? I"m wondering maybe we should have an annotation or status field in the status.Admission for the fast lookup of the affected workloads.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-21T13:10:13Z

- **pkg/cache/queue/cluster_queue.go:366** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-21T13:10:13Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

  >1, When we see draRequestsChanged, do we have a new schedulingHash basically, so that we could compute UpdateSchedulingHash?
  
  IIUC, `UpdateSchedulingHash` would produce the same hash. It's computed from pod spec, count, and priority, and DRA translation doesn't change any of those. It modifies `TotalRequests` on workload.Info, which isn't part of the hash.
  
  > Why not rely on updating the scheduling hash in some already existing code paths, I'm not clear what makes DRA special here
  
  That's what makes DRA different from other state changes as spec edits and priority updates naturally change the hash because they modify what the hash is computed from. DRA changes what the workload actually needs for quota without touching the spec. So clearing the hash here is the way to force re-evaluation.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:17:59Z

- **pkg/cache/queue/cluster_queue.go:366** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:17:59Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

  Hm, what about then including `TotalRequests` in the hash computation? IIUC this would also fix the issue, and keep the model consistent.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-21T13:18:01Z

- **pkg/controller/core/workload_controller.go:377** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-21T13:18:01Z
```diff
@@ -368,21 +369,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			}
 		}
 
-		quotaReservedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-		requeuedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued)
+		// Only clear stale conditions when DRA processing actually resolved
+		// resources. Workloads that enter this block due to domain-qualified
+		// resource names but have no matching DeviceClass produce empty
+		// draResources and should not have their conditions cleared, as
+		// downstream requeue logic needs Requeued=False to set Requeued=True.
+		if len(draResources) > 0 {
```

  This isn't a pre-existing bug. It only surfaces when the gate is default-true because `NeedsDRAReconcile` starts matching any workload with domain-qualified resources. In alpha (gate off by default), only explicit DRA workloads hit this path. Thoughts?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-21T13:28:58Z

- **pkg/controller/core/workload_controller.go:377** by [@mimowo](https://github.com/mimowo) — 2026-05-21T13:28:58Z
```diff
@@ -368,21 +369,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			}
 		}
 
-		quotaReservedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-		requeuedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued)
+		// Only clear stale conditions when DRA processing actually resolved
+		// resources. Workloads that enter this block due to domain-qualified
+		// resource names but have no matching DeviceClass produce empty
+		// draResources and should not have their conditions cleared, as
+		// downstream requeue logic needs Requeued=False to set Requeued=True.
+		if len(draResources) > 0 {
```

  Does this mean that wit this change we run the DRA logic for any users using nvidia.com/gpu, or google.com/tpu, even if they don't actually use DRA? 
  
  I think this might be pretty risky, and I'm wondering how we could mitigate. One approach could be to quickly excape the DRA Reconcile if we confirm the extended resources aren't used.
  
  Another approach I'm thinking about is to have some cache for knowing if a resource is baked by DRA or not.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-21T13:44:00Z

- **pkg/controller/core/workload_controller.go:1667** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-21T13:44:00Z
```diff
@@ -1619,3 +1637,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
```

  Agreed, the full workload scan is too broad. We can follow the RuntimeClass handler pattern i.e. add a workload index keyed by requested extended resource names, then on DeviceClass events only requeue workloads that actually request the affected resource. For updates we'd need to check both old and new `extendedResourceName` since the name itself can change.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-21T15:41:47Z

- **pkg/cache/queue/cluster_queue.go:366** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-21T15:41:47Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

  Yeah,I agreed! Including `TotalRequests` in the hash works. Resource transformations and excluded prefixes are cluster config so they won't cause hash divergence between same-spec workloads and the only realistic case where TotalRequests differs is DRA translation, which is exactly the case we need to handle.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-21T16:01:44Z

- **pkg/controller/core/workload_controller.go:377** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-21T16:01:44Z
```diff
@@ -368,21 +369,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			}
 		}
 
-		quotaReservedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-		requeuedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued)
+		// Only clear stale conditions when DRA processing actually resolved
+		// resources. Workloads that enter this block due to domain-qualified
+		// resource names but have no matching DeviceClass produce empty
+		// draResources and should not have their conditions cleared, as
+		// downstream requeue logic needs Requeued=False to set Requeued=True.
+		if len(draResources) > 0 {
```

  Good call! I think cache is the cleanest approach as it resolves both channel routing and DeviceClass handler narrowing problems.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-21T16:18:45Z

- **pkg/cache/queue/manager.go:492** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-21T16:18:45Z
```diff
@@ -485,7 +485,11 @@ func (m *Manager) addLocalQueueLocked(ctx context.Context, q *kueue.LocalQueue)
 		}
 
 		log := ctrl.LoggerFrom(ctx).WithValues("workload", klog.KObj(&w))
-		if dra.NeedsDRAReconcile(&w) {
+		// Only defer DRA workloads to the reconcile channel if the channel
+		// is set up. Without this check, workloads would be silently dropped
+		// from the queue when the channel is not configured (e.g., in unit
+		// tests that don't run the workload controller).
+		if dra.NeedsDRAReconcile(&w) && m.draReconcileChannel != nil {
```

  Addressed by the DRABackedResources cache. NeedsDRAReconcile now only matches workloads whose extended resources are backed by an actual DeviceClass. In unit tests (no DeviceClasses), workloads with   example.com/gpu go through normal processing. The nil channel skip has been removed.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-21T16:21:35Z

- **pkg/controller/core/workload_controller.go:377** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-21T16:21:34Z
```diff
@@ -368,21 +369,28 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			}
 		}
 
-		quotaReservedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadQuotaReserved)
-		requeuedCond := apimeta.FindStatusCondition(wl.Status.Conditions, kueue.WorkloadRequeued)
+		// Only clear stale conditions when DRA processing actually resolved
+		// resources. Workloads that enter this block due to domain-qualified
+		// resource names but have no matching DeviceClass produce empty
+		// draResources and should not have their conditions cleared, as
+		// downstream requeue logic needs Requeued=False to set Requeued=True.
+		if len(draResources) > 0 {
```

  Added the cache implementation. NeedsDRAReconcile now checks the cache (populated from DeviceClass objects) instead of the broad IsExtendedResourceName format check. Non-DRA workloads with domain-qualified resources no longer enter the DRA reconcile path — they go through normal scheduling. Only workloads whose extended resources match an actual DeviceClass with extendedResourceName are processed through DRA.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-21T17:24:21Z

- **pkg/controller/core/workload_controller.go:1667** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-21T17:24:21Z
```diff
@@ -1619,3 +1637,45 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	h.queueReconcileForDRAWorkloads(ctx, q)
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func (h *deviceClassHandler) queueReconcileForDRAWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	log := ctrl.LoggerFrom(ctx)
+	lst := kueue.WorkloadList{}
+	err := h.r.client.List(ctx, &lst, client.MatchingFields{indexer.WorkloadQuotaReservedKey: string(metav1.ConditionFalse)})
+	if err != nil {
+		log.Error(err, "Could not list pending workloads for DeviceClass event")
+		return
+	}
```

  Added a workload index keyed by extended resource names. The DeviceClass handler now extracts the extendedResourceName from the event and queries only workloads requesting that specific resource, following the RuntimeClass handler pattern. PTAL!

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-21T19:51:46Z

- **pkg/controller/core/indexer/indexer.go:273** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-21T19:51:46Z
```diff
@@ -268,8 +268,9 @@ func Setup(ctx context.Context, indexer client.FieldIndexer) error {
 		}
 	}
 	// Index DeviceClasses by extendedResourceName for fast lookup during extended resource translation.
+	// The index is skipped if the DeviceClass API is not available (e.g., k8s < 1.35).
 	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
-		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil {
+		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil && !apimeta.IsNoMatchError(err) {
```

  https://github.com/kubernetes-sigs/kueue/pull/11405

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-22T11:10:36Z

- **pkg/cache/queue/cluster_queue.go:366** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-22T11:10:36Z
```diff
@@ -363,6 +363,14 @@ func (c *ClusterQueue) PushOrUpdate(wInfo *workload.Info) {
 		}
 		// Workload is leaving inadmissible; account for its resources before moving.
 		c.removeFromInadmissible(key, oldInfo)
+		// DRA translation changes TotalRequests without changing the pod spec,
```

  https://github.com/kubernetes-sigs/kueue/pull/11399

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-22T16:18:29Z

I'm with @mimowo based on his https://github.com/kubernetes-sigs/kueue/pull/10996#issuecomment-4519109275 comment and discussions in this PR.

The previous impls (just confirming resource name) were very aggressive which is not align with today's kube-scheduler ER support. Today's kube-scheduler properly confirms if the detected ER is truly backed by the DRA driver. OTOH, the previous this PR impls don't have that.

Surely, the latest impls properly check if the detected ER name is tied to the DRA driver.
But, this is completely new way which should not be coupled with Beta graduation.

And also the current impls are premature. We should mature the detailed logic before Beta graduation.

So, I'd like to consider this new mechanism as a second alpha for ER, then exclude ER support from Kueue DRA integration Beta criteria.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-23T05:37:08Z

Overall this looks good to me. I have one comment.

- **pkg/controller/core/workload_controller.go:1704** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-23T04:38:53Z
```diff
@@ -1595,3 +1652,69 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
+
+	// Requeue only workloads that request the affected extended resources.
+	for _, name := range resourceNames {
+		if name == "" {
+			continue
+		}
+		lst := kueue.WorkloadList{}
+		err := h.r.client.List(ctx, &lst,
+			client.MatchingFields{indexer.WorkloadExtendedResourceKey: name},
```

  I suspect the second `MatchingFields` might overwrites the first. `MatchingFields.ApplyToList()` does a direct assignment (`opts.FieldSelector = sel`), not a merge, unlike `MatchingLabels` which ANDs requirements together. So only the `QuotaReserved=False` filter applies; the extended resource name filter will silently drop here.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-25T20:00:31Z

- **pkg/controller/core/workload_controller.go:1704** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-25T20:00:31Z
```diff
@@ -1595,3 +1652,69 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
+
+	// Requeue only workloads that request the affected extended resources.
+	for _, name := range resourceNames {
+		if name == "" {
+			continue
+		}
+		lst := kueue.WorkloadList{}
+		err := h.r.client.List(ctx, &lst,
+			client.MatchingFields{indexer.WorkloadExtendedResourceKey: name},
```

  I think you are right. I combined both fields into a single MatchingFields map. Thoughts?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T02:28:39Z

- **pkg/dra/extended_resources.go:38** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T02:28:40Z
```diff
@@ -38,9 +38,6 @@ import (
 // Fast in-memory check with no API calls. Uses a broad format-based filter
```

  The godoc still says "may over-trigger for non-DRA extended resources" but with the `DRABackedResources().Has(name)`  check, it no longer over-triggers. The comment should reflect the new behavior.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T16:18:40Z

- **pkg/dra/extended_resources.go:38** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T16:18:39Z
```diff
@@ -38,9 +38,6 @@ import (
 // Fast in-memory check with no API calls. Uses a broad format-based filter
```

  Fixed

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T16:21:33Z

- **pkg/controller/core/workload_controller.go:1704** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T16:21:33Z
```diff
@@ -1595,3 +1652,69 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
+
+	// Requeue only workloads that request the affected extended resources.
+	for _, name := range resourceNames {
+		if name == "" {
+			continue
+		}
+		lst := kueue.WorkloadList{}
+		err := h.r.client.List(ctx, &lst,
+			client.MatchingFields{indexer.WorkloadExtendedResourceKey: name},
```

  this looks better now.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (approved) — 2026-05-26T16:21:53Z

/lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:01:21Z

- **pkg/controller/core/indexer/indexer.go:301** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:01:21Z
```diff
@@ -268,6 +296,9 @@ func Setup(ctx context.Context, indexer client.FieldIndexer) error {
 		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil && !apimeta.IsNoMatchError(err) {
 			return fmt.Errorf("setting index on extendedResourceName for DeviceClass: %w", err)
 		}
+		if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadExtendedResourceKey, IndexWorkloadExtendedResources); err != nil {
+			return fmt.Errorf("setting index on extended resources for Workload: %w", err)
+		}
```

  Wrap the indexing under the FG

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:03:05Z

- **pkg/controller/core/workload_controller.go:1356** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:03:05Z
```diff
@@ -1348,8 +1350,17 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Watches(&corev1.LimitRange{}, ruh).
 		Watches(&nodev1.RuntimeClass{}, ruh).
 		Watches(&kueue.ClusterQueue{}, wqh).
-		Watches(&kueue.LocalQueue{}, wqh).
-		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+		Watches(&kueue.LocalQueue{}, wqh)
+	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
+		if _, err := mgr.GetRESTMapper().RESTMapping(resourcev1.SchemeGroupVersion.WithKind("DeviceClass").GroupKind()); err != nil && apimeta.IsNoMatchError(err) {
+			ctrl.Log.V(2).Info("DeviceClass API not available, skipping DeviceClass watcher")
```

  We should use roleTracker for all logging

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:04:25Z

- **pkg/controller/core/workload_controller.go:1668** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:04:25Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
+
+	// Requeue only workloads that request the affected extended resources.
+	for _, name := range resourceNames {
+		if name == "" {
+			continue
+		}
+		lst := kueue.WorkloadList{}
+		err := h.r.client.List(ctx, &lst,
+			client.MatchingFields{
+				indexer.WorkloadExtendedResourceKey: name,
+				indexer.WorkloadQuotaReservedKey:    string(metav1.ConditionFalse),
+			},
+		)
+		if err != nil {
+			log.Error(err, "Could not list workloads for extended resource", "resource", name)
+			continue
+		}
+		for _, w := range lst.Items {
+			q.Add(reconcile.Request{
```

  Use AddAfter with 1s delay

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:08:36Z

- **pkg/controller/core/workload_controller.go:1649** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:08:37Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
```

  I don't understand, why calling Sync based on the entire list. we have the modified DeviceClass at hand in the update.

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T21:18:21Z

- **pkg/controller/core/indexer/indexer.go:301** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T21:18:21Z
```diff
@@ -268,6 +296,9 @@ func Setup(ctx context.Context, indexer client.FieldIndexer) error {
 		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil && !apimeta.IsNoMatchError(err) {
 			return fmt.Errorf("setting index on extendedResourceName for DeviceClass: %w", err)
 		}
+		if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadExtendedResourceKey, IndexWorkloadExtendedResources); err != nil {
+			return fmt.Errorf("setting index on extended resources for Workload: %w", err)
+		}
```

  The workload index registration is already inside the `KueueDRAIntegrationExtendedResource` gate block. Both the DeviceClass index and the workload index  are gated together. Is there a different scope you had in mind?

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T21:22:28Z

- **pkg/controller/core/workload_controller.go:1649** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T21:22:28Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
```

  IIUC, K8s permits multiple DeviceClasses with the same extendedResourceName. On Delete, incremental removal could incorrectly drop the name if another DeviceClass still has it. The full rebuild avoids this.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:30:22Z

- **pkg/dra/extended_resource_cache.go:41** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:30:22Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+// extendedResourceCache tracks which extended resource names are backed by DRA
+// DeviceClasses. Updated dynamically by the DeviceClass watcher.
+type extendedResourceCache struct {
+	lock      sync.RWMutex
+	resources sets.Set[corev1.ResourceName]
+}
+
+var globalERCache = &extendedResourceCache{
+	resources: sets.New[corev1.ResourceName](),
+}
+
+// DRABackedResources returns the global extended resource cache.
+func DRABackedResources() *extendedResourceCache {
+	return globalERCache
+}
```

  I think this global state proliferation is getting unconfortably large for DRA, we need to try get rid of it, either this PR or follow ups, opened: https://github.com/kubernetes-sigs/kueue/issues/11660

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T21:33:51Z

- **pkg/controller/core/workload_controller.go:1703** by [@mimowo](https://github.com/mimowo) — 2026-05-26T21:33:51Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
+
+	// Requeue only workloads that request the affected extended resources.
+	for _, name := range resourceNames {
+		if name == "" {
+			continue
+		}
+		lst := kueue.WorkloadList{}
+		err := h.r.client.List(ctx, &lst,
+			client.MatchingFields{
+				indexer.WorkloadExtendedResourceKey: name,
+				indexer.WorkloadQuotaReservedKey:    string(metav1.ConditionFalse),
+			},
+		)
```

  I think this feels like should be part of the manager cache, where the not reserving workloads are placed . This could be part of the follow up to get rid of the global state.

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-26T23:51:38Z

- **pkg/controller/core/workload_controller.go:1356** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-26T23:51:38Z
```diff
@@ -1348,8 +1350,17 @@ func (r *WorkloadReconciler) SetupWithManager(mgr ctrl.Manager, cfg *config.Conf
 		Watches(&corev1.LimitRange{}, ruh).
 		Watches(&nodev1.RuntimeClass{}, ruh).
 		Watches(&kueue.ClusterQueue{}, wqh).
-		Watches(&kueue.LocalQueue{}, wqh).
-		Complete(WithLeadingManager(mgr, r, &kueue.Workload{}, cfg))
+		Watches(&kueue.LocalQueue{}, wqh)
+	if features.Enabled(features.KueueDRAIntegrationExtendedResource) {
+		if _, err := mgr.GetRESTMapper().RESTMapping(resourcev1.SchemeGroupVersion.WithKind("DeviceClass").GroupKind()); err != nil && apimeta.IsNoMatchError(err) {
+			ctrl.Log.V(2).Info("DeviceClass API not available, skipping DeviceClass watcher")
```

  Fixed — using r.logger() which wraps with roleTracker

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:17:04Z

- **pkg/controller/core/indexer/indexer.go:227** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:17:04Z
```diff
@@ -214,6 +216,32 @@ func IndexWorkloadPriorityClass(obj client.Object) []string {
 	return []string{wl.Spec.PriorityClassRef.Name}
 }
 
+// IndexWorkloadExtendedResources indexes Workloads by the extended resource names
+// in their container requests. Used by the DeviceClass handler to find workloads
+// affected by a specific DeviceClass change.
+func IndexWorkloadExtendedResources(obj client.Object) []string {
+	wl, ok := obj.(*kueue.Workload)
+	if !ok {
+		return nil
+	}
+	set := sets.New[string]()
```

  ```suggestion
  	resNames := sets.New[string]()
  ```
  
  nit: We should avoid using a typed name as a variable name

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T17:33:03Z

- **pkg/dra/extended_resource_cache.go:41** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T17:33:02Z
```diff
@@ -0,0 +1,61 @@
+/*
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package dra
+
+import (
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	resourceapi "k8s.io/api/resource/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+)
+
+// extendedResourceCache tracks which extended resource names are backed by DRA
+// DeviceClasses. Updated dynamically by the DeviceClass watcher.
+type extendedResourceCache struct {
+	lock      sync.RWMutex
+	resources sets.Set[corev1.ResourceName]
+}
+
+var globalERCache = &extendedResourceCache{
+	resources: sets.New[corev1.ResourceName](),
+}
+
+// DRABackedResources returns the global extended resource cache.
+func DRABackedResources() *extendedResourceCache {
+	return globalERCache
+}
```

  Removed the global cache approach. Using dependency injection instead.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:46:27Z

- **pkg/controller/core/workload_controller.go:1678** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:46:28Z
```diff
@@ -1625,3 +1643,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
```

  make sure the roleTracker is injected into the logger

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:51:47Z

- **pkg/controller/core/workload_controller.go:1649** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:51:47Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
```

  Hm, Yes, so this is the reason why DRABackedResources would not be a simple set, but it could be a map: ResourceName -> set(DeviceClass), then removing a deviceClass would remove an element from the set, witbout empting the map.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:55:06Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1405** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:55:06Z
```diff
@@ -1253,4 +1253,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not admit new workload after DeviceClass is deleted", func() {
+			ginkgo.By("Creating DeviceClass")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+
+			ginkgo.By("Creating first workload and verifying admission")
+			wl1 := utiltestingapi.MakeWorkload("dc-delete-wl1", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Deleting DeviceClass")
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+
+			ginkgo.By("Creating second workload and verifying it is not admitted")
+			wl2 := utiltestingapi.MakeWorkload("dc-delete-wl2", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
```

  nit. but instead of using consistently could you check the kueue_pending_workloads metric? This way we could save on empty waiting.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:56:36Z

- **test/e2e/dra/baseline/dra_test.go:769** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:56:36Z
```diff
@@ -709,4 +709,102 @@ var _ = ginkgo.Describe("DRA", func() {
 			util.ExpectJobToBeCompleted(ctx, k8sClient, job)
 		})
 	})
+
+	ginkgo.When("Late DeviceClass creation with Extended Resources", func() {
+		var (
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName       = "example.com/gpu"
+			lateDeviceClassName        = "gpu-late-dc.example.com"
+			lateDeviceClassLogicalName = "gpu-late-dc"
+		)
+
+		ginkgo.BeforeEach(func() {
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("late-dc-flavor-" + ns.Name).Obj()
+			util.MustCreate(ctx, k8sClient, resourceFlavor)
+
+			clusterQueue = utiltestingapi.MakeClusterQueue("late-dc-cq-" + ns.Name).
+				ResourceGroup(
+					*utiltestingapi.MakeFlavorQuotas(resourceFlavor.Name).
+						Resource(corev1.ResourceCPU, "4").
+						Resource(corev1.ResourceName(lateDeviceClassLogicalName), "4").
+						Obj()).
+				Obj()
+			util.CreateClusterQueuesAndWaitForActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("late-dc-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			util.CreateLocalQueuesAndWaitForActive(ctx, k8sClient, localQueue)
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should admit job after DeviceClass is created", func() {
+			ginkgo.By("Creating Job with extended resource request before DeviceClass exists")
+			job := testingjob.MakeJob("late-dc-job", ns.Name).
+				Queue(kueue.LocalQueueName(localQueue.Name)).
+				Request(corev1.ResourceCPU, "100m").
+				RequestAndLimit(corev1.ResourceName(extendedResourceName), "1").
+				Image(util.GetAgnHostImage(), util.BehaviorExitFast).
+				Obj()
+			util.MustCreate(ctx, k8sClient, job)
+
+			wlLookupKey := types.NamespacedName{
+				Name:      workloadjob.GetWorkloadNameForJob(job.Name, job.UID),
+				Namespace: ns.Name,
+			}
+
+			ginkgo.By("Verifying job remains suspended (no DeviceClass, no translation)")
+			createdJob := &batchv1.Job{}
+			gomega.Consistently(func(g gomega.Gomega) {
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(job), createdJob)).To(gomega.Succeed())
+				g.Expect(createdJob.Spec.Suspend).To(gomega.Equal(new(true)))
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
```

  probably using ShortConsistentDuration would be enougb, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:57:08Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1339** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:57:08Z
```diff
@@ -1253,4 +1253,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
```

  let's check the metric to make the test faster

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:57:28Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1434** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:57:29Z
```diff
@@ -1253,4 +1253,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not admit new workload after DeviceClass is deleted", func() {
+			ginkgo.By("Creating DeviceClass")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+
+			ginkgo.By("Creating first workload and verifying admission")
+			wl1 := utiltestingapi.MakeWorkload("dc-delete-wl1", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Deleting DeviceClass")
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+
+			ginkgo.By("Creating second workload and verifying it is not admitted")
+			wl2 := utiltestingapi.MakeWorkload("dc-delete-wl2", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass extendedResourceName is updated", func() {
+			const newExtendedResourceName = "example.com/tpu"
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName for gpu")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Creating workload requesting tpu (no matching DeviceClass yet)")
+			wl := utiltestingapi.MakeWorkload("dc-update-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(newExtendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
```

  use the metric for pending workloads

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:57:52Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1479** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:57:52Z
```diff
@@ -1253,4 +1253,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not admit new workload after DeviceClass is deleted", func() {
+			ginkgo.By("Creating DeviceClass")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+
+			ginkgo.By("Creating first workload and verifying admission")
+			wl1 := utiltestingapi.MakeWorkload("dc-delete-wl1", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Deleting DeviceClass")
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+
+			ginkgo.By("Creating second workload and verifying it is not admitted")
+			wl2 := utiltestingapi.MakeWorkload("dc-delete-wl2", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass extendedResourceName is updated", func() {
+			const newExtendedResourceName = "example.com/tpu"
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName for gpu")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Creating workload requesting tpu (no matching DeviceClass yet)")
+			wl := utiltestingapi.MakeWorkload("dc-update-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(newExtendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+
+			ginkgo.By("Updating DeviceClass extendedResourceName to tpu")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var dc resourcev1.DeviceClass
+				g.Expect(k8sClient.Get(ctx, client.ObjectKey{Name: deviceClassName}, &dc)).To(gomega.Succeed())
+				dc.Spec.ExtendedResourceName = ptr.To(newExtendedResourceName)
+				g.Expect(k8sClient.Update(ctx, &dc)).To(gomega.Succeed())
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is requeued and admitted")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass extendedResourceName is added", func() {
+			ginkgo.By("Creating DeviceClass without extendedResourceName")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec:       resourcev1.DeviceClassSpec{},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Creating workload requesting extended resource")
+			wl := utiltestingapi.MakeWorkload("dc-add-ern-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
```

  use the metric for pending workloads

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T18:59:11Z

- **pkg/controller/core/indexer/indexer.go:301** by [@mimowo](https://github.com/mimowo) — 2026-05-27T18:59:11Z
```diff
@@ -268,6 +296,9 @@ func Setup(ctx context.Context, indexer client.FieldIndexer) error {
 		if err := indexer.IndexField(ctx, &resourceapi.DeviceClass{}, DeviceClassExtendedResourceNameIndex, IndexDeviceClassExtendedResourceName); err != nil && !apimeta.IsNoMatchError(err) {
 			return fmt.Errorf("setting index on extendedResourceName for DeviceClass: %w", err)
 		}
+		if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadExtendedResourceKey, IndexWorkloadExtendedResources); err != nil {
+			return fmt.Errorf("setting index on extended resources for Workload: %w", err)
+		}
```

  > 
  The workload index registration is already inside the KueueDRAIntegrationExtendedResource gate
  
  I missed that
  
  >  Is there a different scope you had in mind?
  
  no, the code lgtm

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T19:00:07Z

LGTM, I left only small comments (I think), thank you for the PR 👍

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T19:07:05Z

- **pkg/controller/core/workload_controller.go:1678** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T19:07:05Z
```diff
@@ -1625,3 +1643,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
```

  Done

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T19:07:30Z

- **test/integration/singlecluster/controller/dra/dra_test.go:1405** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T19:07:30Z
```diff
@@ -1253,4 +1253,248 @@ var _ = ginkgo.Describe("DRA Integration", ginkgo.Ordered, ginkgo.ContinueOnFail
 			}, util.Timeout, util.Interval).Should(gomega.Succeed())
 		})
 	})
+
+	ginkgo.When("Event-driven DeviceClass tracking for Extended Resources", func() {
+		var (
+			ns             *corev1.Namespace
+			resourceFlavor *kueue.ResourceFlavor
+			clusterQueue   *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+		)
+
+		const (
+			extendedResourceName = "example.com/gpu"
+			logicalName          = "gpu-claims"
+			deviceClassName      = "gpu.example.com"
+		)
+
+		ginkgo.BeforeAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(func(c *config.Configuration) {
+				c.Resources.DeviceClassMappings = append(c.Resources.DeviceClassMappings, config.DeviceClassMapping{
+					Name:             logicalName,
+					DeviceClassNames: []corev1.ResourceName{deviceClassName},
+				})
+			}))
+		})
+
+		ginkgo.AfterAll(func() {
+			fwk.StopManager(ctx)
+			fwk.StartManager(ctx, cfg, managerSetup(nil))
+		})
+
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dra-dc-tracking-"},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			resourceFlavor = utiltestingapi.MakeResourceFlavor("").Obj()
+			resourceFlavor.GenerateName = "rf-dc-tracking-"
+			gomega.Expect(k8sClient.Create(ctx, resourceFlavor)).To(gomega.Succeed())
+
+			clusterQueue = &kueue.ClusterQueue{
+				ObjectMeta: metav1.ObjectMeta{GenerateName: "dc-tracking-cq-"},
+				Spec: kueue.ClusterQueueSpec{
+					NamespaceSelector: &metav1.LabelSelector{},
+					ResourceGroups: []kueue.ResourceGroup{{
+						CoveredResources: []corev1.ResourceName{logicalName},
+						Flavors: []kueue.FlavorQuotas{{
+							Name: kueue.ResourceFlavorReference(resourceFlavor.Name),
+							Resources: []kueue.ResourceQuota{{
+								Name:         logicalName,
+								NominalQuota: resource.MustParse("8"),
+							}},
+						}},
+					}},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, clusterQueue)).To(gomega.Succeed())
+			util.ExpectClusterQueuesToBeActive(ctx, k8sClient, clusterQueue)
+
+			localQueue = utiltestingapi.MakeLocalQueue("dc-tracking-lq", ns.Name).
+				ClusterQueue(clusterQueue.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, resourceFlavor, true)
+		})
+
+		ginkgo.It("Should requeue inadmissible workload when DeviceClass is created", func() {
+			ginkgo.By("Creating workload before DeviceClass exists")
+			wl := utiltestingapi.MakeWorkload("dc-create-wl", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+
+			ginkgo.By("Verifying workload is not admitted (no DeviceClass, no translation)")
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
+
+			ginkgo.By("Creating DeviceClass with extendedResourceName")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+			defer func() {
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+			}()
+
+			ginkgo.By("Verifying workload is requeued and admitted with logical name as quota key")
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				g.Expect(updatedWl.Status.Admission).NotTo(gomega.BeNil())
+				g.Expect(updatedWl.Status.Admission.PodSetAssignments).To(gomega.HaveLen(1))
+
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not admit new workload after DeviceClass is deleted", func() {
+			ginkgo.By("Creating DeviceClass")
+			deviceClass := &resourcev1.DeviceClass{
+				ObjectMeta: metav1.ObjectMeta{Name: deviceClassName},
+				Spec: resourcev1.DeviceClassSpec{
+					ExtendedResourceName: ptr.To(extendedResourceName),
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, deviceClass)).To(gomega.Succeed())
+
+			ginkgo.By("Creating first workload and verifying admission")
+			wl1 := utiltestingapi.MakeWorkload("dc-delete-wl1", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl1)).To(gomega.Succeed())
+
+			gomega.Eventually(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl1), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeTrue())
+				assignment := updatedWl.Status.Admission.PodSetAssignments[0]
+				g.Expect(assignment.ResourceUsage).To(gomega.HaveKey(corev1.ResourceName(logicalName)))
+			}, util.Timeout, util.Interval).Should(gomega.Succeed())
+
+			ginkgo.By("Deleting DeviceClass")
+			util.ExpectObjectToBeDeleted(ctx, k8sClient, deviceClass, true)
+
+			ginkgo.By("Creating second workload and verifying it is not admitted")
+			wl2 := utiltestingapi.MakeWorkload("dc-delete-wl2", ns.Name).
+				Queue("dc-tracking-lq").
+				Request(corev1.ResourceName(extendedResourceName), "2").
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl2)).To(gomega.Succeed())
+
+			gomega.Consistently(func(g gomega.Gomega) {
+				var updatedWl kueue.Workload
+				g.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(wl2), &updatedWl)).To(gomega.Succeed())
+				g.Expect(workload.HasQuotaReservation(&updatedWl)).To(gomega.BeFalse())
+			}, util.ConsistentDuration, util.ShortInterval).Should(gomega.Succeed())
```

  Makes sense. Switched to use the metric instead

### Review by [@PannagaRao](https://github.com/PannagaRao) (commented) — 2026-05-27T19:51:06Z

- **pkg/controller/core/workload_controller.go:1649** by [@PannagaRao](https://github.com/PannagaRao) — 2026-05-27T19:51:05Z
```diff
@@ -1595,3 +1606,71 @@ func (h *draEventHandler) Generic(ctx context.Context, e event.TypedGenericEvent
 func (r *WorkloadReconciler) GetDRAReconcileChannel() chan<- event.TypedGenericEvent[*kueue.Workload] {
 	return r.draReconcileChannel
 }
+
+type deviceClassHandler struct {
+	r *WorkloadReconciler
+}
+
+func (h *deviceClassHandler) Create(ctx context.Context, e event.CreateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Update(ctx context.Context, e event.UpdateEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	oldDC := e.ObjectOld.(*resourcev1.DeviceClass)
+	newDC := e.ObjectNew.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(oldDC), extendedResourceName(newDC))
+}
+
+func (h *deviceClassHandler) Delete(ctx context.Context, e event.DeleteEvent, q workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+	dc := e.Object.(*resourcev1.DeviceClass)
+	h.syncCacheAndReconcileWorkloads(ctx, q, extendedResourceName(dc))
+}
+
+func (h *deviceClassHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.TypedRateLimitingInterface[reconcile.Request]) {
+}
+
+func extendedResourceName(dc *resourcev1.DeviceClass) string {
+	if dc.Spec.ExtendedResourceName != nil {
+		return *dc.Spec.ExtendedResourceName
+	}
+	return ""
+}
+
+func (h *deviceClassHandler) syncCacheAndReconcileWorkloads(ctx context.Context, q workqueue.TypedRateLimitingInterface[reconcile.Request], resourceNames ...string) {
+	log := ctrl.LoggerFrom(ctx)
+
+	// Rebuild the DRA-backed resource cache from current DeviceClasses.
+	var dcList resourcev1.DeviceClassList
+	if err := h.r.client.List(ctx, &dcList); err != nil {
+		log.Error(err, "Could not list DeviceClasses to rebuild cache")
+		return
+	}
+	dra.DRABackedResources().Sync(dcList.Items)
```

  Done. Using map instead of set

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T20:25:51Z

/lgtm
/approve
Thank you, this looks pretty well 👍
