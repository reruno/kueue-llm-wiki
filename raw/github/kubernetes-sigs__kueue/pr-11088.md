# PR #11088: AgentSkills: Add code-eval skill, that evaluates diff code based on defined rules …

**Summary**: AgentSkills: Add code-eval skill, that evaluates diff code based on defined rules …

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11088

**Last updated**: 2026-05-18T08:41:37Z

---

## Metadata

- **State**: open
- **Author**: [@reruno](https://github.com/reruno)
- **Branch**: `skills/code-eval` → `main`
- **Created**: 2026-05-09T19:10:45Z
- **Updated**: 2026-05-18T08:41:37Z
- **Merged**: —
- **Closed**: —
- **Labels**: `size/L`, `release-note-none`, `cncf-cla: yes`, `area/agents`
- **Assignees**: _none_
- **Requested reviewers**: [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 6   **+181 / -0**

## Description

…and gives a score from 0 to 100

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->

#### What this PR does / why we need it:
I added code evaluation skills with defined rules that was created based on mimowo comments on PRs. The main purpose of the skill is evaluating code quality and representing code quality in numeric form. This will allow us to determine what LLM prompt techniques improve LLMs coding abilities and what do not. Code evaluation skill also could act as a testing tool for future SKILL that focuses on increasing performance of LLM coding abilities.     

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:
Note: Example 1 is 2 month old commit and a lot of findings were already fixed.

tool: claude code
model: opus 4.7

### Example 1 

Prompt 1
```
load AGENTS.md and all skills in AGENTS.md
```

Prompt 2
```
ack
```

Prompt 3
```
evaluate code between these commits 4af7becae5c3d6786d112ba023e572ae3113607c c91286c0aaf45d003ec3c8e93b03c3ab28de5f25
```

Output (run 1)
```
Code Evaluation Report

  Commits: 4af7becae5c3d6786d112ba023e572ae3113607c..c91286c0aaf45d003ec3c8e93b03c3ab28de5f25
  Files changed:
  - Makefile-test.mk (+8/−0)
  - hack/e2e-common.sh (+395/−33)
  - site/content/en/docs/contribution_guidelines/testing.md (+5/−0)
  - site/content/zh-CN/docs/contribution_guidelines/testing.md (+3/−0)

  The change introduces an E2E_ENFORCE_OPERATOR_UPDATE knob letting E2E_MODE=dev runs skip re-installing external
  operators (AppWrapper, JobSet, Kubeflow, MPI, KubeRay, LWS, cert-manager, ClusterProfile CRD, DRA driver) when the
  running version already matches the expected version.

  ---
  Domain: Code Style

  Score: 15/40
  Findings:
  - High | -15 pt | e2e_is_truthy has inverted semantics vs. its name (hack/e2e-common.sh:91-96): the function returns 1
  (shell failure) when the argument is truthy and 0 (success) when it is falsy. Every call site (if e2e_is_truthy "$X"; 
  then …) therefore executes the block when X is falsy. The behavior happens to match the intent only because each call
  site mentally inverts the result. A reviewer reading if e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then <skip 
  install> will reasonably conclude “skip install when enforcement is enabled,” which is the opposite of reality.
  Naming-precision rule violation.
  - Medium | -7 pt | Inconsistent image-extraction patterns across install_ functions*: install_appwrapper,
  install_kubeflow, install_kubeflow_trainer, install_kuberay, install_cert_manager use a precise jsonpath
  containers[?(@.name=="…")].image, while install_jobset, install_mpi, install_lws, install_dra_example_driver iterate all
   containers[*].image and substring-match. Two near-identical idioms doing the same thing in the same file violates the
  "abide by existing conventions" rule and the reuse rule.
  - Low | -3 pt | Redundant LWS image-match conditions (hack/e2e-common.sh:492): [[ "${img}" == *"/lws:"* || "${img}" == 
  *"/lws@"* || "${img}" == *"/lws"* ]] — the third pattern subsumes the first two; the first two are unreachable.

  Domain: Buggy Behavior

  Score: 13/20
  Findings:
  - Medium | -7 pt | KubeRay always issues a delete -k on every CI run (hack/e2e-common.sh:466-468): in E2E_MODE=ci the
  outer skip-block at line 437 is bypassed, so force_reinstall stays false; the condition then evaluates e2e_is_truthy 
  "false" || e2e_is_truthy "false", which with the inverted helper resolves to true || true. Result: a kubectl delete -k …
   (with --ignore-not-found) is invoked on every fresh CI cluster before create. It is functionally safe today but is
  wasted work on the hot test path and makes the gate trivially bypassable in the future (e.g., if anyone replaces
  --ignore-not-found with --wait).

  Domain: Comments

  Score: 2/5
  Findings:
  - Low | -3 pt | Typo in module-level comment (hack/e2e-common.sh:86): # When set (truthy), external operators are 
  forcefully re-installed_version even in E2E_MODE=dev. — the _version fragment is a leftover/typo. Reads as broken
  English at the top of the new feature.

  Domain: Small Architectural Decisions

  Score: 5/30
  Findings:
  - High | -15 pt | Heavy duplication across all install_ functions*: each of the eight installers (install_appwrapper,
  install_jobset, install_kubeflow, install_kubeflow_trainer, install_mpi, install_kuberay, install_lws,
  install_cert_manager, plus the DRA driver) repeats a near-identical 15–25 line block: gate on
  E2E_MODE+E2E_ENFORCE_OPERATOR_UPDATE → check deployment exists → pull container image → extract tag → compare with
  e2e_versions_match → print one of three log messages. This is exactly the "same logic across multiple adapters → extract
   a helper" case in the rules. A helper such as e2e_check_installed_version <kubeconfig> <ns> <deployment> 
  <container-name|pattern> <expected> <friendly-name> would collapse the eight copies into ~30 lines total and fix the
  style inconsistency from the Code Style domain at the same time.
  - Medium | -7 pt | Scope creep: two changes ride along that are unrelated to the operator-skip feature: (a) the MPI
  manifest is now piped through yq to strip empty rules: [] from aggregated ClusterRoles (hack/e2e-common.sh:407-415); (b)
   the DRA example driver image tag changed from hardcoded "local" to "${expected_version#v}" with /→- sanitization
  (hack/e2e-common.sh:607-611). Both deserve their own commits with their own justification — bundling them obscures the
  diff and complicates rollback if either misbehaves.
  - Low | -3 pt | Convoluted control flow in install_kuberay (hack/e2e-common.sh:431,453,466): the function sets
  force_reinstall=true only inside the version-mismatch branch, then ORs it with the env var via the same inverted helper.
   The combination of (env var, dynamically-set local boolean, inverted truthiness predicate) makes the actual decision
  matrix difficult to read at a glance. Either route both signals through the same boolean or fold the env-var check into
  the helper.

  ---
  Final Score:

  36.8% / 100%
  35pt / 95pt

  ---
  Recommendations

  Recommendation 1: Fix e2e_is_truthy semantics or rename it

  Problem: e2e_is_truthy returns 0 when the value is not truthy and 1 when it is truthy, the opposite of every reader's
  expectation. All call sites must mentally invert.

  Reason: This is a latent footgun. Anyone adding a new call site (if e2e_is_truthy "$FLAG"; then enable_feature) will get
   the wrong behavior, and a fix that flips just the function (without auditing the eight current call sites) will
  silently invert every existing install path.

  Solution: Make the function return what its name says, and update call sites:

  # before
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 1 ;;
          *) return 0 ;;
      esac
  }
  # call site: if e2e_is_truthy "${ENFORCE}"; then <skip install>; fi

  # after
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 0 ;;
          *) return 1 ;;
      esac
  }
  # call site: if ! e2e_is_truthy "${ENFORCE}"; then <skip install>; fi

  Recommendation 2: Extract the install-skip block into one helper

  Problem: Eight installers each carry a copy of "if dev and not enforcing, look up deployment image, extract tag, compare
   to expected, log+return-or-fall-through". Duplicated logic, divergent jsonpath idioms, three near-identical log strings
   per installer.

  Reason: Adding a new operator (or fixing a bug in the comparison) requires editing eight identical blocks; the
  JobSet/MPI/LWS branches already drifted to a different image-lookup style than AppWrapper/Kubeflow/cert-manager. Both
  Code Style and Small-Arch domains improve if this lives in one place.

  Solution: Introduce one helper and use it everywhere:

  # Returns 0 if the deployment is already installed at the expected version
  # (caller should `return 0` to skip install). Returns 1 otherwise.
  function e2e_operator_up_to_date {
      local kubeconfig=$1 ns=$2 deployment=$3 container=$4 expected=$5 friendly=$6
      e2e_deployment_exists "${kubeconfig}" "${ns}" "${deployment}" || {
          echo "${friendly} deployment not found; installing."; return 1; }
      local img installed
      img=$(kubectl --kubeconfig="${kubeconfig}" -n "${ns}" get deployment "${deployment}" \
          -o jsonpath="{.spec.template.spec.containers[?(@.name==\"${container}\")].image}" 2>/dev/null || true)
      installed=$(e2e_image_ref_get_tag "${img}" || true)
      if [[ -n "${installed}" ]] && { [[ -z "${expected}" ]] || e2e_versions_match "${installed}" "${expected}"; }; then
          echo "${friendly} already installed (${installed}); skipping install (E2E_MODE=dev)."; return 0
      fi
      echo "${friendly} present at ${installed:-unknown}, expected ${expected:-any}; upgrading."; return 1
  }

  function install_appwrapper {
      local name=$1 kubeconfig=${2:-}
      local ns="${APPWRAPPER_DEPLOYMENT_NAMESPACE:-appwrapper-system}"
      local dep="${APPWRAPPER_CONTROLLER_MANAGER_DEPLOYMENT:-appwrapper-controller-manager}"
      if [[ "${E2E_MODE}" == "dev" ]] && ! e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}" \
          && e2e_operator_up_to_date "${kubeconfig}" "${ns}" "${dep}" manager "${APPWRAPPER_VERSION:-}" AppWrapper; then
          return 0
      fi
      cluster_kind_load_image "${name}" "${APPWRAPPER_IMAGE}"
      kubectl apply --kubeconfig="${kubeconfig}" --server-side -k "${APPWRAPPER_MANIFEST}"
      kubectl wait --kubeconfig="${kubeconfig}" deploy/"${dep}" -n "${ns}" --for=condition=available --timeout=5m
  }

  Recommendation 3: Stop unconditionally delete -k-ing KubeRay in CI

  Problem: With the default E2E_ENFORCE_OPERATOR_UPDATE=false and force_reinstall=false, the condition at install_kuberay
  line 466 still evaluates true, so every fresh CI run executes a kubectl delete -k "${KUBERAY_MANIFEST}" 
  --ignore-not-found=true before create.

  Reason: It is wasted work on every CI run today and a correctness trap tomorrow if anyone tightens --ignore-not-found
  away or adds a --wait.

  Solution: Gate the delete on the actual signal — enforcement requested or version-mismatch detected — without going
  through the inverted helper:

  local need_delete=false
  if [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then  # after fixing Rec 1
      need_delete=true
  fi
  [[ "${force_reinstall}" == "true" ]] && need_delete=true

  if [[ "${need_delete}" == "true" ]]; then
      kubectl ${kubectl_args[@]+"${kubectl_args[@]}"} delete -k "${KUBERAY_MANIFEST}" --ignore-not-found=true
  fi
  kubectl ${kubectl_args[@]+"${kubectl_args[@]}"} create -k "${KUBERAY_MANIFEST}"

  Recommendation 4: Unify LWS image-match condition

  Problem: [[ "${img}" == *"/lws:"* || "${img}" == *"/lws@"* || "${img}" == *"/lws"* ]] — the third pattern subsumes the
  others.

  Reason: Dead branches mislead future maintainers ("the first two patterns must be load-bearing or someone would have
  removed them") and make grep-driven refactors riskier.

  Solution:
  if [[ "${img}" == *"/lws"* ]]; then
      installed_version=$(e2e_image_ref_get_tag "${img}" || true)
      break
  fi
  
  Recommendation 5: Fix the file-level comment typo

  Problem: hack/e2e-common.sh:86 — # When set (truthy), external operators are forcefully re-installed_version even in 
  E2E_MODE=dev.

  Reason: The _version looks like a merge artifact and reads as broken English at the entry-point comment of the new
  feature.

  Solution: # When set (truthy), external operators are forcefully re-installed on each run, even in E2E_MODE=dev.

  Recommendation 6: Split the MPI manifest patch and DRA tag change into their own commits

  Problem: The curl … | yq … | kubectl apply -f - rewrite of the MPI manifest (hack/e2e-common.sh:407-415) and the DRA
  driver image-tag derivation (hack/e2e-common.sh:607-611) are bundled into the dev-mode skip-install change.

  Reason: Each is a behavioral change with its own failure mode (yq pipeline can mask curl errors; tag change invalidates
  cluster_kind_load_image cache keys). Bundling them obscures what motivated the change and makes revert-on-bug surgical
  instead of clean.

  Solution: Move both into separate commits/PRs with their own commit messages explaining the upstream conflict (MPI) and
  the cache-busting motivation (DRA), and let this PR focus on E2E_ENFORCE_OPERATOR_UPDATE.
```

Output (run 2)
```
  Code Evaluation Report

  Commits: 4af7becae5c3d6786d112ba023e572ae3113607c..c91286c0aaf45d003ec3c8e93b03c3ab28de5f25
  Files changed (from --stat):
  - Makefile-test.mk (+10)
  - hack/e2e-common.sh (+395 / -33)
  - site/content/en/docs/contribution_guidelines/testing.md (+5)
  - site/content/zh-CN/docs/contribution_guidelines/testing.md (+3)

  ---
  Domain: Code Style

  Score: 16/40
  Findings:
  - High | -15 pt | e2e_is_truthy has inverted return semantics: hack/e2e-common.sh:35-40 returns 1 (shell-failure /
  "false") for truthy values like true/yes/1 and 0 (shell-success / "true") for everything else. The function name is the
  opposite of what it does. Every caller (install_appwrapper, install_jobset, install_kubeflow, install_kubeflow_trainer,
  install_mpi, install_kuberay, install_lws, install_cert_manager, install_multicluster, install_dra_example_driver)
  silently relies on this inversion, which violates Rule 1 (naming precision).
  - Low | -3 pt | LWS image-tag filter at hack/e2e-common.sh:492 uses *"/lws:"* || *"/lws@"* || *"/lws"* — the third
  pattern already matches the first two, the first two are dead code.
  - Low | -3 pt | Inconsistent env-var names for the same concept across operators: KUBEFLOW_TRAINING_OPERATOR_NAMESPACE,
  KUBEFLOW_TRAINER_NAMESPACE, KUBEFLOW_MPI_OPERATOR_NAMESPACE, KUBERAY_NAMESPACE, LEADERWORKERSET_NAMESPACE,
  CERTMANAGER_NAMESPACE. Some include _OPERATOR_, some don't (Rule 1: symmetry).
  - Low | -3 pt | Inconsistent || true after kubectl wait: install_appwrapper, install_jobset, install_kubeflow,
  install_cert_manager (first wait) treat wait-failures as hard failures; install_mpi, install_kuberay, install_lws, and
  the two follow-up cert-manager waits swallow them. No principled difference is visible.
  
  Domain: Buggy Behavior

  Score: 10/20
  Findings:
  - Medium | -7 pt | install_kuberay second guard at hack/e2e-common.sh:466-468: if e2e_is_truthy 
  "${E2E_ENFORCE_OPERATOR_UPDATE}" || e2e_is_truthy "${force_reinstall}"; then kubectl ... delete -k ...; fi. Reads as
  "delete when either is truthy"; thanks to the inverted e2e_is_truthy, it actually deletes whenever either is non-truthy.
   In CI default (E2E_MODE=ci, E2E_ENFORCE_OPERATOR_UPDATE=false, force_reinstall=false), the function unconditionally
  runs kubectl delete -k … every run. It's only harmless because of --ignore-not-found=true, but the visible intent and
  actual behavior are opposite.
  - Low | -3 pt | || true after kubectl wait in install_mpi, install_kuberay, install_lws and the two follow-up
  cert-manager waits (hack/e2e-common.sh:416,470,513,552-553). If a controller never reaches Available, e2e proceeds
  silently and the suite fails later with confusing downstream errors instead of at the install step.

  Domain: Comments

  Score: 2/5
  Findings:
  - Low | -3 pt | Typo in inline comment at hack/e2e-common.sh:33: "external operators are forcefully re-installed_version
   even in E2E_MODE=dev" — re-installed_version looks like a stray find-replace artifact (Rule 5: comments must not
  contain typos/obvious errors).

  Domain: Small Architectural Decisions

  Score: 5/30
  Findings:
  - High | -15 pt | Massive duplication of the same ~25-line "skip-if-installed-and-version-matches" pattern across 8
  install_X functions (install_appwrapper, install_jobset, install_kubeflow, install_kubeflow_trainer, install_mpi, 
  install_kuberay, install_lws, install_cert_manager, install_dra_example_driver). The only variation across copies is the
   image-filter regex, container name and namespace defaults. Rule 5 (helper extraction): identical logic across multiple
  call sites must live in a shared helper.
  - Medium | -7 pt | install_kuberay (hack/e2e-common.sh:421-471) splits a single concern — "decide whether to reinstall"
  — across two non-adjacent if blocks separated by cluster_kind_load_image calls, with a force_reinstall flag bridging
  them. Together with the inverted e2e_is_truthy, the control flow is hard to verify by reading. Rule 7 (code placement) /
   Rule 2 (no confusing data flow).
  - Low | -3 pt | install_dra_example_driver bundles an unrelated change at hack/e2e-common.sh:907-911: the image tag is
  no longer "local" but derived from ${DRA_EXAMPLE_DRIVER_VERSION#v} with a fallback. Scope creep (Rule 6) — the headline
  of the diff is "skip-if-installed", but the DRA driver image tagging is silently changed too.

  ---
  Final Score:

  34.7% / 100%
  33pt / 95pt

  ---
  Recommendations

  Recommendation 1: Fix e2e_is_truthy so its return value matches its name

  Problem: hack/e2e-common.sh:35-40 returns shell-failure (1) for truthy strings and shell-success (0) for everything
  else, so if e2e_is_truthy "true"; then …; fi never enters the block. Every caller silently relies on the inversion,
  including install_kuberay's reinstall guard whose written meaning is the opposite of its actual behavior.

  Reason: Bash convention is return 0 = success/true. A predicate named is_truthy must return 0 for truthy inputs;
  otherwise readers — and future modifications — will reason about the code incorrectly. Today it works only by
  coincidence (and --ignore-not-found=true).

  Solution: Flip the return codes and update call sites that intentionally relied on inversion (i.e. anywhere the intent
  was "skip when not enforcing update").

  # Before
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 1 ;;
          *) return 0 ;;
      esac
  }

  # After
  function e2e_is_truthy {
      case "${1:-}" in
          1|true|TRUE|True|yes|YES|Yes|y|Y|on|ON|On) return 0 ;;
          *) return 1 ;;
      esac
  }

  Then update the "skip-if-installed" guards from && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}" to && ! e2e_is_truthy 
  "${E2E_ENFORCE_OPERATOR_UPDATE}", and the kuberay second guard so the textual meaning matches the runtime meaning.

  Recommendation 2: Extract a shared _ensure_operator_installed helper

  Problem: The ~25-line block (e2e_deployment_exists → kubectl get deployment -o jsonpath=image → e2e_image_ref_get_tag →
  e2e_versions_match → echo + return) appears in install_appwrapper, install_jobset, install_kubeflow,
  install_kubeflow_trainer, install_mpi, install_kuberay, install_lws, install_cert_manager, and
  install_dra_example_driver. Diffs touch 8 places to fix one bug.

  Reason: Copy-pasted control flow is an architectural smell (Rule 5). Any future fix (e.g. handling digests,
  container-name fallback like cert-manager already does) has to be replicated 8×.

  Solution: Introduce a single helper taking the variable bits — namespace, deployment name, container name (or filter),
  expected version, and a friendly label — and have each install_X delegate.

  # Returns 0 if installed at acceptable version (caller may early-return),
  # 1 if absent or needs upgrade. Caller still does the actual install.
  function _operator_version_ok {
      local label=$1 kubeconfig=$2 ns=$3 deployment=$4
      local container_match=$5 expected_version=$6
      [[ "${E2E_MODE}" == "dev" ]] && e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}" || return 1
      e2e_deployment_exists "${kubeconfig}" "${ns}" "${deployment}" || { echo "${label} not found; installing."; return 1;
   }
      # ... single source of truth for image lookup + version compare ...
  }

  Then install_appwrapper becomes ~5 lines: call helper, return 0 on hit, otherwise load image + apply + wait.

  Recommendation 3: Fix the install_kuberay delete guard and split the responsibility

  Problem: hack/e2e-common.sh:466-468 runs kubectl delete -k … unconditionally in CI default (both flags falsy), and the
  version-check block and force-reinstall block are visually disjoint.

  Reason: Even though --ignore-not-found=true masks the consequence, the code reads as "delete when reinstalling" and
  behaves as "delete almost always". Future maintainers will trust the visible intent.

  Solution: After flipping e2e_is_truthy (Rec 1), the guard becomes correct as written. Additionally, collapse the two
  decision points into one:

  local need_reinstall=false
  if [[ "${E2E_MODE}" != "dev" ]] || e2e_is_truthy "${E2E_ENFORCE_OPERATOR_UPDATE}"; then
      need_reinstall=true
  elif e2e_deployment_exists "${kubeconfig}" "${ns}" "${deployment_name}" \
       && _installed_version_matches …; then
      echo "KubeRay already installed; skipping."
      return 0
  else
      need_reinstall=true
  fi

  cluster_kind_load_image "$name" "${KUBERAY_RAY_IMAGE}"
  cluster_kind_load_image "$name" "${KUBERAY_IMAGE}"
  if "${need_reinstall}"; then
      kubectl "${kubectl_args[@]}" delete -k "${KUBERAY_MANIFEST}" --ignore-not-found=true
  fi
  kubectl "${kubectl_args[@]}" create -k "${KUBERAY_MANIFEST}"

  Recommendation 4: Fix the comment typo

  Problem: hack/e2e-common.sh:33 reads external operators are forcefully re-installed_version even in E2E_MODE=dev.

  Reason: re-installed_version is clearly a stray edit. Inaccurate or malformed comments are worse than no comment (Rule
  3/5 of Comments).

  Solution: Replace with # When truthy, external operators are forcefully re-installed on each run, even in E2E_MODE=dev.
  (it already reads cleanly in Makefile-test.mk:51; align the shell comment to match).

  Recommendation 5: De-bundle the DRA driver image-tag change

  Problem: install_dra_example_driver changes the image tag from the constant "local" to one derived from
  DRA_EXAMPLE_DRIVER_VERSION. This is unrelated to the headline "skip-if-installed" feature.

  Reason: Mixed-scope diffs hide behavioral changes from reviewers (Rule 6: scope discipline).

  Solution: Split into its own commit (or PR) with a one-line rationale, e.g. "tag DRA driver image with the requested
  version so older kept clusters can be detected as out-of-date".

  Recommendation 6: Standardise the kubectl wait failure policy

  Problem: Half the kubectl wait calls are followed by || true, the other half are not. There is no comment justifying the
   difference.

  Reason: A controller that fails to reach Available should fail loudly at install time, not as a confusing test error 10
  minutes later. Silent fallbacks should be the exception, not the default.

  Solution: Default to no || true. Keep it only where there is a known-flaky readiness signal, and add an inline # why
  comment next to those instances.

  Recommendation 7: Unify the operator env-var naming

  Problem: Across the diff the same concept is spelled inconsistently: KUBEFLOW_TRAINING_OPERATOR_NAMESPACE /
  KUBEFLOW_TRAINER_NAMESPACE / KUBEFLOW_MPI_OPERATOR_NAMESPACE vs KUBERAY_NAMESPACE / LEADERWORKERSET_NAMESPACE.

  Reason: Rule 1 / 2 — new identifiers must follow a single convention so users can predict variable names without
  consulting the script.

  Solution: Pick one shape — e.g. <OPERATOR>_NAMESPACE and <OPERATOR>_DEPLOYMENT — and use it everywhere. The helper from
  Recommendation 2 makes this a one-time fix.

  Recommendation 8: Simplify the LWS image filter

  Problem: if [[ "${img}" == *"/lws:"* || "${img}" == *"/lws@"* || "${img}" == *"/lws"* ]]; then at
  hack/e2e-common.sh:492.

  Reason: Pattern 3 already matches patterns 1 and 2; the disjunction is dead code (Rule 3: util reuse / simplicity).

  Solution:

  # Before
  if [[ "${img}" == *"/lws:"* || "${img}" == *"/lws@"* || "${img}" == *"/lws"* ]]; then

  # After
  if [[ "${img}" == *"/lws"* ]]; then
```

### Example 2 
Prompt 1
```
load AGENTS.md and all skills in AGENTS.md
```

Prompt 2
```
ack
```

Prompt 3
```
evaluate code between these commits c43e788acc787db54809ec01954d6441c80ace5a 0b2c1d0f7675448fc2455904e0d62b4a8130a51d
```

Output 
```
  Code Evaluation Report

  Commits: c43e788ac..0b2c1d0f7
  Files changed:
  - pkg/controller/jobframework/reconciler.go (3)
  - pkg/controller/jobframework/validation.go (11)
  - pkg/controller/jobframework/validation_test.go (8)
  - pkg/controller/jobs/leaderworkerset/leaderworkerset_reconciler.go (45)
  - pkg/controller/jobs/leaderworkerset/leaderworkerset_webhook_test.go (32)
  - pkg/util/equality/podset.go (12)
  - pkg/util/equality/podset_test.go (38)
  - test/e2e/singlecluster/leaderworkerset_test.go (63)

  ---
  Domain: Code Style

  Score: 37/40
  Findings:
  - Low | -3 pt | ignoreNodeSelector mutates and returns: pkg/controller/jobframework/validation.go:250-253 sets
  shape["nodeSelector"] = nil on the passed-in map and returns it. Go convention is either "mutate in place, return
  nothing" or "return a new value, don't mutate". Returning the same map encourages misleading call sites like
  ignoreNodeSelector(utilpod.SpecShape(...)) that read as pure but aren't. Naming-wise the symmetric
  WithIgnoreNodeSelector / WithIgnoreTolerations is correct and follows the existing builder pattern. Test rename "change 
  nodeTemplate" → "change nodeSelector" is a precision improvement. No typos.

  Domain: Buggy Behavior

  Score: 17/20
  Findings:
  - Low | -3 pt | Default behavior of comparePodTemplate flipped: previously NodeSelector was not compared at all; after
  the diff it is compared unless WithIgnoreNodeSelector() is passed (pkg/util/equality/podset.go:56-58). The sole in-tree
  caller EquivalentToWorkload (pkg/controller/jobframework/reconciler.go:1210-1211) was updated to opt out, so equivalence
   semantics there are preserved. Verified no other in-tree callers exist (pod controller delegates to 
  jobframework.EquivalentToWorkload). The flipped default is still a latent risk for any out-of-tree caller of the public
  ComparePodSetSlices and for future additions — an additive WithCheckNodeSelector() would have been safer.
  - The new updateWorkload deletes an already-admitted workload when podSets diverge
  (leaderworkerset_reconciler.go:347-349). This is the intended path for the new nodeSelector mutation feature; the e2e
  test (test/e2e/singlecluster/leaderworkerset_test.go) verifies recreation + re-admission. No race observed between
  concurrent eg.Go blocks since toCreate / toUpdate / toDelete operate on disjoint workload sets computed before the
  goroutines start.

  Domain: Comments

  Score: 5/5
  Findings:
  - No findings. The retained // Remove the finalizer before deleting… comment at leaderworkerset_reconciler.go:355 still
  accurately documents the why. The pre-existing TODO at podset.go:43-44 mentioning nodeSelectors(when suspended) is
  arguably partially addressed by this diff, but updating that TODO is out of scope.

  Domain: Small Architectural Decisions

  Score: 23/30
  Findings:
  - Low | -3 pt | Default-behavior flip of public helper ComparePodSetSlices: changing semantics rather than adding an
  opt-in option forces every caller to be audited and to opt out — additive options (WithCheckNodeSelector) keep existing
  call sites stable. The diff handled the one in-tree caller correctly, but the architectural choice is the more fragile
  of the two.
  - Low | -3 pt | ignoreNodeSelector helper does double duty (mutate + return) and lives next to
  validateImmutablePodGroupPodSpecPath even though it is conceptually a generic shape transform. Either inline at the two
  call sites (it's two lines) or move into the utilpod package alongside SpecShape. As-is, it's a one-shot helper that
  adds indirection without reuse.
  - The reconciler refactor (createPrebuiltWorkload → createWorkload; extracted updateWorkload/deleteWorkload) is a clear
  win: each eg.Go block now reads as a single method call, and updateWorkload becomes the natural home for the new
  podSet-diff branch. Scope creep is mild and justified by the new feature.

  ---
  Final Score:

  88% / 100%
  82pt / 95pt

  ▎ Note: the SKILL.md domain table sums to 95 (40 + 20 + 5 + 30), not 100; report uses the table's actual max.

  ---
  Recommendations

  Recommendation 1: Don't mutate-and-return in ignoreNodeSelector

  Problem: ignoreNodeSelector(shape) writes shape["nodeSelector"] = nil and returns shape. Both call sites are
  ignoreNodeSelector(utilpod.SpecShape(...)), so the mutation is hidden behind a function-style call.
  Reason: Functions that look pure but mutate their argument are a frequent source of subtle bugs. If a future caller
  reuses the input map after passing it through, they'll be surprised.
  Solution: Either inline at the two call sites, or rename so the mutation is obvious:

  // Before
  return validateImmutablePodGroupPodSpecPath(
      ignoreNodeSelector(utilpod.SpecShape(newPodSpec)),
      ignoreNodeSelector(utilpod.SpecShape(oldPodSpec)),
      fieldPath,
  )

  // After (inline; SpecShape already returns a fresh map)
  newShape := utilpod.SpecShape(newPodSpec); newShape["nodeSelector"] = nil
  oldShape := utilpod.SpecShape(oldPodSpec); oldShape["nodeSelector"] = nil
  return validateImmutablePodGroupPodSpecPath(newShape, oldShape, fieldPath)

  Recommendation 2: Prefer an additive option over flipping the default

  Problem: comparePodTemplate now compares NodeSelector by default; previously it didn't. The diff updates the one in-tree
   caller but the change is invisible to any external user of ComparePodSetSlices and to future contributors who add a
  caller without realizing nodeSelector is in scope.
  Reason: Public helpers in pkg/util/equality are part of the package surface. Silent behavior flips are a known source of
   regressions during upgrades. The test diff itself shows the trap — "different node selector" flipped from
  wantEquivalent: true to false.
  Solution: Keep the historical default and gate the new check behind an opt-in:

  // Option
  func WithCheckNodeSelector() ComparePodSetsOption { ... }

  // comparePodTemplate
  if opts.checkNodeSelector && !equality.Semantic.DeepEqual(a.NodeSelector, b.NodeSelector) {
      return false
  }

  Then the LWS reconciler passes WithCheckNodeSelector(), and EquivalentToWorkload needs no change.
```
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-09T19:10:50Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 88e957e46985a6149159cffc1fd07d72bbefbea1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0acd3440ec5d00087f442f |

### Comment by [@amy](https://github.com/amy) — 2026-05-10T17:24:57Z

@reruno This is really cool! Thank you! I would be interested in.... how scoring works / how to trust the heuristic like if we're going to use it to evaluate skills. How do ml scientists usually do evals. Maybe there's room for collaboration there.

### Comment by [@reruno](https://github.com/reruno) — 2026-05-11T08:09:34Z

@amy Thanks for the feedback, I really appreciate it! 

> how scoring works / how to trust the heuristic like if we're going to use it to evaluate skills. How do ml scientists usually do evals. Maybe there's room for collaboration there.

https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents
Answering you question, this is very good blog post about evaluating ai agents performance. 

IIUC to grade LLM output we have 3 options
1. Code-based graders (go lint, make verify)
2. Model-based graders (LLM evaluates LLM)
3. Human graders (actual code reviewers)

As of now IMO large chunk of the code is written by LLMs and changed a bit by PR authors to be less ChatGPTish, thus we can think of PR code as LLM output. Code Eval is Model-based grader, which takes LLM output (Diff Code) and grades it according to the rules in the SKILL. Code-based graders are already implemented (make verify), Human graders also present (code reviewers), the only thing missing right now is Model-based grader. 

I am still new to these concepts and I will definitely want to deepen my knowledge how to make grader properly, but I think code-eval is a move in the right direction.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-11T14:04:49Z

/area agents

### Comment by [@reruno](https://github.com/reruno) — 2026-05-12T13:30:01Z

New runs after force push are in special notes to reviewer

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-15T15:46:16Z

ping @amy or @PBundyra ptal

### Comment by [@amy](https://github.com/amy) — 2026-05-15T16:40:56Z

> ping @/amy or @/PBundyra ptal

@mimowo, @reruno and I discussed that we will wait for this one to merge first: https://github.com/kubernetes-sigs/kueue/pull/10819

And then reruno will break up files and use similar structure.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T07:54:41Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11088#" title="Author self-approved">reruno</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [pbundyra](https://github.com/pbundyra) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[cmd/experimental/skills/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/cmd/experimental/skills/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["pbundyra"]} -->

## Reviews

### Review by [@amy](https://github.com/amy) (commented) — 2026-05-10T17:12:03Z

- **cmd/experimental/agent/skills/code-eval.md:7** by [@amy](https://github.com/amy) — 2026-05-10T17:12:03Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
```

  Add a prompt where when the user says something like "perform a code eval", that the agent tells you what arguments it needs.

### Review by [@amy](https://github.com/amy) (commented) — 2026-05-10T17:18:13Z

- **cmd/experimental/agent/skills/code-eval.md:51** by [@amy](https://github.com/amy) — 2026-05-10T17:18:13Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
```

  My overall feedback is that you need to be aware of:
  1. context size
  2. parallelizable tasks
  
  So for instance, each review should be in its own file. As an example: https://github.com/kubernetes-sigs/kueue/pull/10819/changes -> I'm going to make a review folder, then dump each number into its own file. This ensures that the agent can spin up in parallel & close each context when its done with it.

### Review by [@amy](https://github.com/amy) (commented) — 2026-05-10T17:19:10Z

- **cmd/experimental/agent/skills/code-eval.md:51** by [@amy](https://github.com/amy) — 2026-05-10T17:19:10Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
```

  So here, each domain can be a file that is a table of contents that points to each rule file.

### Review by [@amy](https://github.com/amy) (commented) — 2026-05-10T17:20:04Z

- **cmd/experimental/agent/skills/code-eval.md:156** by [@amy](https://github.com/amy) — 2026-05-10T17:20:04Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
+
+Rules:
+1. **Naming precision** — every new identifier must accurately describe what it contains. A reader should not be able to mistake it for something broader or narrower.
+   - Builder/wrapper methods: follow the existing pattern. If the codebase uses `Label(k, v)` not `AddLabel(k, v)`, new methods must match.
+   - Variable names must reflect actual content. If a set called `noMoreBorrowingCohorts` might actually contain cohorts that are already borrowing, the name is wrong.
+   - Feature gate names must match what they actually gate. A gate named `SkipFinalizersForServingWorkloads` that guards a different condition is a hard naming violation.
+   - New API fields that mirror existing ones should be symmetric (e.g. if `excludeResourcePrefixes` exists, the complement should be `includeResourcePrefixes`).
+2. **Naming conventions** — Diff Code MUST abide by existing naming conventions in the file and package.
+3. **Util function reuse** — Diff Code SHOULD reuse existing util/helper functions. Reimplementing logic that already exists elsewhere is a style violation.
+4. **Log verbosity levels** — per-reconciliation-cycle log lines must use `V(4)` or higher. `V(2)` is for coarse-grained lifecycle events only. A `V(2)` log inside a reconcile loop is a recurring-style violation.
+5. **Test function naming** — unit/integration test function names must align with the names of the functions they test.
+
+Deduct points proportionally to the severity and frequency of violations. Naming precision issues that introduce semantic ambiguity are more severe than minor wording choices.
+
+---
+
+### Domain 2: Buggy Behavior (0–20 pts)
+
+Rules:
+1. **Logical correctness** — Diff Code MUST NOT contain logical errors, off-by-one errors, incorrect conditionals, unhandled edge cases, race conditions, or other behavioral defects.
+2. **Backwards compatibility** — if Diff Code deletes or moves code that manages cluster state (finalizers, annotations, ownership, CRD fields), consider what happens during a rolling upgrade. Objects created by the old version must still be handled correctly by the new version. Deleting finalizer-removal code means pre-existing objects will be stuck. This is a **hard blocker** — deleted compatibility code is a serious bug.
+3. **Feature gate interaction** — if Diff Code adds conditional behavior gated by a feature flag, verify the behavior is correct both when the gate is enabled and disabled. Untested gate-off paths are a latent correctness risk.
+4. **Unnecessary guard conditions** — extra conditions that are logically unreachable at the call site add noise and can mask future bugs. They are a mild correctness concern.
+
+Deduct points proportionally based on severity: a missing backwards-compatibility guard or logic error on the hot path is more severe than a minor edge-case miss.
+
+---
+
+### Domain 3: Comments (0–5 pts)
+
+Rules:
+1. Diff Code MUST NOT contain excessive comments that explain obvious or self-documenting code.
+2. Comments are allowed — and encouraged — only for genuinely complex sections where the *why* or the theoretical side is non-obvious to a reader.
+3. **Comment accuracy** — any comment or docstring must match what the code actually does. A comment that says "first batch of Job pods" when the function operates on generic Workloads is wrong and must be updated. Inaccurate comments are worse than no comments.
+4. **TODO comments** — if Diff Code keeps temporary or deprecated code for compatibility reasons, a TODO comment is *required*, not optional. The TODO must specify the exact version when it can be removed (e.g. `// TODO: drop in 0.18`) and reference a tracking issue.
+
+Full marks if comments are absent or appropriately targeted. Deduct for over-commenting, comments that describe *what* rather than *why*, or inaccurate comments. Deduct for missing required TODOs on compatibility shims.
+
+---
+
+### Domain 4: Small Architectural Decisions (0–25 pts)
+
+Architectural decisions at the small scale concern how code is structured, how parts connect, how simple and human-readable the result is. Complex problems may require complex solutions — but complexity must be justified.
+
+Rules:
+1. **Logical and maintainable** — Diff Code MUST be logical and maintainable.
+2. **No nonsensical decisions** — Diff Code MUST NOT contain unnecessary indirection, wrong abstractions, or confusing data flow.
+3. **Simplicity** — Diff Code SHOULD be as simple as possible, but not simpler. Do not penalize necessary complexity.
+4. **No unnecessary intermediate variables** — avoid `x := foo.Bar; use(x)` when `use(foo.Bar)` is clearer. Redundant local variables add noise without adding clarity.
+5. **Helper extraction** — if the same logic appears across multiple types, adapters, or call sites, it should live in a shared helper. Copying identical logic is an architectural smell. Conversely, do not extract a helper unless it is actually reused now or has a clear near-term reuse case.
+6. **Scope discipline** — changes should do one thing. If a diff conflates a bugfix with a refactor, or applies a generic-looking helper to all types when only one type needs it, the scope is too broad. Prefer the smallest change that correctly solves the problem.
+7. **Code placement** — logic should live where a reader would expect to find it. Cleanup logic inside a `Delete` function is fine; the same cleanup scattered across callers is not.
+
+Deduct points for avoidable complexity, poor decomposition, scope creep, or structural choices that make the code harder to understand or extend without good reason.
+
+---
+
+## Step 4 — Produce the report
+
+Rules for findings: assume max score is already achieved, when you find violation of the rules, deduct points from the score and denote it like that "<Finding Title> (-x pt): <Explanation>". Points deduction depends on severity of a violation. Possible severities: high, medium, low. High=15pt, Medium=7pt, Low=3pt. Remember code evaluation MUST promote these principles: Maintainability, Simplicity, Backward Compatibility. 
+
+Output the evaluation in this exact structure:
+
+```
+## Code Evaluation Report
+
+**Commits**: <BaseCommit>..<HeadCommit>
+**Files changed**: (from --stat output)
+
+---
+
+### Domain 1: Code Style
+**Score**: X/50
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
+
+### Domain 2: Buggy Behavior
+**Score**: X/20
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
+
+### Domain 3: Comments
+**Score**: X/5
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
+
+### Domain 4: Small Architectural Decisions
+**Score**: X/25
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
+
+---
+
+### Final Score: X/100
+
+---
+
+## Recommendations
+
+(One recommendation per issue found. Only include recommendations where the score was reduced. Each recommendation must have all three sections below.)
+
+### Recommendation N: <short title>
+
+**Problem**: Describe the specific issue in the diff.
+**Reason**: Explain why this is a problem — what goes wrong or degrades over time.
+**Solution**: Give a concrete, actionable fix. Where possible, show a before/after code snippet.
+```
+
+## Principles
```

  I'd move this to the top.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-11T08:59:15Z

- **cmd/experimental/agent/skills/code-eval.md:125** by [@kshalot](https://github.com/kshalot) — 2026-05-11T08:59:15Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
+
+Rules:
+1. **Naming precision** — every new identifier must accurately describe what it contains. A reader should not be able to mistake it for something broader or narrower.
+   - Builder/wrapper methods: follow the existing pattern. If the codebase uses `Label(k, v)` not `AddLabel(k, v)`, new methods must match.
+   - Variable names must reflect actual content. If a set called `noMoreBorrowingCohorts` might actually contain cohorts that are already borrowing, the name is wrong.
+   - Feature gate names must match what they actually gate. A gate named `SkipFinalizersForServingWorkloads` that guards a different condition is a hard naming violation.
+   - New API fields that mirror existing ones should be symmetric (e.g. if `excludeResourcePrefixes` exists, the complement should be `includeResourcePrefixes`).
+2. **Naming conventions** — Diff Code MUST abide by existing naming conventions in the file and package.
+3. **Util function reuse** — Diff Code SHOULD reuse existing util/helper functions. Reimplementing logic that already exists elsewhere is a style violation.
+4. **Log verbosity levels** — per-reconciliation-cycle log lines must use `V(4)` or higher. `V(2)` is for coarse-grained lifecycle events only. A `V(2)` log inside a reconcile loop is a recurring-style violation.
+5. **Test function naming** — unit/integration test function names must align with the names of the functions they test.
+
+Deduct points proportionally to the severity and frequency of violations. Naming precision issues that introduce semantic ambiguity are more severe than minor wording choices.
+
+---
+
+### Domain 2: Buggy Behavior (0–20 pts)
+
+Rules:
+1. **Logical correctness** — Diff Code MUST NOT contain logical errors, off-by-one errors, incorrect conditionals, unhandled edge cases, race conditions, or other behavioral defects.
+2. **Backwards compatibility** — if Diff Code deletes or moves code that manages cluster state (finalizers, annotations, ownership, CRD fields), consider what happens during a rolling upgrade. Objects created by the old version must still be handled correctly by the new version. Deleting finalizer-removal code means pre-existing objects will be stuck. This is a **hard blocker** — deleted compatibility code is a serious bug.
+3. **Feature gate interaction** — if Diff Code adds conditional behavior gated by a feature flag, verify the behavior is correct both when the gate is enabled and disabled. Untested gate-off paths are a latent correctness risk.
+4. **Unnecessary guard conditions** — extra conditions that are logically unreachable at the call site add noise and can mask future bugs. They are a mild correctness concern.
+
+Deduct points proportionally based on severity: a missing backwards-compatibility guard or logic error on the hot path is more severe than a minor edge-case miss.
+
+---
+
+### Domain 3: Comments (0–5 pts)
+
+Rules:
+1. Diff Code MUST NOT contain excessive comments that explain obvious or self-documenting code.
+2. Comments are allowed — and encouraged — only for genuinely complex sections where the *why* or the theoretical side is non-obvious to a reader.
+3. **Comment accuracy** — any comment or docstring must match what the code actually does. A comment that says "first batch of Job pods" when the function operates on generic Workloads is wrong and must be updated. Inaccurate comments are worse than no comments.
+4. **TODO comments** — if Diff Code keeps temporary or deprecated code for compatibility reasons, a TODO comment is *required*, not optional. The TODO must specify the exact version when it can be removed (e.g. `// TODO: drop in 0.18`) and reference a tracking issue.
+
+Full marks if comments are absent or appropriately targeted. Deduct for over-commenting, comments that describe *what* rather than *why*, or inaccurate comments. Deduct for missing required TODOs on compatibility shims.
+
+---
+
+### Domain 4: Small Architectural Decisions (0–25 pts)
+
+Architectural decisions at the small scale concern how code is structured, how parts connect, how simple and human-readable the result is. Complex problems may require complex solutions — but complexity must be justified.
+
+Rules:
+1. **Logical and maintainable** — Diff Code MUST be logical and maintainable.
+2. **No nonsensical decisions** — Diff Code MUST NOT contain unnecessary indirection, wrong abstractions, or confusing data flow.
+3. **Simplicity** — Diff Code SHOULD be as simple as possible, but not simpler. Do not penalize necessary complexity.
+4. **No unnecessary intermediate variables** — avoid `x := foo.Bar; use(x)` when `use(foo.Bar)` is clearer. Redundant local variables add noise without adding clarity.
+5. **Helper extraction** — if the same logic appears across multiple types, adapters, or call sites, it should live in a shared helper. Copying identical logic is an architectural smell. Conversely, do not extract a helper unless it is actually reused now or has a clear near-term reuse case.
+6. **Scope discipline** — changes should do one thing. If a diff conflates a bugfix with a refactor, or applies a generic-looking helper to all types when only one type needs it, the scope is too broad. Prefer the smallest change that correctly solves the problem.
+7. **Code placement** — logic should live where a reader would expect to find it. Cleanup logic inside a `Delete` function is fine; the same cleanup scattered across callers is not.
+
+Deduct points for avoidable complexity, poor decomposition, scope creep, or structural choices that make the code harder to understand or extend without good reason.
+
+---
+
+## Step 4 — Produce the report
+
+Rules for findings: assume max score is already achieved, when you find violation of the rules, deduct points from the score and denote it like that "<Finding Title> (-x pt): <Explanation>". Points deduction depends on severity of a violation. Possible severities: high, medium, low. High=15pt, Medium=7pt, Low=3pt. Remember code evaluation MUST promote these principles: Maintainability, Simplicity, Backward Compatibility. 
+
+Output the evaluation in this exact structure:
+
+```
+## Code Evaluation Report
+
+**Commits**: <BaseCommit>..<HeadCommit>
+**Files changed**: (from --stat output)
+
+---
+
+### Domain 1: Code Style
+**Score**: X/50
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
```

  This is likely model and chain-of-thought dependent, but I think it might be generally better to first output the findings and then output the score, to make sure that the score follows from the findings and not the other way around.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-05-11T09:10:03Z

- **cmd/experimental/agent/skills/code-eval.md:125** by [@reruno](https://github.com/reruno) — 2026-05-11T09:10:03Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
+
+Rules:
+1. **Naming precision** — every new identifier must accurately describe what it contains. A reader should not be able to mistake it for something broader or narrower.
+   - Builder/wrapper methods: follow the existing pattern. If the codebase uses `Label(k, v)` not `AddLabel(k, v)`, new methods must match.
+   - Variable names must reflect actual content. If a set called `noMoreBorrowingCohorts` might actually contain cohorts that are already borrowing, the name is wrong.
+   - Feature gate names must match what they actually gate. A gate named `SkipFinalizersForServingWorkloads` that guards a different condition is a hard naming violation.
+   - New API fields that mirror existing ones should be symmetric (e.g. if `excludeResourcePrefixes` exists, the complement should be `includeResourcePrefixes`).
+2. **Naming conventions** — Diff Code MUST abide by existing naming conventions in the file and package.
+3. **Util function reuse** — Diff Code SHOULD reuse existing util/helper functions. Reimplementing logic that already exists elsewhere is a style violation.
+4. **Log verbosity levels** — per-reconciliation-cycle log lines must use `V(4)` or higher. `V(2)` is for coarse-grained lifecycle events only. A `V(2)` log inside a reconcile loop is a recurring-style violation.
+5. **Test function naming** — unit/integration test function names must align with the names of the functions they test.
+
+Deduct points proportionally to the severity and frequency of violations. Naming precision issues that introduce semantic ambiguity are more severe than minor wording choices.
+
+---
+
+### Domain 2: Buggy Behavior (0–20 pts)
+
+Rules:
+1. **Logical correctness** — Diff Code MUST NOT contain logical errors, off-by-one errors, incorrect conditionals, unhandled edge cases, race conditions, or other behavioral defects.
+2. **Backwards compatibility** — if Diff Code deletes or moves code that manages cluster state (finalizers, annotations, ownership, CRD fields), consider what happens during a rolling upgrade. Objects created by the old version must still be handled correctly by the new version. Deleting finalizer-removal code means pre-existing objects will be stuck. This is a **hard blocker** — deleted compatibility code is a serious bug.
+3. **Feature gate interaction** — if Diff Code adds conditional behavior gated by a feature flag, verify the behavior is correct both when the gate is enabled and disabled. Untested gate-off paths are a latent correctness risk.
+4. **Unnecessary guard conditions** — extra conditions that are logically unreachable at the call site add noise and can mask future bugs. They are a mild correctness concern.
+
+Deduct points proportionally based on severity: a missing backwards-compatibility guard or logic error on the hot path is more severe than a minor edge-case miss.
+
+---
+
+### Domain 3: Comments (0–5 pts)
+
+Rules:
+1. Diff Code MUST NOT contain excessive comments that explain obvious or self-documenting code.
+2. Comments are allowed — and encouraged — only for genuinely complex sections where the *why* or the theoretical side is non-obvious to a reader.
+3. **Comment accuracy** — any comment or docstring must match what the code actually does. A comment that says "first batch of Job pods" when the function operates on generic Workloads is wrong and must be updated. Inaccurate comments are worse than no comments.
+4. **TODO comments** — if Diff Code keeps temporary or deprecated code for compatibility reasons, a TODO comment is *required*, not optional. The TODO must specify the exact version when it can be removed (e.g. `// TODO: drop in 0.18`) and reference a tracking issue.
+
+Full marks if comments are absent or appropriately targeted. Deduct for over-commenting, comments that describe *what* rather than *why*, or inaccurate comments. Deduct for missing required TODOs on compatibility shims.
+
+---
+
+### Domain 4: Small Architectural Decisions (0–25 pts)
+
+Architectural decisions at the small scale concern how code is structured, how parts connect, how simple and human-readable the result is. Complex problems may require complex solutions — but complexity must be justified.
+
+Rules:
+1. **Logical and maintainable** — Diff Code MUST be logical and maintainable.
+2. **No nonsensical decisions** — Diff Code MUST NOT contain unnecessary indirection, wrong abstractions, or confusing data flow.
+3. **Simplicity** — Diff Code SHOULD be as simple as possible, but not simpler. Do not penalize necessary complexity.
+4. **No unnecessary intermediate variables** — avoid `x := foo.Bar; use(x)` when `use(foo.Bar)` is clearer. Redundant local variables add noise without adding clarity.
+5. **Helper extraction** — if the same logic appears across multiple types, adapters, or call sites, it should live in a shared helper. Copying identical logic is an architectural smell. Conversely, do not extract a helper unless it is actually reused now or has a clear near-term reuse case.
+6. **Scope discipline** — changes should do one thing. If a diff conflates a bugfix with a refactor, or applies a generic-looking helper to all types when only one type needs it, the scope is too broad. Prefer the smallest change that correctly solves the problem.
+7. **Code placement** — logic should live where a reader would expect to find it. Cleanup logic inside a `Delete` function is fine; the same cleanup scattered across callers is not.
+
+Deduct points for avoidable complexity, poor decomposition, scope creep, or structural choices that make the code harder to understand or extend without good reason.
+
+---
+
+## Step 4 — Produce the report
+
+Rules for findings: assume max score is already achieved, when you find violation of the rules, deduct points from the score and denote it like that "<Finding Title> (-x pt): <Explanation>". Points deduction depends on severity of a violation. Possible severities: high, medium, low. High=15pt, Medium=7pt, Low=3pt. Remember code evaluation MUST promote these principles: Maintainability, Simplicity, Backward Compatibility. 
+
+Output the evaluation in this exact structure:
+
+```
+## Code Evaluation Report
+
+**Commits**: <BaseCommit>..<HeadCommit>
+**Files changed**: (from --stat output)
+
+---
+
+### Domain 1: Code Style
+**Score**: X/50
+**Findings**: (bullet list of specific observations, in accordance with the specified rules for findings)
```

  I think state of the art models with thinking chains are not that vulnerable to this kind of errors, also model calculates the Score based on findings. Model assumes diff code is already at max score, and when models find a violation of rule it deducts from the score defined amount of points, so score pretty much depends on number and severity of findings.  Probably for more budget models it will be good change. 
  
  Also IMO having score at the top is just more convenient for an eye  :)

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-05-11T09:13:19Z

- **cmd/experimental/agent/skills/code-eval.md:51** by [@reruno](https://github.com/reruno) — 2026-05-11T09:13:19Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
+
+## Arguments
+
+The user invoked this with two positional arguments:
+- **BaseCommit** — the base (older) commit
+- **HeadCommit** — the head (newer) commit
+
+## Step 1 — Retrieve the diff
+
+Run:
+```
+git diff <BaseCommit> <HeadCommit>
+```
+
+Read the full output. This is the **Diff Code** you will evaluate.
+
+Also run:
+```
+git diff <BaseCommit> <HeadCommit> --stat
+```
+
+to get a high-level overview of changed files.
+
+## Step 2 — Understand context
+
+For each changed file in the diff, read enough of the surrounding code (unchanged lines, nearby functions, imports) to understand:
+- The existing code style and naming conventions
+- Util/helper functions already available in the package or nearby packages
+- The overall structure and architecture of the file
+- What patterns exist for similar operations (e.g. how existing builder methods are named)
+
+Use `git show <BaseCommit>:<filepath>` or read the file at HEAD if it gives better context.
+
+Pay particular attention to:
+- How existing functions/methods in the same package are named (builder pattern vs verb-noun, etc.)
+- Whether shared helper logic already exists for the same operation across other types/adapters
+- Log verbosity conventions (what V-level is used for recurring vs lifecycle events)
+- Whether any deleted code might be needed during a rolling upgrade or version skew
+
+## Step 3 — Evaluate across domains
+
+Score the Diff Code on each domain below. Be strict but fair. Every point deduction must be noted so the user understands what happened.
+
+---
+
+### Domain 1: Code Style (0–50 pts)
```

  https://github.com/kubernetes-sigs/kueue/pull/10819#issuecomment-4415936010
  
  As here was mentioned, I will split domains into different files, when the PR is merged

### Review by [@j-skiba](https://github.com/j-skiba) (commented) — 2026-05-12T08:32:07Z

- **cmd/experimental/agent/skills/code-eval.md:5** by [@j-skiba](https://github.com/j-skiba) — 2026-05-12T08:32:07Z
```diff
@@ -0,0 +1,163 @@
+# Skill: Code Evaluation
+
+Evaluate the quality of a git diff between two commits. Scores the changes across four domains — Code Style, Buggy Behavior, Comments, and Small Architectural Decisions — then provides actionable recommendations for improvement.
+
+Use this when a user asks to evaluate, score, or review a diff between two commits.
```

  I think we should use frontmatter here - https://agentskills.io/specification#frontmatter
