# PR #11122: Increase flaky pull-kueue-test-e2e-multikueue-sequential-main verbosity.

**Summary**: Increase flaky pull-kueue-test-e2e-multikueue-sequential-main verbosity.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11122

**Last updated**: 2026-05-14T08:53:36Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@polinasand](https://github.com/polinasand)
- **Branch**: `fix/flake-e2e-test-10848` → `main`
- **Created**: 2026-05-11T14:33:45Z
- **Updated**: 2026-05-14T08:53:36Z
- **Merged**: 2026-05-14T08:46:27Z
- **Closed**: 2026-05-14T08:46:27Z
- **Labels**: `lgtm`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `cncf-cla: yes`, `area/multikueue`, `area/testing`
- **Assignees**: [@olekzabl](https://github.com/olekzabl)
- **Requested reviewers**: _none_
- **Changed files**: 3   **+32 / -33**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->
/area testing
/area multikueue
/kind flake

#### What this PR does / why we need it:
This PR adds logging with workloads details inside a flaky test, to provide more data about resources state in case of future flakiness.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Progresses #10848

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE

```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-11T14:33:56Z

Hi @polinasand. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-11T14:33:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | e4179dbb1e3c5cfb8eb2265a382c47708d0143ff |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a046eac2b6eac00087cb9f3 |

### Comment by [@kshalot](https://github.com/kshalot) — 2026-05-11T14:43:47Z

/ok-to-test

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-12T16:02:10Z

/approve
/cherrypick release-0.17
/cherrypick release-0.16
leaving lgtm to @kshalot or @olekzabl for sanity check

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-12T16:02:13Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11122#issuecomment-4432427072):

>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>leaving lgtm to @kshalot or @olekzabl for sanity check


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-12T16:02:20Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11122#issuecomment-4432427072" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11122#" title="Author self-approved">polinasand</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[test/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/test/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-12T16:02:27Z

Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-14T08:16:37Z

LGTM label has been added.  <details>Git tree hash: 1a060f27adda95b8a1bc341cddd4f5b3f4818a62</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-14T08:47:12Z

@mimowo: new pull request created: #11180

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11122#issuecomment-4432427072):

>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>leaving lgtm to @kshalot or @olekzabl for sanity check


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-14T08:47:48Z

@mimowo: #11122 failed to apply on top of branch "release-0.16":
```
Applying: Increase timeout for test
Using index info to reconstruct a base tree...
A	test/e2e/multikueue/sequential/e2e_test.go
Falling back to patching base and 3-way merge...
Auto-merging test/e2e/multikueue-sequential/e2e_test.go
CONFLICT (content): Merge conflict in test/e2e/multikueue-sequential/e2e_test.go
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 Increase timeout for test

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11122#issuecomment-4432427072):

>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>leaving lgtm to @kshalot or @olekzabl for sanity check


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-14T08:53:36Z

@polinasand please check if it makes sense to CP for 0.16, if so then prepare the CP manually

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T14:48:00Z

- **test/e2e/multikueue/sequential/e2e_test.go:884** by [@mimowo](https://github.com/mimowo) — 2026-05-11T14:48:00Z
```diff
@@ -876,3 +880,24 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 		})
 	})
 })
+
+func assertMsg(msg string, wlKey client.ObjectKey) func() string {
```

  Move these predefined function https://github.com/kubernetes-sigs/kueue/blob/048c2f01639162068013bf957e9d609b75699940/test/e2e/multikueue/baseline/e2e_test.go#L1995C6-L2015
  to test/util/e2e.go and reuse instead of duplicating.
  
  We need to rename it to avoid conflict, maybe `AssertMsgForMk`

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-11T14:48:15Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@kshalot](https://github.com/kshalot) — 2026-05-11T14:48:15Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  Maybe we could move `worker1HighWorkload` and `worker2HighWorkload` outside of the block and use `util.AssertMsg` on the entire `Eventually` case? like:
  ```go
  gomega.Eventually(func(g gomega.Gomega) {
  ...
  }, util.MediumTimeout, util.Interval).Should(gomega.Succeed(), util.AssertMsg("some good message", worker1HighWorkload, worker2HighWorkload))

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-11T14:48:55Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@kshalot](https://github.com/kshalot) — 2026-05-11T14:48:55Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  If the workload was not found it will remain `nil`. And this won't require adding any new logic.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-11T23:55:42Z

- **test/e2e/multikueue/sequential/e2e_test.go:834** by [@olekzabl](https://github.com/olekzabl) — 2026-05-11T23:35:18Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
```

  Nit: Looking at these 2 expectations, aren't they *equivalent*, just the latter equipped with a custom message?
  How about just this adding the message to the existing expectation?
  ```suggestion
  g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted), assertMsg("Workload not Admitted in worker1", highWlKey))
  ```

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-11T23:56:14Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@olekzabl](https://github.com/olekzabl) — 2026-05-11T23:56:14Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  @kshalot 
  Not sure if I'm following. The remark on `nil` is cryptic to me, and I'm not sure what you mean by "new logic" (possibly obsoleted by [this comment](https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3219830336)?).
  
  But anyway, given that @polinasand apparently wants to log workload state from all the clusters, I guess it wouldn't hurt to replace your proposed message with sth like `assertWlMsg("sth1", worker1HighWorkload) + assertWlMsg("sth2", worker2HighWorkload)`?

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-12T07:02:38Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@kshalot](https://github.com/kshalot) — 2026-05-12T07:02:38Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  My point is that if we initialize the variables outside (maybe we can even assign `&kueue.Workload{}` to them, it doesn't matter, then we could just wrap the entire block with the existing `util.AssertMsg`.
  
  This would mean that wherever it fails, we will get the message. For example, currently this PR does not cover the line that was originally failing:
  ```go
  g.Expect(worker1Error == nil).NotTo(gomega.Equal(worker2Error == nil))
  ```
  
  My point with `nil` was that if the call fails:
  ```go
  worker1Error := k8sWorker1Client.Get(ctx, highWlKey, worker1HighWorkload)
  ```
  then `worker1HighWorkload` will remain `&kueue.Workload{}`, which conveys the message that something went wrong and we can log it as is, without any additional logic handling "not found" etc. (so I misspoke with `nil`, it's more accurate to say `&kueue.Workload{}`).
  
  One difference is that it won't include the manager workload, which might or might not be useful.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-12T11:00:21Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@olekzabl](https://github.com/olekzabl) — 2026-05-12T11:00:20Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  Thanks, now I understand you better.
  
  +1 for wishing to cover more potentially failing lines, in particular `(w1err == nil) != (w2err == nil)` (where, in particular, two non-nil errs could be a misleading case).
  
  > One difference is that it won't include the manager workload, which might or might not be useful.
  
  Agreed, and my intuition is to rather include it, if it's not problematic.
  Every iteration of this ping-pong research costs time (to merge the PR, and then to wait for a repro), so I'd lean towards gathering more info whenever we have some nice idea.
  
  So, I think we're converging towards sth like `gomega.Eventually(..., assertWlMsg(w1) + assertWlMsg(w2))`.
  (And if so, [my other comment](https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3222786352) will become obsolete)

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-12T11:33:45Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@kshalot](https://github.com/kshalot) — 2026-05-12T11:33:45Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  Ok, let's include the manager. But I'd consider moving it to utils as something more generic, as proposed in https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3219830336.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-12T11:50:23Z

- **test/e2e/multikueue/sequential/e2e_test.go:840** by [@olekzabl](https://github.com/olekzabl) — 2026-05-12T11:50:23Z
```diff
@@ -829,11 +831,13 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 
 					if worker1Error == nil {
 						g.Expect(worker1HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker1HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker1", highWlKey))
 						evictedWlKey = lowWlKey1
 						unaffectedWlKey = lowWlKey2
 						unaffectedWorkerClient = k8sWorker2Client
 					} else {
 						g.Expect(worker2HighWorkload.Status.Conditions).To(utiltesting.HaveConditionStatusTrue(kueue.WorkloadAdmitted))
+						g.Expect(workload.IsAdmitted(worker2HighWorkload)).To(gomega.BeTrue(), assertMsg("Workload not Admitted in worker2", highWlKey))
```

  Oh, yes. 
  
  BTW, to clarify: my understanding of that comment is that we shouldn't move the helpers from this PR to utils.
  Instead, we should move pre-existing helpers ([here](https://github.com/kubernetes-sigs/kueue/pull/11122)) to utils, and use them in this PR.
  So that this PR does not need to add new logging helpers.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T15:16:12Z

- **test/util/e2e.go:808** by [@mimowo](https://github.com/mimowo) — 2026-05-12T15:16:12Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(msg string, wl *kueue.Workload, ctx context.Context, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  nit, ctx is always the first param by convention https://pkg.go.dev/context "The Context should be the first parameter, typically named ctx:"

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-12T17:20:17Z

- **test/e2e/multikueue/sequential/e2e_test.go:843** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-12T17:20:18Z
```diff
@@ -838,7 +838,10 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 						unaffectedWlKey = lowWlKey1
 						unaffectedWorkerClient = k8sWorker1Client
 					}
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed(), func() string {
+					return util.AssertMsgForMk(ctx, "Workload not Admitted in worker1", worker1HighWorkload, k8sManagerClient, k8sWorker1Client, k8sWorker2Client)() +
+						util.AssertMsgForMk(ctx, "Workload not Admitted in worker2", worker2HighWorkload, k8sManagerClient, k8sWorker1Client, k8sWorker2Client)()
```

  I’m actually not sure why we have this duplication. Can we use `AssertMsg()` for `worker1HighWorkload` and `worker2HighWorkload`?
  
  I agree that we need to print the current state for `worker1HighWorkload` and `worker2HighWorkload`, but I think we can introduce one more helper for `k8sManagerClient`, `k8sWorker1Client`, and `k8sWorker2Client` and reuse it in `AssertMsgForMk()`.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-12T17:33:46Z

- **test/util/e2e.go:815** by [@kshalot](https://github.com/kshalot) — 2026-05-12T17:33:46Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  I'm not sure I like this. Since you are doing requests in `getWorkload` **after** the flake already occurred, you might fetch the new state that would actually pass the flaky, no? I mean - it's not guaranteed that what you fetch from the cluster here will be consistent with what the test observed.
  
  That's another reason why I liked the approach I described in https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3219831812, since it's using exactly what it encountered in the test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T17:55:49Z

- **test/util/e2e.go:815** by [@mimowo](https://github.com/mimowo) — 2026-05-12T17:55:49Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  Yes, but if the Eventually fails then it means we waited 45s or 90s already. The assumption is that at this point we can be confident the object is not making progress and will not change if added another second. 
  
  Still, I would not object another approach which doesn't have the drawback, but I'm not sure if we have such currently.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-12T17:58:06Z

- **test/util/e2e.go:815** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-12T17:58:06Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  We still keep AssertMsg(msg, wl)(), which shows the current state of the workload.

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-12T18:18:54Z

- **test/util/e2e.go:815** by [@kshalot](https://github.com/kshalot) — 2026-05-12T18:18:53Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  True that my concern might not be practical given the long timeouts. But we can consider https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3219831812 like I said. It displays what was returned from the workers without any post-flake calls, but it won't log the manager.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-12T22:45:46Z

- **test/util/e2e.go:815** by [@olekzabl](https://github.com/olekzabl) — 2026-05-12T22:45:46Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  So anyway we get the state after, say, either "45s" or "45s + tiny delay" (where I also don't believe that tiny delay matters).
  So maybe the largest risk is that the valuable info may have disappeared way before 45s elapsed?
  
  I'm thinking if we can nicely log "intermediate checkpoints" while waiting on the long timeout.
  The "ginkgo-idiomatic" way seems to be `PollProgressInterval` + `AttachProgressReporter`; see [here](https://onsi.github.io/ginkgo/#getting-visibility-into-long-running-specs).
  
  But maybe it's overdoing the logs... WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T07:13:20Z

- **test/e2e/multikueue/sequential/e2e_test.go:843** by [@mimowo](https://github.com/mimowo) — 2026-05-13T07:13:20Z
```diff
@@ -838,7 +838,10 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 						unaffectedWlKey = lowWlKey1
 						unaffectedWorkerClient = k8sWorker1Client
 					}
-				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed())
+				}, util.MediumTimeout, util.Interval).Should(gomega.Succeed(), func() string {
+					return util.AssertMsgForMk(ctx, "Workload not Admitted in worker1", worker1HighWorkload, k8sManagerClient, k8sWorker1Client, k8sWorker2Client)() +
+						util.AssertMsgForMk(ctx, "Workload not Admitted in worker2", worker2HighWorkload, k8sManagerClient, k8sWorker1Client, k8sWorker2Client)()
```

  +1, I think this will result in printing the same set of workloads twice because `AssertMsgForMk` already handles lookups to all clusters.
  
  I think to make it less confusing `AssertMsgForMk` should only accept the wlKey so that it is agnostic of the client used. The code here could be simplified to just say:
  
  `return util.AssertMsgForMk(ctx, "Workload was expected to be admitted on exactly one worker", highWlKey, k8sManagerClient, k8sWorker1Client, k8sWorker2Client)`

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T07:37:46Z

- **test/util/e2e.go:815** by [@mimowo](https://github.com/mimowo) — 2026-05-13T07:37:45Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  I understand your concern @kshalot, and I agree the proposal in https://github.com/kubernetes-sigs/kueue/pull/11122#discussion_r3222786352 does not have the drawback, but it seems to have another - it is more work on the test side to each time think which information is needed for debugging MK workloads, rather than to have a standardized approach with AssertMsgForMk which could dump the workloads just based on the "wlKey".
  
  Still, I'm wondering if we could have the best of both worlds if we encapsulated the three workloads in a struct like `struct mkWorkload` , say
  
  ```golang 
  type MKWorkload  struct {
    manager
    worker1
    worker2
    
    managerClinet
    worker1Client
    worker2Client
  }
  ```
  Then it could have the `Get` function which would refresh the state of the struct. Then, `mkWorkload.worker1`, `mkWorkload.worker2` , `mkWorkload.manager`  could be used for asserts, and to build the failure message, without the delay.
  
  Then it would make `AssertMsgForMk` obsolete, but this is significantly more work to do well, so I'm leaning to go ahead with `AssertMsgForMk` as pilot and search for alternatives like MKWorkload if this proves confusing / not suffecient. 
  
  BTW: The post-failure dump strategy is used in k8s core extensively with [DumpAllNamespaceInfo](https://github.com/kubernetes/kubernetes/blob/master/test/e2e/framework/debug/dump.go#L66), and has proven to be useful. Still, I agree what we do with `AssertMsgForMk` is somewhere in-between.

### Review by [@polinasand](https://github.com/polinasand) (commented) — 2026-05-13T12:14:36Z

- **test/util/e2e.go:815** by [@polinasand](https://github.com/polinasand) — 2026-05-13T12:14:36Z
```diff
@@ -804,3 +804,25 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	}
 	return cq
 }
+
+func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+	return func() string {
+		wlKey := client.ObjectKeyFromObject(wl)
+		return strings.Join([]string{
+			AssertMsg(msg, wl)(),
+			AssertMsg("Manager", getWorkload(ctx, k8sManagerClient, wlKey))(),
+			AssertMsg("Worker1", getWorkload(ctx, k8sWorker1Client, wlKey))(),
+			AssertMsg("Worker2", getWorkload(ctx, k8sWorker2Client, wlKey))(),
```

  Agreed to stay with current approach for now.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-13T13:13:18Z

- **test/util/e2e.go:808** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-13T13:13:18Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  I’m actually not sure about that. We need to show the current state of the object instead of the newly fetched one. It could be confusing if the original object and the fetched object are different. So I propose reverting these changes and printing the failed object instead.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-13T13:16:52Z

- **test/util/e2e.go:808** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-13T13:16:52Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  My concern was different. I think we should print worker1HighWorkload and worker2HighWorkload as failed workloads, and also fetch worker3HighWorkload to provide more details.

### Review by [@polinasand](https://github.com/polinasand) (commented) — 2026-05-13T14:37:18Z

- **test/util/e2e.go:808** by [@polinasand](https://github.com/polinasand) — 2026-05-13T14:37:18Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  What is `worker3HighWorkload` here?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-13T14:42:12Z

- **test/util/e2e.go:808** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-13T14:42:12Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  > What is worker3HighWorkload here?
  
  Oops, sorry, I mean workload for manager.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T14:51:59Z

- **test/util/e2e.go:808** by [@mimowo](https://github.com/mimowo) — 2026-05-13T14:51:59Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  > t could be confusing if the original object and the fetched object are different. So I propose reverting these changes and printing the failed object instead.
  
  I don't understand, we re-fetch the object in both approaches. The object can be changed in the tiny amount of time anyway, yes this is a known drawback, but it is not getting fixed by passing the entire workload object. Passing the entire workload makes it more confusing imo, because it asks a question: which cluster is the workload coming from? But this question is not relevant, what matters is only the "key" - because the same key is used for all clusters.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-13T15:15:11Z

- **test/util/e2e.go:808** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-13T15:15:11Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  No. In this case, we don’t re-fetch the failed object. We print the failed object, so we should see the failed object.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-13T15:17:09Z

- **test/util/e2e.go:808** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-13T15:17:08Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  Otherwise, we may fetch an updated object, which could make debugging confusing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-13T15:51:52Z

- **test/util/e2e.go:808** by [@mimowo](https://github.com/mimowo) — 2026-05-13T15:51:52Z
```diff
@@ -805,11 +805,9 @@ func SetResourceNominalQuota(cq *kueue.ClusterQueue, resourceName corev1.Resourc
 	return cq
 }
 
-func AssertMsgForMk(ctx context.Context, msg string, wl *kueue.Workload, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
+func AssertMsgForMk(ctx context.Context, msg string, wlKey client.ObjectKey, k8sManagerClient client.Client, k8sWorker1Client client.Client, k8sWorker2Client client.Client) func() string {
```

  I think we already discussed we are ok with this risk, also this risk remains for other clusters.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-05-14T08:16:29Z

/lgtm
though with one super-nit comment

- **test/e2e/multikueue/sequential/e2e_test.go:824** by [@olekzabl](https://github.com/olekzabl) — 2026-05-14T08:16:11Z
```diff
@@ -818,10 +818,10 @@ var _ = ginkgo.Describe("MultiKueue Sequential", func() {
 			var unaffectedWorkerClient client.Client
 
 			ginkgo.By("Checking that the high-priority workload is admitted in one of the workers", func() {
-				gomega.Eventually(func(g gomega.Gomega) {
-					worker1HighWorkload := &kueue.Workload{}
-					worker2HighWorkload := &kueue.Workload{}
+				worker1HighWorkload := &kueue.Workload{}
+				worker2HighWorkload := &kueue.Workload{}
 
+				gomega.Eventually(func(g gomega.Gomega) {
```

  Super nit: this move should be ideally reverted now.
  (So that `worker{1,2}HighWorkload` have the minimal necessary scope).
