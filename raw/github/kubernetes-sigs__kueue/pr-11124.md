# PR #11124: Add a script to fetch and summarize prow times for CI jobs

**Summary**: Add a script to fetch and summarize prow times for CI jobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11124

**Last updated**: 2026-05-11T18:07:53Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `get-testgrid-times` → `main`
- **Created**: 2026-05-11T15:07:19Z
- **Updated**: 2026-05-11T18:07:53Z
- **Merged**: 2026-05-11T18:07:50Z
- **Closed**: 2026-05-11T18:07:50Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@vladikkuzn](https://github.com/vladikkuzn)
- **Requested reviewers**: [@sohankunkerkar](https://github.com/sohankunkerkar), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 1   **+306 / -0**

## Description

#### What type of PR is this?

/kind feature
/kind cleanup

#### What this PR does / why we need it:

Two use cases:
1. identify longest running CI jobs as targets for optimization, used here: https://github.com/kubernetes-sigs/kueue/issues/10995
2. determine thresholds for the CI performance jobs, can be helpful here: https://github.com/kubernetes-sigs/kueue/issues/10773

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-11T15:07:32Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11124#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-11T15:07:32Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | cc6797b74df47b9f0165d83e47c49686eac9ac06 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a02143b3bda900008c8355d |

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-11T15:07:36Z

cc @mbobrovskyi ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:39:33Z

applied the edit suggestion

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T18:03:52Z

Thank you!

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-11T18:04:01Z

LGTM label has been added.  <details>Git tree hash: b66f594f989a6cf35d650e75067ba71f9568d5fc</details>

### Comment by [@vladikkuzn](https://github.com/vladikkuzn) — 2026-05-11T18:07:21Z

/lgtm

## Reviews

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:05:01Z

- **hack/tools/prow-runtimes/prow_runtimes.py:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:05:02Z
  Could you please add the copyright prefix?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:22:57Z

- **hack/tools/prow-runtimes/prow_runtimes.py:1** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:22:57Z
  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:25:11Z

- **hack/tools/prow-runtimes/prow_runtimes.py:1** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:25:11Z
  This also inspired me to open the issue: https://github.com/kubernetes-sigs/kueue/issues/11129

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:27:39Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:27:39Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  ```suggestion
  Copyright 2026 The Kubernetes Authors.
  ```

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:29:59Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:29:59Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  I think we should add the year if it was not generated, like we did in other files.
  
  https://github.com/kubernetes-sigs/kueue/blob/1083269cf820e9d91086fa90b75ec599cba065ff/hack/cherry_pick_pull.sh#L3

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:31:16Z

- **hack/tools/prow-runtimes/prow_runtimes.py:17** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:31:17Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
```

  Probably the shebang should come before the copyright header, right?
  
  https://github.com/kubernetes-sigs/kueue/blob/1083269cf820e9d91086fa90b75ec599cba065ff/hack/cherry_pick_pull.sh#L1

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:32:53Z

- **hack/tools/prow-runtimes/prow_runtimes.py:1** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:32:53Z
  Could you please add `chmod +x` to make it executable by default?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:40:03Z

- **hack/tools/prow-runtimes/prow_runtimes.py:189** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:40:03Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
```

  It looks like it prints the entire file. Should we add a `--verbose` flag for this? WDYT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:40:31Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:40:30Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  without year returns 916 files: https://github.com/search?q=repo%3Akubernetes-sigs%2Fkueue+%22Copyright+The+Kubernetes+Authors.%22&type=code
  
  - no year - 916 files
  - 2022 - 4 files
  - 2023 - 4 files
  - 2024 - 30 files
  - 2025 - 19 files
  - 2026 - 4 files
  
  So, I think we are more consistent without the year actually. I think that is ok.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:43:28Z

- **hack/tools/prow-runtimes/prow_runtimes.py:1** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:43:28Z
  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:43:36Z

- **hack/tools/prow-runtimes/prow_runtimes.py:17** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:43:36Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
```

  done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:46:06Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:46:06Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
+    return sorted(dict.fromkeys(names))
+
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs() if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        print("checking: " + job)
```

  When I run it with the `--json` flag (`./hack/tools/prow-runtimes/prow_runtimes.py --kueue-periodics --limit 5 --only-success --json > out.json`), the JSON output is corrupted.
  
  ```
  ['periodic-kueue-test-unit-main', 'periodic-kueue-verify-website-links-main', 'periodic-kueue-test-integration-main', 'periodic-kueue-test-integration-multikueue-main', 'periodic-kueue-test-scheduling-perf-main', 'periodic-kueue-test-tas-scheduling-perf-main', 'periodic-kueue-test-large-scale-scheduling-perf-main', 'periodic-kueue-test-e2e-main-1-33', 'periodic-kueue-test-e2e-main-1-34', 'periodic-kueue-test-e2e-main-1-35', 'periodic-kueue-test-e2e-multikueue-main', 'periodic-kueue-test-e2e-multikueue-sequential-main', 'periodic-kueue-test-e2e-tas-baseline-main', 'periodic-kueue-test-e2e-tas-extended-main', 'periodic-kueue-test-e2e-sequential-baseline-main', 'periodic-kueue-test-e2e-sequential-extended-main', 'periodic-kueue-test-e2e-certmanager-main', 'periodic-kueue-test-e2e-dra-main', 'periodic-kueue-test-e2e-multikueue-dra-main', 'periodic-kueue-test-e2e-k8s-main-was', 'periodic-kueue-test-e2e-kueueviz-main']
  checking: periodic-kueue-test-e2e-certmanager-main
  checking: periodic-kueue-test-e2e-dra-main
  checking: periodic-kueue-test-e2e-k8s-main-was
  checking: periodic-kueue-test-e2e-kueueviz-main
  checking: periodic-kueue-test-e2e-main-1-33
  checking: periodic-kueue-test-e2e-main-1-34
  checking: periodic-kueue-test-e2e-main-1-35
  checking: periodic-kueue-test-e2e-multikueue-dra-main
  checking: periodic-kueue-test-e2e-multikueue-main
  checking: periodic-kueue-test-e2e-multikueue-sequential-main
  checking: periodic-kueue-test-e2e-sequential-baseline-main
  checking: periodic-kueue-test-e2e-sequential-extended-main
  checking: periodic-kueue-test-e2e-tas-baseline-main
  checking: periodic-kueue-test-e2e-tas-extended-main
  checking: periodic-kueue-test-integration-main
  checking: periodic-kueue-test-integration-multikueue-main
  checking: periodic-kueue-test-large-scale-scheduling-perf-main
  checking: periodic-kueue-test-scheduling-perf-main
  checking: periodic-kueue-test-tas-scheduling-perf-main
  checking: periodic-kueue-test-unit-main
  checking: periodic-kueue-verify-website-links-main
  [
    {
      "job": "periodic-kueue-test-e2e-k8s-main-was",
      "build_count": 5,
      "avg_seconds": 2839,
      "avg": "47:19",
      "runtimes": [
        "46:58",
        "44:38",
        "45:46",
        "49:00",
        "50:13"
      ],
  ...
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:47:32Z

- **hack/tools/prow-runtimes/prow_runtimes.py:189** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:47:32Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
```

  Good idea, I injected this by myself for debugging, because the generated code didn't work - there was a bug in the regex and I had to manually debug. 
  
  So it might also be helpful for others extending the script in the future.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T16:48:59Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T16:48:59Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  > without year returns 916 files
  
  Yeah, it’s because it was generated by golangci-lint. But the Bash and Python scripts are not generated, so I just want to confirm that this is okay.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T16:52:07Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mimowo](https://github.com/mimowo) — 2026-05-11T16:52:07Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
+    return sorted(dict.fromkeys(names))
+
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs() if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        print("checking: " + job)
```

  The json is mostly correct, it is just that the lines in front are simple prints. How would you like to fix it? 
  I'm also ok to drop the json and csv optinons - never used them actually, they were just auto-gen.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:00:06Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:00:06Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  The latest boilerplate in the k8s core does not have the year: https://github.com/search?q=repo%3Akubernetes%2Fkubernetes%20%22Copyright%20The%20Kubernetes%20Authors.%22&type=code
  
  So, if k8s core does not use the year, then I think Kueue also does not need: https://github.com/kubernetes/kubernetes/pull/134835

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:01:02Z

- **hack/tools/prow-runtimes/prow_runtimes.py:191** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:01:03Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
```

  Do we need this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:01:32Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:01:32Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
+    return sorted(dict.fromkeys(names))
+
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs() if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        print("checking: " + job)
```

  another easy idea to execute is to redirect all debug messages (in verbose) to stderr, then I think your command would work well. Let me check

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:03:13Z

- **hack/tools/prow-runtimes/prow_runtimes.py:191** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:03:12Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
```

  Do you ask about a specific line, or the parsing of the file?
  
  I think this is very useful to parse the file to find all periodics. This proved to be useful here: https://github.com/kubernetes-sigs/kueue/issues/10995

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:04:07Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:04:07Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
+    return sorted(dict.fromkeys(names))
+
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs() if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        print("checking: " + job)
```

  ```suggestion
          # Only show "checking" message when running directly in the terminal
          # (skip it automatically when user does > results.json or | something)
          if sys.stdout.isatty():
              print("checking: " + job)
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:05:46Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:05:46Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
+    return sorted(dict.fromkeys(names))
+
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs() if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        print("checking: " + job)
```

  I just pushed the change with debug messages going to stderr, seems reasonable to me

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:07:23Z

- **hack/tools/prow-runtimes/prow_runtimes.py:191** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:07:23Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
```

  Ah, you probably asked about `print(names)` - yeah no need since we anyway print them all with "checking". Let me drop

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-11T17:07:52Z

- **hack/tools/prow-runtimes/prow_runtimes.py:191** by [@mimowo](https://github.com/mimowo) — 2026-05-11T17:07:52Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
```

  Done

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:13:54Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:13:54Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+def load_kueue_periodic_jobs(*, verbose: bool = False) -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    if verbose:
+        print(text, file=sys.stderr)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    return sorted(dict.fromkeys(names))
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+    parser.add_argument("--verbose", "-v", action="store_true", help="Enable verbose output (prints raw data from URLs).")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs(verbose=args.verbose) if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        if args.verbose:
+            print("checking: " + job, file=sys.stderr)
```

  ```suggestion
          print("checking: " + job, file=sys.stderr)
  ```
  I think we can keep this without `--verbose`. The output looks reasonable. WDYT?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:14:41Z

- **hack/tools/prow-runtimes/prow_runtimes.py:273** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:14:41Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+def load_kueue_periodic_jobs(*, verbose: bool = False) -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    if verbose:
+        print(text, file=sys.stderr)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    return sorted(dict.fromkeys(names))
+def fmt_duration(seconds: float | int) -> str:
+    seconds = int(round(seconds))
+    h, rem = divmod(seconds, 3600)
+    m, s = divmod(rem, 60)
+    return f"{h}:{m:02d}:{s:02d}" if h else f"{m}:{s:02d}"
+
+def summarize(job: str, builds: list[BuildRuntime]) -> dict[str, Any]:
+    durations = [b.duration_seconds for b in builds]
+    return {
+        "job": job,
+        "build_count": len(builds),
+        "avg_seconds": statistics.mean(durations) if durations else None,
+        "avg": fmt_duration(statistics.mean(durations)) if durations else "N/A",
+        "runtimes": [fmt_duration(x) for x in durations],
+        "build_ids": [b.build_id for b in builds],
+        "results": [b.result for b in builds],
+        "prow_urls": [b.prow_url for b in builds],
+    }
+
+def print_markdown(rows: list[dict[str, Any]]) -> None:
+    print("| Rank | Job | Avg | Last runtimes | Build IDs | Results |")
+    print("|---:|---|---:|---|---|---|")
+    for i, row in enumerate(rows, 1):
+        print(
+            f"| {i} | `{row['job']}` | {row['avg']} | "
+            f"{', '.join(row['runtimes']) or 'N/A'} | "
+            f"{', '.join(row['build_ids']) or 'N/A'} | "
+            f"{', '.join(row['results']) or 'N/A'} |"
+        )
+
+def print_csv(rows: list[dict[str, Any]]) -> None:
+    writer = csv.DictWriter(
+        sys.stdout,
+        fieldnames=["rank", "job", "avg", "avg_seconds", "runtimes", "build_ids", "results", "prow_urls"],
+    )
+    writer.writeheader()
+    for i, row in enumerate(rows, 1):
+        writer.writerow({
+            "rank": i,
+            "job": row["job"],
+            "avg": row["avg"],
+            "avg_seconds": row["avg_seconds"],
+            "runtimes": ";".join(row["runtimes"]),
+            "build_ids": ";".join(row["build_ids"]),
+            "results": ";".join(row["results"]),
+            "prow_urls": ";".join(row["prow_urls"]),
+        })
+
+def main() -> int:
+    parser = argparse.ArgumentParser(
+        epilog=(
+            "Example:\n"
+            "  python prow_runtimes.py --kueue-periodics --limit 5 --only-success\n\n"
+            "The command is particularly useful for:\n"
+            "  (1) determining the longest taking CI jobs as targets for optimization,\n"
+            "  (2) setting up the thresholds for CI performance jobs."
+        ),
+        formatter_class=argparse.RawDescriptionHelpFormatter,
+    )
+    group = parser.add_mutually_exclusive_group(required=True)
+    group.add_argument("--job", action="append", help="Job name, logs/... prefix, gs:// prefix, or storage URL. Repeatable.")
+    group.add_argument("--kueue-periodics", action="store_true", help="Read all periodic-kueue* jobs from kueue-periodics-main.yaml.")
+    parser.add_argument("--bucket", default=DEFAULT_BUCKET)
+    parser.add_argument("--limit", type=int, default=5)
+    parser.add_argument("--max-to-check", type=int, default=200)
+    parser.add_argument("--only-success", action="store_true")
+    parser.add_argument("--json", action="store_true")
+    parser.add_argument("--csv", action="store_true")
+    parser.add_argument("--verbose", "-v", action="store_true", help="Enable verbose output (prints raw data from URLs).")
+
+    if len(sys.argv) == 1:
+        parser.print_help()
+        return 0
+
+    args = parser.parse_args()
+
+    jobs = load_kueue_periodic_jobs(verbose=args.verbose) if args.kueue_periodics else args.job
+    rows = []
+
+    for job in jobs:
+        if args.verbose:
+            print("checking: " + job, file=sys.stderr)
```

  I also tested `./hack/tools/prow-runtimes/prow_runtimes.py --kueue-periodics --limit 5 --only-success --json --verbose > out.json`. It works!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:17:02Z

- **hack/tools/prow-runtimes/prow_runtimes.py:3** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:17:02Z
```diff
@@ -0,0 +1,307 @@
+"""
+Copyright The Kubernetes Authors.
```

  Good point! We probably need to remove the year from the copyright in a follow-up PR.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:19:00Z

- **hack/tools/prow-runtimes/prow_runtimes.py:191** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-11T17:19:00Z
```diff
@@ -0,0 +1,307 @@
+#!/usr/bin/env python3
+"""
+Copyright The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+"""
+
+from __future__ import annotations
+
+import argparse
+import csv
+import datetime as dt
+import json
+import re
+import statistics
+import sys
+import time
+import urllib.error
+import urllib.parse
+import urllib.request
+from dataclasses import dataclass
+from typing import Any, Iterable
+
+DEFAULT_BUCKET = "kubernetes-ci-logs"
+KUEUE_PERIODICS_URL = (
+    "https://raw.githubusercontent.com/kubernetes/test-infra/master/config/jobs/kubernetes-sigs/kueue/kueue-periodics-main.yaml"
+)
+
+@dataclass(frozen=True)
+class BuildRuntime:
+    job: str
+    build_id: str
+    result: str
+    start: dt.datetime
+    finish: dt.datetime
+    duration_seconds: int
+    prow_url: str
+
+def http_get(url: str, *, tries: int = 4, timeout: int = 30) -> bytes:
+    last_exc: Exception | None = None
+    for attempt in range(tries):
+        try:
+            req = urllib.request.Request(url, headers={"User-Agent": "kueue-prow-runtime/1.0"})
+            with urllib.request.urlopen(req, timeout=timeout) as resp:
+                return resp.read()
+        except urllib.error.HTTPError as e:
+            if e.code == 404:
+                raise
+            last_exc = e
+        except urllib.error.URLError as e:
+            last_exc = e
+        time.sleep(0.5 * (2**attempt))
+    raise last_exc  # type: ignore[misc]
+
+def http_json(url: str) -> dict[str, Any]:
+    return json.loads(http_get(url).decode("utf-8"))
+
+def parse_gs_prefix(job_or_prefix: str, bucket: str = DEFAULT_BUCKET) -> tuple[str, str, str]:
+    value = job_or_prefix.strip().rstrip("/")
+
+    if value.startswith("gs://"):
+        rest = value.removeprefix("gs://")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("https://storage.googleapis.com/"):
+        rest = value.removeprefix("https://storage.googleapis.com/").rstrip("/")
+        bucket_name, _, path = rest.partition("/")
+        return path.rstrip("/").split("/")[-1], bucket_name, path.rstrip("/")
+
+    if value.startswith("logs/") or value.startswith("pr-logs/"):
+        return value.rstrip("/").split("/")[-1], bucket, value.rstrip("/")
+
+    return value, bucket, f"logs/{value}"
+
+def object_url(bucket: str, path: str) -> str:
+    return f"https://storage.googleapis.com/{bucket}/{urllib.parse.quote(path, safe='/')}"
+
+def list_build_ids(bucket: str, prefix: str) -> list[str]:
+    ids: list[str] = []
+    page_token: str | None = None
+
+    while True:
+        params = {
+            "prefix": f"{prefix.rstrip('/')}/",
+            "delimiter": "/",
+            "fields": "nextPageToken,prefixes",
+        }
+        if page_token:
+            params["pageToken"] = page_token
+
+        url = f"https://storage.googleapis.com/storage/v1/b/{bucket}/o?{urllib.parse.urlencode(params)}"
+        data = http_json(url)
+
+        for common_prefix in data.get("prefixes", []):
+            build_id = common_prefix.rstrip("/").split("/")[-1]
+            if build_id.isdigit():
+                ids.append(build_id)
+
+        page_token = data.get("nextPageToken")
+        if not page_token:
+            break
+
+    return sorted(set(ids), key=int, reverse=True)
+
+def parse_ts(value: Any) -> dt.datetime:
+    if isinstance(value, (int, float)):
+        return dt.datetime.fromtimestamp(float(value), tz=dt.timezone.utc)
+
+    text = str(value).strip()
+    if re.fullmatch(r"\d+(\.\d+)?", text):
+        return dt.datetime.fromtimestamp(float(text), tz=dt.timezone.utc)
+
+    if text.endswith("Z"):
+        text = text[:-1] + "+00:00"
+    parsed = dt.datetime.fromisoformat(text)
+    if parsed.tzinfo is None:
+        parsed = parsed.replace(tzinfo=dt.timezone.utc)
+    return parsed.astimezone(dt.timezone.utc)
+
+def first_present(d: dict[str, Any], keys: Iterable[str]) -> Any:
+    for key in keys:
+        if key in d and d[key] not in (None, ""):
+            return d[key]
+    raise KeyError(f"none of these keys found: {list(keys)}")
+
+def read_build_runtime(job: str, bucket: str, prefix: str, build_id: str) -> BuildRuntime | None:
+    base = f"{prefix.rstrip('/')}/{build_id}"
+    try:
+        started = http_json(object_url(bucket, f"{base}/started.json"))
+        finished = http_json(object_url(bucket, f"{base}/finished.json"))
+    except urllib.error.HTTPError as e:
+        if e.code == 404:
+            return None
+        raise
+
+    start = parse_ts(first_present(started, ["timestamp", "startTime", "started"]))
+    finish = parse_ts(first_present(finished, ["timestamp", "completionTime", "finished"]))
+    seconds = int(round((finish - start).total_seconds()))
+    if seconds < 0:
+        raise ValueError(f"negative duration for {job} build {build_id}: {seconds}s")
+
+    result = str(finished.get("result") or finished.get("passed") or "unknown")
+    return BuildRuntime(
+        job=job,
+        build_id=build_id,
+        result=result,
+        start=start,
+        finish=finish,
+        duration_seconds=seconds,
+        prow_url=f"https://prow.k8s.io/view/gs/{bucket}/{base}",
+    )
+
+def last_n_builds(
+    job_or_prefix: str,
+    *,
+    limit: int,
+    bucket: str = DEFAULT_BUCKET,
+    only_success: bool = False,
+    max_to_check: int = 200,
+) -> list[BuildRuntime]:
+    job, bucket, prefix = parse_gs_prefix(job_or_prefix, bucket=bucket)
+    out: list[BuildRuntime] = []
+
+    for build_id in list_build_ids(bucket, prefix)[:max_to_check]:
+        build = read_build_runtime(job, bucket, prefix, build_id)
+        if build is None:
+            continue
+        if only_success and build.result not in {"SUCCESS", "success", "true", "True"}:
+            continue
+        out.append(build)
+        if len(out) >= limit:
+            break
+
+    return out
+
+def load_kueue_periodic_jobs() -> list[str]:
+    text = http_get(KUEUE_PERIODICS_URL).decode("utf-8")
+    print(text)
+    names = re.findall(r"(?m)^\s*-?\s+name:\s+(periodic-kueue[-A-Za-z0-9_.]+)\s*$", text)
+    print(names)
```

  Thank you!

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-11T17:33:06Z

Overall LGTM.

Just one comment https://github.com/kubernetes-sigs/kueue/pull/11124#discussion_r3220782629.
