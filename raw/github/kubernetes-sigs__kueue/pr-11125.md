# PR #11125: [ConcurrentAdmission] Do not allow migration to a flavor below the minPreferredFlavor

**Summary**: [ConcurrentAdmission] Do not allow migration to a flavor below the minPreferredFlavor

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11125

**Last updated**: 2026-05-18T15:58:04Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `ca-min-migration` → `main`
- **Created**: 2026-05-11T15:07:53Z
- **Updated**: 2026-05-18T15:58:04Z
- **Merged**: 2026-05-18T15:58:02Z
- **Closed**: 2026-05-18T15:58:02Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92)
- **Changed files**: 4   **+134 / -12**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->

#### What this PR does / why we need it:
When using the ConcurrentAdmission feature, once one Variant is running it prevents from migrating to a more favorable flavor that is below the `minPreferredFlavorName`

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-11T15:08:01Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8a34aa43394e65c91d69491a5adaa846f5590538 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0af081cdc09e00080c3e93 |

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-11T15:09:27Z

/assign @mbobrovskyi

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T15:02:34Z

LGTM label has been added.  <details>Git tree hash: 32f56e32835d05b6f9aa3282d5cdb49c24ada80d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T15:02:36Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11125#pullrequestreview-4311301340" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11125#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [PBundyra,mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-05-11T17:13:55Z

- **pkg/scheduler/scheduler.go:1186** by [@mwielgus](https://github.com/mwielgus) — 2026-05-11T17:13:36Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
```

  TBH, I have a problem understanding what is minimal here. Is it the first or last flavor? Are we sure this is the best name for that setting?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:51:40Z

- **pkg/scheduler/scheduler.go:1189** by [@mimowo](https://github.com/mimowo) — 2026-05-12T07:51:41Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	flavors := cq.ResourceGroups[0].Flavors
+	minPrefIdx := slices.Index(flavors, minPrefFlavor)
+	wlFlavorIdx, err := getFlavorIndex(&e.Info, flavors)
+	if err != nil || minPrefIdx < 0 {
+		return true
```

  Do not silent errors, propagate up, or at the very least log

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:52:37Z

- **pkg/scheduler/scheduler.go:458** by [@mimowo](https://github.com/mimowo) — 2026-05-12T07:52:37Z
```diff
@@ -455,6 +455,9 @@ func (s *Scheduler) processEntry(
 
 	if features.Enabled(features.ConcurrentAdmission) && concurrentadmission.IsVariant(e.Obj) {
 		if lessFavorableSibling := s.findAdmittedLessFavorableSibling(&e.Info, snapshot); lessFavorableSibling != nil {
+			if !s.isMigrationAllowed(cq, e, log) {
```

  This function does not use `lessFavorableSibling` as an argument, so why don't we check it before calling `findAdmittedLessFavorableSibling`?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-12T07:54:56Z

- **pkg/scheduler/scheduler.go:1187** by [@mimowo](https://github.com/mimowo) — 2026-05-12T07:54:56Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	flavors := cq.ResourceGroups[0].Flavors
+	minPrefIdx := slices.Index(flavors, minPrefFlavor)
+	wlFlavorIdx, err := getFlavorIndex(&e.Info, flavors)
```

  nit, this is preexisting, but let's rename it from `getFlavorIndex` to `resolveFlavorIndex` to reflect this is not a simple "get"

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T15:10:19Z

- **pkg/scheduler/scheduler.go:1186** by [@mimowo](https://github.com/mimowo) — 2026-05-15T15:10:19Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
```

  I guess this is a good question but is not really relevant for the implementation PR as the API was already reviewed. We may open separate discussion on improving the API naming, but I don't consider this blocking for this PR. Still, if we think this is important to fix, then let's do it before 0.18 is released which is soon.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-05-15T15:30:15Z

- **pkg/scheduler/scheduler.go:1186** by [@mwielgus](https://github.com/mwielgus) — 2026-05-15T15:30:14Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
```

  This is somewhat relevant as the PR is precisely about minPreferredFlavor and if the name is misleading that the whole mechanics of the PR may be wrong.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T15:37:16Z

- **pkg/scheduler/scheduler.go:1186** by [@mimowo](https://github.com/mimowo) — 2026-05-15T15:37:16Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
```

  Maybe, but in that case i would suggest a small kep update PR first to align on the name. Im open to better suggestions

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:32:41Z

- **pkg/scheduler/scheduler.go:1189** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:32:41Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	flavors := cq.ResourceGroups[0].Flavors
+	minPrefIdx := slices.Index(flavors, minPrefFlavor)
+	wlFlavorIdx, err := getFlavorIndex(&e.Info, flavors)
+	if err != nil || minPrefIdx < 0 {
+		return true
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:32:54Z

- **pkg/scheduler/scheduler.go:458** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:32:53Z
```diff
@@ -455,6 +455,9 @@ func (s *Scheduler) processEntry(
 
 	if features.Enabled(features.ConcurrentAdmission) && concurrentadmission.IsVariant(e.Obj) {
 		if lessFavorableSibling := s.findAdmittedLessFavorableSibling(&e.Info, snapshot); lessFavorableSibling != nil {
+			if !s.isMigrationAllowed(cq, e, log) {
```

  ~~Done~~
  
  Edit: Reverted

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:32:59Z

- **pkg/scheduler/scheduler.go:1187** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:32:59Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
+	flavors := cq.ResourceGroups[0].Flavors
+	minPrefIdx := slices.Index(flavors, minPrefFlavor)
+	wlFlavorIdx, err := getFlavorIndex(&e.Info, flavors)
```

  Done

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T10:58:03Z

- **pkg/scheduler/scheduler.go:458** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T10:58:03Z
```diff
@@ -455,6 +455,9 @@ func (s *Scheduler) processEntry(
 
 	if features.Enabled(features.ConcurrentAdmission) && concurrentadmission.IsVariant(e.Obj) {
 		if lessFavorableSibling := s.findAdmittedLessFavorableSibling(&e.Info, snapshot); lessFavorableSibling != nil {
+			if !s.isMigrationAllowed(cq, e, log) {
```

  Even we don't use `lessFavorableSibling` as an argument, we still use the information that it exists. We only want to check if the Variant is below `minPreferredFlavor` is we know that there's a running Sibling

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T15:00:39Z

- **pkg/scheduler/scheduler.go:1186** by [@mimowo](https://github.com/mimowo) — 2026-05-18T15:00:39Z
```diff
@@ -1174,6 +1177,25 @@ func (s *Scheduler) findAdmittedSiblingMatching(wl *workload.Info, cq *schdcache
 	return nil
 }
 
+func (s *Scheduler) isMigrationAllowed(cq *schdcache.ClusterQueueSnapshot, e *entry, log klog.Logger) bool {
+	if cq.ConcurrentAdmissionPolicy == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints == nil || cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName == nil {
+		return true
+	}
+	minPrefFlavor := *cq.ConcurrentAdmissionPolicy.Migration.Constraints.MinPreferredFlavorName
```

  I had a sync with @PBundyra and @mwielgus and we will try to rename the field in a follow up PR, the currently leading idea is `lastAcceptableFlavor`, cc @tenzen-y

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T15:02:25Z

/lgtm
/approve
Let's merge the PR as it fixes an important gap in the mechanism. The naming issue (raised in the [comment](https://github.com/kubernetes-sigs/kueue/pull/11125/changes#r3220780904)) will be addressed as a separate PR along with the KEP update.
