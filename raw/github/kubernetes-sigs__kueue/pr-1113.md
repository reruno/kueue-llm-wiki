# PR #1113: Preemption admission check controller

**Summary**: Preemption admission check controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1113

**Last updated**: 2024-04-18T14:48:37Z

---

## Metadata

- **State**: closed
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `preemption-check-controller` → `main`
- **Created**: 2023-09-12T14:58:04Z
- **Updated**: 2024-04-18T14:48:37Z
- **Merged**: —
- **Closed**: 2024-04-18T14:48:34Z
- **Labels**: `release-note`, `size/XXL`, `needs-rebase`, `do-not-merge/hold`, `tide/merge-method-rebase`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 29   **+1335 / -83**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Add the possibility to delay the eviction triggering in case of preemption based on  the preempting workload admission checks configuration.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1677

#### Special notes for your reviewer:

Some additional test will be added.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Manage the eviction on preemption based on admission checks.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-12T14:58:06Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-09-12T14:58:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 371072a4272eaf8a2e6e8b59cad319663608c097 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/652785db451b7100086f382e |

### Comment by [@trasc](https://github.com/trasc) — 2023-09-15T11:04:14Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-16T07:15:07Z

/assign

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-18T07:24:42Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1113#" title="Author self-approved">trasc</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [tenzen-y](https://github.com/tenzen-y). For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@trasc](https://github.com/trasc) — 2023-09-29T16:28:35Z

/label tide/merge-method-rebase
/hold
 for squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-29T16:29:40Z

I'd like to take a look at this. I hope to do so by Monday

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:56:47Z

Can you send a PR to update https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission outlining the approach?

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-20T23:30:30Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T13:31:12Z

@trasc: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-e2e-main-1-29 | 371072a4272eaf8a2e6e8b59cad319663608c097 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1113/pull-kueue-test-e2e-main-1-29/1737920176097071104) | true | `/test pull-kueue-test-e2e-main-1-29`
pull-kueue-test-multikueue-e2e-main-1-29 | 371072a4272eaf8a2e6e8b59cad319663608c097 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1113/pull-kueue-test-multikueue-e2e-main-1-29/1773315240277053440) | true | `/test pull-kueue-test-multikueue-e2e-main-1-29`
pull-kueue-test-scheduling-perf-main | 371072a4272eaf8a2e6e8b59cad319663608c097 | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1113/pull-kueue-test-scheduling-perf-main/1780952185073307648) | true | `/test pull-kueue-test-scheduling-perf-main`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=1113). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atrasc). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2024-04-18T14:48:31Z

Let's close this for now.

Given all the work we are doing to implement fair sharing preemption, it's not a good time to put this in place.

/close

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-04-18T14:48:35Z

@alculquicondor: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1113#issuecomment-2064081808):

>Let's close this for now.
>
>Given all the work we are doing to implement fair sharing preemption, it's not a good time to put this in place.
>
>/close


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T09:17:33Z

- **pkg/cache/cache.go:82** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T09:17:33Z
```diff
@@ -78,7 +79,7 @@ type Cache struct {
 	assumedWorkloads  map[string]string
 	resourceFlavors   map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
 	podsReadyTracking bool
-	admissionChecks   sets.Set[string]
+	admissionChecks   map[string]kueue.AdmissionCheckPreemptionPolicy
```

  The naming pattern in the struct seems to be driven by the value type inside the map. Rename the field to admissionChecksPreemptionPolicies (or similar) or change value type to a struct containing more information about AdmisisonChecks.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T09:20:10Z

- **pkg/cache/cache.go:224** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T09:20:09Z
```diff
@@ -220,14 +221,14 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	c.admissionChecks[ac.Name] = ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime)
```

  How can a different value than Anytime be provided?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T10:23:23Z

- **pkg/constants/constants.go:43** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T10:23:23Z
```diff
@@ -34,4 +34,6 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  Why do we need this?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T10:26:15Z

- **pkg/controller/admissionchecks/preemption/preemption.go:275** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T10:26:15Z
```diff
@@ -0,0 +1,287 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// there is noting to on worklods creation
+		return
+	}
+	if newWl == nil {
+		// deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// if quota gets released
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// is a preemption pending workload
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "AC policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run() {
+	c.log.V(2).Info("Start run")
+
+	if !c.cache.ShouldRunPreemption() {
+		// skip the snapshot creation
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove it from Snapshot
+		snapshot.RemoveWorkload(wl)
+		//2. check if preemption is needed
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		if len(needPreemption) == 0 {
+			// 2.1 - set the check to true
+			// the preemption is done , flip the condition
+			if err := c.updateFnc(c.ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// 2.2 - issue eviction
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				//2.2.1 - preemption is no longer an option, flip the condition to false
+				if err := c.updateFnc(c.ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				count, err := c.preemptor.IssuePreemptions(c.ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorklods(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
+	cond := metav1.Condition{
+		Type:    constants.PreemptionAdmissionCheckName,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.CheckStateReady, // the reason is not relevant, add this to keep it aligned wit the other checks
+		Message: "Preemption done",
```

  The code is a bit confusing, maybe consider:
  
  if successful {
  
  } else {
  
  
  }

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T10:26:52Z

- **pkg/controller/admissionchecks/preemption/preemption.go:278** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T10:26:52Z
```diff
@@ -0,0 +1,287 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// there is noting to on worklods creation
+		return
+	}
+	if newWl == nil {
+		// deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// if quota gets released
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// is a preemption pending workload
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "AC policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run() {
+	c.log.V(2).Info("Start run")
+
+	if !c.cache.ShouldRunPreemption() {
+		// skip the snapshot creation
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove it from Snapshot
+		snapshot.RemoveWorkload(wl)
+		//2. check if preemption is needed
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		if len(needPreemption) == 0 {
+			// 2.1 - set the check to true
+			// the preemption is done , flip the condition
+			if err := c.updateFnc(c.ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// 2.2 - issue eviction
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				//2.2.1 - preemption is no longer an option, flip the condition to false
+				if err := c.updateFnc(c.ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				count, err := c.preemptor.IssuePreemptions(c.ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorklods(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
```

  Successful in what?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-18T12:44:19Z

- **pkg/cache/cache.go:82** by [@trasc](https://github.com/trasc) — 2023-09-18T11:38:01Z
```diff
@@ -78,7 +79,7 @@ type Cache struct {
 	assumedWorkloads  map[string]string
 	resourceFlavors   map[kueue.ResourceFlavorReference]*kueue.ResourceFlavor
 	podsReadyTracking bool
-	admissionChecks   sets.Set[string]
+	admissionChecks   map[string]kueue.AdmissionCheckPreemptionPolicy
```

  My main problem with going with `admissionChecksPreemptionPolicies` is that it might indicate  that the map is used for existence check of the admission check.  Changed to struct.

- **pkg/cache/cache.go:224** by [@trasc](https://github.com/trasc) — 2023-09-18T12:32:02Z
```diff
@@ -220,14 +221,14 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	c.admissionChecks[ac.Name] = ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime)
```

  By API, it's part of the AdmissionCheck resource spec.

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-18T12:34:36Z
```diff
@@ -34,4 +34,6 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  I the name of the check, managed by the new controller.

- **pkg/controller/admissionchecks/preemption/preemption.go:278** by [@trasc](https://github.com/trasc) — 2023-09-18T12:38:02Z
```diff
@@ -0,0 +1,287 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// there is noting to on worklods creation
+		return
+	}
+	if newWl == nil {
+		// deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// if quota gets released
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// is a preemption pending workload
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "AC policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run() {
+	c.log.V(2).Info("Start run")
+
+	if !c.cache.ShouldRunPreemption() {
+		// skip the snapshot creation
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove it from Snapshot
+		snapshot.RemoveWorkload(wl)
+		//2. check if preemption is needed
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		if len(needPreemption) == 0 {
+			// 2.1 - set the check to true
+			// the preemption is done , flip the condition
+			if err := c.updateFnc(c.ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// 2.2 - issue eviction
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				//2.2.1 - preemption is no longer an option, flip the condition to false
+				if err := c.updateFnc(c.ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				count, err := c.preemptor.IssuePreemptions(c.ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorklods(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
```

  In doing the preemption, there might be cases when even if at the time the scheduler did the quota reservation there ware candidates for preemption which they ware replace by higher priority workloads, therefore we can no longer do the preemption. Then this happens the quota reservation is removed.

- **pkg/controller/admissionchecks/preemption/preemption.go:275** by [@trasc](https://github.com/trasc) — 2023-09-18T12:41:39Z
```diff
@@ -0,0 +1,287 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// there is noting to on worklods creation
+		return
+	}
+	if newWl == nil {
+		// deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// if quota gets released
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// is a preemption pending workload
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "AC policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run() {
+	c.log.V(2).Info("Start run")
+
+	if !c.cache.ShouldRunPreemption() {
+		// skip the snapshot creation
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove it from Snapshot
+		snapshot.RemoveWorkload(wl)
+		//2. check if preemption is needed
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		if len(needPreemption) == 0 {
+			// 2.1 - set the check to true
+			// the preemption is done , flip the condition
+			if err := c.updateFnc(c.ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// 2.2 - issue eviction
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				//2.2.1 - preemption is no longer an option, flip the condition to false
+				if err := c.updateFnc(c.ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				count, err := c.preemptor.IssuePreemptions(c.ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorklods(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
+	cond := metav1.Condition{
+		Type:    constants.PreemptionAdmissionCheckName,
+		Status:  metav1.ConditionTrue,
+		Reason:  kueue.CheckStateReady, // the reason is not relevant, add this to keep it aligned wit the other checks
+		Message: "Preemption done",
```

  done

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T13:38:52Z

- **pkg/constants/constants.go:43** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T13:38:52Z
```diff
@@ -34,4 +34,6 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  This is an exported const, should have a description and some explanation why is it needed.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T13:43:50Z

- **pkg/cache/admissioncheck.go:22** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T13:43:50Z
```diff
@@ -0,0 +1,28 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+type PreemptionPolicy int
+
+const (
+	PreemptAnytime PreemptionPolicy = iota
```

  Why not api constants?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T13:49:20Z

- **pkg/controller/admissionchecks/preemption/preemption.go:52** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T13:49:20Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
```

  Can we have some comment explaining what this controller is doing and when?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-18T13:55:57Z

- **pkg/controller/admissionchecks/preemption/preemption.go:114** by [@mwielgus](https://github.com/mwielgus) — 2023-09-18T13:55:57Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
```

  What is the point of this code? Please add some comments.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-19T08:04:32Z

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-19T06:52:22Z
```diff
@@ -34,4 +34,6 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  done

- **pkg/cache/admissioncheck.go:22** by [@trasc](https://github.com/trasc) — 2023-09-19T06:56:15Z
```diff
@@ -0,0 +1,28 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+type PreemptionPolicy int
+
+const (
+	PreemptAnytime PreemptionPolicy = iota
```

  Those are strings and since we've added this dedicated cache type, we can save some time in the comparisons we will on on them.

- **pkg/controller/admissionchecks/preemption/preemption.go:52** by [@trasc](https://github.com/trasc) — 2023-09-19T07:28:17Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:114** by [@trasc](https://github.com/trasc) — 2023-09-19T07:39:55Z
```diff
@@ -0,0 +1,289 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	c.ctx = ctx
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		if trigger && timeout {
+			c.run()
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		c.log.V(2).Info("Triggered", logValues...)
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-19T17:14:22Z

I left comments for the first pass.

- **apis/kueue/v1beta1/admissioncheck_types.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:17:10Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
 }
 
+// +kubebuilder:validation:Enum=Anytime;AfterCheckPassedOrOnDemand
+type AdmissionCheckPreemptionPolicy string
```

  ```suggestion
  type AdmissionCheckPreemptionPolicy string
  ```
  
  I don't think this will work fine.

- **apis/kueue/v1beta1/admissioncheck_types.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:18:16Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  ```suggestion
  	// +kubebuilder:default=Anytime
  	// +kubebuilder:validation:Enum=Anytime;AfterCheckPassedOrOnDemand
  	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
  ```

- **pkg/cache/cache.go:227** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:30:35Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
```

  If we use api constants instead of `iota`, we can remove this logic.

- **pkg/cache/cache.go:228** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:38:04Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
+	if val, fond := c.admissionChecks[ac.Name]; fond {
+		val.PreemptionPolicy = checkPolicy
+		c.admissionChecks[ac.Name] = val
+	} else {
+		c.admissionChecks[ac.Name] = AdmissionCheck{
+			PreemptionPolicy: checkPolicy,
+		}
+
+	}
```

  ```suggestion
  	if val, found := c.admissionChecks[ac.Name]; found {
  		val.PreemptionPolicy = checkPolicy
  		c.admissionChecks[ac.Name] = val
  	} else {
  		c.admissionChecks[ac.Name] = AdmissionCheck{
  			PreemptionPolicy: checkPolicy,
  		}
  	}
  ```

- **pkg/cache/cache.go:228** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:40:30Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
+	if val, fond := c.admissionChecks[ac.Name]; fond {
+		val.PreemptionPolicy = checkPolicy
+		c.admissionChecks[ac.Name] = val
+	} else {
+		c.admissionChecks[ac.Name] = AdmissionCheck{
+			PreemptionPolicy: checkPolicy,
+		}
+
+	}
```

  ```suggestion
  	c.admissionChecks[ac.Name] = AdmissionCheck{
  			PreemptionPolicy: checkPolicy,
  	}
  ```
  
  I think we can replace part of this with like this. Why did you add this condition?

- **pkg/cache/cache.go:738** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T08:50:05Z
```diff
@@ -721,6 +734,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldRunPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldRunPreemption() bool {
```

  ```suggestion
  // ShouldPreemptWorkloads returns true if it has workloads pending preemption now.
  func (c *Cache) ShouldPreemptWorkloads() bool {
  ```
  
  I think this name is more proper.

- **apis/kueue/v1beta1/admissioncheck_types.go:77** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T09:27:20Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
```

  ```suggestion
  	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass before issuing preemptions,
  ```

- **pkg/cache/clusterqueue.go:416** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T09:50:51Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorklods - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorklods(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
```

  Could you add unit tests?

- **pkg/cache/clusterqueue.go:428** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T16:51:08Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorklods - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorklods(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, wl := range cq.Workloads {
+		// if the workload has the preemption check set to unknown
+		if apimeta.IsStatusConditionPresentAndEqual(wl.Obj.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+			checkNow := true
+			for i := range wl.Obj.Status.AdmissionChecks {
+				c := &wl.Obj.Status.AdmissionChecks[i]
+				if c.Type == constants.PreemptionAdmissionCheckName {
+					continue
+				}
+				if acMap[c.Type].PreemptionPolicy != PreemptOnDemand {
```

  ```suggestion
  				if acMap[c.Type].PreemptionPolicy == Anytime {
  ```
  
  For more strict.

- **pkg/constants/constants.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T16:59:15Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  What happens If the external controllers use this?
  I guess the external controller hacks preemption logic and then it will allow the external controller to break preemptionPolicy.
  
  Can we restrict AdmissionChecks so that only the kueue-controller uses this name?
  
  cc: @alculquicondor

- **pkg/controller/admissionchecks/preemption/preemption.go:60** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T17:09:25Z
```diff
@@ -0,0 +1,301 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
```

  Can we avoid to have `ctx` in this `Controller`?
  
  https://pkg.go.dev/context#pkg-overview
  
  > Do not store Contexts inside a struct type; instead, pass a Context explicitly to each function that needs it. The Context should be the first parameter, typically named ctx:

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T17:13:42Z
  This controller looks like a mixed roles of controller and preemptor. So can we split out roles to [admissionController](https://github.com/kubernetes-sigs/kueue/blob/c2ff1bd87e3674e047969884dfa9dd773bfb0b0d/pkg/controller/core/admissioncheck_controller.go) and [scheduler/preemptor](https://github.com/kubernetes-sigs/kueue/blob/c2ff1bd87e3674e047969884dfa9dd773bfb0b0d/pkg/scheduler/preemption/preemption.go)?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-20T10:25:03Z

- **pkg/cache/admissioncheck.go:22** by [@mwielgus](https://github.com/mwielgus) — 2023-09-20T10:25:03Z
```diff
@@ -0,0 +1,28 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+type PreemptionPolicy int
+
+const (
+	PreemptAnytime PreemptionPolicy = iota
```

  Let's not do such optimisations. The comparison will happen only a handful times during the workload lifecycle.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-20T11:39:51Z

- **apis/kueue/v1beta1/admissioncheck_types.go:88** by [@trasc](https://github.com/trasc) — 2023-09-20T04:59:37Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
 }
 
+// +kubebuilder:validation:Enum=Anytime;AfterCheckPassedOrOnDemand
+type AdmissionCheckPreemptionPolicy string
```

  It works , check config/components/crd/bases/kueue.x-k8s.io_admissionchecks.yaml.

- **apis/kueue/v1beta1/admissioncheck_types.go:85** by [@trasc](https://github.com/trasc) — 2023-09-20T05:01:49Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  It's ok to provide the enum at type level.

- **pkg/cache/cache.go:227** by [@trasc](https://github.com/trasc) — 2023-09-20T05:05:49Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
```

  Not fully , we still need to do the deref. Also using ints will make all the future a bit operations faster.

- **pkg/controller/admissionchecks/preemption/preemption.go:60** by [@trasc](https://github.com/trasc) — 2023-09-20T05:33:58Z
```diff
@@ -0,0 +1,301 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	ctx         context.Context
```

  done

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-20T05:41:43Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  We could enforce this by webhooks on AdmissionCheck name and ClusterQueue but only to avoid name conflicts.

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@trasc](https://github.com/trasc) — 2023-09-20T05:42:31Z
  I don't think I follow. What will be the benefits?

- **pkg/cache/cache.go:228** by [@trasc](https://github.com/trasc) — 2023-09-20T05:43:42Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
+	if val, fond := c.admissionChecks[ac.Name]; fond {
+		val.PreemptionPolicy = checkPolicy
+		c.admissionChecks[ac.Name] = val
+	} else {
+		c.admissionChecks[ac.Name] = AdmissionCheck{
+			PreemptionPolicy: checkPolicy,
+		}
+
+	}
```

  done

- **pkg/cache/cache.go:228** by [@trasc](https://github.com/trasc) — 2023-09-20T05:45:23Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
+	if val, fond := c.admissionChecks[ac.Name]; fond {
+		val.PreemptionPolicy = checkPolicy
+		c.admissionChecks[ac.Name] = val
+	} else {
+		c.admissionChecks[ac.Name] = AdmissionCheck{
+			PreemptionPolicy: checkPolicy,
+		}
+
+	}
```

  removed

- **apis/kueue/v1beta1/admissioncheck_types.go:77** by [@trasc](https://github.com/trasc) — 2023-09-20T05:46:32Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
```

  done

- **pkg/cache/cache.go:738** by [@trasc](https://github.com/trasc) — 2023-09-20T05:56:36Z
```diff
@@ -721,6 +734,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldRunPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldRunPreemption() bool {
```

  Renamed to `ShouldCheckWorkloadsPreemprion`.

- **pkg/cache/clusterqueue.go:428** by [@trasc](https://github.com/trasc) — 2023-09-20T05:59:51Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorklods - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorklods(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, wl := range cq.Workloads {
+		// if the workload has the preemption check set to unknown
+		if apimeta.IsStatusConditionPresentAndEqual(wl.Obj.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+			checkNow := true
+			for i := range wl.Obj.Status.AdmissionChecks {
+				c := &wl.Obj.Status.AdmissionChecks[i]
+				if c.Type == constants.PreemptionAdmissionCheckName {
+					continue
+				}
+				if acMap[c.Type].PreemptionPolicy != PreemptOnDemand {
```

  done

- **pkg/cache/clusterqueue.go:416** by [@trasc](https://github.com/trasc) — 2023-09-20T09:56:03Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorklods - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorklods(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-20T11:43:10Z

- **pkg/cache/admissioncheck.go:22** by [@trasc](https://github.com/trasc) — 2023-09-20T11:43:10Z
```diff
@@ -0,0 +1,28 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+type PreemptionPolicy int
+
+const (
+	PreemptAnytime PreemptionPolicy = iota
```

  The checks are done for every check of all the workloads pending preemption every time a preemption run starts.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-20T11:56:50Z

- **pkg/cache/cache.go:227** by [@trasc](https://github.com/trasc) — 2023-09-20T11:56:49Z
```diff
@@ -220,14 +221,26 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	checkPolicy := PreemptAnytime
+	if ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime) == kueue.AfterCheckPassedOrOnDemand {
+		checkPolicy = PreemptOnDemand
+	}
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-20T11:57:05Z

- **pkg/cache/admissioncheck.go:22** by [@trasc](https://github.com/trasc) — 2023-09-20T11:57:05Z
```diff
@@ -0,0 +1,28 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package cache
+
+type PreemptionPolicy int
+
+const (
+	PreemptAnytime PreemptionPolicy = iota
```

  however, done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-21T06:56:43Z

- **apis/kueue/v1beta1/admissioncheck_types.go:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T06:56:43Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
 }
 
+// +kubebuilder:validation:Enum=Anytime;AfterCheckPassedOrOnDemand
+type AdmissionCheckPreemptionPolicy string
```

  Oh, I see. This marker seems to work fine.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-21T06:58:06Z

- **apis/kueue/v1beta1/admissioncheck_types.go:85** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T06:58:06Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  I prefer to put this marker here since we used to put such markers on the type level in other places.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-21T15:03:05Z

- **pkg/cache/cache.go:227** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T07:00:48Z
```diff
@@ -220,14 +221,17 @@ func (c *Cache) DeleteResourceFlavor(rf *kueue.ResourceFlavor) sets.Set[string]
 func (c *Cache) AddOrUpdateAdmissionCheck(ac *kueue.AdmissionCheck) sets.Set[string] {
 	c.Lock()
 	defer c.Unlock()
-	c.admissionChecks.Insert(ac.Name)
+	c.admissionChecks[ac.Name] = AdmissionCheck{
+		PreemptionPolicy: ptr.Deref(ac.Spec.PreemptionPolicy, kueue.Anytime),
```

  I imaged the same way as this. This way looks good.

- **pkg/cache/clusterqueue_test.go:389** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T09:13:33Z
```diff
@@ -265,3 +270,144 @@ func TestCohortCanFit(t *testing.T) {
 	}
 
 }
+
+func TestGetPreemptingWorklods(t *testing.T) {
+	cases := map[string]struct {
+		workloads        []*kueue.Workload
+		checks           map[string]AdmissionCheck
+		wantPreemptNow   []string
+		wantPreemptLater []string
+	}{
+		"empty": {},
+		"one workload no preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").Obj(),
+			},
+		},
+		"one workload preempt check pendind, preempt anytime": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.Anytime,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"one workload preempt check pendind, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptLater: []string{"ns1/wl1"},
+		},
+		"one workload check request, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"one workload check ready, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"multiple workloads": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl3", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkAnytime", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl4", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+				utiltesting.MakeWorkload("wl5", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl6", "ns1").
+					SetOrReplaceAdmissionCheck("checkAnytime", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"checkOnDemand": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+				"checkAnytime": {
+					PreemptionPolicy: kueue.Anytime,
+				},
+			},
+			wantPreemptNow:   []string{"ns1/wl1", "ns1/wl3", "ns1/wl4"},
+			wantPreemptLater: []string{"ns1/wl2", "ns1/wl5"},
+		},
```

  Adding a case that a workload has both `checkOnDemand` and `checkAnytime` admissionChecks would be better.

- **pkg/constants/constants.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T09:23:14Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  > only to avoid name conflicts.
  
  That's right. So I'm on the fence about whether or not we should have admissionCheck webhook only for this.
  
  @alculquicondor Do you think that adding webhooks is worth it? Or we should only mention this in the document?

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T10:00:53Z
  > So can we split out roles to [admissionController](https://github.com/kubernetes-sigs/kueue/blob/c2ff1bd87e3674e047969884dfa9dd773bfb0b0d/pkg/controller/core/admissioncheck_controller.go) and [scheduler/preemptor](https://github.com/kubernetes-sigs/kueue/blob/c2ff1bd87e3674e047969884dfa9dd773bfb0b0d/pkg/scheduler/preemption/preemption.go)?
  
  NVM
  
  I reconsidered why I was thinking we should split out this controller to 2 roles.
  
  I think this controller has a different concept from other controllers, such as the clusterqueue-controller. 
  So I think we should put this component outside of `/pkg/controller` like `/pkg/admissionchecker`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-21T15:40:27Z

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@trasc](https://github.com/trasc) — 2023-09-21T15:40:27Z
  I don't have a very strong opinion on this, but this is an admission check controller... (and for sure I'd prefer to not move it)
  
  @alculquicondor any opinion on this?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-21T16:11:19Z

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-21T16:11:18Z
  I actually like the current location, but I would be fine with `/pkg/admissionchecks/preemption`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-21T20:12:37Z

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-21T20:12:36Z
  While defining `controller`, I thought that the `controller` must have a reconciler. So I was thinking that putting this outside `/pkg/controller` might be better. However, @trasc and @alculquicondor seem to think it doesn't matter whether the component has a reconciler, right?
  
  If so, I'm ok with recognizing this as a controller.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-22T15:08:28Z

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@trasc](https://github.com/trasc) — 2023-09-22T15:08:28Z
  It is responding to API events and makes changes to API objects the name of the function should not matter. It's not using a reconcileer interface due to performance and potential race condition reasons. In the future other admission check controllers can use reconcile.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-22T15:10:11Z

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-22T15:10:10Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  An alternative can be to use a CEL validation rule, but it's hard to maintain the constant string and the rule in sync.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-22T20:03:59Z

- **pkg/controller/admissionchecks/preemption/preemption.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T20:03:58Z
  SGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T08:07:58Z

- **hack/update-codegen.sh:28** by [@mimowo](https://github.com/mimowo) — 2023-09-25T07:49:20Z
```diff
@@ -25,13 +25,15 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 cd $(dirname ${BASH_SOURCE[0]})/..
 
 chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-source "${CODEGEN_PKG}/generate-groups.sh" \
+chmod +x "${CODEGEN_PKG}/generate-groups.sh"
```

  Any reason to change this? If it's a fix, maybe a separate PR would be good for visibility why this is needed.

- **pkg/cache/cache.go:729** by [@mimowo](https://github.com/mimowo) — 2023-09-25T07:50:40Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
```

  ```suggestion
  func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
  ```

- **pkg/cache/cache.go:733** by [@mimowo](https://github.com/mimowo) — 2023-09-25T07:51:04Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorklods(c.admissionChecks); len(now) > 0 {
```

  ```suggestion
  		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
  ```

- **pkg/cache/cache.go:736** by [@mimowo](https://github.com/mimowo) — 2023-09-25T07:53:01Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  Locked iteration over the CQs (+inner iteration over all workloads). This is effectively iteration over all workloads in the system each time the function is called, so might be excessive. I guess we could track this information based on updates to the cache?

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T08:02:04Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  This is used as a condition type, so should be camel cased according to: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apimachinery/pkg/apis/meta/v1/types.go#L1498-L1507

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-25T09:04:52Z

- **apis/kueue/v1beta1/admissioncheck_types.go:85** by [@trasc](https://github.com/trasc) — 2023-09-25T06:03:06Z
```diff
@@ -63,8 +68,28 @@ type AdmissionCheckSpec struct {
 	// Parameters identifies the resource providing additional check parameters.
 	// +optional
 	Parameters *AdmissionCheckParametersReference `json:"parameters,omitempty"`
+
+	// preemptionPolicy determines when to issue preemptions for the Workload,
+	// if necessary, in relationship to the status of the admission check.
+	// The possible values are:
+	// - `Anytime`: No need to wait for this check to pass before issuing preemptions.
+	//   Preemptions might be blocked on the preemptionPolicy of other AdmissionChecks.
+	// - `AfterCheckPassedOrOnDemand`: Wait for this check to pass pass before issuing preemptions,
+	//   unless this or other checks requests preemptions through the Workload's admissionChecks.
+	// Defaults to `Anytime`.
+	// +optional
+	// +kubebuilder:default=Anytime
+	PreemptionPolicy *AdmissionCheckPreemptionPolicy `json:"preemptionPolicy,omitempty"`
```

  done

- **pkg/cache/clusterqueue_test.go:389** by [@trasc](https://github.com/trasc) — 2023-09-25T07:31:51Z
```diff
@@ -265,3 +270,144 @@ func TestCohortCanFit(t *testing.T) {
 	}
 
 }
+
+func TestGetPreemptingWorklods(t *testing.T) {
+	cases := map[string]struct {
+		workloads        []*kueue.Workload
+		checks           map[string]AdmissionCheck
+		wantPreemptNow   []string
+		wantPreemptLater []string
+	}{
+		"empty": {},
+		"one workload no preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").Obj(),
+			},
+		},
+		"one workload preempt check pendind, preempt anytime": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.Anytime,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"one workload preempt check pendind, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptLater: []string{"ns1/wl1"},
+		},
+		"one workload check request, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"one workload check ready, preempt on demand": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"check1": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+			},
+			wantPreemptNow: []string{"ns1/wl1"},
+		},
+		"multiple workloads": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+				utiltesting.MakeWorkload("wl2", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl3", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkAnytime", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl4", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+				utiltesting.MakeWorkload("wl5", "ns1").
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("checkOnDemand", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				utiltesting.MakeWorkload("wl6", "ns1").
+					SetOrReplaceAdmissionCheck("checkAnytime", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			checks: map[string]AdmissionCheck{
+				"checkOnDemand": {
+					PreemptionPolicy: kueue.AfterCheckPassedOrOnDemand,
+				},
+				"checkAnytime": {
+					PreemptionPolicy: kueue.Anytime,
+				},
+			},
+			wantPreemptNow:   []string{"ns1/wl1", "ns1/wl3", "ns1/wl4"},
+			wantPreemptLater: []string{"ns1/wl2", "ns1/wl5"},
+		},
```

  done

- **hack/update-codegen.sh:28** by [@trasc](https://github.com/trasc) — 2023-09-25T08:57:32Z
```diff
@@ -25,13 +25,15 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 cd $(dirname ${BASH_SOURCE[0]})/..
 
 chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-source "${CODEGEN_PKG}/generate-groups.sh" \
+chmod +x "${CODEGEN_PKG}/generate-groups.sh"
```

  It was a separate PR and merged.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-25T09:42:02Z

- **pkg/cache/cache.go:733** by [@trasc](https://github.com/trasc) — 2023-09-25T09:17:33Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorklods(c.admissionChecks); len(now) > 0 {
```

  done

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-25T09:22:11Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  For this builtin admission-check it is doable, however other should hold the object name of the admission check name which need to be DNS subdomain.

- **pkg/cache/cache.go:736** by [@trasc](https://github.com/trasc) — 2023-09-25T09:29:47Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  The problem with this is that it will get triggered a lot more (at least for workload and admission check updates), an depending on the current queue state and the change taking place we might need to do the same iteration.
  
  On the plus side for the current implementation, the number of calls is limited to the number of preemption runs, on average we should not have more that 2/sec, while only holding the read lock and it will return on the first queue that needs preemption.

- **pkg/cache/cache.go:729** by [@trasc](https://github.com/trasc) — 2023-09-25T09:32:35Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T10:33:38Z

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T10:33:38Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  It would be good o be compatible with k8s recommendation. Is there a way? Maybe we could have a DNS name & condition type name per admission check declared at the cluster queue level? wdyt @mwielgus @trasc ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T10:41:52Z

- **pkg/cache/cache.go:736** by [@mimowo](https://github.com/mimowo) — 2023-09-25T10:41:51Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  > The problem with this is that it will get triggered a lot more (at least for workload and admission check updates), an depending on the current queue state and the change taking place we might need to do the same iteration.
  
   We already keep track and take lock for workload updates in cache in `cache.UpdateWorkload`. This is the strategy we use to keep the `podsReady` condition in-sync. 
  
  > On the plus side for the current implementation, the number of calls is limited to the number of preemption runs, on average we should not have more that 2/sec, while only holding the read lock and it will return on the first queue that needs preemption.
  
  I see, but I'm concern with the worst case scenario of iteration over all workloads. There might be users where it counts in millions (EDIT: millions might be too much, but hundred thousand at least). FYI @alculquicondor, @mwielgus ?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-25T11:14:50Z

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-25T11:14:50Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  To follow the proposed pattern ` (dns1123SubdomainFmt/)?(qualifiedNameFmt)` we can append something like `/CheckCondition` to the admission check name, however for me it looks a bit "artificial" and require  additional
  strings manipulation with no added value.
  
  An alternative could to have a custom Condition type,  maybe we can look at this in a followup.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T11:42:36Z

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T11:42:36Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  > To follow the proposed pattern  (dns1123SubdomainFmt/)?(qualifiedNameFmt) we can append something like /CheckCondition to the admission check name, however for me it looks a bit "artificial" and require additional
  strings manipulation with no added value.
  
  Agree, I'm not a big fun of artificial constructs. IMO it's fine as is, if there is no good option otherwise. Is there a way providing a mapping between the names, or lowercasing?
  
  > An alternative could to have a custom Condition type, maybe we can look at this in a followup.
  
  If we plan for follow up, it would mean changing the API, and the conditions might be used by external controllers, IIUC, so it will be costly,

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T11:43:30Z

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T11:43:30Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  Could you point out in code where the name used as a DNS domain name?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-25T12:32:42Z

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-25T12:32:42Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  The `Type` of the conditions is extracted from the list of checks associated with a clusterQueue, which is a list of `AdmissionChecks` identified by their names (kubernetes resource name).

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T13:20:03Z

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T13:20:03Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  I see, would it work if clusterqueue spec, instead of `[]string` contained `[]AdmissionCheckInfo`, and each admission check info contained two fields `conditionType` and `name`. Then, name could be "some-check", while the condition "SomeCheck"? Still, it seems, that for PreemptionAdmissionCheckName in this PR we don't need mapping, do we? @mwielgus wdyt?

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-25T13:31:14Z

- **pkg/constants/constants.go:43** by [@mwielgus](https://github.com/mwielgus) — 2023-09-25T13:31:14Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  I don't think that we should create yet another mapping in clusterqueue spec. Maybe keeping the same name everywhere is not so bad idea :) ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T13:33:25Z

- **pkg/constants/constants.go:43** by [@mimowo](https://github.com/mimowo) — 2023-09-25T13:33:25Z
```diff
@@ -37,4 +37,8 @@ const (
 
 	WorkloadPriorityClassSource = "kueue.x-k8s.io/workloadpriorityclass"
 	PodPriorityClassSource      = "scheduling.k8s.io/priorityclass"
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  sgtm

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-26T08:54:16Z

- **pkg/cache/cache.go:736** by [@mwielgus](https://github.com/mwielgus) — 2023-09-26T08:54:16Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  Hundred thousand is also a bit of a stretch at this moment. Realistically, I would rather expect  small two digit thousand workloads being queued at  the same time.  
  
  Obviously the loop over all workloads could be improved and with the proper workload status update handling - completely avoided. The question is - does it matter enough at this moment. With the 2 stage admission being quite complex, i would rather suggest documenting all non-critical bottlenecks with:
  
  * how bad they are
  * how often they are triggered and how can we limit them
  * what happens if they are triggered
  * how can they be fixed
  
  instead of trying to fix them all, in one uber-complex PR.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-26T08:57:18Z

- **pkg/cache/cache.go:735** by [@mwielgus](https://github.com/mwielgus) — 2023-09-26T08:57:18Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  Can we do it only in the queue has any admissionchecks configured? If yes, that could reduce the number of workloads to be checked to only those using 2stage admission.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T09:49:19Z

- **pkg/cache/cache.go:735** by [@trasc](https://github.com/trasc) — 2023-09-26T09:49:19Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  Unfortunately no, we'll need to keep 2 ways of managing preemption in-place, also the list of checks of a cluster-queue could be changed at any point.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-26T10:50:54Z

- **pkg/cache/cache.go:736** by [@mimowo](https://github.com/mimowo) — 2023-09-26T10:50:54Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  I'm fine with just documenting (probably a good comment in code would suffice) the potential bottlenecks. Especially, as the code makes the fact we iterate over all workloads hidden, as the loops are on different levels.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T11:07:37Z

- **pkg/cache/cache.go:736** by [@trasc](https://github.com/trasc) — 2023-09-26T11:07:37Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-28T04:11:40Z

- **pkg/constants/constants.go:43** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T01:41:20Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  > An alternative can be to use a CEL validation rule, but it's hard to maintain the constant string and the rule in sync.
  
  We already have the same literal in constant string, CRD validation, and defaulting. So, I think that the concern is too late now.

- **pkg/controller/admissionchecks/preemption/preemption.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T01:43:49Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
```

  Can we support the leader election?

- **pkg/controller/admissionchecks/preemption/preemption.go:168** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:00:51Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
```

  ```suggestion
  		// 1. remove the workload from the snapshot
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:173** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:00:59Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
```

  ```suggestion
  		// 2. check if preemption is sill needed
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:132** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:01:34Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
```

  ```suggestion
  			c.trigger("event", "Workload with reservation was deleted", "workload", klog.KObj(oldWl))
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:139** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:01:53Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
```

  ```suggestion
  		c.trigger("event", "Quota released", "workload", klog.KObj(oldWl))
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:02:05Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
```

  ```suggestion
  		c.trigger("event", "Pending workload updated", "workload", klog.KObj(oldWl))
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:152** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:22:38Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
```

  ```suggestion
  		c.trigger("event", "Admission check preemption policy changed", "admissionCheck", klog.KObj(oldAc))
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:200** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:39:37Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
```

  ```suggestion
  				c.trigger("retryUpdate")
  ```
  
  `workload` has already been added in L172.

- **pkg/controller/admissionchecks/preemption/preemption.go:214** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:40:04Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
```

  ```suggestion
  					c.trigger("retryUpdate")
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:225** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:40:16Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
```

  ```suggestion
  					c.trigger("retryEviction")
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:290** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:41:30Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
+	cond := metav1.Condition{
+		Type: constants.PreemptionAdmissionCheckName,
+	}
+
+	if successful {
+		cond.Status = metav1.ConditionTrue
+		cond.Reason = kueue.CheckStateReady
+		cond.Message = "Preemption done"
+
```

  ```suggestion
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:267** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T02:51:52Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
```

  Move `cache.FlavorResourceQuantities` to workload pkg to avoid circular reference. Then, can we also move this function to workload pkg?

- **pkg/controller/admissionchecks/preemption/preemption.go:265** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:08:05Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
```

  When the `flvQuotas.Resources[rName]` is nil, a nil pointer error would happen, right?

- **pkg/controller/admissionchecks/preemption/preemption.go:266** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:09:39Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
```

  ```suggestion
  				exceedsLimit := cqResUsage[rName]+rReq > limit
  ```
  The `limit` means `nominal+borrowingLimit` here.

- **pkg/controller/admissionchecks/preemption/preemption.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:18:27Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
```

  This function triggers preemption based only on the quotaReservation condition.
  If the workload is already admitted, does the controller need to trigger preemption logic?

- **pkg/controller/admissionchecks/preemption/preemption.go:151** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:24:59Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
```

  ```suggestion
  	if oldAc != nil && newAc != nil && !ptr.Equal(oldAc.Spec.PreemptionPolicy, newAc.Spec.PreemptionPolicy) {
  ```

- **pkg/controller/admissionchecks/preemption/preemption_test.go:50** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:26:03Z
```diff
@@ -0,0 +1,379 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+type preemptionUpdateRecord struct {
+	Workload string
+	State    bool
+}
+
+const (
+	testingNamespace  = "ns1"
+	testingCQName     = "cq1"
+	testingFlavorName = "rf1"
+)
+
+func TestOneRun(t *testing.T) {
+
```

  ```suggestion
  ```

- **pkg/controller/admissionchecks/preemption/preemption_test.go:337** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:32:17Z
```diff
@@ -0,0 +1,379 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+type preemptionUpdateRecord struct {
+	Workload string
+	State    bool
+}
+
+const (
+	testingNamespace  = "ns1"
+	testingCQName     = "cq1"
+	testingFlavorName = "rf1"
+)
+
+func TestOneRun(t *testing.T) {
+
+	rf1 := utiltesting.MakeResourceFlavor(testingFlavorName).Obj()
+
+	qCPU2b2 := utiltesting.MakeClusterQueue(testingCQName).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas(testingFlavorName).Resource(corev1.ResourceCPU, "2", "2").Obj(),
+		).
+		Preemption(kueue.ClusterQueuePreemption{
+			ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+			WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+		}).
+		Cohort("c1").
+		Obj()
+
+	resourceFlavors := []*kueue.ResourceFlavor{rf1}
+	clusterQueues := []*kueue.ClusterQueue{qCPU2b2}
+
+	otherWl := utiltesting.MakeWorkload("wl2", testingNamespace).
+		Request(corev1.ResourceCPU, "2").
+		Priority(2).
+		ReserveQuota(
+			utiltesting.MakeAdmission(testingCQName).
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: testingFlavorName,
+						},
+						ResourceUsage: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](1),
+					},
+				).
+				Obj(),
+		).
+		SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionTrue, kueue.CheckStateReady).
+		Obj()
+
+	cases := map[string]struct {
+		workloads       []*kueue.Workload
+		admissionChecks []*kueue.AdmissionCheck
+
+		wantEvents           []preemptionUpdateRecord
+		wantEvictedWorkloads []string
+	}{
+		"nothing to do": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+			},
+		},
+		"finish the preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			wantEvents: []preemptionUpdateRecord{
+				{Workload: "ns1/wl1", State: true},
+			},
+		},
+		"the preemption is no longer possible": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(1).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			wantEvents: []preemptionUpdateRecord{
+				{Workload: "ns1/wl1", State: false},
+			},
+		},
+		"the eviction is triggered": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"AfterCheckPassedOrOnDemand Pending": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+		},
+		"AfterCheckPassedOrOnDemand CheckStatePreemptionRequired": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"AfterCheckPassedOrOnDemand Ready": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"Anytime Pending": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.Anytime).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			fakeClient := utiltesting.NewFakeClient()
+			cache := cache.New(fakeClient)
+			for _, rf := range resourceFlavors {
+				cache.AddOrUpdateResourceFlavor(rf.DeepCopy())
+			}
+
+			for _, ac := range tc.admissionChecks {
+				cache.AddOrUpdateAdmissionCheck(ac.DeepCopy())
+			}
```

  ```suggestion
  			for _, ac := range tc.admissionChecks {
  				cache.AddOrUpdateAdmissionCheck(ac)
  			}
  ```
  
  I think this deecopy is no need.

- **pkg/controller/admissionchecks/preemption/preemption.go:206** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:37:36Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
```

  ```suggestion
  		// 3. add it back to the Snapshot
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:63** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T03:54:50Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
```

  ```suggestion
  	updateCheckStatus func(context.Context, client.Client, *kueue.Workload, bool) error
  ```
  
  The name of `updateFunc` is unclear what this function will do. Also, `type updateStatusFnc` is referenced only by this.

- **pkg/controller/admissionchecks/preemption/preemption.go:227** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-28T04:02:07Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
```

  Why don't we need to update the workload with the check status?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-28T04:51:20Z

- **pkg/controller/admissionchecks/preemption/preemption.go:82** by [@trasc](https://github.com/trasc) — 2023-09-28T04:51:20Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
```

  I think that happends before mgr.Start()

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-28T07:26:04Z

- **pkg/controller/admissionchecks/preemption/preemption.go:221** by [@mimowo](https://github.com/mimowo) — 2023-09-28T06:45:23Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
```

  ```suggestion
  		snapshot.RemoveWorkload(wl)
  ```

- **pkg/cache/clusterqueue_test.go:287** by [@mimowo](https://github.com/mimowo) — 2023-09-28T06:46:00Z
```diff
@@ -265,3 +270,154 @@ func TestCohortCanFit(t *testing.T) {
 	}
 
 }
+
+func TestGetPreemptingWorklods(t *testing.T) {
+	cases := map[string]struct {
+		workloads        []*kueue.Workload
+		checks           map[string]AdmissionCheck
+		wantPreemptNow   []string
+		wantPreemptLater []string
+	}{
+		"empty": {},
+		"one workload no preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").Obj(),
+			},
+		},
+		"one workload preempt check pendind, preempt anytime": {
```

  ```suggestion
  		"one workload preempt check pending, preempt anytime": {
  ```
  Same for some tests below

- **test/integration/scheduler/preemption_test.go:300** by [@mimowo](https://github.com/mimowo) — 2023-09-28T07:21:38Z
```diff
@@ -181,6 +193,147 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, wl3)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)
 		})
+
+		ginkgo.It("Should preempt immediately when anytime check is used", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkAnytime.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after succeed", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after demand", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
```

  It seems the test modified the queue shared with other tests. This can be tricky to debug or run them in parallel. Can we use a dedicated queue for this?

- **test/integration/scheduler/preemption_test.go:342** by [@mimowo](https://github.com/mimowo) — 2023-09-28T07:24:50Z
```diff
@@ -181,6 +193,147 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, wl3)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)
 		})
+
+		ginkgo.It("Should preempt immediately when anytime check is used", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkAnytime.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after succeed", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after demand", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
```

  I'm not sure I follow the test. This line suggests that the workload was preempted (evicted), while the title of the tests says "Should not preempt immediately". Was it triggered by `util.SetWorkloadsAdmissionCkeck`? If so, I would suggest adding `ginkgo.By` to explain. Also, can we verify it was not preempted immediately as the title suggests?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-28T09:17:18Z

- **pkg/controller/admissionchecks/preemption/preemption.go:63** by [@trasc](https://github.com/trasc) — 2023-09-28T05:21:11Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:121** by [@trasc](https://github.com/trasc) — 2023-09-28T05:29:46Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
```

  `HasQuotaReservation` checks are for the cases where quota gets released by some other workload , meaning the eviction triggered earlier (on a low priority workload) has ended or some other unrelated workload has finished its work, in either case we should check if the preemption pending workloads now fit.

- **pkg/controller/admissionchecks/preemption/preemption.go:132** by [@trasc](https://github.com/trasc) — 2023-09-28T05:30:27Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:139** by [@trasc](https://github.com/trasc) — 2023-09-28T05:30:42Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:144** by [@trasc](https://github.com/trasc) — 2023-09-28T05:31:01Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:151** by [@trasc](https://github.com/trasc) — 2023-09-28T05:33:54Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:152** by [@trasc](https://github.com/trasc) — 2023-09-28T05:34:23Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:168** by [@trasc](https://github.com/trasc) — 2023-09-28T05:34:46Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:173** by [@trasc](https://github.com/trasc) — 2023-09-28T05:35:07Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:200** by [@trasc](https://github.com/trasc) — 2023-09-28T05:37:50Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
```

  trigger will use another log object.

- **pkg/controller/admissionchecks/preemption/preemption.go:214** by [@trasc](https://github.com/trasc) — 2023-09-28T05:39:00Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
```

  trigger will use another log object.

- **pkg/controller/admissionchecks/preemption/preemption.go:225** by [@trasc](https://github.com/trasc) — 2023-09-28T05:39:17Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
```

  trigger will use another log object.

- **pkg/controller/admissionchecks/preemption/preemption.go:227** by [@trasc](https://github.com/trasc) — 2023-09-28T05:41:22Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
```

  We need to wait for the quota to actually be released, (the evicted workloads to stop).

- **pkg/controller/admissionchecks/preemption/preemption.go:206** by [@trasc](https://github.com/trasc) — 2023-09-28T05:41:43Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:267** by [@trasc](https://github.com/trasc) — 2023-09-28T05:44:50Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
```

  `cache.FlavorResourceQuantities` is nmoved in workload by #849, however even after that I don't see a reason to move this and make it public. ... Maybe in a follow-up.

- **pkg/controller/admissionchecks/preemption/preemption.go:290** by [@trasc](https://github.com/trasc) — 2023-09-28T08:22:38Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
+				exceedsBorrowing := cq.Cohort != nil && cohortResUsage[rName]+rReq > cohortResRequestable[rName]
+				if exceedsNominal || exceedsBorrowing {
+					if _, found := ret[flvQuotas.Name]; !found {
+						ret[flvQuotas.Name] = sets.New(rName)
+					} else {
+						ret[flvQuotas.Name].Insert(rName)
+					}
+				}
+			}
+		}
+	}
+	return ret
+}
+
+func updateCheckStatus(ctx context.Context, c client.Client, wl *kueue.Workload, successful bool) error {
+	cond := metav1.Condition{
+		Type: constants.PreemptionAdmissionCheckName,
+	}
+
+	if successful {
+		cond.Status = metav1.ConditionTrue
+		cond.Reason = kueue.CheckStateReady
+		cond.Message = "Preemption done"
+
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:265** by [@trasc](https://github.com/trasc) — 2023-09-28T08:33:47Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:266** by [@trasc](https://github.com/trasc) — 2023-09-28T09:07:35Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
+
+func resourcesNeedingPreemption(wl *workload.Info, usage cache.FlavorResourceQuantities, snap *cache.Snapshot) preemption.ResourcesPerFlavor {
+	ret := make(preemption.ResourcesPerFlavor)
+
+	cq := snap.ClusterQueues[wl.ClusterQueue]
+	for _, rg := range cq.ResourceGroups {
+		for _, flvQuotas := range rg.Flavors {
+			flvReq, found := usage[flvQuotas.Name]
+			if !found {
+				// Workload doesn't request this flavor.
+				continue
+			}
+			cqResUsage := cq.Usage[flvQuotas.Name]
+			var cohortResUsage, cohortResRequestable map[corev1.ResourceName]int64
+			if cq.Cohort != nil {
+				cohortResUsage = cq.Cohort.Usage[flvQuotas.Name]
+				cohortResRequestable = cq.Cohort.RequestableResources[flvQuotas.Name]
+			}
+			for rName, rReq := range flvReq {
+				limit := flvQuotas.Resources[rName].Nominal
+				if flvQuotas.Resources[rName].BorrowingLimit != nil {
+					limit += *flvQuotas.Resources[rName].BorrowingLimit
+				}
+				exceedsNominal := cqResUsage[rName]+rReq > limit
```

  done

- **pkg/controller/admissionchecks/preemption/preemption_test.go:50** by [@trasc](https://github.com/trasc) — 2023-09-28T09:08:15Z
```diff
@@ -0,0 +1,379 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+type preemptionUpdateRecord struct {
+	Workload string
+	State    bool
+}
+
+const (
+	testingNamespace  = "ns1"
+	testingCQName     = "cq1"
+	testingFlavorName = "rf1"
+)
+
+func TestOneRun(t *testing.T) {
+
```

  done

- **pkg/controller/admissionchecks/preemption/preemption_test.go:337** by [@trasc](https://github.com/trasc) — 2023-09-28T09:09:31Z
```diff
@@ -0,0 +1,379 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	corev1 "k8s.io/api/core/v1"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+type preemptionUpdateRecord struct {
+	Workload string
+	State    bool
+}
+
+const (
+	testingNamespace  = "ns1"
+	testingCQName     = "cq1"
+	testingFlavorName = "rf1"
+)
+
+func TestOneRun(t *testing.T) {
+
+	rf1 := utiltesting.MakeResourceFlavor(testingFlavorName).Obj()
+
+	qCPU2b2 := utiltesting.MakeClusterQueue(testingCQName).
+		ResourceGroup(
+			*utiltesting.MakeFlavorQuotas(testingFlavorName).Resource(corev1.ResourceCPU, "2", "2").Obj(),
+		).
+		Preemption(kueue.ClusterQueuePreemption{
+			ReclaimWithinCohort: kueue.PreemptionPolicyAny,
+			WithinClusterQueue:  kueue.PreemptionPolicyLowerPriority,
+		}).
+		Cohort("c1").
+		Obj()
+
+	resourceFlavors := []*kueue.ResourceFlavor{rf1}
+	clusterQueues := []*kueue.ClusterQueue{qCPU2b2}
+
+	otherWl := utiltesting.MakeWorkload("wl2", testingNamespace).
+		Request(corev1.ResourceCPU, "2").
+		Priority(2).
+		ReserveQuota(
+			utiltesting.MakeAdmission(testingCQName).
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "main",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: testingFlavorName,
+						},
+						ResourceUsage: corev1.ResourceList{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](1),
+					},
+				).
+				Obj(),
+		).
+		SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionTrue, kueue.CheckStateReady).
+		Obj()
+
+	cases := map[string]struct {
+		workloads       []*kueue.Workload
+		admissionChecks []*kueue.AdmissionCheck
+
+		wantEvents           []preemptionUpdateRecord
+		wantEvictedWorkloads []string
+	}{
+		"nothing to do": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+			},
+		},
+		"finish the preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+			},
+			wantEvents: []preemptionUpdateRecord{
+				{Workload: "ns1/wl1", State: true},
+			},
+		},
+		"the preemption is no longer possible": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(1).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			wantEvents: []preemptionUpdateRecord{
+				{Workload: "ns1/wl1", State: false},
+			},
+		},
+		"the eviction is triggered": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"AfterCheckPassedOrOnDemand Pending": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+		},
+		"AfterCheckPassedOrOnDemand CheckStatePreemptionRequired": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"AfterCheckPassedOrOnDemand Ready": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionTrue, kueue.CheckStateReady).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.AfterCheckPassedOrOnDemand).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+		"Anytime Pending": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", testingNamespace).
+					Request(corev1.ResourceCPU, "1").
+					Priority(3).
+					ReserveQuota(
+						utiltesting.MakeAdmission(testingCQName).
+							PodSets(
+								kueue.PodSetAssignment{
+									Name: "main",
+									Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+										corev1.ResourceCPU: testingFlavorName,
+									},
+									ResourceUsage: corev1.ResourceList{
+										corev1.ResourceCPU: resource.MustParse("1"),
+									},
+									Count: ptr.To[int32](1),
+								},
+							).
+							Obj(),
+					).
+					SetOrReplaceAdmissionCheck(constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown, kueue.CheckStatePending).
+					SetOrReplaceAdmissionCheck("check1", metav1.ConditionUnknown, kueue.CheckStatePending).
+					Obj(),
+				otherWl.DeepCopy(),
+			},
+			admissionChecks: []*kueue.AdmissionCheck{
+				utiltesting.MakeAdmissionCheck("check1").Policy(kueue.Anytime).Obj(),
+			},
+			wantEvictedWorkloads: []string{"ns1/wl2"},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			ctx, _ := utiltesting.ContextWithLog(t)
+			fakeClient := utiltesting.NewFakeClient()
+			cache := cache.New(fakeClient)
+			for _, rf := range resourceFlavors {
+				cache.AddOrUpdateResourceFlavor(rf.DeepCopy())
+			}
+
+			for _, ac := range tc.admissionChecks {
+				cache.AddOrUpdateAdmissionCheck(ac.DeepCopy())
+			}
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-28T10:31:04Z

- **pkg/cache/clusterqueue_test.go:287** by [@trasc](https://github.com/trasc) — 2023-09-28T09:57:53Z
```diff
@@ -265,3 +270,154 @@ func TestCohortCanFit(t *testing.T) {
 	}
 
 }
+
+func TestGetPreemptingWorklods(t *testing.T) {
+	cases := map[string]struct {
+		workloads        []*kueue.Workload
+		checks           map[string]AdmissionCheck
+		wantPreemptNow   []string
+		wantPreemptLater []string
+	}{
+		"empty": {},
+		"one workload no preemption": {
+			workloads: []*kueue.Workload{
+				utiltesting.MakeWorkload("wl1", "ns1").Obj(),
+			},
+		},
+		"one workload preempt check pendind, preempt anytime": {
```

  done

- **pkg/constants/constants.go:43** by [@trasc](https://github.com/trasc) — 2023-09-28T09:58:41Z
```diff
@@ -34,4 +34,8 @@ const (
 	// that do not specify any priority class and there is no priority class
 	// marked as default.
 	DefaultPriority = 0
+
+	// PreemptionAdmissionCheckName is the name of the builtin preemption
+	// admission check controller.
+	PreemptionAdmissionCheckName = "kueue-preemption"
```

  done

- **pkg/controller/admissionchecks/preemption/preemption.go:221** by [@trasc](https://github.com/trasc) — 2023-09-28T09:59:40Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
```

  done

- **test/integration/scheduler/preemption_test.go:300** by [@trasc](https://github.com/trasc) — 2023-09-28T10:22:01Z
```diff
@@ -181,6 +193,147 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, wl3)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)
 		})
+
+		ginkgo.It("Should preempt immediately when anytime check is used", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkAnytime.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after succeed", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after demand", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
```

  cq is created (both as go object and as kubernetes object) before each test and deleted after. This is the way most of the integrations tests are done. 
  
  This should be save according to [ginkgo doc](https://onsi.github.io/ginkgo/#spec-parallelization)

- **test/integration/scheduler/preemption_test.go:342** by [@trasc](https://github.com/trasc) — 2023-09-28T10:28:44Z
```diff
@@ -181,6 +193,147 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, wl3)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)
 		})
+
+		ginkgo.It("Should preempt immediately when anytime check is used", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkAnytime.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after succeed", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after demand", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionUnknown, kueue.CheckStatePreemptionRequired)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-29T16:20:56Z

- **test/integration/scheduler/preemption_test.go:300** by [@mimowo](https://github.com/mimowo) — 2023-09-29T16:20:56Z
```diff
@@ -181,6 +193,147 @@ var _ = ginkgo.Describe("Preemption", func() {
 			util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, wl3)
 			util.ExpectWorkloadsToBePending(ctx, k8sClient, wl4)
 		})
+
+		ginkgo.It("Should preempt immediately when anytime check is used", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkAnytime.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkAnytime.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after succeed", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			lowWl := testing.MakeWorkload("low-wl", ns.Name).
+				Queue(q.Name).
+				Priority(lowPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+
+			ginkgo.By("Creating the first workload wait its admission", func() {
+				gomega.Expect(k8sClient.Create(ctx, lowWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, lowWl)
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, lowWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, lowWl)
+			})
+
+			highWl := testing.MakeWorkload("high-wl", ns.Name).
+				Queue(q.Name).
+				Priority(highPriority).
+				Request(corev1.ResourceCPU, "4").
+				Obj()
+			ginkgo.By("Creating a high priority Workload", func() {
+				gomega.Expect(k8sClient.Create(ctx, highWl)).To(gomega.Succeed())
+				util.ExpectWorkloadsToHaveQuotaReservation(ctx, k8sClient, cq.Name, highWl)
+				gomega.Consistently(func() bool {
+					readWl := &kueue.Workload{}
+					err := k8sClient.Get(ctx, client.ObjectKeyFromObject(lowWl), readWl)
+					if err != nil {
+						return false
+					}
+					return apimeta.IsStatusConditionTrue(readWl.Status.Conditions, kueue.WorkloadAdmitted)
+
+				}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+				util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, highWl, checkOnDemand.Name, metav1.ConditionTrue, kueue.CheckStateReady)
+				util.FinishEvictionForWorkloads(ctx, k8sClient, lowWl)
+				util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, cq.Name, highWl)
+			})
+		})
+
+		ginkgo.It("Should not preempt immediately when on-demand check is used, after demand", func() {
+			ginkgo.By("Adding the check to the ClusterQueue", func() {
+				cqKey := client.ObjectKeyFromObject(cq)
+				gomega.Eventually(func() error {
+					updatedQueue := &kueue.ClusterQueue{}
+					err := k8sClient.Get(ctx, cqKey, updatedQueue)
+					if err != nil {
+						return err
+					}
+					updatedQueue.Spec.AdmissionChecks = []string{checkOnDemand.Name}
+					return k8sClient.Update(ctx, updatedQueue)
```

  yes, this is all good then

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-02T19:59:24Z

short pass on the first files

- **apis/kueue/v1beta1/clusterqueue_types.go:94** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T19:48:35Z
```diff
@@ -91,6 +91,7 @@ type ClusterQueueSpec struct {
 
 	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
 	// +optional
+	// +kubebuilder:validation:XValidation:rule="!('kueue-preemption' in self)",message="Admission name 'kueue-preemption' is reserved for internal kueue use"
```

  Let's not use CEL yet. Write it in the webhook.

- **pkg/cache/cache.go:736** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T19:56:46Z
```diff
@@ -721,6 +725,18 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemprion returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemprion() bool {
+	c.RLock()
+	defer c.RUnlock()
+	for _, cq := range c.clusterQueues {
```

  Please document in an issue, and reference back here.

- **pkg/cache/cache.go:729** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T19:57:52Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
```

  ```suggestion
  func (c *Cache) HasPreemptingWorkloads() bool {
  ```

- **pkg/cache/cache.go:735** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T19:58:07Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	// Depending on the overall state this may end up iterating over all the workloads present
+	// in the cache (workloads having QuotaReservation).
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  ```suggestion
  		if cq.HasPreemptingWorkloads(c.admissionChecks) {
  ```
  
  Even if the logic looks similar, returning true/false would be much faster.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-03T10:49:00Z

- **apis/kueue/v1beta1/clusterqueue_types.go:94** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T06:59:39Z
```diff
@@ -91,6 +91,7 @@ type ClusterQueueSpec struct {
 
 	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
 	// +optional
+	// +kubebuilder:validation:XValidation:rule="!('kueue-preemption' in self)",message="Admission name 'kueue-preemption' is reserved for internal kueue use"
```

  ```suggestion
  	// +kubebuilder:validation:XValidation:rule="!('kueue-preemption' in self)",message="Admission name 'kueue-preemption' is reserved for internal kueue use."
  ```

- **pkg/controller/admissionchecks/preemption/preemption.go:82** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T07:58:55Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
```

  I meant that can `Controller` implement the `LeaderElectionRunnable` interface?

- **pkg/controller/admissionchecks/preemption/preemption.go:200** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T08:01:51Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
```

  Ah, it's correct. Thanks.

- **pkg/controller/admissionchecks/preemption/preemption.go:267** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-29T08:11:35Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
+				}
+			}
+		}
+		// 3 add it back to the Snapshot
+		snapshot.AddWorkload(wl)
+	}
+}
+
+func filterWorkloads(snapsot *cache.Snapshot) []*workload.Info {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, cq := range snapsot.ClusterQueues {
+		now, later := cq.GetPreemptingWorkloads(snapsot.AdmissionChecks)
+		preemptNow = append(preemptNow, now...)
+		preemptLater = append(preemptLater, later...)
+	}
+
+	// remove the workloads that should not evaluated now to limit the number of evictions
+	// needed at the current point in time
+	for _, wl := range preemptLater {
+		snapsot.RemoveWorkload(wl)
+	}
+	return preemptNow
+}
+
+func totalRequestsForWorkload(wl *workload.Info) cache.FlavorResourceQuantities {
+	usage := make(cache.FlavorResourceQuantities)
+	for _, ps := range wl.TotalRequests {
+		for res, q := range ps.Requests {
+			flv := ps.Flavors[res]
+			resUsage := usage[flv]
+			if resUsage == nil {
+				resUsage = make(map[corev1.ResourceName]int64)
+				usage[flv] = resUsage
+			}
+			resUsage[res] += q
+		}
+	}
+	return usage
+}
```

  SGTM
  
  I commented about whether moving `cache.FlavorResourceQuantities` to workload pkg is still needed at #849.
  If it is needed, let's work on that in a follow-up. Thanks.

- **pkg/controller/admissionchecks/preemption/preemption.go:121** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T10:43:39Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
```

  That makes sense. Maybe we can reduce the number of triggering `run` by calling the `cache.ShouldCheckWorkloadsPreemption` here. However, the `cache.ShouldCheckWorkloadsPreemption` is expensive.
  
  So, the current implementation might be better.

- **pkg/controller/admissionchecks/preemption/preemption.go:227** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T10:48:45Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
```

  I see. So when is the check status updated? Do we expect the updates when triggered next time?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T11:34:50Z

- **pkg/cache/cache.go:729** by [@trasc](https://github.com/trasc) — 2023-10-03T11:34:50Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
```

  The main issue with this naming is that depending on the policy, we have workloads needing preemption now and needing preemption later.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-03T11:36:52Z

- **pkg/scheduler/scheduler.go:222** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T11:25:18Z
```diff
@@ -204,18 +204,12 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		ctx := ctrl.LoggerInto(ctx, log)
 		if e.assignment.RepresentativeMode() != flavorassigner.Fit {
 			if len(e.preemptionTargets) != 0 {
-				preempted, err := s.preemptor.IssuePreemptions(ctx, e.preemptionTargets, cq)
-				if err != nil {
-					log.Error(err, "Failed to preempt workloads")
-				}
-				if preempted != 0 {
-					e.inadmissibleMsg += fmt.Sprintf(". Pending the preemption of %d workload(s)", preempted)
-					e.requeueReason = queue.RequeueReasonPendingPreemption
-				}
+				e.inadmissibleMsg += fmt.Sprintf(". May require the preemption of %d workload(s)", len(e.preemptionTargets))
+				e.requeueReason = queue.RequeueReasonPendingPreemption
 			} else {
 				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionReclaimWithinCohort", cq.Preemption.ReclaimWithinCohort, "preemptionWithinClusterQueue", cq.Preemption.WithinClusterQueue)
+				continue
 			}
-			continue
```

  Why do we need to admit workloads if there are preemption targets?
  This means why do we need to move `continue` to L211?

- **pkg/scheduler/scheduler_test.go:188** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T11:25:52Z
```diff
@@ -185,8 +185,8 @@ func TestSchedule(t *testing.T) {
 		wantLeft map[string]sets.Set[string]
 		// wantInadmissibleLeft is the workload keys that are left in the inadmissible state after this cycle.
 		wantInadmissibleLeft map[string]sets.Set[string]
-		// wantPreempted is the keys of the workloads that get preempted in the scheduling cycle.
-		wantPreempted sets.Set[string]
+		// wantReservedPendingPreemption is the set wof workloads expected to have quota reserved and pending preemption
```

  ```suggestion
  		// wantReservedPendingPreemption is the set of workloads expected to have quota reserved and pending preemption
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1152** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T11:36:13Z
```diff
@@ -1145,7 +1145,7 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
 		gomega.Expect(k8sClient.Create(ctx, highPriorityClass))
 		ginkgo.DeferCleanup(func() {
-			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Eventually(func() error { return k8sClient.Delete(ctx, highPriorityClass) }, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  Why do we need this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T11:50:24Z

- **pkg/controller/admissionchecks/preemption/preemption.go:82** by [@trasc](https://github.com/trasc) — 2023-10-03T11:50:24Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
```

  It looks like unless we need some "dynamic" need for leader election the behavior is the same.
  
  https://github.com/kubernetes-sigs/controller-runtime/blob/c93e2abcb28eb71fccad7dc565f0547cc07e5566/pkg/manager/runnable_group.go#L55-L73

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T11:52:55Z

- **pkg/controller/admissionchecks/preemption/preemption.go:227** by [@trasc](https://github.com/trasc) — 2023-10-03T11:52:55Z
```diff
@@ -0,0 +1,300 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package preemption
+
+import (
+	"context"
+	"time"
+
+	"github.com/go-logr/logr"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/util/sets"
+	"k8s.io/client-go/tools/record"
+	"k8s.io/klog/v2"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/cache"
+	"sigs.k8s.io/kueue/pkg/constants"
+	"sigs.k8s.io/kueue/pkg/scheduler/preemption"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	controllerName  = "PreemptionController"
+	throttleTimeout = 500 * time.Millisecond
+)
+
+type updateStatusFnc func(context.Context, client.Client, *kueue.Workload, bool) error
+
+// Controller manages the preemption process of the workloads after quota reservation taking into account
+// its admission checks states. It will issue eviction for other workloads, and mark the preemption as
+// successful or not based on the current state of the resources pool. Check run() for more details.
+// The controller will react to workload and admission check changes, check NotifyWorkloadUpdate()
+// and NotifyAdmissionCheckUpdate() for details.
+type Controller struct {
+	log         logr.Logger
+	cache       *cache.Cache
+	client      client.Client
+	recorder    record.EventRecorder
+	preemptor   *preemption.Preemptor
+	triggerChan chan struct{}
+
+	// stub, only for test
+	updateFnc updateStatusFnc
+}
+
+func NewController(cache *cache.Cache) *Controller {
+	return &Controller{
+		log:   ctrl.Log.WithName(controllerName),
+		cache: cache,
+		// keep this buffered so we can "requeue" in case of API errors
+		triggerChan: make(chan struct{}, 1),
+		updateFnc:   updateCheckStatus,
+	}
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	c.client = mgr.GetClient()
+	c.recorder = mgr.GetEventRecorderFor(controllerName)
+	c.preemptor = preemption.New(c.client, c.recorder)
+	return mgr.Add(c)
+}
+
+var _ manager.Runnable = (*Controller)(nil)
+
+func (c *Controller) Start(ctx context.Context) error {
+	c.log.V(5).Info("Staring main loop")
+	ctx = ctrl.LoggerInto(ctx, c.log)
+	ticker := time.NewTicker(throttleTimeout)
+	trigger := false
+	timeout := false
+	for {
+		select {
+		case <-c.triggerChan:
+			trigger = true
+		case <-ticker.C:
+			timeout = true
+		case <-ctx.Done():
+			c.log.V(5).Info("End main loop")
+			return nil
+		}
+
+		// If a run was triggered and at least throttle timeout has passed.
+		if trigger && timeout {
+			c.run(ctx)
+			trigger = false
+			timeout = false
+		}
+	}
+}
+
+// trigger - triggers a controller run if not already pending.
+func (c *Controller) trigger(logValues ...interface{}) {
+	select {
+	case c.triggerChan <- struct{}{}:
+		// The run was triggered.
+		c.log.V(2).Info("Triggered", logValues...)
+	default:
+		// The channel is already full, a run in already pending.
+	}
+
+}
+
+func (c *Controller) NotifyWorkloadUpdate(oldWl, newWl *kueue.Workload) {
+	if oldWl == nil {
+		// There is noting to on workloads creation.
+		return
+	}
+	if newWl == nil {
+		// Deleting a workload that reserves quota can speed up the
+		// transition of preemption pending workloads.
+		if workload.HasQuotaReservation(oldWl) {
+			c.trigger("event", "Workload with reservation was deleted", "wl", klog.KObj(oldWl))
+		}
+		return
+	}
+
+	// If quota gets released.
+	if workload.HasQuotaReservation(oldWl) && !workload.HasQuotaReservation(newWl) {
+		c.trigger("event", "Quota released", "wl", klog.KObj(oldWl))
+	}
+
+	// Is a preemption pending workload.
+	if apimeta.IsStatusConditionPresentAndEqual(newWl.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+		c.trigger("event", "Pending workload updated", "wl", klog.KObj(oldWl))
+	}
+}
+
+func (c *Controller) NotifyAdmissionCheckUpdate(oldAc, newAc *kueue.AdmissionCheck) {
+	// Adding or removing a check will lead to a workload update later on
+	// we should ignore those events here.
+	if oldAc != nil && newAc != nil && ptr.Deref(oldAc.Spec.PreemptionPolicy, "") != ptr.Deref(newAc.Spec.PreemptionPolicy, "") {
+		c.trigger("event", "Admission check preemption policy changed", "ac", klog.KObj(oldAc))
+	}
+}
+
+func (c *Controller) run(ctx context.Context) {
+	c.log.V(2).Info("Start run")
+
+	// If there is nothing to do at this point.
+	if !c.cache.ShouldCheckWorkloadsPreemption() {
+		// skip the snapshot creation and exit
+		return
+	}
+
+	snapshot := c.cache.Snapshot()
+	workloads := filterWorkloads(&snapshot)
+	for _, wl := range workloads {
+		//1. remove the workload from the snapshot
+		snapshot.RemoveWorkload(wl)
+		usage := totalRequestsForWorkload(wl)
+		needPreemption := resourcesNeedingPreemption(wl, usage, &snapshot)
+		log := c.log.WithValues("workload", klog.KObj(wl.Obj))
+		//2. check if preemption is sill needed
+		if len(needPreemption) == 0 {
+			// No more resources need to be freed to accommodate this workload.
+			// The preemption is successful.
+			if err := c.updateFnc(ctx, c.client, wl.Obj, true); err != nil {
+				log.V(2).Error(err, "Unable to update the check state to True")
+				c.trigger("retryUpdate", klog.KObj(wl.Obj))
+			} else {
+				log.V(2).Info("Preemption ended")
+			}
+		} else {
+			// Additional resources need to be freed.
+			targets := c.preemptor.GetTargetsForResources(wl, needPreemption, usage, &snapshot)
+			if len(targets) == 0 {
+				// There are no candidates for eviction, the preemption can no linger be done.
+				// The preemption failed.
+				if err := c.updateFnc(ctx, c.client, wl.Obj, false); err != nil {
+					log.V(2).Error(err, "Unable to update the check state to False")
+					c.trigger("retryUpdate", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption is no longer possible")
+				}
+			} else {
+				// Issue the evictions, the controller will run again once the evicted workloads are fully stopped.
+				count, err := c.preemptor.IssuePreemptions(ctx, targets, snapshot.ClusterQueues[wl.ClusterQueue])
+				if err != nil {
+					log.V(2).Error(err, "Unable to issue preemption")
+					c.trigger("retryEviction", klog.KObj(wl.Obj))
+				} else {
+					log.V(2).Info("Preemption triggered", "count", count)
```

  yes, when the preemptee finishes execution,  we'll  check the fit an update the state accordingly.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T12:12:48Z

- **pkg/scheduler/scheduler.go:222** by [@trasc](https://github.com/trasc) — 2023-10-03T12:12:48Z
```diff
@@ -204,18 +204,12 @@ func (s *Scheduler) schedule(ctx context.Context) {
 		ctx := ctrl.LoggerInto(ctx, log)
 		if e.assignment.RepresentativeMode() != flavorassigner.Fit {
 			if len(e.preemptionTargets) != 0 {
-				preempted, err := s.preemptor.IssuePreemptions(ctx, e.preemptionTargets, cq)
-				if err != nil {
-					log.Error(err, "Failed to preempt workloads")
-				}
-				if preempted != 0 {
-					e.inadmissibleMsg += fmt.Sprintf(". Pending the preemption of %d workload(s)", preempted)
-					e.requeueReason = queue.RequeueReasonPendingPreemption
-				}
+				e.inadmissibleMsg += fmt.Sprintf(". May require the preemption of %d workload(s)", len(e.preemptionTargets))
+				e.requeueReason = queue.RequeueReasonPendingPreemption
 			} else {
 				log.V(2).Info("Workload requires preemption, but there are no candidate workloads allowed for preemption", "preemptionReclaimWithinCohort", cq.Preemption.ReclaimWithinCohort, "preemptionWithinClusterQueue", cq.Preemption.WithinClusterQueue)
+				continue
 			}
-			continue
```

  We don't fully admit them, we reserve the quota only. Previously we wold have keep the workload at the head of the queue until it wold have fit with no extra eviction needed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T13:17:13Z

- **test/integration/controller/jobs/job/job_controller_test.go:1152** by [@trasc](https://github.com/trasc) — 2023-10-03T13:17:13Z
```diff
@@ -1145,7 +1145,7 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		highPriorityClass := testing.MakePriorityClass("high").PriorityValue(100).Obj()
 		gomega.Expect(k8sClient.Create(ctx, highPriorityClass))
 		ginkgo.DeferCleanup(func() {
-			gomega.Expect(k8sClient.Delete(ctx, highPriorityClass)).To(gomega.Succeed())
+			gomega.Eventually(func() error { return k8sClient.Delete(ctx, highPriorityClass) }, util.Timeout, util.Interval).Should(gomega.Succeed())
```

  I saw a flaky behavior on this on during my local test that was caused by client throttling and  there  is no legitimate expectation from our side to be able to delete that priority class on the first try.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-03T13:49:49Z

- **apis/kueue/v1beta1/clusterqueue_types.go:94** by [@trasc](https://github.com/trasc) — 2023-10-03T13:41:24Z
```diff
@@ -91,6 +91,7 @@ type ClusterQueueSpec struct {
 
 	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
 	// +optional
+	// +kubebuilder:validation:XValidation:rule="!('kueue-preemption' in self)",message="Admission name 'kueue-preemption' is reserved for internal kueue use"
```

  removed

- **apis/kueue/v1beta1/clusterqueue_types.go:94** by [@trasc](https://github.com/trasc) — 2023-10-03T13:41:31Z
```diff
@@ -91,6 +91,7 @@ type ClusterQueueSpec struct {
 
 	// admissionChecks lists the AdmissionChecks required by this ClusterQueue
 	// +optional
+	// +kubebuilder:validation:XValidation:rule="!('kueue-preemption' in self)",message="Admission name 'kueue-preemption' is reserved for internal kueue use"
```

  done

- **pkg/cache/cache.go:735** by [@trasc](https://github.com/trasc) — 2023-10-03T13:43:33Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	// Depending on the overall state this may end up iterating over all the workloads present
+	// in the cache (workloads having QuotaReservation).
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  same as L729

- **pkg/scheduler/scheduler_test.go:188** by [@trasc](https://github.com/trasc) — 2023-10-03T13:46:45Z
```diff
@@ -185,8 +185,8 @@ func TestSchedule(t *testing.T) {
 		wantLeft map[string]sets.Set[string]
 		// wantInadmissibleLeft is the workload keys that are left in the inadmissible state after this cycle.
 		wantInadmissibleLeft map[string]sets.Set[string]
-		// wantPreempted is the keys of the workloads that get preempted in the scheduling cycle.
-		wantPreempted sets.Set[string]
+		// wantReservedPendingPreemption is the set wof workloads expected to have quota reserved and pending preemption
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-03T14:45:39Z

Overall this change is highly disruptive, so it should warrant a KEP, or at least a detailed PR description.

What are the semantics of the "preemption" admission check? What happens if it itself has a PreemptionPolicy?

Would it make more sense for Preemption to be just a controller (instead of an admission check)?

- **pkg/cache/clusterqueue.go:416** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:32:53Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
```

  ```suggestion
  func (cq *ClusterQueue) PreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
  ```

- **pkg/cache/clusterqueue.go:418** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:33:44Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
```

  ```suggestion
  	var preemptNow, preemptLater []*workload.Info
  ```

- **pkg/cache/cache.go:729** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:35:09Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
```

  ok, but the name should reflect what this function calculates, not what this is used for.

- **pkg/cache/cache.go:735** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:35:43Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	// Depending on the overall state this may end up iterating over all the workloads present
+	// in the cache (workloads having QuotaReservation).
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  ok, but make it return true/false, instead of a list that is not used.

- **pkg/cache/clusterqueue.go:428** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-03T14:41:19Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, wl := range cq.Workloads {
+		// if the workload has the preemption check set to unknown
+		if apimeta.IsStatusConditionPresentAndEqual(wl.Obj.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+			checkNow := true
+			for i := range wl.Obj.Status.AdmissionChecks {
+				c := &wl.Obj.Status.AdmissionChecks[i]
+				if c.Type == constants.PreemptionAdmissionCheckName {
+					continue
+				}
+				if acMap[c.Type].PreemptionPolicy == kueue.Anytime {
```

  guard against the case where the check doesn't exist (it could have been mistyped).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T13:00:12Z

- **pkg/cache/clusterqueue.go:416** by [@trasc](https://github.com/trasc) — 2023-10-04T11:40:27Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
```

  done

- **pkg/cache/clusterqueue.go:418** by [@trasc](https://github.com/trasc) — 2023-10-04T11:40:46Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
```

  done

- **pkg/cache/clusterqueue.go:428** by [@trasc](https://github.com/trasc) — 2023-10-04T11:42:08Z
```diff
@@ -408,3 +409,35 @@ func workloadBelongsToLocalQueue(wl *kueue.Workload, q *kueue.LocalQueue) bool {
 func reportAdmittedActiveWorkloads(cqName string, val int) {
 	metrics.AdmittedActiveWorkloads.WithLabelValues(cqName).Set(float64(val))
 }
+
+// GetPreemptingWorkloads - returns two list of Workloads
+// the first one contains the workloads that are pending preemption and should be evaluated now
+// the second one contains the workloads that are pending preemption and should be evaluated later
+func (cq *ClusterQueue) GetPreemptingWorkloads(acMap map[string]AdmissionCheck) ([]*workload.Info, []*workload.Info) {
+	preemptNow := []*workload.Info{}
+	preemptLater := []*workload.Info{}
+	for _, wl := range cq.Workloads {
+		// if the workload has the preemption check set to unknown
+		if apimeta.IsStatusConditionPresentAndEqual(wl.Obj.Status.AdmissionChecks, constants.PreemptionAdmissionCheckName, metav1.ConditionUnknown) {
+			checkNow := true
+			for i := range wl.Obj.Status.AdmissionChecks {
+				c := &wl.Obj.Status.AdmissionChecks[i]
+				if c.Type == constants.PreemptionAdmissionCheckName {
+					continue
+				}
+				if acMap[c.Type].PreemptionPolicy == kueue.Anytime {
```

  done

- **pkg/cache/cache.go:735** by [@trasc](https://github.com/trasc) — 2023-10-04T11:46:03Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
+	c.RLock()
+	defer c.RUnlock()
+	// Depending on the overall state this may end up iterating over all the workloads present
+	// in the cache (workloads having QuotaReservation).
+	for _, cq := range c.clusterQueues {
+		if now, _ := cq.GetPreemptingWorkloads(c.admissionChecks); len(now) > 0 {
```

  `GetPreemptingWorkloads` is also used in `pkg/controller/admissionchecks/preemption/preemption.go:L211 `

- **pkg/cache/cache.go:729** by [@trasc](https://github.com/trasc) — 2023-10-04T11:48:24Z
```diff
@@ -721,6 +725,20 @@ func (c *Cache) MatchingClusterQueues(nsLabels map[string]string) sets.Set[strin
 	return cqs
 }
 
+// ShouldCheckWorkloadsPreemption returns true if it has workloads pending preemption now.
+func (c *Cache) ShouldCheckWorkloadsPreemption() bool {
```

  `HasWorkloadsPreemptingNow` , done
