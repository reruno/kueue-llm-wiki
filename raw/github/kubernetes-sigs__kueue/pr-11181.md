# PR #11181: Fix finished Workload GC with finalizers

**Summary**: Fix finished Workload GC with finalizers

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11181

**Last updated**: 2026-05-15T03:23:36Z

---

## Metadata

- **State**: open
- **Author**: [@ShaanveerS](https://github.com/ShaanveerS)
- **Branch**: `gc-wl` → `main`
- **Created**: 2026-05-14T09:34:58Z
- **Updated**: 2026-05-15T03:23:36Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/bug`, `release-note`, `size/M`, `cncf-cla: yes`
- **Assignees**: _none_
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 5   **+69 / -20**

## Description

#### What type of PR is this?
/kind bug

#### What this PR does / why we need it:
When finished Workload retention expires, if the Workload still has the `resource-in-use` finalizer attached, Kubernetes marks it for deletion but won't actually remove it.

This was already called out as a caveat in [KEP-1618](https://github.com/kubernetes-sigs/kueue/blob/main/keps/1618-optional-gc-of-workloads/README.md#L108-L115):

> The deletion mechanism assumes that finished Workload objects do not have any finalizers attached to them. After an investigation, the [exact place in the code](https://github.com/kubernetes-sigs/kueue/blob/a128ae3354a56670ffa58695fef1eca270fe3a47/pkg/controller/jobframework/reconciler.go#L332-L340) was found that removes the resource-in-use finalizer when the Workload state transitions to finished. If that behavior ever changes, or if there is an external source of a finalizer being attached to the Workload, it will be marked for deletion by the Kubernetes client but will not actually be deleted until the finalizer is removed.

WorkloadSlices were added after the KEP. 
Replaced WorkloadSlices are marked finished by the scheduler in [`replaceWorkloadSlice`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/scheduler/scheduler.go#L1047-L1057), and that path only calls [`workload.Finish`](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/workload/workload.go#L1709-L1720), which sets the finished condition but does not remove the finalizer.

The fix is to remove it in the finished Workload GC path, so the Workload can actually be removed once retention has elapsed.
#### Which issue(s) this PR fixes:
Fixes #11130

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
```release-note
Fix a bug where finished Workloads could remain stuck after object retention deletion if they still had Kueue's resource-in-use finalizer.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-14T09:35:04Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2b33d9015d1c9ae5db8ced64edab1d90bf8a19e5 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0691aa4b2df50008b9ec1f |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-14T09:35:06Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11181#" title="Author self-approved">ShaanveerS</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T12:39:35Z

the failed test seems another recurrence of https://github.com/kubernetes-sigs/kueue/issues/10298

## Reviews

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-05-14T09:43:35Z

- **pkg/controller/core/workload_controller.go:267** by [@mwielgus](https://github.com/mwielgus) — 2026-05-14T09:43:21Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
```

  This is getting more complex and tricky. Please add comments here and above explaining what is being done and why.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:45:41Z

- **pkg/controller/core/workload_controller.go:277** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:45:41Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		if !wl.DeletionTimestamp.IsZero() {
+			return ctrl.Result{}, nil
+		}
+
 		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
 		if err := r.client.Delete(ctx, &wl); err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
```

  We already have this pattern in a couple of places where we remove finalizer, then delete a workload, check if there is potential to comminize to avoid bugs in the future

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T09:46:43Z

- **pkg/controller/core/workload_controller.go:270** by [@mimowo](https://github.com/mimowo) — 2026-05-14T09:46:43Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		if !wl.DeletionTimestamp.IsZero() {
```

  IIUC this is just an optimization to skip setting the deletion timestamp if already done? I'm ok-ish with that but we should at least log at v(3) that we skip the deletion becuase already in progress.

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T10:26:20Z

- **pkg/controller/core/workload_controller.go:270** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T10:26:20Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		if !wl.DeletionTimestamp.IsZero() {
```

  Done!

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T10:26:25Z

- **pkg/controller/core/workload_controller.go:267** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T10:26:25Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
```

  Done!

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T10:32:38Z

- **pkg/controller/core/workload_controller.go:277** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T10:32:38Z
```diff
@@ -264,11 +264,18 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
+		if err := workload.RemoveFinalizer(ctx, r.client, &wl); err != nil {
+			return ctrl.Result{}, client.IgnoreNotFound(err)
+		}
+		if !wl.DeletionTimestamp.IsZero() {
+			return ctrl.Result{}, nil
+		}
+
 		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
 		if err := r.client.Delete(ctx, &wl); err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
```

  added workload.Delete helper. Thanks!

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T11:09:12Z

- **pkg/workload/workload.go:1559** by [@mimowo](https://github.com/mimowo) — 2026-05-14T11:09:12Z
```diff
@@ -1550,6 +1550,24 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	return nil
 }
 
+// Delete removes Kueue's resource-in-use finalizer before deleting the Workload.
+// If deletion is already in progress, finalizer removal is enough for Kubernetes
+// to continue object cleanup, so Delete returns false without issuing another
+// delete request. The returned bool reports whether this call successfully
+// requested deletion.
+func Delete(ctx context.Context, c client.Client, wl *kueue.Workload) (bool, error) {
+	if err := RemoveFinalizer(ctx, c, wl); err != nil {
```

  Handle the NotFound error gracefully - this means already deleted, so return (true, nil)

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T11:11:06Z

- **pkg/workload/workload.go:1559** by [@mimowo](https://github.com/mimowo) — 2026-05-14T11:11:06Z
```diff
@@ -1550,6 +1550,24 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	return nil
 }
 
+// Delete removes Kueue's resource-in-use finalizer before deleting the Workload.
+// If deletion is already in progress, finalizer removal is enough for Kubernetes
+// to continue object cleanup, so Delete returns false without issuing another
+// delete request. The returned bool reports whether this call successfully
+// requested deletion.
+func Delete(ctx context.Context, c client.Client, wl *kueue.Workload) (bool, error) {
+	if err := RemoveFinalizer(ctx, c, wl); err != nil {
```

  Ahh, this is already handled externally, let me re-check, that could be ok too

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T11:13:28Z

- **pkg/controller/core/workload_controller.go:277** by [@mimowo](https://github.com/mimowo) — 2026-05-14T11:13:28Z
```diff
@@ -264,10 +264,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
-		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			log.V(3).Info("Skipping workload delete because deletion is already in progress")
```

  I would rather push this log down inside "delete" so we don't need to handle here

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T12:08:12Z

- **pkg/controller/core/workload_controller.go:277** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T12:08:12Z
```diff
@@ -264,10 +264,21 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
-		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			log.V(3).Info("Skipping workload delete because deletion is already in progress")
```

  Agreed, done!

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T12:08:53Z

- **pkg/workload/workload.go:1559** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T12:08:52Z
```diff
@@ -1550,6 +1550,24 @@ func RemoveFinalizer(ctx context.Context, c client.Client, wl *kueue.Workload) e
 	return nil
 }
 
+// Delete removes Kueue's resource-in-use finalizer before deleting the Workload.
+// If deletion is already in progress, finalizer removal is enough for Kubernetes
+// to continue object cleanup, so Delete returns false without issuing another
+// delete request. The returned bool reports whether this call successfully
+// requested deletion.
+func Delete(ctx context.Context, c client.Client, wl *kueue.Workload) (bool, error) {
+	if err := RemoveFinalizer(ctx, c, wl); err != nil {
```

  Yeah it was because each caller already handles NotFound in its own way: 
  workload GC ignores it, jobframework ignores it but only records an event when delete succeeds, and LWS just returns the error through its existing path.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T12:34:28Z

- **pkg/controller/core/workload_controller.go:280** by [@mimowo](https://github.com/mimowo) — 2026-05-14T12:34:28Z
```diff
@@ -264,10 +264,20 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
-		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			return ctrl.Result{}, nil
+		}
+
+		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
```

  Can we move the comment above - where it was as it is saying "deleting" . We will also have an event after the operation anyway.

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T12:39:27Z

- **pkg/controller/core/workload_controller.go:280** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T12:39:27Z
```diff
@@ -264,10 +264,20 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 			return ctrl.Result{RequeueAfter: remainingTime}, nil
 		}
 
-		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			return ctrl.Result{}, nil
+		}
+
+		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
```

  done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T12:42:40Z

- **pkg/controller/core/workload_controller_test.go:2727** by [@mimowo](https://github.com/mimowo) — 2026-05-14T12:42:41Z
```diff
@@ -2724,6 +2724,40 @@ func TestReconcile(t *testing.T) {
 			wantWorkload: nil,
 			wantError:    nil,
 		},
+		"should remove finalizer from owned finished workload already being deleted, object retention configured": {
```

  One more question: why hasn't the bug surfaced earlier in the pre-existing tests that we have in test/e2e/sequential/baseline/objectretentionpolicies_test.go?

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T12:50:10Z

- **pkg/controller/core/workload_controller_test.go:2727** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T12:50:10Z
```diff
@@ -2724,6 +2724,40 @@ func TestReconcile(t *testing.T) {
 			wantWorkload: nil,
 			wantError:    nil,
 		},
+		"should remove finalizer from owned finished workload already being deleted, object retention configured": {
```

  Because those tests seems to use the path where the job reconciler already removes Kueue's finalizer when the Workload finishes whereas this bug needs a path like replaced WorkloadSlices, where we don't.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:00:18Z

- **pkg/controller/core/workload_controller.go:282** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:00:18Z
```diff
@@ -265,9 +265,20 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			return ctrl.Result{}, nil
+		}
+
 		r.recorder.Eventf(&wl, nil, corev1.EventTypeNormal, "Deleted", "Deleted", "Deleted finished workload due to elapsed retention")
```

  nit, but  let's move the record under `if deleteRequested`, becasuse the `return` statements don't differ

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:00:46Z

- **pkg/controller/core/workload_controller.go:282** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:00:46Z
```diff
@@ -265,9 +265,20 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			return ctrl.Result{}, nil
+		}
+
 		r.recorder.Eventf(&wl, nil, corev1.EventTypeNormal, "Deleted", "Deleted", "Deleted finished workload due to elapsed retention")
```

  Analogously as it is done here: https://github.com/kubernetes-sigs/kueue/pull/11181/changes#diff-552ab657667de70900b502e2617bcb405fbf4203ff08b2a35d7c87f2d637a75bR976-R980

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-14T13:02:08Z

- **pkg/controller/core/workload_controller_test.go:2727** by [@mimowo](https://github.com/mimowo) — 2026-05-14T13:02:08Z
```diff
@@ -2724,6 +2724,40 @@ func TestReconcile(t *testing.T) {
 			wantWorkload: nil,
 			wantError:    nil,
 		},
+		"should remove finalizer from owned finished workload already being deleted, object retention configured": {
```

  sgtm

### Review by [@ShaanveerS](https://github.com/ShaanveerS) (commented) — 2026-05-14T13:05:38Z

- **pkg/controller/core/workload_controller.go:282** by [@ShaanveerS](https://github.com/ShaanveerS) — 2026-05-14T13:05:38Z
```diff
@@ -265,9 +265,20 @@ func (r *WorkloadReconciler) Reconcile(ctx context.Context, req ctrl.Request) (c
 		}
 
 		log.V(2).Info("Deleting workload because it has finished and the retention period has elapsed", "retention", *r.workloadRetention.afterFinished)
-		if err := r.client.Delete(ctx, &wl); err != nil {
+
+		// Finished Workloads should no longer need Kueue's resource-in-use finalizer.
+		// However, WorkloadSlices and other paths can mark a Workload as Finished without
+		// going through the job finalization path that normally removes it. Remove only
+		// Kueue's finalizer before deleting; otherwise Kubernetes may only set
+		// deletionTimestamp and the Workload can remain stuck until the finalizer is removed.
+		deleteRequested, err := workload.Delete(ctx, r.client, &wl)
+		if err != nil {
 			return ctrl.Result{}, client.IgnoreNotFound(err)
 		}
+		if !deleteRequested {
+			return ctrl.Result{}, nil
+		}
+
 		r.recorder.Eventf(&wl, nil, corev1.EventTypeNormal, "Deleted", "Deleted", "Deleted finished workload due to elapsed retention")
```

  done
