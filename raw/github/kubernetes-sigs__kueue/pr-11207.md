# PR #11207: MultiKueue: prevent a hung remote watch from stopping all-cluster admission

**Summary**: MultiKueue: prevent a hung remote watch from stopping all-cluster admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11207

**Last updated**: 2026-05-19T17:07:46Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trilamsr](https://github.com/trilamsr)
- **Branch**: `fix/multikueue-watch-establish-timeout` → `main`
- **Created**: 2026-05-15T00:54:39Z
- **Updated**: 2026-05-19T17:07:46Z
- **Merged**: 2026-05-18T18:59:11Z
- **Closed**: 2026-05-18T18:59:11Z
- **Labels**: `kind/bug`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`, `area/multikueue`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@kshalot](https://github.com/kshalot)
- **Requested reviewers**: [@kshalot](https://github.com/kshalot), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+156 / -4**

## Description

#### What type of PR is this?

/kind bug
/area multikueue

#### What this PR does / why we need it:

A hung `client.Watch()` against one remote MultiKueueCluster could block the single `multikueuecluster` reconciler worker indefinitely, preventing every other cluster behind it from being reconciled. Those clusters keep `remoteClient.connecting=true`, the dispatcher excludes them, and admission stops cluster-wide.

This bounds the Watch establishment phase with `watchEstablishTimeout` (default 60s, package-level var overridable in tests). On timeout the in-flight Watch is canceled and `errWatchEstablishTimeout` is returned, falling back to the existing `failedConnAttempts` / `retryAfter` backoff. The successful-watch stream lifetime is unchanged — the returned watcher continues to use a context derived from the caller's `ctx`, and its cancel is owned by the watcher's `Stop()` method via a small wrapper (no goroutine leak, no lostcancel warning).

Follow-up to #9968, which fixed the most common trigger (Cloudflare Tunnel buffering empty chunked watch responses) by enabling `AllowWatchBookmarks`. This change defends against any future condition that hangs `client.Watch()` similarly.

#### Which issue(s) this PR fixes:

Fixes #11206

#### Special notes for your reviewer:

- Helper extracted as `watchWithEstablishTimeout` for direct unit testing.
- New unit test `TestWatchWithEstablishTimeout` covers three paths: hung Watch (timeout), Watch error (propagated immediately), Watch success (no timeout wait).
- Tested locally with `go test ./pkg/controller/admissionchecks/multikueue/` — all package tests pass.

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-15T00:54:46Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9bd2d79e8c7ac73aac1bb48c3b4cfe89c2a4cae7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0b4bbb4d66990008278ef5 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T00:54:50Z

Hi @trilamsr. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T03:14:04Z

/ok-to-test

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T13:15:43Z

LGTM label has been added.  <details>Git tree hash: 4f3aea30d663ca56ae7340e9771ba034777feaca</details>

### Comment by [@trilamsr](https://github.com/trilamsr) — 2026-05-15T13:25:35Z

Thanks @kshalot!
- Inline nit: `TestEstablishWatch` is now table-driven.
- e2e via `iptables -j DROP`: interesting idea, that does seem like the cleanest way to simulate an unresponsive remote apiserver

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:35:28Z

/release-note-edit
```release-note
MultiKueue: bound the establishment phase of remote watches with a 10min timeout so a hung `client.Watch()` against one remote cannot head-of-line block the multikueuecluster reconciler worker and stop admission cluster-wide.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:38:47Z

@trilamsr release note proposal:

/release-note-edit
```release-note
MultiKueue: Fixed a bug where a hung watch connection to one remote cluster could block
reconciliation of other MultiKueueClusters, leaving them inactive and preventing workload
admission. Kueue now applies a 10-minute circuit-breaking timeout while establishing
remote-cluster watches, allowing reconciliation to recover instead of blocking indefinitely.
```

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:40:08Z

Thank you for fixing the issue upstream (in Kueue), nice contribution 👍 
/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T17:40:11Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11207#issuecomment-4480254881):

>Thank you for fixing the issue upstream (in Kueue), nice contribution 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T17:40:17Z

LGTM label has been added.  <details>Git tree hash: 4736e99c91362f408845898446646b17f4eda435</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T17:40:19Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11207#issuecomment-4480254881" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11207#" title="Author self-approved">trilamsr</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:40:25Z

Please also work on CPs in case the robot fails due to conflicts

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T18:59:59Z

@mimowo: new pull request created: #11298

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11207#issuecomment-4480254881):

>Thank you for fixing the issue upstream (in Kueue), nice contribution 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T19:00:36Z

@mimowo: new pull request created: #11299

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11207#issuecomment-4480254881):

>Thank you for fixing the issue upstream (in Kueue), nice contribution 👍 
>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-19T17:07:37Z

Clearing the release note as we will have a combined release note in the follow up PR: https://github.com/kubernetes-sigs/kueue/pull/11304
/release-note-edit
```release-note
NONE
```

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-15T13:15:34Z

/lgtm

I'm thinking about e2e testing this, although it might be tricky to simulate in an easy way (I was thinking of maybe using `DROP` in `iptables`).

- **pkg/controller/admissionchecks/multikueue/multikueuecluster_test.go:1164** by [@kshalot](https://github.com/kshalot) — 2026-05-15T10:02:11Z
```diff
@@ -925,3 +925,68 @@ func TestValidateKubeconfig(t *testing.T) {
 		})
 	}
 }
+
+func TestEstablishWatch(t *testing.T) {
```

  Nit: Usually in the codebase unit tests are table-driven.
  
  In this case maybe it would look something like this:
  ```go
  cases := map[string]struct {
      interceptor interceptor.Funcs
      wantErr     error
  }{ ... }
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T14:08:50Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:77** by [@mimowo](https://github.com/mimowo) — 2026-05-15T14:08:50Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
```

  Exploratory question to confirm my understanding: could this issue be also mitigated by increasing the `GroupKindConcurrency` level for MultiKueueCluster for the controller, wdyt? Code pointer to the configuration: https://github.com/kubernetes-sigs/kueue/blob/main/apis/config/v1beta2/configuration_types.go#L264C2-L264C22
  In that case when one Reconcile is stuck the other may continue

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T14:36:57Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@mimowo](https://github.com/mimowo) — 2026-05-15T14:36:57Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  I'm wondering if the timeout at 60s is safe in practice. IIUC this watch is used for all types, that includes Workloads, which might be numerous. And when Watch is opened IIUC all workloads are Listed, which may take a lot of time at the api-server side to prepare the response, in particular if the Workload objects need to go via conversion webhooks.
  
  Recently we had an issue in Kueue (single cluster) that at scale of 50k workloads when conversion webhooks are running it may take around 8min, whilst the etcd compaction timeout is only around 2.5min. The test we are working on here is to prevent that in the future: https://github.com/kubernetes-sigs/kueue/issues/9145
  
  Also, it should not be that long when conversion webhooks are not necessary.
  
  If my suspection is correct then maybe we should:
  - use increased timeout for Workload objects (a bit hacky but maybe works well in practice (say 10min)
  - use exponential timeout which is increased on retries (we already have the retry mechanism recently improved here: https://github.com/kubernetes-sigs/kueue/pull/10990

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-18T08:33:59Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@kshalot](https://github.com/kshalot) — 2026-05-18T08:33:59Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  OTOH, in this particular case, will it actually list all the workloads? I think here it will just make na HTTP call, get the headers and then the events will start streaming (IIUC it will stream an ADDED event for every resource because we are not passing any resource version in the options).
  
  AFAIR the 50k workloads issue happened because the Informer first does a List and then a Watch.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T08:38:47Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@mimowo](https://github.com/mimowo) — 2026-05-18T08:38:47Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  > OTOH, in this particular case, will it actually list all the workloads? I think here it will just make na HTTP call, get the headers and then the events will start streaming (IIUC it will stream an ADDED event for every resource because we are not passing any resource version in the options).
  
  Possible, I'm not sure about the exact semantics, if this is the case then we are good. 
  
  Let's research that more and support the claim  (that opening the watch does not entail blocking on listing the workloads) with references or experiments.

### Review by [@trilamsr](https://github.com/trilamsr) (commented) — 2026-05-18T17:09:21Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@trilamsr](https://github.com/trilamsr) — 2026-05-18T17:09:21Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  > Let's research that more…
  
  Did some digging. TLDR: @kshalot is right on the client side, @mimowo is right on the server side, and the server side is the one that bites.
  
  **Client side**: traced controller-runtime → client-go → `newStreamWatcher`. `c.Watch()` returns as soon as HTTP 200 headers arrive; there's no client-side `List`, no full-body buffering. So the 50k-workloads Informer scenario doesn't transfer directly. (Happy to drop in file:line refs if useful.)
  
  **Server side**: `c.Watch()` still blocks until the apiserver *sends* those headers — and that's exactly the case @mimowo flagged. From [kubernetes/kubernetes#136950](https://github.com/kubernetes/kubernetes/issues/136950):
  
  > If the API server does not have a "warm" cache […] before returning to Kueue it tries to convert them sequentially to v1beta2 […] estimated to take around 8min.
  
  With 60s we'd cancel + retry on every cycle and never get past warmup, arguably worse than the pre-PR "wait forever, eventually succeed" for that path.
  
  **Proposal**: bump `defaultWatchEstablishTimeout` to **10 min** in this PR (matching @mimowo's "say 10min"). Tiny diff, preserves the hung-remote fix, avoids the cold-start regression.
  
  For the more refined version — exponential timeout on retries that stacks with the `failedConnAttempts` / `retryAfter` machinery from #10990. I'll open a follow-up so this PR stays focused. WDYT?

### Review by [@trilamsr](https://github.com/trilamsr) (commented) — 2026-05-18T17:16:31Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:77** by [@trilamsr](https://github.com/trilamsr) — 2026-05-18T17:16:31Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
```

  > could this issue be also mitigated by increasing the `GroupKindConcurrency` level…
  
  I don't think it actually buys us anything in this code path. The knob is wired through correctly (the controller doesn't override `MaxConcurrentReconciles`), so in principle you'd get N parallel workers. But Reconcile calls `setRemoteClientConfig`, which grabs `clustersReconciler.lock` (a single controller wide write mutex at `multikueuecluster.go:394`) and holds it through the entire `setConfig` → `establishWatch` → `c.Watch()` chain. So when one worker is parked inside `c.Watch()`, every other Reconcile that wants to do anything with a different cluster sits waiting on that same lock.
  
  In practice concurrency=N collapses back to 1 whenever any remote is hung. Bumping the knob would only help after we also split that lock per cluster, or released it before establishing the watch. Probably a separate PR. Happy to file a follow up issue for it if you want.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T17:19:30Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:19:29Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  @trilamsr thank you for all the digging and super precise descriptions. We need to capture that with comments. 
  
  > Proposal: bump defaultWatchEstablishTimeout to 10 min in this PR (matching @mimowo's "say 10min"). Tiny diff, preserves the hung-remote fix, avoids the cold-start regression.
  
  I think 10min should be ok, espectially since we don't have now the discrepancy between the storage and serving versions, which bite us in 0.15. We may need to introduce the discrepancy when transitioning to v1beta3 one day, but at that point we will probably need to fix larger problems with https://github.com/kubernetes/kubernetes/issues/136950

### Review by [@trilamsr](https://github.com/trilamsr) (commented) — 2026-05-18T17:30:04Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@trilamsr](https://github.com/trilamsr) — 2026-05-18T17:30:04Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  Done in 9bd2d79e8. Bumped `defaultWatchEstablishTimeout` to `10 * time.Minute` and expanded the doc block above it to capture the cold-cache + conversion-webhook reasoning and the link to kubernetes/kubernetes#136950. PTAL.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T17:33:13Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:77** by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:33:13Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
```

  I see, thank you for the summary, I now understand why this would not work currently, and why fixing the problem is not straighforward.
  
  Having said that, I think this renders `GroupKindConcurrency` useless for `MultiKueueCluster` due to very technical reasons which can be considered a bug by a user.
  
  So, please file an issue for that. Feel free to work on it, or just keep it posted for some contributors.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T17:34:45Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:76** by [@mimowo](https://github.com/mimowo) — 2026-05-18T17:34:45Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
+	defaultWatchEstablishTimeout = 60 * time.Second
```

  sgtm

### Review by [@trilamsr](https://github.com/trilamsr) (commented) — 2026-05-18T17:55:43Z

- **pkg/controller/admissionchecks/multikueue/multikueuecluster.go:77** by [@trilamsr](https://github.com/trilamsr) — 2026-05-18T17:55:43Z
```diff
@@ -70,6 +70,15 @@ const (
 	// this set will provide waiting time between 0 to 5m20s
 	retryIncrement = 5 * time.Second
 	retryMaxSteps  = 7
+
+	// Bounds how long startWatcher waits for client.Watch() to return,
+	// so a hung remote cannot head-of-line block the reconciler. See #11206.
```

  Filed #11297 to track the lock refactor. Leaving it unassigned for now so it's open for any contributor to pick up.
