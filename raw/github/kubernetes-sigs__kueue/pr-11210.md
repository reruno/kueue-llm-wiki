# PR #11210: TAS: Stop implicitly assuming ResrouceFlavor:Node = 1:N

**Summary**: TAS: Stop implicitly assuming ResrouceFlavor:Node = 1:N

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11210

**Last updated**: 2026-05-20T23:30:46Z

---

## Metadata

- **State**: open
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `allow-multiple-flavors-for-node` → `main`
- **Created**: 2026-05-15T05:53:37Z
- **Updated**: 2026-05-20T23:30:46Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/bug`, `release-note`, `size/XXL`, `approved`, `needs-rebase`, `do-not-merge/hold`, `do-not-merge/work-in-progress`, `cncf-cla: yes`, `area/tas`
- **Assignees**: _none_
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl)
- **Changed files**: 14   **+1232 / -39**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
/area tas

#### What this PR does / why we need it:
I fixed a TAS over-subscription bug where multiple `ResourceFlavor` referencing the same Topology and overlapping on physical nodes each independently track `usage`, so sibling flavors believe a shared node is only partially used.

This PR refined the TAS cache so that sibling flavors aggregate usage.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/10659

#### Special notes for your reviewer:

https://github.com/kubernetes-sigs/kueue/pull/10657 is problem reproduced PR.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: fix over-subscription of nodes that belong to multiple ResourceFlavors sharing the same hostname-leaf Topology.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T05:53:40Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-15T05:53:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | b2aac879d1fe7335be2e33a7afebc86ede5b5786 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0c236ffe680200086843cd |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExMjEwLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.tS541BK50CysEXVBLP81Hvuum0Cow6dgE5LpjHyyVbs)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T05:53:44Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11210#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T05:53:47Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T07:06:05Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T11:58:10Z

/hold
for reviewers

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-15T12:00:12Z

cc @pajakd @PBundyra ptal

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:11:51Z

@pajakd Thank you for your review, I addressed all your comments, PTAL, thank you 🙏

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:20:28Z

@tenzen-y please rebase and I think it would be good to squeeze the commits in case we cherrypick

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:23:24Z

cc @Ladicle maybe could also give it a pass who already have pretty solid understanding of TAS

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:40:43Z

> Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

No objections at all.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:42:48Z

> Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

Sure, let's do that.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:50:06Z

In terms of squashing, I will squash all commits after @pajakd checked all changes. Because I tagged each commit with each of his review comments. Each commit can help understand fixes for @pajakd, I think.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:32:15Z

I rebased this PR on top of the main branch.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:33:34Z

I'm currently working on the dedicated FG introduction, `TASHandleOverlappingFlavors`.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:34:35Z

@pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:43:30Z

> I'm currently working on the dedicated FG introduction, `TASHandleOverlappingFlavors`.

@mimowo I'm assuming that we will introduce `TASHandleOverlappingFlavors` with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?

### Comment by [@pajakd](https://github.com/pajakd) — 2026-05-19T06:56:43Z

> @pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.

Checked all the changes. All looks good.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-19T07:06:00Z

> @mimowo I'm assuming that we will introduce TASHandleOverlappingFlavors with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?

Yes, it can be beta Imo  (I will yet double check), because it is bugfix. However, since TAS is already used in prod at scale we need to be careful about changes which have a risk of a performance hit. So, introducing the FG as a bailout is good.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T07:47:22Z

> > @pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.
> 
> Checked all the changes. All looks good.

Thank you for checking that. Let me squash all commits into one from now.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T07:48:12Z

> > @mimowo I'm assuming that we will introduce TASHandleOverlappingFlavors with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?
> 
> Yes, it can be beta Imo (I will yet double check), because it is bugfix. However, since TAS is already used in prod at scale we need to be careful about changes which have a risk of a performance hit. So, introducing the FG as a bailout is good.

That sounds reasonable to me, thank you for confirming that 👍

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T08:59:07Z

I turned this into a draft state to add FG.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-20T23:30:46Z

PR needs rebase.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T06:33:47Z

- **pkg/cache/scheduler/snapshot.go:198** by [@pajakd](https://github.com/pajakd) — 2026-05-18T06:33:47Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  Can we move it closer to its first usage?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T07:31:04Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:206** by [@pajakd](https://github.com/pajakd) — 2026-05-18T07:31:04Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
```

  This is tested in the test "multi-PodSet workload across sibling flavors must not self-overlap on shared node", right?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:19:37Z

- **test/integration/singlecluster/tas/tas_test.go:5157** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:19:37Z
```diff
@@ -4894,6 +4894,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel).
+		// tas-flavor-b has an extra NodeTaint that only the pending workload
+		// tolerates. When tas-flavor-a's nodes are fully consumed by prior
+		// workloads, the pending workload must land on the free node under
+		// tas-flavor-b. Exercises cross-flavor TAS usage aggregation on shared
+		// nodes.
+		ginkgo.When("Two TAS ResourceFlavors share nodes and the primary flavor is full", func() {
+			var (
+				tasFlavorA   *kueue.ResourceFlavor
+				tasFlavorB   *kueue.ResourceFlavor
+				topology     *kueue.Topology
+				localQueue   *kueue.LocalQueue
+				clusterQueue *kueue.ClusterQueue
+				nodes        []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("x1").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x1").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("x2").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x2").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+				}
+				util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+				topology = utiltestingapi.MakeDefaultOneLevelTopology("default")
+				util.MustCreate(ctx, k8sClient, topology)
+
+				tasFlavorA = utiltestingapi.MakeResourceFlavor("tas-flavor-a").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorA)
+
+				tasFlavorB = utiltestingapi.MakeResourceFlavor("tas-flavor-b").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "b",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorB)
+
+				clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorA.Name).
+							Resource(corev1.ResourceCPU, "400m").
+							Resource(corev1.ResourceMemory, "400Mi").Obj(),
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorB.Name).
+							Resource(corev1.ResourceCPU, "100m").
+							Resource(corev1.ResourceMemory, "100Mi").Obj(),
+					).
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyNever,
+					}).
+					Obj()
+				util.MustCreate(ctx, k8sClient, clusterQueue)
+
+				localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorA, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorB, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should admit the pending workload to tas-flavor-b on the free node when tas-flavor-a is full", func() {
```

  How would this test fail without the fix?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:22:50Z

- **pkg/util/maps/maps_test.go:458** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:22:51Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  shouldn't we check the value of `wantCollected` in this test? This is the only test where it should differ from `seed`, no?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:24:20Z

- **pkg/cache/scheduler/snapshot.go:198** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:24:20Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  Yes, sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:25:42Z

- **test/integration/singlecluster/tas/tas_test.go:5157** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:25:42Z
```diff
@@ -4894,6 +4894,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel).
+		// tas-flavor-b has an extra NodeTaint that only the pending workload
+		// tolerates. When tas-flavor-a's nodes are fully consumed by prior
+		// workloads, the pending workload must land on the free node under
+		// tas-flavor-b. Exercises cross-flavor TAS usage aggregation on shared
+		// nodes.
+		ginkgo.When("Two TAS ResourceFlavors share nodes and the primary flavor is full", func() {
+			var (
+				tasFlavorA   *kueue.ResourceFlavor
+				tasFlavorB   *kueue.ResourceFlavor
+				topology     *kueue.Topology
+				localQueue   *kueue.LocalQueue
+				clusterQueue *kueue.ClusterQueue
+				nodes        []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("x1").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x1").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("x2").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x2").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+				}
+				util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+				topology = utiltestingapi.MakeDefaultOneLevelTopology("default")
+				util.MustCreate(ctx, k8sClient, topology)
+
+				tasFlavorA = utiltestingapi.MakeResourceFlavor("tas-flavor-a").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorA)
+
+				tasFlavorB = utiltestingapi.MakeResourceFlavor("tas-flavor-b").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "b",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorB)
+
+				clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorA.Name).
+							Resource(corev1.ResourceCPU, "400m").
+							Resource(corev1.ResourceMemory, "400Mi").Obj(),
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorB.Name).
+							Resource(corev1.ResourceCPU, "100m").
+							Resource(corev1.ResourceMemory, "100Mi").Obj(),
+					).
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyNever,
+					}).
+					Obj()
+				util.MustCreate(ctx, k8sClient, clusterQueue)
+
+				localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorA, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorB, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should admit the pending workload to tas-flavor-b on the free node when tas-flavor-a is full", func() {
```

  This is repro PR: https://github.com/kubernetes-sigs/kueue/pull/10657/changes#r3118973170

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:46:36Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:206** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:46:36Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
```

  Yes, exactly, right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:47:57Z

- **pkg/util/maps/maps_test.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:47:56Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  Oh, good point. You're right.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:58:19Z

- **pkg/util/maps/maps.go:140** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:58:19Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  Since this function holds only rlock, if someone would try to call it with f(k,v) that modifies the underlying map, it could be dangerous (deadlock?). Shouldn't we add a comment warning about this?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T09:26:15Z

- **pkg/cache/scheduler/snapshot.go:202** by [@pajakd](https://github.com/pajakd) — 2026-05-18T09:26:15Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  In the loop above we don't initialize the `sharedSnapshotUsage[topologyName]` if `usageLength == 0`. But here we don't check for that. But if a topology has 0 usage, it might still need the shared map (for example if the first workload has multiple podsets)?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T10:33:51Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@pajakd](https://github.com/pajakd) — 2026-05-18T10:33:52Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  if tasFlavorCache would be nil it would panic in the line below this if. The comment above says that "`tasFlavor` is already in the snapshot" so I think `tasFlavorCache != nil` should either be removed entirely or changed to a guard clause.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:19:19Z

- **pkg/util/maps/maps.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:19:19Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  Yes, good point.
  I'm wondering if we should take RW lock instead of RLock here.
  
  Any preference adding comments vs taking RW lock?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:38:10Z

- **pkg/cache/scheduler/snapshot.go:202** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:38:10Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  Good catch, we should initialize that when `sharedSnapshotUsage[topologyName]` was not initialized in the previous loop block due to the `usageLength`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:49:33Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:49:33Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  That totally makes sense. Let's remove `tasFlavorCache != nil` because we can expect that it has already been checked as described in the above code comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:43:09Z

- **pkg/util/maps/maps_test.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:43:09Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  I fixed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/70b843439f7d4ee4a432486c215b2c332b480f32
  
  The `seed` is map. So, the expectation is not stable. Hence, I added a verification if the `collected` map key-value pairs match with seed's one.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:44:44Z

- **pkg/cache/scheduler/snapshot.go:198** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:44:44Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  I addressed it in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/52901477a36fc902f9e0c852628e4f833380cc75

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:45:57Z

- **pkg/cache/scheduler/snapshot.go:202** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:45:57Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  I addressed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/1be5bd2b238c7a11654e0b620fc71d3d612b9cea
  
  The `sharedSnapshotUsage[topologyName]` is always initialized even when the length of usageLength is zero.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T17:10:38Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:10:38Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  I address it in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/3afdebb6e3209d1aed12d7464c73d8d25c73833d

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T17:11:25Z

- **pkg/util/maps/maps.go:140** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:11:25Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  I just added a comment in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/8bfa984968f186595fd95713d8e06aa9979489de
  
  Because Writable `Range` will cause performance regressions for syncMap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T18:21:31Z

- **pkg/cache/scheduler/snapshot.go:208** by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:21:31Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  Exract this code to a helper function so that we can use the Lock, defer UnLock pattern

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T18:22:48Z

Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-19T05:53:19Z

- **pkg/cache/scheduler/snapshot.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:53:19Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  That sounds good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-19T06:33:02Z

- **pkg/cache/scheduler/snapshot.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:33:02Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  I addressed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/5387a99a9f205ed43ca4870f6c2df7d39e903761
