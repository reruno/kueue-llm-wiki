# PR #11210: TAS: Stop implicitly assuming ResrouceFlavor:Node = 1:N

**Summary**: TAS: Stop implicitly assuming ResrouceFlavor:Node = 1:N

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11210

**Last updated**: 2026-05-28T20:20:03Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@tenzen-y](https://github.com/tenzen-y)
- **Branch**: `allow-multiple-flavors-for-node` → `main`
- **Created**: 2026-05-15T05:53:37Z
- **Updated**: 2026-05-28T20:20:03Z
- **Merged**: 2026-05-28T20:18:51Z
- **Closed**: 2026-05-28T20:18:51Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`, `area/tas`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@olekzabl](https://github.com/olekzabl), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 13   **+1195 / -35**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
/area tas

#### What this PR does / why we need it:
I fixed a TAS over-subscription bug where multiple `ResourceFlavor` referencing the same Topology and overlapping on physical nodes each independently track `usage`, so sibling flavors believe a shared node is only partially used.

This PR refined the TAS cache so that sibling flavors aggregate usage.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/10659

#### Special notes for your reviewer:

https://github.com/kubernetes-sigs/kueue/pull/10657 is problem reproduced PR.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: fix over-subscription of nodes that belong to multiple ResourceFlavors sharing the same hostname-leaf Topology.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-15T05:53:40Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-15T05:53:43Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c7efbca3d9129caee9f53c27ee13a66959100ac7 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a1891152379f70008f11648 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExMjEwLS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.tS541BK50CysEXVBLP81Hvuum0Cow6dgE5LpjHyyVbs)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-11210--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T05:53:47Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T07:06:05Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T11:58:10Z

/hold
for reviewers

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-15T12:00:12Z

cc @pajakd @PBundyra ptal

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:11:51Z

@pajakd Thank you for your review, I addressed all your comments, PTAL, thank you 🙏

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:20:28Z

@tenzen-y please rebase and I think it would be good to squeeze the commits in case we cherrypick

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:23:24Z

cc @Ladicle maybe could also give it a pass who already have pretty solid understanding of TAS

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:40:43Z

> Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

No objections at all.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:42:48Z

> Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

Sure, let's do that.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:50:06Z

In terms of squashing, I will squash all commits after @pajakd checked all changes. Because I tagged each commit with each of his review comments. Each commit can help understand fixes for @pajakd, I think.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:32:15Z

I rebased this PR on top of the main branch.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:33:34Z

I'm currently working on the dedicated FG introduction, `TASHandleOverlappingFlavors`.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:34:35Z

@pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:43:30Z

> I'm currently working on the dedicated FG introduction, `TASHandleOverlappingFlavors`.

@mimowo I'm assuming that we will introduce `TASHandleOverlappingFlavors` with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?

### Comment by [@pajakd](https://github.com/pajakd) — 2026-05-19T06:56:43Z

> @pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.

Checked all the changes. All looks good.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-19T07:06:00Z

> @mimowo I'm assuming that we will introduce TASHandleOverlappingFlavors with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?

Yes, it can be beta Imo  (I will yet double check), because it is bugfix. However, since TAS is already used in prod at scale we need to be careful about changes which have a risk of a performance hit. So, introducing the FG as a bailout is good.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T07:47:22Z

> > @pajakd Please let me know once you have checked the changes for your comments. After that, I will squach all commits into one for backports.
> 
> Checked all the changes. All looks good.

Thank you for checking that. Let me squash all commits into one from now.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T07:48:12Z

> > @mimowo I'm assuming that we will introduce TASHandleOverlappingFlavors with Beta stage in the main branch, but we degrade the FG phase with alpha when we backport it to release branches, the same as previously, right?
> 
> Yes, it can be beta Imo (I will yet double check), because it is bugfix. However, since TAS is already used in prod at scale we need to be careful about changes which have a risk of a performance hit. So, introducing the FG as a bailout is good.

That sounds reasonable to me, thank you for confirming that 👍

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T08:59:07Z

I turned this into a draft state to add FG.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T04:34:20Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T05:08:46Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T06:54:40Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T07:20:58Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T09:27:09Z

/test all

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T10:19:02Z

@pajakd @mimowo I added feature gate guards as we discussed.
PTAL, thanks!

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T15:21:30Z

/test pull-kueue-test-e2e-multikueue-sequential-main

due to https://github.com/kubernetes-sigs/kueue/issues/11631

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:44:45Z

> @tenzen-y: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:
> 
> Test name	Commit	Details	Required	Rerun command
> pull-kueue-test-integration-shard-2-main	[fa0431d](https://github.com/kubernetes-sigs/kueue/commit/fa0431dabfa18aaeca0d5e30fb2298f98bce6f85)	[link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/11210/pull-kueue-test-integration-shard-2-main/2059689283937112064)	true	`/test pull-kueue-test-integration-shard-2-main`
> [Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=11210). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atenzen-y). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.
> 
> Details

opened https://github.com/kubernetes-sigs/kueue/issues/11694

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:04:26Z

I'm currently checking an integration test failure. I will push fix if it is not flake.
Give me some time.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:18:17Z

> I'm currently checking an integration test failure. I will push fix if it is not flake. Give me some time.

I got the bug point. I will push bug fix code.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:33:22Z

> > I'm currently checking an integration test failure. I will push fix if it is not flake. Give me some time.
> 
> I got the bug point. I will push bug fix code.

I pushed the bug fix code. Let's see if this can resolve that.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:36:43Z

> @tenzen-y: The following tests **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:
> 
> Test name	Commit	Details	Required	Rerun command
> pull-kueue-test-integration-shard-0-main	[a07fa04](https://github.com/kubernetes-sigs/kueue/commit/a07fa048ddf56b4652f7ccfd84a86173e32a562b)	[link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/11210/pull-kueue-test-integration-shard-0-main/2060044168830390272)	true	`/test pull-kueue-test-integration-shard-0-main`
> pull-kueue-test-integration-shard-1-main	[940ab12](https://github.com/kubernetes-sigs/kueue/commit/940ab1232aff067991bc5f9c2665d6e6805ccb37)	[link](https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/11210/pull-kueue-test-integration-shard-1-main/2060051729759604736)	true	`/test pull-kueue-test-integration-shard-1-main`
> [Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=11210). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atenzen-y). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.
> 
> Details

infra

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T19:03:11Z

1. I reverted to making TASHandleOverlappingFlavors alpha (new code enables it by default)
2. I squashed all commits into one
3. I added logs as you mentioned.

Thanks for the review 👍

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-28T19:09:15Z

LGTM label has been added.  <details>Git tree hash: 5ce53d8fa65e8cac4fdd6c2fb271f9498cd99b5d</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-28T19:09:18Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11210#pullrequestreview-4383953270" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11210#" title="Author self-approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T19:11:45Z

> /lgtm /approve Thank you for closing the long-standing gap in TAS 👍 I propose to CP as alpha being careful about the prod use of TAS.

I will open PRs before tomorrow's release.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T19:37:50Z

/hold cancel

## Reviews

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T06:33:47Z

- **pkg/cache/scheduler/snapshot.go:190** by [@pajakd](https://github.com/pajakd) — 2026-05-18T06:33:47Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  Can we move it closer to its first usage?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T07:31:04Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:205** by [@pajakd](https://github.com/pajakd) — 2026-05-18T07:31:04Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
```

  This is tested in the test "multi-PodSet workload across sibling flavors must not self-overlap on shared node", right?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:19:37Z

- **test/integration/singlecluster/tas/tas_test.go:5311** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:19:37Z
```diff
@@ -4894,6 +4894,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel).
+		// tas-flavor-b has an extra NodeTaint that only the pending workload
+		// tolerates. When tas-flavor-a's nodes are fully consumed by prior
+		// workloads, the pending workload must land on the free node under
+		// tas-flavor-b. Exercises cross-flavor TAS usage aggregation on shared
+		// nodes.
+		ginkgo.When("Two TAS ResourceFlavors share nodes and the primary flavor is full", func() {
+			var (
+				tasFlavorA   *kueue.ResourceFlavor
+				tasFlavorB   *kueue.ResourceFlavor
+				topology     *kueue.Topology
+				localQueue   *kueue.LocalQueue
+				clusterQueue *kueue.ClusterQueue
+				nodes        []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("x1").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x1").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("x2").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x2").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+				}
+				util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+				topology = utiltestingapi.MakeDefaultOneLevelTopology("default")
+				util.MustCreate(ctx, k8sClient, topology)
+
+				tasFlavorA = utiltestingapi.MakeResourceFlavor("tas-flavor-a").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorA)
+
+				tasFlavorB = utiltestingapi.MakeResourceFlavor("tas-flavor-b").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "b",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorB)
+
+				clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorA.Name).
+							Resource(corev1.ResourceCPU, "400m").
+							Resource(corev1.ResourceMemory, "400Mi").Obj(),
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorB.Name).
+							Resource(corev1.ResourceCPU, "100m").
+							Resource(corev1.ResourceMemory, "100Mi").Obj(),
+					).
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyNever,
+					}).
+					Obj()
+				util.MustCreate(ctx, k8sClient, clusterQueue)
+
+				localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorA, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorB, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should admit the pending workload to tas-flavor-b on the free node when tas-flavor-a is full", func() {
```

  How would this test fail without the fix?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:22:50Z

- **pkg/util/maps/maps_test.go:458** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:22:51Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  shouldn't we check the value of `wantCollected` in this test? This is the only test where it should differ from `seed`, no?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:24:20Z

- **pkg/cache/scheduler/snapshot.go:190** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:24:20Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  Yes, sure.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:25:42Z

- **test/integration/singlecluster/tas/tas_test.go:5311** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:25:42Z
```diff
@@ -4894,6 +4894,218 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel).
+		// tas-flavor-b has an extra NodeTaint that only the pending workload
+		// tolerates. When tas-flavor-a's nodes are fully consumed by prior
+		// workloads, the pending workload must land on the free node under
+		// tas-flavor-b. Exercises cross-flavor TAS usage aggregation on shared
+		// nodes.
+		ginkgo.When("Two TAS ResourceFlavors share nodes and the primary flavor is full", func() {
+			var (
+				tasFlavorA   *kueue.ResourceFlavor
+				tasFlavorB   *kueue.ResourceFlavor
+				topology     *kueue.Topology
+				localQueue   *kueue.LocalQueue
+				clusterQueue *kueue.ClusterQueue
+				nodes        []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("x1").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x1").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("x2").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x2").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+				}
+				util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+				topology = utiltestingapi.MakeDefaultOneLevelTopology("default")
+				util.MustCreate(ctx, k8sClient, topology)
+
+				tasFlavorA = utiltestingapi.MakeResourceFlavor("tas-flavor-a").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorA)
+
+				tasFlavorB = utiltestingapi.MakeResourceFlavor("tas-flavor-b").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "b",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorB)
+
+				clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorA.Name).
+							Resource(corev1.ResourceCPU, "400m").
+							Resource(corev1.ResourceMemory, "400Mi").Obj(),
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorB.Name).
+							Resource(corev1.ResourceCPU, "100m").
+							Resource(corev1.ResourceMemory, "100Mi").Obj(),
+					).
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyNever,
+					}).
+					Obj()
+				util.MustCreate(ctx, k8sClient, clusterQueue)
+
+				localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorA, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorB, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should admit the pending workload to tas-flavor-b on the free node when tas-flavor-a is full", func() {
```

  This is repro PR: https://github.com/kubernetes-sigs/kueue/pull/10657/changes#r3118973170

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:46:36Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:205** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:46:36Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
```

  Yes, exactly, right.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T08:47:57Z

- **pkg/util/maps/maps_test.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T08:47:56Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  Oh, good point. You're right.

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T08:58:19Z

- **pkg/util/maps/maps.go:136** by [@pajakd](https://github.com/pajakd) — 2026-05-18T08:58:19Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  Since this function holds only rlock, if someone would try to call it with f(k,v) that modifies the underlying map, it could be dangerous (deadlock?). Shouldn't we add a comment warning about this?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T09:26:15Z

- **pkg/cache/scheduler/snapshot.go:219** by [@pajakd](https://github.com/pajakd) — 2026-05-18T09:26:15Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  In the loop above we don't initialize the `sharedSnapshotUsage[topologyName]` if `usageLength == 0`. But here we don't check for that. But if a topology has 0 usage, it might still need the shared map (for example if the first workload has multiple podsets)?

### Review by [@pajakd](https://github.com/pajakd) (commented) — 2026-05-18T10:33:51Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@pajakd](https://github.com/pajakd) — 2026-05-18T10:33:52Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  if tasFlavorCache would be nil it would panic in the line below this if. The comment above says that "`tasFlavor` is already in the snapshot" so I think `tasFlavorCache != nil` should either be removed entirely or changed to a guard clause.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:19:19Z

- **pkg/util/maps/maps.go:136** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:19:19Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  Yes, good point.
  I'm wondering if we should take RW lock instead of RLock here.
  
  Any preference adding comments vs taking RW lock?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:38:10Z

- **pkg/cache/scheduler/snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:38:10Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  Good catch, we should initialize that when `sharedSnapshotUsage[topologyName]` was not initialized in the previous loop block due to the `usageLength`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T15:49:33Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T15:49:33Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  That totally makes sense. Let's remove `tasFlavorCache != nil` because we can expect that it has already been checked as described in the above code comment.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:43:09Z

- **pkg/util/maps/maps_test.go:458** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:43:09Z
```diff
@@ -292,3 +293,225 @@ func TestFilterKeys(t *testing.T) {
 		})
 	}
 }
+
+func TestSyncMapUpdate(t *testing.T) {
+	cases := map[string]struct {
+		seed         map[string]int
+		key          string
+		fn           func(prev int, exists bool) int
+		wantPrev     int
+		wantExists   bool
+		wantStored   int
+		wantContents map[string]int
+	}{
+		"missing key: fn sees zero/false, result stored": {
+			seed:       map[string]int{},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev + 7 },
+			wantPrev:   0,
+			wantExists: false,
+			wantStored: 7,
+			wantContents: map[string]int{
+				"k": 7,
+			},
+		},
+		"existing key: fn sees stored value, result replaces it": {
+			seed:       map[string]int{"k": 3},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev * 2 },
+			wantPrev:   3,
+			wantExists: true,
+			wantStored: 6,
+			wantContents: map[string]int{
+				"k": 6,
+			},
+		},
+		"fn returns prev unchanged: idempotent": {
+			seed:       map[string]int{"k": 42},
+			key:        "k",
+			fn:         func(prev int, _ bool) int { return prev },
+			wantPrev:   42,
+			wantExists: true,
+			wantStored: 42,
+			wantContents: map[string]int{
+				"k": 42,
+			},
+		},
+		"fn returns zero value: entry remains with zero, not deleted": {
+			seed:       map[string]int{"k": 5},
+			key:        "k",
+			fn:         func(_ int, _ bool) int { return 0 },
+			wantPrev:   5,
+			wantExists: true,
+			wantStored: 0,
+			wantContents: map[string]int{
+				"k": 0,
+			},
+		},
+		"other keys preserved": {
+			seed:       map[string]int{"a": 1, "b": 2},
+			key:        "b",
+			fn:         func(prev int, _ bool) int { return prev + 10 },
+			wantPrev:   2,
+			wantExists: true,
+			wantStored: 12,
+			wantContents: map[string]int{
+				"a": 1,
+				"b": 12,
+			},
+		},
+	}
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			m := NewSyncMap[string, int](0)
+			for k, v := range tc.seed {
+				m.Add(k, v)
+			}
+
+			var gotPrev int
+			var gotExists bool
+			m.Update(tc.key, func(prev int, exists bool) int {
+				gotPrev = prev
+				gotExists = exists
+				return tc.fn(prev, exists)
+			})
+
+			if gotPrev != tc.wantPrev {
+				t.Errorf("fn received prev = %d, want %d", gotPrev, tc.wantPrev)
+			}
+			if gotExists != tc.wantExists {
+				t.Errorf("fn received exists = %t, want %t", gotExists, tc.wantExists)
+			}
+			got, ok := m.Get(tc.key)
+			if !ok {
+				t.Fatalf("stored entry for key %q missing after Update", tc.key)
+			}
+			if got != tc.wantStored {
+				t.Errorf("stored value = %d, want %d", got, tc.wantStored)
+			}
+
+			contents := make(map[string]int, m.Len())
+			m.Range(func(k string, v int) bool {
+				contents[k] = v
+				return true
+			})
+			if diff := cmp.Diff(contents, tc.wantContents); diff != "" {
+				t.Errorf("Unexpected map contents (-got,+want):\n%s", diff)
+			}
+		})
+	}
+
+	t.Run("sequential updates accumulate", func(t *testing.T) {
+		m := NewSyncMap[string, int](0)
+		increment := func(prev int, _ bool) int { return prev + 1 }
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		m.Update("counter", increment)
+		got, ok := m.Get("counter")
+		if !ok || got != 3 {
+			t.Errorf("after 3 increments, Get(counter) = (%d, %t); want (3, true)", got, ok)
+		}
+	})
+
+	t.Run("atomic under concurrent callers", func(t *testing.T) {
+		const N = 100
+		m := NewSyncMap[string, int](0)
+		var wg sync.WaitGroup
+		for range N {
+			wg.Go(func() {
+				m.Update("counter", func(prev int, _ bool) int { return prev + 1 })
+			})
+		}
+		wg.Wait()
+		got, ok := m.Get("counter")
+		if !ok {
+			t.Fatal("counter entry missing after concurrent Updates")
+		}
+		if got != N {
+			t.Errorf("concurrent Updates lost increments: got %d, want %d", got, N)
+		}
+	})
+}
+
+func TestSyncMapRange(t *testing.T) {
+	cases := map[string]struct {
+		seed          map[string]int
+		stopAfter     int // 0 = visit all; >0 = return false after that many visits
+		wantCalls     int
+		wantCollected map[string]int // set when stopAfter == 0
+	}{
+		"empty map: fn never invoked": {
+			seed:          map[string]int{},
+			wantCalls:     0,
+			wantCollected: map[string]int{},
+		},
+		"single entry": {
+			seed:          map[string]int{"k": 1},
+			wantCalls:     1,
+			wantCollected: map[string]int{"k": 1},
+		},
+		"multiple entries: every entry visited": {
+			seed:          map[string]int{"a": 1, "b": 2, "c": 3},
+			wantCalls:     3,
+			wantCollected: map[string]int{"a": 1, "b": 2, "c": 3},
+		},
+		"early termination: stops on first false": {
```

  I fixed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/70b843439f7d4ee4a432486c215b2c332b480f32
  
  The `seed` is map. So, the expectation is not stable. Hence, I added a verification if the `collected` map key-value pairs match with seed's one.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:44:44Z

- **pkg/cache/scheduler/snapshot.go:190** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:44:44Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
```

  I addressed it in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/52901477a36fc902f9e0c852628e4f833380cc75

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T16:45:57Z

- **pkg/cache/scheduler/snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T16:45:57Z
```diff
@@ -185,8 +187,39 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		flavorClone := c.tasCache.Clone()
+
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			usageLength := info.Usage.Len()
+			if usageLength == 0 {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, usageLength)
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
+
+		for flavor, cache := range flavorClone {
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if cache.topology.Usage != nil {
```

  I addressed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/1be5bd2b238c7a11654e0b620fc71d3d612b9cea
  
  The `sharedSnapshotUsage[topologyName]` is always initialized even when the length of usageLength is zero.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T17:10:38Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:10:38Z
```diff
@@ -197,13 +198,32 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	sharedAssumed := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	// Iterate flavors in deterministic order so the shared assumedUsage map
+	// produces reproducible placements across scheduling cycles. Go map
+	// iteration order is randomized otherwise.
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if tasFlavorCache != nil && tasFlavorCache.isLowestLevelNode {
```

  I address it in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/3afdebb6e3209d1aed12d7464c73d8d25c73833d

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-18T17:11:25Z

- **pkg/util/maps/maps.go:136** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-18T17:11:25Z
```diff
@@ -132,3 +132,20 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
```

  I just added a comment in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/8bfa984968f186595fd95713d8e06aa9979489de
  
  Because Writable `Range` will cause performance regressions for syncMap.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T18:21:31Z

- **pkg/cache/scheduler/snapshot.go:208** by [@mimowo](https://github.com/mimowo) — 2026-05-18T18:21:31Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  Exract this code to a helper function so that we can use the Lock, defer UnLock pattern

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T18:22:48Z

Given the amount of non-trivial changes I would consider a feature gate for the fix, like `TASHandleOverlappingFlavors`

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-19T05:53:19Z

- **pkg/cache/scheduler/snapshot.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T05:53:19Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  That sounds good to me.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-19T06:33:02Z

- **pkg/cache/scheduler/snapshot.go:208** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-19T06:33:02Z
```diff
@@ -184,16 +178,43 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	log := ctrl.LoggerFrom(ctx)
 	cqNames := c.hm.ClusterQueues()
 	for _, cq := range cqNames {
-		if reason := skipInactiveCQReason(cq); reason != "" {
-			log.V(3).Info("Skipping ClusterQueue", "clusterQueue", cq.Name, "reason", reason)
+		if !cq.Active() || (cq.HasParent() && hierarchy.HasCycle(cq.Parent())) {
 			snap.InactiveClusterQueueSets.Insert(cq.Name)
 			continue
 		}
+		// ensure all workloads are accounted for TAS before building TAS snapshots
+		cq.ensureTASIsSynced(log)
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		// Clone topologies[T].Usage once per eligible Topology so every
+		// sibling-flavor snapshot for T aliases the same fresh map.
+		// In-cycle mutations (preemption, fair-sharing) therefore propagate
+		// cross-flavor but do not leak back to the live cache.
+		sharedSnapshotUsage := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+		c.tasCache.RLock()
+		for topologyName, info := range c.tasCache.topologies {
+			if info.Usage == nil {
+				continue
+			}
+			clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+			info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+				clonedUsage[domainID] = req.Clone()
+				return true
+			})
+			sharedSnapshotUsage[topologyName] = clonedUsage
+		}
+		c.tasCache.RUnlock()
```

  I addressed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/5387a99a9f205ed43ca4870f6c2df7d39e903761

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-26T10:39:31Z

- **pkg/cache/scheduler/snapshot.go:197** by [@mimowo](https://github.com/mimowo) — 2026-05-26T10:39:31Z
```diff
@@ -192,8 +194,14 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		sharedSnapshotUsage := c.snapshotTopologyUsages()
```

  IIUC this is ungated currently, I'm wondering if we can gate this too

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-26T13:40:33Z

- **pkg/cache/scheduler/snapshot.go:197** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T13:40:33Z
```diff
@@ -192,8 +194,14 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		sharedSnapshotUsage := c.snapshotTopologyUsages()
```

  Good point, let me guard this by TASHandleOverlappingFlavors.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-26T13:46:03Z

- **pkg/cache/scheduler/snapshot.go:197** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-26T13:46:03Z
```diff
@@ -192,8 +194,14 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		sharedSnapshotUsage := c.snapshotTopologyUsages()
```

  Done in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/52e4733bfd58781a5b04f044f944fb56f2b8a135

### Review by [@sohankunkerkar](https://github.com/sohankunkerkar) (commented) — 2026-05-26T22:11:54Z

- **pkg/cache/scheduler/tas_flavor.go:211** by [@sohankunkerkar](https://github.com/sohankunkerkar) — 2026-05-26T21:59:49Z
```diff
@@ -160,12 +194,33 @@ func (c *TASFlavorCache) updateUsage(topologyRequests []workload.TopologyDomainR
 		if !found {
 			c.usage[domainID] = resources.Requests{}
 		}
+		total := tr.TotalRequests()
+		podDelta := resources.Requests{corev1.ResourcePods: int64(tr.Count)}
 		if op == subtract {
-			c.usage[domainID].Sub(tr.TotalRequests())
-			c.usage[domainID].Sub(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Sub(total)
+			c.usage[domainID].Sub(podDelta)
 		} else {
-			c.usage[domainID].Add(tr.TotalRequests())
-			c.usage[domainID].Add(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Add(total)
+			c.usage[domainID].Add(podDelta)
+		}
+		// For hostname-leaf topologies, mirror the delta to the shared
+		// SyncMap so sibling TASFlavorCache instances and Cache.Snapshot's
+		// clone path observe the cross-flavor aggregate. SyncMap.Update is
+		// atomic so concurrent sibling writers serialize on its lock.
+		if features.Enabled(features.TASHandleOverlappingFlavors) && c.topology.Usage != nil {
+			c.topology.Usage.Update(domainID, func(prev resources.Requests, _ bool) resources.Requests {
```

  I think the integration test only exercises the create→admit path. Could we add a test case that admits a workload via flavor A, finishes/deletes it, and then verifies flavor B can use the freed node? That would cover the subtract side of the shared accounting.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T09:42:28Z

- **pkg/cache/scheduler/tas_flavor.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T09:42:27Z
```diff
@@ -160,12 +194,33 @@ func (c *TASFlavorCache) updateUsage(topologyRequests []workload.TopologyDomainR
 		if !found {
 			c.usage[domainID] = resources.Requests{}
 		}
+		total := tr.TotalRequests()
+		podDelta := resources.Requests{corev1.ResourcePods: int64(tr.Count)}
 		if op == subtract {
-			c.usage[domainID].Sub(tr.TotalRequests())
-			c.usage[domainID].Sub(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Sub(total)
+			c.usage[domainID].Sub(podDelta)
 		} else {
-			c.usage[domainID].Add(tr.TotalRequests())
-			c.usage[domainID].Add(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Add(total)
+			c.usage[domainID].Add(podDelta)
+		}
+		// For hostname-leaf topologies, mirror the delta to the shared
+		// SyncMap so sibling TASFlavorCache instances and Cache.Snapshot's
+		// clone path observe the cross-flavor aggregate. SyncMap.Update is
+		// atomic so concurrent sibling writers serialize on its lock.
+		if features.Enabled(features.TASHandleOverlappingFlavors) && c.topology.Usage != nil {
+			c.topology.Usage.Update(domainID, func(prev resources.Requests, _ bool) resources.Requests {
```

  That sounds reasonable, let me expand integration test case

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T12:39:14Z

- **test/integration/singlecluster/tas/tas_test.go:5428** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T12:39:14Z
```diff
@@ -5186,6 +5186,334 @@ var _ = ginkgo.Describe("Topology Aware Scheduling", ginkgo.Ordered, func() {
 				})
 			})
 		})
+
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel)
+		// but are made mutually exclusive via flavor-only NodeTaints
+		// (tas-class=a on flavor-a, tas-class=b on flavor-b) so each workload
+		// targets exactly one flavor. It also blocks exercise the
+		// cross-flavor TAS usage aggregation on shared nodes in the following:
+		//   1. It verifies the add side (sibling-flavor placement skips a node already
+		//   occupied by the primary flavor)
+		//   2. the second It verifies the subtract side (the freed node becomes reusable
+		//   by the sibling flavor once the primary workload finishes).
+		ginkgo.When("Two TAS ResourceFlavors share nodes", func() {
+			var (
+				tasFlavorA   *kueue.ResourceFlavor
+				tasFlavorB   *kueue.ResourceFlavor
+				topology     *kueue.Topology
+				localQueue   *kueue.LocalQueue
+				clusterQueue *kueue.ClusterQueue
+				nodes        []corev1.Node
+			)
+
+			ginkgo.BeforeEach(func() {
+				nodes = []corev1.Node{
+					*testingnode.MakeNode("x1").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x1").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+					*testingnode.MakeNode("x2").
+						Label("node-group", "tas").
+						Label(corev1.LabelHostname, "x2").
+						StatusAllocatable(corev1.ResourceList{
+							corev1.ResourceCPU:    resource.MustParse("400m"),
+							corev1.ResourceMemory: resource.MustParse("400Mi"),
+							corev1.ResourcePods:   resource.MustParse("10"),
+						}).
+						Taints(corev1.Taint{
+							Key:    "node-group",
+							Value:  "tas",
+							Effect: corev1.TaintEffectNoSchedule,
+						}).
+						Ready().
+						Obj(),
+				}
+				util.CreateNodesWithStatus(ctx, k8sClient, nodes)
+
+				topology = utiltestingapi.MakeDefaultOneLevelTopology("default")
+				util.MustCreate(ctx, k8sClient, topology)
+
+				tasFlavorA = utiltestingapi.MakeResourceFlavor("tas-flavor-a").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "a",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorA)
+
+				tasFlavorB = utiltestingapi.MakeResourceFlavor("tas-flavor-b").
+					NodeLabel("node-group", "tas").
+					Taint(corev1.Taint{
+						Key:    "node-group",
+						Value:  "tas",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					Taint(corev1.Taint{
+						Key:    "tas-class",
+						Value:  "b",
+						Effect: corev1.TaintEffectNoSchedule,
+					}).
+					TopologyName("default").
+					Obj()
+				util.MustCreate(ctx, k8sClient, tasFlavorB)
+
+				clusterQueue = utiltestingapi.MakeClusterQueue("cluster-queue").
+					ResourceGroup(
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorA.Name).
+							Resource(corev1.ResourceCPU, "400m").
+							Resource(corev1.ResourceMemory, "400Mi").Obj(),
+						*utiltestingapi.MakeFlavorQuotas(tasFlavorB.Name).
+							Resource(corev1.ResourceCPU, "100m").
+							Resource(corev1.ResourceMemory, "100Mi").Obj(),
+					).
+					Preemption(kueue.ClusterQueuePreemption{
+						ReclaimWithinCohort: kueue.PreemptionPolicyNever,
+					}).
+					Obj()
+				util.MustCreate(ctx, k8sClient, clusterQueue)
+
+				localQueue = utiltestingapi.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(clusterQueue.Name).Obj()
+				util.MustCreate(ctx, k8sClient, localQueue)
+			})
+
+			ginkgo.AfterEach(func() {
+				gomega.Expect(util.DeleteAllPodsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteWorkloadsInNamespace(ctx, k8sClient, ns)).Should(gomega.Succeed())
+				gomega.Expect(util.DeleteObject(ctx, k8sClient, localQueue)).Should(gomega.Succeed())
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, clusterQueue, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorA, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, tasFlavorB, true)
+				util.ExpectObjectToBeDeleted(ctx, k8sClient, topology, true)
+				for _, node := range nodes {
+					util.ExpectObjectToBeDeleted(ctx, k8sClient, &node, true)
+				}
+			})
+
+			ginkgo.It("should admit the pending workload to tas-flavor-b on the free node when tas-flavor-a is full", func() {
+				var (
+					primaryWl1 *kueue.Workload
+					primaryWl2 *kueue.Workload
+					pendingWl  *kueue.Workload
+				)
+
+				ginkgo.By("creating the first tas-flavor-a workload pinned to x1", func() {
+					primaryWl1 = utiltestingapi.MakeWorkload("primary-wl1", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet("one", 1).
+							NodeSelector(map[string]string{corev1.LabelHostname: "x1"}).
+							PreferredTopologyRequest(corev1.LabelHostname).
+							Request(corev1.ResourceCPU, "200m").
+							Request(corev1.ResourceMemory, "200Mi").
+							Toleration(corev1.Toleration{
+								Key:      "node-group",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "tas",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Toleration(corev1.Toleration{
+								Key:      "tas-class",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "a",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Obj()).Obj()
+					util.MustCreate(ctx, k8sClient, primaryWl1)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, primaryWl1)
+					createPodsForWorkload(primaryWl1, ns.Name, false, true)
+				})
+
+				ginkgo.By("creating the second tas-flavor-a workload pinned to x1", func() {
+					primaryWl2 = utiltestingapi.MakeWorkload("primary-wl2", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet("one", 1).
+							NodeSelector(map[string]string{corev1.LabelHostname: "x1"}).
+							PreferredTopologyRequest(corev1.LabelHostname).
+							Request(corev1.ResourceCPU, "200m").
+							Request(corev1.ResourceMemory, "200Mi").
+							Toleration(corev1.Toleration{
+								Key:      "node-group",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "tas",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Toleration(corev1.Toleration{
+								Key:      "tas-class",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "a",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Obj()).Obj()
+					util.MustCreate(ctx, k8sClient, primaryWl2)
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, primaryWl2)
+					createPodsForWorkload(primaryWl2, ns.Name, false, true)
+				})
+
+				ginkgo.By("verifying both primary workloads consumed tas-flavor-a and share node x1", func() {
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(primaryWl1), primaryWl1)).To(gomega.Succeed())
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(primaryWl2), primaryWl2)).To(gomega.Succeed())
+					for _, wl := range []*kueue.Workload{primaryWl1, primaryWl2} {
+						gomega.Expect(wl.Status.Admission.PodSetAssignments[0].Flavors[corev1.ResourceCPU]).To(
+							gomega.Equal(kueue.ResourceFlavorReference(tasFlavorA.Name)))
+						gomega.Expect(wl.Status.Admission.PodSetAssignments[0].Flavors[corev1.ResourceMemory]).To(
+							gomega.Equal(kueue.ResourceFlavorReference(tasFlavorA.Name)))
+						ta := utiltas.InternalFrom(wl.Status.Admission.PodSetAssignments[0].TopologyAssignment)
+						gomega.Expect(ta.Domains).To(gomega.HaveLen(1))
+						gomega.Expect(ta.Domains[0].Values).To(gomega.Equal([]string{"x1"}))
+					}
+				})
+
+				ginkgo.By("creating the pending workload that tolerates both taints", func() {
+					pendingWl = utiltestingapi.MakeWorkload("pending-wl", ns.Name).
+						Queue(kueue.LocalQueueName(localQueue.Name)).
+						PodSets(*utiltestingapi.MakePodSet("one", 1).
+							PreferredTopologyRequest(corev1.LabelHostname).
+							Request(corev1.ResourceCPU, "100m").
+							Request(corev1.ResourceMemory, "100Mi").
+							Toleration(corev1.Toleration{
+								Key:      "node-group",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "tas",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Toleration(corev1.Toleration{
+								Key:      "tas-class",
+								Operator: corev1.TolerationOpEqual,
+								Value:    "b",
+								Effect:   corev1.TaintEffectNoSchedule,
+							}).
+							Obj()).Obj()
+					util.MustCreate(ctx, k8sClient, pendingWl)
+				})
+
+				ginkgo.By("verifying the pending workload is admitted to tas-flavor-b on the free node x2", func() {
+					util.ExpectWorkloadsToBeAdmitted(ctx, k8sClient, pendingWl)
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(pendingWl), pendingWl)).To(gomega.Succeed())
+					gomega.Expect(pendingWl.Status.Admission.PodSetAssignments[0].Flavors[corev1.ResourceCPU]).To(
+						gomega.Equal(kueue.ResourceFlavorReference(tasFlavorB.Name)))
+					gomega.Expect(pendingWl.Status.Admission.PodSetAssignments[0].Flavors[corev1.ResourceMemory]).To(
+						gomega.Equal(kueue.ResourceFlavorReference(tasFlavorB.Name)))
+					gomega.Expect(pendingWl.Status.Admission.PodSetAssignments[0].TopologyAssignment).Should(gomega.BeComparableTo(
+						utiltas.V1Beta2From(&utiltas.TopologyAssignment{
+							Levels: []string{corev1.LabelHostname},
+							Domains: []utiltas.TopologyDomainAssignment{
+								{
+									Count:  1,
+									Values: []string{"x2"},
+								},
+							},
+						}),
+					))
+				})
+			})
+
+			ginkgo.It("should admit a sibling-flavor workload on the freed node once the primary workload finishes", func() {
```

  @sohankunkerkar This is for https://github.com/kubernetes-sigs/kueue/pull/11210#discussion_r3307108634

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T12:39:25Z

- **pkg/cache/scheduler/tas_flavor.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T12:39:25Z
```diff
@@ -160,12 +194,33 @@ func (c *TASFlavorCache) updateUsage(topologyRequests []workload.TopologyDomainR
 		if !found {
 			c.usage[domainID] = resources.Requests{}
 		}
+		total := tr.TotalRequests()
+		podDelta := resources.Requests{corev1.ResourcePods: int64(tr.Count)}
 		if op == subtract {
-			c.usage[domainID].Sub(tr.TotalRequests())
-			c.usage[domainID].Sub(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Sub(total)
+			c.usage[domainID].Sub(podDelta)
 		} else {
-			c.usage[domainID].Add(tr.TotalRequests())
-			c.usage[domainID].Add(resources.Requests{corev1.ResourcePods: int64(tr.Count)})
+			c.usage[domainID].Add(total)
+			c.usage[domainID].Add(podDelta)
+		}
+		// For hostname-leaf topologies, mirror the delta to the shared
+		// SyncMap so sibling TASFlavorCache instances and Cache.Snapshot's
+		// clone path observe the cross-flavor aggregate. SyncMap.Update is
+		// atomic so concurrent sibling writers serialize on its lock.
+		if features.Enabled(features.TASHandleOverlappingFlavors) && c.topology.Usage != nil {
+			c.topology.Usage.Update(domainID, func(prev resources.Requests, _ bool) resources.Requests {
```

  Done.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T17:13:03Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:207** by [@mimowo](https://github.com/mimowo) — 2026-05-27T17:13:03Z
```diff
@@ -198,13 +199,36 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	var sharedAssumed map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
```

  Why is the sharedAssumed key-ed by the topologyName? I think it should not really matter, as long as two Pods use the same domain (node) which toplogyName they were assigned using, wdyt?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T17:24:47Z

- **pkg/scheduler/scheduler_tas_test.go:2452** by [@mimowo](https://github.com/mimowo) — 2026-05-27T17:24:47Z
```diff
@@ -2448,6 +2448,243 @@ func TestScheduleForTAS(t *testing.T) {
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
 			},
 		},
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel and
+		// Topology). tas-flavor-b has an extra NodeTaint that only the pending
```

  Do we have test case where the two ResourceFlavors overlap on nodes, but using different topology? Is that supported?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:31:36Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:31:36Z
```diff
@@ -198,13 +199,36 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	var sharedAssumed map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
```

  Yes, that's right. Actually, I was taking another approach in the POC phase.
  This `topoloyName` key style is not needed in this selected approach.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:33:39Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:33:39Z
```diff
@@ -198,13 +199,36 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	// One shared assumedUsage map per TopologyName for this workload. PodSets
+	// landing on different sibling flavors (same Topology, hostname leaf)
+	// reserve against the same map, preventing intra-workload self-overlap on
+	// a shared physical node. Cache-write-time aggregation does not cover
+	// in-flight reservations because pending workloads have not hit addUsage.
+	var sharedAssumed map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
```

  Done in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/fa0431dabfa18aaeca0d5e30fb2298f98bce6f85

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T17:36:37Z

- **pkg/cache/scheduler/snapshot.go:254** by [@mimowo](https://github.com/mimowo) — 2026-05-27T17:36:37Z
```diff
@@ -221,6 +232,30 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	return &snap, nil
 }
 
+// snapshotTopologyUsages takes snapshots from topologies[T].Usage
+// once per eligible Topology so every sibling-flavor snapshot for T aliases the same fresh map.
+// In-cycle mutations (preemption, fair-sharing) therefore propagate
+// cross-flavor but do not leak back to the live cache.
+func (c *Cache) snapshotTopologyUsages() map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests {
+	c.tasCache.RLock()
+	defer c.tasCache.RUnlock()
+
+	topologyUsages := make(map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests)
+	for topologyName, info := range c.tasCache.topologies {
+		if info.Usage == nil {
+			continue
+		}
+		clonedUsage := make(map[utiltas.TopologyDomainID]resources.Requests, info.Usage.Len())
+
+		info.Usage.Range(func(domainID utiltas.TopologyDomainID, req resources.Requests) bool {
+			clonedUsage[domainID] = req.Clone()
+			return true
+		})
+		topologyUsages[topologyName] = clonedUsage
```

  Here I'm also not sure why this code is tracking the usage per topologyName, conceptually I think we just have a cluster-wide TAS usage per domain, coming from admitted workloads.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:37:39Z

- **pkg/scheduler/scheduler_tas_test.go:2452** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:37:39Z
```diff
@@ -2448,6 +2448,243 @@ func TestScheduleForTAS(t *testing.T) {
 				utiltesting.MakeEventRecord("default", "foo", "Admitted", corev1.EventTypeNormal).Obj(),
 			},
 		},
+		// Two TAS ResourceFlavors share the same physical nodes (same NodeLabel and
+		// Topology). tas-flavor-b has an extra NodeTaint that only the pending
```

  We don't have such a test case because I didn't imagine such a case, as both resource flavors point to the same nodes. 
  
  If we support it, we allow them to assign a different topology to a single Node. It sounds curious from my PoV.
  
  Do you see the request from your side?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:56:21Z

- **pkg/features/kube_features.go:677** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:56:21Z
```diff
@@ -664,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
+	},
```

  As we discussed with @mimowo offline, we've decided to start this FG from the alpha phase.
  Because this change is slightly performance sensitive (like changing the usage calculation mechanism).
  
  So, we disable this by default.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-27T17:57:07Z

- **pkg/features/kube_features.go:677** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-27T17:57:06Z
```diff
@@ -664,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
+	},
```

  I will replace this feature phase with the alpha tomorrow.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T20:08:43Z

- **pkg/cache/scheduler/snapshot.go:207** by [@mimowo](https://github.com/mimowo) — 2026-05-27T20:08:43Z
```diff
@@ -192,8 +194,17 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		var sharedSnapshotUsage map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			sharedSnapshotUsage = c.snapshotTopologyUsages()
+		}
 		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.TASHandleOverlappingFlavors) && cache.topology.Usage != nil {
+				sharedUsage = sharedSnapshotUsage[cache.flavor.TopologyName]
+			}
+			tasSnapshots[flavor] = cache.snapshot(log, nodes, sharedUsage)
```

  IIUC the code aims computing globalTASUsage and pass it inside `snapshot`, is that correct? If so then I would call it globalTASUsage.
  
  Second, I think we may not have the whole usage at hand collected from all flavors when calling snapshot as we are building it greedily.
  
  Maybe I'm missing something but I'm wondering if we could make it more readable by the two-pass approach:
  
  0. tasCacheClone = c.tasCache.Clone()
  1. compute the aggregated globalTASUsage based on  tasCacheClone
  2. trim the globalTASUsage to the flavor based on `nodes`
  
  This way I think we can signifficantly simplify:
  - `snapshotTopologyUsages` does not need to track per topologyName
  - `Usage` could be dropped under topologyInformation
  - https://github.com/kubernetes-sigs/kueue/pull/11210/changes#diff-aa793cfc2528f289b2b59725f714db6e6f6b2c6e9edf4c75925e656e0cc055b7R206-R223 - could be dropped

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T20:10:26Z

- **pkg/features/kube_features.go:677** by [@mimowo](https://github.com/mimowo) — 2026-05-27T20:10:26Z
```diff
@@ -664,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
+	},
```

  If I'm not mistaken in this comment https://github.com/kubernetes-sigs/kueue/pull/11210/changes#r3313550771 then we can signifficantly simplify the code. If this is the case then I'm open to re-consider Beta.
  
  I was just hesitant going streight to beta seeing the complications. I'm happy to also sync if I'm missing something.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T04:42:10Z

- **pkg/cache/scheduler/snapshot.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T04:42:10Z
```diff
@@ -192,8 +194,17 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		var sharedSnapshotUsage map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			sharedSnapshotUsage = c.snapshotTopologyUsages()
+		}
 		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.TASHandleOverlappingFlavors) && cache.topology.Usage != nil {
+				sharedUsage = sharedSnapshotUsage[cache.flavor.TopologyName]
+			}
+			tasSnapshots[flavor] = cache.snapshot(log, nodes, sharedUsage)
```

  Yes, we can simplify code by using the alternative approach. Indeed, I implemented a similar one as my PoC PR: https://github.com/kubernetes-sigs/kueue/pull/10688
  
  But, this has scheduling performance degrading risks, so I am moving this #11210 approach.
  
  #10688 has to aggregate all usages at snapshot timing, which takes a longer time than computing aggregation at usage change timing. It could lead to longer TAS scheduling duration every scheduling cycle because #10688 doesn't preserve aggregation information anywhere.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T04:46:52Z

- **pkg/cache/scheduler/snapshot.go:207** by [@mimowo](https://github.com/mimowo) — 2026-05-28T04:46:51Z
```diff
@@ -192,8 +194,17 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		var sharedSnapshotUsage map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			sharedSnapshotUsage = c.snapshotTopologyUsages()
+		}
 		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.TASHandleOverlappingFlavors) && cache.topology.Usage != nil {
+				sharedUsage = sharedSnapshotUsage[cache.flavor.TopologyName]
+			}
+			tasSnapshots[flavor] = cache.snapshot(log, nodes, sharedUsage)
```

  But the aggregation does not increase asymptotic complexity, because we need to copy the usage for all flavors anyway. It just has another pass to aggregate the data. I think it is also using the same memory.
  
  I think I'm more worried about performance regressions due to a bug due to a too complicated code.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T04:59:19Z

- **pkg/cache/scheduler/snapshot.go:207** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T04:59:18Z
```diff
@@ -192,8 +194,17 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		var sharedSnapshotUsage map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			sharedSnapshotUsage = c.snapshotTopologyUsages()
+		}
 		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.TASHandleOverlappingFlavors) && cache.topology.Usage != nil {
+				sharedUsage = sharedSnapshotUsage[cache.flavor.TopologyName]
+			}
+			tasSnapshots[flavor] = cache.snapshot(log, nodes, sharedUsage)
```

  > But the aggregation does not increase asymptotic complexity,
  
  Yes, for each scheduling cycle, but from the sum of scheduling cycles during the arbitrary time PoV, it will be different because #11210 can reuse already computed aggregation information.
  
  > I think I'm more worried about performance regressions due to a bug due to a too complicated code.
  
  Isn't it always? Adding any computing mechanism to snapshotting timing is easy because we can just add our business logic, but the cache mechanism's advantage will not be performed. 
  
  The feature gate is responsible for guarding Kueue by breaking existing behavior. So, I think that we can avoid worrying about bugs.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T05:12:16Z

- **pkg/cache/scheduler/snapshot.go:207** by [@mimowo](https://github.com/mimowo) — 2026-05-28T05:12:16Z
```diff
@@ -192,8 +194,17 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
+		var sharedSnapshotUsage map[kueue.TopologyReference]map[utiltas.TopologyDomainID]resources.Requests
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			sharedSnapshotUsage = c.snapshotTopologyUsages()
+		}
 		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+			nodes := c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels)
+			var sharedUsage map[utiltas.TopologyDomainID]resources.Requests
+			if features.Enabled(features.TASHandleOverlappingFlavors) && cache.topology.Usage != nil {
+				sharedUsage = sharedSnapshotUsage[cache.flavor.TopologyName]
+			}
+			tasSnapshots[flavor] = cache.snapshot(log, nodes, sharedUsage)
```

  > Yes, for each scheduling cycle, but from the sum of scheduling cycles during the arbitrary time PoV, it will be different because https://github.com/kubernetes-sigs/kueue/pull/11210 can reuse already computed aggregation information.
  
  Maybe, but it still does not improve the asymptotic complexity of a single cycle.
  
  > Isn't it always? Adding any computing mechanism to snapshotting timing is easy because we can just add our business logic, but the cache mechanism's advantage will not be performed.
  
  I want to avoid premature optimization. My gut feeling is that in this case we really don't gain much by the optimization given how much TAS is processing overall. 
  
  I would start simple, and complicate code when we prove this is a bottlneck. One idea to prove would be to run our tas perf tests with CPU profiling enabled and confirm the results. I don't expect visible difference. 
  
  I know it is late for the experiments in this cycle, so I still propose simple Alpha, and we can consider more sothisticated code for Beta if we prove bottlneck.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T16:11:04Z

- **pkg/cache/scheduler/tas_flavor.go:153** by [@mimowo](https://github.com/mimowo) — 2026-05-28T16:11:04Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
+			}
+			snapshot.leaves[domainID].tasUsage = aggregatedResources
+		}
 	}
 	snapshot.initialize()
-	for domainID, usage := range c.usage {
-		snapshot.addTASUsage(domainID, usage)
+	if aggregatedDomainUsages == nil || !features.Enabled(features.TASHandleOverlappingFlavors) {
+		for domainID, usage := range c.usage {
+			snapshot.addTASUsage(domainID, usage)
+		}
```

  I'm wondering if we could commonize adding the TAS usage to snaphot if we introduced a variable like
  
  tasUsageToAdd := c.usage
  if features.Enabled(features.TASHandleOverlappingFlavors)  {
    tasUsageToAdd = // some conputation 
  }
  snapshot.initialize()
  
  for domainID, usage := tasUsageToAdd {
  	snapshot.addTASUsage(domainID, usage)
  }

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T16:12:48Z

- **pkg/cache/scheduler/tas_flavor.go:144** by [@mimowo](https://github.com/mimowo) — 2026-05-28T16:12:49Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
```

  Why mutating the map in here, it would be cleaner if we could fully pre-compute it before calling snapshot(), is there some issue with that?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T16:16:23Z

- **pkg/cache/scheduler/tas_flavor.go:153** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T16:16:23Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
+			}
+			snapshot.leaves[domainID].tasUsage = aggregatedResources
+		}
 	}
 	snapshot.initialize()
-	for domainID, usage := range c.usage {
-		snapshot.addTASUsage(domainID, usage)
+	if aggregatedDomainUsages == nil || !features.Enabled(features.TASHandleOverlappingFlavors) {
+		for domainID, usage := range c.usage {
+			snapshot.addTASUsage(domainID, usage)
+		}
```

  Sorry, I wasn't sure where do we want to commonize?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T16:19:59Z

- **pkg/cache/scheduler/tas_flavor.go:153** by [@mimowo](https://github.com/mimowo) — 2026-05-28T16:19:59Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
+			}
+			snapshot.leaves[domainID].tasUsage = aggregatedResources
+		}
 	}
 	snapshot.initialize()
-	for domainID, usage := range c.usage {
-		snapshot.addTASUsage(domainID, usage)
+	if aggregatedDomainUsages == nil || !features.Enabled(features.TASHandleOverlappingFlavors) {
+		for domainID, usage := range c.usage {
+			snapshot.addTASUsage(domainID, usage)
+		}
```

  I mean this two paths:
  
  1. new: `snapshot.leaves[domainID].tasUsage = aggregatedResources` L146
  2. old `snapshot.addTASUsage(domainID, usage)` L152
  
  Because IIUC they both do the same just slightly differently. So I wanted to reuse the pre-existing code, just feed it with `tasUsageToAdd` rather than c.usage

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T16:28:22Z

- **pkg/cache/scheduler/tas_flavor.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T16:28:22Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
```

  Good call.
  I think that we can pre-compute. This was preserved from my previous PoC codes.
  So, there is really no need.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T16:46:54Z

- **pkg/cache/scheduler/tas_flavor.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T16:46:54Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
```

  I moved this node calculation to `snapshot()` in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/f68c1c75f1734506386b4509f471261bfe866581
  
  Please let me know if I am still missing anything, thanks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T16:51:19Z

- **pkg/cache/scheduler/tas_flavor.go:153** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T16:51:19Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
+			}
+			snapshot.leaves[domainID].tasUsage = aggregatedResources
+		}
 	}
 	snapshot.initialize()
-	for domainID, usage := range c.usage {
-		snapshot.addTASUsage(domainID, usage)
+	if aggregatedDomainUsages == nil || !features.Enabled(features.TASHandleOverlappingFlavors) {
+		for domainID, usage := range c.usage {
+			snapshot.addTASUsage(domainID, usage)
+		}
```

  I see, that makes sense. Let me try to consolidate such duplicated mechanisms into one.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T16:53:29Z

- **pkg/cache/scheduler/tas_flavor.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T16:53:28Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
```

  After https://github.com/kubernetes-sigs/kueue/pull/11210#discussion_r3319204994 change, we might not need this change. Let me check what we should do.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:03:39Z

- **pkg/cache/scheduler/tas_flavor.go:153** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:03:38Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
+			}
+			snapshot.leaves[domainID].tasUsage = aggregatedResources
+		}
 	}
 	snapshot.initialize()
-	for domainID, usage := range c.usage {
-		snapshot.addTASUsage(domainID, usage)
+	if aggregatedDomainUsages == nil || !features.Enabled(features.TASHandleOverlappingFlavors) {
+		for domainID, usage := range c.usage {
+			snapshot.addTASUsage(domainID, usage)
+		}
```

  I addressed this in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/a07fa048ddf56b4652f7ccfd84a86173e32a562b

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:03:49Z

- **pkg/cache/scheduler/tas_flavor.go:144** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:03:49Z
```diff
@@ -115,19 +116,41 @@ func (c *TASFlavorCache) TopologyLevels() []string {
 	return c.topology.Levels
 }
 
-func (c *TASFlavorCache) snapshot(log logr.Logger, nodes []*nodeInfo) *TASFlavorSnapshot {
+func (c *TASFlavorCache) snapshot(
+	log logr.Logger, nodes []*nodeInfo, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) *TASFlavorSnapshot {
 	c.RLock()
 	defer c.RUnlock()
-	log.V(3).Info("Constructing TAS snapshot", "nodeLabels", c.flavor.NodeLabels,
-		"levels", c.topology.Levels, "nodeCount", len(nodes))
+
+	infoKV := []any{
+		"nodeLabels", c.flavor.NodeLabels,
+		"levels", c.topology.Levels,
+		"nodeCount", len(nodes),
+	}
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		infoKV = append(infoKV, "crossFlavorAggregation", aggregatedDomainUsages != nil)
+	}
+	log.V(3).Info("Constructing TAS snapshot", infoKV...)
+
 	snapshot := newTASFlavorSnapshot(log, c.flavor.TopologyName, c.topology.Levels, c.flavor.Tolerations)
 	nodeToDomain := make(map[string]utiltas.TopologyDomainID)
 	for _, node := range nodes {
-		nodeToDomain[node.Name] = snapshot.addNode(node)
+		domainID := snapshot.addNode(node)
+		nodeToDomain[node.Name] = domainID
+		if features.Enabled(features.TASHandleOverlappingFlavors) && aggregatedDomainUsages != nil {
+			aggregatedResources, ok := aggregatedDomainUsages[domainID]
+			if !ok {
+				aggregatedResources = resources.Requests{}
+				aggregatedDomainUsages[domainID] = aggregatedResources
```

  https://github.com/kubernetes-sigs/kueue/pull/11210/commits/a07fa048ddf56b4652f7ccfd84a86173e32a562b is the final version.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T17:31:44Z

- **pkg/cache/scheduler/snapshot.go:251** by [@mimowo](https://github.com/mimowo) — 2026-05-28T17:31:44Z
```diff
@@ -221,6 +236,24 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	return &snap, nil
 }
 
+func (c *Cache) snapshotTopologyDomainUsages(
+	tasFlvCache *TASFlavorCache, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) {
+	tasFlvCache.RLock()
+	defer tasFlvCache.RUnlock()
+
+	if len(tasFlvCache.topology.Levels) == 0 || !utiltas.IsLowestLevelHostname(tasFlvCache.topology.Levels) {
+		return
+	}
+	for domainID, domainUsage := range tasFlvCache.usage {
+		if _, ok := aggregatedDomainUsages[domainID]; ok {
+			aggregatedDomainUsages[domainID].Add(domainUsage)
+			continue
```

  nit, this continue is a bit tricky to follow, maybe instead use `else` for the outer "if"?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T17:33:01Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@mimowo](https://github.com/mimowo) — 2026-05-28T17:33:01Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  This code looks copy-pasted in a couple places, let's commonize

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T17:33:56Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:221** by [@mimowo](https://github.com/mimowo) — 2026-05-28T17:33:56Z
```diff
@@ -198,15 +199,26 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	var aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		aggregatedDomainUsages = make(map[utiltas.TopologyDomainID]resources.Requests)
+	}
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if features.Enabled(features.TASHandleOverlappingFlavors) && tasFlavorCache.isLowestLevelNode {
+			flvOpts = append(slices.Clone(options), WithAggregatedDomainUsages(aggregatedDomainUsages))
+		}
+		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, flvOpts...)
 		maps.Copy(result, flvResult)
 	}
+
```

  nit, drop empty line insertion

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:34:03Z

- **pkg/cache/scheduler/snapshot.go:251** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:34:03Z
```diff
@@ -221,6 +236,24 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	return &snap, nil
 }
 
+func (c *Cache) snapshotTopologyDomainUsages(
+	tasFlvCache *TASFlavorCache, aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests,
+) {
+	tasFlvCache.RLock()
+	defer tasFlvCache.RUnlock()
+
+	if len(tasFlvCache.topology.Levels) == 0 || !utiltas.IsLowestLevelHostname(tasFlvCache.topology.Levels) {
+		return
+	}
+	for domainID, domainUsage := range tasFlvCache.usage {
+		if _, ok := aggregatedDomainUsages[domainID]; ok {
+			aggregatedDomainUsages[domainID].Add(domainUsage)
+			continue
```

  I fixed this in https://github.com/kubernetes-sigs/kueue/compare/f5f810f6dc7a8778df9985ac1cfe2f9aca8f91f4..940ab1232aff067991bc5f9c2665d6e6805ccb37

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:34:37Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:34:36Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  Yes, right. Are you ok to introduce prod commonized codes to UT?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T17:35:44Z

- **pkg/util/maps/maps.go:154** by [@mimowo](https://github.com/mimowo) — 2026-05-28T17:35:44Z
```diff
@@ -132,3 +132,24 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+// Range calls f for each key-value pair under the read lock. f must not
+// invoke any SyncMap method that acquires the write lock (Add, Update,
+// Delete, Swap) on the same instance. The sync.RWMutex is not re-entrant
+// and such a call will deadlock.
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
+	dwc.lock.RLock()
+	defer dwc.lock.RUnlock()
+	for k, v := range dwc.m {
+		if !f(k, v) {
+			return
+		}
+	}
+}
+
+func (dwc *SyncMap[K, V]) Update(k K, fn func(prev V, exists bool) V) {
+	dwc.lock.Lock()
+	defer dwc.lock.Unlock()
+	prev, exists := dwc.m[k]
+	dwc.m[k] = fn(prev, exists)
```

  Are these in use in the new impl? If not then drop

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:36:03Z

- **pkg/util/maps/maps.go:154** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:36:03Z
```diff
@@ -132,3 +132,24 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+// Range calls f for each key-value pair under the read lock. f must not
+// invoke any SyncMap method that acquires the write lock (Add, Update,
+// Delete, Swap) on the same instance. The sync.RWMutex is not re-entrant
+// and such a call will deadlock.
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
+	dwc.lock.RLock()
+	defer dwc.lock.RUnlock()
+	for k, v := range dwc.m {
+		if !f(k, v) {
+			return
+		}
+	}
+}
+
+func (dwc *SyncMap[K, V]) Update(k K, fn func(prev V, exists bool) V) {
+	dwc.lock.Lock()
+	defer dwc.lock.Unlock()
+	prev, exists := dwc.m[k]
+	dwc.m[k] = fn(prev, exists)
```

  No need. That is dead code. Let's remove it.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:37:39Z

- **pkg/cache/scheduler/clusterqueue_snapshot.go:221** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:37:39Z
```diff
@@ -198,15 +199,26 @@ func (c *ClusterQueueSnapshot) FindTopologyAssignmentsForWorkload(
 		option(opts)
 	}
 
+	var aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests
+	if features.Enabled(features.TASHandleOverlappingFlavors) {
+		aggregatedDomainUsages = make(map[utiltas.TopologyDomainID]resources.Requests)
+	}
+
 	result := make(TASAssignmentsResult)
-	for tasFlavor, flavorTASRequests := range tasRequestsByFlavor {
+	for _, tasFlavor := range slices.Sorted(maps.Keys(tasRequestsByFlavor)) {
+		flavorTASRequests := tasRequestsByFlavor[tasFlavor]
 		// We assume the `tasFlavor` is already in the snapshot as this was
 		// already checked earlier during flavor assignment, and the set of
 		// flavors is immutable in snapshot.
 		tasFlavorCache := c.TASFlavors[tasFlavor]
-		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, options...)
+		flvOpts := options
+		if features.Enabled(features.TASHandleOverlappingFlavors) && tasFlavorCache.isLowestLevelNode {
+			flvOpts = append(slices.Clone(options), WithAggregatedDomainUsages(aggregatedDomainUsages))
+		}
+		flvResult := tasFlavorCache.FindTopologyAssignmentsForFlavor(flavorTASRequests, flvOpts...)
 		maps.Copy(result, flvResult)
 	}
+
```

  removed in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/bbd278f9cff14f9b9ac95859482b0eb6bbd8326c

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T17:38:42Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@mimowo](https://github.com/mimowo) — 2026-05-28T17:38:42Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  yes, why not.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:39:03Z

- **pkg/util/maps/maps.go:154** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:39:03Z
```diff
@@ -132,3 +132,24 @@ func (dwc *SyncMap[K, V]) Swap(k K, v V) (V, bool) {
 	dwc.m[k] = v
 	return old, existed
 }
+
+// Range calls f for each key-value pair under the read lock. f must not
+// invoke any SyncMap method that acquires the write lock (Add, Update,
+// Delete, Swap) on the same instance. The sync.RWMutex is not re-entrant
+// and such a call will deadlock.
+func (dwc *SyncMap[K, V]) Range(f func(key K, value V) bool) {
+	dwc.lock.RLock()
+	defer dwc.lock.RUnlock()
+	for k, v := range dwc.m {
+		if !f(k, v) {
+			return
+		}
+	}
+}
+
+func (dwc *SyncMap[K, V]) Update(k K, fn func(prev V, exists bool) V) {
+	dwc.lock.Lock()
+	defer dwc.lock.Unlock()
+	prev, exists := dwc.m[k]
+	dwc.m[k] = fn(prev, exists)
```

  Removed those in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/05e0d2754e9252fc41b9209b2bb1a2b19a5e7e83

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T17:42:48Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T17:42:47Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  just checked that. let me confirm how we can commonize those into comprehensive func.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:01:30Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:01:30Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  Uhm, it seems that it's not easy to commonize this mechanism in prod code.
  If we want to commonize this, we should have test specific util library.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:05:04Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@mimowo](https://github.com/mimowo) — 2026-05-28T18:05:04Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  We have util for package for TAS, maybe place it there, or at least commonize the two similar places in unit tests

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:11:04Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:11:04Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  The core reason why this is just a copy-paste style is `TestFindTopologyAssignmentsMultiLayerReplacement` and `TestFindTopologyAssignments` are the mostly same. `TestFindTopologyAssignmentsMultiLayerReplacement` has just enabled specific FG.
  
  https://github.com/kubernetes-sigs/kueue/blob/851a097d19c4326bdfdf0276492625e17a08495e/pkg/cache/scheduler/tas_cache_test.go#L7206
  
  https://github.com/kubernetes-sigs/kueue/blob/851a097d19c4326bdfdf0276492625e17a08495e/pkg/cache/scheduler/tas_cache_test.go#L56

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:13:00Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:13:00Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  > We have util for package for TAS, maybe place it there, or at least commonize the two similar places in unit tests
  
  That meant that we have to consolidate `TestFindTopologyAssignmentsMultiLayerReplacement` and `TestFindTopologyAssignments` into one...

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:13:30Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@mimowo](https://github.com/mimowo) — 2026-05-28T18:13:30Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  I see , we can keep it for follow up, but maybe we could consolidate the two tests?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:16:05Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:16:05Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  My proposal is that 
  
  1) adding a temporarily consolidated function for line 7685-7694 in this file, then
  2) consolidate TestFindTopologyAssignmentsMultiLayerReplacement and TestFindTopologyAssignments into one
  3) In the same PR as 2), we should remove 1) the tmp commonized function.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:17:36Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@mimowo](https://github.com/mimowo) — 2026-05-28T18:17:36Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  Sgtm

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:49:41Z

- **pkg/cache/scheduler/tas_cache_test.go:7694** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:49:40Z
```diff
@@ -7535,12 +7676,24 @@ func TestFindTopologyAssignmentsMultiLayerReplacement(t *testing.T) {
 			if tcLevels == nil {
 				tcLevels = defaultLevels
 			}
-			tasFlavorCache := tasCache.NewTASFlavorCache(
-				topologyInformation{Levels: tcLevels},
-				flavorInformation{TopologyName: "default"},
-			)
 
-			snapshot := tasFlavorCache.snapshot(log, tasCache.nodesCache.find(tasFlavorCache.flavor.NodeLabels, tasFlavorCache.topology.Levels))
+			topologyInfo := topologyInformation{Levels: tcLevels}
+			flvInfo := flavorInformation{TopologyName: "default"}
+
+			tasFlavorCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+
+			if features.Enabled(features.TASHandleOverlappingFlavors) && len(tc.priorFlavorUsage) > 0 {
+				siblingCache := tasCache.NewTASFlavorCache(topologyInfo, flvInfo)
+				siblingCache.addUsage("prior-wl", tc.priorFlavorUsage)
+				if tc.aggregatedDomainUsages == nil {
+					tc.aggregatedDomainUsages = make(map[tas.TopologyDomainID]resources.Requests, len(siblingCache.usage))
+				}
+				for domainID, usage := range siblingCache.usage {
+					tc.aggregatedDomainUsages[domainID] = usage.Clone()
+				}
+			}
```

  done in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/d22e18bdae2e368b985fd6963fb8a0869e87139b

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T18:50:39Z

- **pkg/features/kube_features.go:677** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T18:50:39Z
```diff
@@ -675,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  I disabled this FG by default in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/ae1db16ca849bff7a47b1435f76146935f4a937d

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:54:47Z

- **pkg/cache/scheduler/snapshot.go:206** by [@mimowo](https://github.com/mimowo) — 2026-05-28T18:54:47Z
```diff
@@ -192,8 +194,27 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		var aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests
+		flvTASCache := c.tasCache.Clone()
+
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			aggregatedDomainUsages = make(map[utiltas.TopologyDomainID]resources.Requests)
+			for _, cache := range flvTASCache {
+				c.snapshotTopologyDomainUsages(cache, aggregatedDomainUsages)
+			}
+		}
```

  
  Maybe we could add here a log line, so that users can tell how long roughly different phases of TAS take
  
  log.V(4).Info("Aggregated TAS usage across flavors")

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:55:58Z

LGTM, just left a small comment to consider enriching the logs for the new code.

Seeing how much the code is simplified I'm good to go to Beta in 0.18. I'm also ok to cherrypick, probably as alpha.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T18:57:19Z

- **pkg/features/kube_features.go:677** by [@mimowo](https://github.com/mimowo) — 2026-05-28T18:57:19Z
```diff
@@ -675,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  Ok, I just said that I'm ok with Beta in 0.18 https://github.com/kubernetes-sigs/kueue/pull/11210#pullrequestreview-4383838161
  
  I'm happy of the current code shape, up to you. I would just like to keep it alpha if for the release branches so that we can have more time before it lands on prod actually.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T19:03:23Z

- **pkg/cache/scheduler/snapshot.go:206** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T19:03:23Z
```diff
@@ -192,8 +194,27 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	}
 	tasSnapshots := make(map[kueue.ResourceFlavorReference]*TASFlavorSnapshot)
 	if features.Enabled(features.TopologyAwareScheduling) {
-		for flavor, cache := range c.tasCache.Clone() {
-			tasSnapshots[flavor] = cache.snapshot(log, c.tasCache.nodesCache.find(cache.flavor.NodeLabels, cache.topology.Levels))
+		var aggregatedDomainUsages map[utiltas.TopologyDomainID]resources.Requests
+		flvTASCache := c.tasCache.Clone()
+
+		if features.Enabled(features.TASHandleOverlappingFlavors) {
+			aggregatedDomainUsages = make(map[utiltas.TopologyDomainID]resources.Requests)
+			for _, cache := range flvTASCache {
+				c.snapshotTopologyDomainUsages(cache, aggregatedDomainUsages)
+			}
+		}
```

  Added in https://github.com/kubernetes-sigs/kueue/pull/11210/commits/c7efbca3d9129caee9f53c27ee13a66959100ac7

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-28T19:04:39Z

- **pkg/features/kube_features.go:677** by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-28T19:04:39Z
```diff
@@ -675,10 +669,12 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	WorkloadPriorityClassDefaulting: {
 		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
 	},
-
 	MetricsForCohorts: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	TASHandleOverlappingFlavors: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Alpha},
+	},
```

  I turned this TASHandleOverlappingFlavors to Beta in this branch.
  
  I will manually open cherry-pick PRs to make TASHandleOverlappingFlavors Alpha after this PR is mergd.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-28T19:09:06Z

/lgtm
/approve
Thank you for closing the long-standing gap in TAS 👍  I propose to CP as alpha being careful about the prod use of TAS.
