# PR #11242: kep: update graduation criteria

**Summary**: kep: update graduation criteria

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11242

**Last updated**: 2026-05-22T17:14:48Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `update-dynamic-sized-jobs-kep` → `main`
- **Created**: 2026-05-15T15:08:02Z
- **Updated**: 2026-05-22T17:14:48Z
- **Merged**: 2026-05-22T17:14:45Z
- **Closed**: 2026-05-22T17:14:45Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Changed files**: 1   **+35 / -27**

## Description

Update KEP-77 graduation criteria based on #8651 discussion

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes https://github.com/kubernetes-sigs/kueue/issues/8651#issuecomment-4380241502

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-15T15:08:11Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 60bb6d5d96b9475671c9c771dae5cb710309fef8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0b84d6d5d4250008f71473 |

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-15T15:50:18Z

/cc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-19T13:31:38Z

LGTM label has been added.  <details>Git tree hash: 5ea0e89ec39db8120483a03e0fe28690713c7cd5</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-20T17:23:50Z

> /lgtm /approve Thank you for refreshing the graduation criteria. I think this plan for Beta looks sensible and achievable 👍
> 
> /hold In case @tenzen-y has extra comments.

Ack 
This will be scheduled into tomorrow

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-22T16:24:45Z

/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-22T16:24:47Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11242#pullrequestreview-4319409744" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11242#" title="Author self-approved">sohankunkerkar</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11242#pullrequestreview-4347123860" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-05-15T15:11:58Z

## Pull request overview

Updates KEP-77 (“Dynamically Sized Jobs”) graduation criteria to better scope Beta/GA requirements for `ElasticJobsViaWorkloadSlices`, and introduces separate graduation criteria for the TAS-gated integration (`ElasticJobsViaWorkloadSlicesWithTAS`) based on the #8651 discussion.

**Changes:**
- Refines Beta graduation criteria to emphasize validation, observability, and controller readiness, while marking a few items as completed.
- Reframes GA graduation criteria to include broader ecosystem enablement, GC expectations, and re-evaluation items.
- Adds a dedicated “`ElasticJobsViaWorkloadSlicesWithTAS` Graduation Criteria” section with Alpha/Beta milestones for TAS integration.





---

💡 <a href="/kubernetes-sigs/kueue/new/main?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **keps/77-dynamically-sized-jobs/README.md:551** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-15T15:11:57Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
```

  The term "elastic annotation" is ambiguous here. Earlier in this document the opt-in is explicitly `kueue.x-k8s.io/elastic-job: "true"`; consider naming that exact annotation key (and/or linking back to it) so readers know what must be validated.

- **keps/77-dynamically-sized-jobs/README.md:552** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-15T15:11:57Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing PodSchedulingReadiness gate on admitted workload slices.
```

  "PodSchedulingReadiness gate" is misleading terminology here: the pod field being cleared is `spec.schedulingGates` (PodSchedulingGates), while PodSchedulingReadiness is the Kubernetes feature name. Consider rephrasing to "removing PodSchedulingGates / scheduling gates" to match the rest of this KEP (e.g., the Alpha bullet referencing `PodSchedulingGates`).

- **keps/77-dynamically-sized-jobs/README.md:592** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-15T15:11:58Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing PodSchedulingReadiness gate on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
+* [ ] Slice preemption consistently handled and visible in workload status conditions.
+* [ ] All core controllers (scheduler, preemptor, queue manager) validated under slice-enabled workloads.
+* [x] Old workload slice finished only after replacement slice is admitted.
 
 #### GA (Stable)
 
-* [ ] Proven stability under production-scale workloads, verified through internal deployments or community reports.
-* [ ] Full backwards compatibility: workloads that do not opt into `WorkloadSlice` continue to function identically.
-* [ ] API guarantees around slice naming, preemption markers, and flavor enforcement are documented and stable.
-* [ ] No known correctness issues across single- and multi-cluster environments.
-* [ ] User-configurable policies (optional) for flavor migration or slice aggregation behavior are validated.
 * [ ] Feature is permanently enabled and no longer gated.
-* [ ] Associated documentation, examples, and operational best practices are published as part of the GA release.
+* [ ] Proven stability under production-scale workloads.
+* [ ] Full backwards compatibility for workloads that do not opt into WorkloadSlice.
+* [ ] API guarantees around slice naming, preemption markers, and flavor enforcement documented and stable.
+* [ ] No known correctness issues across single- and multi-cluster environments.
+* [ ] Documentation, examples, and operational best practices published.
+* [ ] Dynamic resizing enabled for all Kueue-managed workloads (JobSet, Kubeflow jobs, etc.).
+* [ ] Garbage collection of finished workload slices.
+* [ ] Re-evaluate the WorkloadSlice implementation considering alternatives ([#5897](https://github.com/kubernetes-sigs/kueue/issues/5897)).
+* [ ] Re-evaluate PartialAdmission and Elastic combination.
+* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
+* [ ] Re-evaluate MultiKueue JobAdapter Sync routine for elastic-specific functionality ([#7065](https://github.com/kubernetes-sigs/kueue/issues/7065)).
+* [ ] Re-evaluate cache-level quota accounting during slice replacement to avoid temporary double-counting.
+
+### `ElasticJobsViaWorkloadSlicesWithTAS` Graduation Criteria
+
+TAS integration is gated separately via `ElasticJobsViaWorkloadSlicesWithTAS`
+(requires both `ElasticJobsViaWorkloadSlices` and `TopologyAwareScheduling`).
+
+#### Alpha
+
+* [x] Unconstrained topology mode supported for scale-up.
+* [x] Webhook validation to reject elastic jobs with required/preferred topology annotations.
+* [x] Integration tests for elastic workloads with unconstrained topology.
+
+#### Beta
 
+* [ ] Re-evaluate in production-like environments with scale-up/scale-down cycles.
+* [ ] Re-evaluate full integration with required/preferred topology modes.
+* [ ] Re-evaluate handling scale-ups and scale-downs during node repairing.
```

  These Beta graduation criteria use "Re-evaluate" wording, which doesn't define an objective completion condition. For graduation criteria, consider using measurable outcomes (e.g., "Validated in production-like environments with scale-up/scale-down cycles" / "Either supports required/preferred modes or explicitly rejects them") so it's clear when Beta is achieved.

- **keps/77-dynamically-sized-jobs/README.md:592** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-15T15:11:58Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing PodSchedulingReadiness gate on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
+* [ ] Slice preemption consistently handled and visible in workload status conditions.
+* [ ] All core controllers (scheduler, preemptor, queue manager) validated under slice-enabled workloads.
+* [x] Old workload slice finished only after replacement slice is admitted.
 
 #### GA (Stable)
 
-* [ ] Proven stability under production-scale workloads, verified through internal deployments or community reports.
-* [ ] Full backwards compatibility: workloads that do not opt into `WorkloadSlice` continue to function identically.
-* [ ] API guarantees around slice naming, preemption markers, and flavor enforcement are documented and stable.
-* [ ] No known correctness issues across single- and multi-cluster environments.
-* [ ] User-configurable policies (optional) for flavor migration or slice aggregation behavior are validated.
 * [ ] Feature is permanently enabled and no longer gated.
-* [ ] Associated documentation, examples, and operational best practices are published as part of the GA release.
+* [ ] Proven stability under production-scale workloads.
+* [ ] Full backwards compatibility for workloads that do not opt into WorkloadSlice.
+* [ ] API guarantees around slice naming, preemption markers, and flavor enforcement documented and stable.
+* [ ] No known correctness issues across single- and multi-cluster environments.
+* [ ] Documentation, examples, and operational best practices published.
+* [ ] Dynamic resizing enabled for all Kueue-managed workloads (JobSet, Kubeflow jobs, etc.).
+* [ ] Garbage collection of finished workload slices.
+* [ ] Re-evaluate the WorkloadSlice implementation considering alternatives ([#5897](https://github.com/kubernetes-sigs/kueue/issues/5897)).
+* [ ] Re-evaluate PartialAdmission and Elastic combination.
+* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
+* [ ] Re-evaluate MultiKueue JobAdapter Sync routine for elastic-specific functionality ([#7065](https://github.com/kubernetes-sigs/kueue/issues/7065)).
+* [ ] Re-evaluate cache-level quota accounting during slice replacement to avoid temporary double-counting.
+
+### `ElasticJobsViaWorkloadSlicesWithTAS` Graduation Criteria
+
+TAS integration is gated separately via `ElasticJobsViaWorkloadSlicesWithTAS`
+(requires both `ElasticJobsViaWorkloadSlices` and `TopologyAwareScheduling`).
+
+#### Alpha
+
+* [x] Unconstrained topology mode supported for scale-up.
+* [x] Webhook validation to reject elastic jobs with required/preferred topology annotations.
+* [x] Integration tests for elastic workloads with unconstrained topology.
+
+#### Beta
 
+* [ ] Re-evaluate in production-like environments with scale-up/scale-down cycles.
+* [ ] Re-evaluate full integration with required/preferred topology modes.
+* [ ] Re-evaluate handling scale-ups and scale-downs during node repairing.
```

  The phrase "node repairing" reads awkwardly; consider changing it to "node repair" (or another clearer term like "node remediation").

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-15T15:12:53Z

cc @ichekrygin @hiboyang @yaroslava-serdiuk  ptal

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T09:09:39Z

- **keps/77-dynamically-sized-jobs/README.md:565** by [@mimowo](https://github.com/mimowo) — 2026-05-18T09:09:39Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing scheduling gates on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
+* [ ] Slice preemption consistently handled and visible in workload status conditions.
+* [ ] All core controllers (scheduler, preemptor, queue manager) validated under slice-enabled workloads.
+* [x] Old workload slice finished only after replacement slice is admitted.
 
 #### GA (Stable)
 
-* [ ] Proven stability under production-scale workloads, verified through internal deployments or community reports.
-* [ ] Full backwards compatibility: workloads that do not opt into `WorkloadSlice` continue to function identically.
-* [ ] API guarantees around slice naming, preemption markers, and flavor enforcement are documented and stable.
-* [ ] No known correctness issues across single- and multi-cluster environments.
-* [ ] User-configurable policies (optional) for flavor migration or slice aggregation behavior are validated.
 * [ ] Feature is permanently enabled and no longer gated.
-* [ ] Associated documentation, examples, and operational best practices are published as part of the GA release.
+* [ ] Proven stability under production-scale workloads.
+* [ ] Full backwards compatibility for workloads that do not opt into WorkloadSlice.
```

  I don't think we need to list that specifically actually, Backwards compatibility is the "implicit" requirement for all features, unless some futures make it very specific otherwise.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T09:11:00Z

- **keps/77-dynamically-sized-jobs/README.md:558** by [@mimowo](https://github.com/mimowo) — 2026-05-18T09:11:00Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing scheduling gates on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
+* [ ] Slice preemption consistently handled and visible in workload status conditions.
+* [ ] All core controllers (scheduler, preemptor, queue manager) validated under slice-enabled workloads.
```

  That is a bit vague " All core controllers (scheduler, preemptor, queue manager) validated under slice-enabled workloads." with unclear criteria. I would just drop it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T09:11:58Z

- **keps/77-dynamically-sized-jobs/README.md:557** by [@mimowo](https://github.com/mimowo) — 2026-05-18T09:11:58Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing scheduling gates on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
+* [ ] Slice preemption consistently handled and visible in workload status conditions.
```

  What is actually behind this point, "Slice preemption consistently handled and visible in workload status conditions.". We should not be actually using "preemption" concept, but replacement, and IIUC this is already done, let me know if I'm missing something.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T09:13:07Z

- **keps/77-dynamically-sized-jobs/README.md:556** by [@mimowo](https://github.com/mimowo) — 2026-05-18T09:13:07Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing scheduling gates on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
+* [ ] Documentation with examples for users and integrators.
+* [ ] Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation.
```

  Point: "Conformance tests for scale-up, scale-down, slice replacement, sticky flavor, multi-cluster propagation."
  
  Let's rename conformance tests to "e2e" as we don't really have the notion of conformance in kueue currently, just e2e.
  
  Let's also move the multi-cluster e2e coverage and support to "stable".

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T09:15:54Z

- **keps/77-dynamically-sized-jobs/README.md:554** by [@mimowo](https://github.com/mimowo) — 2026-05-18T09:15:54Z
```diff
@@ -547,38 +547,49 @@ Here’s a structured and detailed **Graduation Criteria** section for KEP-77: *
 #### Beta
 
 * [ ] Feature is enabled by default in Kueue, still guarded by the feature gate for opt-out.
-* [ ] Complete support for garbage collection of preempted and inactive `WorkloadSlice`s, with tunable retention (e.g., `revisionHistoryLimit`-like mechanism).
-* [ ] Metrics and events emitted for slice lifecycle transitions (e.g., created, admitted, failed admission).
-* [ ] Documentation includes examples for users and integrators to adopt the WorkloadSlice model.
-* [ ] Formal conformance tests validate end-to-end behavior for:
-    * Horizontal scale-up and scale-down
-    * Slice replacements
-    * Sticky flavor enforcement
-    * Multi-cluster propagation
-* [ ] At least one additional framework beyond `batch/v1.Job` (e.g., RayCluster) integrates and validates the WorkloadSlice flow.
-* [ ] Slice lifecycle events (e.g., admitted, preempted, finished) are observable via `kubectl describe workload` or equivalent API tools.
-* [ ] Slice preemption is consistently handled and visible in workload status conditions.
-* [ ] All Kueue core controllers (scheduler, preemptor, queue manager) are validated under slice-enabled workloads.
-* [ ] Dynamic resizing is enabled for all Kueue-managed workloads that support the elastic-job feature (including JobSet, RayJob, Kubeflow jobs, etc.)
-* [ ] Re-evaluate the WorkloadSlice implementation to ensure compatibility with elastic workloads, considering all current and emerging alternatives within Kueue.
-* [ ] Re-evaluate currently disallowed per-job-instance combination of enabled PartialAdmission and ElasticJobs.
-* [ ] Re-evaluate the approach for removing PodSchedulingReadiness gate for admitted workload slices to use a dedicated controller rather than calling from Job reconciler (see 3. in [comment](https://github.com/kubernetes-sigs/kueue/pull/5510#issuecomment-3060737465)).
-* [ ] Re-evaluate integration frameworks leveraging `ElasticJobsViaWorkloadSlices`.
-* [ ] Re-evaluate `ElasticJobsViaWorkloadSlices` by leveraging the MultiKueue JobAdapter’s `Sync` routine for ElasticJobs-specific functionality, particularly in detecting `JobUpdate` events as described in [issue #7065](https://github.com/kubernetes-sigs/kueue/issues/7065).
-* [ ] TAS-integration: Validated in production-like environments with scale-up/scale-down cycles.
-* [ ] TAS-integration: Full integration with Topology-Aware Scheduling (support for required/preferred topology modes), or clear validation/rejection of unsupported options.
-* [ ] TAS-integration: Re-evaluate the approach to handling scale-ups and scale-downs during node repairing.
+* [ ] Webhook validation to reject elastic annotation on unsupported frameworks.
+* [ ] Webhook validation to reject PartialAdmission and Elastic on the same job.
+* [x] Dedicated controller for removing scheduling gates on admitted workload slices.
+* [x] At least one additional framework beyond `batch/v1.Job` integrates the WorkloadSlice flow.
+* [ ] Metrics and events for slice lifecycle transitions.
```

  I think the metrics may already be covered to some extent as we report already finished workloads metric. Let's check if there is a reason for the metric which could indicate the WorkloadSliceReplacement.  I think we can generally move the metric addition to Beta as we get more adoption, so that the metrics are driven by use cases. This is how we did for TAS for example.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-19T13:31:27Z

/lgtm
/approve
Thank you for refreshing the graduation criteria. I think this plan for Beta looks sensible and achievable 👍 

/hold
In case @tenzen-y has extra comments.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2026-05-22T16:24:37Z

Thank you, LGTM!
/lgtm
/approve
