# PR #1127: [scheduler] Check the consistency of the workloads admission check list before fully admitting it.

**Summary**: [scheduler] Check the consistency of the workloads admission check list before fully admitting it.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1127

**Last updated**: 2023-09-26T14:41:41Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `dont-admit-before-first-reconcile` → `main`
- **Created**: 2023-09-18T05:37:22Z
- **Updated**: 2023-09-26T14:41:41Z
- **Merged**: 2023-09-26T14:41:24Z
- **Closed**: 2023-09-26T14:41:24Z
- **Labels**: `kind/bug`, `lgtm`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 6   **+29 / -8**

## Description

This way, if the scheduler runs before the first workload reconcile, we avoid the admission of a new workload based on false positive check status.

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Check the consistency of the workloads admission check list with the one of its clusterqueue before fully admitting it. 

This way, if the scheduler assigns a quotaReservation before the first workload reconcile, the admission is delayed until the checks are in sync.
 
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1090

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-09-18T05:37:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 8339e25cea2d8294781d77d112d91ec56c97bb22 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6509ea38b5659000086aed41 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-19T14:46:11Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1127#pullrequestreview-1633491450" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1127#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@trasc](https://github.com/trasc) — 2023-09-26T10:49:37Z

/hold cancel

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-26T14:20:36Z

LGTM label has been added.  <details>Git tree hash: e8e0254c4cbdd357550d2c0e09722d3fd62c7c28</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-18T12:29:14Z

- **pkg/controller/core/workload_controller.go:279** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T12:29:14Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  A dummy check doesn't seem right.
  
  Can you expand, perhaps here or in the PR description, what the sequence of events that lead to the race is?
  Why is the workload admitted?
  
  Would it be possible for the scheduler to add both the QuotaReserved condition and Unknown condition checks?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-18T13:06:13Z

- **pkg/controller/core/workload_controller.go:279** by [@trasc](https://github.com/trasc) — 2023-09-18T13:06:13Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  It could be tricky, at the scheduler level,  to make the distinction between a workload that has no checks and one that did go in the first reconcile yet .
  
  We could try to get the number of admission checks of the target queue from the snapshot and don't set the Admission at the same time time if the numbers are different.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-18T16:41:26Z

- **pkg/controller/core/workload_controller.go:279** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T16:41:26Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  That sounds reasonable.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-18T16:42:10Z

- **pkg/controller/core/workload_controller.go:279** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-18T16:42:10Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  Or syncing the checks from the clusterqueue in the snapshot.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-19T04:03:03Z

- **pkg/controller/core/workload_controller.go:279** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-19T04:03:02Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  +1 to `Or syncing the checks from the clusterqueue in the snapshot.`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-19T06:42:13Z

- **pkg/controller/core/workload_controller.go:279** by [@trasc](https://github.com/trasc) — 2023-09-19T06:42:13Z
```diff
@@ -276,6 +276,15 @@ func (r *WorkloadReconciler) Create(e event.CreateEvent) bool {
 	r.adjustResources(log, wlCopy)
 
 	if !workload.HasQuotaReservation(wl) {
+		// The scheduler can kick in before the first reconcile when the list of checks is out of sync
```

  Reworked. Now it checks that the queue checks (retrieved from the snapshot) are all present in the workload list. If not we only do the quota reservation, the workload will transition to fully admitted when the checks are in sync and ready.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-19T14:45:51Z

- **pkg/workload/workload.go:456** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-19T14:45:51Z
```diff
@@ -444,6 +443,15 @@ func HasAllChecksReady(wl *kueue.Workload) bool {
 	return true
 }
 
+// Returns true if all the mustHaveChecks are present in the workload.
+func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
+	mustHaveChecks = mustHaveChecks.Clone()
+	for i := range wl.Status.AdmissionChecks {
+		mustHaveChecks.Delete(wl.Status.AdmissionChecks[i].Type)
+	}
+	return mustHaveChecks.Len() == 0
```

  it should be equivalent to check that the lengths are the same and each type present in mustHaveChecks (because we know that all the types are different).
  
  That should avoid one Clone

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-19T14:46:02Z

/approve
/hold for nit

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-19T18:40:54Z

- **pkg/workload/workload.go:456** by [@trasc](https://github.com/trasc) — 2023-09-19T18:40:50Z
```diff
@@ -444,6 +443,15 @@ func HasAllChecksReady(wl *kueue.Workload) bool {
 	return true
 }
 
+// Returns true if all the mustHaveChecks are present in the workload.
+func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
+	mustHaveChecks = mustHaveChecks.Clone()
+	for i := range wl.Status.AdmissionChecks {
+		mustHaveChecks.Delete(wl.Status.AdmissionChecks[i].Type)
+	}
+	return mustHaveChecks.Len() == 0
```

  Preemption will be a special kind of check that could be present in the workloads list but not in the queues. Hard-code ignoring it here will look strange and be hard to maintain.
  
  Adding a fast way out in case the must have set is empty.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-26T10:03:57Z

- **pkg/workload/workload.go:456** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-26T10:03:57Z
```diff
@@ -444,6 +443,15 @@ func HasAllChecksReady(wl *kueue.Workload) bool {
 	return true
 }
 
+// Returns true if all the mustHaveChecks are present in the workload.
+func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
+	mustHaveChecks = mustHaveChecks.Clone()
+	for i := range wl.Status.AdmissionChecks {
+		mustHaveChecks.Delete(wl.Status.AdmissionChecks[i].Type)
+	}
+	return mustHaveChecks.Len() == 0
```

  > Preemption will be a special kind of check
  
  @trasc Does that mean this?
  
  https://github.com/kubernetes-sigs/kueue/blob/97bc58e555d47b395e8cedb57f56db4a9fec74f9/pkg/constants/constants.go#L41-L43

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T10:19:09Z

- **pkg/workload/workload.go:456** by [@trasc](https://github.com/trasc) — 2023-09-26T10:19:08Z
```diff
@@ -444,6 +443,15 @@ func HasAllChecksReady(wl *kueue.Workload) bool {
 	return true
 }
 
+// Returns true if all the mustHaveChecks are present in the workload.
+func HasAllChecks(wl *kueue.Workload, mustHaveChecks sets.Set[string]) bool {
+	mustHaveChecks = mustHaveChecks.Clone()
+	for i := range wl.Status.AdmissionChecks {
+		mustHaveChecks.Delete(wl.Status.AdmissionChecks[i].Type)
+	}
+	return mustHaveChecks.Len() == 0
```

  yes

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-26T14:20:29Z

/lgtm
