# PR #11286: Make the read-only contract on Snapshot explicit

**Summary**: Make the read-only contract on Snapshot explicit

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11286

**Last updated**: 2026-05-18T16:34:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `make-snapshot-read-only-explicit` → `main`
- **Created**: 2026-05-18T11:56:22Z
- **Updated**: 2026-05-18T16:34:08Z
- **Merged**: 2026-05-18T16:32:09Z
- **Closed**: 2026-05-18T16:32:09Z
- **Labels**: `kind/bug`, `release-note`, `size/L`, `lgtm`, `approved`, `cncf-cla: yes`
- **Assignees**: [@PBundyra](https://github.com/PBundyra)
- **Requested reviewers**: [@kannon92](https://github.com/kannon92), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 2   **+115 / -3**

## Description


#### What type of PR is this?

/kind bug

#### What this PR does / why we need it:

Snapshot() is conceptually a read-only operation, but the current implementation calls a mutating helper, which makes the locking correctness very hard to reason about. To make the intended contract explicit, I am proposing this PR: Snapshot() no longer tries to repair TAS usage; instead, it skips TAS ClusterQueues whose TAS usage is not synced yet. The cache update paths remain responsible for performing TAS sync under the cache write lock.




#### Special notes for your reviewer:

I’ve analyzed this case, and I think this is a false-positive bug detection from the system-level perspective.

The concern is valid locally: `Snapshot()` currently calls `ensureTASIsSynced()` while holding only the cache read lock, and `ensureTASIsSynced()` is a mutating helper. 

However, due to the interaction between the scheduler cache update paths and the controllers, `ensureTASIsSynced()` called from Scheduler does not perform the mutating repair.

The problematic state would be:

```
cq.isTASInitialized() == true
cq.isTASSynced == false
```

This is the only state where `ensureTASIsSynced()` would perform the mutating repair path and re-account existing workloads for TAS usage.

When a Topology is added or updated, the scheduler cache handles it under the cache write lock:
```
Cache.AddOrUpdateTopology
  -> Cache.Lock()
  -> tasCache.AddTopology(...)
  -> updateClusterQueues(...)
      -> cq.UpdateWithFlavors(...)
          -> cq.updateQueueStatus(...)
              -> cq.ensureTASIsSynced(...)
  -> Cache.Unlock()
```

The same applies when a ResourceFlavor is added or updated:
```
Cache.AddOrUpdateResourceFlavor
  -> Cache.Lock()
  -> tasCache.AddResourceFlavor(...)
  -> updateClusterQueues(...)
      -> cq.UpdateWithFlavors(...)
          -> cq.updateQueueStatus(...)
              -> cq.ensureTASIsSynced(...)
  -> Cache.Unlock()
```

So if adding or updating the Topology or ResourceFlavor makes TAS usage sync performed under the cache write lock, blocking `Snapshot()` which needs to take the read lock.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
TAS: ensure that `Snapshot()` does not perform update the list of workloads under the read-lock.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-18T11:56:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9de96c97aacad5797eac17637397912abf382d5c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a0b20e8ef6feb00084807c2 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T11:56:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11286#" title="Author self-approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T11:56:59Z

cc @PBundyra ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T12:20:12Z

https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/11286/pull-kueue-test-e2e-multikueue-main/2056343144110231552
This looks like the known flake: https://github.com/kubernetes-sigs/kueue/issues/11062

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T12:24:32Z

/test pull-kueue-test-e2e-multikueue-main

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T13:36:49Z

/lgtm
/hold
Thanks, I just left a small nit, feel free to unhold if you think it's insignificant

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T13:36:57Z

LGTM label has been added.  <details>Git tree hash: 213e4ce1a0601d51e9f039d865f16146b2706294</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T14:24:04Z

/unhold
as the remark is addressed

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T15:27:17Z

I think the current Snapshot() calling ensureTASIsSynced is so misleading that we can categorize this as a bug for cherrypicking even if there is no system-level issue
/cherrypick release-0.17
/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T15:27:20Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11286#issuecomment-4479178820):

>I think the current Snapshot() calling ensureTASIsSynced is so misleading that we can categorize this as a bug for cherrypicking even if there is no system-level issue
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T15:29:49Z

/lgtm
Thanks

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-18T15:29:56Z

LGTM label has been added.  <details>Git tree hash: ba47b3cc4b8eb481974364a6a07401844bae938f</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T16:32:53Z

@mimowo: new pull request created: #11294

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11286#issuecomment-4479178820):

>I think the current Snapshot() calling ensureTASIsSynced is so misleading that we can categorize this as a bug for cherrypicking even if there is no system-level issue
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-18T16:33:05Z

/cherrypick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T16:33:31Z

@mimowo: new pull request created: #11295

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11286#issuecomment-4479178820):

>I think the current Snapshot() calling ensureTASIsSynced is so misleading that we can categorize this as a bug for cherrypicking even if there is no system-level issue
>/cherrypick release-0.17
>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-18T16:34:07Z

@mimowo: new pull request could not be created: failed to create pull request against kubernetes-sigs/kueue#release-0.16 from head k8s-infra-cherrypick-robot:cherry-pick-11286-to-release-0.16: status code 422 not one of [201], body: {"message":"Validation Failed","errors":[{"resource":"PullRequest","code":"custom","message":"A pull request already exists for k8s-infra-cherrypick-robot:cherry-pick-11286-to-release-0.16."}],"documentation_url":"https://docs.github.com/rest/pulls/pulls#create-a-pull-request","status":"422"}

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11286#issuecomment-4479734111):

>/cherrypick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-05-18T12:15:48Z

- **pkg/cache/scheduler/snapshot.go:237** by [@kshalot](https://github.com/kshalot) — 2026-05-18T12:15:48Z
```diff
@@ -214,6 +221,23 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	return &snap, nil
 }
 
+// skipInactiveCQReason reports why the CQ should not be considered for
+// admitting workloads in this scheduler snapshot. If the CQ can be considered,
+// an empty reason is returned.
+func skipInactiveCQReason(cq *clusterQueue) inactiveCQReason {
+	if !cq.Active() {
+		return inactiveCQReasonNotActive
+	}
+	if cq.HasParent() && hierarchy.HasCycle(cq.Parent()) {
+		return inactiveCQReasonHasCycle
+	}
+	if features.Enabled(features.TopologyAwareScheduling) && len(cq.tasFlavors) > 0 && !cq.isTASSynced {
+		// The CQ uses a TAS flavor, but TAS usage is not synced yet.
+		return inactiveCQReasonTASUsageNotSynced
+	}
```

  I think in the case of MultiKueue, the topology is not created on the manager cluster, right? I just wanted to check in whether `cq.isTASSynced` won't be always false in this case. There's explicit handling in `isTASViolated` for example:
  https://github.com/kubernetes-sigs/kueue/blob/cbbb65b4fc16dd9c633682fdaeb5ced8b42d54b6/pkg/cache/scheduler/clusterqueue.go#L349-L352

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T13:08:03Z

- **pkg/cache/scheduler/snapshot.go:237** by [@mimowo](https://github.com/mimowo) — 2026-05-18T13:08:03Z
```diff
@@ -214,6 +221,23 @@ func (c *Cache) Snapshot(ctx context.Context, options ...SnapshotOption) (*Snaps
 	return &snap, nil
 }
 
+// skipInactiveCQReason reports why the CQ should not be considered for
+// admitting workloads in this scheduler snapshot. If the CQ can be considered,
+// an empty reason is returned.
+func skipInactiveCQReason(cq *clusterQueue) inactiveCQReason {
+	if !cq.Active() {
+		return inactiveCQReasonNotActive
+	}
+	if cq.HasParent() && hierarchy.HasCycle(cq.Parent()) {
+		return inactiveCQReasonHasCycle
+	}
+	if features.Enabled(features.TopologyAwareScheduling) && len(cq.tasFlavors) > 0 && !cq.isTASSynced {
+		// The CQ uses a TAS flavor, but TAS usage is not synced yet.
+		return inactiveCQReasonTASUsageNotSynced
+	}
```

  Currently the design assumes creation of the Topology on the management cluster, as created here: https://github.com/kubernetes-sigs/kueue/blob/main/test/e2e/multikueue/baseline/tas_test.go#L120-L121
  
  That was added in this PR: https://github.com/kubernetes-sigs/kueue/pull/5361 and here is where we defer the actual TAS computations of MultiKueue management cluster: https://github.com/kubernetes-sigs/kueue/pull/5361/changes#diff-5a39cea8316e94ece1214aef72728e4476f4531d9714f30c2b00f33b00b47e4fR80-R82
  
  This skipping of validation for MK is something we may need to revisit, this looks too strong IMO, but the design of the MultiKueue + TAS story is not ideal for sure. I'm hoping we can improve on the design in the future.
  
  That being said I'm not yet sure how this impacts correctness of the code changes, I don't think it does. I think regardless if MultiKueue, or not MultiKueue then key think is that the event handlers for Topology, and ResourceFlavor will trigger `cq.ensureTASIsSynced`, and so there is nothing to be done for Snapshot().

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2026-05-18T13:34:57Z

- **pkg/cache/scheduler/snapshot_test.go:772** by [@PBundyra](https://github.com/PBundyra) — 2026-05-18T13:34:57Z
```diff
@@ -768,6 +769,90 @@ func TestSnapshot(t *testing.T) {
 				),
 			},
 		},
+		"clusterQueue with unsynced TAS usage is excluded from snapshot": {
```

  ```suggestion
  		"clusterQueue with unsynced TAS usage due to missing topology is excluded from snapshot": {
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-18T14:23:46Z

- **pkg/cache/scheduler/snapshot_test.go:772** by [@mimowo](https://github.com/mimowo) — 2026-05-18T14:23:46Z
```diff
@@ -768,6 +769,90 @@ func TestSnapshot(t *testing.T) {
 				),
 			},
 		},
+		"clusterQueue with unsynced TAS usage is excluded from snapshot": {
```

  Done, added in brackets so that if reads better
