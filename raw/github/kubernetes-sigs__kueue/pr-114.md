# PR #114: Enhance Kueue.Queue.Status test suite

**Summary**: Enhance Kueue.Queue.Status test suite

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/114

**Last updated**: 2022-03-14T13:39:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Branch**: `devel/branch_fix` → `main`
- **Created**: 2022-03-11T12:32:07Z
- **Updated**: 2022-03-14T13:39:08Z
- **Merged**: 2022-03-11T20:46:23Z
- **Closed**: 2022-03-11T20:46:23Z
- **Labels**: `lgtm`, `approved`, `size/M`, `kind/cleanup`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 6   **+55 / -31**

## Description

Signed-off-by: Carlos Eduardo Arango Gutierrez <carangog@redhat.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind cleanup

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #

#### Special notes for your reviewer:

## Discussion

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:34:57Z

/assign @alculquicondor

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:47:11Z

/retest-required

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T12:47:51Z

Previous test failure:
```bash
Test started today at 7:32 AM failed after 15m0s. ([less info](https://prow.k8s.io/spyglass/lens/metadata/iframe?req=%7B%22artifacts%22%3A%5B%22finished.json%22%2C%22podinfo.json%22%2C%22prowjob.json%22%2C%22started.json%22%5D%2C%22index%22%3A0%2C%22src%22%3A%22gs%2Fkubernetes-jenkins%2Fpr-logs%2Fpull%2Fkubernetes-sigs_kueue%2F114%2Fpull-kueue-build-image-main%2F1502261086198435840%22%7D&topURL=https%3A//prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/114/pull-kueue-build-image-main/1502261086198435840&lensIndex=0#))

Init container clonerefs not ready: (state: waiting, reason: "PodInitializing", message: "") Init container initupload not ready: (state: waiting, reason: "PodInitializing", message: "") Init container place-entrypoint not ready: (state: waiting, reason: "PodInitializing", message: "")
```

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T20:44:42Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-11T20:44:54Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/114#issuecomment-1065509153" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/114#" title="Author self-approved">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T19:00:54Z

- **pkg/queue/manager_test.go:276** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T18:54:06Z
```diff
@@ -271,8 +271,26 @@ func TestStatus(t *testing.T) {
 				CreationTimestamp: metav1.NewTime(now),
 			},
 			Spec: kueue.QueuedWorkloadSpec{QueueName: "foo"},
+			Status: kueue.QueuedWorkloadStatus{
+				Conditions: []kueue.QueuedWorkloadCondition{
+					{Type: kueue.QueuedWorkloadAdmitted},
```

  In reality an admitted workload shouldn't count against the pending counter, but the unit test is testing a logic that is not related to that, so I wouldn't add this here because it is confusing.

- **test/integration/controller/core/queue_controller_test.go:86** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T18:56:56Z
```diff
@@ -79,10 +79,29 @@ var _ = ginkgo.Describe("Queue controller", func() {
 		}
 		gomega.Eventually(func() kueue.QueueStatus {
 			var updatedQueue kueue.Queue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(Queue), &updatedQueue)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
 			return updatedQueue.Status
 		}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.QueueStatus{PendingWorkloads: 3}))
 
+		ginkgo.By("Assigning workloads")
```

  ```suggestion
  		ginkgo.By("Admitting workloads")
  ```

- **test/integration/controller/core/queue_controller_test.go:96** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T18:58:45Z
```diff
@@ -79,10 +79,29 @@ var _ = ginkgo.Describe("Queue controller", func() {
 		}
 		gomega.Eventually(func() kueue.QueueStatus {
 			var updatedQueue kueue.Queue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(Queue), &updatedQueue)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
 			return updatedQueue.Status
 		}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.QueueStatus{PendingWorkloads: 3}))
 
+		ginkgo.By("Assigning workloads")
+		admissions := []*kueue.Admission{
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+		}
+		for i, w := range workloads {
+			w.Spec.Admission = admissions[i]
```

  remove the array above and 
  
  ```suggestion
  			w.Spec.Admission = testing.MakeAdmission(clusterQueue.Name).
  				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj()
  ```

- **pkg/queue/manager.go:186** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T19:00:40Z
```diff
@@ -183,13 +183,16 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) GetPendingQW(q *kueue.Queue) (int32, error) {
```

  ```suggestion
  func (m *Manager) PendingWorkloads(q *kueue.Queue) (int32, error) {
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T19:39:14Z

- **test/integration/controller/core/queue_controller_test.go:87** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T19:39:13Z
```diff
@@ -79,10 +79,30 @@ var _ = ginkgo.Describe("Queue controller", func() {
 		}
 		gomega.Eventually(func() kueue.QueueStatus {
 			var updatedQueue kueue.Queue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(Queue), &updatedQueue)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
 			return updatedQueue.Status
 		}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.QueueStatus{PendingWorkloads: 3}))
 
+		ginkgo.By("Admitting workloads")
+		admissions := []*kueue.Admission{
```

  remove this

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T19:43:56Z

- **pkg/queue/manager.go:186** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:33:57Z
```diff
@@ -183,13 +183,16 @@ func (m *Manager) DeleteQueue(q *kueue.Queue) {
 	delete(m.queues, key)
 }
 
-func (m *Manager) Status(q *kueue.Queue) (int, error) {
+func (m *Manager) GetPendingQW(q *kueue.Queue) (int32, error) {
```

  sure

- **test/integration/controller/core/queue_controller_test.go:96** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:34:31Z
```diff
@@ -79,10 +79,29 @@ var _ = ginkgo.Describe("Queue controller", func() {
 		}
 		gomega.Eventually(func() kueue.QueueStatus {
 			var updatedQueue kueue.Queue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(Queue), &updatedQueue)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
 			return updatedQueue.Status
 		}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.QueueStatus{PendingWorkloads: 3}))
 
+		ginkgo.By("Assigning workloads")
+		admissions := []*kueue.Admission{
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+			testing.MakeAdmission(clusterQueue.Name).
+				Flavor(corev1.ResourceCPU, flavorOnDemand).Obj(),
+		}
+		for i, w := range workloads {
+			w.Spec.Admission = admissions[i]
```

  cool

- **test/integration/controller/core/queue_controller_test.go:87** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:43:44Z
```diff
@@ -79,10 +79,30 @@ var _ = ginkgo.Describe("Queue controller", func() {
 		}
 		gomega.Eventually(func() kueue.QueueStatus {
 			var updatedQueue kueue.Queue
-			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(Queue), &updatedQueue)).To(gomega.Succeed())
+			gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(queue), &updatedQueue)).To(gomega.Succeed())
 			return updatedQueue.Status
 		}, framework.Timeout, framework.Interval).Should(testing.Equal(kueue.QueueStatus{PendingWorkloads: 3}))
 
+		ginkgo.By("Admitting workloads")
+		admissions := []*kueue.Admission{
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2022-03-14T13:29:58Z

- **api/v1alpha1/queue_types.go:36** by [@alculquicondor](https://github.com/alculquicondor) — 2022-03-14T13:29:58Z
```diff
@@ -33,6 +33,7 @@ type ClusterQueueReference string
 type QueueStatus struct {
 	// PendingWorkloads is the number of workloads currently admitted to this
 	// queue not yet admitted to a ClusterQueue.
+	// +optional
```

  Can you do the same for ClusterQueue?

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-14T13:31:20Z

- **api/v1alpha1/queue_types.go:36** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-14T13:31:20Z
```diff
@@ -33,6 +33,7 @@ type ClusterQueueReference string
 type QueueStatus struct {
 	// PendingWorkloads is the number of workloads currently admitted to this
 	// queue not yet admitted to a ClusterQueue.
+	// +optional
```

  sure

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-14T13:39:08Z

- **api/v1alpha1/queue_types.go:36** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-14T13:39:08Z
```diff
@@ -33,6 +33,7 @@ type ClusterQueueReference string
 type QueueStatus struct {
 	// PendingWorkloads is the number of workloads currently admitted to this
 	// queue not yet admitted to a ClusterQueue.
+	// +optional
```

  https://github.com/kubernetes-sigs/kueue/pull/117
