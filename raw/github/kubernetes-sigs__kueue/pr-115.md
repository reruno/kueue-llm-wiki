# PR #115: Kueue.ClusterQueue.Status Add pendingWorkloads

**Summary**: Kueue.ClusterQueue.Status Add pendingWorkloads

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/115

**Last updated**: 2022-03-11T21:18:24Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ArangoGutierrez](https://github.com/ArangoGutierrez)
- **Branch**: `devel/7_pending` → `main`
- **Created**: 2022-03-11T15:01:15Z
- **Updated**: 2022-03-11T21:18:24Z
- **Merged**: 2022-03-11T21:18:24Z
- **Closed**: 2022-03-11T21:18:24Z
- **Labels**: `kind/feature`, `lgtm`, `approved`, `kind/api-change`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@ahg-g](https://github.com/ahg-g)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 5   **+52 / -19**

## Description

Signed-off-by: Carlos Eduardo Arango Gutierrez <carangog@redhat.com>

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind feature
/kind api-change
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Remaining implementation for #7

#### Special notes for your reviewer:
 Add pendingWrkloads to Kueue.ClusterQueue.Status

## Discussion

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T15:02:42Z

typo on title
/retitle Kueue.ClusterQueue.Status Add pendingWorkloads

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T15:20:59Z

/assign @alculquicondor

### Comment by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:13:47Z

/retest-required

### Comment by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T21:17:19Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2022-03-11T21:17:31Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/115#issuecomment-1065536753" title="Approved">ahg-g</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/115#" title="Author self-approved">ArangoGutierrez</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [ahg-g]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T18:42:15Z

- **api/v1alpha1/clusterqueue_types.go:268** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T18:42:14Z
```diff
@@ -264,7 +264,11 @@ type ClusterQueueStatus struct {
 	// workloads assigned to this clusterQueue.
 	UsedResources UsedResources `json:"usedResources,omitempty"`
 
-	// AdmittedWorkloads is the number of workloads currently assigned to this
+	// PendingWorkloads is the number of workloads currently waiting to be
+	// admitted to a clusterQueue.
```

  ```suggestion
  	// admitted to this clusterQueue.
  ```

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T18:45:08Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T18:44:29Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  why this move? seems better to have this defined as close as possible to where it is used.

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T19:11:39Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:11:39Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  I can move it back, during devel, it sounded better to be there, but maybe just to my eyes

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T19:22:32Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T19:21:17Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  actually, we don't need this copy, it seems we are recreating the whole status every time. I suggest we create a function Status(), something like this:
  
  ```
  func (r *ClusterQueue) Status(cq *kueue.ClusterQueue) (*kueue.ClusterQueueStatus, error) {
     usage, workloads, err := r.cache.Usage(&cqObj)
     if err != nil {
  	log.Error(err, "Failed getting usage from cache")
  	// This is likely because the cluster queue was recently removed,
  	// but we didn't process that event yet.
  	return nil, err
     }
     return &kueue.ClusterQueueStatus{
        UsedResources: usage,
        AdmittedWorkloads: int32(workloads),
        PendingWorkloads: r.qManager.Pending(cqj)
     }, nil
  }
  ```

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T19:49:32Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:49:32Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  we still need the old one for 
  ```go
  !equality.Semantic.DeepEqual(oldStatus, cqObj.Status)
  ```

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T19:52:28Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:52:27Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  I like the wrapper idea, but we need the old one to avoid updating with the same info

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T19:53:02Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T19:53:02Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  unless we add a timestamp

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T20:08:57Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T20:08:56Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  ```go
  func (r *ClusterQueue) Status(cq *kueue.ClusterQueue) (*kueue.ClusterQueueStatus, error) {
  	usage, workloads, err := r.cache.Usage(cq)
  	now := metav1.Now()
  	if err != nil {
  		r.log.Error(err, "Failed getting usage from cache")
  		// This is likely because the cluster queue was recently removed,
  		// but we didn't process that event yet.
  		return nil, err
  	}
  	return &kueue.ClusterQueueStatus{
  		UsedResources:     usage,
  		AdmittedWorkloads: int32(workloads),
  		PendingWorkloads:  r.qManager.Pending(cq),
  		LastUpdateTime:    now,
  	}, nil
  }
  ```
  Would look like this

### Review by [@ahg-g](https://github.com/ahg-g) (commented) — 2022-03-11T20:42:28Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ahg-g](https://github.com/ahg-g) — 2022-03-11T20:42:28Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  why do we need the timestamp? can't we do something like this (give the Status func above):
  
  ```
  status, err := r.Status(cqObj)
  if err != nil {
    return ctrl.Result{}, err
  }
  if !equality.Semantic.DeepEqual(status, cqObj.Status) {
    cqObj.Status = status
    err = r.client.Status().Update(ctx, &cqObj)
    return ctrl.Result{}, client.IgnoreNotFound(err)
  }
  
  ```

### Review by [@ArangoGutierrez](https://github.com/ArangoGutierrez) (commented) — 2022-03-11T20:46:07Z

- **pkg/controller/core/clusterqueue_controller.go:76** by [@ArangoGutierrez](https://github.com/ArangoGutierrez) — 2022-03-11T20:46:07Z
```diff
@@ -67,17 +67,20 @@ func (r *ClusterQueue) Reconcile(ctx context.Context, req ctrl.Request) (ctrl.Re
 	ctx = ctrl.LoggerInto(ctx, log)
 	log.V(2).Info("Reconciling ClusterQueue")
 
+	// Shallow copy enough for now.
+	oldStatus := cqObj.Status
+
 	usage, workloads, err := r.cache.Usage(&cqObj)
 	if err != nil {
 		log.Error(err, "Failed getting usage from cache")
 		// This is likely because the cluster queue was recently removed,
 		// but we didn't process that event yet.
 		return ctrl.Result{}, err
 	}
-	// Shallow copy enough for now.
-	oldStatus := cqObj.Status
+
```

  done!
