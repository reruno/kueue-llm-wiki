# PR #1151: Updated the tool to generate client-go libraries

**Summary**: Updated the tool to generate client-go libraries

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1151

**Last updated**: 2024-01-29T11:28:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@PBundyra](https://github.com/PBundyra)
- **Branch**: `switch-code-gen-tool` → `main`
- **Created**: 2023-09-22T12:34:57Z
- **Updated**: 2024-01-29T11:28:39Z
- **Merged**: 2023-11-07T10:02:21Z
- **Closed**: 2023-11-07T10:02:21Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+44 / -22**

## Description

#### What type of PR is this?
/kind cleanup
/kind deprecation

#### Which issue(s) this PR fixes:
Fixes #1134

#### Special notes for your reviewer:
Since  the new ```kube_codegen.sh``` searches for ```types.go``` file in the input-pkg directory I added artificial ```types.go``` to the ```apis/kueue/v1beta1```. 

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-09-22T12:35:03Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 476a5d54d415f03a491548a615f23caa68da18ee |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6549fc8ac5baad0008072ab0 |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-22T12:35:06Z

Hi @PBundyra. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-22T12:35:18Z

Adding the "do-not-merge/release-note-label-needed" label and removing any existing "release-note-none" label because there is a "kind/deprecation" label on the PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-09-22T12:40:47Z

/test all

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-22T12:41:01Z

@PBundyra: Cannot trigger testing until a trusted user reviews the PR and leaves an `/ok-to-test` message.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1151#issuecomment-1731347916):

>/test all
>


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@PBundyra](https://github.com/PBundyra) — 2023-09-22T12:41:38Z

/assign @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T12:44:44Z

/ok-to-test

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-23T00:10:34Z

/remove-kind deprecation

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-23T00:11:04Z

/release-note-none

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-06T10:15:38Z

LGTM label has been added.  <details>Git tree hash: 7fe169e7acff67befaa0223b0e3726dbc99d8e11</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-06T22:14:04Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1151#pullrequestreview-1716379944" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1151#" title="Author self-approved">PBundyra</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-11-07T09:57:52Z

LGTM label has been added.  <details>Git tree hash: af46d0673a435e7c6287ec0aa9ea7fa0a3a4e7bc</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-09-22T20:14:36Z

- **hack/update-codegen.sh:28** by [@tenzen-y](https://github.com/tenzen-y) — 2023-09-22T20:14:34Z
```diff
@@ -25,7 +25,7 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 cd $(dirname ${BASH_SOURCE[0]})/..
 
 chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-source "${CODEGEN_PKG}/generate-groups.sh" \
+source "${CODEGEN_PKG}/kube_codegen.sh" \
```

  I don't think this works well. Maybe, when you remove the `client-go` folder, then you perform `make generate`, client-go codes wouldn't be generated. 
  
  I think we must use the following new command:
  
  https://github.com/kubernetes/code-generator/blob/e4611069dfb4b0c04c7751afae1b9fef64828964/kube_codegen.sh#L381-L422

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T06:38:04Z

- **hack/update-codegen.sh:28** by [@mimowo](https://github.com/mimowo) — 2023-09-25T06:38:04Z
```diff
@@ -25,7 +25,7 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 cd $(dirname ${BASH_SOURCE[0]})/..
 
 chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-source "${CODEGEN_PKG}/generate-groups.sh" \
+source "${CODEGEN_PKG}/kube_codegen.sh" \
```

  Yeah, we probably can use this as an example: https://github.com/kubernetes/sample-controller/blob/master/hack/update-codegen.sh

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-25T16:11:59Z

LGTM, couple nits

- **hack/update-codegen.sh:47** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:03:48Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --with-watch \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt \
+  --with-applyconfig
```

  nit: maybe put this up, together with --with-watch

- **apis/kueue/v1beta1/types.go:2** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:04:11Z
```diff
@@ -0,0 +1,24 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  ```suggestion
  Copyright 2023 The Kubernetes Authors.
  ```

- **hack/update-codegen.sh:32** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:05:24Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
```

  Maybe we could make it relative to `${KUEUE_ROOT}`? Since we anyway use it for boilerplate param.

- **hack/update-codegen.sh:38** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:05:43Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --with-watch \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
```

  Same here, maybe we can use KUEUE_ROOT

- **apis/kueue/v1beta1/types.go:19** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:07:40Z
```diff
@@ -0,0 +1,24 @@
+/*
+Copyright 2022 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+/*
+This file is needed for kubernetes/code-generator/kube_codegen.sh script used in hack/update-codegen.sh. 
+Do not remove it without consideration.
```

  nit: I think this line does not add much :)

- **hack/update-codegen.sh:41** by [@mimowo](https://github.com/mimowo) — 2023-09-25T16:10:11Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --with-watch \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt \
+  --with-applyconfig
 
 
 # Future releases of of code-generator add better support for out of GOPATH generation,
 # check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
 # for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
```

  I would suggest avoiding unnecessary changes in this section, but would be happy to delete it entirely if possible now :). @trasc do you think this is still needed?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-25T17:22:06Z

- **hack/update-codegen.sh:41** by [@trasc](https://github.com/trasc) — 2023-09-25T17:18:00Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --with-watch \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt \
+  --with-applyconfig
 
 
 # Future releases of of code-generator add better support for out of GOPATH generation,
 # check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
 # for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
```

  I wold expect (and hope) that we can remove it all.
  
  All the generators should support `--output-base`  now

- **hack/update-codegen.sh:32** by [@trasc](https://github.com/trasc) — 2023-09-25T17:18:23Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
```

  +1 
  
  use `"${KUEUE_ROOT}/client-go"`.

- **hack/update-codegen.sh:38** by [@trasc](https://github.com/trasc) — 2023-09-25T17:21:33Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --with-watch \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
```

  +1 , and maybe move it on the next line

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-09-26T08:12:41Z

- **hack/update-codegen.sh:32** by [@PBundyra](https://github.com/PBundyra) — 2023-09-26T08:12:41Z
```diff
@@ -24,24 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "$(dirname "${BASH_SOURCE[0]}")/../../.." \
```

  Using ```"${KUEUE_ROOT}/client-go"``` as ```--output-base``` changes the name of the generated package, so imho it's not the best idea. However I'll make it the ```--output-base``` relative to ```"${KUEUE_ROOT}/client-go"```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T08:27:51Z

- **hack/update-codegen.sh:38** by [@trasc](https://github.com/trasc) — 2023-09-26T08:26:29Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  I think this assumes that the code is loacted in `<some-path>/sigs.k8s.io/kueue`, can you tray 
  
  ```suggestion
    --output-pkg-root client-go \
    --output-base "${KUEUE_ROOT}" \
  ```

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-26T08:35:55Z

- **apis/kueue/v1beta1/types.go:2** by [@mimowo](https://github.com/mimowo) — 2023-09-26T08:35:55Z
```diff
@@ -0,0 +1,24 @@
+/*
+Copyright 2022 The Kubernetes Authors.
```

  Didn't push?

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-09-26T08:41:56Z

- **hack/update-codegen.sh:38** by [@PBundyra](https://github.com/PBundyra) — 2023-09-26T08:41:55Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  Indeed this is the assumption, however changing that results in change of the name of the generated package

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-09-26T08:55:01Z

- **hack/update-codegen.sh:38** by [@PBundyra](https://github.com/PBundyra) — 2023-09-26T08:55:01Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  Let me investigate it further

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-09-26T12:16:46Z

- **hack/update-codegen.sh:38** by [@PBundyra](https://github.com/PBundyra) — 2023-09-26T12:16:46Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  According to this [documentation](https://github.com/kubernetes/code-generator/blob/791c213a776b4b92f9af0805c1cf57c118ba951c/kube_codegen.sh#L389) ```--output-pug-root``` must be Go package syntax which in our case is ```sigs.k8s.io/kueue/client-go```. What I can add to prevent misusing this script is a check that generates a short warning/error if ```kueue/``` is not a subdirectory of ```sigs.k8s.io/```. WDYT @trasc

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T12:46:12Z

- **hack/update-codegen.sh:38** by [@trasc](https://github.com/trasc) — 2023-09-26T12:46:11Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  That will be problematic, at least for me personally, I'll not be able to do anything on the project unless I change the way I structure my work dirs for years. :) 
  
  Are we sure that there is no way around this?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-26T13:01:35Z

- **hack/update-codegen.sh:38** by [@mimowo](https://github.com/mimowo) — 2023-09-26T13:01:35Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  @trasc IIUC the previous code also assumed that kueue is inside `sigs.k8s.io`, didn't it? If this is the case it wouldn't be a regression.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T13:38:19Z

- **hack/update-codegen.sh:38** by [@trasc](https://github.com/trasc) — 2023-09-26T13:38:19Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  It will be a regression on our side, the previous script used to work outside of GOPATH but would generate the files in a different dir, hence the moving hack.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-09-26T15:21:20Z

- **hack/update-codegen.sh:38** by [@PBundyra](https://github.com/PBundyra) — 2023-09-26T15:21:20Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  You're right @trasc. I came up with this solution based on [this sample](https://github.com/kubernetes/sample-controller/commit/b0e4f8301717cfb1f2d1832ddafe3b1003de1d49). However, it seems like the ```kube_codegen.sh``` directly search for ```--input-pkg-root``` which is not necessarily ```sigs.k8s.io/kueue/apis```. To address this problem we can copy ```apis/``` directory to match with ```--input-pkg-root```, but it doesnt seem to be the most elegant solution. Do you have any other ideas on how I can tackle this?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T15:36:58Z

- **hack/update-codegen.sh:38** by [@trasc](https://github.com/trasc) — 2023-09-26T15:36:58Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  cloud-provider-gsp looks to be trying to accomplish this, however I did not looked into details on what they are doing.
  https://github.com/kubernetes/cloud-provider-gcp/blob/32ba3c7b4240d23c0cfc55d57d5dd50bccbb87c7/crd/hack/generate-groups.sh#L52

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-03T11:56:36Z

- **hack/update-codegen.sh:38** by [@mimowo](https://github.com/mimowo) — 2023-11-03T11:56:35Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  I played with this today a little. 
  
  First, I reproduced the issue with the client-go generation when the kueue is in a different folder than `sigs.k8s.io`. 
  
  <del>Second, I was able to resolve the issue with this:</del>
  ```sh
  PARENT_DIR=$(basename $(dirname ${KUEUE_ROOT}))
  ...
  kube::codegen::gen_helpers \
    --input-pkg-root "${PARENT_DIR}/kueue/apis" \
  ...
  kube::codegen::gen_client \
    --input-pkg-root "${PARENT_DIR}/kueue/apis" \
    --output-pkg-root "${PARENT_DIR}/kueue/client-go" \
  ...
  ```
  <del>@trasc, do you think this addresses the issue? @PBundyra please update the PR accordingly if you double-check and this works.</del>
  
  EDIT: sorry for confusion, does not seem to work, I might have done a mistake when testing.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-03T17:11:10Z

- **hack/update-codegen.sh:38** by [@mimowo](https://github.com/mimowo) — 2023-11-03T17:11:10Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  Ok, I think I found a solution that works even if kueue is out of sigs.k8s.io, but using a soft link to force it :)
  ```sh
  ln -s .. sigs.k8s.io
  
  kube::codegen::gen_helpers \
    --input-pkg-root sigs.k8s.io/kueue/apis \
    --output-base "${KUEUE_ROOT}" \
    --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
  
  kube::codegen::gen_client \
    --input-pkg-root sigs.k8s.io/kueue/apis \
    --output-pkg-root sigs.k8s.io/kueue/client-go \
    --output-base "${KUEUE_ROOT}" \
    --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt \
    --with-watch \
    --with-applyconfig
  
  rm sigs.k8s.io
  ```
  This can be refined to make the trick with soft link conditional only if we are out of `sigs.k8s.io`, but not sure it is worth it. It also avoids, copying or moving any files to the desired layout as before.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-06T08:29:16Z

- **hack/update-codegen.sh:38** by [@mimowo](https://github.com/mimowo) — 2023-11-06T08:29:15Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  Or even better we can have something like here: https://github.com/kubernetes-sigs/kueue/commit/70b3bd3077bdabb23ad4346ce415f8bc35cec6ef. Feel free to cherry-pick the commit on your branch.

### Review by [@PBundyra](https://github.com/PBundyra) (commented) — 2023-11-06T10:09:10Z

- **hack/update-codegen.sh:38** by [@PBundyra](https://github.com/PBundyra) — 2023-11-06T10:09:10Z
```diff
@@ -24,28 +24,21 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
+source "${CODEGEN_PKG}/kube_codegen.sh"
+
+kube::codegen::gen_helpers \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-base "${KUEUE_ROOT}/../../" \
+  --boilerplate ${KUEUE_ROOT}/hack/boilerplate.go.txt
+
+kube::codegen::gen_client \
+  --input-pkg-root sigs.k8s.io/kueue/apis \
+  --output-pkg-root sigs.k8s.io/kueue/client-go \
+  --output-base "${KUEUE_ROOT}/../../" \
```

  LGTM

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-06T10:15:28Z

/lgtm
/assign @alculquicondor @tenzen-y

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-11-06T15:47:34Z

LGTM

one nit.

- **hack/update-codegen.sh:27** by [@trasc](https://github.com/trasc) — 2023-11-06T15:42:00Z
```diff
@@ -24,28 +24,28 @@ CODEGEN_PKG=$($GO_CMD list -m -f "{{.Dir}}" k8s.io/code-generator)
 
 cd $(dirname ${BASH_SOURCE[0]})/..
 
-chmod +x "${CODEGEN_PKG}/generate-internal-groups.sh"
-chmod +x "${CODEGEN_PKG}/generate-groups.sh"
-"${CODEGEN_PKG}/generate-groups.sh" \
-  applyconfiguration,client,lister,informer \
-  sigs.k8s.io/kueue/client-go \
-  sigs.k8s.io/kueue/apis \
-  kueue:v1beta1 \
-  --go-header-file ${KUEUE_ROOT}/hack/boilerplate.go.txt
-
-
-# Future releases of of code-generator add better support for out of GOPATH generation,
-# check https://github.com/kubernetes/code-generator/blob/master/examples/hack/update-codegen.sh for details,
-# for now we should just move the generated code to `client-go`.
-if  [ -d sigs.k8s.io/kueue/client-go ]
-then
-  echo "Out of GOPATH generation, move the generated files to ../client-go"
-  if  [ -d ${KUEUE_ROOT}/client-go ]
-  then
-    rm -r ${KUEUE_ROOT}/client-go
-  fi
-  mv sigs.k8s.io/kueue/client-go ${KUEUE_ROOT}/client-go
-fi
+chmod +x "${CODEGEN_PKG}/kube_codegen.sh"
```

  nit: no need to chmod if we only source the file

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-06T22:13:54Z

/approve
/hold
for @trasc's nit

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-11-07T09:57:43Z

/lgtm
/hold cancel
