# PR #1152: KEP-1145: Additional labels, annotations, tolerations and node selector for AdmissionChecks

**Summary**: KEP-1145: Additional labels, annotations, tolerations and node selector for AdmissionChecks

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1152

**Last updated**: 2023-10-06T12:57:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mwielgus](https://github.com/mwielgus)
- **Branch**: `labels-kep` → `main`
- **Created**: 2023-09-22T13:10:33Z
- **Updated**: 2023-10-06T12:57:17Z
- **Merged**: 2023-10-06T12:57:15Z
- **Closed**: 2023-10-06T12:57:15Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 2   **+198 / -0**

## Description

#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Allows to set additional labels, annotations, tolerations and node selectors on Workload's pods based on AdmissionCheck needs.

#### Which issue(s) this PR fixes:

Fixes #1145 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-09-22T13:10:39Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 32cad02332d5ecab83c747440b049702e6d0da98 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/651f2da682de730008bd01e9 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-02T19:40:48Z

Please update based on #1174

### Comment by [@mwielgus](https://github.com/mwielgus) — 2023-10-05T21:42:24Z

@alculquicondor @mimowo All comments addressed, updated the API to reflect changes made to conditions, PTAL.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T12:54:23Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-06T12:54:30Z

LGTM label has been added.  <details>Git tree hash: b940a8041d97e10f514be6ffc822fd43a6fa06bd</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-06T12:54:30Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1152#issuecomment-1750628063" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1152#" title="Author self-approved">mwielgus</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-09-22T14:55:31Z

- **keps/1145-additional-labels/README.md:41** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:34:14Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
```

  ```suggestion
  * Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
  ```

- **keps/1145-additional-labels/README.md:51** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:37:26Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
```

  I don't think we can specify taints on pods. So just annotations, labels and tolerations I suppose.

- **keps/1145-additional-labels/README.md:59** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:39:20Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
```

  I think it could be more clear if we mentioned that the nodes are preallocated by an external controller managing the state of the `AdmissionCheck`.

- **keps/1145-additional-labels/README.md:66** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:41:26Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
```

  This isn't clear to me. Do we need the story? I suggest to either drop it, since story 1 provides already enough justification, or explain a little more the use case.

- **keps/1145-additional-labels/README.md:71** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:44:27Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
```

  IIUC all integrations eventually create pods, so labels, annotations, tolerations and node selectors are supported. The pods are typically created based on some CRD, such as batch.Job.

- **keps/1145-additional-labels/README.md:124** by [@mimowo](https://github.com/mimowo) — 2023-09-22T14:50:48Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
+}
+
+// PodSetUpdate contains a list of pod set modifications suggested by AdmissionChecks.
+// The modifications should be additive only - modifications of already existing keys
+// are not allowed and will result in failure during workload admission.
+type PodSetUpdate struct {
+    // Name of the PodSet to modify. Should match to one of the Workload's PodSets.
+    Name string `json:"name"`
+
+    // +optional
+    Labels map[string]string `json:"labels,omitempty"
+
+    // +optional
+    Annotations map[string]string `json:"annotations,omitempty" 
+
+    // +optional
+    NodeSelector map[string]string `json:"nodeSelector,omitempty"
+
+    // +optional
+    Tolerations []Toleration `json:"tolerations,omitempty"
+}
+```
+
+Each of the `AdmissionChecks` controllers should populate the needed fields before 
+(or at the same time) flipping the corresponding `AdmissionCheck` to `True`. The values
+from `PodSetUpdates` are combined with `ResourceFlavors` into integration framework
+`PodSetInfo` (that will get the missing labels, tolerations and annotations fields) 
+and then passed to the framework controller for application on the CRD.
```

  I would like to see two things discussed:
  1. how the information is restored, say the batch.Job, when we suspend the workload, so that potentially new labels, annotations, and tollerations can be assigned on re-admission
  2. how about single pods support? I suppose we cannot mutate them, and there is no CRD to manage them, so IIUC we will need to recreate them, thus supporting them might require extra effort.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:15:50Z

- **keps/1145-additional-labels/README.md:41** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:15:50Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:16:40Z

- **keps/1145-additional-labels/README.md:51** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:16:40Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
```

  Sure, i meant selectors.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:18:03Z

- **keps/1145-additional-labels/README.md:59** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:18:03Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
```

  OK, updated.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:18:57Z

- **keps/1145-additional-labels/README.md:66** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:18:56Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
```

  This one justifies annotations and labels. For preallocated nodes you don't necessarily need these.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:22:10Z

- **keps/1145-additional-labels/README.md:71** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:22:09Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
```

  Yes, eventually, but we may have limited options to influence their shape. I can imagine some integrations whose CRD only exposes a fraction of PodSpecTemplate. For example - SparkOperator from Stackable, which is currently being considered for Kubeflow. See https://github.com/stackabletech/spark-k8s-operator/blob/main/docs/modules/spark-k8s/pages/crd-reference.adoc.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-22T17:28:20Z

- **keps/1145-additional-labels/README.md:124** by [@mwielgus](https://github.com/mwielgus) — 2023-09-22T17:28:20Z
```diff
@@ -0,0 +1,152 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Notes/Constraints/Caveats (Optional)](#notesconstraintscaveats-optional)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through wich `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, taints and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My `AdmissionCheck` preallocates specific nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
+}
+
+// PodSetUpdate contains a list of pod set modifications suggested by AdmissionChecks.
+// The modifications should be additive only - modifications of already existing keys
+// are not allowed and will result in failure during workload admission.
+type PodSetUpdate struct {
+    // Name of the PodSet to modify. Should match to one of the Workload's PodSets.
+    Name string `json:"name"`
+
+    // +optional
+    Labels map[string]string `json:"labels,omitempty"
+
+    // +optional
+    Annotations map[string]string `json:"annotations,omitempty" 
+
+    // +optional
+    NodeSelector map[string]string `json:"nodeSelector,omitempty"
+
+    // +optional
+    Tolerations []Toleration `json:"tolerations,omitempty"
+}
+```
+
+Each of the `AdmissionChecks` controllers should populate the needed fields before 
+(or at the same time) flipping the corresponding `AdmissionCheck` to `True`. The values
+from `PodSetUpdates` are combined with `ResourceFlavors` into integration framework
+`PodSetInfo` (that will get the missing labels, tolerations and annotations fields) 
+and then passed to the framework controller for application on the CRD.
```

  1. In the same way as currently we restore node selectors and tollerations upon workload suspensions. We will just expand the serialization by 2 fields. 
  2. We can mutate pods but only in a more limiting way. See: https://github.com/kubernetes/enhancements/tree/master/keps/sig-scheduling/3838-pod-mutable-scheduling-directives

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T10:17:30Z

- **keps/1145-additional-labels/README.md:82** by [@trasc](https://github.com/trasc) — 2023-09-26T10:17:30Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
```

  @mimowo @mwielgus this cold be another reason to change  `AdmissionChecks []metav1.Condition ` to a custom type that also contains the `PodSetUpdate` fields. (this being being in the context of the condition naming discussion we had in #1113  )

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-09-26T10:25:18Z

- **keps/1145-additional-labels/README.md:82** by [@mwielgus](https://github.com/mwielgus) — 2023-09-26T10:25:18Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
```

  Yes, we could put `PodSetUpdates[]` in the forked condition. It would give a little bit of extra clarity about who wants to set what, but at the cost of redoing all the utility functions for conditions.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-09-26T13:41:22Z

- **keps/1145-additional-labels/README.md:82** by [@trasc](https://github.com/trasc) — 2023-09-26T13:41:22Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
```

  There are mostly only two utility functions  (SetStatusConditions..., IsStatusCond... ) we can-provide our own.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-09-27T13:54:42Z

- **keps/1145-additional-labels/README.md:71** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-27T13:49:41Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
```

  ```suggestion
  Some integrations may not support setting all of the desired information into the pods due to their CRD limitations.
  ```

- **keps/1145-additional-labels/README.md:82** by [@alculquicondor](https://github.com/alculquicondor) — 2023-09-27T13:51:03Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
```

  +1 on not using metav1.Condition and creating our own. The way we are already using the `reason` field is odd enough.
  
  Also, in the existing proposal, it's not clear how multiple AdmissionChecks would interact to each provide a modification to the template.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-04T15:04:01Z

- **keps/1145-additional-labels/README.md:101** by [@mimowo](https://github.com/mimowo) — 2023-10-04T15:03:57Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
+}
+
+// PodSetUpdate contains a list of pod set modifications suggested by AdmissionChecks.
+// The modifications should be additive only - modifications of already existing keys
+// are not allowed and will result in failure during workload admission.
```

  Some questions, potentially points to clarify: 
  1. failing on conflicting keys does not seem consistent with what we do for nodeSelectors, where we overwrite (based on how the `MergeKeepFirst` is used): https://github.com/kubernetes-sigs/kueue/blob/f3bb6f2b6e509727e33b4aac84d2503cc9edaa0a/pkg/controller/jobs/job/job_controller.go#L228. 
  2. Should we fail admission if there are tolerations with the same key, but different operator, or effect? It seems there might be legitimate use cases for adding an annotation in PodSetUpdate that has the same key, but different effect.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-05T21:38:06Z

- **keps/1145-additional-labels/README.md:82** by [@mwielgus](https://github.com/mwielgus) — 2023-10-05T21:38:06Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
```

  Ok, modified.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-05T21:39:10Z

- **keps/1145-additional-labels/README.md:71** by [@mwielgus](https://github.com/mwielgus) — 2023-10-05T21:39:10Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
```

  Done.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-05T21:41:23Z

- **keps/1145-additional-labels/README.md:101** by [@mwielgus](https://github.com/mwielgus) — 2023-10-05T21:41:23Z
```diff
@@ -0,0 +1,153 @@
+# KEP-1145: Additional labels, annotations, tolerations and selector
+
+<!-- toc -->
+- [Summary](#summary)
+- [Motivation](#motivation)
+  - [Goals](#goals)
+  - [Non-Goals](#non-goals)
+- [Proposal](#proposal)
+  - [User Stories (Optional)](#user-stories-optional)
+    - [Story 1](#story-1)
+    - [Story 2](#story-2)
+  - [Risks and Mitigations](#risks-and-mitigations)
+- [Design Details](#design-details)
+  - [Test Plan](#test-plan)
+      - [Prerequisite testing updates](#prerequisite-testing-updates)
+    - [Unit Tests](#unit-tests)
+    - [Integration tests](#integration-tests)
+  - [Graduation Criteria](#graduation-criteria)
+- [Implementation History](#implementation-history)
+- [Drawbacks](#drawbacks)
+- [Alternatives](#alternatives)
+<!-- /toc -->
+
+## Summary
+
+Allow (AdmissionCheck)[https://github.com/kubernetes-sigs/kueue/tree/main/keps/993-two-phase-admission] 
+to set some additional location/admission information
+on workload's pods, so that they land on the machines of the `AdmissionCheck`
+choosing.
+
+## Motivation
+
+`AdmissionChecks` is a "plugin" mechanism for Kueue. Some of the plugins while 
+admitting may either narrow down on which machines the workload should start (by adding
+some extra node selector/tolerations) or add some tracking information
+that may be needed by other system components outside of Kueue.
+
+### Goals
+
+* Establish a mechanism through which `AdmissionChecks` can add annotations/labels/node selector/tolerations
+  to workload pods.
+
+### Non-Goals
+
+* Allow `AdmissionChecks` to change pod definition in other way, for instance change command or image name.
+* Hard code any specific labels/annotations that are passed down.
+
+## Proposal
+
+* Add a struct in `Workload` status that allows to set annotations, labels, selectors and tolerations on PodSets.
+* Expend PodSetInfo in Integration Framework to pass this information.
+* Modify existing integrations to support setting more data on the pods.
+
+### User Stories (Optional)
+
+#### Story 1
+
+My external controller managing an `AdmissionCheck` preallocates specific
+nodes (tainted and labeled) for a Workload to run, before it is 
+admitted (and creates any worker pods). Workload pods need to be modified
+so that they end up on correct nodes.
+
+#### Story 2
+
+My `AdmissionCheck` does budget checking and would like to mark pods that are possibly crossing budget with a 
+specific label for easier tracking and alerting.
+
+### Risks and Mitigations
+
+Some integrations may not support setting all of the desired information due to their CRD limitations.
+
+## Design Details
+
+Modify WorkloadStatus API object:
+
+```
+// WorkloadStatus defines the observed state of Workload
+type WorkloadStatus struct {
+    [...]
+    AdmissionChecks []metav1.Condition `json:"admissionChecks,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
+
+    PodSetUpdates []PodSetUpdate `json:"podSetUpdates,omitempty"
+}
+
+// PodSetUpdate contains a list of pod set modifications suggested by AdmissionChecks.
+// The modifications should be additive only - modifications of already existing keys
+// are not allowed and will result in failure during workload admission.
```

  For now, we should fail the workload completely if there is a conflict in node selectors/labels/etc. At least for now. If we get some feedback that conflict resolution is needed we can add it later as a non breaking change.
