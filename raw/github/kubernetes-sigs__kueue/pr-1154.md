# PR #1154: ProvisioningRequest admission check controller

**Summary**: ProvisioningRequest admission check controller

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1154

**Last updated**: 2023-10-25T17:36:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `provisioning-AC` → `main`
- **Created**: 2023-09-22T14:30:55Z
- **Updated**: 2023-10-25T17:36:54Z
- **Merged**: 2023-10-23T13:53:42Z
- **Closed**: 2023-10-23T13:53:42Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `tide/merge-method-rebase`, `cncf-cla: yes`
- **Assignees**: [@mwielgus](https://github.com/mwielgus), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@mwielgus](https://github.com/mwielgus), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 40   **+4132 / -38**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Adds a new admission check controller `ProvisioningRequestController` managing the necessary ProvisioningRequests and the workloads relevant admission checks. 


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1136

#### Special notes for your reviewer:
Since the component relays heavily on the k8s API the testing is done in integration tests.
tip: run `INTEGRATION_TARGET=./test/integration/controller/admissionchecks/provisioning/ GINKGO_ARGS="--coverpkg=sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning --coverprofile=.coverage-report.out"  make test-integration && go tool cover -html=bin/.coverage-report.out ` to check the coverage.
#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Integrate with Cluster Autoscaler's ProvisioningRequest via two stage admission
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-09-22T14:30:57Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-09-22T14:31:02Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | bcc6308be6c9dee1d2de16532a527ff2949eee8c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/653675d73816380007d3fb02 |

### Comment by [@trasc](https://github.com/trasc) — 2023-10-04T14:45:38Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2023-10-05T09:53:14Z

/test all

### Comment by [@trasc](https://github.com/trasc) — 2023-10-05T13:28:06Z

/cc @mwielgus 
/cc @alculquicondor

### Comment by [@trasc](https://github.com/trasc) — 2023-10-07T07:36:18Z

/retest

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:00:48Z

/release-note-edit
```release-note
Integrate with Cluster Autoscaler's ProvisioningRequest via two stage admission
```

### Comment by [@trasc](https://github.com/trasc) — 2023-10-19T09:54:22Z

/label tide/merge-method-rebase
/hold
for squash

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:06:37Z

overall LGTM, but we still need to complete some manual testing

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-20T14:00:05Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1154#pullrequestreview-1690109150" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1154#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:29:16Z

@trasc @alculquicondor Can we add docs for cluster autoscaler integration?

### Comment by [@trasc](https://github.com/trasc) — 2023-10-23T12:34:08Z

> @trasc @alculquicondor Can we add docs for cluster autoscaler integration?

#1242

### Comment by [@mwielgus](https://github.com/mwielgus) — 2023-10-23T13:25:33Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-23T13:25:40Z

LGTM label has been added.  <details>Git tree hash: 14b7233a2f0e7b42695d910af5690a5fbf7fdfdd</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-23T13:29:12Z

/hold cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-23T13:33:22Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-23T13:33:29Z

LGTM label has been added.  <details>Git tree hash: 8b265f87f38b67ed7adb18789bd397280ce5a1e2</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:36:52Z

/kind feature

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-11T00:01:58Z

First pass.
I didn't check tests yet.

- **pkg/controller/admissionchecks/provisioning/cache.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T22:58:18Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
```

  Use a full word: Delete or Remove.

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:30** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:03:49Z
```diff
@@ -0,0 +1,74 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
```

  Where is this validation coming from?

- **pkg/controller/admissionchecks/provisioning/controller.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:09:09Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
```

  ```suggestion
  	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:10:04Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RmCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	ach *acHandler
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) {
+		p.ach.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.RmConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.ach.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if err := p.ach.reconcileWorkloadsUsing(ctx, user, q); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	ach := &acHandler{
+		client: c.client,
+		cache:  c.cache,
+	}
+	prch := &prcHandler{ach: ach}
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueue.Workload{}).
+		Owns(&papi.ProvisioningRequest{}).
+		Watches(&kueue.AdmissionCheck{}, ach).
+		Watches(&kueue.ProvisioningRequestConfig{}, prch).
+		Complete(c)
+}
+
+func AddToScheme(s *runtime.Scheme) error {
```

  This confused me quite a bit, because this is not an API package.
  
  Prefer to import the autoscaling package directly where necessary, instead of adding this extra function.

- **cmd/kueue/main.go:174** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:11:46Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ManagerSupportsProvisioning(mgr) {
```

  why is this not inside `setupControllers`?

- **go.mod:21** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:13:34Z
```diff
@@ -18,6 +18,7 @@ require (
 	k8s.io/api v0.28.2
 	k8s.io/apimachinery v0.28.2
 	k8s.io/apiserver v0.28.2
+	k8s.io/autoscaler/cluster-autoscaler v0.0.0-20230925095857-cf8c507d2421
```

  Let's keep an open issue in kueue to update to the latest released version, when available.
  
  @kisieland, do you have any estimate for that?

- **pkg/controller/admissionchecks/provisioning/cache.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:15:16Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
```

  same here

- **pkg/controller/admissionchecks/provisioning/cache.go:68** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:16:44Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for i := range states {
```

  ```suggestion
  	for _, state := range states {
  ```

- **pkg/controller/admissionchecks/provisioning/cache.go:83** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:18:59Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for i := range states {
+		if ac, found := c.checks[states[i].Name]; found {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive
+}
+
+// GetConfigForCheck - get the config used by the check identified by its name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *cache) GetConfigForCheck(name string) *kueue.ProvisioningRequestConfig {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+
+	ac, acFound := c.checks[name]
+	if !acFound || !parametersRefValid(ac.Spec.Parameters) {
```

  We should validate the reference in the webhook.

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:20:42Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  Without reading the rest of the code:
  
  Why do we need a cache, instead of using the informer cache provided by the controller-runtime client?

- **pkg/controller/admissionchecks/provisioning/controller.go:80** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:22:32Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
```

  this line is not necessary.

- **pkg/controller/admissionchecks/provisioning/indexer.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:25:07Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
```

  ```suggestion
  	WorkloadsControlledByCkeckKey = "status.admissionChecks"
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:25:38Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
```

  ```suggestion
  	RequestsOwnedByWorkloadKey    = "metadata.ownerReferences"
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:27:19Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
+)
+
+func indexRequestsOwner(obj client.Object) []string {
+	refs := obj.GetOwnerReferences()
+	if len(refs) == 0 {
+		return nil
+	}
+	return slices.Map(refs, func(r *metav1.OwnerReference) string { return r.Name })
+}
+
+func indexWorkloadsChecks(obj client.Object) []string {
+	wl, isWl := obj.(*kueue.Workload)
+	if !isWl || len(wl.Status.AdmissionChecks) == 0 {
+		return nil
+	}
+	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+}
+
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+	if err := indexer.IndexField(ctx, &papi.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
+		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
+	}
+	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsControlledByCkeckKey, indexWorkloadsChecks); err != nil {
+		return fmt.Errorf("setting index on workloads checks: %w", err)
+	}
+	return nil
+}
+
+func ManagerSupportsProvisioning(mgr manager.Manager) bool {
```

  ```suggestion
  func ServerSupportsProvisioningRequest(mgr manager.Manager) bool {
  ```

- **pkg/features/kube_features.go:51** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:27:48Z
```diff
@@ -47,6 +47,13 @@ const (
 	//
 	// Enables flavor fungibility.
 	FlavorFungibility featuregate.Feature = "FlavorFungibility"
+
+	// owner: @strasc
```

  ```suggestion
  	// owner: @trasc
  ```
  
  :)

- **pkg/util/testing/wrappers.go:236** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:27:55Z
```diff
@@ -233,6 +233,11 @@ func (p *PodSetWrapper) Request(r corev1.ResourceName, q string) *PodSetWrapper
 	return p
 }
 
+func (p *PodSetWrapper) Iamage(image string) *PodSetWrapper {
```

  Image

- **pkg/controller/admissionchecks/provisioning/controller.go:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:30:39Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
```

  ```golang
  log := ctrl.LoggerFrom(ctx).WithValues("workload", klog.KObj(wl))
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:38:51Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
+)
+
+func indexRequestsOwner(obj client.Object) []string {
+	refs := obj.GetOwnerReferences()
+	if len(refs) == 0 {
+		return nil
+	}
+	return slices.Map(refs, func(r *metav1.OwnerReference) string { return r.Name })
+}
+
+func indexWorkloadsChecks(obj client.Object) []string {
+	wl, isWl := obj.(*kueue.Workload)
+	if !isWl || len(wl.Status.AdmissionChecks) == 0 {
+		return nil
+	}
+	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+}
+
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
```

  Don't we need an index for which AdmissionChecks are for ProvReq?

- **pkg/controller/admissionchecks/provisioning/controller.go:164** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:50:56Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
```

  ```suggestion
  					log.V(5).Error(err, "deleting the request", "check", checkName)
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:225** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:53:56Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
```

  ```suggestion
  func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string) error {
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:482** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T23:59:29Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
```

  if this is the point of the cache, I think we can replace it with a client index?

- **pkg/controller/admissionchecks/provisioning/controller.go:497** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T00:00:03Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
```

  We didn't make ControllerName immutable? we should

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-11T09:29:05Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:30** by [@trasc](https://github.com/trasc) — 2023-10-11T07:28:31Z
```diff
@@ -0,0 +1,74 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
```

  `k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1` - the rule used for  `ProvisioningRequestSpec.ProvisioningClassName`

- **cmd/kueue/main.go:174** by [@trasc](https://github.com/trasc) — 2023-10-11T07:30:53Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ManagerSupportsProvisioning(mgr) {
```

  it could be, if the admission check controller will remain a part of kueue.

- **go.mod:21** by [@trasc](https://github.com/trasc) — 2023-10-11T07:38:03Z
```diff
@@ -18,6 +18,7 @@ require (
 	k8s.io/api v0.28.2
 	k8s.io/apimachinery v0.28.2
 	k8s.io/apiserver v0.28.2
+	k8s.io/autoscaler/cluster-autoscaler v0.0.0-20230925095857-cf8c507d2421
```

  https://github.com/kubernetes-sigs/kueue/issues/1194

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@trasc](https://github.com/trasc) — 2023-10-11T07:40:52Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  It's quicker an easier to follow, the most used  operation is `GetConfigForCheck` which will require geting the AC then getting the parameters.

- **pkg/controller/admissionchecks/provisioning/cache.go:44** by [@trasc](https://github.com/trasc) — 2023-10-11T07:42:04Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
```

  done

- **pkg/controller/admissionchecks/provisioning/cache.go:56** by [@trasc](https://github.com/trasc) — 2023-10-11T07:42:11Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
```

  done

- **pkg/controller/admissionchecks/provisioning/cache.go:68** by [@trasc](https://github.com/trasc) — 2023-10-11T07:59:42Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for i := range states {
```

  we can avoid copying the state, however done

- **pkg/controller/admissionchecks/provisioning/cache.go:83** by [@trasc](https://github.com/trasc) — 2023-10-11T08:10:35Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RmCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RmConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for i := range states {
+		if ac, found := c.checks[states[i].Name]; found {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive
+}
+
+// GetConfigForCheck - get the config used by the check identified by its name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *cache) GetConfigForCheck(name string) *kueue.ProvisioningRequestConfig {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+
+	ac, acFound := c.checks[name]
+	if !acFound || !parametersRefValid(ac.Spec.Parameters) {
```

  Currently we don't have a webbhook for AdmissionChecks, and since the ParametersReference is very flexible it's not trivial to check the existence of the object (we might need to ad a version field), In my opinion its better to leave this up to the specific admission check controllers (the controllers could register web-hooks if necessary).
  
  For now, in my opinion, it's sufficient to make this check a part of the `Active` condition evaluation of the admission check.

- **pkg/controller/admissionchecks/provisioning/controller.go:34** by [@trasc](https://github.com/trasc) — 2023-10-11T08:12:01Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:80** by [@trasc](https://github.com/trasc) — 2023-10-11T08:12:23Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:95** by [@trasc](https://github.com/trasc) — 2023-10-11T08:17:15Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
```

  The reconcile target is already identifiable.
  ```
   2023-10-11T10:20:42.17895194+03:00	LEVEL(-2)	provisioning/controller.go:93	Reconcile workload	
   	{
   		"controller": "workload", 
   		"controllerGroup": "kueue.x-k8s.io", 
   		"controllerKind": "Workload", 
   		"Workload": {
   			"name":"wl",
   			"namespace":"provisioning-sq6xz"
   			}, 
   		"namespace": "provisioning-sq6xz", 
   		"name": "wl", 
   		"reconcileID": "1f00c4b1-27a7-4ee8-89e7-def031993c20"
   	}
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:164** by [@trasc](https://github.com/trasc) — 2023-10-11T08:19:23Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:225** by [@trasc](https://github.com/trasc) — 2023-10-11T08:20:45Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:482** by [@trasc](https://github.com/trasc) — 2023-10-11T08:22:55Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
```

  not only this  , also `GetConfigForCheck(check)` which is used much more.

- **pkg/controller/admissionchecks/provisioning/controller.go:497** by [@trasc](https://github.com/trasc) — 2023-10-11T08:28:20Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
```

  we could. maybe we should create a issue to add a admission check web-hook. (from an earlier discussion I remember that CEL validation should be avoided).

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@trasc](https://github.com/trasc) — 2023-10-11T08:30:24Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RmCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	ach *acHandler
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) {
+		p.ach.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.RmConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.ach.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if err := p.ach.reconcileWorkloadsUsing(ctx, user, q); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	ach := &acHandler{
+		client: c.client,
+		cache:  c.cache,
+	}
+	prch := &prcHandler{ach: ach}
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueue.Workload{}).
+		Owns(&papi.ProvisioningRequest{}).
+		Watches(&kueue.AdmissionCheck{}, ach).
+		Watches(&kueue.ProvisioningRequestConfig{}, prch).
+		Complete(c)
+}
+
+func AddToScheme(s *runtime.Scheme) error {
```

  It was added to have the option of making the provisioning API changeable. let's keep it as is for now.
   (the same approach is used for job integrations).

- **pkg/controller/admissionchecks/provisioning/indexer.go:34** by [@trasc](https://github.com/trasc) — 2023-10-11T08:32:53Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer.go:35** by [@trasc](https://github.com/trasc) — 2023-10-11T08:32:59Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer.go:64** by [@trasc](https://github.com/trasc) — 2023-10-11T08:34:08Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
+)
+
+func indexRequestsOwner(obj client.Object) []string {
+	refs := obj.GetOwnerReferences()
+	if len(refs) == 0 {
+		return nil
+	}
+	return slices.Map(refs, func(r *metav1.OwnerReference) string { return r.Name })
+}
+
+func indexWorkloadsChecks(obj client.Object) []string {
+	wl, isWl := obj.(*kueue.Workload)
+	if !isWl || len(wl.Status.AdmissionChecks) == 0 {
+		return nil
+	}
+	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+}
+
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
```

  no

- **pkg/controller/admissionchecks/provisioning/indexer.go:64** by [@trasc](https://github.com/trasc) — 2023-10-11T08:35:02Z
```diff
@@ -0,0 +1,73 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "spec.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "spec.admissionChecks"
+)
+
+func indexRequestsOwner(obj client.Object) []string {
+	refs := obj.GetOwnerReferences()
+	if len(refs) == 0 {
+		return nil
+	}
+	return slices.Map(refs, func(r *metav1.OwnerReference) string { return r.Name })
+}
+
+func indexWorkloadsChecks(obj client.Object) []string {
+	wl, isWl := obj.(*kueue.Workload)
+	if !isWl || len(wl.Status.AdmissionChecks) == 0 {
+		return nil
+	}
+	return slices.Map(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+}
+
+func SetupIndexer(ctx context.Context, indexer client.FieldIndexer) error {
+	if err := indexer.IndexField(ctx, &papi.ProvisioningRequest{}, RequestsOwnedByWorkloadKey, indexRequestsOwner); err != nil {
+		return fmt.Errorf("setting index on provisionRequest owner: %w", err)
+	}
+	if err := indexer.IndexField(ctx, &kueue.Workload{}, WorkloadsControlledByCkeckKey, indexWorkloadsChecks); err != nil {
+		return fmt.Errorf("setting index on workloads checks: %w", err)
+	}
+	return nil
+}
+
+func ManagerSupportsProvisioning(mgr manager.Manager) bool {
```

  done

- **pkg/features/kube_features.go:51** by [@trasc](https://github.com/trasc) — 2023-10-11T08:36:12Z
```diff
@@ -47,6 +47,13 @@ const (
 	//
 	// Enables flavor fungibility.
 	FlavorFungibility featuregate.Feature = "FlavorFungibility"
+
+	// owner: @strasc
```

  done

- **pkg/util/testing/wrappers.go:236** by [@trasc](https://github.com/trasc) — 2023-10-11T08:37:20Z
```diff
@@ -233,6 +233,11 @@ func (p *PodSetWrapper) Request(r corev1.ResourceName, q string) *PodSetWrapper
 	return p
 }
 
+func (p *PodSetWrapper) Iamage(image string) *PodSetWrapper {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-16T12:50:23Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T12:50:23Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  Yes, but having another cache uses more memory and can lead to synchronization problems. There should be a reason to have the cache beyond wrapping some functions. A cache for scheduling is justified, because we need to keep track of usage and do snapshots. I don't see that need here.
  You can add a wrapper around the client to get a similar code separation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-16T12:53:56Z

- **pkg/controller/admissionchecks/provisioning/controller.go:497** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T12:53:56Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
```

  yeah, let's do that in a separate PR

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-16T17:00:18Z

- **cmd/kueue/main.go:174** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:00:18Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ManagerSupportsProvisioning(mgr) {
```

  It should. Let's move it there.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-16T20:46:47Z

- **pkg/controller/admissionchecks/provisioning/controller.go:497** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:17:50Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
```

  Although it might be better to merge that PR before this. We probably should have spec.parameters be immutable.

- **pkg/controller/admissionchecks/provisioning/controller.go:523** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:19:23Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
```

  ```suggestion
  			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on delete event", "admissionCheck", klog.KRef(ac))
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:508** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:20:26Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
```

  ```suggestion
  			ctrl.LoggerFrom(ctx).V(5).Error(err, "Failure on update event", "admissionCheck", klog.Kref(oldAc))
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:145** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:39:21Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
```

  ```suggestion
  	requestNeededForCheck := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:146** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:40:12Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
```

  ```suggestion
  	requestToCheckName := slices.ToMap(relevantChecks, func(i int) (string, string) {
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:44:09Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
```

  worth testing E2E whether overhead and limits are respected by ProvReq.

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:53** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:46:41Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
```

  Ah, this is new. Can you send an update to the KEP?

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T17:46:52Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
```

  ```suggestion
  	// If empty, all resources are considered managed.
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:649** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T18:00:46Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
+		newCondition.Message = "Unexpected parameters reference"
+	} else if !a.cache.HasConfig(ac.Spec.Parameters.Name) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
```

  ```suggestion
  		newCondition.Reason = "UnknownParametersRef"
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:645** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T18:01:51Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
```

  ```suggestion
  		newCondition.Reason = "BadParametersRef"
  ```

- **pkg/controller/admissionchecks/provisioning/controller.go:622** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T18:02:19Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
```

  Move this to another file, as it's largely independent.

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T18:03:02Z
  Add unit tests for this file.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:39:20Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
```

  ```suggestion
  	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
  ```
  
  as a Spanish speaker, `papi` sounds very weird :)

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:265** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:40:48Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
```

  ```suggestion
  			ginkgo.By("Checking provision request is deleted", func() {
  ```
  ?

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:41:00Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
```

  This should go into unit tests

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:451** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:44:49Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
```

  It could potentially be a unit test.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:531** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:45:17Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.Message).To(gomega.Equal(provisioning.NoRequestNeeded))
+					g.Expect(state.PodSetUpdates).To(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+		})
+		ginkgo.It("Should set the condition rejected when the provision fails", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request as Provisioned", func() {
```

  ```suggestion
  			ginkgo.By("Setting the provision request as Failed", func() {
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:698** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-16T20:46:34Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.Message).To(gomega.Equal(provisioning.NoRequestNeeded))
+					g.Expect(state.PodSetUpdates).To(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+		})
+		ginkgo.It("Should set the condition rejected when the provision fails", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Failed,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should keep the provisioning config in sync", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Checking that the provision request is created", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+			})
+
+			ginkgo.By("Changing the provisioning request config content", func() {
+				updatedPRC := &kueue.ProvisioningRequestConfig{}
+				prcKey := types.NamespacedName{Name: prc.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPRC)
+					if err != nil {
+						return err
+					}
+					updatedPRC.Spec.ProvisioningClassName = "provisioning-class-updated"
+					updatedPRC.Spec.Parameters = map[string]kueue.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}
+					return k8sClient.Update(ctx, updatedPRC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class-updated"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config2"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class2"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check to a missing one", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config-missing"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+
+			ginkgo.By("Checking the admission check state indicates an inactive check", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.Message).To(gomega.Equal(provisioning.CheckInactiveMessage))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
+	ginkgo.When("An AdmissionCheck is created with a bad config reference", func() {
```

  It could potentially be a unit test.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-16T22:46:14Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@mwielgus](https://github.com/mwielgus) — 2023-10-16T22:46:13Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+// ProvisioningRequestConfigStatus defines the observed state of ProvisioningRequestConfig
+type ProvisioningRequestConfigStatus struct {
+}
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the admissionchecks API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   ProvisioningRequestConfigSpec   `json:"spec,omitempty"`
```

  I'm not sure that we need the separation to Spec and Status here. These objects are unlikely to ever be reconciled, there probably won't be any conditions to be put, and should probably follow the pattern of ConfigMap where there is no spec and status.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-16T22:57:43Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@mwielgus](https://github.com/mwielgus) — 2023-10-16T22:57:43Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+// ProvisioningRequestConfigStatus defines the observed state of ProvisioningRequestConfig
+type ProvisioningRequestConfigStatus struct {
+}
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the admissionchecks API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   ProvisioningRequestConfigSpec   `json:"spec,omitempty"`
```

  Official K8S api convention says: 
  ```
  Objects whose state cannot vary from the user's desired intent MAY have only spec, and MAY rename spec to a more appropriate name.
  ```
  So we can skip both spec and status. However, we do have Spec in ResourceFlavor which is also a configuration setting (but with a bigger potential for reconciliation and status). So maybe let's drop status and keep just spec. 
  
  WDYT?
  
  cc: @alculquicondor

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-16T23:00:41Z

- **pkg/controller/admissionchecks/provisioning/cache.go:78** by [@mwielgus](https://github.com/mwielgus) — 2023-10-16T23:00:41Z
```diff
@@ -0,0 +1,115 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RemoveCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RemoveConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for _, state := range states {
+		if ac, found := c.checks[state.Name]; found {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive
+}
+
+// GetConfigForCheck - get the config used by the check identified by its name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *cache) GetConfigForCheck(name string) *kueue.ProvisioningRequestConfig {
```

  Please clarify in the comment whose name is used in params. AdmissionCheck or ProvisioningRequestConfig.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-17T10:08:00Z

- **cmd/kueue/main.go:175** by [@mwielgus](https://github.com/mwielgus) — 2023-10-17T10:08:00Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  Should be a warning.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-17T10:20:00Z

- **pkg/controller/admissionchecks/provisioning/controller.go:98** by [@mwielgus](https://github.com/mwielgus) — 2023-10-17T10:20:00Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
```

  Does it also handle workloads that are finished?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-17T12:25:25Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@trasc](https://github.com/trasc) — 2023-10-17T12:23:40Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  From a memory perspective, since we only cache `AdmissionChecks` having the provisioning controller set and `ProvisioningRequestsConfigs`, I don't think it should be an issue. 
  From a synchronization point of view, I don't think it's less reliable that the informer cache.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T14:18:16Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:18:16Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+// ProvisioningRequestConfigStatus defines the observed state of ProvisioningRequestConfig
+type ProvisioningRequestConfigStatus struct {
+}
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the admissionchecks API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   ProvisioningRequestConfigSpec   `json:"spec,omitempty"`
```

  I'm fine dropping Status for a CRD that has "Config" in the name

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T14:19:05Z

- **cmd/kueue/main.go:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:19:04Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  We don't use Warning in k8s https://github.com/kubernetes/community/blob/master/contributors/devel/sig-instrumentation/migration-to-structured-logging.md#change-log-functions

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T14:20:36Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:20:36Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered managed.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the provisioningrequestconfig API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec ProvisioningRequestConfigSpec `json:"spec,omitempty"`
```

  Just flatten the fields here

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-17T14:39:41Z

- **cmd/kueue/main.go:175** by [@mwielgus](https://github.com/mwielgus) — 2023-10-17T14:39:41Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  We do, if it is not a structure log. K/K has still plenty of klog.warning. Anyway, If provisioning request is not supported and provisioningcontroller is on, we should have something more than info. So let it be a fatal error.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T15:04:22Z

- **keps/1136-provisioning-request-support/README.md:117** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:21:07Z
```diff
@@ -105,33 +108,67 @@ OwnerReference).
 The definition of `ProvisioningRequestConfig` is relatively simple and is based on
 what can be set in `ProvisioningRequest`.
 
-```
+```go
+// ProvisioningRequestConfig is the Schema for the provisioningrequestconfig API
 type ProvisioningRequestConfig struct {
-    metav1.TypeMeta   `json:",inline"`
-    metav1.ObjectMeta `json:"metadata,omitempty"`
-    
-    ProvisioningClass string
-    Parameters map[string]string
-} 
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec ProvisioningRequestConfigSpec `json:"spec,omitempty"`
```

  revert adding spec

- **pkg/controller/admissionchecks/provisioning/controller.go:206** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:34:47Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
```

  I discussed this with @kisieland and agreed that this should be handled by CA.

- **pkg/controller/admissionchecks/provisioning/controller_test.go:54** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:40:05Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
```

  this would ignore the name of the object, which is important to unit test in our case. Maybe ignore specific fields?

- **pkg/controller/admissionchecks/provisioning/controller_test.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:42:21Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
```

  put it in the wrapper package?

- **pkg/controller/admissionchecks/provisioning/controller_test.go:115** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:43:58Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
```

  ```suggestion
  	baseFlavor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
  	baseFlavor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
  ```

- **pkg/controller/admissionchecks/provisioning/controller_test.go:297** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:46:11Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
```

  ```suggestion
  		"missing one template": {
  ```

- **pkg/controller/admissionchecks/provisioning/indexer_test.go:42** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:53:53Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	TestNamespace = "ns"
+)
+
+func getClientBuilder() (*fake.ClientBuilder, context.Context) {
```

  We already have `testing.NewClientBuilder` that does something similar.

- **pkg/controller/admissionchecks/provisioning/controller_test.go:561** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:56:29Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
+			workload:  baseWorkload.DeepCopy(),
+			checks:    []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:   []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:   []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:  []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request out of sync": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check1",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-main",
+								},
+								Count: 1,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v0",
+						},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request removed on workload finished": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+
+			checks:               []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:              []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:              []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:             []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates:            []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check1"},
+		},
+		"when request fails": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Failed, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}).
+					Obj(),
+			},
+		},
+		"when capacity is available": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.CapacityAvailable, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when request is provisioned": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Provisioned, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when no request is needed": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{"example.org/gpu"},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: NoRequestNeeded,
+					}).
+					Obj(),
+			},
+		},
+		"when request is needed for one PodSet": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				"wl-check1": {
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-ps2",
+								},
+								Count: 3,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v1",
+						},
+					},
+				},
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"when the request is removed while the check is ready": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				AdmissionChecks(kueue.AdmissionCheckState{
+					Name:  "check1",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+
+			checks:  []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors: []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+
+			builder = builder.WithObjects(tc.workload)
+			builder = builder.WithStatusSubresource(tc.workload)
+
+			builder = builder.WithObjects(slices.Map(tc.requests, func(o **autoscaling.ProvisioningRequest) client.Object { return *o })...)
```

  you can use `builder.WithLists`

- **pkg/controller/admissionchecks/provisioning/controller_test.go:614** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:58:31Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
+			workload:  baseWorkload.DeepCopy(),
+			checks:    []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:   []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:   []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:  []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request out of sync": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check1",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-main",
+								},
+								Count: 1,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v0",
+						},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request removed on workload finished": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+
+			checks:               []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:              []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:              []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:             []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates:            []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check1"},
+		},
+		"when request fails": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Failed, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}).
+					Obj(),
+			},
+		},
+		"when capacity is available": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.CapacityAvailable, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when request is provisioned": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Provisioned, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when no request is needed": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{"example.org/gpu"},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: NoRequestNeeded,
+					}).
+					Obj(),
+			},
+		},
+		"when request is needed for one PodSet": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				"wl-check1": {
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-ps2",
+								},
+								Count: 3,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v1",
+						},
+					},
+				},
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"when the request is removed while the check is ready": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				AdmissionChecks(kueue.AdmissionCheckState{
+					Name:  "check1",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+
+			checks:  []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors: []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+
+			builder = builder.WithObjects(tc.workload)
+			builder = builder.WithStatusSubresource(tc.workload)
+
+			builder = builder.WithObjects(slices.Map(tc.requests, func(o **autoscaling.ProvisioningRequest) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.templates, func(o **corev1.PodTemplate) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.configs, func(o **kueue.ProvisioningRequestConfig) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.checks, func(o **kueue.AdmissionCheck) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.flavors, func(o **kueue.ResourceFlavor) client.Object { return *o })...)
+
+			k8sclient := builder.Build()
+			controller := NewController(k8sclient)
+			for _, check := range tc.checks {
+				controller.cache.AddOrUpdateCheck(check)
+			}
+
+			for _, config := range tc.configs {
+				controller.cache.AddOrUpdateConfig(config)
+			}
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: TestNamespace,
+					Name:      tc.workload.Name,
+				},
+			}
+			_, gotReconcileError := controller.Reconcile(ctx, req)
+			if diff := cmp.Diff(tc.wantReconcileError, gotReconcileError); diff != "" {
+				t.Errorf("unexpected reconcile error (-want/+got):\n%s", diff)
+			}
+
+			for name, wantWl := range tc.wantWorkloads {
+				gotWl := &kueue.Workload{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotWl); err != nil {
+					t.Errorf("unexpected error getting workload %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantWl, gotWl, wlCmpOptions...); diff != "" {
+					t.Errorf("unexpected workload %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for name, wantRequest := range tc.wantRequests {
+				gotRequest := &autoscaling.ProvisioningRequest{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotRequest); err != nil {
+					t.Errorf("unexpected error getting request %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantRequest, gotRequest, reqCmpOptions...); diff != "" {
+					t.Errorf("unexpected request %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for name, wantTemplate := range tc.wantTemplates {
+				gotTemplate := &corev1.PodTemplate{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotTemplate); err != nil {
+					t.Errorf("unexpected error getting template %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantTemplate, gotTemplate, tmplCmpOptions...); diff != "" {
+					t.Errorf("unexpected template %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for _, name := range tc.wantRequestsNotFound {
+				gotRequest := &autoscaling.ProvisioningRequest{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotRequest); !apierrors.IsNotFound(err) {
+					t.Errorf("request %q should no longer be found", name)
+				}
+			}
+
+		})
+	}
+
+}
+
+func TestReconcileCheck(t *testing.T) {
```

  ```suggestion
  func TestReconcileAdmissionCheck(t *testing.T) {
  ```
  
  Put it in a separate file, just like the code should as well.

- **pkg/controller/admissionchecks/provisioning/indexer_test.go:66** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T14:59:37Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	TestNamespace = "ns"
+)
+
+func getClientBuilder() (*fake.ClientBuilder, context.Context) {
+
+	scheme := runtime.NewScheme()
+	if err := clientgoscheme.AddToScheme(scheme); err != nil {
+		panic(err)
+	}
+	if err := kueue.AddToScheme(scheme); err != nil {
+		panic(err)
+	}
+
+	if err := AddToScheme(scheme); err != nil {
+		panic(err)
+	}
+
+	ctx := context.Background()
+	builder := fake.NewClientBuilder().WithScheme(scheme).WithObjects(&corev1.Namespace{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: TestNamespace,
+		},
+	})
+	_ = SetupIndexer(ctx, utiltesting.AsIndexer(builder))
+	return builder, ctx
+}
+
+func TestIndexRequests(t *testing.T) {
```

  ```suggestion
  func TestIndexProvisioningRequests(t *testing.T) {
  ```

- **pkg/controller/admissionchecks/provisioning/controller_test.go:108** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:01:30Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
```

  Let's add an Admission check that is not related to ProvReq

- **test/util/util.go:462** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:02:29Z
```diff
@@ -436,3 +458,26 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func SetWorkloadsAdmissionCkeck(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, check string, state kueue.CheckState, expectExisting bool) {
```

  ```suggestion
  func SetWorkloadsAdmissionCheck(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, check string, state kueue.CheckState, expectExisting bool) {
  ```

- **test/util/util.go:334** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:03:10Z
```diff
@@ -317,6 +326,19 @@ func ExpectAdmissionCheckToBeDeleted(ctx context.Context, k8sClient client.Clien
 	}, Timeout, Interval).Should(testing.BeNotFoundError())
 }
 
+func ExpectProvisioningRequestConfigToBeDeleted(ctx context.Context, k8sClient client.Client, prc *kueue.ProvisioningRequestConfig, deleteAC bool) {
+	if prc == nil {
+		return
+	}
+	if deleteAC {
+		gomega.Expect(client.IgnoreNotFound(DeleteProvisioningRequestConfig(ctx, k8sClient, prc))).To(gomega.Succeed())
```

  ```suggestion
  		gomega.ExpectWithOffset(1, client.IgnoreNotFound(DeleteProvisioningRequestConfig(ctx, k8sClient, prc))).To(gomega.Succeed())
  ```

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:277** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:03:37Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
```

  You can remove this test now.

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:451** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:03:50Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
```

  Remove this test

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:698** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:04:16Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.Message).To(gomega.Equal(provisioning.NoRequestNeeded))
+					g.Expect(state.PodSetUpdates).To(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+		})
+		ginkgo.It("Should set the condition rejected when the provision fails", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Failed,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should keep the provisioning config in sync", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Checking that the provision request is created", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+			})
+
+			ginkgo.By("Changing the provisioning request config content", func() {
+				updatedPRC := &kueue.ProvisioningRequestConfig{}
+				prcKey := types.NamespacedName{Name: prc.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPRC)
+					if err != nil {
+						return err
+					}
+					updatedPRC.Spec.ProvisioningClassName = "provisioning-class-updated"
+					updatedPRC.Spec.Parameters = map[string]kueue.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}
+					return k8sClient.Update(ctx, updatedPRC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class-updated"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config2"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class2"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check to a missing one", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config-missing"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+
+			ginkgo.By("Checking the admission check state indicates an inactive check", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.Message).To(gomega.Equal(provisioning.CheckInactiveMessage))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
+	ginkgo.When("An AdmissionCheck is created with a bad config reference", func() {
```

  Let's keep this one

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T15:07:57Z

- **cmd/kueue/main.go:175** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:07:56Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  > We do, if it is not a structure log
  
  That's legacy code that we are cleaning up.
  
  I much rather do an error and exit
  
  If we are going to error, we should have a permanent configuration (not just feature gate) that allows to disable the feature, and probably disabled by default, as not every cluster has a cluster autoscaler.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-17T15:16:06Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:42** by [@trasc](https://github.com/trasc) — 2023-10-17T13:07:04Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
```

  done

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:53** by [@trasc](https://github.com/trasc) — 2023-10-17T13:37:33Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
```

  done

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:75** by [@trasc](https://github.com/trasc) — 2023-10-17T13:37:45Z
```diff
@@ -0,0 +1,90 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered manged.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+// ProvisioningRequestConfigStatus defines the observed state of ProvisioningRequestConfig
+type ProvisioningRequestConfigStatus struct {
+}
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:subresource:status
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the admissionchecks API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec   ProvisioningRequestConfigSpec   `json:"spec,omitempty"`
```

  done

- **cmd/kueue/main.go:175** by [@trasc](https://github.com/trasc) — 2023-10-17T13:45:06Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  We only have `error` and `non error` options with an optional added V level , this is highest `non error`.

- **pkg/controller/admissionchecks/provisioning/cache.go:78** by [@trasc](https://github.com/trasc) — 2023-10-17T13:48:49Z
```diff
@@ -0,0 +1,115 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
+	lock    sync.RWMutex
+	checks  map[string]*kueue.AdmissionCheck
+	configs map[string]*kueue.ProvisioningRequestConfig
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+func (c *cache) AddOrUpdateCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.checks[check.Name] = check
+}
+
+func (c *cache) RemoveCheck(check *kueue.AdmissionCheck) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.checks, check.Name)
+}
+
+func (c *cache) AddOrUpdateConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	c.configs[config.Name] = config
+}
+
+func (c *cache) RemoveConfig(config *kueue.ProvisioningRequestConfig) {
+	c.lock.Lock()
+	defer c.lock.Unlock()
+	delete(c.configs, config.Name)
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *cache) FilterChecks(states []kueue.AdmissionCheckState) []string {
+	c.lock.RLock()
+	defer c.lock.RUnlock()
+	var retActive []string
+	for _, state := range states {
+		if ac, found := c.checks[state.Name]; found {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive
+}
+
+// GetConfigForCheck - get the config used by the check identified by its name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *cache) GetConfigForCheck(name string) *kueue.ProvisioningRequestConfig {
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:1** by [@trasc](https://github.com/trasc) — 2023-10-17T13:49:21Z
  done

- **pkg/controller/admissionchecks/provisioning/controller.go:145** by [@trasc](https://github.com/trasc) — 2023-10-17T13:51:53Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:146** by [@trasc](https://github.com/trasc) — 2023-10-17T14:30:09Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:206** by [@trasc](https://github.com/trasc) — 2023-10-17T14:31:44Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
```

  I remember this was discussed offline, aoutoscaling should take care of this on it's side.
  removed

- **pkg/controller/admissionchecks/provisioning/controller.go:497** by [@trasc](https://github.com/trasc) — 2023-10-17T14:39:01Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
```

  #1218

- **pkg/controller/admissionchecks/provisioning/controller.go:508** by [@trasc](https://github.com/trasc) — 2023-10-17T14:45:03Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:523** by [@trasc](https://github.com/trasc) — 2023-10-17T14:45:30Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:645** by [@trasc](https://github.com/trasc) — 2023-10-17T14:47:35Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:649** by [@trasc](https://github.com/trasc) — 2023-10-17T14:47:42Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
+		newCondition.Message = "Unexpected parameters reference"
+	} else if !a.cache.HasConfig(ac.Spec.Parameters.Name) {
+		newCondition.Status = metav1.ConditionFalse
+		newCondition.Reason = "BadRef"
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:27** by [@trasc](https://github.com/trasc) — 2023-10-17T14:50:09Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:265** by [@trasc](https://github.com/trasc) — 2023-10-17T14:50:49Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:277** by [@trasc](https://github.com/trasc) — 2023-10-17T14:51:33Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:451** by [@trasc](https://github.com/trasc) — 2023-10-17T14:52:23Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:531** by [@trasc](https://github.com/trasc) — 2023-10-17T14:53:17Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.Message).To(gomega.Equal(provisioning.NoRequestNeeded))
+					g.Expect(state.PodSetUpdates).To(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+		})
+		ginkgo.It("Should set the condition rejected when the provision fails", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request as Provisioned", func() {
```

  done

- **test/integration/controller/admissionchecks/provisioning/provisioning_test.go:698** by [@trasc](https://github.com/trasc) — 2023-10-17T14:53:55Z
```diff
@@ -0,0 +1,797 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+	http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+package provisioning
+
+import (
+	"github.com/google/go-cmp/cmp/cmpopts"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/admissionchecks/provisioning"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	"sigs.k8s.io/kueue/pkg/workload"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	customResourceOne = "example.org/res1"
+	customResourceTwo = "example.org/res2"
+)
+
+var _ = ginkgo.Describe("Provisioning", func() {
+
+	ginkgo.When("A workload is using a provision admission check", func() {
+		var (
+			ns        *corev1.Namespace
+			wlKey     types.NamespacedName
+			ac        *kueue.AdmissionCheck
+			prc       *kueue.ProvisioningRequestConfig
+			prc2      *kueue.ProvisioningRequestConfig
+			rf        *kueue.ResourceFlavor
+			admission *kueue.Admission
+		)
+		ginkgo.BeforeEach(func() {
+			ns = &corev1.Namespace{
+				ObjectMeta: metav1.ObjectMeta{
+					GenerateName: "provisioning-",
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, ns)).To(gomega.Succeed())
+
+			prc = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1",
+						"p2": "v2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc)).To(gomega.Succeed())
+
+			prc2 = &kueue.ProvisioningRequestConfig{
+				ObjectMeta: metav1.ObjectMeta{
+					Name: "prov-config2",
+				},
+				Spec: kueue.ProvisioningRequestConfigSpec{
+					ProvisioningClassName: "provisioning-class2",
+					Parameters: map[string]kueue.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					},
+				},
+			}
+			gomega.Expect(k8sClient.Create(ctx, prc2)).To(gomega.Succeed())
+
+			ac = testing.MakeAdmissionCheck("ac-prov").
+				ControllerName(provisioning.ControllerName).
+				Parameters(kueue.GroupVersion.Group, "ProvisioningRequestConfig", prc.Name).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, ac)).To(gomega.Succeed())
+
+			rf = testing.MakeResourceFlavor("rf1").Label("ns1", "ns1v").Obj()
+			gomega.Expect(k8sClient.Create(ctx, rf)).To(gomega.Succeed())
+
+			wl := testing.MakeWorkload("wl", ns.Name).
+				PodSets(
+					*testing.MakePodSet("ps1", 3).
+						Request(corev1.ResourceCPU, "1").
+						Image("iamge").
+						Obj(),
+					*testing.MakePodSet("ps2", 6).
+						Request(corev1.ResourceCPU, "500m").
+						Request(customResourceOne, "1").
+						Limit(customResourceOne, "1").
+						Image("iamge").
+						Obj(),
+				).
+				Obj()
+			gomega.Expect(k8sClient.Create(ctx, wl)).To(gomega.Succeed())
+			wlKey = client.ObjectKeyFromObject(wl)
+
+			admission = testing.MakeAdmission("q").
+				PodSets(
+					kueue.PodSetAssignment{
+						Name: "ps1",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("3"),
+						},
+						Count: ptr.To[int32](3),
+					},
+					kueue.PodSetAssignment{
+						Name: "ps2",
+						Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+							corev1.ResourceCPU: kueue.ResourceFlavorReference(rf.Name),
+						},
+						ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+							corev1.ResourceCPU: resource.MustParse("2"),
+						},
+						Count: ptr.To[int32](4),
+					},
+				).
+				Obj()
+
+		})
+
+		ginkgo.AfterEach(func() {
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, rf, true)
+			util.ExpectAdmissionCheckToBeDeleted(ctx, k8sClient, ac, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc2, true)
+			util.ExpectProvisioningRequestConfigToBeDeleted(ctx, k8sClient, prc, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+		})
+
+		ginkgo.It("Should not create provisioning requests before quota is reserved", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Consistently(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.ConsistentDuration, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests after quota is reserved and remove it when reservation is lost", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(2))
+
+				ps1 := createdRequest.Spec.PodSets[0]
+				gomega.Expect(ps1.Count).To(gomega.Equal(int32(3)))
+				gomega.Expect(ps1.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps1.PodTemplateRef.Name,
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[0].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+
+				ps2 := createdRequest.Spec.PodSets[1]
+				gomega.Expect(ps2.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps2.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+
+				// check the created pod template
+				templateKey.Name = ps2.PodTemplateRef.Name
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should create provisioning requests for relevant podsets only", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceOne,
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			ginkgo.By("Checking that the provision request is created", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ignoreContainersDefaults := cmpopts.IgnoreFields(corev1.Container{}, "TerminationMessagePath", "TerminationMessagePolicy", "ImagePullPolicy")
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+				gomega.Expect(createdRequest.Spec.PodSets).To(gomega.HaveLen(1))
+
+				// check the created pod template
+				createdTemplate := &corev1.PodTemplate{}
+				ps := createdRequest.Spec.PodSets[0]
+				templateKey := types.NamespacedName{
+					Namespace: createdRequest.Namespace,
+					Name:      ps.PodTemplateRef.Name,
+				}
+				gomega.Expect(ps.Count).To(gomega.Equal(int32(4)))
+				gomega.Expect(ps.PodTemplateRef.Name).NotTo(gomega.BeEmpty())
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, templateKey, createdTemplate)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+				gomega.Expect(createdTemplate.Template.Spec.Containers).To(gomega.BeComparableTo(updatedWl.Spec.PodSets[1].Template.Spec.Containers, ignoreContainersDefaults))
+				gomega.Expect(createdTemplate.Template.Spec.NodeSelector).To(gomega.BeComparableTo(map[string]string{"ns1": "ns1v"}))
+			})
+
+			ginkgo.By("Removing the quota reservation from the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, nil)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready when the provision succeed", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Provisioned,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Provisioned,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.PodSetUpdates).To(gomega.BeComparableTo([]kueue.PodSetUpdate{
+						{
+							Name: "ps1",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+						{
+							Name: "ps2",
+							Annotations: map[string]string{
+								provisioning.ConsumesAnnotationKey: provReqKey.Name,
+							},
+						},
+					}))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should set the condition ready with no request created when no podsets are relevant", func() {
+			ginkgo.By("Setting the list of managed resources", func() {
+				prcKey := client.ObjectKeyFromObject(prc)
+				updatedPrc := &kueue.ProvisioningRequestConfig{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPrc)
+					if err != nil {
+						return err
+					}
+					updatedPrc.Spec.ManagedResources = []corev1.ResourceName{
+						customResourceTwo,
+					}
+					return k8sClient.Update(ctx, updatedPrc)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateReady))
+					g.Expect(state.Message).To(gomega.Equal(provisioning.NoRequestNeeded))
+					g.Expect(state.PodSetUpdates).To(gomega.BeEmpty())
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+		})
+		ginkgo.It("Should set the condition rejected when the provision fails", func() {
+			ginkgo.By("Setting the admission check to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the provision request as Provisioned", func() {
+				createdRequest := &papi.ProvisioningRequest{}
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					if err != nil {
+						return err
+					}
+					apimeta.SetStatusCondition(&createdRequest.Status.Conditions, metav1.Condition{
+						Type:   papi.Failed,
+						Status: metav1.ConditionTrue,
+						Reason: papi.Failed,
+					})
+					return k8sClient.Status().Update(ctx, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking the admission check", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.State).To(gomega.Equal(kueue.CheckStateRejected))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+
+		ginkgo.It("Should keep the provisioning config in sync", func() {
+			updatedWl := &kueue.Workload{}
+			ginkgo.By("Setting the admission check to the workload", func() {
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Setting the quota reservation to the workload", func() {
+				updatedWl := &kueue.Workload{}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, wlKey, updatedWl)
+					if err != nil {
+						return err
+					}
+					gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, updatedWl, admission)).To(gomega.Succeed())
+					util.SetWorkloadsAdmissionCkeck(ctx, k8sClient, updatedWl, ac.Name, kueue.CheckStatePending, false)
+					return nil
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			createdRequest := &papi.ProvisioningRequest{}
+			provReqKey := types.NamespacedName{
+				Namespace: wlKey.Namespace,
+				Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+			}
+			ginkgo.By("Checking that the provision request is created", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, provReqKey, createdRequest)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the provision requests content", func() {
+				gomega.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class"))
+				gomega.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+					"p1": "v1",
+					"p2": "v2",
+				}))
+			})
+
+			ginkgo.By("Changing the provisioning request config content", func() {
+				updatedPRC := &kueue.ProvisioningRequestConfig{}
+				prcKey := types.NamespacedName{Name: prc.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, prcKey, updatedPRC)
+					if err != nil {
+						return err
+					}
+					updatedPRC.Spec.ProvisioningClassName = "provisioning-class-updated"
+					updatedPRC.Spec.Parameters = map[string]kueue.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}
+					return k8sClient.Update(ctx, updatedPRC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class-updated"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1updated",
+						"p3": "v3",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config2"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking that the config values are propagated", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					err := k8sClient.Get(ctx, provReqKey, createdRequest)
+					g.Expect(err).To(gomega.Succeed())
+					g.Expect(createdRequest.Spec.ProvisioningClassName).To(gomega.Equal("provisioning-class2"))
+					g.Expect(createdRequest.Spec.Parameters).To(gomega.BeComparableTo(map[string]papi.Parameter{
+						"p1": "v1.2",
+						"p2": "v2.2",
+					}))
+
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Changing the provisioning request config used by the admission check to a missing one", func() {
+				updatedAC := &kueue.AdmissionCheck{}
+				acKey := types.NamespacedName{Name: ac.Name}
+				gomega.Eventually(func() error {
+					err := k8sClient.Get(ctx, acKey, updatedAC)
+					if err != nil {
+						return err
+					}
+					updatedAC.Spec.Parameters.Name = "prov-config-missing"
+					return k8sClient.Update(ctx, updatedAC)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("Checking no provision request is deleted", func() {
+				provReqKey := types.NamespacedName{
+					Namespace: wlKey.Namespace,
+					Name:      provisioning.GetProvisioningRequestName(wlKey.Name, ac.Name),
+				}
+				gomega.Eventually(func() error {
+					request := &papi.ProvisioningRequest{}
+					return k8sClient.Get(ctx, provReqKey, request)
+				}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+			})
+
+			ginkgo.By("Checking the admission check state indicates an inactive check", func() {
+				gomega.Eventually(func(g gomega.Gomega) {
+					g.Expect(k8sClient.Get(ctx, wlKey, updatedWl)).To(gomega.Succeed())
+					state := workload.FindAdmissionCheck(updatedWl.Status.AdmissionChecks, ac.Name)
+					g.Expect(state).NotTo(gomega.BeNil())
+					g.Expect(state.Message).To(gomega.Equal(provisioning.CheckInactiveMessage))
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+		})
+	})
+
+	ginkgo.When("An AdmissionCheck is created with a bad config reference", func() {
```

  done

- **pkg/controller/admissionchecks/provisioning/controller.go:98** by [@trasc](https://github.com/trasc) — 2023-10-17T15:03:09Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
```

  this was changed

- **pkg/controller/admissionchecks/provisioning/controller_test.go:54** by [@trasc](https://github.com/trasc) — 2023-10-17T15:12:49Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
```

  the name is used to `Get` the object just before the checks

- **pkg/controller/admissionchecks/provisioning/controller.go:622** by [@trasc](https://github.com/trasc) — 2023-10-17T15:14:21Z
```diff
@@ -0,0 +1,715 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &autoscaling.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestExists := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return relevantChecks[i], c.requestIsNeeded(wl, relevantChecks[i]) })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestExists[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestNeeded[checkName] || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.V(5).Error(err, "deleting the request", "check", checkName)
+					return err
+				}
+			} else {
+				requestExists[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestExists {
+		checkName := requestCheckMaping[requestName]
+		if !requestNeeded[checkName] {
+			continue
+		}
+		//get the config
+		prc := c.cache.GetConfigForCheck(checkName)
+		if prc == nil {
+			// the check is not active
+			continue
+		}
+		if !exists {
+			req := &autoscaling.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: autoscaling.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+
+			expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+			psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+			podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+			for _, psName := range expectedPodSets {
+				ps, psFound := podSetMap[psName]
+				psa, psaFound := psaMap[psName]
+				if !psFound || !psaFound {
+					return errInconsistentPodSetAssignments
+				}
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, autoscaling.PodSet{
+					PodTemplateRef: autoscaling.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, psName),
+					},
+					Count: ptr.Deref(psa.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName, prc); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkName string, prc *kueue.ProvisioningRequestConfig) error {
+	request := &autoscaling.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkName),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, request)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	expectedPodSets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	podsetRefsMap := slices.ToMap(expectedPodSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, expectedPodSets[i]), expectedPodSets[i]
+	})
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(request.Spec.PodSets) != len(expectedPodSets) {
+		return c.client.Delete(ctx, request)
+	}
+
+	psaMap := slices.ToRefMap(wl.Status.Admission.PodSetAssignments, func(p *kueue.PodSetAssignment) string { return p.Name })
+	podSetMap := slices.ToRefMap(wl.Spec.PodSets, func(ps *kueue.PodSet) string { return ps.Name })
+
+	for i := range request.Spec.PodSets {
+		reqPS := &request.Spec.PodSets[i]
+		psName, refFound := podsetRefsMap[reqPS.PodTemplateRef.Name]
+		ps, psFound := podSetMap[psName]
+		psa, psaFound := psaMap[psName]
+
+		if !refFound || !psFound || !psaFound || ptr.Deref(psa.Count, 0) != reqPS.Count {
+			return c.client.Delete(ctx, request)
+		}
+
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: request.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: ps.Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, psaMap[psName])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(request, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) requestIsNeeded(wl *kueue.Workload, checkName string) bool {
+	prc := c.cache.GetConfigForCheck(checkName)
+	if prc == nil {
+		return false
+	}
+	expectdPodsets := requiredPodSets(wl.Spec.PodSets, prc.Spec.ManagedResources)
+	return len(expectdPodsets) > 0
+}
+
+func requiredPodSets(podSets []kueue.PodSet, resources []corev1.ResourceName) []string {
+	resourcesSet := sets.New(resources...)
+	users := make([]string, 0, len(podSets))
+	for i := range podSets {
+		ps := &podSets[i]
+		if len(resources) == 0 || podUses(&ps.Template.Spec, resourcesSet) {
+			users = append(users, ps.Name)
+		}
+	}
+	return users
+}
+
+func podUses(pod *corev1.PodSpec, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for i := range pod.InitContainers {
+		if containerUses(&pod.InitContainers[i], resourceSet) {
+			return true
+		}
+	}
+	for i := range pod.Containers {
+		if containerUses(&pod.Containers[i], resourceSet) {
+			return true
+		}
+	}
+	return false
+}
+
+func containerUses(cont *corev1.Container, resourceSet sets.Set[corev1.ResourceName]) bool {
+	for r := range cont.Resources.Requests {
+		if resourceSet.Has(r) {
+			return true
+		}
+	}
+	return false
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]autoscaling.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]autoscaling.Parameter, len(in))
+	for k, v := range in {
+		out[k] = autoscaling.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *autoscaling.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != autoscaling.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else if !c.requestIsNeeded(wl, check) {
+			if checkState.State != kueue.CheckStateReady {
+				updated = true
+				checkState.State = kueue.CheckStateReady
+				checkState.Message = NoRequestNeeded
+				checkState.PodSetUpdates = nil
+			}
+		} else {
+			pr := &autoscaling.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, autoscaling.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr, check)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, pr *autoscaling.ProvisioningRequest, checkName string) []kueue.PodSetUpdate {
+	podSets := wl.Spec.PodSets
+	refMap := slices.ToMap(podSets, func(i int) (string, string) {
+		return getProvisioningRequestPodTemplateName(wl.Name, checkName, podSets[i].Name), podSets[i].Name
+	})
+	return slices.Map(pr.Spec.PodSets, func(ps *autoscaling.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        refMap[ps.PodTemplateRef.Name],
+			Annotations: map[string]string{ConsumesAnnotationKey: pr.Name},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RemoveCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RemoveCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	cache             *cache
+	acHandlerOverride func(ctx context.Context, config string, q workqueue.RateLimitingInterface) error
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) || !slices.CmpNoOrder(oldPRC.Spec.ManagedResources, newPRC.Spec.ManagedResources) {
+		p.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.cache.RemoveConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if p.acHandlerOverride != nil {
+			if err := p.acHandlerOverride(ctx, user, q); err != nil {
+				return err
+			}
+		} else {
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Name: user,
+				},
+			}
+			q.Add(req)
+		}
+	}
+	return nil
+}
+
+type acReconciler struct {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-17T15:21:55Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:70** by [@trasc](https://github.com/trasc) — 2023-10-17T15:21:55Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered managed.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the provisioningrequestconfig API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec ProvisioningRequestConfigSpec `json:"spec,omitempty"`
```

  will it help? 
  
  From https://github.com/kubernetes-sigs/kueue/pull/1154#discussion_r1361340920 I understand that it should be Spec or sorting else.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-17T15:46:16Z

- **apis/kueue/v1beta1/provisioningrequestconfig_types.go:70** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-17T15:46:15Z
```diff
@@ -0,0 +1,84 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package v1beta1
+
+import (
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+)
+
+// ProvisioningRequestConfigSpec defines the desired state of ProvisioningRequestConfig
+type ProvisioningRequestConfigSpec struct {
+	// ProvisioningClassName describes the different modes of provisioning the resources.
+	// Check autoscaling.x-k8s.io ProvisioningRequestSpec.ProvisioningClassName for details.
+	//
+	// +kubebuilder:validation:Required
+	// +kubebuilder:validation:Pattern=`^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$`
+	// +kubebuilder:validation:MaxLength=253
+	ProvisioningClassName string `json:"provisioningClassName"`
+
+	// Parameters contains all other parameters classes may require.
+	//
+	// +optional
+	// +kubebuilder:validation:MaxProperties=100
+	Parameters map[string]Parameter `json:"parameters,omitempty"`
+
+	// managedResources contains the list of resources managed by the autoscaling.
+	//
+	// If empty, all resources are considered managed.
+	//
+	// If not empty, the ProvisioningRequest will contain only the podsets that are
+	// requesting at least one of them.
+	//
+	// If none of the workloads podsets is requesting at least a managed resource,
+	// the workload is considered ready.
+	//
+	// +optional
+	// +listType=set
+	// +kubebuilder:validation:MaxItems=100
+	ManagedResources []corev1.ResourceName `json:"managedResources,omitempty"`
+}
+
+// Parameter is limited to 255 characters.
+// +kubebuilder:validation:MaxLength=255
+type Parameter string
+
+//+genclient
+//+kubebuilder:object:root=true
+//+kubebuilder:storageversion
+//+kubebuilder:resource:scope=Cluster
+
+// ProvisioningRequestConfig is the Schema for the provisioningrequestconfig API
+type ProvisioningRequestConfig struct {
+	metav1.TypeMeta   `json:",inline"`
+	metav1.ObjectMeta `json:"metadata,omitempty"`
+
+	Spec ProvisioningRequestConfigSpec `json:"spec,omitempty"`
```

  Good point, let's keep it as Spec

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2023-10-17T15:58:09Z

- **cmd/kueue/main.go:175** by [@mwielgus](https://github.com/mwielgus) — 2023-10-17T15:58:09Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+				setupLog.Info("Provisioning Requests are not supported, skip admission check controller")
```

  Eventually, yes, we need a flag/field like with integrations. I guess for now/this PR it may be just a featuregate.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-17T16:57:38Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-17T16:57:38Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  I'm still wondering if we should have this cache. 
  
  > From a synchronization point of view, I don't think it's less reliable that the informer cache.
  
  Certainly, currently, this cache is much more reliable since this is the fresh code and reviewed by many reviewers. However, codes are going to degrade little by little.
  
  I think that the informer cache is more reliable than this one since, in general, libraries used in many places can get feedback and bug reports. So the informer cache would be reliable than this cache from a long-term perspective.
  
  Also, I think we shouldn't reinvent the wheel as much as possible. If existing cache systems don't match kueue use cases, we can implement new cache systems like `/pkg/cache`.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-18T08:54:08Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@trasc](https://github.com/trasc) — 2023-10-18T08:54:08Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  Well, one could argue that `/pkg/cache` could be removed :) ... however , cache removed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-18T09:38:01Z

- **pkg/controller/admissionchecks/provisioning/controller_test.go:64** by [@trasc](https://github.com/trasc) — 2023-10-18T09:00:35Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
```

  do we really want to add the dependecy to autoscaling there just for this?

- **pkg/controller/admissionchecks/provisioning/controller_test.go:108** by [@trasc](https://github.com/trasc) — 2023-10-18T09:07:47Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
```

  done

- **pkg/controller/admissionchecks/provisioning/controller_test.go:115** by [@trasc](https://github.com/trasc) — 2023-10-18T09:08:40Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
```

  done

- **pkg/controller/admissionchecks/provisioning/controller_test.go:297** by [@trasc](https://github.com/trasc) — 2023-10-18T09:09:09Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
```

  done

- **pkg/controller/admissionchecks/provisioning/controller_test.go:561** by [@trasc](https://github.com/trasc) — 2023-10-18T09:13:51Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
+			workload:  baseWorkload.DeepCopy(),
+			checks:    []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:   []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:   []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:  []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request out of sync": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check1",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-main",
+								},
+								Count: 1,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v0",
+						},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request removed on workload finished": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+
+			checks:               []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:              []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:              []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:             []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates:            []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check1"},
+		},
+		"when request fails": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Failed, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}).
+					Obj(),
+			},
+		},
+		"when capacity is available": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.CapacityAvailable, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when request is provisioned": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Provisioned, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when no request is needed": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{"example.org/gpu"},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: NoRequestNeeded,
+					}).
+					Obj(),
+			},
+		},
+		"when request is needed for one PodSet": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				"wl-check1": {
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-ps2",
+								},
+								Count: 3,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v1",
+						},
+					},
+				},
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"when the request is removed while the check is ready": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				AdmissionChecks(kueue.AdmissionCheckState{
+					Name:  "check1",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+
+			checks:  []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors: []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+
+			builder = builder.WithObjects(tc.workload)
+			builder = builder.WithStatusSubresource(tc.workload)
+
+			builder = builder.WithObjects(slices.Map(tc.requests, func(o **autoscaling.ProvisioningRequest) client.Object { return *o })...)
```

  we'll need to either change the way we store them in the `tc` of do another type of conversion

- **pkg/controller/admissionchecks/provisioning/controller_test.go:614** by [@trasc](https://github.com/trasc) — 2023-10-18T09:16:08Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
+			workload:  baseWorkload.DeepCopy(),
+			checks:    []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:   []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:   []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:  []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request out of sync": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check1",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-main",
+								},
+								Count: 1,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v0",
+						},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request removed on workload finished": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+
+			checks:               []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:              []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:              []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:             []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates:            []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check1"},
+		},
+		"when request fails": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Failed, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}).
+					Obj(),
+			},
+		},
+		"when capacity is available": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.CapacityAvailable, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when request is provisioned": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Provisioned, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when no request is needed": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{"example.org/gpu"},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: NoRequestNeeded,
+					}).
+					Obj(),
+			},
+		},
+		"when request is needed for one PodSet": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				"wl-check1": {
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-ps2",
+								},
+								Count: 3,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v1",
+						},
+					},
+				},
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"when the request is removed while the check is ready": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				AdmissionChecks(kueue.AdmissionCheckState{
+					Name:  "check1",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+
+			checks:  []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors: []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+
+			builder = builder.WithObjects(tc.workload)
+			builder = builder.WithStatusSubresource(tc.workload)
+
+			builder = builder.WithObjects(slices.Map(tc.requests, func(o **autoscaling.ProvisioningRequest) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.templates, func(o **corev1.PodTemplate) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.configs, func(o **kueue.ProvisioningRequestConfig) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.checks, func(o **kueue.AdmissionCheck) client.Object { return *o })...)
+			builder = builder.WithObjects(slices.Map(tc.flavors, func(o **kueue.ResourceFlavor) client.Object { return *o })...)
+
+			k8sclient := builder.Build()
+			controller := NewController(k8sclient)
+			for _, check := range tc.checks {
+				controller.cache.AddOrUpdateCheck(check)
+			}
+
+			for _, config := range tc.configs {
+				controller.cache.AddOrUpdateConfig(config)
+			}
+			req := reconcile.Request{
+				NamespacedName: types.NamespacedName{
+					Namespace: TestNamespace,
+					Name:      tc.workload.Name,
+				},
+			}
+			_, gotReconcileError := controller.Reconcile(ctx, req)
+			if diff := cmp.Diff(tc.wantReconcileError, gotReconcileError); diff != "" {
+				t.Errorf("unexpected reconcile error (-want/+got):\n%s", diff)
+			}
+
+			for name, wantWl := range tc.wantWorkloads {
+				gotWl := &kueue.Workload{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotWl); err != nil {
+					t.Errorf("unexpected error getting workload %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantWl, gotWl, wlCmpOptions...); diff != "" {
+					t.Errorf("unexpected workload %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for name, wantRequest := range tc.wantRequests {
+				gotRequest := &autoscaling.ProvisioningRequest{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotRequest); err != nil {
+					t.Errorf("unexpected error getting request %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantRequest, gotRequest, reqCmpOptions...); diff != "" {
+					t.Errorf("unexpected request %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for name, wantTemplate := range tc.wantTemplates {
+				gotTemplate := &corev1.PodTemplate{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotTemplate); err != nil {
+					t.Errorf("unexpected error getting template %q", name)
+
+				}
+
+				if diff := cmp.Diff(wantTemplate, gotTemplate, tmplCmpOptions...); diff != "" {
+					t.Errorf("unexpected template %q (-want/+got):\n%s", name, diff)
+				}
+			}
+
+			for _, name := range tc.wantRequestsNotFound {
+				gotRequest := &autoscaling.ProvisioningRequest{}
+				if err := k8sclient.Get(ctx, types.NamespacedName{Namespace: TestNamespace, Name: name}, gotRequest); !apierrors.IsNotFound(err) {
+					t.Errorf("request %q should no longer be found", name)
+				}
+			}
+
+		})
+	}
+
+}
+
+func TestReconcileCheck(t *testing.T) {
```

  done

- **pkg/controller/admissionchecks/provisioning/indexer_test.go:42** by [@trasc](https://github.com/trasc) — 2023-10-18T09:18:19Z
```diff
@@ -0,0 +1,278 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	clientgoscheme "k8s.io/client-go/kubernetes/scheme"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/fake"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	TestNamespace = "ns"
+)
+
+func getClientBuilder() (*fake.ClientBuilder, context.Context) {
```

  the `scheme` and indexes we want to use are different.

- **test/util/util.go:462** by [@trasc](https://github.com/trasc) — 2023-10-18T09:20:36Z
```diff
@@ -436,3 +458,26 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func SetWorkloadsAdmissionCkeck(ctx context.Context, k8sClient client.Client, wl *kueue.Workload, check string, state kueue.CheckState, expectExisting bool) {
```

  done

- **test/util/util.go:334** by [@trasc](https://github.com/trasc) — 2023-10-18T09:21:24Z
```diff
@@ -317,6 +326,19 @@ func ExpectAdmissionCheckToBeDeleted(ctx context.Context, k8sClient client.Clien
 	}, Timeout, Interval).Should(testing.BeNotFoundError())
 }
 
+func ExpectProvisioningRequestConfigToBeDeleted(ctx context.Context, k8sClient client.Client, prc *kueue.ProvisioningRequestConfig, deleteAC bool) {
+	if prc == nil {
+		return
+	}
+	if deleteAC {
+		gomega.Expect(client.IgnoreNotFound(DeleteProvisioningRequestConfig(ctx, k8sClient, prc))).To(gomega.Succeed())
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-18T10:03:12Z

- **cmd/kueue/main.go:174** by [@trasc](https://github.com/trasc) — 2023-10-18T10:03:12Z
```diff
@@ -163,6 +168,24 @@ func main() {
 	// Controllers who register after manager starts will start directly.
 	go setupControllers(mgr, cCache, queues, certsReady, &cfg, serverVersionFetcher)
 
+	if features.Enabled(features.ProvisioningACC) {
+		// setup provision controller
+		go func() {
+			if !provisioning.ManagerSupportsProvisioning(mgr) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-18T10:03:22Z

- **pkg/controller/admissionchecks/provisioning/controller.go:554** by [@trasc](https://github.com/trasc) — 2023-10-18T10:03:22Z
```diff
@@ -0,0 +1,576 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"crypto/sha1"
+	"encoding/hex"
+	"errors"
+	"fmt"
+	"maps"
+	"sync"
+
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/runtime"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/apimachinery/pkg/util/sets"
+	papi "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/client-go/util/workqueue"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/event"
+	"sigs.k8s.io/controller-runtime/pkg/handler"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	"sigs.k8s.io/kueue/pkg/workload"
+)
+
+const (
+	objNameHashLength = 5
+	// 253 is the maximal length for a CRD name. We need to subtract one for '-', and the hash length.
+	objNameMaxPrefixLength = 252 - objNameHashLength
+	podTemplatesPrefix     = "ppt"
+)
+
+var (
+	errInconsistentPodSetAssignments = errors.New("inconsistent podSet assignments")
+)
+
+type Controller struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ reconcile.Reconciler = (*Controller)(nil)
+
+// +kubebuilder:rbac:groups="",resources=events,verbs=create;watch;update
+// +kubebuilder:rbac:groups="",resources=podtemplates,verbs=get;list;watch;create;delete;update
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests,verbs=get;list;watch;create;update;patch;delete
+// +kubebuilder:rbac:groups=autoscaling.x-k8s.io,resources=provisioningrequests/status,verbs=get
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads,verbs=get;list;watch;update;patch;delete
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=workloads/status,verbs=get;update;patch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=admissionchecks,verbs=get;list;watch
+// +kubebuilder:rbac:groups=kueue.x-k8s.io,resources=provisioningrequestconfigs,verbs=get;list;watch
+
+func NewController(client client.Client) *Controller {
+	return &Controller{
+		client: client,
+		cache: &cache{
+			lock:    sync.RWMutex{},
+			checks:  make(map[string]*kueue.AdmissionCheck),
+			configs: make(map[string]*kueue.ProvisioningRequestConfig),
+		},
+	}
+}
+
+// Reconcile performs a full reconciliation for the object referred to by the Request.
+// The Controller will requeue the Request to be processed again if an error is non-nil or
+// Result.Requeue is true, otherwise upon completion it will remove the work from the queue.
+func (c *Controller) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	wl := &kueue.Workload{}
+	log := ctrl.LoggerFrom(ctx)
+	log.V(2).Info("Reconcile workload")
+
+	err := c.client.Get(ctx, req.NamespacedName, wl)
+	if err != nil {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	if !workload.HasQuotaReservation(wl) {
+		//1.2 workload has no reservation
+		log.V(5).Info("workload with no reservation, delete owned requests")
+		return reconcile.Result{}, c.deleteOwnedProvisionRequests(ctx, req.Namespace, req.Name)
+	}
+
+	// get the lists of relevant checks
+	relevantChecks := c.cache.FilterChecks(wl.Status.AdmissionChecks)
+
+	if workload.IsAdmitted(wl) {
+		// check the state of the provision requests, eventually toggle the checks to false
+		// otherwise there is nothing to here
+		log.V(5).Info("workload admitted, sync checks")
+		return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+	}
+
+	if err := c.syncOwnedProvisionRequest(ctx, wl, relevantChecks); err != nil {
+		// this can also delete unneeded checks
+		log.V(2).Error(err, "syncOwnedProvisionRequest failed")
+		return reconcile.Result{}, err
+	}
+
+	return reconcile.Result{}, c.syncCheckStates(ctx, wl, relevantChecks)
+}
+
+func (c *Controller) deleteOwnedProvisionRequests(ctx context.Context, namespace string, name string) error {
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: name}); err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	for i := range list.Items {
+		if err := c.client.Delete(ctx, &list.Items[i]); client.IgnoreNotFound(err) != nil {
+			return fmt.Errorf("delete requests for %s/%s: %w", namespace, name, err)
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncOwnedProvisionRequest(ctx context.Context, wl *kueue.Workload, relevantChecks []string) error {
+	log := ctrl.LoggerFrom(ctx)
+	list := &papi.ProvisioningRequestList{}
+	if err := c.client.List(ctx, list, client.InNamespace(wl.Namespace), client.MatchingFields{RequestsOwnedByWorkloadKey: wl.Name}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	requestsNeeded := slices.ToMap(relevantChecks, func(i int) (string, bool) { return GetProvisioningRequestName(wl.Name, relevantChecks[i]), false })
+	requestCheckMaping := slices.ToMap(relevantChecks, func(i int) (string, string) {
+		return GetProvisioningRequestName(wl.Name, relevantChecks[i]), relevantChecks[i]
+	})
+
+	for i := range list.Items {
+		req := &list.Items[i]
+		if _, needed := requestsNeeded[req.Name]; !needed {
+			if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+				return err
+			}
+		} else {
+			// get the parameters
+			checkName := requestCheckMaping[req.Name]
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil || !requestHasParamaters(req, prc) {
+				// the check is not active or the parameters are out of sync
+				if err := c.client.Delete(ctx, req); client.IgnoreNotFound(err) != nil {
+					log.WithValues("check", checkName).V(5).Error(err, "deleting the request")
+					return err
+				}
+			} else {
+				requestsNeeded[req.Name] = true
+			}
+		}
+	}
+
+	// create the missing ones
+	for requestName, exists := range requestsNeeded {
+		checkName := requestCheckMaping[requestName]
+		if !exists {
+			//get the config
+			prc := c.cache.GetConfigForCheck(checkName)
+			if prc == nil {
+				// the check is not active
+				continue
+			}
+			req := &papi.ProvisioningRequest{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      requestName,
+					Namespace: wl.Namespace,
+				},
+				Spec: papi.ProvisioningRequestSpec{
+					ProvisioningClassName: prc.Spec.ProvisioningClassName,
+					Parameters:            parametersKueueToProvisioning(prc.Spec.Parameters),
+				},
+			}
+			// verify unlikely but..
+			if len(wl.Spec.PodSets) != len(wl.Status.Admission.PodSetAssignments) {
+				return errInconsistentPodSetAssignments
+			}
+			for i := range wl.Spec.PodSets {
+				ps := &wl.Spec.PodSets[i]
+				psAssignment := &wl.Status.Admission.PodSetAssignments[i]
+				// note: besides the difference in count the assignments resources take into account
+				// the priority class overhead and limit default. Could this be useful? How?
+				req.Spec.PodSets = append(req.Spec.PodSets, papi.PodSet{
+					PodTemplateRef: papi.Reference{
+						Name: getProvisioningRequestPodTemplateName(wl.Name, checkName, ps.Name),
+					},
+					Count: ptr.Deref(psAssignment.Count, ps.Count),
+				})
+			}
+
+			if err := ctrl.SetControllerReference(wl, req, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err := c.client.Create(ctx, req); err != nil {
+				return err
+			}
+		}
+		if err := c.syncProvisionRequestsPodTemplates(ctx, wl, checkName); err != nil {
+			return err
+		}
+	}
+	return nil
+}
+
+func (c *Controller) syncProvisionRequestsPodTemplates(ctx context.Context, wl *kueue.Workload, checkNeme string) error {
+	reqest := &papi.ProvisioningRequest{}
+	requestKey := types.NamespacedName{
+		Name:      GetProvisioningRequestName(wl.Name, checkNeme),
+		Namespace: wl.Namespace,
+	}
+	err := c.client.Get(ctx, requestKey, reqest)
+	if err != nil {
+		return client.IgnoreNotFound(err)
+	}
+
+	// the order of the podSets should be the same in the workload and prov. req.
+	// if the number is different, just delete the request
+	if len(wl.Spec.PodSets) != len(reqest.Spec.PodSets) || len(wl.Status.Admission.PodSetAssignments) != len(reqest.Spec.PodSets) {
+		return c.client.Delete(ctx, reqest)
+	}
+
+	psa := wl.Status.Admission.PodSetAssignments
+	flvAssignmentMap := slices.ToMap(psa, func(i int) (string, *kueue.PodSetAssignment) { return psa[i].Name, &psa[i] })
+
+	for i := range wl.Spec.PodSets {
+		reqPS := &reqest.Spec.PodSets[i]
+		pt := &corev1.PodTemplate{}
+		ptKey := types.NamespacedName{
+			Namespace: reqest.Namespace,
+			Name:      reqPS.PodTemplateRef.Name,
+		}
+
+		err := c.client.Get(ctx, ptKey, pt)
+
+		if client.IgnoreNotFound(err) != nil {
+			return err
+		}
+
+		if err != nil {
+			// it's a not found, so create it
+			newPt := &corev1.PodTemplate{
+				ObjectMeta: metav1.ObjectMeta{
+					Name:      ptKey.Name,
+					Namespace: ptKey.Namespace,
+				},
+				Template: wl.Spec.PodSets[i].Template,
+			}
+
+			// apply the admission node selectors to the Template
+			selectors, err := c.getFlavorSelectors(ctx, flvAssignmentMap[wl.Spec.PodSets[i].Name])
+			if err != nil {
+				return err
+			}
+			newPt.Template.Spec.NodeSelector = utilmaps.MergeKeepFirst(newPt.Template.Spec.NodeSelector, selectors)
+
+			if err := ctrl.SetControllerReference(reqest, newPt, c.client.Scheme()); err != nil {
+				return err
+			}
+
+			if err = c.client.Create(ctx, newPt); err != nil {
+				return err
+			}
+		}
+		// maybe check the consistency deeper
+	}
+	return nil
+}
+
+func (c *Controller) getFlavorSelectors(ctx context.Context, assignment *kueue.PodSetAssignment) (map[string]string, error) {
+	processedFlvs := sets.NewString()
+	nodeSelector := make(map[string]string)
+	for _, flvRef := range assignment.Flavors {
+		flvName := string(flvRef)
+		if processedFlvs.Has(flvName) {
+			continue
+		}
+		flv := kueue.ResourceFlavor{}
+		if err := c.client.Get(ctx, types.NamespacedName{Name: string(flvName)}, &flv); err != nil {
+			return nil, err
+		}
+		for k, v := range flv.Spec.NodeLabels {
+			nodeSelector[k] = v
+		}
+		processedFlvs.Insert(flvName)
+	}
+	return nodeSelector, nil
+}
+
+func parametersKueueToProvisioning(in map[string]kueue.Parameter) map[string]papi.Parameter {
+	if in == nil {
+		return nil
+	}
+
+	out := make(map[string]papi.Parameter, len(in))
+	for k, v := range in {
+		out[k] = papi.Parameter(v)
+	}
+	return out
+}
+
+func requestHasParamaters(req *papi.ProvisioningRequest, prc *kueue.ProvisioningRequestConfig) bool {
+	if req.Spec.ProvisioningClassName != prc.Spec.ProvisioningClassName {
+		return false
+	}
+	if len(req.Spec.Parameters) != len(prc.Spec.Parameters) {
+		return false
+	}
+	for k, vReq := range req.Spec.Parameters {
+		if vCfg, found := prc.Spec.Parameters[k]; !found || vReq != papi.Parameter(vCfg) {
+			return false
+		}
+	}
+	return true
+}
+
+func (c *Controller) syncCheckStates(ctx context.Context, wl *kueue.Workload, checks []string) error {
+	checksMap := slices.ToRefMap(wl.Status.AdmissionChecks, func(c *kueue.AdmissionCheckState) string { return c.Name })
+	wlPatch := workload.BaseSSAWorkload(wl)
+	updated := false
+	for _, check := range checks {
+		checkState := *checksMap[check]
+		if c.cache.GetConfigForCheck(check) == nil {
+			// the check is not active
+			if checkState.State != kueue.CheckStatePending || checkState.Message != CheckInactiveMessage {
+				updated = true
+				checkState.State = kueue.CheckStatePending
+				checkState.Message = CheckInactiveMessage
+			}
+		} else {
+			pr := &papi.ProvisioningRequest{}
+			if err := c.client.Get(ctx, types.NamespacedName{Namespace: wl.Namespace, Name: GetProvisioningRequestName(wl.Name, check)}, pr); err != nil {
+				return client.IgnoreNotFound(err)
+			}
+
+			prFailed := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Failed)
+			prAccepted := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.Provisioned)
+			prAvaiable := apimeta.IsStatusConditionTrue(pr.Status.Conditions, papi.CapacityAvailable)
+
+			switch {
+			case prFailed:
+				if checkState.State != kueue.CheckStateRejected {
+					updated = true
+					checkState.State = kueue.CheckStateRejected
+				}
+			case prAccepted || prAvaiable:
+				if checkState.State != kueue.CheckStateReady {
+					updated = true
+					checkState.State = kueue.CheckStateReady
+					// add the pod podSetUpdates
+					checkState.PodSetUpdates = podSetUpdates(wl, pr.Name)
+				}
+			default:
+				if checkState.State != kueue.CheckStatePending {
+					updated = true
+					checkState.State = kueue.CheckStatePending
+				}
+			}
+		}
+		workload.SetAdmissionCheckState(&wlPatch.Status.AdmissionChecks, checkState)
+	}
+	if updated {
+		return c.client.Status().Patch(ctx, wlPatch, client.Apply, client.FieldOwner(ControllerName), client.ForceOwnership)
+	}
+	return nil
+}
+
+func podSetUpdates(wl *kueue.Workload, reqName string) []kueue.PodSetUpdate {
+	return slices.Map(wl.Spec.PodSets, func(ps *kueue.PodSet) kueue.PodSetUpdate {
+		return kueue.PodSetUpdate{
+			Name:        ps.Name,
+			Annotations: map[string]string{ConsumesAnnotationKey: reqName},
+		}
+	})
+}
+
+type acHandler struct {
+	client client.Client
+	cache  *cache
+}
+
+var _ handler.EventHandler = (*acHandler)(nil)
+
+func (a *acHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on create event")
+		}
+	}
+}
+
+func (a *acHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldAc, isOldAc := event.ObjectOld.(*kueue.AdmissionCheck)
+	newAc, isNewAc := event.ObjectNew.(*kueue.AdmissionCheck)
+	if !isNewAc || !isOldAc {
+		return
+	}
+	if newAc.Spec.ControllerName == ControllerName {
+		a.cache.AddOrUpdateCheck(newAc)
+	} else {
+		if oldAc.Spec.ControllerName == ControllerName {
+			a.cache.RmCheck(oldAc)
+		}
+	}
+
+	if oldAc.Spec.ControllerName == ControllerName || newAc.Spec.ControllerName == ControllerName {
+		err := a.reconcileWorkloadsUsing(ctx, oldAc.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", oldAc.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (a *acHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	ac, isAc := event.Object.(*kueue.AdmissionCheck)
+	if !isAc {
+		return
+	}
+
+	if ac.Spec.ControllerName == ControllerName {
+		a.cache.RmCheck(ac)
+		err := a.reconcileWorkloadsUsing(ctx, ac.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "admissionCheck", ac.Name).V(5).Error(err, "on delete event")
+		}
+	}
+}
+
+func (a *acHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (a *acHandler) reconcileWorkloadsUsing(ctx context.Context, check string, q workqueue.RateLimitingInterface) error {
+	list := &kueue.WorkloadList{}
+	if err := a.client.List(ctx, list, client.MatchingFields{WorkloadsControlledByCkeckKey: check}); client.IgnoreNotFound(err) != nil {
+		return err
+	}
+
+	for i := range list.Items {
+		wl := &list.Items[i]
+		req := reconcile.Request{
+			NamespacedName: types.NamespacedName{
+				Name:      wl.Name,
+				Namespace: wl.Namespace,
+			},
+		}
+		q.Add(req)
+	}
+
+	return nil
+}
+
+type prcHandler struct {
+	ach *acHandler
+}
+
+var _ handler.EventHandler = (*prcHandler)(nil)
+
+func (p *prcHandler) Create(ctx context.Context, event event.CreateEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.AddOrUpdateConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on create event")
+	}
+}
+
+func (p *prcHandler) Update(ctx context.Context, event event.UpdateEvent, q workqueue.RateLimitingInterface) {
+	oldPRC, isOldPRC := event.ObjectOld.(*kueue.ProvisioningRequestConfig)
+	newPRC, isNewPRC := event.ObjectNew.(*kueue.ProvisioningRequestConfig)
+	if !isNewPRC || !isOldPRC {
+		return
+	}
+
+	if oldPRC.Spec.ProvisioningClassName != newPRC.Spec.ProvisioningClassName || !maps.Equal(oldPRC.Spec.Parameters, newPRC.Spec.Parameters) {
+		p.ach.cache.AddOrUpdateConfig(newPRC)
+		err := p.reconcileWorkloadsUsing(ctx, oldPRC.Name, q)
+		if err != nil {
+			ctrl.LoggerFrom(ctx, "provisioningRequestConfig", oldPRC.Name).V(5).Error(err, "on update event")
+		}
+	}
+}
+
+func (p *prcHandler) Delete(ctx context.Context, event event.DeleteEvent, q workqueue.RateLimitingInterface) {
+	prc, isPRC := event.Object.(*kueue.ProvisioningRequestConfig)
+	if !isPRC {
+		return
+	}
+	p.ach.cache.RmConfig(prc)
+	err := p.reconcileWorkloadsUsing(ctx, prc.Name, q)
+	if err != nil {
+		ctrl.LoggerFrom(ctx, "provisioningRequestConfig", prc.Name).V(5).Error(err, "on delete event")
+	}
+}
+
+func (p *prcHandler) Generic(_ context.Context, _ event.GenericEvent, _ workqueue.RateLimitingInterface) {
+	// nothing to do for now
+}
+
+func (p *prcHandler) reconcileWorkloadsUsing(ctx context.Context, config string, q workqueue.RateLimitingInterface) error {
+	users := p.ach.cache.GetConfigUsers(config)
+	for _, user := range users {
+		if err := p.ach.reconcileWorkloadsUsing(ctx, user, q); err != nil {
+			return err
+		}
+	}
+
+	return nil
+}
+
+func (c *Controller) SetupWithManager(mgr ctrl.Manager) error {
+	ach := &acHandler{
+		client: c.client,
+		cache:  c.cache,
+	}
+	prch := &prcHandler{ach: ach}
+	return ctrl.NewControllerManagedBy(mgr).
+		For(&kueue.Workload{}).
+		Owns(&papi.ProvisioningRequest{}).
+		Watches(&kueue.AdmissionCheck{}, ach).
+		Watches(&kueue.ProvisioningRequestConfig{}, prch).
+		Complete(c)
+}
+
+func AddToScheme(s *runtime.Scheme) error {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-18T12:23:16Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-18T12:23:16Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  > Well, one could argue that /pkg/cache could be removed :) ... however , cache removed.
  
  Yes, if it is possible, it should remove `/pkg/cache`. However, completely removing the `/pkg/cache` is hard because, actually, many components (controllers, scheduler...) propagate cached information using `/pkg/cache` with each other.
  
  However, I think that we should make more simply and smaller the `/pkg/cache` as much as possible.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-18T14:46:37Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:46:37Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  The existing `pkg/cache` serves the purpose of caching resource calculations. Yes, it can be removed, but with significant penalty to the runtime.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-18T15:01:43Z

- **pkg/controller/admissionchecks/provisioning/cache.go:25** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-18T15:01:42Z
```diff
@@ -0,0 +1,106 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"sync"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type cache struct {
```

  Yes, I also think `pkg/cache` has advantages more than disadvantages. I just wanted to say that I want to avoid the no-need cache, such as the below:
  
  https://github.com/kubernetes-sigs/kueue/blob/525098b838eb28ee241cf1aaee5e53f161d30a0c/pkg/cache/cache.go#L246

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-18T15:12:03Z

Just some variable name changes for better readability.
Otherwise LGTM.

- **cmd/kueue/main.go:196** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:49:03Z
```diff
@@ -185,6 +190,16 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 		return err
 	}
 
+	// setup provision admission check controller indexes
+	if features.Enabled(features.ProvisioningACC) {
+		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+			setupLog.Info("Provisioning Requests are not supported, skip admission check controller setup")
```

  ```suggestion
  			setupLog.Error(nil, "Provisioning Requests are not supported, skipped admission check controller setup")
  ```

- **pkg/controller/admissionchecks/provisioning/confighelper.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:53:32Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
```

  ```suggestion
  // FilterChecksForProvReq - returns a list of check names controlled by ProvisioningRequest.
  ```

- **pkg/controller/admissionchecks/provisioning/confighelper.go:34** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:54:08Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
```

  It's not just about config, but also AdmissionChecks.... I would just call it `store`.

- **pkg/controller/admissionchecks/provisioning/confighelper.go:63** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:55:44Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
```

  ```suggestion
  func (c *configHelper) GetConfigForAdmissionCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
  ```

- **pkg/controller/admissionchecks/provisioning/confighelper.go:77** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:56:28Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: checkName}, ac); err != nil {
+		return nil, err
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		return nil, ErrBadParametersRef
+	}
+
+	return c.GetConfig(ctx, ac.Spec.Parameters.Name)
+}
+
+// GetConfig - returns the config identified by its name
+func (c *configHelper) GetConfig(ctx context.Context, name string) (*kueue.ProvisioningRequestConfig, error) {
```

  ```suggestion
  func (c *configHelper) ProvReqConfig(ctx context.Context, name string) (*kueue.ProvisioningRequestConfig, error) {
  ```

- **pkg/controller/admissionchecks/provisioning/confighelper.go:87** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:58:08Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: checkName}, ac); err != nil {
+		return nil, err
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		return nil, ErrBadParametersRef
+	}
+
+	return c.GetConfig(ctx, ac.Spec.Parameters.Name)
+}
+
+// GetConfig - returns the config identified by its name
+func (c *configHelper) GetConfig(ctx context.Context, name string) (*kueue.ProvisioningRequestConfig, error) {
+	cfg := &kueue.ProvisioningRequestConfig{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: name}, cfg); err != nil {
+		return nil, err
+	}
+	return cfg, nil
+}
+
+// GetConfigUsers - returns a list containing the names of the checks using
+// the provided config.
+func (c *configHelper) GetConfigUsers(ctx context.Context, name string) ([]string, error) {
```

  ```suggestion
  func (c *configHelper) AdmissionChecksUsingProvReqConfig(ctx context.Context, name string) ([]string, error) {
  ```

- **pkg/controller/admissionchecks/provisioning/indexer.go:35** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T15:01:27Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "metadata.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "status.admissionChecks"
```

  ```suggestion
  	WorkloadsWithAdmissionCheckKey = "status.admissionChecks"
  ```

- **pkg/controller/admissionchecks/provisioning/controller_test.go:64** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T15:07:33Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
```

  yeah, not worth just for one function.

- **pkg/controller/admissionchecks/provisioning/controller_test.go:561** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T15:10:00Z
```diff
@@ -0,0 +1,738 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"testing"
+
+	"github.com/google/go-cmp/cmp"
+	"github.com/google/go-cmp/cmp/cmpopts"
+	corev1 "k8s.io/api/core/v1"
+	apierrors "k8s.io/apimachinery/pkg/api/errors"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	"k8s.io/apimachinery/pkg/api/resource"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+	utiltesting "sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+var (
+	wlCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(kueue.AdmissionCheckState{}, "LastTransitionTime"),
+	}
+
+	reqCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+
+	tmplCmpOptions = []cmp.Option{
+		cmpopts.EquateEmpty(),
+		cmpopts.IgnoreTypes(metav1.ObjectMeta{}, metav1.TypeMeta{}),
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+		cmpopts.IgnoreFields(corev1.PodSpec{}, "RestartPolicy"),
+	}
+
+	acCmpOptions = []cmp.Option{
+		cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime"),
+	}
+)
+
+func requestWithCondition(r *autoscaling.ProvisioningRequest, conditionType string, status metav1.ConditionStatus) *autoscaling.ProvisioningRequest {
+	r = r.DeepCopy()
+	apimeta.SetStatusCondition(&r.Status.Conditions, metav1.Condition{
+		Type:   conditionType,
+		Status: status,
+	})
+	return r
+}
+
+func TestReconcile(t *testing.T) {
+	baseWorkload := utiltesting.MakeWorkload("wl", TestNamespace).
+		PodSets(
+			*utiltesting.MakePodSet("ps1", 4).
+				Request(corev1.ResourceCPU, "1").
+				Obj(),
+			*utiltesting.MakePodSet("ps2", 4).
+				Request(corev1.ResourceMemory, "1M").
+				Obj(),
+		).
+		ReserveQuota(utiltesting.MakeAdmission("q1").PodSets(
+			kueue.PodSetAssignment{
+				Name: "ps1",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv1",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("4"),
+				},
+				Count: ptr.To[int32](4),
+			},
+			kueue.PodSetAssignment{
+				Name: "ps2",
+				Flavors: map[corev1.ResourceName]kueue.ResourceFlavorReference{
+					corev1.ResourceCPU: "flv2",
+				},
+				ResourceUsage: map[corev1.ResourceName]resource.Quantity{
+					corev1.ResourceCPU: resource.MustParse("3M"),
+				},
+				Count: ptr.To[int32](3),
+			},
+		).
+			Obj()).
+		AdmissionChecks(kueue.AdmissionCheckState{
+			Name:  "check1",
+			State: kueue.CheckStatePending,
+		}).
+		Obj()
+
+	baseFalvor1 := utiltesting.MakeResourceFlavor("flv1").Label("f1l1", "v1").Obj()
+	baseFalvor2 := utiltesting.MakeResourceFlavor("flv2").Label("f2l1", "v1").Obj()
+
+	baseRequest := &autoscaling.ProvisioningRequest{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "wl-check1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl",
+				},
+			},
+		},
+		Spec: autoscaling.ProvisioningRequestSpec{
+			PodSets: []autoscaling.PodSet{
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps1",
+					},
+					Count: 4,
+				},
+				{
+					PodTemplateRef: autoscaling.Reference{
+						Name: "ppt-wl-check1-ps2",
+					},
+					Count: 3,
+				},
+			},
+			ProvisioningClassName: "class1",
+			Parameters: map[string]autoscaling.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseTemplate1 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps1",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceCPU: resource.MustParse("1"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f1l1": "v1"},
+			},
+		},
+	}
+
+	baseTemplate2 := &corev1.PodTemplate{
+		ObjectMeta: metav1.ObjectMeta{
+			Namespace: TestNamespace,
+			Name:      "ppt-wl-check1-ps2",
+			OwnerReferences: []metav1.OwnerReference{
+				{
+					Name: "wl-check1",
+				},
+			},
+		},
+		Template: corev1.PodTemplateSpec{
+			Spec: corev1.PodSpec{
+				Containers: []corev1.Container{
+					{
+						Name: "c",
+						Resources: corev1.ResourceRequirements{
+							Requests: corev1.ResourceList{
+								corev1.ResourceMemory: resource.MustParse("1M"),
+							},
+						},
+					},
+				},
+				NodeSelector: map[string]string{"f2l1": "v1"},
+			},
+		},
+	}
+
+	baseConfig := &kueue.ProvisioningRequestConfig{
+		ObjectMeta: metav1.ObjectMeta{
+			Name: "config1",
+		},
+		Spec: kueue.ProvisioningRequestConfigSpec{
+			ProvisioningClassName: "class1",
+			Parameters: map[string]kueue.Parameter{
+				"p1": "v1",
+			},
+		},
+	}
+
+	baseCheck := utiltesting.MakeAdmissionCheck("check1").
+		ControllerName(ControllerName).
+		Parameters(kueue.GroupVersion.Group, ConfigKind, "config1").
+		Obj()
+
+	cases := map[string]struct {
+		requests             []*autoscaling.ProvisioningRequest
+		templates            []*corev1.PodTemplate
+		checks               []*kueue.AdmissionCheck
+		configs              []*kueue.ProvisioningRequestConfig
+		flavors              []*kueue.ResourceFlavor
+		workload             *kueue.Workload
+		wantReconcileError   error
+		wantWorkloads        map[string]*kueue.Workload
+		wantRequests         map[string]*autoscaling.ProvisioningRequest
+		wantTemplates        map[string]*corev1.PodTemplate
+		wantRequestsNotFound []string
+	}{
+		"unrelated workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").Obj(),
+		},
+		"unrelated workload with reservation": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Obj(),
+		},
+		"unrelated admitted workload": {
+			workload: utiltesting.MakeWorkload("wl", "ns").
+				ReserveQuota(utiltesting.MakeAdmission("q1").Obj()).
+				Admitted(true).
+				Obj(),
+		},
+		"missing config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStatePending,
+						Message: CheckInactiveMessage,
+					}).
+					Obj(),
+			},
+		},
+		"with config": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"remove unnecessary requests": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check2",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+				},
+			},
+			wantWorkloads:        map[string]*kueue.Workload{baseWorkload.Name: baseWorkload.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check2"},
+		},
+		"missing template": {
+			workload:  baseWorkload.DeepCopy(),
+			checks:    []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:   []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:   []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:  []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request out of sync": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				{
+					ObjectMeta: metav1.ObjectMeta{
+						Namespace: TestNamespace,
+						Name:      "wl-check1",
+						OwnerReferences: []metav1.OwnerReference{
+							{
+								Name: "wl",
+							},
+						},
+					},
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-main",
+								},
+								Count: 1,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v0",
+						},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				baseRequest.Name: baseRequest.DeepCopy(),
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate1.Name: baseTemplate1.DeepCopy(),
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"request removed on workload finished": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				Condition(metav1.Condition{
+					Type:   kueue.WorkloadFinished,
+					Status: metav1.ConditionTrue,
+				}).
+				Obj(),
+
+			checks:               []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:              []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:              []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests:             []*autoscaling.ProvisioningRequest{baseRequest.DeepCopy()},
+			templates:            []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantRequestsNotFound: []string{"wl-check1"},
+		},
+		"when request fails": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Failed, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateRejected,
+					}).
+					Obj(),
+			},
+		},
+		"when capacity is available": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.CapacityAvailable, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when request is provisioned": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs:  []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			requests: []*autoscaling.ProvisioningRequest{
+				requestWithCondition(baseRequest, autoscaling.Provisioned, metav1.ConditionTrue),
+			},
+			templates: []*corev1.PodTemplate{baseTemplate1.DeepCopy(), baseTemplate2.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name:        "ps1",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+							{
+								Name:        "ps2",
+								Annotations: map[string]string{"cluster-autoscaler.kubernetes.io/consume-provisioning-request": "wl-check1"},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when no request is needed": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{"example.org/gpu"},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+					AdmissionChecks(kueue.AdmissionCheckState{
+						Name:    "check1",
+						State:   kueue.CheckStateReady,
+						Message: NoRequestNeeded,
+					}).
+					Obj(),
+			},
+		},
+		"when request is needed for one PodSet": {
+			workload: baseWorkload.DeepCopy(),
+			checks:   []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors:  []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{
+				{ObjectMeta: metav1.ObjectMeta{
+					Name: "config1",
+				},
+					Spec: kueue.ProvisioningRequestConfigSpec{
+						ProvisioningClassName: "class1",
+						Parameters: map[string]kueue.Parameter{
+							"p1": "v1",
+						},
+						ManagedResources: []corev1.ResourceName{corev1.ResourceMemory},
+					},
+				},
+			},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+			wantRequests: map[string]*autoscaling.ProvisioningRequest{
+				"wl-check1": {
+					Spec: autoscaling.ProvisioningRequestSpec{
+						PodSets: []autoscaling.PodSet{
+							{
+								PodTemplateRef: autoscaling.Reference{
+									Name: "ppt-wl-check1-ps2",
+								},
+								Count: 3,
+							},
+						},
+						ProvisioningClassName: "class1",
+						Parameters: map[string]autoscaling.Parameter{
+							"p1": "v1",
+						},
+					},
+				},
+			},
+			wantTemplates: map[string]*corev1.PodTemplate{
+				baseTemplate2.Name: baseTemplate2.DeepCopy(),
+			},
+		},
+		"when the request is removed while the check is ready": {
+			workload: (&utiltesting.WorkloadWrapper{Workload: *baseWorkload.DeepCopy()}).
+				AdmissionChecks(kueue.AdmissionCheckState{
+					Name:  "check1",
+					State: kueue.CheckStateReady,
+				}).
+				Obj(),
+
+			checks:  []*kueue.AdmissionCheck{baseCheck.DeepCopy()},
+			flavors: []*kueue.ResourceFlavor{baseFalvor1.DeepCopy(), baseFalvor2.DeepCopy()},
+			configs: []*kueue.ProvisioningRequestConfig{baseConfig.DeepCopy()},
+			wantWorkloads: map[string]*kueue.Workload{
+				baseWorkload.Name: baseWorkload.DeepCopy(),
+			},
+		},
+	}
+
+	for name, tc := range cases {
+		t.Run(name, func(t *testing.T) {
+			builder, ctx := getClientBuilder()
+
+			builder = builder.WithObjects(tc.workload)
+			builder = builder.WithStatusSubresource(tc.workload)
+
+			builder = builder.WithObjects(slices.Map(tc.requests, func(o **autoscaling.ProvisioningRequest) client.Object { return *o })...)
```

  You can just change from `[]*autoscaling.ProvisioningRequest` to `[]autoscaling.ProvisioningRequest` and do:
  
  ```
  builder.WithLists(
    autoscaling.ProvisioningRequestList{Items: tc.requests},
    ....{Items: tc.configs},
  )
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T03:57:14Z

- **cmd/kueue/main.go:196** by [@trasc](https://github.com/trasc) — 2023-10-19T03:57:14Z
```diff
@@ -185,6 +190,16 @@ func setupIndexes(ctx context.Context, mgr ctrl.Manager, cfg *configapi.Configur
 		return err
 	}
 
+	// setup provision admission check controller indexes
+	if features.Enabled(features.ProvisioningACC) {
+		if !provisioning.ServerSupportsProvisioningRequest(mgr) {
+			setupLog.Info("Provisioning Requests are not supported, skip admission check controller setup")
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T03:59:40Z

- **pkg/controller/admissionchecks/provisioning/confighelper.go:46** by [@trasc](https://github.com/trasc) — 2023-10-19T03:59:40Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T04:02:24Z

- **pkg/controller/admissionchecks/provisioning/confighelper.go:63** by [@trasc](https://github.com/trasc) — 2023-10-19T04:02:23Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T04:03:08Z

- **pkg/controller/admissionchecks/provisioning/confighelper.go:77** by [@trasc](https://github.com/trasc) — 2023-10-19T04:03:08Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: checkName}, ac); err != nil {
+		return nil, err
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		return nil, ErrBadParametersRef
+	}
+
+	return c.GetConfig(ctx, ac.Spec.Parameters.Name)
+}
+
+// GetConfig - returns the config identified by its name
+func (c *configHelper) GetConfig(ctx context.Context, name string) (*kueue.ProvisioningRequestConfig, error) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T04:04:34Z

- **pkg/controller/admissionchecks/provisioning/confighelper.go:87** by [@trasc](https://github.com/trasc) — 2023-10-19T04:04:34Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
+	client client.Client
+}
+
+func parametersRefValid(params *kueue.AdmissionCheckParametersReference) bool {
+	if params == nil {
+		return false
+	}
+	return params.Kind == ConfigKind && params.APIGroup == kueue.GroupVersion.Group && params.Name != ""
+}
+
+// FilterChecks - returns a list of check names controlling one of the provided states
+// and present in the cache.
+func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.AdmissionCheckState) ([]string, error) {
+	var retActive []string
+	for _, state := range states {
+		ac := &kueue.AdmissionCheck{}
+
+		if err := c.client.Get(ctx, types.NamespacedName{Name: state.Name}, ac); client.IgnoreNotFound(err) != nil {
+			return nil, err
+		} else if err == nil && ac.Spec.ControllerName == ControllerName {
+			retActive = append(retActive, ac.Name)
+		}
+	}
+	return retActive, nil
+}
+
+// GetConfigForCheck - get the config used by the check identified by the checks name,
+// nil is return if the config is missing or improperly configured (the check is not active).
+func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: checkName}, ac); err != nil {
+		return nil, err
+	}
+
+	if !parametersRefValid(ac.Spec.Parameters) {
+		return nil, ErrBadParametersRef
+	}
+
+	return c.GetConfig(ctx, ac.Spec.Parameters.Name)
+}
+
+// GetConfig - returns the config identified by its name
+func (c *configHelper) GetConfig(ctx context.Context, name string) (*kueue.ProvisioningRequestConfig, error) {
+	cfg := &kueue.ProvisioningRequestConfig{}
+	if err := c.client.Get(ctx, types.NamespacedName{Name: name}, cfg); err != nil {
+		return nil, err
+	}
+	return cfg, nil
+}
+
+// GetConfigUsers - returns a list containing the names of the checks using
+// the provided config.
+func (c *configHelper) GetConfigUsers(ctx context.Context, name string) ([]string, error) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T04:06:14Z

- **pkg/controller/admissionchecks/provisioning/indexer.go:35** by [@trasc](https://github.com/trasc) — 2023-10-19T04:06:13Z
```diff
@@ -0,0 +1,86 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"fmt"
+
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	autoscaling "k8s.io/autoscaler/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/client/apiutil"
+	"sigs.k8s.io/controller-runtime/pkg/manager"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+const (
+	RequestsOwnedByWorkloadKey    = "metadata.ownedByWorkload"
+	WorkloadsControlledByCkeckKey = "status.admissionChecks"
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T05:39:54Z

- **pkg/controller/admissionchecks/provisioning/confighelper.go:34** by [@trasc](https://github.com/trasc) — 2023-10-19T05:39:54Z
```diff
@@ -0,0 +1,93 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+	"errors"
+
+	"k8s.io/apimachinery/pkg/types"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/slices"
+)
+
+var (
+	ErrBadParametersRef = errors.New("bad parameters reference")
+)
+
+type configHelper struct {
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T14:50:10Z

- **pkg/controller/admissionchecks/provisioning/constants.go:21** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T14:50:10Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
```

  Let's namespace this: `kueue.x-k8s.io/provisioning-request`

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T14:59:21Z

- **pkg/controller/admissionchecks/provisioning/storehelper.go:62** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T14:55:47Z
```diff
@@ -58,9 +57,9 @@ func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.Admissio
 	return retActive, nil
 }
 
-// GetConfigForCheck - get the config used by the check identified by the checks name,
+// GetConfigForAdmissionCheck - get the config used by the check identified by the checks name,
 // nil is return if the config is missing or improperly configured (the check is not active).
-func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+func (c *storeHelper) GetConfigForAdmissionCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
```

  ```suggestion
  func (c *storeHelper) ProvReqConfigForAdmissionCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
  ```
  
  I should have said this before, sorry.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-19T17:32:37Z

Sorry. I forgot to submit comments.

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:49** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-19T06:15:55Z
```diff
@@ -0,0 +1,67 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type acReconciler struct {
+	client       client.Client
+	configHelper *configHelper
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
```

  if "parameters" is valid, this message is an empty, right?
  Should we add some message?

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-19T17:11:07Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  @alculquicondor @trasc IIUC, k-sigs project needs to use `x-k8s.io` domain instead of `kubernetes.io`, right?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T18:42:18Z

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T18:42:18Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  Not necessarily. Officially reviewed APIs, even if not part of core k8s, can use `kubernetes.io`. The GatewayAPI is one of them.
  
  But I guess this is the current value.
  
  @kisieland can you confirm if there any plans to change this?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-19T18:45:23Z

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-19T18:45:23Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  `ProvisioningRequest` API seems to use `x-k8s.io`.
  
  https://github.com/kubernetes/autoscaler/blob/8c35e0aa4bdbab9caf03f8b8926d94f321c6e9a4/cluster-autoscaler/provisioningrequest/apis/autoscaling.x-k8s.io/v1beta1/register.go#L27-L28

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T19:33:28Z

- **pkg/controller/admissionchecks/provisioning/constants.go:21** by [@trasc](https://github.com/trasc) — 2023-10-19T19:33:28Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
```

  why?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T19:50:47Z

- **pkg/controller/admissionchecks/provisioning/constants.go:21** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T19:50:47Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
```

  it's generally a good practice, in case other extensions have another form of ProvisioningRequest

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T05:55:26Z

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@trasc](https://github.com/trasc) — 2023-10-20T05:55:26Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  This is part of the kep and used by the autoscaler.
  https://github.com/kubernetes-sigs/kueue/blob/91d5f645a326dae1ea8542879adcc62ba4db7f5d/keps/1136-provisioning-request-support/README.md?plain=1#L95-L99
  
  We can make it configurable in the future, if needed.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T05:55:40Z

- **pkg/controller/admissionchecks/provisioning/constants.go:21** by [@trasc](https://github.com/trasc) — 2023-10-20T05:55:40Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
```

  This name is part of the KEP
  https://github.com/kubernetes-sigs/kueue/blob/91d5f645a326dae1ea8542879adcc62ba4db7f5d/keps/1136-provisioning-request-support/README.md?plain=1#L84-L87

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T06:11:18Z

- **pkg/controller/admissionchecks/provisioning/admissioncheck_reconciler.go:49** by [@trasc](https://github.com/trasc) — 2023-10-20T06:11:18Z
```diff
@@ -0,0 +1,67 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+import (
+	"context"
+
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	"sigs.k8s.io/controller-runtime/pkg/reconcile"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+)
+
+type acReconciler struct {
+	client       client.Client
+	configHelper *configHelper
+}
+
+var _ reconcile.Reconciler = (*acReconciler)(nil)
+
+func (a *acReconciler) Reconcile(ctx context.Context, req reconcile.Request) (reconcile.Result, error) {
+	ac := &kueue.AdmissionCheck{}
+	if err := a.client.Get(ctx, req.NamespacedName, ac); err != nil || ac.Spec.ControllerName != ControllerName {
+		return reconcile.Result{}, client.IgnoreNotFound(err)
+	}
+
+	currentCondition := ptr.Deref(apimeta.FindStatusCondition(ac.Status.Conditions, kueue.AdmissionCheckActive), metav1.Condition{})
+	newCondition := metav1.Condition{
+		Type:    kueue.AdmissionCheckActive,
+		Status:  metav1.ConditionTrue,
+		Reason:  "Active",
+		Message: "",
```

  added "The admission check is active"

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T06:11:36Z

- **pkg/controller/admissionchecks/provisioning/storehelper.go:62** by [@trasc](https://github.com/trasc) — 2023-10-20T06:11:35Z
```diff
@@ -58,9 +57,9 @@ func (c *configHelper) FilterChecks(ctx context.Context, states []kueue.Admissio
 	return retActive, nil
 }
 
-// GetConfigForCheck - get the config used by the check identified by the checks name,
+// GetConfigForAdmissionCheck - get the config used by the check identified by the checks name,
 // nil is return if the config is missing or improperly configured (the check is not active).
-func (c *configHelper) GetConfigForCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
+func (c *storeHelper) GetConfigForAdmissionCheck(ctx context.Context, checkName string) (*kueue.ProvisioningRequestConfig, error) {
```

  done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T07:05:10Z

- **pkg/controller/admissionchecks/provisioning/constants.go:21** by [@trasc](https://github.com/trasc) — 2023-10-20T07:05:09Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
```

  done

### Review by [@kisieland](https://github.com/kisieland) (commented) — 2023-10-20T07:18:48Z

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@kisieland](https://github.com/kisieland) — 2023-10-20T07:18:47Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  There are no plans to change it.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T07:39:45Z

- **pkg/controller/admissionchecks/provisioning/constants.go:22** by [@trasc](https://github.com/trasc) — 2023-10-20T07:39:45Z
```diff
@@ -0,0 +1,26 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package provisioning
+
+const (
+	ConfigKind            = "ProvisioningRequestConfig"
+	ControllerName        = "ProvisioningRequestController"
+	ConsumesAnnotationKey = "cluster-autoscaler.kubernetes.io/consume-provisioning-request"
```

  thanks @kisieland

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T13:59:56Z

/approve
/hold

@mwielgus can you take a final pass?
