# PR #11557: [release-0.17] Finish old workload slice only after new slice is admitted

**Summary**: [release-0.17] Finish old workload slice only after new slice is admitted

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11557

**Last updated**: 2026-05-29T11:46:16Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Branch**: `cherry-pick-11195-11292-0.17` → `release-0.17`
- **Created**: 2026-05-23T03:08:10Z
- **Updated**: 2026-05-29T11:46:16Z
- **Merged**: 2026-05-25T05:09:16Z
- **Closed**: 2026-05-25T05:09:16Z
- **Labels**: `kind/bug`, `release-note`, `lgtm`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mimowo](https://github.com/mimowo), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Requested reviewers**: [@mbobrovskyi](https://github.com/mbobrovskyi), [@tenzen-y](https://github.com/tenzen-y)
- **Changed files**: 4   **+90 / -7**

## Description

This is a manual cherry-pick of https://github.com/kubernetes-sigs/kueue/pull/11195 and https://github.com/kubernetes-sigs/kueue/pull/11292

/assign sohankunkerkar

```release-note
ElasticJobsViaWorkloadSlices: Fix quota leak during elastic workload scale-up where old slice was finished before replacement slice was admitted.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-23T03:08:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 2c159e5f21c4bc2f5c6e5b233443fe498dc97cf3 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a111a1dc8811300087f2555 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-11557--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-11557--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExNTU3LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.88bAAyzXKDXzt7dqb5o_Cbo2aG0n0YdeI2Z944bi54Q)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-11557--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T04:33:40Z

/lgtm
/apporve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-25T04:33:49Z

LGTM label has been added.  <details>Git tree hash: 92eeecb4dc9cc1c1a5996ee35c0da9969a250a4a</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T05:07:53Z

/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-25T05:08:02Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11557#issuecomment-4531677756" title="Approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11557#" title="Author self-approved">sohankunkerkar</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/release-0.17/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T08:45:50Z

/kind bug

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2026-05-29T11:46:13Z

/release-note-edit
```release-note
ElasticJobsViaWorkloadSlices: Fix quota leak during elastic workload scale-up where old slice was finished before replacement slice was admitted.
```

## Reviews

### Review by [@copilot-pull-request-reviewer[bot]](https://github.com/apps/copilot-pull-request-reviewer) (commented) — 2026-05-23T03:10:02Z

## Pull request overview

> [!NOTE]
> Copilot was unable to run its full agentic suite in this review.

This PR ensures correct ordering during elastic Job workload-slice replacement: the old workload-slice should not be marked Finished before the new slice becomes Admitted.

**Changes:**
- Move workload-slice replacement/finishing to occur after the replacement slice is admitted (scheduler).
- Add a watch-based test helper to assert ordering of “new admitted” vs “old finished”.
- Add an integration test covering the ordering and adjust scheduler test event order.

### Reviewed changes

Copilot reviewed 4 out of 4 changed files in this pull request and generated 4 comments.

| File | Description |
| ---- | ----------- |
| test/util/util.go | Adds a watch-based assertion helper for workload-slice ordering. |
| test/integration/singlecluster/controller/jobs/job/job_controller_test.go | Adds an integration test that sets up a Workload watch and verifies ordering on scale-up. |
| pkg/scheduler/scheduler.go | Changes scheduler flow to admit the new slice first, then finish the old slice. |
| pkg/scheduler/scheduler_test.go | Updates expected event ordering to match new scheduler behavior. |






---

💡 <a href="/kubernetes-sigs/kueue/new/release-0.17?filename=.github/instructions/*.instructions.md" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Add Copilot custom instructions</a> for smarter, more guided reviews. <a href="https://docs.github.com/en/copilot/customizing-copilot/adding-repository-custom-instructions-for-github-copilot" class="Link--inTextBlock" target="_blank" rel="noopener noreferrer">Learn how to get started</a>.

- **test/util/util.go:1384** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-23T03:10:01Z
```diff
@@ -1364,6 +1365,43 @@ func ExpectNewWorkloadSlice(ctx context.Context, k8sClient client.Client, oldWor
 	return newWorkload
 }
 
+// ExpectWorkloadSliceAdmittedBeforeOldFinished watches workload events and asserts
+// that the old workload slice is not marked Finished before the new slice is Admitted.
+// The watcher must be started before the scale-up that triggers the replacement.
+func ExpectWorkloadSliceAdmittedBeforeOldFinished(watcher watch.Interface, oldWorkloadName string, timeout time.Duration) {
+	ginkgo.GinkgoHelper()
+	oldSliceFinished := false
+	newSliceAdmitted := false
+	timeoutCh := time.After(timeout)
+	for !newSliceAdmitted {
+		select {
+		case evt, ok := <-watcher.ResultChan():
+			gomega.Expect(ok).Should(gomega.BeTrue(), "watch channel closed unexpectedly")
+			if evt.Type == watch.Error {
+				status, _ := evt.Object.(*metav1.Status)
+				gomega.Expect(evt.Type).ShouldNot(gomega.Equal(watch.Error), fmt.Sprintf("watch error: %v", status))
+			}
+			if evt.Type != watch.Modified {
```

  This helper ignores `watch.Added` events. If the replacement Workload is created already in an Admitted state (or the watch delivers an initial Added with the admitted status), this loop can miss it and time out. Consider handling both `watch.Added` and `watch.Modified` (or checking admission/finished conditions for any event type that includes an object), while still ignoring non-object events like `watch.Bookmark`.

- **test/util/util.go:1397** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-23T03:10:02Z
```diff
@@ -1364,6 +1365,43 @@ func ExpectNewWorkloadSlice(ctx context.Context, k8sClient client.Client, oldWor
 	return newWorkload
 }
 
+// ExpectWorkloadSliceAdmittedBeforeOldFinished watches workload events and asserts
+// that the old workload slice is not marked Finished before the new slice is Admitted.
+// The watcher must be started before the scale-up that triggers the replacement.
+func ExpectWorkloadSliceAdmittedBeforeOldFinished(watcher watch.Interface, oldWorkloadName string, timeout time.Duration) {
+	ginkgo.GinkgoHelper()
+	oldSliceFinished := false
+	newSliceAdmitted := false
+	timeoutCh := time.After(timeout)
+	for !newSliceAdmitted {
+		select {
+		case evt, ok := <-watcher.ResultChan():
+			gomega.Expect(ok).Should(gomega.BeTrue(), "watch channel closed unexpectedly")
+			if evt.Type == watch.Error {
+				status, _ := evt.Object.(*metav1.Status)
+				gomega.Expect(evt.Type).ShouldNot(gomega.Equal(watch.Error), fmt.Sprintf("watch error: %v", status))
+			}
+			if evt.Type != watch.Modified {
+				continue
+			}
+			wl, isWorkload := evt.Object.(*kueue.Workload)
+			gomega.Expect(isWorkload).Should(gomega.BeTrue())
+
+			if wl.Name == oldWorkloadName && workload.IsFinished(wl) {
+				oldSliceFinished = true
+			}
+			if wl.Name != oldWorkloadName && workload.IsAdmitted(wl) {
+				gomega.Expect(oldSliceFinished).Should(gomega.BeFalse(),
+					"old workload slice was finished before new slice was admitted")
+				newSliceAdmitted = true
+			}
```

  Identifying the 'new slice' solely as `wl.Name != oldWorkloadName` makes this assertion vulnerable to unrelated Workloads in the same namespace (including background/system tests) becoming Admitted and prematurely satisfying the condition. To make the test deterministic, filter the watch using label/field selectors that uniquely identify the job/slice group, or pass the expected new workload name/UID (or a predicate) into the helper and match on that instead.

- **test/util/util.go:1382** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-23T03:10:02Z
```diff
@@ -1364,6 +1365,43 @@ func ExpectNewWorkloadSlice(ctx context.Context, k8sClient client.Client, oldWor
 	return newWorkload
 }
 
+// ExpectWorkloadSliceAdmittedBeforeOldFinished watches workload events and asserts
+// that the old workload slice is not marked Finished before the new slice is Admitted.
+// The watcher must be started before the scale-up that triggers the replacement.
+func ExpectWorkloadSliceAdmittedBeforeOldFinished(watcher watch.Interface, oldWorkloadName string, timeout time.Duration) {
+	ginkgo.GinkgoHelper()
+	oldSliceFinished := false
+	newSliceAdmitted := false
+	timeoutCh := time.After(timeout)
+	for !newSliceAdmitted {
+		select {
+		case evt, ok := <-watcher.ResultChan():
+			gomega.Expect(ok).Should(gomega.BeTrue(), "watch channel closed unexpectedly")
+			if evt.Type == watch.Error {
+				status, _ := evt.Object.(*metav1.Status)
+				gomega.Expect(evt.Type).ShouldNot(gomega.Equal(watch.Error), fmt.Sprintf("watch error: %v", status))
```

  This error handling is effectively a redundant assertion (it asserts `evt.Type != watch.Error` inside the `evt.Type == watch.Error` branch) and also silently ignores a failed cast to `*metav1.Status`. Prefer immediately failing the test when receiving `watch.Error` (e.g., via `gomega.Fail(...)` or an unconditional failing expectation) and include a robust message that handles `status == nil`.

- **pkg/scheduler/scheduler.go:725** by [@Copilot](https://github.com/apps/copilot-pull-request-reviewer) — 2026-05-23T03:10:02Z
```diff
@@ -721,6 +718,11 @@ func (s *Scheduler) admit(ctx context.Context, e *entry, cq *schdcache.ClusterQu
 			s.recordWorkloadAdmissionMetrics(log, newWorkload, e.Obj, admission, consideredStr)
 
 			log.V(2).Info("Workload successfully admitted and assigned flavors", "assignments", admission.PodSetAssignments)
+			if features.Enabled(features.ElasticJobsViaWorkloadSlices) && oldWorkloadSlice != nil {
+				if err := s.replaceWorkloadSlice(ctx, oldWorkloadSlice.WorkloadInfo.ClusterQueue, e.Obj, oldWorkloadSlice.WorkloadInfo.Obj.DeepCopy()); err != nil {
+					log.Error(err, "Failed to finish old workload slice after admitting replacement; job reconciler will handle recovery")
+				}
+			}
```

  Swallowing `replaceWorkloadSlice` failures after the replacement is admitted can leave the old slice not Finished (and potentially still holding quota/state) while the new slice is already Admitted. That can cause temporary (or prolonged) quota accounting issues and scheduling starvation until reconciliation corrects it. Consider propagating this error back to the caller (so admission is retried/treated as failed) or implementing a more deterministic recovery path (e.g., enqueue immediate follow-up work to finish the old slice and/or emit an event/condition that drives prompt reconciliation).
