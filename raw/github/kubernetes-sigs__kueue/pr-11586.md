# PR #11586: test(e2e): retry cluster creation on port collision (#11278)

**Summary**: test(e2e): retry cluster creation on port collision (#11278)

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11586

**Last updated**: 2026-06-02T04:45:29Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Mostafahassen1](https://github.com/Mostafahassen1)
- **Branch**: `retry-kind-create` → `main`
- **Created**: 2026-05-25T07:17:41Z
- **Updated**: 2026-06-02T04:45:29Z
- **Merged**: 2026-05-27T14:08:52Z
- **Closed**: 2026-05-27T14:08:52Z
- **Labels**: `kind/bug`, `lgtm`, `kind/flake`, `release-note-none`, `approved`, `size/M`, `cncf-cla: yes`
- **Assignees**: [@mbobrovskyi](https://github.com/mbobrovskyi)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@sohankunkerkar](https://github.com/sohankunkerkar), [@ivnovakov](https://github.com/ivnovakov), [@kshalot](https://github.com/kshalot), [@reruno](https://github.com/reruno)
- **Changed files**: 2   **+51 / -13**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind flake
/kind bug

Fixes #11278

#### What this PR does / why we need it:

### The Error

* Multiple concurrent CI jobs randomly pull the same ephemeral port from the host OS network.
* KinD cluster creation crashes with `port is already allocated`, causing a 100% infrastructure test failure.

### The Solution

* Wrap the KinD cluster creation step inside a 3-attempt retry loop with a 3-second sleep.
* After a failure, KinD deletes the broken setup and requests a brand new random port.
* Use a `grep` guard to only retry specifically when the "port is already allocated" string appears.
* Avoid retrying real bugs like configuration mistakes, missing dependencies, or bad test code.
* Keep ports completely dynamic so multiple separate Pull Request test jobs can still run in parallel.
* Fix this directly inside `hack/testing/e2e-common.sh` to permanently stabilize all E2E test suites at once.

```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-25T07:17:47Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 85416d6bf53a2ef33e237456c1e102b90b3dc485 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a155095dd67d600083e4825 |

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-25T07:51:01Z

/retest

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-25T08:24:54Z

Hi @mimowo 
>qq could we use the hack/retry.sh script for this purpose?

Thanks for the suggestion! However, the current `max_attempts` loop in `cluster_create` isn't a simple retry. It specifically checks if the failure is due to a port collision (`grep "port is already allocated"`), runs a cleanup step (`kind delete cluster`) before the next attempt, and aborts early if the error is something else.

The `hack/retry.sh` script executes a single command and doesn't natively support running intermediate cleanup steps or breaking early on specific errors.


Would you prefer to use the script in hack/retry.sh?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T08:50:13Z

I see thanks for the summary, we need the stuff like fail fast etc , and iiuc the script is not supporting that. Is there a chance to generalize the script so that we support all your improvements?

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T09:10:22Z

I injected the empty release note which was part of the PR template, please keep it next time becaue our robot requires it

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-25T09:37:24Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T10:54:36Z

cc @reruno ptal

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-25T11:03:45Z

cc @ivnovakov

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-26T08:01:00Z

Hi @mbobrovskyi 

>Can we test this somehow to make sure the fix works correctly?

I searched throughout the project and couldn't find any existing automated test for this. Is there a specific way or a standard pattern in the project that we can use to test it?

### Comment by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-26T08:15:37Z

Thanks for the changes, lgtm.

Only the testing question is open. 
Here's a comment from another PR showing how I verified things manually:
https://github.com/kubernetes-sigs/kueue/pull/11238#discussion_r3258681310

### Comment by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-26T08:15:44Z

/retest

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-26T09:09:25Z

> I searched throughout the project and couldn't find any existing automated test for this. Is there a specific way or a standard pattern in the project that we can use to test it?

Yeah, I mean locally and provide logs.

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-26T09:09:58Z

Or maybe simulate error to see in Prow.

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-26T09:15:09Z

/retest

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-26T09:39:36Z

>Only the testing question is open.
Here's a comment from another PR showing how I verified things manually:

@mimowo @ivnovakov @mbobrovskyi 

I verified the changes manually in my local terminal by mocking the commands and the log file output. It perfectly handles both scenarios: aborting early (fail-fast) on unrelated errors, and triggering the cleanup/retry loop when the port collision occurs.

Here is the output from my local tests:

Scenario 1: Random Error (Triggers Fail-Fast)
```bash
mostafa@DESKTOP-RLAAD31:~/K8sProjects/kueue$ ./hack/retry.sh \
  --attempts 3 \
  --delay 2 \
  --continue-if "grep -q 'port is already allocated' test.log" \
  --cleanup "echo 'Cleanup executed'" \
  -- bash -c "echo 'Random connection timeout error' > test.log; exit 1"
retry [1/3]: bash -c echo 'Random connection timeout error' > test.log; exit 1
retry [1/3] failed
retry: aborting early as continue-if condition failed (fail-fast)
```


Scenario 2: Port Collision Error (Triggers Cleanup and Retries)


```bash
mostafa@DESKTOP-RLAAD31:~/K8sProjects/kueue$ ./hack/retry.sh \
  --attempts 3 \
  --delay 2 \
  --continue-if "grep -q 'port is already allocated' test.log" \
  --cleanup "echo 'Cleanup executed'" \
  -- bash -c "echo 'port is already allocated' > test.log; exit 1"
retry [1/3]: bash -c echo 'port is already allocated' > test.log; exit 1
retry [1/3] failed
retry: running cleanup command...
Cleanup executed
retry [2/3]: bash -c echo 'port is already allocated' > test.log; exit 1
retry [2/3] failed
retry: running cleanup command...
Cleanup executed
retry [3/3]: bash -c echo 'port is already allocated' > test.log; exit 1
retry [3/3] failed
```

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-26T10:11:07Z

Awesome! Thank you! What about e2e tests?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-26T10:15:03Z

> mostafa@DESKTOP-RLAAD31:~/K8sProjects/kueue$ ./hack/retry.sh \
  --attempts 3 \
  --delay 2 \
  --continue-if "grep -q 'port is already allocated' test.log" \
  --cleanup "echo 'Cleanup executed'" \
  -- bash -c "echo 'port is already allocated' > test.log; exit 1"

Could you please also test the case where the error occurs only on the first attempt?

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-26T11:09:40Z

@mbobrovskyi 

>Could you please also test the case where the error occurs only on the first attempt?


```bash
mostafa@DESKTOP-RLAAD31:~/K8sProjects/kueue$ ./hack/retry.sh \
  --attempts 3 \
  --delay 2 \
  --continue-if "grep -q 'port is already allocated' test.log" \
  --cleanup "echo 'Cleanup executed'" \
  -- bash -c "if [ ! -f test_state.txt ]; then echo 'port is already allocated' > test.log; touch test_state.txt; exit 1; else echo 'Success on attempt 2!'; rm -f test_state.txt; exit 0; fi"

retry [1/3]: bash -c if [ ! -f test_state.txt ]; then echo 'port is already allocated' > test.log; touch test_state.txt; exit 1; else echo 'Success on attempt 2!'; rm -f test_state.txt; exit 0; fi
retry [1/3] failed
retry: running cleanup command...
Cleanup executed
retry [2/3]: bash -c if [ ! -f test_state.txt ]; then echo 'port is already allocated' > test.log; touch test_state.txt; exit 1; else echo 'Success on attempt 2!'; rm -f test_state.txt; exit 0; fi
Success on attempt 2!
```

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-26T11:11:04Z

> Awesome! Thank you! What about e2e tests?

@mbobrovskyi If you are speaking about the CI, it ran successfully after I updated the code.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T12:23:45Z

LGTM label has been added.  <details>Git tree hash: b51024cb3ca67ee62ac41aae5e61a84a5ab6c991</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T12:24:06Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11586#pullrequestreview-4372278756" title="Approved">mbobrovskyi</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11586#" title="Author self-approved">Mostafahassen1</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[hack/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/hack/OWNERS)~~ [mbobrovskyi]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T12:30:55Z

Let's also CP, at least to 0.17

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-27T12:43:53Z

> Let's also CP, at least to 0.17

Is there any action needed from my side, such as building the same thing in the 0.17 version, or can this be done automatically?

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-27T12:46:38Z

Let's give a chance.

/cherry-pick release-0.17
/cherry-pick release-0.16

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T12:46:40Z

@mbobrovskyi: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11586#issuecomment-4554618510):

>Let's give a chance.
>
>/cherry-pick release-0.17
>/cherry-pick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-27T12:47:37Z

@Mostafahassen1 could you please update PR description?

Instead of `Issue Number: #11278` write `Fixes #11278` to close issue automatically.

### Comment by [@Mostafahassen1](https://github.com/Mostafahassen1) — 2026-05-27T13:01:09Z

/retest

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T14:07:29Z

/test pull-kueue-test-integration-shard-0-main
https://prow.k8s.io/view/gs/kubernetes-ci-logs/pr-logs/pull/kubernetes-sigs_kueue/11586/pull-kueue-test-integration-shard-0-main/2059630398882713600

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T14:09:34Z

@mbobrovskyi: #11586 failed to apply on top of branch "release-0.17":
```
Applying: test(e2e): retry cluster creation on port collision (#11278)
Using index info to reconstruct a base tree...
M	hack/testing/e2e-common.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/testing/e2e-common.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-common.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 test(e2e): retry cluster creation on port collision (#11278)

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11586#issuecomment-4554618510):

>Let's give a chance.
>
>/cherry-pick release-0.17
>/cherry-pick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T14:10:09Z

@mbobrovskyi: #11586 failed to apply on top of branch "release-0.16":
```
Applying: test(e2e): retry cluster creation on port collision (#11278)
Using index info to reconstruct a base tree...
M	hack/testing/e2e-common.sh
Falling back to patching base and 3-way merge...
Auto-merging hack/testing/e2e-common.sh
CONFLICT (content): Merge conflict in hack/testing/e2e-common.sh
error: Failed to merge in the changes.
hint: Use 'git am --show-current-patch=diff' to see the failed patch
hint: When you have resolved this problem, run "git am --continue".
hint: If you prefer to skip this patch, run "git am --skip" instead.
hint: To restore the original branch and stop patching, run "git am --abort".
hint: Disable this message with "git config set advice.mergeConflict false"
Patch failed at 0001 test(e2e): retry cluster creation on port collision (#11278)

```

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11586#issuecomment-4554618510):

>Let's give a chance.
>
>/cherry-pick release-0.17
>/cherry-pick release-0.16


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-06-02T04:44:49Z

/cherry-pick release-0.17

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-06-02T04:45:28Z

@mbobrovskyi: new pull request created: #11882

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11586#issuecomment-4598788159):

>/cherry-pick release-0.17


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-25T08:04:34Z

- **hack/testing/e2e-common.sh:427** by [@mimowo](https://github.com/mimowo) — 2026-05-25T08:04:34Z
```diff
@@ -424,11 +424,29 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local max_attempts=3
```

  qq could we use the  hack/retry.sh script for this purpose?

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-25T11:24:26Z

- **hack/testing/e2e-common.sh:428** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-25T11:24:26Z
```diff
@@ -424,11 +424,23 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local log_file="$ARTIFACTS/$cluster-create.log"
+    local create_cmd="$KIND create cluster --name \"$cluster\" --image \"$E2E_KIND_VERSION\" --config \"$kind_config\" --kubeconfig=\"$kubeconfig\" --wait 1m -v 5 > \"$log_file\" 2>&1"
```

  Could you please explain why `--wait 5m` was changed to `--wait 1m`?
  As far as I know port allocation happens before wait event starts, so this could be unrelated change to this particular flake.

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-05-25T11:33:43Z

- **hack/retry.sh:21** by [@reruno](https://github.com/reruno) — 2026-05-25T11:33:43Z
```diff
@@ -18,7 +18,7 @@
 # where the captured stdout becomes the variable value.
 #
 # Usage:
-#   retry.sh [--attempts N] [--delay SECONDS] -- <command> [args...]
+#   retry.sh [--attempts N] [--delay SECONDS] [--retry-condition "CMD"] [--cleanup "CMD"] -- <command> [args...]
```

  nit: Maybe retry-condition should be named like --continue-if
  
  I think retry-condition may be interpreted like retry until it is true, continue-if sounds more appropriate in this case

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-05-25T11:34:08Z

- **hack/retry.sh:70** by [@reruno](https://github.com/reruno) — 2026-05-25T11:34:09Z
```diff
@@ -50,6 +54,23 @@ for i in $(seq 1 "$attempts"); do
         exit 0
     fi
     echo "retry [$i/$attempts] failed" 1>&2
-    [ "$i" -lt "$attempts" ] && sleep "$delay"
+
+    if [ "$i" -lt "$attempts" ]; then
+        if [ -n "$retry_condition" ]; then
+            # Evaluate the fail-fast condition
+            if ! eval "$retry_condition"; then
+                echo "retry: aborting early as retry condition failed (fail-fast)" 1>&2
+                exit 1
+            fi
+        fi
+
+        if [ -n "$cleanup" ]; then
+            # Run the cleanup command before the next attempt
+            echo "retry: running cleanup command..." 1>&2
+            eval "$cleanup"
```

  maybe exit if cleanup fails OR log the failure into stderr

### Review by [@reruno](https://github.com/reruno) (commented) — 2026-05-25T11:37:24Z

- **hack/testing/e2e-common.sh:442** by [@reruno](https://github.com/reruno) — 2026-05-25T11:37:24Z
```diff
@@ -424,11 +424,23 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local log_file="$ARTIFACTS/$cluster-create.log"
+    local create_cmd="$KIND create cluster --name \"$cluster\" --image \"$E2E_KIND_VERSION\" --config \"$kind_config\" --kubeconfig=\"$kubeconfig\" --wait 1m -v 5 > \"$log_file\" 2>&1"
+
+    local retry_condition="grep -q 'port is already allocated' \"$log_file\""
+
+    local cleanup_cmd="$KIND delete cluster --name \"$cluster\" 2>/dev/null || true"
+
+    echo "Creating kind cluster '$cluster'..."
+    if ! "${ROOT_DIR}/hack/retry.sh" \
+        --attempts 3 \
+        --delay 3 \
+        --retry-condition "$retry_condition" \
+        --cleanup "$cleanup_cmd" \
+        -- bash -c "$create_cmd"; then
+
+        echo "ERROR: unable to start the $cluster cluster after retries." >&2
```

  Capitalize error message as it was in origin code
  
  ```
  ERROR: Unable to st...
  ```

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-25T11:54:14Z

- **hack/retry.sh:16** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-25T11:54:14Z
  I believe it's worth updating a header here to cover new retry-condition/cleanup behavior.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-25T12:11:36Z

- **hack/testing/e2e-common.sh:430** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-25T12:11:36Z
```diff
@@ -424,11 +424,23 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local log_file="$ARTIFACTS/$cluster-create.log"
+    local create_cmd="$KIND create cluster --name \"$cluster\" --image \"$E2E_KIND_VERSION\" --config \"$kind_config\" --kubeconfig=\"$kubeconfig\" --wait 1m -v 5 > \"$log_file\" 2>&1"
+
+    local retry_condition="grep -q 'port is already allocated' \"$log_file\""
```

  What if this error is already in the log file from previous attempt?

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-25T12:13:25Z

Can we test this somehow to make sure the fix works correctly?

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-25T13:38:19Z

- **hack/testing/e2e-common.sh:430** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-25T13:38:19Z
```diff
@@ -424,11 +424,23 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local log_file="$ARTIFACTS/$cluster-create.log"
+    local create_cmd="$KIND create cluster --name \"$cluster\" --image \"$E2E_KIND_VERSION\" --config \"$kind_config\" --kubeconfig=\"$kubeconfig\" --wait 1m -v 5 > \"$log_file\" 2>&1"
+
+    local retry_condition="grep -q 'port is already allocated' \"$log_file\""
```

  I think it's truncated with > in create_cmd.
  That brings a question the next question: should we preserve logs of failed attempts?
  I think we should, I'll suggest adding a move command in cleanup to preserve attempt log before it gets truncated.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-25T13:41:31Z

- **hack/testing/e2e-common.sh:430** by [@mbobrovskyi](https://github.com/mbobrovskyi) — 2026-05-25T13:41:30Z
```diff
@@ -424,11 +424,23 @@ function cluster_create {
         cat "$kind_config"
     fi
 
-    if ! $KIND create cluster --name "$cluster" --image "$E2E_KIND_VERSION" \
-            --config "$kind_config" --kubeconfig="$kubeconfig" --wait 5m -v 5 \
-            > "$ARTIFACTS/$cluster-create.log" 2>&1; then
-        echo "ERROR: Unable to create kind cluster '$cluster'." >&2
-        cat "$ARTIFACTS/$cluster-create.log" >&2
+    local log_file="$ARTIFACTS/$cluster-create.log"
+    local create_cmd="$KIND create cluster --name \"$cluster\" --image \"$E2E_KIND_VERSION\" --config \"$kind_config\" --kubeconfig=\"$kubeconfig\" --wait 1m -v 5 > \"$log_file\" 2>&1"
+
+    local retry_condition="grep -q 'port is already allocated' \"$log_file\""
```

  Agree. We should keep all logs.

### Review by [@mbobrovskyi](https://github.com/mbobrovskyi) (commented) — 2026-05-27T12:23:30Z

Thank you!

/lgtm
/approve
