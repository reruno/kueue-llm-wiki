# PR #11668: Trigger backoff if nominated multikueue clusters are not yet connected

**Summary**: Trigger backoff if nominated multikueue clusters are not yet connected

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11668

**Last updated**: 2026-05-28T12:29:30Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@ivnovakov](https://github.com/ivnovakov)
- **Branch**: `fix/multikueue-connecting-requeue` → `main`
- **Created**: 2026-05-27T06:17:16Z
- **Updated**: 2026-05-28T12:29:30Z
- **Merged**: 2026-05-27T17:06:48Z
- **Closed**: 2026-05-27T17:06:48Z
- **Labels**: `kind/bug`, `lgtm`, `kind/regression`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`, `size/S`, `area/multikueue`
- **Assignees**: [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@mwielgus](https://github.com/mwielgus), [@mbobrovskyi](https://github.com/mbobrovskyi), [@PBundyra](https://github.com/PBundyra)
- **Changed files**: 3   **+21 / -4**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind bug
/kind regression

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature
/kind kep

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression

Please also consider setting the area:
/area tas
/area integrations
/area multikueue
/area dashboard
/area localization
/area testing
/area website
-->
/area multikueue
#### What this PR does / why we need it:

Before #11559, remoteClientsForAC returned ErrNoActiveClusters, this propagated up through readGroup, Reconcile, controller-runtime applied exponential backoff, eventually retried and clusters were connected by then.

After #11559, remoteClientsForAC returns an empty map, readGroup succeeds with an empty wlGroup.remotes, reconcileGroup, in nominateAndSynchronizeWorkers, it iterates over an empty map of remotes and returns (Result{}, nil). So, result is no requeue, no error, workload is left hanging in a Pending state.

This PR introduces a fail-fast check in `nominateAndSynchronizeWorkers`.
If any cluster in the `nominatedWorkers` list is missing from the connected `remoteClients` map, it now returns a new `ErrNoRemoteClientForNominatedCluster` error. This successfully triggers the controller-runtime exponential backoff, automatically retrying the sync until the cluster finishes connecting.

This way it does not break `workerLostTimeout` feature for admitted workloads, which #11559 brought back on.

> 	// Return an empty map (rather than an error) when all configured clusters
	// are disconnected or reconnecting. This allows readGroup to construct an
	// empty group, so reconcileGroup can apply the workerLostTimeout delay via
	// the existing "reserving remote lost" branch instead of failing with
	// ErrNoActiveClusters and retrying via controller-runtime backoff.

Tested locally, was able to reproduce the issue. Added logging indicated cases when remoteClient was not ready, and after few retries remoteClient was ready for worker1 and then for worker2.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #11631

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-05-27T06:17:22Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 07a5f22ca4cd3a77ba1e7108c19b552254db673d |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a171827fd2b0700086a33ae |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T06:17:27Z

Hi @ivnovakov. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work.

>[!TIP]
>**We noticed you've done this a few times! Consider [joining the org](https://git.k8s.io/community/community-membership.md#member) to skip this step and gain `/lgtm` and other bot rights.** We recommend asking approvers on your previous PRs to sponsor you.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T06:47:41Z

/ok-to-test

### Comment by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-27T15:57:53Z

@mimowo @mwielgus 
I pushed solution with different approach and updated a PR description, ptal.

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T16:26:54Z

LGTM, we may just add a unit test which would demonstrate we don't short circuit the syncs after the first non-connected worker, asked here: https://github.com/kubernetes-sigs/kueue/pull/11668#discussion_r3312244370

### Comment by [@mimowo](https://github.com/mimowo) — 2026-05-27T17:04:38Z

/lgtm
/approve
/cherrypick release-0.17
/cherrypick release-0.16
Let me merge already as this flake is pretty common now. We can follow up with the extra test case.

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T17:04:42Z

@mimowo: once the present PR merges, I will cherry-pick it on top of `release-0.16`, `release-0.17` in new PRs and assign them to you.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11668#issuecomment-4556772708):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Let me merge already as this flake is pretty common now. We can follow up with the extra test case.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T17:04:48Z

LGTM label has been added.  <details>Git tree hash: 370debc4cee2802a2a27651098ef3d06defda611</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-05-27T17:04:49Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11668#" title="Author self-approved">ivnovakov</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/11668#issuecomment-4556772708" title="Approved">mimowo</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [mimowo]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T17:07:31Z

@mimowo: new pull request created: #11692

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11668#issuecomment-4556772708):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Let me merge already as this flake is pretty common now. We can follow up with the extra test case.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@k8s-infra-cherrypick-robot](https://github.com/k8s-infra-cherrypick-robot) — 2026-05-27T17:08:09Z

@mimowo: new pull request created: #11693

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/11668#issuecomment-4556772708):

>/lgtm
>/approve
>/cherrypick release-0.17
>/cherrypick release-0.16
>Let me merge already as this flake is pretty common now. We can follow up with the extra test case.


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T06:48:53Z

- **pkg/controller/admissionchecks/multikueue/workload.go:67** by [@mimowo](https://github.com/mimowo) — 2026-05-27T06:48:53Z
```diff
@@ -63,6 +63,10 @@ var (
 	singleClusterPreemptionTimeout = 5 * time.Minute
 )
 
+// The remote-client reconnect backoff starts at 5s,
+// so 2s usually catches the first successful connect.
```

  This sounds a little bit fishy, is this a pragmatic fix that would work most of the time, or something solid? 
  
  I cannot understand from the comment.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T06:49:42Z

- **pkg/controller/admissionchecks/multikueue/workload.go:268** by [@mimowo](https://github.com/mimowo) — 2026-05-27T06:49:42Z
```diff
@@ -250,16 +261,27 @@ func (w *wlReconciler) updateACS(ctx context.Context, wl *kueue.Workload, acs *k
 	})
 }
 
-func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName kueue.AdmissionCheckReference) (map[string]*remoteClient, error) {
+// remoteClientsForAC returns the connected clients for acName.
+// The bool is true if any cluster was skipped because its client is still connecting.
+// When that happens the caller must retry via RequeueAfter:
+// nothing else re-runs the workload reconcile when the connection comes up.
+func (w *wlReconciler) remoteClientsForAC(ctx context.Context, acName kueue.AdmissionCheckReference) (map[string]*remoteClient, bool, error) {
```

  The logic here seems quite tricky already, inject logging at V3 for the cluster recononnection steps

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T06:50:59Z

- **pkg/controller/admissionchecks/multikueue/workload.go:251** by [@mimowo](https://github.com/mimowo) — 2026-05-27T06:51:00Z
```diff
@@ -238,7 +242,14 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		return reconcile.Result{}, nil
 	}
 
-	return w.reconcileGroup(ctx, grp)
+	res, err := w.reconcileGroup(ctx, grp)
+	if err == nil && connecting {
+		// Don't overwrite a shorter requeue already set by reconcileGroup.
+		if res.RequeueAfter == 0 || res.RequeueAfter > connectingRetryAfter {
+			res.RequeueAfter = connectingRetryAfter
+		}
+	}
```

  Inject some logic to guide future investigations in the area. Cluster reconnection is a rare thing, and happening in OMG situations, so we can affort V3, or maybe even V2 in some cases.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T06:54:40Z

- **pkg/controller/admissionchecks/multikueue/workload.go:248** by [@mimowo](https://github.com/mimowo) — 2026-05-27T06:54:41Z
```diff
@@ -238,7 +242,14 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		return reconcile.Result{}, nil
 	}
 
-	return w.reconcileGroup(ctx, grp)
+	res, err := w.reconcileGroup(ctx, grp)
+	if err == nil && connecting {
+		// Don't overwrite a shorter requeue already set by reconcileGroup.
+		if res.RequeueAfter == 0 || res.RequeueAfter > connectingRetryAfter {
```

  It seems the semantic of RequeueAfter==0 is overloaded, because it can have two meanings:
  - requeue immediately
  - do not requeue at all
  
  Seeing the code I'm feeling this is (2.), correct me if I'm misinterpreting the code.
  
  Maybe we could make it cleaner with using RequeuAfter as a ptr.

### Review by [@mwielgus](https://github.com/mwielgus) (commented) — 2026-05-27T08:00:06Z

- **pkg/controller/admissionchecks/multikueue/workload.go:66** by [@mwielgus](https://github.com/mwielgus) — 2026-05-27T07:58:44Z
```diff
@@ -63,6 +63,10 @@ var (
 	singleClusterPreemptionTimeout = 5 * time.Minute
 )
 
+// The remote-client reconnect backoff starts at 5s,
```

  Why not use constant directly computed from ther remote client backoff start (like max(backoff/2, 2s))?

- **pkg/controller/admissionchecks/multikueue/workload.go:229** by [@mwielgus](https://github.com/mwielgus) — 2026-05-27T08:00:03Z
```diff
@@ -222,7 +226,7 @@ func (w *wlReconciler) Reconcile(ctx context.Context, req reconcile.Request) (re
 		}
 	}
 
-	grp, err := w.readGroup(ctx, wl, mkAc.Name, adapter, owner.Name)
+	grp, connecting, err := w.readGroup(ctx, wl, mkAc.Name, adapter, owner.Name)
```

  Is connecting always true between retries?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T16:03:39Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@mimowo](https://github.com/mimowo) — 2026-05-27T16:03:39Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  When we hit this error is the code going to return here?

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-27T16:07:34Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-27T16:07:34Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  Yes, because next code block will try to access remoteClient of nominated worker.

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-27T16:15:57Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-27T16:15:57Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  Thanks for the catch, we shouldn't return error there, just append.
  That would've blocked sync for nominated workers that have connection already.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T16:21:21Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@mimowo](https://github.com/mimowo) — 2026-05-27T16:21:21Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  I see, I didn't mean this case specifically, but nice that it inspired you to improve the code.
  
  Do we have a unit test for the improvement?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T16:23:39Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@mimowo](https://github.com/mimowo) — 2026-05-27T16:23:39Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  Ah I see you test in wantCreated, but it does not show the code would also perform any sync after the error is hit.

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-27T16:25:07Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-27T16:25:07Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  Unit test caught the change. 
  I updated it accordingly, nominated worker with remoteClient available was put in wantCreated.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2026-05-27T16:25:51Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@mimowo](https://github.com/mimowo) — 2026-05-27T16:25:51Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  > Before https://github.com/kubernetes-sigs/kueue/pull/11559, remoteClientsForAC returned ErrNoActiveClusters, this propagated up through readGroup, Reconcile, controller-runtime applied exponential backoff, eventually retried and clusters were connected by then.
  
  This is actually answering my initial question, that with the new code you are bringing back the original behavior of the retries using controller-runtime backoff, for workloads during nomination phase. This is great.

### Review by [@ivnovakov](https://github.com/ivnovakov) (commented) — 2026-05-27T17:12:10Z

- **pkg/controller/admissionchecks/multikueue/workload.go:801** by [@ivnovakov](https://github.com/ivnovakov) — 2026-05-27T17:12:10Z
```diff
@@ -795,6 +795,13 @@ func (w *wlReconciler) nominateAndSynchronizeWorkers(ctx context.Context, group
 		nominatedWorkers = group.local.Status.NominatedClusterNames
 	}
 
+	for _, nom := range nominatedWorkers {
+		if _, ok := group.remoteClients[nom]; !ok {
+			log.V(3).Info("Nominated cluster not yet connected", "cluster", nom)
+			return reconcile.Result{}, fmt.Errorf("cluster %s: %w", nom, admissioncheck.ErrNoRemoteClientForNominatedCluster)
```

  I looked more carefully and added test actually checks that synchronization is not blocked for nominated workers with remoteClient available, so in my opinion we don't need a follow up here.
