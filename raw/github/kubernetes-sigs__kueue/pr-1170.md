# PR #1170: add docs for workloadPriority

**Summary**: add docs for workloadPriority

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1170

**Last updated**: 2023-10-10T18:46:54Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@Gekko0114](https://github.com/Gekko0114)
- **Branch**: `priority_doc` → `main`
- **Created**: 2023-10-01T14:42:42Z
- **Updated**: 2023-10-10T18:46:54Z
- **Merged**: 2023-10-10T18:46:52Z
- **Closed**: 2023-10-10T18:46:52Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 6   **+221 / -11**

## Description

#### What type of PR is this?
/kind documentation

#### What this PR does / why we need it:
add docs for workloadPriority

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1157 

#### Does this PR introduce a user-facing change?
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-01T14:42:48Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 05c2f0f8793232ed00f733afaa53186efb89598f |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6523e5c76d379100086cd425 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1170--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1170--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExNzAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.qeQPmNKkmJ5LcJXaUCVjeMIItdqa_zdnam0zbTKdfik)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-01T14:42:51Z

Hi @Gekko0114. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-01T14:46:38Z

Hi @alculquicondor , @tenzen-y ,
Could you review this PR? Thanks,

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-01T14:48:13Z

/assign @alculquicondor
/assign @tenzen-y

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T05:45:09Z

/ok-to-test

### Comment by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-03T15:00:28Z

> Also, can you add a concept for the WorkloadPriority to https://github.com/kubernetes-sigs/kueue/tree/f6d9f1e7c4efefba1d67825c2b80e73a17840bdf/site/content/en/docs/concepts?

Sure, added a concept.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T11:05:39Z

/assign @trasc

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-09T12:21:59Z

LGTM label has been added.  <details>Git tree hash: 4549ab081455effaaa613e8f0078acc81f21fe52</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-10T18:41:25Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1170#pullrequestreview-1668588396" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1170#" title="Author self-approved">Gekko0114</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-03T05:45:02Z

Also, can you add a concept for the WorkloadPriority to https://github.com/kubernetes-sigs/kueue/tree/f6d9f1e7c4efefba1d67825c2b80e73a17840bdf/site/content/en/docs/concepts?

- **site/content/en/docs/tasks/_index.md:42** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-03T05:40:26Z
```diff
@@ -38,7 +38,8 @@ As a batch user, you can learn how to:
   Kueue supports MPIJob v2beta1, PyTorchJob, TFJob, XGBoostJob, and PaddleJob.
 - [Run a Kueue managed KubeRay RayJob](/docs/tasks/run_rayjobs).
 - [Submit Kueue jobs from Python](/docs/tasks/run_python_jobs).
-- [Run a Kueue managed plain Pod](/docs/tasks/run_plain_pods)
+- [Run a Kueue managed plain Pod](/docs/tasks/run_plain_pods).
+- [Run a Kueue managed Jobs with WorkloadPriority](/docs/tasks/run_job_with_workload_priority).
```

  We should move this to `batch administrator` section.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-05T11:05:23Z

- **site/content/en/docs/concepts/workload.md:107** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:27:16Z
```diff
@@ -103,4 +103,5 @@ creating a corresponding Workload object for it.
 
```

  Can you update the `Priority` section in this file as well?

- **site/content/en/docs/concepts/workload_priority_class.md:20** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:28:51Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
```

  ```suggestion
  apiVersion: kueue.x-k8s.io/v1beta1
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:49** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:39:39Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
```

  ```suggestion
  spec:
  ...
  ```
  
  Remove unrelated parameters. We shows the concrete examples in the tasks section.

- **site/content/en/docs/concepts/workload_priority_class.md:71** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:40:22Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
```

  ```suggestion
  ...
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:52** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:43:59Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
```

  ```suggestion
  The following workload is generated by the yaml above. The `priorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` names as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:2** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:46:56Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
```

  ```suggestion
  title: "WorkloadPriorityClass"
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:30** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:47:19Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
```

  ```suggestion
  The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:96** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:49:26Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
```

  ```suggestion
  About scenario #1, the `WorkloadPriorityClass` is used for the workload's priority. On the other hand, the `PriorityClass` is used for the pod's priority.  
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:97** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:50:30Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
```

  ```suggestion
  About scenario #2, the `WorkloadPriorityClass` is used for the workload's priority. On the other hand, `WorkloadPriorityClass` is not used for pod's priority.  
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:83** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:50:50Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
```

  ```suggestion
  About scenario #3,  the `PriorityClass` is used for the workload's priority and pod's priority. 
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:92** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:51:52Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
```

  ```suggestion
  1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:93** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:52:03Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
```

  ```suggestion
  2. A job specifies only `WorkloadPriorityClass`
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:94** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:52:17Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
```

  ```suggestion
  3. A job specifies only `PriorityClass`
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T10:54:54Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  Also, it would be better to explain what happens if the job implement the [`JobWithPriorityClass`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84) interface.

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:22** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T11:04:49Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
+
+This page shows an instruction on how to run a job with workloadPriority.
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+
+## 0. Create workloadPriorityClass
```

  ```suggestion
  ## 0. Create WorkloadPriorityClass
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-05T12:23:47Z

- **keps/973-workload-priority/README.md:182** by [@trasc](https://github.com/trasc) — 2023-10-05T11:48:30Z
```diff
@@ -176,11 +176,10 @@ To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` fiel
 When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
 
 ```yaml
-# sample-workload.yaml
 apiVersion: kueue.x-k8s.io/v1beta1
 kind: Workload
 metadata:
-  name: job-sample-job-jf5fb-f5982
+  name: job-sample-job-jf5fb
```

  ```suggestion
    name: job-sample-job-7f173
  ```
  `jf5fb` was the part generated by `kubectl create`

- **site/content/en/docs/concepts/workload_priority_class.md:2** by [@trasc](https://github.com/trasc) — 2023-10-05T11:55:01Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
```

  It's better wit spaces to be consistent with the existing content 
  
  https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/site/content/en/docs/concepts/resource_flavor.md?plain=1#L2

- **site/content/en/docs/concepts/workload_priority_class.md:83** by [@trasc](https://github.com/trasc) — 2023-10-05T11:58:20Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
```

  Maybe add the details in the numbered list above.

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@trasc](https://github.com/trasc) — 2023-10-05T12:05:16Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  @tenzen-y  this might be too developer focused and a user cannot do too much about it.

- **site/content/en/docs/tasks/_index.md:28** by [@trasc](https://github.com/trasc) — 2023-10-05T12:06:54Z
```diff
@@ -25,6 +25,7 @@ As a batch administrator, you can learn how to:
 - Setup [Sequential Admission with Ready Pods](/docs/tasks/setup_sequential_admission).
 - As a batch administrator, you can learn how to
   [monitor pending workloads](/docs/tasks/monitor_pending_workloads).
+- If you don't want to use pod's priority for workload, you can learn how to [run a Kueue managed Jobs with WorkloadPriority](/docs/tasks/run_job_with_workload_priority).
```

  ```suggestion
  -As a batch administrator, you can learn how to [run a Kueue managed Jobs with a custom WorkloadPriority](/docs/tasks/run_job_with_workload_priority).
  ```
  
  This also might be more relevant for  batch users. And maybe administrators targeted section for the WorkloadPriority definition .

- **site/content/en/docs/concepts/workload_priority_class.md:92** by [@trasc](https://github.com/trasc) — 2023-10-05T12:15:04Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
+## Where workload's priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+
+## Workload's priority values are always mutable
+
+Workload's `Priority` field is always mutable because it might be useful for the preemption.  
```

  Is this something relevant for a user?

- **site/content/en/docs/concepts/workload_priority_class.md:107** by [@trasc](https://github.com/trasc) — 2023-10-05T12:18:50Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
+## Where workload's priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+
+## Workload's priority values are always mutable
+
+Workload's `Priority` field is always mutable because it might be useful for the preemption.  
+Workload's `PriorityClass` and `PriorityClassName` fields are immutable for simplicity.
```

  Shold it be like? 
  
  ```suggestion
  Workload's `PriorityClassSource` and `PriorityClassName` fields are immutable for simplicity.
  ```

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:37** by [@trasc](https://github.com/trasc) — 2023-10-05T12:23:18Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
+
+This page shows an instruction on how to run a job with workloadPriority.
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+
+## 0. Create workloadPriorityClass
+
+The workloadPriorityClass should be created first.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## 1. Create Job with `kueue.x-k8s.io/priority-class` label
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`. This is same as other CRDs like `RayJob`.  
```

  ```suggestion
  The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`. This is same for other CRDs like `RayJob`.  
  ```

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-05T13:55:56Z

- **site/content/en/docs/concepts/workload_priority_class.md:2** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T13:55:56Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
```

  Oh, I missed the other titles. It makes sense.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-05T14:01:36Z

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T14:01:36Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  I was also concerned about what you say. However, actually, which priorities are used depends on the interface.
  For example, we can set three priorities (`.spec.runPolicy.priorityClass`, `.spec.labels.kueue.x-k8s.io/priority-class`, and `.spec.mpiReplicaSecs[*].template.spec.priorityClassName`) on the MPIJob.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-10-05T15:20:27Z

- **site/content/en/docs/concepts/workload_priority_class.md:92** by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-05T15:20:27Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
+## Where workload's priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+
+## Workload's priority values are always mutable
+
+Workload's `Priority` field is always mutable because it might be useful for the preemption.  
```

  I think so, I added an example

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-10-05T15:20:43Z

- **site/content/en/docs/concepts/workload.md:107** by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-05T15:20:43Z
```diff
@@ -103,4 +103,5 @@ creating a corresponding Workload object for it.
 
```

  updated

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-05T17:36:58Z

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-05T17:36:58Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  Or, instead of mentioning the interface, raise MPIJob as a specific case.
  
  @alculquicondor Any better ideas?

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-10-06T13:07:08Z

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-06T13:07:08Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  Thanks for your comments! Added the explanation related to JobWithPriorityClass

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-06T18:34:28Z

- **site/content/en/docs/concepts/_index.md:39** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:06:28Z
```diff
@@ -34,6 +34,12 @@ single tenant.
 An application that will run to completion. It is the unit of _admission_ in
 Kueue. Sometimes referred to as _job_.
 
+### [Workload Priority Class](/docs/concepts/workload_priority_class)
+
+`WorkloadPriorityClass` is a `PriorityClass` independent from pod's priority. 
```

  ```suggestion
  `WorkloadPriorityClass` defines a priority class for a workload, independently from [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/).
  ```

- **site/content/en/docs/concepts/_index.md:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:08:07Z
```diff
@@ -34,6 +34,12 @@ single tenant.
 An application that will run to completion. It is the unit of _admission_ in
 Kueue. Sometimes referred to as _job_.
 
+### [Workload Priority Class](/docs/concepts/workload_priority_class)
+
+`WorkloadPriorityClass` is a `PriorityClass` independent from pod's priority. 
+`WorkloadPriorityClass` is specified through a label of each job. 
```

  I would remove this line and leave it to the main document.

- **site/content/en/docs/concepts/_index.md:41** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:09:16Z
```diff
@@ -34,6 +34,12 @@ single tenant.
 An application that will run to completion. It is the unit of _admission_ in
 Kueue. Sometimes referred to as _job_.
 
+### [Workload Priority Class](/docs/concepts/workload_priority_class)
+
+`WorkloadPriorityClass` is a `PriorityClass` independent from pod's priority. 
+`WorkloadPriorityClass` is specified through a label of each job. 
+This priority value is only used for managing the queueing and preemption of workloads.
```

  ```suggestion
  This priority value from a WorkloadPriorityClass is only used for managing the queueing and preemption of  [Workloads](#workload).
  ```

- **site/content/en/docs/concepts/workload.md:98** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:11:34Z
```diff
@@ -95,6 +95,9 @@ For a `batch/v1.Job`, Kueue sets the priority of the Workload based on the
 [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/)
 of the Job's pod template.
 
+Sometimes developers would like to control workload's priority without affecting pod's priority.  
```

  Prefer to make a change in line 92 that explains that there are two ways of setting the workload priority for a Job. Then use an unordered list to show the two options.

- **site/content/en/docs/concepts/workload_priority_class.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:12:06Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
```

  ```suggestion
    A priority class which value is utilized by Kueue controller and is independent from pod's priority.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:13:06Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
```

  Use the second person:
  A WorkloadPriorityClass allows you to control the Workload's priority without affecting the pod's priority.

- **site/content/en/docs/concepts/workload_priority_class.md:15** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:13:43Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
```

  with the suggestion above, this paragraph becomes unnecessary.

- **site/content/en/docs/concepts/workload_priority_class.md:9** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:14:57Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
```

  Also link to the documentation for Workload.

- **site/content/en/docs/concepts/workload_priority_class.md:30** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:15:43Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
```

  You can specify the WorkloadPriorityClass by setting the label X

- **site/content/en/docs/concepts/workload_priority_class.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:16:02Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
```

  Kueue generates the following workload for the Job above.

- **site/content/en/docs/concepts/workload_priority_class.md:59** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:16:30Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
```

  ```suggestion
  For other job frameworks, you can set `WorkloadPriorityClass` using the same label.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:76** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:18:51Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job.
```

  When creating a Workload for a given job, Kueue considers the following scenarios:

- **site/content/en/docs/concepts/workload_priority_class.md:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:19:29Z
```diff
@@ -0,0 +1,112 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1alpha1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The workloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+The following workload is generated by the yaml above. The `PriorityClass` field can accept both k8s `PriorityClass` and `workloadPriorityClass` names as values. To distinguish, when using `workloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using k8s `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-jf5fb
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+  podSets:
+  - count: 3
+    name: dummy-job
+    template:
+      spec:
+        containers:
+        - image: gcr.io/k8s-staging-perf-tests/sleep:latest
+          name: dummy-job
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same as Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+.....
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job (other job frameworks are same).
+1. A job specifies both `workloadPriorityClass` and `PriorityClass`
+2. A job specifies only `workloadPriorityClass`
+3. A job specifies only `priorityClass`
+
+About #1's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, the `priorityClass` is used for the `pod's priority`.  
+About #2's scenario, the `workloadPriorityClass` is used for the `workload's priority`. On the other hand, `workloadPriorityClass` is not used for `pod's priority`.  
+About #3' scenario,  the `PriorityClass` is used for the `workload's priority` and `pod's priority`. 
+
```

  I like how Hiroyuki phrased it :)

- **site/content/en/docs/concepts/workload_priority_class.md:86** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:23:31Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job.
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that have multiple fields related to pod's priorities within their Spec, or pod priorities are defined in a different location compared to other jobs. In such scenarios, Kueue controller utilizes [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84).  
```

  ```suggestion
  In certain job frameworks, there are CRDs that:
  - Define multiple pod specs, where each can have their own pod priority, or
  - Define the overall pod priority in a dedicated field.
  
  In such scenarios, the integration of the CRD with Kueue utilizes the [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84) to obtain the priority class. You can read the documentation for each job integration to learn how the priority class is obtained.
  ```
  
  (do we have that information?)

- **site/content/en/docs/concepts/workload_priority_class.md:91** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:25:35Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job.
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that have multiple fields related to pod's priorities within their Spec, or pod priorities are defined in a different location compared to other jobs. In such scenarios, Kueue controller utilizes [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84).  
+When defining workload priority, the first step is to check if they have a `WorkloadPriorityClass`. If not, the priority determination process follows the logic defined in the `JobWithPriorityClass interface` for each CRD.
+
+## Where workload's priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
```

  ```suggestion
  The priority of workloads is used for:
  - Sorting the workloads in the ClusterQueues and cohorts.
  - Determining whether a workload can preempt others.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:29:03Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job.
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that have multiple fields related to pod's priorities within their Spec, or pod priorities are defined in a different location compared to other jobs. In such scenarios, Kueue controller utilizes [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84).  
+When defining workload priority, the first step is to check if they have a `WorkloadPriorityClass`. If not, the priority determination process follows the logic defined in the `JobWithPriorityClass interface` for each CRD.
+
+## Where workload's priority is used
+
+The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue.
+
+## Workload's priority values are always mutable
+
+Workload's `Priority` field is always mutable because it might be useful for the preemption. Thanks to this feature, when there is a workload that has been queued for a while, you can rewrite its priority to execute it with higher priority.  
```

  ```suggestion
  The Workload's `priority` field is always mutable.
  If a Workload has been pending for a while, you can consider updating its priority to execute it earlier,
  based on your own policies. 
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:23** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:30:10Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
```

  Mention that these objects are cluster scoped, so they can be used by a job in any namespace.

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:12** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:32:50Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
+
+This page shows an instruction on how to run a job with workloadPriority.
```

  ```suggestion
  This page contains instructions on how to run a job with workload priority.
  ```

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:10** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:33:05Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
```

  ```suggestion
  By using a `WorkloadPriorityClass`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
  ```

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:10** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:33:44Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
```

  link to the concept page.

- **site/content/en/docs/tasks/run_job_with_workload_priority.md:60** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T18:34:16Z
```diff
@@ -0,0 +1,80 @@
+---
+title: "Run job with WorkloadPriority"
+date: 2023-10-02
+weight: 8
+description: >
+  Run job with WorkloadPriority, which is independent from Pod's priority
+---
+
+Usually, in Kueue, workload's priority is calculated using for pod's priority for queuing and preemption.  
+By using `WorkloadPriority`, you can independently manage the priority of workloads for queuing and preemption, separate from pod's priority.  
+
+This page shows an instruction on how to run a job with workloadPriority.
+
+## Before you begin
+
+Make sure the following conditions are met:
+
+- A Kubernetes cluster is running.
+- The kubectl command-line tool has communication with your cluster.
+- [Kueue is installed](/docs/installation).
+
+## 0. Create WorkloadPriorityClass
+
+The WorkloadPriorityClass should be created first.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## 1. Create Job with `kueue.x-k8s.io/priority-class` label
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`. This is same for other CRDs like `RayJob`.  
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+  parallelism: 3
+  completions: 3
+  suspend: true
+  template:
+    spec:
+      containers:
+      - name: dummy-job
+        image: gcr.io/k8s-staging-perf-tests/sleep:latest
+      restartPolicy: Never
+```
+
+Then the following workload will be generated. The priority of workloads is utilized in queuing, preemption, and other scheduling processes in Kueue. This priority doesn't affect pod's priority.  
+Workload's `Priority` field is always mutable because it might be useful for the preemption. Workload's `PriorityClassSource` and `PriorityClassName` fields are immutable for simplicity.
```

  ```suggestion
  Workload's `Priority` field is always mutable because it might be useful for the preemption. Workload's `PriorityClassSource` and `PriorityClassName` fields are immutable.
  ```
  
  "for simplicity" is a design decision that doesn't need to be exposed to users.

### Review by [@Gekko0114](https://github.com/Gekko0114) (commented) — 2023-10-07T07:27:02Z

- **site/content/en/docs/concepts/workload_priority_class.md:86** by [@Gekko0114](https://github.com/Gekko0114) — 2023-10-07T07:27:01Z
```diff
@@ -0,0 +1,101 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priorityClass which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+Sometimes developers would like to control workload's priority without affecting pod's priority, like these cases:
+
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A WorkloadPriorityClass is a priorityClass which value is utilized by Kueue controller and 
+is independent from pod's priority.
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+## How to use WorkloadPriorityClass on Jobs
+
+The WorkloadPriorityClass is specified through a label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+The following workload is generated by the yaml above. The `PriorityClassName` field can accept either `PriorityClass` or `WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`, a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value. When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` through a label same for Job.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+There are three scenarios for creating a workload from a job.
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that have multiple fields related to pod's priorities within their Spec, or pod priorities are defined in a different location compared to other jobs. In such scenarios, Kueue controller utilizes [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84).  
```

  > You can read the documentation for each job integration to learn how the priority class is obtained.
  (do we have that information?)
  
  No, I couldn't find a good data source.
  Instead, I wrote like this:
  You can read the **code** for each job integration to learn how the priority class is obtained.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-09T08:47:44Z

lgtm
leaving `/lgtm` on @alculquicondor

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-09T10:39:39Z

- **site/content/en/docs/concepts/workload_priority_class.md:96** by [@trasc](https://github.com/trasc) — 2023-10-09T10:37:44Z
```diff
@@ -0,0 +1,113 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priority class which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+A `WorkloadPriorityClass` allows you to control the [`Workload`'s](/docs/concepts/workload) priority without affecting the pod's priority.
+This feature is useful for these cases:
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+`WorkloadPriorityClass` objects are cluster scoped, so they can be used by a job in any namespace.
+
+## How to use WorkloadPriorityClass on Jobs
+
+You can specify the `WorkloadPriorityClass` by setting the label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+Kueue generates the following `Workload` for the Job above.
+The `PriorityClassName` field can accept either `PriorityClass` or
+`WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`,
+a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` using the same label.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+When creating a `Workload` for a given job, Kueue considers the following scenarios:
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that:
+- Define multiple pod specs, where each can have their own pod priority, or
+- Define the overall pod priority in a dedicated field.
+In such scenarios, the integration of the CRD with Kueue utilizes the
+[`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84)
+to obtain the priority class. You can read the code for each job integration
+to learn how the priority class is obtained.
```

  ```suggestion
  In certain job frameworks, there are CRDs that:
  - Define multiple pod specs, where each can have their own pod priority, or
  - Define the overall pod priority in a dedicated field.
  By default kueue will take the PriorityClassName of the first PodSet having one set, However the integration of the CRD with Kueue can implement
  [`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84)
  to change this behavior. You can read the code for each job integration
  to learn how the priority class is obtained.
  ```

- **site/content/en/docs/concepts/workload_priority_class.md:100** by [@trasc](https://github.com/trasc) — 2023-10-09T10:38:15Z
```diff
@@ -0,0 +1,113 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priority class which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+A `WorkloadPriorityClass` allows you to control the [`Workload`'s](/docs/concepts/workload) priority without affecting the pod's priority.
+This feature is useful for these cases:
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+`WorkloadPriorityClass` objects are cluster scoped, so they can be used by a job in any namespace.
+
+## How to use WorkloadPriorityClass on Jobs
+
+You can specify the `WorkloadPriorityClass` by setting the label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+Kueue generates the following `Workload` for the Job above.
+The `PriorityClassName` field can accept either `PriorityClass` or
+`WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`,
+a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` using the same label.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+When creating a `Workload` for a given job, Kueue considers the following scenarios:
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that:
+- Define multiple pod specs, where each can have their own pod priority, or
+- Define the overall pod priority in a dedicated field.
+In such scenarios, the integration of the CRD with Kueue utilizes the
+[`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84)
+to obtain the priority class. You can read the code for each job integration
+to learn how the priority class is obtained.
+
+## Where workload's priority is used
+
+The priority of workloads is used for:
+- Sorting the workloads in the ClusterQueues and cohorts.
```

  ```suggestion
  - Sorting the workloads in the ClusterQueues.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-09T12:21:53Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-10T18:41:13Z

- **site/content/en/docs/concepts/workload_priority_class.md:95** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T18:41:12Z
```diff
@@ -0,0 +1,114 @@
+---
+title: "Workload Priority Class"
+date: 2023-10-02
+weight: 6
+description: >
+  A priority class which value is utilized by Kueue controller and is independent from pod's priority.
+---
+
+A `WorkloadPriorityClass` allows you to control the [`Workload`'s](/docs/concepts/workload) priority without affecting the pod's priority.
+This feature is useful for these cases:
+- want to prioritize workloads that remain inactive for a specific duration
+- want to set a lower priority for development workloads and higher priority for production workloads
+
+A sample WorkloadPriorityClass looks like the following:
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: WorkloadPriorityClass
+metadata:
+  name: sample-priority
+value: 10000
+description: "Sample priority"
+```
+
+`WorkloadPriorityClass` objects are cluster scoped, so they can be used by a job in any namespace.
+
+## How to use WorkloadPriorityClass on Jobs
+
+You can specify the `WorkloadPriorityClass` by setting the label `kueue.x-k8s.io/priority-class`.
+
+```yaml
+apiVersion: batch/v1
+kind: Job
+metadata:
+  name: sample-job
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+Kueue generates the following `Workload` for the Job above.
+The `PriorityClassName` field can accept either `PriorityClass` or
+`WorkloadPriorityClass` name as a value. To distinguish, when using `WorkloadPriorityClass`,
+a `priorityClassSource` field has the `kueue.x-k8s.io/workloadpriorityclass` value.
+When using `PriorityClass`, a `priorityClassSource` field has the `scheduling.k8s.io/priorityclass` value.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: Workload
+metadata:
+  name: job-sample-job-7f173
+spec:
+  priorityClassSource: kueue.x-k8s.io/workloadpriorityclass
+  priorityClassName: sample-priority
+  priority: 10000
+  queueName: user-queue
+...
+```
+
+For other job frameworks, you can set `WorkloadPriorityClass` using the same label.
+The Following is an example of `MPIJob`.
+
+```yaml
+apiVersion: kubeflow.org/v2beta1
+kind: MPIJob
+metadata:
+  name: pi
+  labels:
+    kueue.x-k8s.io/queue-name: user-queue
+    kueue.x-k8s.io/priority-class: sample-priority
+spec:
+...
+```
+
+## The relationship between pod's priority and workload's priority
+
+When creating a `Workload` for a given job, Kueue considers the following scenarios:
+1. A job specifies both `WorkloadPriorityClass` and `PriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `PriorityClass` is used for the pod's priority.
+2. A job specifies only `WorkloadPriorityClass`
+- `WorkloadPriorityClass` is used for the workload's priority.
+- `WorkloadPriorityClass` is not used for pod's priority.
+3. A job specifies only `PriorityClass`
+- `PriorityClass` is used for the workload's priority and pod's priority.
+
+In certain job frameworks, there are CRDs that:
+- Define multiple pod specs, where each can have their own pod priority, or
+- Define the overall pod priority in a dedicated field.
+By default kueue will take the PriorityClassName of the first PodSet having one set,
+however the integration of the CRD with Kueue can implement
+[`JobWithPriorityClass interface`](https://github.com/kubernetes-sigs/kueue/blob/e162f8508b503d20feb9b31fd0b27d91e58f2c2f/pkg/controller/jobframework/interface.go#L81-L84)
+to change this behavior. You can read the code for each job integration
```

  I guess that's what we have today :(
  
  If you have the time, please update the documentation for MPIJob and other kubeflow CRDs to explain how we obtain the priority class for each.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-10T18:41:18Z

/approve
