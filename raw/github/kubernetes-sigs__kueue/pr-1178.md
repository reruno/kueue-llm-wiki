# PR #1178: Preemption admission check controller. KEP update.

**Summary**: Preemption admission check controller. KEP update.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1178

**Last updated**: 2025-07-14T12:20:46Z

---

## Metadata

- **State**: closed
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `preemption-check-controller-kep` → `main`
- **Created**: 2023-10-04T11:03:16Z
- **Updated**: 2025-07-14T12:20:46Z
- **Merged**: —
- **Closed**: 2025-07-14T12:20:45Z
- **Labels**: `release-note-none`, `size/M`, `lifecycle/rotten`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+38 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:

Preemption admission check controller. KEP update.

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1677

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-04T11:03:23Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9ddfc1b8698867b3ae78ffa9ba5767c83c61a75c |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6572ef43336a6b00080893da |

### Comment by [@trasc](https://github.com/trasc) — 2023-10-04T11:03:37Z

/assign @alculquicondor

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-05T07:06:44Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1178#" title="Author self-approved">trasc</a>*
**Once this PR has been reviewed and has the lgtm label**, please ask for approval from [alculquicondor](https://github.com/alculquicondor). For more information see [the Kubernetes Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["alculquicondor"]} -->

### Comment by [@trasc](https://github.com/trasc) — 2023-10-09T05:37:00Z

> Any implications of this update to https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order ?

No, the changes done by that only have effect before quota reservation.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T21:32:20Z

I think one important question is whether the preemption controller needs to be an admission check.

In some scenarios, the preemption check is running only once all the other admission checks have passed. This has the implication that now no other admission check can do the same, otherwise they will block each other.

So that's an argument against it being an admission check.

I'll give another pass to the proposal tomorrow and think about it more.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2024-03-28T11:49:35Z

@trasc: The following test **failed**, say `/retest` to rerun all failed tests or `/retest-required` to rerun all mandatory failed tests:

Test name | Commit | Details | Required | Rerun command
--- | --- | --- | --- | ---
pull-kueue-test-multikueue-e2e-main-1-29 | 9ddfc1b8698867b3ae78ffa9ba5767c83c61a75c | [link](https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1178/pull-kueue-test-multikueue-e2e-main-1-29/1773315236598648832) | true | `/test pull-kueue-test-multikueue-e2e-main-1-29`

[Full PR test history](https://prow.k8s.io/pr-history?org=kubernetes-sigs&repo=kueue&pr=1178). [Your PR dashboard](https://prow.k8s.io/pr?query=is%3Apr%20state%3Aopen%20author%3Atrasc). Please help us cut down on flakes by [linking to](https://git.k8s.io/community/contributors/devel/sig-testing/flaky-tests.md#filing-issues-for-flaky-tests) an [open issue](https://github.com/kubernetes-sigs/kueue/issues?q=is:issue+is:open) when you hit one in your PR.

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository. I understand the commands that are listed [here](https://go.k8s.io/bot-commands).
</details>
<!-- test report -->

### Comment by [@k8s-triage-robot](https://github.com/k8s-triage-robot) — 2025-05-15T10:43:34Z

The Kubernetes project currently lacks enough contributors to adequately respond to all PRs.

This bot triages PRs according to the following rules:
- After 90d of inactivity, `lifecycle/stale` is applied
- After 30d of inactivity since `lifecycle/stale` was applied, `lifecycle/rotten` is applied
- After 30d of inactivity since `lifecycle/rotten` was applied, the PR is closed

You can:
- Mark this PR as fresh with `/remove-lifecycle stale`
- Close this PR with `/close`
- Offer to help out with [Issue Triage][1]

Please send feedback to sig-contributor-experience at [kubernetes/community](https://github.com/kubernetes/community).

/lifecycle stale

[1]: https://www.kubernetes.dev/docs/guide/issue-triage/

### Comment by [@k8s-triage-robot](https://github.com/k8s-triage-robot) — 2025-06-14T11:31:17Z

The Kubernetes project currently lacks enough active contributors to adequately respond to all PRs.

This bot triages PRs according to the following rules:
- After 90d of inactivity, `lifecycle/stale` is applied
- After 30d of inactivity since `lifecycle/stale` was applied, `lifecycle/rotten` is applied
- After 30d of inactivity since `lifecycle/rotten` was applied, the PR is closed

You can:
- Mark this PR as fresh with `/remove-lifecycle rotten`
- Close this PR with `/close`
- Offer to help out with [Issue Triage][1]

Please send feedback to sig-contributor-experience at [kubernetes/community](https://github.com/kubernetes/community).

/lifecycle rotten

[1]: https://www.kubernetes.dev/docs/guide/issue-triage/

### Comment by [@k8s-triage-robot](https://github.com/k8s-triage-robot) — 2025-07-14T12:20:40Z

The Kubernetes project currently lacks enough active contributors to adequately respond to all issues and PRs.

This bot triages PRs according to the following rules:
- After 90d of inactivity, `lifecycle/stale` is applied
- After 30d of inactivity since `lifecycle/stale` was applied, `lifecycle/rotten` is applied
- After 30d of inactivity since `lifecycle/rotten` was applied, the PR is closed

You can:
- Reopen this PR with `/reopen`
- Mark this PR as fresh with `/remove-lifecycle rotten`
- Offer to help out with [Issue Triage][1]

Please send feedback to sig-contributor-experience at [kubernetes/community](https://github.com/kubernetes/community).

/close

[1]: https://www.kubernetes.dev/docs/guide/issue-triage/

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2025-07-14T12:20:46Z

@k8s-triage-robot: Closed this PR.

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1178#issuecomment-3069281681):

>The Kubernetes project currently lacks enough active contributors to adequately respond to all issues and PRs.
>
>This bot triages PRs according to the following rules:
>- After 90d of inactivity, `lifecycle/stale` is applied
>- After 30d of inactivity since `lifecycle/stale` was applied, `lifecycle/rotten` is applied
>- After 30d of inactivity since `lifecycle/rotten` was applied, the PR is closed
>
>You can:
>- Reopen this PR with `/reopen`
>- Mark this PR as fresh with `/remove-lifecycle rotten`
>- Offer to help out with [Issue Triage][1]
>
>Please send feedback to sig-contributor-experience at [kubernetes/community](https://github.com/kubernetes/community).
>
>/close
>
>[1]: https://www.kubernetes.dev/docs/guide/issue-triage/


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes-sigs/prow](https://github.com/kubernetes-sigs/prow/issues/new?title=Prow%20issue:) repository.
</details>

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-04T15:49:03Z

cc @tenzen-y @mwielgus

- **keps/993-two-phase-admission/README.md:259** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T14:41:27Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
```

  ```suggestion
  In this proposal, the time to evict the preemption candidates varies based on the preemptor state
  ```

- **keps/993-two-phase-admission/README.md:260** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T14:53:30Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  why an admission check instead of a Condition?
  
  Does it even have to be a new signal in the API? Could the preemption controller simply set the Admitted condition once all the preemptions have completed?

- **keps/993-two-phase-admission/README.md:269** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:06:38Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
```

  ```suggestion
  Since evaluating the current preemption state and the needs of a workload requires checking the state of all resource pool, the events are rate limited and the kueue's cache is used.
  ```
  Not sure if I'm interpreting the sentence correctly.
  which events is this referring to?

- **keps/993-two-phase-admission/README.md:272** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:07:58Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
```

  so we will get a snapshot that doesn't contain the workloads with pending preemptions, right?

- **keps/993-two-phase-admission/README.md:274** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:10:03Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
```

  Can you confirm if this is the implementation? and update the KEP accordingly.
  In the first pass we would use the previously calculated set of preemption candidates. If this is not enough, we would recalculate the preemption candidates from scratch.
  
  Nit: use a number list to show the steps of the algorithm.

- **keps/993-two-phase-admission/README.md:275** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:16:09Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
+If the updated list of candidates is empty, meaning that the preemption can no longer succeed, the preemption admission check is set as `Retry`, the workload will louse it's quota reservation and requeued.
```

  ```suggestion
  If the updated list of candidates is empty, meaning that the preemption can no longer succeed, the preemption admission check is set as `Retry`, the workload will lose it's quota reservation and requeued.
  ```
  
  This might be a good reason for have preemption as an AdmissionCheck, if having a permanent record for when preemptions were issued, or were not possible.
  
  But is such permanent record useful? Given the continuous nature of preemptions, events might be a better fit. We would like to say:
  1. preemptions were issued
  2. preemptions were not possible.
  
  (1) is completely continuous, so not a good fit for conditions or admission check, because lastTransitionTime wouldn't change if we issue more and more preemptions.
  (2) is more permanent: once issued, we would like to requeue. But this could be represented by setting the condition `QuotaReserved` to false with reason `UnsatisfiablePreemptions`.

- **keps/993-two-phase-admission/README.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:21:49Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  ```suggestion
  For every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
  ```
  
  Preemption can run before other checks have finished their transition to Ready. By the time every check has transitioned to Ready, we still need to check if we need more preemptions.
  So, in a sense, there is no concept of "preemption ready", we need to continue checking until the workload is fully admitted. And once it fits, and the checks are all "ready", we can directly transition to admitted.

- **keps/993-two-phase-admission/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T15:48:47Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  I have an alternative solution (please consider it, even though we might still go with your proposed solution):
  
  Once a workload is allowed to preempt, we put it back in the scheduling queue. When poped from the queue, this workload uses a different snapshot: one that doesn't have the workload with quota reserved but not admitted.
  Then we run the same algorithm we have, including preemption.
  
  - If the checks all passed, then we can admit the workload.
  - Otherwise, it goes back into the queue, probably into the "inadmissible" staging area and it can get poped back once all checks have passed.
  
  This has the advantage that a single component is "admitting" workloads and issuing preemptions, reducing the risk for race conditions. In your proposed approach, it is possible that the preemption controller determines that a workload is admissible, at the same time that the scheduler is admitting another workload that doesn't require preemptions or checks. And the workloads that require preemption are not prioritized by the same rules as the ones that go through the scheduling queue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T16:40:24Z

- **keps/993-two-phase-admission/README.md:260** by [@trasc](https://github.com/trasc) — 2023-10-04T16:40:24Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  A single condition is not sufficient, we have  "no preemption", "preemption in progress" , "preemption should start later" ... . Also the two step admission is set to "admit" when "all checks are ready" and "it has reservation" adding another "and" will make it hard to follow.  
  
  Regarding signals ,  With or without this the eviction process take a number of API signals, (issue eviction, wait for the evicted workload to stop)

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T17:19:18Z

- **keps/993-two-phase-admission/README.md:269** by [@trasc](https://github.com/trasc) — 2023-10-04T17:19:18Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
```

  The three listed above.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T17:19:24Z

- **keps/993-two-phase-admission/README.md:272** by [@trasc](https://github.com/trasc) — 2023-10-04T17:19:23Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
```

  The workloads that don't require to issue evictions at that particular point in time are removed from the snapshot.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T17:26:33Z

- **keps/993-two-phase-admission/README.md:275** by [@trasc](https://github.com/trasc) — 2023-10-04T17:26:33Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
+If the updated list of candidates is empty, meaning that the preemption can no longer succeed, the preemption admission check is set as `Retry`, the workload will louse it's quota reservation and requeued.
```

  I believe that k8s events are a better for this.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-04T18:28:00Z

- **keps/993-two-phase-admission/README.md:274** by [@trasc](https://github.com/trasc) — 2023-10-04T18:28:00Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
```

  We don't keep track of previous lists, we are always "generating" an new list of candidates (some workload that is not present in a  previous eviction candidates list  might have ended its execution making the current list of candidates shorter).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-04T18:45:24Z

- **keps/993-two-phase-admission/README.md:269** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T18:40:00Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
```

  ah, got it now, let me propose another rephrasing:
  ```suggestion
  The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
  ```
  
  Not sure if I'm leaving something out that could be relevant to the design (not sure how the events come into play).

- **keps/993-two-phase-admission/README.md:272** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-04T18:43:22Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
```

  Oh I see. So if there is a workload that requires preemptions and it's ready for preemption, it itself could be preempted (it's QuotaReserved would be cleared). Am I understanding correctly?
  
  An alternative implementation (maybe weaker) would be that we only take into consideration in the snapshot the workloads that are fully admitted (not just quota reserved).

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-05T05:57:01Z

- **keps/993-two-phase-admission/README.md:273** by [@trasc](https://github.com/trasc) — 2023-10-05T05:57:01Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  The quota reserved by a workload is considered  in use by the scheduler, if another workload gets is reservation after the fist one gets to "preemption ready" , that second workload will need to trigger it's own set of evictions and so on.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-05T06:21:27Z

- **keps/993-two-phase-admission/README.md:257** by [@trasc](https://github.com/trasc) — 2023-10-05T06:21:27Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > Once a workload is allowed to preempt, we put it back in the scheduling queue. 
  
  The problem is that there is no guarantee that the same assignment will be available.
  
  > This has the advantage that a single component is "admitting" workloads and issuing preemption, reducing the risk for race conditions. 
  
  The problem is that the triggers the scheduler should respond to and the type of tasks it needs to do will dramatically increase. (I did think of having the scheduler more involved when I first started working on two-step admission but keeping the scheduler involved in only managing quota reservation is the best option in my opinion).
  
  >  In your proposed approach, it is possible that the preemption controller determines that a workload is admissible, at the same time that the scheduler is admitting another workload that doesn't require preemptions or checks.
  
  Actually the quota reserved by all the preemption pending  workloads is "in use" from the schedulers point of view, If a new workload gets quota reservation from the scheduler it will be with no preemption only if it fists in the potential space that was "over evicted" by the first one or freed by some other workload.
  
  >  And the workloads that require preemption are not prioritized by the same rules as the ones that go through the scheduling queue.
  
  Indeed when it comes to the time when they are issuing eviction ,  and this one of the purpose of having preemption policies. However to get in the preemption pending state the rules are the same and during the preemption pending they are still  eligible to be evicted by a higher prio workload.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-05T07:06:45Z

- **keps/993-two-phase-admission/README.md:259** by [@trasc](https://github.com/trasc) — 2023-10-05T06:49:13Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
```

  done

- **keps/993-two-phase-admission/README.md:269** by [@trasc](https://github.com/trasc) — 2023-10-05T06:56:24Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
```

  done, (the throttling is an implementation detail )

- **keps/993-two-phase-admission/README.md:272** by [@trasc](https://github.com/trasc) — 2023-10-05T06:58:19Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
```

  > Oh I see. So if there is a workload that requires preemption and it's ready for preemption, it itself could be preempted (it's QuotaReserved would be cleared). Am I understanding correctly?
  
  Yes.
  
  > An alternative implementation (maybe weaker) would be that we only take into consideration in the snapshot the workloads that are fully admitted (not just quota reserved).
  
  Unfortunately the management will be harder, you will somehow check that your eviction candidate is not the candidate of some other preempting workload, who takes quota the released by "natural causes".

- **keps/993-two-phase-admission/README.md:273** by [@trasc](https://github.com/trasc) — 2023-10-05T07:03:21Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  (fore -> for done)

- **keps/993-two-phase-admission/README.md:275** by [@trasc](https://github.com/trasc) — 2023-10-05T07:04:16Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
+If the updated list of candidates is empty, meaning that the preemption can no longer succeed, the preemption admission check is set as `Retry`, the workload will louse it's quota reservation and requeued.
```

  (misspell done)

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T10:21:39Z

- **keps/993-two-phase-admission/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T10:10:09Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
```

  What is the difference between the admission check controller and the preemption controller? Are those the different components? Or one component?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-06T10:58:33Z

- **keps/993-two-phase-admission/README.md:269** by [@trasc](https://github.com/trasc) — 2023-10-06T10:58:33Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
```

  `Admission Check Controller` is a controller that manages a specific type of `AdmissionCheck`.
  
  `preemption controller` is short for  `Preemption Admission Check Controller`.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T11:01:23Z

- **keps/993-two-phase-admission/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T11:01:23Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
```

  I see. Let's modify this section name, "Preemption Admission Check Controller" to the proper one.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-06T13:23:36Z

- **keps/993-two-phase-admission/README.md:269** by [@trasc](https://github.com/trasc) — 2023-10-06T13:23:32Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
```

  The section is named "Preemption Admission Check Controller" L257 but sure we can change L263 as well.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T13:30:54Z

- **keps/993-two-phase-admission/README.md:269** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T13:30:53Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
```

  Probably, "Admission Check controller" is https://github.com/kubernetes-sigs/kueue/blob/main/pkg/controller/core/admissioncheck_controller.go, and "preemption controller" is https://github.com/kubernetes-sigs/kueue/blob/ce7e628a1f6bb1e375790edd6fc7df3f7ac68958/pkg/controller/admissionchecks/preemption/preemption.go, right?
  
  I wanted to clarify which sections do mention which controllers. 
  
  So `but sure we can change L263 as well.` sounds good to me.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-06T15:57:06Z

Any implications of this update to https://github.com/kubernetes-sigs/kueue/tree/main/keps/582-preempt-based-on-flavor-order ?

- **keps/993-two-phase-admission/README.md:260** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T15:53:54Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  I agree that a condition is not a good fit. But I don't think an admission check is, either.
  The concept of Preemption Ready is wrong. Once we have determined that all preemptions are done, another instance of the preemption controller can also say the same for another workload, leading to over subscription.
  In other words, there could only be one instance of a preemption controller and, once determined that the preemptions are done, setting the Admitted condition should be done in the same controller. Unless we also check the Preemption status? We might as well just set the Admitted condition.

- **keps/993-two-phase-admission/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T15:56:37Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > The problem is that there is no guarantee that the same assignment will be available.
  
  The same problem would be faced by a preemption controller. It needs to check that the assignment is still viable. We can have a mechanism in the scheduler logic to first try the assignment that was already calculated.
  
  > Actually the quota reserved by all the preemption pending workloads is "in use" from the schedulers point of view
  
  True.
  
  > However to get in the preemption pending state the rules are the same and during the preemption pending they are still eligible to be evicted by a higher prio workload.
  
  If two workloads have their admission checks completed, there is no guarantee that both will be able to fit after issuing preemptions. Therefore, we should prioritize them. Sure, the higher priority one could still preempt the lower priority one, but the disruption could be avoided if we had prioritized preemptions.
  
  It's almost starting to sound like we should have a a second "preemption scheduler", instead of a "preemption controller"

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T17:22:57Z

- **keps/993-two-phase-admission/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T17:22:57Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > "preemption scheduler"
  
  @alculquicondor Does this mean we should introduce a scheduler for preemption separate from an existing scheduler (admission scheduler?)?
  
  I'm concerned that two schedulers can work in asynchronous cooperation as expected.
  
  I was thinking about the separate scheduler when I reviewed @trasc 's implementation PR. 
  However, in the PR, I said we might want to somehow implement the preemption logic to the scheduling cycle instead of introducing a new scheduler.
  
  Because I thought that it might be hard to handle different schedulers well due to some conflicts, such as lower being preempted by higher, as you mentioned. Moreover, I'm wondering if the issue would be more complex when the borrowing semantics would be related to the scheduling logic.
  
  Honestly, I don't have a clear answer. However, I'm wondering if we should discuss the second scheduler for the preemption focused on the existing preemption logic, not including two-phase admission once away from two-phase admission with preemption.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-06T17:37:53Z

- **keps/993-two-phase-admission/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-06T17:37:52Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  I'm already thinking of leaving this change out of release-0.5, as it's highly complex and we don't have clear answers yet.
  
  That said, I think the idea of two schedulers isn't that bad, based on:
  
  > Actually the quota reserved by all the preemption pending workloads is "in use" from the schedulers point of view
  
  So they might not conflict with each other. Still, it might be valuable to revisit the idea that a single scheduler has the two modes: preempting and non-preempting.
  
  Let's give a thought to all these ideas and come back to it in a couple of weeks.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T17:50:03Z

- **keps/993-two-phase-admission/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T17:50:03Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > That said, I think the idea of two schedulers isn't that bad, based on:
  
  I don't have strong objections regarding two schedulers. I just wanted to say that I'm wondering if we shouldn't move this forward in a hurry.
  
  > I'm already thinking of leaving this change out of release-0.5, as it's highly complex and we don't have clear answers yet.
  
  I have the completely same opinion.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-06T18:07:20Z

- **keps/993-two-phase-admission/README.md:257** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-06T18:07:20Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  This is just an idea, as a byproduct, If we introduce a preemption scheduler separate from an existing scheduler, it might make providing [pluggable queue policies](https://github.com/kubernetes-sigs/kueue/issues/10) much easier since we might separate preemption logic from the flavorassigner.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-09T05:46:15Z

- **keps/993-two-phase-admission/README.md:260** by [@trasc](https://github.com/trasc) — 2023-10-09T05:46:15Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  > Once we have determined that all preemptions are done, another instance of the preemption controller can also say the same for another workload, leading to over subscription.
  
  I don't follow.  
  
  > In other words, there could only be one instance of a preemption controller and, once determined that the preemptions are done
  
  There is only one.
  
  > setting the Admitted condition should be done in the same controller
  
  It's done in the workload controller , it need to check all the Admission Checks.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-09T06:12:31Z

- **keps/993-two-phase-admission/README.md:257** by [@trasc](https://github.com/trasc) — 2023-10-09T06:12:31Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > If two workloads have their admission checks completed, there is no guarantee that both will be able to fit after issuing preemptions. Therefore, we should prioritize them.
  
  The priority is taken into account when creating the updated list of eviction candidates. 
  
  Say you have a queue with capacity 5cpu and :
  wl0 (prio 0,  3cpu, admitted )
  wl1 (prio 1, 3cpu, quota reserved, ready for preemption)
  wl2 (prio 2, 3cpu, quota reserved, ready for preemption)
  During the preemption check the preemption check controller will try to:
  
  - Get the list of candidates for wl1 while the queue has wl0 and wl2 using quota, since wl2 has higher priority the list will be empty, therefore the preemption failed for wl1. (check state `false`)
  - Get the list of candidates for wl2 while the queue has wl0 and wl1 using quota, the list will contain (wl1 and wl2) they will be marked for eviction.
   
  (the order in which the steps above are done is irrelevant)
  
  I a future run (after the eviction are done) wl2 will fit. (check state `true`)
  
  > I'm already thinking of leaving this change out of release-0.5, as it's highly complex and we don't have clear answers yet.
  
  We have a working implemetation.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T21:12:34Z

- **keps/993-two-phase-admission/README.md:275** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T21:12:33Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
+If eviction of other workload is still needed, an updated list candidates is created and eviction is issued for all of them.
+If the updated list of candidates is empty, meaning that the preemption can no longer succeed, the preemption admission check is set as `Retry`, the workload will louse it's quota reservation and requeued.
```

  sounds good, then I'll consider that a "record" alone is not sufficient to decide to implement preemptions as an AdmissionCheck.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T21:18:26Z

- **keps/993-two-phase-admission/README.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T21:18:26Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  > The quota reserved by a workload is considered in use by the scheduler, if another workload gets is reservation after the fist one gets to "preemption ready" , that second workload will need to trigger it's own set of evictions and so on.
  
  Yes, that I understand.
  
  Let me state my thinking again. There are scenarios where preemption needs to run before other admission checks have completed, because other AdmissionChecks can request preemptions.
  In that case, we will issue preemptions, but even when completed, we might still need preemptions later on, when all the AdmissionChecks actually complete.
  
  That's why I say that there might not be a concept of "preemptions are ready".

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-11-29T21:30:03Z

- **keps/993-two-phase-admission/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2023-11-29T21:30:03Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  > (the order in which the steps above are done is irrelevant)
  
  After reviewing your argument, I agree with this assessment.
  
  > We have a working implemetation.
  
  The problem is that we are touching currently stable functionality in ways that are hard to predict the outcomes. We might need to evaluate where our coverage is weak, so we can add it before changing the logic. We can't merge such a big PR lightly.
  
  But now we are at a good point in the release cycle in which we can proceed with such an overtake. I still need a little more time to review.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-05T20:27:21Z

- **keps/993-two-phase-admission/README.md:259** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:14:16Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
```

  ```suggestion
  In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
  ```

- **keps/993-two-phase-admission/README.md:257** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:16:51Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
```

  We can go ahead with a separate controller/reconciler.

- **keps/993-two-phase-admission/README.md:260** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:17:49Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  > There is only one.
  
  Please mention it in the design, given that most reconcilers have more than one worker.

- **keps/993-two-phase-admission/README.md:274** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:18:58Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
```

  ```suggestion
  - `preemtingLater` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
  ```

- **keps/993-two-phase-admission/README.md:284** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:23:29Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
```

  Please clarify which ones are candidates? Is it only workloads that are fully Admitted? I don't think you want to consider ones that only have ResourceQuota.

- **keps/993-two-phase-admission/README.md:287** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:24:33Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
```

  At this point? Shouldn't we wait for the next reconcile, given that jobs can take time to terminate?

- **keps/993-two-phase-admission/README.md:289** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-05T20:25:00Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Ready`
```

  Is it Ready?
  What happens to the workload if we cannot preempt anything to accommodate it?
  
  Ideally, it should go back to the queue, with the possibility to get it assigned under a new flavor.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-06T07:02:49Z

- **keps/993-two-phase-admission/README.md:259** by [@trasc](https://github.com/trasc) — 2023-12-06T06:32:52Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
```

  done

- **keps/993-two-phase-admission/README.md:260** by [@trasc](https://github.com/trasc) — 2023-12-06T06:35:05Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
```

  done

- **keps/993-two-phase-admission/README.md:274** by [@trasc](https://github.com/trasc) — 2023-12-06T06:36:57Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
```

  done

- **keps/993-two-phase-admission/README.md:284** by [@trasc](https://github.com/trasc) — 2023-12-06T06:48:08Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
```

  The candidates are picked from the list of workloads that hold a quota reservation, just because maybe a admission check of a workload is transitioning slowly to ready dose not mean that it should be able to hold its quota against a higher priority workload.

- **keps/993-two-phase-admission/README.md:287** by [@trasc](https://github.com/trasc) — 2023-12-06T06:58:09Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
```

  Not really, we add it in the snapshot so lower priority workloads will see the quota reservation and continue to check the lower priority workloads, some of which, if not waiting for the same resources might already fit.
  
  For the current workload we don't do anything else, when the evicted jobs terminate,  the preemption admission check controller will run again and it will be viewed  as  fitting.

- **keps/993-two-phase-admission/README.md:289** by [@trasc](https://github.com/trasc) — 2023-12-06T07:01:16Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Ready`
```

  It's "Retry", updated.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-06T07:13:24Z

- **keps/993-two-phase-admission/README.md:273** by [@trasc](https://github.com/trasc) — 2023-12-06T07:13:24Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  The preemption is finished when the workload can fit without the need of any additional evictions. Pass this point its  quota is locked and can start execution, or wait more.  The quote remains locked until this workload finishes or gets evicted.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-06T17:18:57Z

- **keps/993-two-phase-admission/README.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-06T17:18:57Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  That makes me think that the existing `QuotaReserved` condition will have different semantics. And the idea of `QuotaReserved` could actually be used to signal that preemption is "ready".
  
  But maybe that's a bit of over-engineering.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-07T06:39:12Z

- **keps/993-two-phase-admission/README.md:273** by [@trasc](https://github.com/trasc) — 2023-12-07T06:39:12Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  ... we could a yet another Quota specific condition like `QuotaLocked`, to only be set when all the evictions are done, then change the Admission logic from `QuoataReserved && AdmissionChecks==Ready` to `QuoataLocked && AdmissionChecks==Ready`.
  
  The preemption controller can then manage workloads that have QuoataReserved but not QuoataLocked.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-07T18:00:27Z

- **keps/993-two-phase-admission/README.md:273** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-07T18:00:27Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  Would QuotaReserved be equivalent to "preemption ready" then?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-07T18:07:41Z

- **keps/993-two-phase-admission/README.md:274** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-07T18:01:04Z
```diff
@@ -271,7 +271,7 @@ The preemption controller uses the kueue cache, since it needs to check the stat
 At every run the controller will get the list of workloads pending preemption.
 
 The workloads pending preemption are divided into:
-- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
```

  Later?

- **keps/993-two-phase-admission/README.md:287** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-07T18:04:07Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
```

  Just to be clear, you mean the snapshot, not the cache, correct?

- **keps/993-two-phase-admission/README.md:291** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-07T18:07:37Z
```diff
@@ -253,6 +254,42 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
+The scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a single instance of a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Retry`, the quota reservation will be lost and the workload placed in the queue waiting for a new QuotaReservation.
+
+**NOTE** The list of candidates is picked out from the list of workloads holding a QuotaReservation, regardless if they are fully Admitted or not.
```

  Is QuotaReservation enough? Consider jobs waiting for ProvReq to be fulfilled could be waiting for hours. There is no point in preempting them yet.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-07T18:08:32Z

- **keps/993-two-phase-admission/README.md:291** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-07T18:08:32Z
```diff
@@ -253,6 +254,42 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
+The scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a single instance of a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Retry`, the quota reservation will be lost and the workload placed in the queue waiting for a new QuotaReservation.
+
+**NOTE** The list of candidates is picked out from the list of workloads holding a QuotaReservation, regardless if they are fully Admitted or not.
```

  cc @mwielgus

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T07:06:22Z

- **keps/993-two-phase-admission/README.md:273** by [@trasc](https://github.com/trasc) — 2023-12-08T07:06:22Z
```diff
@@ -253,6 +254,27 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+Because now, the time when the eviction of the preemption candidate should vary based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+Since evaluating the current preemption state and the needs of a workload requires checking the sate of all resource pool the events are rate limited the kueue's cache is used.
+
+At every run the controller will get the list of workloads pending preemption.
+Since for some of these workloads is not necessary to issue eviction at that given point (eg. Having a pending check that uses AfterCheckPassedOrOnDemand policy) their quota reservation will be ignored.
+Fore every other preemption pending workloads, it will check if it can fit without evicting other workloads, case in which the preemption admission check condition will be set to `Ready`.
```

  The other way around, will have less impact on the code, The scheduler creates the admission struct and sets the QuotaReserved, the preemption controller will set the new condition when no evictions are needed. The workload controller will set the Admitted condition when the new condition is true and all Admission checks are ready. (sure we can add a fast path in the scheduler so if at reservation time the wl fits without the need of evictions set both conditions)

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T07:06:42Z

- **keps/993-two-phase-admission/README.md:287** by [@trasc](https://github.com/trasc) — 2023-12-08T07:06:42Z
```diff
@@ -253,6 +254,40 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state
+the scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
```

  Shapshot

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T07:17:23Z

- **keps/993-two-phase-admission/README.md:291** by [@trasc](https://github.com/trasc) — 2023-12-08T07:17:23Z
```diff
@@ -253,6 +254,42 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
+The scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a single instance of a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Retry`, the quota reservation will be lost and the workload placed in the queue waiting for a new QuotaReservation.
+
+**NOTE** The list of candidates is picked out from the list of workloads holding a QuotaReservation, regardless if they are fully Admitted or not.
```

  The ProvRequest Admission check can be configured with `AfterCheckPassedOrOnDemand` policy, the evictions will be done only when the provisioning is finished (and only if still needed).
  
  In the future , if we can know that the provisioning request has no chance to succeed for long period of times, the ProvRecAC can set its state to Retry this will fully release the quota.After that time the state can be set to Pending to put wl back in the queue.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-08T10:26:38Z

- **keps/993-two-phase-admission/README.md:274** by [@trasc](https://github.com/trasc) — 2023-12-08T10:26:38Z
```diff
@@ -271,7 +271,7 @@ The preemption controller uses the kueue cache, since it needs to check the stat
 At every run the controller will get the list of workloads pending preemption.
 
 The workloads pending preemption are divided into:
-- `preemtingLetaer` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-12-08T18:12:58Z

- **keps/993-two-phase-admission/README.md:291** by [@alculquicondor](https://github.com/alculquicondor) — 2023-12-08T18:12:58Z
```diff
@@ -253,6 +254,42 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
+The scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a single instance of a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Retry`, the quota reservation will be lost and the workload placed in the queue waiting for a new QuotaReservation.
+
+**NOTE** The list of candidates is picked out from the list of workloads holding a QuotaReservation, regardless if they are fully Admitted or not.
```

  My question is more about the candidates for eviction. Obviously, fully Admitted workloads are candidates for eviction.
  Other than that, I think it should be only the ones that have checks ready or are requesting preemption themselves.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-12-11T08:37:24Z

- **keps/993-two-phase-admission/README.md:291** by [@trasc](https://github.com/trasc) — 2023-12-11T08:37:23Z
```diff
@@ -253,6 +254,42 @@ The controller implementing a particular check should:
 * After approving the workload, keep an eye on the check if it starts failing,
   fail the check and cause the workload to move back to the suspended state.
 
+### Preemption Admission Check Controller
+
+In this proposal, the time to evict the preemption candidates varies based on the preemptor state.
+The scheduler will not issue the eviction during it's process instead it will set a `Pending` admission check
+that is manged by a single instance of a new built-in admission check controller.
+
+The **Preemption Admission Check Controller** will:
+
+- Watch for a change in state of the workloads pending preemption.
+- Watch for workloads that are finishing execution and therefore releasing quota.
+- Watch for AdmissionCheck changes, since the preemption policy can change.
+
+The preemption controller uses the kueue cache, since it needs to check the state of workloads admitted to the ClusterQueues.
+
+At every run the controller will get the list of workloads pending preemption.
+
+The workloads pending preemption are divided into:
+- `preemtingLeter` - Workloads having at least one check that uses AfterCheckPassedOrOnDemand policy with the state `pending`.
+- `preemtingNow` - Workloads that expect to be able to issue evictions or potentially change their preemption state in the current cycle.
+
+Then:
+1. Remove all workloads from the snapshot.
+2. For every workload in `preemtingNow` , in the order of their priority:
+  - If it can fit without the need of evicting other workloads
+    - Set its admission check to `Ready`
+    - Add it to the snapshot
+  - If it cannot fit
+    - Get an updated list of eviction candidates
+      - If the updated list is not empty.
+        - Issue the eviction to the candidates.
+        - Add it to the snapshot
+      - If the updated list is empty, meaning the preemption cannot be done.
+        - Set its admission check to `Retry`, the quota reservation will be lost and the workload placed in the queue waiting for a new QuotaReservation.
+
+**NOTE** The list of candidates is picked out from the list of workloads holding a QuotaReservation, regardless if they are fully Admitted or not.
```

  That can  be a source of priority inversion. Say we have  wl1 (p=1, reserving, one check pending), wl2 (p=2, fully admited), wl3(p=3, needs to evict wl1 or wl2), We should not evict wl2 just because wl1 has some pending admission check.
