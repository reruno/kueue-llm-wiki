# PR #1180: Propagate podsetupdates to jobs

**Summary**: Propagate podsetupdates to jobs

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1180

**Last updated**: 2023-11-29T14:58:21Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@mimowo](https://github.com/mimowo)
- **Branch**: `propagate-labels` → `main`
- **Created**: 2023-10-04T15:01:06Z
- **Updated**: 2023-11-29T14:58:21Z
- **Merged**: 2023-10-13T12:40:08Z
- **Closed**: 2023-10-13T12:40:08Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mwielgus](https://github.com/mwielgus), [@trasc](https://github.com/trasc), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 23   **+1706 / -109**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:


#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Part of  #1145

#### Special notes for your reviewer:

For all fields we want to fail on conflict according to the KEP: https://github.com/kubernetes-sigs/kueue/tree/main/keps/1145-additional-labels.

Adjusted unit tests which tested update of nodeSelectors, because this scenario could not happen e2e.

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
A mechanism for AdmissionChecks to provide labels, annotations, tolerations and node selectors to the pod templates when starting a job
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-04T15:01:12Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-04T15:01:16Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 85bc270cb6e45cafaea1e8171d44192373d85295 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65293246bf1a9200086a5634 |

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-09T10:59:20Z

/assign @alculquicondor @mwielgus @trasc 
Ready for review

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:11:50Z

> Also, it looks like the PR description is outdated?

Which part of the description? I looks all accurate to me.

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-12T07:45:16Z

> > Also, it looks like the PR description is outdated?
> 
> Which part of the description? I looks all accurate to me.

Adjusted the description due to the drop of the nodeSelectorOverwrite

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-12T07:46:18Z

I think all comments are addressed.

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-13T12:33:21Z

LGTM label has been added.  <details>Git tree hash: 975543b57801ad9a50214e3490fafb8b5b8ef51a</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-13T12:33:23Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1180#" title="Author self-approved">mimowo</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1180#pullrequestreview-1676383463" title="Approved">tenzen-y</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:46:23Z

/release-note-edit
```release-note
A mechanism for AdmissionChecks to provide labels, annotations, tolerations and node selectors to the pod templates when starting a job
```

## Reviews

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-09T12:20:08Z

- **Makefile:109** by [@trasc](https://github.com/trasc) — 2023-10-09T11:12:16Z
```diff
@@ -106,7 +106,7 @@ help: ## Display this help.
 manifests: controller-gen ## Generate WebhookConfiguration, ClusterRole and CustomResourceDefinition objects.
 	$(CONTROLLER_GEN) \
 		rbac:roleName=manager-role output:rbac:artifacts:config=config/components/rbac\
-		crd output:crd:artifacts:config=config/components/crd/bases\
+		crd:generateEmbeddedObjectMeta=true output:crd:artifacts:config=config/components/crd/bases\
```

  Just curios, how is this relevant for this PR?

- **pkg/controller/jobframework/podsetinfo.go:92** by [@trasc](https://github.com/trasc) — 2023-10-09T11:22:15Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
+		Annotations:  meta.Annotations,
+		Labels:       meta.Labels,
+		NodeSelector: spec.NodeSelector,
+		Tolerations:  spec.Tolerations,
+	}); err != nil {
+		return err
+	}
+	meta.Annotations = info.Annotations
+	meta.Labels = info.Labels
+	spec.NodeSelector = info.NodeSelector
+	spec.NodeSelector = utilmaps.MergeKeepFirst(info.NodeSelectorOverwrite, spec.NodeSelector)
+	spec.Tolerations = info.Tolerations
+	return nil
+}
+
+// Update sets replica metadata and spec fields based on PodSetInfo.
+// It returns true if there is any change.
+func Update(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
+	if !maps.Equal(meta.Annotations, info.Annotations) ||
+		!maps.Equal(meta.Labels, info.Labels) ||
+		!maps.Equal(spec.NodeSelector, info.NodeSelector) ||
+		!slices.Equal(spec.Tolerations, info.Tolerations) {
+		meta.Annotations = maps.Clone(info.Annotations)
+		meta.Labels = maps.Clone(info.Labels)
+		spec.NodeSelector = maps.Clone(info.NodeSelector)
+		spec.Tolerations = slices.Clone(info.Tolerations)
+		return true
+	}
+	return false
+}
```

  ```suggestion
  // Update sets replica metadata and spec fields based on PodSetInfo.
  // It returns true if there is any change.
  func Update(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
          changed:=false
  	if !maps.Equal(meta.Annotations, info.Annotations) {
  	meta.Annotations = maps.Clone(info.Annotations)
  	changed = true
  	}
  	
  	if !maps.Equal(meta.Labels , info.Labels ) {
  	meta.Labels  = maps.Clone(info.Labels )
  	changed = true
  	}
  	.....
  
  	return changed
  }
  ```
  
  this way we avoid unnecessary clones when not all the fields need update

- **pkg/controller/jobframework/reconciler.go:649** by [@trasc](https://github.com/trasc) — 2023-10-09T11:26:19Z
```diff
@@ -643,26 +645,24 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
 // getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
 func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
```

  ```suggestion
  func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
  ```
  nit.

- **pkg/controller/jobs/job/job_controller_test.go:374** by [@trasc](https://github.com/trasc) — 2023-10-09T11:59:31Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
```

  can the queue name be added in the `baseJobWrapper` ? (it takes a bit of digging to see why is needed in `job` and not in `wantJob`)

- **pkg/controller/jobs/job/job_controller_test.go:420** by [@trasc](https://github.com/trasc) — 2023-10-09T12:03:46Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				PodLabel("ac-key", "ac-value").
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on labels": {
```

  ```suggestion
  		"when workload is admitted and PodSetUpdates conflict between admission checks on labels, the workload is finished with failure": {
  ```
  maybe?

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@trasc](https://github.com/trasc) — 2023-10-09T12:04:24Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				PodLabel("ac-key", "ac-value").
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on labels": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value1",
+								},
+							},
+						},
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check2",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value2",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value1",
+								},
+							},
+						},
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check2",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value2",
+								},
+							},
+						},
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "FailedToStart",
+						Message: `invalid admission check PodSetUpdate: conflict for labels, error: conflict for key=ac-key, value1=ac-value1, value2=ac-value2`,
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on annotations": {
```

  similar to L420

- **pkg/controller/jobframework/podsetinfo.go:79** by [@trasc](https://github.com/trasc) — 2023-10-09T12:09:37Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
+		Annotations:  meta.Annotations,
+		Labels:       meta.Labels,
+		NodeSelector: spec.NodeSelector,
+		Tolerations:  spec.Tolerations,
+	}); err != nil {
+		return err
+	}
+	meta.Annotations = info.Annotations
+	meta.Labels = info.Labels
+	spec.NodeSelector = info.NodeSelector
+	spec.NodeSelector = utilmaps.MergeKeepFirst(info.NodeSelectorOverwrite, spec.NodeSelector)
+	spec.Tolerations = info.Tolerations
+	return nil
+}
+
+// Update sets replica metadata and spec fields based on PodSetInfo.
+// It returns true if there is any change.
+func Update(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
```

  ```suggestion
  func Restore(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
  ```
  maybe it fits better

- **test/integration/controller/jobs/jobset/jobset_controller_test.go:407** by [@trasc](https://github.com/trasc) — 2023-10-09T12:17:28Z
```diff
@@ -284,6 +286,196 @@ var _ = ginkgo.Describe("JobSet controller", ginkgo.Ordered, ginkgo.ContinueOnFa
 			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+
+	ginkgo.When("the queue has admission checks", func() {
+		var (
+			clusterQueueAc *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			testFlavor     *kueue.ResourceFlavor
+			jobLookupKey   *types.NamespacedName
+			wlLookupKey    *types.NamespacedName
+			admissionCheck *kueue.AdmissionCheck
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueueAc = testing.MakeClusterQueue("prod-cq-with-checks").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("test-flavor").Resource(corev1.ResourceCPU, "5").Obj(),
+				).AdmissionChecks("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueueAc)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueueAc.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+			testFlavor = testing.MakeResourceFlavor("test-flavor").Label(instanceKey, "test-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, testFlavor)).Should(gomega.Succeed())
+			admissionCheck = testing.MakeAdmissionCheck("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, admissionCheck)).To(gomega.Succeed())
+
+			jobLookupKey = &types.NamespacedName{Name: jobSetName, Namespace: ns.Name}
+			wlLookupKey = &types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSetName), Namespace: ns.Name}
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAdmissionCheck(ctx, k8sClient, admissionCheck)).To(gomega.Succeed())
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, testFlavor, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueueAc, true)
+		})
+
+		ginkgo.It("labels and annotations should be propagated from admission check to job", func() {
+			createdJob := &jobsetapi.JobSet{}
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("creating the job", func() {
+				job := testingjobset.MakeJobSet(jobSetName, ns.Name).ReplicatedJobs(
+					testingjobset.ReplicatedJobRequirements{
+						Name:        "replicated-job-1",
+						Replicas:    1,
+						Parallelism: 1,
+						Completions: 1,
+					}, testingjobset.ReplicatedJobRequirements{
+						Name:        "replicated-job-2",
+						Replicas:    3,
+						Parallelism: 1,
+						Completions: 1,
+					},
+				).
+					Queue("queue").
+					Request("replicated-job-1", corev1.ResourceCPU, "1").
+					Request("replicated-job-2", corev1.ResourceCPU, "1").
+					Obj()
+				job.Spec.ReplicatedJobs[0].Template.Spec.Template.Annotations = map[string]string{
+					"old-ann-key": "old-ann-value",
+				}
+				job.Spec.ReplicatedJobs[0].Template.Spec.Template.Labels = map[string]string{
+					"old-label-key": "old-label-value",
+				}
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("fetch the job and verify it is suspended as the checks are not ready", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, *jobLookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+			})
+
+			ginkgo.By("checking the workload is created", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, *wlLookupKey, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("add labels & annotations to the admission check in PodSetUpdates", func() {
+				gomega.Eventually(func() error {
+					var newWL kueue.Workload
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(createdWorkload), &newWL)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&newWL.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "replicated-job-1",
+								Annotations: map[string]string{
+									"ann1": "ann-value1",
+								},
+								Labels: map[string]string{
+									"label1": "label-value1",
+								},
+								NodeSelector: map[string]string{
+									"selector1": "selector-value1",
+								},
+								Tolerations: []corev1.Toleration{
+									{
+										Key:      "selector1",
+										Value:    "selector-value1",
+										Operator: corev1.TolerationOpEqual,
+										Effect:   corev1.TaintEffectNoSchedule,
+									},
+								},
+							},
+							{
+								Name: "replicated-job-2",
+							},
```

  can we add some updates for this pod-set also?

- **test/integration/controller/jobs/mpijob/mpijob_controller_test.go:361** by [@trasc](https://github.com/trasc) — 2023-10-09T12:19:04Z
```diff
@@ -285,6 +287,183 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+
+	ginkgo.When("the queue has admission checks", func() {
+		var (
+			clusterQueueAc *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			testFlavor     *kueue.ResourceFlavor
+			jobLookupKey   *types.NamespacedName
+			wlLookupKey    *types.NamespacedName
+			admissionCheck *kueue.AdmissionCheck
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueueAc = testing.MakeClusterQueue("prod-cq-with-checks").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("test-flavor").Resource(corev1.ResourceCPU, "5").Obj(),
+				).AdmissionChecks("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueueAc)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueueAc.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+			testFlavor = testing.MakeResourceFlavor("test-flavor").Label(instanceKey, "test-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, testFlavor)).Should(gomega.Succeed())
+			admissionCheck = testing.MakeAdmissionCheck("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, admissionCheck)).To(gomega.Succeed())
+
+			jobLookupKey = &types.NamespacedName{Name: jobName, Namespace: ns.Name}
+			wlLookupKey = &types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(jobName), Namespace: ns.Name}
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAdmissionCheck(ctx, k8sClient, admissionCheck)).To(gomega.Succeed())
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, testFlavor, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueueAc, true)
+		})
+
+		ginkgo.It("labels and annotations should be propagated from admission check to job", func() {
+			createdJob := &kubeflow.MPIJob{}
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("creating the job with pod labels & annotations", func() {
+				job := testingmpijob.MakeMPIJob(jobName, ns.Name).
+					Queue(localQueue.Name).
+					PodAnnotation(kubeflow.MPIReplicaTypeWorker, "old-ann-key", "old-ann-value").
+					PodLabel(kubeflow.MPIReplicaTypeWorker, "old-label-key", "old-label-value").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("fetch the job and verify it is suspended as the checks are not ready", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, *jobLookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+			})
+
+			ginkgo.By("fetch the created workload", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, *wlLookupKey, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("add labels & annotations to the admission check", func() {
+				gomega.Eventually(func() error {
+					var newWL kueue.Workload
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(createdWorkload), &newWL)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&newWL.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "launcher",
```

  same, maybe add some updates for this podset

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:48:25Z

- **Makefile:109** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:48:24Z
```diff
@@ -106,7 +106,7 @@ help: ## Display this help.
 manifests: controller-gen ## Generate WebhookConfiguration, ClusterRole and CustomResourceDefinition objects.
 	$(CONTROLLER_GEN) \
 		rbac:roleName=manager-role output:rbac:artifacts:config=config/components/rbac\
-		crd output:crd:artifacts:config=config/components/crd/bases\
+		crd:generateEmbeddedObjectMeta=true output:crd:artifacts:config=config/components/crd/bases\
```

  This is needed to generate the `metadata.labels` and `metadata.annotations` in the CRD schema for the template (`PodTemplateSpec) in PodSet. Without it, the fields are dropped at the client side. 
  
  The template fields are used to keep the original labels, annotations, nodeSelector, and tolerations. 
  
  `generateEmbeddedObjectMeta` is also used for other projects such as:
  - [jobset](https://github.com/kubernetes-sigs/jobset/blob/68d1afb37c1b102d1f69c6710c5c42081c49181c/Makefile#L85)
  - [mpioperator](https://github.com/kubeflow/mpi-operator/blob/64db27bef2bc34bc1c9af3112a8452dcb2dd9bb2/Makefile#L46)
  - [training-operator](https://github.com/kubeflow/training-operator/blob/a4c0cec561a4bfe478720f1a102f305ed656071b/Makefile#L4)
  
  Alternatively, I could keep labels and annotation directly on newly declared fields in `PodSet`, but it seems preferred to avoid new fields.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:53:34Z

- **pkg/controller/jobframework/podsetinfo.go:92** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:53:34Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
+		Annotations:  meta.Annotations,
+		Labels:       meta.Labels,
+		NodeSelector: spec.NodeSelector,
+		Tolerations:  spec.Tolerations,
+	}); err != nil {
+		return err
+	}
+	meta.Annotations = info.Annotations
+	meta.Labels = info.Labels
+	spec.NodeSelector = info.NodeSelector
+	spec.NodeSelector = utilmaps.MergeKeepFirst(info.NodeSelectorOverwrite, spec.NodeSelector)
+	spec.Tolerations = info.Tolerations
+	return nil
+}
+
+// Update sets replica metadata and spec fields based on PodSetInfo.
+// It returns true if there is any change.
+func Update(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
+	if !maps.Equal(meta.Annotations, info.Annotations) ||
+		!maps.Equal(meta.Labels, info.Labels) ||
+		!maps.Equal(spec.NodeSelector, info.NodeSelector) ||
+		!slices.Equal(spec.Tolerations, info.Tolerations) {
+		meta.Annotations = maps.Clone(info.Annotations)
+		meta.Labels = maps.Clone(info.Labels)
+		spec.NodeSelector = maps.Clone(info.NodeSelector)
+		spec.Tolerations = slices.Clone(info.Tolerations)
+		return true
+	}
+	return false
+}
```

  And avoids a spagetti condition, done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:55:47Z

- **pkg/controller/jobframework/reconciler.go:649** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:55:47Z
```diff
@@ -643,26 +645,24 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
 // getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
 func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
```

  Done + comment update

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:55:57Z

- **pkg/controller/jobframework/podsetinfo.go:79** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:55:57Z
```diff
@@ -0,0 +1,91 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
+		Annotations:  meta.Annotations,
+		Labels:       meta.Labels,
+		NodeSelector: spec.NodeSelector,
+		Tolerations:  spec.Tolerations,
+	}); err != nil {
+		return err
+	}
+	meta.Annotations = info.Annotations
+	meta.Labels = info.Labels
+	spec.NodeSelector = info.NodeSelector
+	spec.NodeSelector = utilmaps.MergeKeepFirst(info.NodeSelectorOverwrite, spec.NodeSelector)
+	spec.Tolerations = info.Tolerations
+	return nil
+}
+
+// Update sets replica metadata and spec fields based on PodSetInfo.
+// It returns true if there is any change.
+func Update(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) bool {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:57:13Z

- **pkg/controller/jobs/job/job_controller_test.go:420** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:57:13Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				PodLabel("ac-key", "ac-value").
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on labels": {
```

  makes sense

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T12:59:30Z

- **pkg/controller/jobs/job/job_controller_test.go:497** by [@mimowo](https://github.com/mimowo) — 2023-10-09T12:59:30Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(false).
+				PodLabel("ac-key", "ac-value").
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on labels": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
+				Obj(),
+			wantJob: *baseJobWrapper.Clone().
+				Suspend(true).
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value1",
+								},
+							},
+						},
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check2",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value2",
+								},
+							},
+						},
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("a", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					ReserveQuota(utiltesting.MakeAdmission("cq").AssignmentPodCount(10).Obj()).
+					Admitted(true).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check1",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value1",
+								},
+							},
+						},
+					}).
+					AdmissionCheck(kueue.AdmissionCheckState{
+						Name:  "check2",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "main",
+								Labels: map[string]string{
+									"ac-key": "ac-value2",
+								},
+							},
+						},
+					}).
+					Condition(metav1.Condition{
+						Type:    kueue.WorkloadFinished,
+						Status:  metav1.ConditionTrue,
+						Reason:  "FailedToStart",
+						Message: `invalid admission check PodSetUpdate: conflict for labels, error: conflict for key=ac-key, value1=ac-value1, value2=ac-value2`,
+					}).
+					Obj(),
+			},
+		},
+		"when workload is admitted the PodSetUpdates conflict between admission checks on annotations": {
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T13:07:23Z

- **pkg/controller/jobs/job/job_controller_test.go:374** by [@mimowo](https://github.com/mimowo) — 2023-10-09T13:07:22Z
```diff
@@ -368,6 +369,444 @@ func TestReconciler(t *testing.T) {
 		wantWorkloads     []kueue.Workload
 		wantErr           error
 	}{
+		"when workload is admitted the PodSetUpdates are propagated to job": {
+			job: *baseJobWrapper.Clone().
+				Queue("foo").
```

  Yeah, agree. It triggers small changes in preexisting tests, but this is fine. Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T13:22:52Z

- **test/integration/controller/jobs/jobset/jobset_controller_test.go:407** by [@mimowo](https://github.com/mimowo) — 2023-10-09T13:22:51Z
```diff
@@ -284,6 +286,196 @@ var _ = ginkgo.Describe("JobSet controller", ginkgo.Ordered, ginkgo.ContinueOnFa
 			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+
+	ginkgo.When("the queue has admission checks", func() {
+		var (
+			clusterQueueAc *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			testFlavor     *kueue.ResourceFlavor
+			jobLookupKey   *types.NamespacedName
+			wlLookupKey    *types.NamespacedName
+			admissionCheck *kueue.AdmissionCheck
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueueAc = testing.MakeClusterQueue("prod-cq-with-checks").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("test-flavor").Resource(corev1.ResourceCPU, "5").Obj(),
+				).AdmissionChecks("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueueAc)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueueAc.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+			testFlavor = testing.MakeResourceFlavor("test-flavor").Label(instanceKey, "test-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, testFlavor)).Should(gomega.Succeed())
+			admissionCheck = testing.MakeAdmissionCheck("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, admissionCheck)).To(gomega.Succeed())
+
+			jobLookupKey = &types.NamespacedName{Name: jobSetName, Namespace: ns.Name}
+			wlLookupKey = &types.NamespacedName{Name: workloadjobset.GetWorkloadNameForJobSet(jobSetName), Namespace: ns.Name}
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAdmissionCheck(ctx, k8sClient, admissionCheck)).To(gomega.Succeed())
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, testFlavor, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueueAc, true)
+		})
+
+		ginkgo.It("labels and annotations should be propagated from admission check to job", func() {
+			createdJob := &jobsetapi.JobSet{}
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("creating the job", func() {
+				job := testingjobset.MakeJobSet(jobSetName, ns.Name).ReplicatedJobs(
+					testingjobset.ReplicatedJobRequirements{
+						Name:        "replicated-job-1",
+						Replicas:    1,
+						Parallelism: 1,
+						Completions: 1,
+					}, testingjobset.ReplicatedJobRequirements{
+						Name:        "replicated-job-2",
+						Replicas:    3,
+						Parallelism: 1,
+						Completions: 1,
+					},
+				).
+					Queue("queue").
+					Request("replicated-job-1", corev1.ResourceCPU, "1").
+					Request("replicated-job-2", corev1.ResourceCPU, "1").
+					Obj()
+				job.Spec.ReplicatedJobs[0].Template.Spec.Template.Annotations = map[string]string{
+					"old-ann-key": "old-ann-value",
+				}
+				job.Spec.ReplicatedJobs[0].Template.Spec.Template.Labels = map[string]string{
+					"old-label-key": "old-label-value",
+				}
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("fetch the job and verify it is suspended as the checks are not ready", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, *jobLookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+			})
+
+			ginkgo.By("checking the workload is created", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, *wlLookupKey, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("add labels & annotations to the admission check in PodSetUpdates", func() {
+				gomega.Eventually(func() error {
+					var newWL kueue.Workload
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(createdWorkload), &newWL)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&newWL.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "replicated-job-1",
+								Annotations: map[string]string{
+									"ann1": "ann-value1",
+								},
+								Labels: map[string]string{
+									"label1": "label-value1",
+								},
+								NodeSelector: map[string]string{
+									"selector1": "selector-value1",
+								},
+								Tolerations: []corev1.Toleration{
+									{
+										Key:      "selector1",
+										Value:    "selector-value1",
+										Operator: corev1.TolerationOpEqual,
+										Effect:   corev1.TaintEffectNoSchedule,
+									},
+								},
+							},
+							{
+								Name: "replicated-job-2",
+							},
```

  sure, added some more

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-09T13:23:03Z

- **test/integration/controller/jobs/mpijob/mpijob_controller_test.go:361** by [@mimowo](https://github.com/mimowo) — 2023-10-09T13:23:02Z
```diff
@@ -285,6 +287,183 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 			return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
 	})
+
+	ginkgo.When("the queue has admission checks", func() {
+		var (
+			clusterQueueAc *kueue.ClusterQueue
+			localQueue     *kueue.LocalQueue
+			testFlavor     *kueue.ResourceFlavor
+			jobLookupKey   *types.NamespacedName
+			wlLookupKey    *types.NamespacedName
+			admissionCheck *kueue.AdmissionCheck
+		)
+
+		ginkgo.BeforeEach(func() {
+			clusterQueueAc = testing.MakeClusterQueue("prod-cq-with-checks").
+				ResourceGroup(
+					*testing.MakeFlavorQuotas("test-flavor").Resource(corev1.ResourceCPU, "5").Obj(),
+				).AdmissionChecks("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, clusterQueueAc)).Should(gomega.Succeed())
+			localQueue = testing.MakeLocalQueue("queue", ns.Name).ClusterQueue(clusterQueueAc.Name).Obj()
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).To(gomega.Succeed())
+			testFlavor = testing.MakeResourceFlavor("test-flavor").Label(instanceKey, "test-flavor").Obj()
+			gomega.Expect(k8sClient.Create(ctx, testFlavor)).Should(gomega.Succeed())
+			admissionCheck = testing.MakeAdmissionCheck("check").Obj()
+			gomega.Expect(k8sClient.Create(ctx, admissionCheck)).To(gomega.Succeed())
+
+			jobLookupKey = &types.NamespacedName{Name: jobName, Namespace: ns.Name}
+			wlLookupKey = &types.NamespacedName{Name: workloadmpijob.GetWorkloadNameForMPIJob(jobName), Namespace: ns.Name}
+		})
+
+		ginkgo.AfterEach(func() {
+			gomega.Expect(util.DeleteAdmissionCheck(ctx, k8sClient, admissionCheck)).To(gomega.Succeed())
+			util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, testFlavor, true)
+			gomega.Expect(util.DeleteNamespace(ctx, k8sClient, ns)).To(gomega.Succeed())
+			util.ExpectClusterQueueToBeDeleted(ctx, k8sClient, clusterQueueAc, true)
+		})
+
+		ginkgo.It("labels and annotations should be propagated from admission check to job", func() {
+			createdJob := &kubeflow.MPIJob{}
+			createdWorkload := &kueue.Workload{}
+
+			ginkgo.By("creating the job with pod labels & annotations", func() {
+				job := testingmpijob.MakeMPIJob(jobName, ns.Name).
+					Queue(localQueue.Name).
+					PodAnnotation(kubeflow.MPIReplicaTypeWorker, "old-ann-key", "old-ann-value").
+					PodLabel(kubeflow.MPIReplicaTypeWorker, "old-label-key", "old-label-value").
+					Obj()
+				gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("fetch the job and verify it is suspended as the checks are not ready", func() {
+				gomega.Eventually(func() *bool {
+					gomega.Expect(k8sClient.Get(ctx, *jobLookupKey, createdJob)).Should(gomega.Succeed())
+					return createdJob.Spec.RunPolicy.Suspend
+				}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+			})
+
+			ginkgo.By("fetch the created workload", func() {
+				gomega.Eventually(func() error {
+					return k8sClient.Get(ctx, *wlLookupKey, createdWorkload)
+				}, util.Timeout, util.Interval).Should(gomega.Succeed())
+			})
+
+			ginkgo.By("add labels & annotations to the admission check", func() {
+				gomega.Eventually(func() error {
+					var newWL kueue.Workload
+					gomega.Expect(k8sClient.Get(ctx, client.ObjectKeyFromObject(createdWorkload), &newWL)).To(gomega.Succeed())
+					workload.SetAdmissionCheckState(&newWL.Status.AdmissionChecks, kueue.AdmissionCheckState{
+						Name:  "check",
+						State: kueue.CheckStateReady,
+						PodSetUpdates: []kueue.PodSetUpdate{
+							{
+								Name: "launcher",
```

  sure, done

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-10T10:56:12Z

- **Makefile:109** by [@trasc](https://github.com/trasc) — 2023-10-10T10:56:11Z
```diff
@@ -106,7 +106,7 @@ help: ## Display this help.
 manifests: controller-gen ## Generate WebhookConfiguration, ClusterRole and CustomResourceDefinition objects.
 	$(CONTROLLER_GEN) \
 		rbac:roleName=manager-role output:rbac:artifacts:config=config/components/rbac\
-		crd output:crd:artifacts:config=config/components/crd/bases\
+		crd:generateEmbeddedObjectMeta=true output:crd:artifacts:config=config/components/crd/bases\
```

  Thanks

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-10T11:03:15Z

LGTM but since there are more reviewers leave `/lgtm` for then future

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-11T15:55:39Z

Just one potential simplification.
Also, it looks like the PR description is outdated?

- **pkg/controller/jobframework/podsetinfo.go:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:31:09Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
```

  would this modify the incoming PodSetInfo passed to `Merge`? is that desired?

- **pkg/controller/jobframework/podsetinfo.go:46** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:31:39Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
```

  ```suggestion
  		_, exists := o.NodeSelectorOverwrite[k]
  		return exists
  ```

- **pkg/controller/jobframework/podsetinfo.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:35:03Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
```

  why are meta and spec passed separately?

- **pkg/controller/jobframework/podsetinfo.go:56** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:36:29Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
```

  does the order matter? should it be `PodSetInfo{...}.Merge(info)`?

- **pkg/controller/jobframework/reconciler.go:666** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:40:20Z
```diff
@@ -643,26 +645,25 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
 
 	for i, podSetFlavor := range w.Status.Admission.PodSetAssignments {
 		processedFlvs := sets.NewString()
-		nodeSelector := PodSetInfo{
-			Name:         podSetFlavor.Name,
-			NodeSelector: make(map[string]string),
-			Count:        ptr.Deref(podSetFlavor.Count, w.Spec.PodSets[i].Count),
+		podSetInfo := PodSetInfo{
+			Name:                  podSetFlavor.Name,
+			NodeSelector:          make(map[string]string),
+			NodeSelectorOverwrite: make(map[string]string),
+			Count:                 ptr.Deref(podSetFlavor.Count, w.Spec.PodSets[i].Count),
+			Labels:                make(map[string]string),
+			Annotations:           make(map[string]string),
+			Tolerations:           make([]corev1.Toleration, 0),
```

  since we use `append`, there is no need to instantiate this field.

- **pkg/controller/jobframework/reconciler.go:679** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:41:54Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  why not just `NodeSelector`?

- **pkg/controller/jobframework/reconciler.go:692** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:45:43Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
 			}
 			processedFlvs.Insert(flvName)
 		}
-
-		nodeSelectors[i] = nodeSelector
+		for _, admissionCheck := range w.Status.AdmissionChecks {
+			for _, podSetUpdate := range admissionCheck.PodSetUpdates {
+				if podSetUpdate.Name == podSetInfo.Name {
+					if err := podSetInfo.Merge(PodSetInfo{
+						Labels:       podSetUpdate.Labels,
+						Annotations:  podSetUpdate.Annotations,
+						Tolerations:  podSetUpdate.Tolerations,
+						NodeSelector: podSetUpdate.NodeSelector,
+					}); err != nil {
+						return nil, err
```

  `fmt.Errorf("in admission check %s: %w", admissionCheck.Name, err)`

- **pkg/controller/jobframework/reconciler.go:812** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:46:13Z
```diff
@@ -790,3 +807,7 @@ func resetMinCounts(in []kueue.PodSet) []kueue.PodSet {
 func BadPodSetsInfoLenError(want, got int) error {
 	return fmt.Errorf("%w: expecting %d podset, got %d", ErrInvalidPodsetInfo, got, want)
 }
+
+func BadPodSetsUpdateError(update string, err error) error {
+	return fmt.Errorf("%w: conflict for %v, error: %v", ErrInvalidPodSetUpdate, update, err)
```

  ```suggestion
  	return fmt.Errorf("%w: conflict for %v: %v", ErrInvalidPodSetUpdate, update, err)
  ```

- **pkg/util/maps/maps.go:65** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:51:27Z
```diff
@@ -61,6 +62,16 @@ func MergeKeepFirst[K comparable, V any, S ~map[K]V](a, b S) S {
 	return Merge(a, b, func(v, _ V) V { return v })
 }
 
+// HavaConflict checks if a and b have the same key, but different value
```

  ```suggestion
  // HaveConflict checks if a and b have the same key, but different value
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T15:53:23Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  why two tests?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:04:57Z

- **pkg/controller/jobframework/podsetinfo.go:55** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:04:57Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
```

  I could not pass them in PodSpecTemplate, because there is no PodSpecTemplate for pod integration. Alternatively, we can have a wrapping struct.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:07:22Z

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:07:22Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  This one integrates with scheduler, I thought it might be valuable, but I'm also ok dropping it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:15:38Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:15:38Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  `NodeSelectorOverwrite` is set based on admission, to preserve the behavior on master that the node selectors from admission overwrite what is in job nodeSelector (there is no error on conflict). I think we could simplify, and error on conflicts in that case, but it would mean changing the master behavior as a drive by. I prefer that to be done in a dedicated PR if we want. WDYT? FYI @mwielgus ?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:29:06Z

- **pkg/controller/jobframework/reconciler.go:692** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:29:06Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
 			}
 			processedFlvs.Insert(flvName)
 		}
-
-		nodeSelectors[i] = nodeSelector
+		for _, admissionCheck := range w.Status.AdmissionChecks {
+			for _, podSetUpdate := range admissionCheck.PodSetUpdates {
+				if podSetUpdate.Name == podSetInfo.Name {
+					if err := podSetInfo.Merge(PodSetInfo{
+						Labels:       podSetUpdate.Labels,
+						Annotations:  podSetUpdate.Annotations,
+						Tolerations:  podSetUpdate.Tolerations,
+						NodeSelector: podSetUpdate.NodeSelector,
+					}); err != nil {
+						return nil, err
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:30:04Z

- **pkg/controller/jobframework/reconciler.go:666** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:30:04Z
```diff
@@ -643,26 +645,25 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
 
 	for i, podSetFlavor := range w.Status.Admission.PodSetAssignments {
 		processedFlvs := sets.NewString()
-		nodeSelector := PodSetInfo{
-			Name:         podSetFlavor.Name,
-			NodeSelector: make(map[string]string),
-			Count:        ptr.Deref(podSetFlavor.Count, w.Spec.PodSets[i].Count),
+		podSetInfo := PodSetInfo{
+			Name:                  podSetFlavor.Name,
+			NodeSelector:          make(map[string]string),
+			NodeSelectorOverwrite: make(map[string]string),
+			Count:                 ptr.Deref(podSetFlavor.Count, w.Spec.PodSets[i].Count),
+			Labels:                make(map[string]string),
+			Annotations:           make(map[string]string),
+			Tolerations:           make([]corev1.Toleration, 0),
```

  Dropped

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-11T16:44:53Z

- **pkg/controller/jobframework/podsetinfo.go:55** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T16:44:53Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
```

  I see. Let's leave it like this for now.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:46:38Z

- **pkg/controller/jobframework/podsetinfo.go:44** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:46:38Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
```

  it was a side effect I was aware of, but it will be cleaner not to modify it. Updated.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-11T16:47:51Z

- **pkg/controller/jobframework/reconciler.go:679** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T16:47:51Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  There shouldn't be any conflicts, because the node selectors coming from the job are used to filter out flavors. https://kueue.sigs.k8s.io/docs/concepts/resource_flavor/#resourceflavor-labels

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:48:05Z

- **pkg/controller/jobframework/podsetinfo.go:56** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:48:04Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
```

  Yes, the order matters currently, because I modify the podSetInfo on which the function is called. This could be changed to do not modify the objects, but return a new one. Let me know if you think it will be better. We could also have it a small follow up.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-11T16:48:41Z

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-11T16:48:41Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  it should be ok to drop. We might be better of doing an E2E using ProvReq.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:50:38Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:50:38Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  We have currently tests (unit) that show the overwrite behavior, and they are failing otherwise: https://github.com/kubernetes-sigs/kueue/blob/d099039f66b2e0c9d243791ab275912ac606e0b4/pkg/controller/jobs/job/job_controller_test.go#L201-L225. So, the "update" as a test case name suggests this is the intention.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T16:52:54Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-11T16:52:54Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  So, in this test, we have a job with "orig-key": "orig-val", and it gets overwritten during run.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T17:00:29Z

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@mimowo](https://github.com/mimowo) — 2023-10-11T17:00:29Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  Dropped, but actually I found it flakes https://prow.k8s.io/view/gs/kubernetes-jenkins/pr-logs/pull/kubernetes-sigs_kueue/1180/pull-kueue-test-integration-main/1712147680169299968, as the workload doesn't get admitted. It passes locally for me most of the time. I would like to investigate as a follow up. It seems unrelated to the changes, just exposed by the new test.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T17:11:39Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-11T17:11:39Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  OTOH, I'm happy to adjust the test (drop the "update" case) in this PR if you think this is the way to go.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T17:21:37Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-11T17:21:37Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  Tentatively removed the idea of overwrite in https://github.com/kubernetes-sigs/kueue/pull/1180/commits/8555fd3124ac1234418bf9e5c3170ce4d4d2b51c. Updating description

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T17:42:50Z

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@mimowo](https://github.com/mimowo) — 2023-10-11T17:42:49Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  In case of failures, I get "Workload is inadmissible because ClusterQueue is inactive". After adding `util.SetAdmissionCheckActive(ctx, k8sClient, admissionCheck, metav1.ConditionTrue)` it seems to pass consistently. Anyway, dropping the test. Just updated the other one.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-12T11:48:27Z

Overall LGTM
I left a comment for a nit.

- **test/integration/controller/jobs/tfjob/tfjob_controller_test.go:526** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T11:46:05Z
```diff
@@ -520,6 +522,193 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 			},
 		}),
 	)
+
+	ginkgo.When("the queue has admission checks", func() {
```

  nit: I think we should move this case to pytorchjob_controller_test because we have all the test cases for kubeflowjobs in the integration tests for the PyTorchJob. So other kubeflowjobs including TFJob, don't have all cases.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-12T11:51:40Z

- **test/integration/controller/jobs/tfjob/tfjob_controller_test.go:526** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T11:51:40Z
```diff
@@ -520,6 +522,193 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 			},
 		}),
 	)
+
+	ginkgo.When("the queue has admission checks", func() {
```

  We used to verify only behavior depending on TFJob in the TFJob integration tests.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-12T12:09:59Z

- **test/integration/controller/jobs/tfjob/tfjob_controller_test.go:526** by [@mimowo](https://github.com/mimowo) — 2023-10-12T12:09:59Z
```diff
@@ -520,6 +522,193 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 			},
 		}),
 	)
+
+	ginkgo.When("the queue has admission checks", func() {
```

  Got it, will move it

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-12T12:23:08Z

- **test/integration/controller/jobs/tfjob/tfjob_controller_test.go:526** by [@mimowo](https://github.com/mimowo) — 2023-10-12T12:23:08Z
```diff
@@ -520,6 +522,193 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 			},
 		}),
 	)
+
+	ginkgo.When("the queue has admission checks", func() {
```

  Moved in the last commit, PTAL

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-12T12:29:39Z

- **test/integration/controller/jobs/tfjob/tfjob_controller_test.go:526** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T12:29:39Z
```diff
@@ -520,6 +522,193 @@ var _ = ginkgo.Describe("Job controller when waitForPodsReady enabled", ginkgo.O
 			},
 		}),
 	)
+
+	ginkgo.When("the queue has admission checks", func() {
```

  It looks good :) Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T05:03:43Z

- **pkg/controller/jobframework/podsetinfo.go:56** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T17:15:41Z
```diff
@@ -0,0 +1,98 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+    http://www.apache.org/licenses/LICENSE-2.0
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package jobframework
+
+import (
+	"maps"
+	"slices"
+
+	corev1 "k8s.io/api/core/v1"
+	v1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+
+	utilmaps "sigs.k8s.io/kueue/pkg/util/maps"
+)
+
+type PodSetInfo struct {
+	Name                  string
+	Count                 int32
+	NodeSelectorOverwrite map[string]string
+	Annotations           map[string]string
+	Labels                map[string]string
+	NodeSelector          map[string]string
+	Tolerations           []corev1.Toleration
+}
+
+func (podSetInfo *PodSetInfo) Merge(o PodSetInfo) error {
+	if err := utilmaps.HaveConflict(podSetInfo.Annotations, o.Annotations); err != nil {
+		return BadPodSetsUpdateError("annotations", err)
+	}
+	if err := utilmaps.HaveConflict(podSetInfo.Labels, o.Labels); err != nil {
+		return BadPodSetsUpdateError("labels", err)
+	}
+	maps.DeleteFunc(o.NodeSelector, func(k, v string) bool {
+		_, overwriteExists := o.NodeSelectorOverwrite[k]
+		return overwriteExists
+	})
+	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, o.NodeSelector); err != nil {
+		return BadPodSetsUpdateError("nodeSelector", err)
+	}
+	podSetInfo.Annotations = utilmaps.MergeKeepFirst(podSetInfo.Annotations, o.Annotations)
+	podSetInfo.Labels = utilmaps.MergeKeepFirst(podSetInfo.Labels, o.Labels)
+	podSetInfo.NodeSelector = utilmaps.MergeKeepFirst(podSetInfo.NodeSelector, o.NodeSelector)
+	podSetInfo.Tolerations = append(podSetInfo.Tolerations, o.Tolerations...)
+	return nil
+}
+
+// Merge updates or appends the replica metadata & spec fields based on PodSetInfo.
+// If returns error if there is a conflict.
+func Merge(meta *metav1.ObjectMeta, spec *v1.PodSpec, info PodSetInfo) error {
+	if err := info.Merge(PodSetInfo{
```

  I'm ok with either way. If @alculquicondor would like to take another way let us any comment here.

- **pkg/controller/jobframework/reconciler.go:655** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T17:21:32Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  Which phrase is correct? `podsSetInfos` vs `podSetsInfos` vs `podSetInfos` Maybe, does `podSetinfos` correct?
  Probably, we should use the same phrase across the whole of Kueue.
  
  @mimowo Could you open PR to re-phrase these?

- **pkg/controller/jobframework/reconciler.go:679** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T05:03:42Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  > There shouldn't be any conflicts, because the node selectors coming from the job are used to filter out flavors. https://kueue.sigs.k8s.io/docs/concepts/resource_flavor/#resourceflavor-labels
  
  In almost situations, resourceFlavors are created by batch admins and jobs are created by batch users. So I guess that conflicts could happen. Certainly, when users set the proper parameters for flvs and jobs, conflicts won't happen.
  
  Actually, current implementation, each integration controller overwrites podSpec's nodeSelector with podSetInfo one (flvs):
  
  https://github.com/kubernetes-sigs/kueue/blob/c3cfc1827b05b65c0dbc8a56e1ee84e76810f9f1/pkg/controller/jobs/job/job_controller.go#L227-L228
  
  However, I think the previous implementation (https://github.com/kubernetes-sigs/kueue/commit/8555fd3124ac1234418bf9e5c3170ce4d4d2b51c) looks a bit hacky.
  
  ```go
  ...
  	newNodeSelector := make(map[string]string)
  	for k, v := range o.NodeSelector {
  		if _, exists := podSetInfo.NodeSelectorOverwrite[k]; !exists {
  			newNodeSelector[k] = v
  		}
  	}
  	if err := utilmaps.HaveConflict(podSetInfo.NodeSelector, newNodeSelector); err != nil {
  ...
  ```
  
  In this implementation, maybe `utilmaps.HaveConflict(podSetInfo.NodeSelector, newNodeSelector)` always returns nil, right?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T05:11:56Z

- **test/integration/controller/jobs/job/job_controller_test.go:1018** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T05:11:55Z
```diff
@@ -790,6 +1014,163 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 		util.ExpectResourceFlavorToBeDeleted(ctx, k8sClient, spotUntaintedFlavor, true)
 	})
 
+	ginkgo.When("the queue has admission checks", func() {
```

  That makes sense. I guess that this failure happened due to https://github.com/kubernetes-sigs/kueue/pull/1193 being merged into the main even though this PR opened first.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T07:00:28Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mimowo](https://github.com/mimowo) — 2023-10-13T07:00:28Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  I think `podSetInfos` is correct, it seems the other version are only in lines I update anyway, so let me update in this PR.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T07:01:41Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mimowo](https://github.com/mimowo) — 2023-10-13T07:01:41Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  Done

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T07:14:08Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-13T07:14:08Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  >In almost situations, resourceFlavors are created by batch admins and jobs are created by batch users. So I guess that conflicts could happen. Certainly, when users set the proper parameters for flvs and jobs, conflicts won't happen.
  
   IIUC, if the job configured by user can only get assigned a flavor, if the flavor's `NodeLabels` match the job's nodeSelector. Thus we will not have conflicts later when starting the job. So, e2e `MergeKeepFirst` does not matter, and the unit tests only tested the overwrite in isolation, assuming a flavor with conflicting labels is assigned.
  
  > In this implementation, maybe `utilmaps.HaveConflict(podSetInfo.NodeSelector, newNodeSelector)` always returns nil, right?
  
  Not really, it is possible, that `NodeSelectorOverwrite` was empty. Then, `newNodeSelector = o.NodeSelector`, and this could conflict with `podSetInfo.NodeSelector`.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T08:32:46Z

- **pkg/controller/jobframework/reconciler.go:679** by [@mimowo](https://github.com/mimowo) — 2023-10-13T08:32:46Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  /cc @trasc who added the [unit tests for node selector overwrite](https://github.com/kubernetes-sigs/kueue/blob/c3cfc1827b05b65c0dbc8a56e1ee84e76810f9f1/pkg/controller/jobs/job/job_controller_test.go#L201C4-L225). 
  
  Did you have some e2e scenario in mind that could lead to the overwrite, or the purpose of the tests is to just document the unit / component behavior?

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-13T08:59:16Z

- **pkg/controller/jobframework/reconciler.go:679** by [@trasc](https://github.com/trasc) — 2023-10-13T08:59:15Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  It was added for good unit coverage. Indeed I don't think it should happen in real life.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T11:56:05Z

- **pkg/controller/jobframework/reconciler.go:655** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T11:56:04Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  Actually, I can see cases, `PodSetsInfo`:
  
  - https://github.com/kubernetes-sigs/kueue/blob/c3cfc1827b05b65c0dbc8a56e1ee84e76810f9f1/pkg/controller/jobframework/reconciler.go#L790 
  - https://github.com/kubernetes-sigs/kueue/blob/c3cfc1827b05b65c0dbc8a56e1ee84e76810f9f1/pkg/controller/jobframework/interface.go#L37
  
  So, I think we should re-phrase these at another PR since this is out of the scope of this PR. 
  
  Anyway, I'm ok with https://github.com/kubernetes-sigs/kueue/pull/1180/commits/e6d8c2b5338bad6d3563591396e622d40359375d .

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T12:02:58Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mimowo](https://github.com/mimowo) — 2023-10-13T12:02:57Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  Ah, I see, I will open another PR to rename the two places you point out.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-13T12:04:52Z

- **pkg/controller/jobframework/reconciler.go:655** by [@mimowo](https://github.com/mimowo) — 2023-10-13T12:04:52Z
```diff
@@ -643,26 +645,23 @@ func extractPriorityFromPodSets(podSets []kueue.PodSet) string {
 	return ""
 }
 
-type PodSetInfo struct {
-	Name         string            `json:"name"`
-	NodeSelector map[string]string `json:"nodeSelector"`
-	Count        int32             `json:"count"`
-}
-
-// getPodSetsInfoFromAdmission will extract podSetsInfo and podSets count from admitted workloads.
-func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
+// getPodSetsInfoFromStatus extracts podSetsInfos from workload status, based on
+// admission, and admission checks.
+func (r *JobReconciler) getPodSetsInfoFromStatus(ctx context.Context, w *kueue.Workload) ([]PodSetInfo, error) {
 	if len(w.Status.Admission.PodSetAssignments) == 0 {
 		return nil, nil
 	}
 
-	nodeSelectors := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
+	podsSetInfos := make([]PodSetInfo, len(w.Status.Admission.PodSetAssignments))
```

  I just pushed a commit to revert the unnecessary rename in interface.go

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T12:29:25Z

- **pkg/controller/jobframework/reconciler.go:679** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:29:24Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  > IIUC, if the job configured by user can only get assigned a flavor, if the flavor's NodeLabels match the job's nodeSelector. Thus we will not have conflicts later when starting the job. So, e2e MergeKeepFirst does not matter, and the unit tests only tested the overwrite in isolation, assuming a flavor with conflicting labels is assigned.
  
  Correct. Actually, after workloads are admitted, `getPodSetsInfoFromAdmission` is called. So we don't need to be concerned about any conflicts. Thank you for the clarification.
  
  > Not really, it is possible, that NodeSelectorOverwrite was empty. Then, newNodeSelector = o.NodeSelector, and this could conflict with podSetInfo.NodeSelector.
  
  Ah, that's right. I missed the case NodeSelectorOverwrite was empty. Thank you!

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T12:31:01Z

- **pkg/controller/jobframework/reconciler.go:679** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:31:00Z
```diff
@@ -675,14 +676,27 @@ func (r *JobReconciler) getPodSetsInfoFromAdmission(ctx context.Context, w *kueu
 				return nil, err
 			}
 			for k, v := range flv.Spec.NodeLabels {
-				nodeSelector.NodeSelector[k] = v
+				podSetInfo.NodeSelectorOverwrite[k] = v
```

  > It was added for good unit coverage. Indeed I don't think it should happen in real life.
  
  I think we can leave an `update` test case since this is unit tests, not integration tests.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T12:33:15Z

This is nice work! Thank you!
/lgtm
/approve
