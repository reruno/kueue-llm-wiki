# PR #1181: Two phase admission documentation

**Summary**: Two phase admission documentation

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1181

**Last updated**: 2023-10-25T10:14:43Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `doc-two-step-admission` → `main`
- **Created**: 2023-10-05T14:33:21Z
- **Updated**: 2023-10-25T10:14:43Z
- **Merged**: 2023-10-24T16:03:58Z
- **Closed**: 2023-10-24T16:03:58Z
- **Labels**: `size/L`, `lgtm`, `release-note-none`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 3   **+118 / -5**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Document Admission Checks.
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Relates to #993

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-05T14:33:23Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-05T14:33:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 80f0de205b365131f1a75b0bc3117ed8126a9abd |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6537d22a4b3a4600081de730 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1181--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1181--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExODEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.DYbRYh5fNecKGC8GaltHuy4Qnazi8-v0p6cwLWhqLjQ)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:44:24Z

@trasc Is this still WIP?

### Comment by [@trasc](https://github.com/trasc) — 2023-10-23T07:15:24Z

> @trasc Is this still WIP?

It's ok for review now.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:09:03Z

/release-note-none

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-23T19:09:05Z

@alculquicondor: you can only set the release note label to release-note-none if the release-note block in the PR body text is empty or "none".

<details>

In response to [this](https://github.com/kubernetes-sigs/kueue/pull/1181#issuecomment-1775857936):

>/release-note-none


Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:09:16Z

/release-note-edit
```release-note
NONE
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T15:59:37Z

LGTM label has been added.  <details>Git tree hash: 51f728dbc0d24f2a9ae95ae28d747570a170bb21</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T15:59:37Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1181#pullrequestreview-1695307864" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1181#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T19:37:25Z

- **site/content/en/docs/concepts/_index.md:45** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:10:49Z
```diff
@@ -40,18 +40,26 @@ Kueue. Sometimes referred to as _job_.
 independently from [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/).  
 This priority value from a `WorkloadPriorityClass` is only used for managing the queueing and preemption of [Workloads](#workload).
 
+### [Admission Check](/docs/concepts/admission_check)
+
+A mechanism allowing custom internal or external components to control the timing of
```

  ```suggestion
  A mechanism allowing internal or external components to influence the timing of
  ```

- **site/content/en/docs/concepts/_index.md:61** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:11:37Z
```diff
@@ -40,18 +40,26 @@ Kueue. Sometimes referred to as _job_.
 independently from [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/).  
 This priority value from a `WorkloadPriorityClass` is only used for managing the queueing and preemption of [Workloads](#workload).
 
+### [Admission Check](/docs/concepts/admission_check)
+
+A mechanism allowing custom internal or external components to control the timing of
+workloads admission.
+
 ![Components](/images/queueing-components.svg)
 
 ## Glossary
 
-### Admission
-
-The process of admitting a Workload to start (Pods to be created). A Workload
-is admitted by a ClusterQueue according to the available resources and gets
-resource flavors assigned for each requested resource.
+### Quota Reservation
 
 Sometimes referred to as _workload scheduling_ or _job scheduling_
 (not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+Is the process during which the kueue scheduler locks the resources needed by a workload within the targeted [ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+
+### Admission
+
+The process of admitting a Workload to start (Pods to be created). A Workload
+is admitted when it has a Quota Reservation and all it's potential [AdmissionCheckStates](/docs/concepts/admission_check)
```

  ```suggestion
  is admitted when it has a Quota Reservation and all its [AdmissionCheckStates](/docs/concepts/admission_check)
  ```

- **site/content/en/docs/concepts/admission_check.md:6** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:13:24Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
```

  ```suggestion
    A mechanism allowing internal or external components to influence the timing of
  ```

- **site/content/en/docs/concepts/admission_check.md:14** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:15:48Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
```

  ```suggestion
  Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
  ```

- **site/content/en/docs/concepts/admission_check.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:17:33Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
```

  I think it would be easier to read if we just use a single unnumbered list for the description of all fields.

- **site/content/en/docs/concepts/admission_check.md:8** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:23:18Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
```

  Let's add a paragraph here that explains the high level idea:
  
  AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload. When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
  positive signal to the Workload before it can be admitted (link to the admission definition).
  You can configure the mechanism using the following components:

- **site/content/en/docs/concepts/admission_check.md:27** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:26:06Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
```

  "An AdmissionCheck object looks like the following:"

- **site/content/en/docs/concepts/admission_check.md:18** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:27:18Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
```

  ```suggestion
  It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
  ```

- **site/content/en/docs/concepts/admission_check.md:26** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:28:37Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
```

  ```suggestion
  Identifies an additional resource providing additional parameters for the check.
  ```

- **site/content/en/docs/concepts/admission_check.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:28:47Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
```

  ```suggestion
  Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
  
  Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
  ```

- **site/content/en/docs/concepts/admission_check.md:48** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:30:40Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
```

  ```suggestion
  AdmissionCheckState is the way the state of an AdmissionCkeck for a specific Workload is tracked.
  AdmissionCheckStates are listed in the Workload's `.status.admissionCheckStates` field.
  ```

- **site/content/en/docs/concepts/admission_check.md:52** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:31:56Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
```

  Let's have an example instead.
  
  "The status of a Workload that has pending AdmissionChecks looks like the following:"

- **site/content/en/docs/concepts/admission_check.md:100** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:33:08Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
```

  ```suggestion
  Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue. Kueue adds new checks with the `Pending` state.
  ```

- **site/content/en/docs/concepts/admission_check.md:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:34:36Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
```

  Let's add a section in the Workload page for the important conditions (Admitted, QuotaReserved) and add a link to it here.

- **site/content/en/docs/concepts/admission_check.md:110** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:34:56Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+- `PreemptionRequired` is a state specific to PreemptionPolicies, from an Admission point of view being synonym to `Pending`
```

  This was not added yet, right?

- **site/content/en/docs/concepts/admission_check.md:115** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:36:45Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+- `PreemptionRequired` is a state specific to PreemptionPolicies, from an Admission point of view being synonym to `Pending`
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
+The logic based on which an `AdmissionCheck` changes states in not in the scope of Kueue.
```

  ```suggestion
  The logic for how an `AdmissionCheck` changes states is not part of Kueue.
  ```

- **site/static/examples/sample-admission-check.yaml:13** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T19:37:21Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: ProvisioningRequestController
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+
+---
+
```

  let's remove one empty space

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-24T05:33:06Z

- **site/content/en/docs/concepts/_index.md:45** by [@trasc](https://github.com/trasc) — 2023-10-24T05:03:06Z
```diff
@@ -40,18 +40,26 @@ Kueue. Sometimes referred to as _job_.
 independently from [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/).  
 This priority value from a `WorkloadPriorityClass` is only used for managing the queueing and preemption of [Workloads](#workload).
 
+### [Admission Check](/docs/concepts/admission_check)
+
+A mechanism allowing custom internal or external components to control the timing of
```

  done

- **site/content/en/docs/concepts/_index.md:61** by [@trasc](https://github.com/trasc) — 2023-10-24T05:03:40Z
```diff
@@ -40,18 +40,26 @@ Kueue. Sometimes referred to as _job_.
 independently from [pod priority](https://kubernetes.io/docs/concepts/scheduling-eviction/pod-priority-preemption/).  
 This priority value from a `WorkloadPriorityClass` is only used for managing the queueing and preemption of [Workloads](#workload).
 
+### [Admission Check](/docs/concepts/admission_check)
+
+A mechanism allowing custom internal or external components to control the timing of
+workloads admission.
+
 ![Components](/images/queueing-components.svg)
 
 ## Glossary
 
-### Admission
-
-The process of admitting a Workload to start (Pods to be created). A Workload
-is admitted by a ClusterQueue according to the available resources and gets
-resource flavors assigned for each requested resource.
+### Quota Reservation
 
 Sometimes referred to as _workload scheduling_ or _job scheduling_
 (not to be confused with [pod scheduling](https://kubernetes.io/docs/concepts/scheduling-eviction/assign-pod-node/)).
+Is the process during which the kueue scheduler locks the resources needed by a workload within the targeted [ClusterQueues ResourceGroups](/docs/concepts/cluster_queue/#resource-groups)
+
+### Admission
+
+The process of admitting a Workload to start (Pods to be created). A Workload
+is admitted when it has a Quota Reservation and all it's potential [AdmissionCheckStates](/docs/concepts/admission_check)
```

  done

- **site/content/en/docs/concepts/admission_check.md:6** by [@trasc](https://github.com/trasc) — 2023-10-24T05:04:21Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
```

  done

- **site/content/en/docs/concepts/admission_check.md:8** by [@trasc](https://github.com/trasc) — 2023-10-24T05:07:14Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
```

  done

- **site/content/en/docs/concepts/admission_check.md:14** by [@trasc](https://github.com/trasc) — 2023-10-24T05:07:35Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
```

  done

- **site/content/en/docs/concepts/admission_check.md:18** by [@trasc](https://github.com/trasc) — 2023-10-24T05:08:16Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
```

  done

- **site/content/en/docs/concepts/admission_check.md:26** by [@trasc](https://github.com/trasc) — 2023-10-24T05:11:32Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
```

  done

- **site/content/en/docs/concepts/admission_check.md:26** by [@trasc](https://github.com/trasc) — 2023-10-24T05:11:39Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
```

  done

- **site/content/en/docs/concepts/admission_check.md:27** by [@trasc](https://github.com/trasc) — 2023-10-24T05:12:14Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
```

  done

- **site/content/en/docs/concepts/admission_check.md:44** by [@trasc](https://github.com/trasc) — 2023-10-24T05:13:25Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
```

  done

- **site/content/en/docs/concepts/admission_check.md:48** by [@trasc](https://github.com/trasc) — 2023-10-24T05:16:42Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
```

  done

- **site/content/en/docs/concepts/admission_check.md:52** by [@trasc](https://github.com/trasc) — 2023-10-24T05:16:48Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
```

  done

- **site/content/en/docs/concepts/admission_check.md:100** by [@trasc](https://github.com/trasc) — 2023-10-24T05:17:53Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
```

  done

- **site/content/en/docs/concepts/admission_check.md:110** by [@trasc](https://github.com/trasc) — 2023-10-24T05:19:00Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+- `PreemptionRequired` is a state specific to PreemptionPolicies, from an Admission point of view being synonym to `Pending`
```

  yes, removed

- **site/content/en/docs/concepts/admission_check.md:115** by [@trasc](https://github.com/trasc) — 2023-10-24T05:19:56Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+- `PreemptionRequired` is a state specific to PreemptionPolicies, from an Admission point of view being synonym to `Pending`
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
+The logic based on which an `AdmissionCheck` changes states in not in the scope of Kueue.
```

  done

- **site/static/examples/sample-admission-check.yaml:13** by [@trasc](https://github.com/trasc) — 2023-10-24T05:20:38Z
```diff
@@ -0,0 +1,22 @@
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: ProvisioningRequestController
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+
+---
+
```

  done

- **site/content/en/docs/concepts/admission_check.md:73** by [@trasc](https://github.com/trasc) — 2023-10-24T05:32:39Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
```

  The workload is described as  " unit of admission" linked to glossary that will link to here. 
  
  I see the conditions more like an implementation detail, and and since no other part of the workload status is described there, I'd say to leave it as is.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-24T12:56:40Z

- **site/content/en/docs/concepts/admission_check.md:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T12:56:40Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
```

  I don't think it's an implementation detail. It's the canonical way how Kueue communicates (to other controllers and end users) that a Workload is admitted.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-24T13:01:13Z

- **site/content/en/docs/concepts/admission_check.md:52** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T12:59:00Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
```

  You forgot to change this for an example, instead of the schema

- **site/content/en/docs/concepts/admission_check.md:44** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T12:59:15Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
```

  ```suggestion
  Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not marked it as `Active`, the ClusterQueue will be marked as Inactive.
  ```

- **site/content/en/docs/concepts/admission_check.md:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T13:00:05Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensurers that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, new checks being added with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
```

  But I think this is fine for now

- **site/content/en/docs/concepts/admission_check.md:115** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T13:00:51Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+AdmissionCheckStates are listed in the Workload's `.status.admissionCheckStates` field.
+
+The status of a Workload that has pending AdmissionChecks looks like the following:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
```

  ```suggestion
  Is a component that monitors Workloads maintaining the content of its specific `admissionCheckStates` and the `Active` condition of the AdmissionChecks  its  controlling.
  ```

- **site/content/en/docs/concepts/admission_check.md:116** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T13:01:08Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+AdmissionCheckStates are listed in the Workload's `.status.admissionCheckStates` field.
+
+The status of a Workload that has pending AdmissionChecks looks like the following:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
+The logic for how an `AdmissionCheck` changes states is not part of Kueue.
```

  to standardize how we refer to CRD objects
  
  ```suggestion
  The logic for how an AdmissionCheck changes states is not part of Kueue.
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-24T14:18:21Z

- **site/content/en/docs/concepts/admission_check.md:52** by [@trasc](https://github.com/trasc) — 2023-10-24T14:12:52Z
```diff
@@ -0,0 +1,115 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing custom internal or external components to control the timing of
+  workloads admission.
+---
+
+## Components
+
+### Admission Check
+
+Admission check is a none namespaced API object used to define details about an `AdmissionCheck` like:
+
+#### controllerName
+
+Is the name with which controller identifies with, not necessarily a K8S Pod or Deployment name. Cannot be empty.
+
+#### retryDelayMinutes
+
+Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+
+#### parameters
+
+Identifies the resource providing additional check parameters.
+
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined an AdmissionCheck can be referenced in the ClsterQueues Spec resulting in all Workloads associated with the queue needing to be evaluated by its controller before being admitted. Similarly with `ResourceFalvors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+It is following the schema:
+```yaml
+properties:
```

  done

- **site/content/en/docs/concepts/admission_check.md:44** by [@trasc](https://github.com/trasc) — 2023-10-24T14:13:56Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
```

  done

- **site/content/en/docs/concepts/admission_check.md:115** by [@trasc](https://github.com/trasc) — 2023-10-24T14:16:02Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+AdmissionCheckStates are listed in the Workload's `.status.admissionCheckStates` field.
+
+The status of a Workload that has pending AdmissionChecks looks like the following:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
```

  done

- **site/content/en/docs/concepts/admission_check.md:116** by [@trasc](https://github.com/trasc) — 2023-10-24T14:16:21Z
```diff
@@ -0,0 +1,116 @@
+---
+title: "Admission Check"
+date: 2023-10-05
+weight: 6
+description: >
+  A mechanism allowing internal or external components to influence the timing of
+  workloads admission.
+---
+
+AdmissionChecks are a mechanism that allows Kueue to consider additional criteria before admitting a Workload.
+When a ClusterQueue has AdmissionChecks configured, each of the checks has to provide a
+positive signal to the Workload before it can be [Admitted](docs/concepts/#admission).
+
+## Components
+
+You can configure the mechanism using the following components:
+
+### Admission Check
+
+Admission check is a non-namespaced API object used to define details about an `AdmissionCheck` like:
+
+- **controllerName** - It's an identifier for the controller that processes this AdmissionCheck, not necessarily a Kubernetes Pod or Deployment name. Cannot be empty.
+- **retryDelayMinutes** - Specifies how long to keep the workload suspended after a failed check (after it transitioned to False). After that the check state goes to "Unknown". The default is 15 min.
+- **parameters** - Identifies an additional resource providing additional parameters for the check.
+
+An AdmissionCheck object looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: AdmissionCheck
+metadata:
+  name: prov-test
+spec:
+  controllerName: kueue.x-k8s.io/provisioning-request
+  retryDelayMinutes: 15
+  parameters:
+    apiGroup: kueue.x-k8s.io
+    kind: ProvisioningRequestConfig
+    name: prov-test-config
+```
+
+### ClusterQueue admissionChecks
+
+Once defined, an AdmissionCheck can be referenced in the ClusterQueues' spec. All Workloads associated with the queue need to be evaluated by the AdmissionCheck's controller before being admitted.
+Similarly to `ResourceFlavors`, if an `AdmissionCheck` is not found or its controller has not mark it as `Active`, the cluster queue will be marked as Inactive.
+
+### AdmissionCheckState
+
+AdmissionCheckState is the way the state of an AdmissionCkeck for a specific Workload is tracked.
+
+AdmissionCheckStates are listed in the Workload's `.status.admissionCheckStates` field.
+
+The status of a Workload that has pending AdmissionChecks looks like the following:
+```yaml
+properties:
+  lastTransitionTime:
+    description: lastTransitionTime is the last time the condition
+      transitioned from one status to another. This should be when
+      the underlying condition changed.  If that is not known, then
+      using the time when the API field changed is acceptable.
+    format: date-time
+    type: string
+  message:
+    description: message is a human readable message indicating
+      details about the transition. This may be an empty string.
+    maxLength: 32768
+    type: string
+  name:
+    description: name identifies the admission check.
+    maxLength: 316
+    type: string
+  podSetUpdates:
+    items:
+      description: PodSetUpdate contains a list of pod set modifications
+        suggested by AdmissionChecks. The modifications should be
+        additive only - modifications of already existing keys or
+        having the same key provided by multiple AdmissionChecks
+        is not allowed and will result in failure during workload
+        admission.
+  <...>
+      type: object
+    type: array
+    x-kubernetes-list-type: atomic
+  state:
+    description: status of the condition, one of True, False, Unknown.
+    enum:
+    - Pending
+    - Ready
+    - Retry
+    - Rejected
+    - PreemptionRequired
+    type: string
+required:
+- lastTransitionTime
+- message
+- name
+- state
+type: object
+```
+
+A list of states being maintained in the Status of all the monitored Workloads.
+
+Kueue ensures that the list of the Workloads AdmissionCheckStates is in sync with the list of its associated ClusterQueue, Kueue adds new checks with the `Pending` state.
+
+- Once a workload has QuotaReservation and all its AdmissionChecks are in "Ready" state it will become Admitted.
+- If at least one of the Workloads AdmissionCheck is in the `Retry` state.
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+- If at least one of the Workloads AdmissionCheck is in the `Rejected`:
+  - If `Admitted` the workload is evicted.
+  - If the workload has `QuotaReservation` it will be release released.
+  - The workload is marked as 'Finished' with a relevant failure message.
+
+### Admission Check Controller
+
+Is a component that monitors Workloads maintaining the content of its specific `AdmissionCheckStates` and the `Active` condition of the `AdmissionCheck`s  its  controlling.
+The logic for how an `AdmissionCheck` changes states is not part of Kueue.
```

  done

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-24T15:59:30Z

/lgtm
/approve
