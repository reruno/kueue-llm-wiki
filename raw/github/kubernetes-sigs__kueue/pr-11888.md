# PR #11888: Add kueue_flavor_admitted_active_workloads metric

**Summary**: Add kueue_flavor_admitted_active_workloads metric

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/11888

**Last updated**: 2026-06-03T15:37:40Z

---

## Metadata

- **State**: open
- **Author**: [@olekzabl](https://github.com/olekzabl)
- **Branch**: `br45` → `main`
- **Created**: 2026-06-02T09:56:54Z
- **Updated**: 2026-06-03T15:37:40Z
- **Merged**: —
- **Closed**: —
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `cncf-cla: yes`
- **Assignees**: [@kshalot](https://github.com/kshalot)
- **Requested reviewers**: [@mimowo](https://github.com/mimowo), [@pajakd](https://github.com/pajakd), [@sohankunkerkar](https://github.com/sohankunkerkar)
- **Changed files**: 10   **+170 / -10**

## Description


#### What type of PR is this?

/kind feature

#### What this PR does / why we need it:

Adds a new metric `kueue_flavor_admitted_active_workloads`, reporting admitted workloads count in CQ broken down by flavor.

This is built on top of #11504.

#### Which issue(s) this PR fixes:

Part of #11644.

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?

```release-note
Add a `kueue_flavor_admitted_active_workloads` metric, counting admitted workloads per flavor.
```

## Discussion

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-06-02T09:56:57Z

Skipping CI for Draft Pull Request.
If you want CI signal for your change, please convert it to an actual PR.
You can still manually trigger a test run with `/test all`

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2026-06-02T09:57:00Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d3a0e13af2a26ea814253aa66d052adae88506e8 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/projects/kubernetes-sigs-kueue/deploys/6a203afb6ba3a40008e9195d |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-11888--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-11888--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExODg4LS1rdWJlcm5ldGVzLXNpZ3Mta3VldWUubmV0bGlmeS5hcHAifQ.iftWQ3dMc7wrw_99k_aVak8s2Ft8rKuN2Lk4FpjjA5k)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---
<!-- [kubernetes-sigs-kueue Preview](https://deploy-preview-11888--kubernetes-sigs-kueue.netlify.app) -->
_To edit notification comments on pull requests, go to your [Netlify project configuration](https://app.netlify.com/projects/kubernetes-sigs-kueue/configuration/notifications#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-06-02T09:57:02Z

[APPROVALNOTIFIER] This PR is **NOT APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/11888#" title="Author self-approved">olekzabl</a>*
**Once this PR has been reviewed and has the lgtm label**, please assign [tenzen-y](https://github.com/tenzen-y) for approval. For more information see [the Code Review Process](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process).

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details open>
Needs approval from an approver in each of these files:

- **[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)**

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":["tenzen-y"]} -->

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T09:57:03Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T10:19:48Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T11:50:50Z

/test all

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T13:09:40Z

/kind feature

I placed it in PR description but Prow seems to somehow have missed it.

### Comment by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T15:16:03Z

> @olekzabl Should be close #11504, which is where most if not all of this diff comes from?

Not all :) See the commits history.

Yes, we should close #11504, though I seem unable to. Will ask Polina.

### Comment by [@kshalot](https://github.com/kshalot) — 2026-06-02T15:17:43Z

> Yes, we should close #11504, though I seem unable to. Will ask Polina.

I closed it (with the slashclose command).

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2026-06-03T15:37:40Z

LGTM label has been added.  <details>Git tree hash: 98e5197213d0e5de5b248ae357ed1d8d8e4c576d</details>

## Reviews

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-06-02T14:05:27Z

@olekzabl Should be close #11504, which is where most if not all of this diff comes from?

- **pkg/features/kube_features.go:445** by [@kshalot](https://github.com/kshalot) — 2026-06-02T13:51:28Z
```diff
@@ -435,11 +435,19 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/7539
 	// Enable reporting of Cohort related metrics (also including ClusterQueueInfo metric).
 	MetricsForCohorts featuregate.Feature = "MetricsForCohorts"
+
 	// owner: @tenzen-y
 	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/10659
 	// Enable accurately topology aware scheduling when multiple flavors cover the same Node.
 	TASHandleOverlappingFlavors featuregate.Feature = "TASHandleOverlappingFlavors"
+
+	// owner: @polinasand
```

  ```suggestion
  	// owner: @olekzabl
  ```

- **pkg/features/kube_features.go:446** by [@kshalot](https://github.com/kshalot) — 2026-06-02T13:51:45Z
```diff
@@ -435,11 +435,19 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/7539
 	// Enable reporting of Cohort related metrics (also including ClusterQueueInfo metric).
 	MetricsForCohorts featuregate.Feature = "MetricsForCohorts"
+
 	// owner: @tenzen-y
 	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/10659
 	// Enable accurately topology aware scheduling when multiple flavors cover the same Node.
 	TASHandleOverlappingFlavors featuregate.Feature = "TASHandleOverlappingFlavors"
+
+	// owner: @polinasand
+	// kep: https://github.com/kubernetes-sigs/kueue/issues/11644
```

  ```suggestion
  	// issue: https://github.com/kubernetes-sigs/kueue/issues/11644
  ```

- **pkg/features/kube_features.go:698** by [@kshalot](https://github.com/kshalot) — 2026-06-02T13:53:27Z
```diff
@@ -686,6 +694,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASHandleOverlappingFlavors: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	KueueAdmittedWorkloadsCountByFlavor: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  It's false, but in beta. If this should be disabled by default, I'd say this should be in alpha.
  
  Although personally I think this can be enabled by default:
  
  ```suggestion
  		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
  ```

- **pkg/cache/scheduler/clusterqueue.go:598** by [@kshalot](https://github.com/kshalot) — 2026-06-02T13:56:01Z
```diff
@@ -584,6 +590,12 @@ func (c *clusterQueue) updateWorkloadUsage(log logr.Logger, wi *workload.Info, o
 		updateFlavorUsage(frUsage, c.AdmittedUsage, op)
 		c.Parent().updateAdmittedWorkloadsCount(op.asSignedOne())
 		c.admittedWorkloadsCount += op.asSignedOne()
+
+		flavors := sets.New[kueue.ResourceFlavorReference]()
+		for fr := range frUsage {
+			flavors.Insert(fr.Flavor)
+		}
+		updateAdmittedWorkloadsCountByFlavor(flavors, c.admittedWorkloadsCountByFlavor, op)
```

  I'd rather that all new code would be hidden behind the feature gate. It's less wasteful and potentially safer.

- **pkg/cache/scheduler/clusterqueue.go:597** by [@kshalot](https://github.com/kshalot) — 2026-06-02T14:00:22Z
```diff
@@ -584,6 +590,12 @@ func (c *clusterQueue) updateWorkloadUsage(log logr.Logger, wi *workload.Info, o
 		updateFlavorUsage(frUsage, c.AdmittedUsage, op)
 		c.Parent().updateAdmittedWorkloadsCount(op.asSignedOne())
 		c.admittedWorkloadsCount += op.asSignedOne()
+
+		flavors := sets.New[kueue.ResourceFlavorReference]()
+		for fr := range frUsage {
+			flavors.Insert(fr.Flavor)
+		}
```

  Nit: Since this is all private functions, we could skip building this set and just pass `frUsage` to `updateAdmittedWorkloadsCountByFlavor`. Then it would be all encapsulated in the function call.

- **pkg/metrics/metrics.go:719** by [@kshalot](https://github.com/kshalot) — 2026-06-02T14:03:17Z
```diff
@@ -706,6 +710,17 @@ The label 'reason' can have the following values:
 	)
 	trackGaugeVec(AdmittedActiveWorkloads, gaugeCleanupScopeClusterQueueCache)
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`",flavor="the resource flavor name"
+	FlavorAdmittedActiveWorkloads = prometheus.NewGaugeVec(
+		prometheus.GaugeOpts{
+			Subsystem: constants.KueueName,
+			Name:      "flavor_admitted_active_workloads",
+			Help:      "The number of admitted Workloads that are active (unsuspended and not finished), per 'cluster_queue' and 'flavor'",
```

  The help of `AdmittedActiveWorkloads` reads:
  
  > The number of admitted Workloads that are active, per 'cluster_queue'
  
  We could keep them the same so it's clear that it's the same thing (modulo the breakdown by flavor).
  
  Also, it might be good to mention that this metric does not sum to the total workload count because of workloads with multiple flavors.

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:1** by [@kshalot](https://github.com/kshalot) — 2026-06-02T14:04:13Z
  Should this be in the fair sharing suite?

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-02T15:00:08Z

- **pkg/features/kube_features.go:445** by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T15:00:08Z
```diff
@@ -435,11 +435,19 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/7539
 	// Enable reporting of Cohort related metrics (also including ClusterQueueInfo metric).
 	MetricsForCohorts featuregate.Feature = "MetricsForCohorts"
+
 	// owner: @tenzen-y
 	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/10659
 	// Enable accurately topology aware scheduling when multiple flavors cover the same Node.
 	TASHandleOverlappingFlavors featuregate.Feature = "TASHandleOverlappingFlavors"
+
+	// owner: @polinasand
```

  Done

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-02T15:04:39Z

- **pkg/cache/scheduler/clusterqueue.go:597** by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T15:04:39Z
```diff
@@ -584,6 +590,12 @@ func (c *clusterQueue) updateWorkloadUsage(log logr.Logger, wi *workload.Info, o
 		updateFlavorUsage(frUsage, c.AdmittedUsage, op)
 		c.Parent().updateAdmittedWorkloadsCount(op.asSignedOne())
 		c.admittedWorkloadsCount += op.asSignedOne()
+
+		flavors := sets.New[kueue.ResourceFlavorReference]()
+		for fr := range frUsage {
+			flavors.Insert(fr.Flavor)
+		}
```

  OK. I initially preferred to pull it out - but you convinced me.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-02T15:05:12Z

- **test/integration/singlecluster/scheduler/fairsharing/metrics_test.go:1** by [@olekzabl](https://github.com/olekzabl) — 2026-06-02T15:05:11Z
  Sticking to the convention. This is the only file testing the original metric ("admitted workloads" with no breakdown).

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-06-03T12:46:16Z

- **pkg/cache/scheduler/clusterqueue.go:649** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:14:05Z
```diff
@@ -630,6 +639,17 @@ func updateFlavorUsage(newUsage resources.FlavorResourceQuantities, oldUsage res
 	}
 }
 
+func updateAdmittedWorkloadsCountByFlavor(frUsage resources.FlavorResourceQuantities, admittedWorkloadsCountByFlavor map[kueue.ResourceFlavorReference]int, op usageOp) {
+	sign := op.asSignedOne()
+	flavors := sets.New[kueue.ResourceFlavorReference]()
+	for fr := range frUsage {
+		flavors.Insert(fr.Flavor)
+	}
+	for flavor := range flavors {
+		admittedWorkloadsCountByFlavor[flavor] += sign
```

  ```suggestion
  	flavors := sets.New[kueue.ResourceFlavorReference]()
  	for fr := range frUsage {
  		flavors.Insert(fr.Flavor)
  	}
  	for flavor := range flavors {
  		admittedWorkloadsCountByFlavor[flavor] += op.asSignedOne()
  ```
  
  Nano-nit: I don't care much whether this is applied or not

- **pkg/features/kube_features.go:449** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:40:22Z
```diff
@@ -435,11 +435,19 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/7539
 	// Enable reporting of Cohort related metrics (also including ClusterQueueInfo metric).
 	MetricsForCohorts featuregate.Feature = "MetricsForCohorts"
+
 	// owner: @tenzen-y
 	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/10659
 	// Enable accurately topology aware scheduling when multiple flavors cover the same Node.
 	TASHandleOverlappingFlavors featuregate.Feature = "TASHandleOverlappingFlavors"
+
+	// owner: @polinasand
+	// kep: https://github.com/kubernetes-sigs/kueue/issues/11644
+	//
+	// When enabled, metrics for Kueue active admitted workloads count by flavor
+	// are populated by clusterqueue.
```

  I don't love this phrasing but I also don't think it's a big deal. Usually the FGs are described as "Enable ..." and not "When enabled...".
  
  Sth like:
  ```suggestion
  	// Enable reporting of the count of active admitted workloads broken down by flavor.
  ```
  
  I think it should also be clear that this does not change the existing metric, but adds a new one.

- **pkg/features/kube_features.go:698** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:41:01Z
```diff
@@ -686,6 +694,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASHandleOverlappingFlavors: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	AdmittedWorkloadsCountByFlavor: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  https://github.com/kubernetes-sigs/kueue/pull/11888/changes#r3341701619
  
  ^ Here, I proposed enabling this by default. This comment is now resolved, but I see no change.

- **pkg/metrics/metrics.go:216** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:43:09Z
```diff
@@ -212,6 +212,10 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`"
 	AdmittedActiveWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`",flavor="the resource flavor name"
```

  ```suggestion
  	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",flavor="the resource flavor name",replica_role="one of `leader`, `follower`, or `standalone`"
  ```
  
  Nit: This order makes more sense to me

- **pkg/metrics/metrics.go:714** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:43:37Z
```diff
@@ -706,6 +710,17 @@ The label 'reason' can have the following values:
 	)
 	trackGaugeVec(AdmittedActiveWorkloads, gaugeCleanupScopeClusterQueueCache)
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`",flavor="the resource flavor name"
```

  Same comment about ordering.

- **pkg/metrics/metrics.go:217** by [@kshalot](https://github.com/kshalot) — 2026-06-03T12:46:11Z
```diff
@@ -212,6 +212,10 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`"
 	AdmittedActiveWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`",flavor="the resource flavor name"
+	FlavorAdmittedActiveWorkloads *prometheus.GaugeVec
```

  Maybe `AdmittedActiveWorkloadsByFlavor`? When you search for the metrics in some dropdown (if someone has a UI like this or smth), both this new metric and the existing `AdmittedActiveWorkloads` will be listed when you search by prefix.
  
  We even call the FG itself `AdmittedWorkloadsCountByFlavor`

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-03T13:13:10Z

- **pkg/cache/scheduler/clusterqueue.go:649** by [@olekzabl](https://github.com/olekzabl) — 2026-06-03T13:13:10Z
```diff
@@ -630,6 +639,17 @@ func updateFlavorUsage(newUsage resources.FlavorResourceQuantities, oldUsage res
 	}
 }
 
+func updateAdmittedWorkloadsCountByFlavor(frUsage resources.FlavorResourceQuantities, admittedWorkloadsCountByFlavor map[kueue.ResourceFlavorReference]int, op usageOp) {
+	sign := op.asSignedOne()
+	flavors := sets.New[kueue.ResourceFlavorReference]()
+	for fr := range frUsage {
+		flavors.Insert(fr.Flavor)
+	}
+	for flavor := range flavors {
+		admittedWorkloadsCountByFlavor[flavor] += sign
```

  This was deliberate, for consistency with `updateFlavorUsage` above.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-03T14:35:24Z

- **pkg/metrics/metrics.go:217** by [@olekzabl](https://github.com/olekzabl) — 2026-06-03T14:35:24Z
```diff
@@ -212,6 +212,10 @@ var (
 	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`"
 	AdmittedActiveWorkloads *prometheus.GaugeVec
 
+	// +metricsdoc:group=clusterqueue
+	// +metricsdoc:labels=cluster_queue="the name of the ClusterQueue",replica_role="one of `leader`, `follower`, or `standalone`",flavor="the resource flavor name"
+	FlavorAdmittedActiveWorkloads *prometheus.GaugeVec
```

  Yes, I like it, not just for alpha-sorting reasons.
  
  Initially we followed `Flavor...` to stick to the existing `LocalQueue...` names - but I see `...ByStatus` already exists, and I think for the pending workload case we'll need `...By...` anyway. (Otherwise, the name would be too entangled).

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-03T14:35:31Z

- **pkg/features/kube_features.go:449** by [@olekzabl](https://github.com/olekzabl) — 2026-06-03T14:35:31Z
```diff
@@ -435,11 +435,19 @@ const (
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/7539
 	// Enable reporting of Cohort related metrics (also including ClusterQueueInfo metric).
 	MetricsForCohorts featuregate.Feature = "MetricsForCohorts"
+
 	// owner: @tenzen-y
 	// kep: https://github.com/kubernetes-sigs/kueue/tree/main/keps/2724-topology-aware-scheduling
 	// issue: https://github.com/kubernetes-sigs/kueue/issues/10659
 	// Enable accurately topology aware scheduling when multiple flavors cover the same Node.
 	TASHandleOverlappingFlavors featuregate.Feature = "TASHandleOverlappingFlavors"
+
+	// owner: @polinasand
+	// kep: https://github.com/kubernetes-sigs/kueue/issues/11644
+	//
+	// When enabled, metrics for Kueue active admitted workloads count by flavor
+	// are populated by clusterqueue.
```

  "When enabled" also happens. It's Polina's wording; I didn't care.
  
  As you brought my attention to this, I picked some yet another wording.

### Review by [@olekzabl](https://github.com/olekzabl) (commented) — 2026-06-03T14:35:37Z

- **pkg/features/kube_features.go:698** by [@olekzabl](https://github.com/olekzabl) — 2026-06-03T14:35:37Z
```diff
@@ -686,6 +694,9 @@ var defaultVersionedFeatureGates = map[featuregate.Feature]featuregate.Versioned
 	TASHandleOverlappingFlavors: {
 		{Version: version.MustParse("0.18"), Default: true, PreRelease: featuregate.Beta},
 	},
+	AdmittedWorkloadsCountByFlavor: {
+		{Version: version.MustParse("0.18"), Default: false, PreRelease: featuregate.Beta},
```

  Oh. I did that... but somehow reverted it later. No idea how. Done again.
  
  BTW same about other neighboring changes (owner, KEP -> issue) :)

### Review by [@kshalot](https://github.com/kshalot) (commented) — 2026-06-03T15:37:31Z

/lgtm
