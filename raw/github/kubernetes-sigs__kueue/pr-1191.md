# PR #1191: Commonize KubeflowJob Tests

**Summary**: Commonize KubeflowJob Tests

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1191

**Last updated**: 2023-10-23T19:20:39Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `kubeflowjob` → `main`
- **Created**: 2023-10-10T07:39:08Z
- **Updated**: 2023-10-23T19:20:39Z
- **Merged**: 2023-10-23T19:20:37Z
- **Closed**: 2023-10-23T19:20:37Z
- **Labels**: `lgtm`, `size/XXL`, `release-note-none`, `approved`, `kind/cleanup`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@ArangoGutierrez](https://github.com/ArangoGutierrez), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 14   **+442 / -1131**

## Description

#### What type of PR is this?

/kind cleanup

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-10T07:39:14Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | d561a3f2559462b123026e293bf374a03ccd0090 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/65362cf73af9100008fa3086 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1191--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1191--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTExOTEtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.GY362fDK1jgA-asJ79fkq3VYGXGuhZu0b9_sfjip09k)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-10T07:39:16Z

Welcome @yaroslava-serdiuk! <br><br>It looks like this is your first PR to <a href='https://github.com/kubernetes-sigs/kueue'>kubernetes-sigs/kueue</a> 🎉. Please refer to our [pull request process documentation](https://git.k8s.io/community/contributors/guide/pull-requests.md) to help your PR have a smooth ride to approval. <br><br>You will be prompted by a bot to use commands during the review process. Do not be afraid to follow the prompts! It is okay to experiment. [Here is the bot commands documentation](https://go.k8s.io/bot-commands). <br><br>You can also check if kubernetes-sigs/kueue has [its own contribution guidelines](https://github.com/kubernetes-sigs/kueue/tree/master/CONTRIBUTING.md). <br><br>You may want to refer to our [testing guide](https://git.k8s.io/community/contributors/devel/sig-testing/testing.md) if you run into trouble with your tests not passing. <br><br>If you are having difficulty getting your pull request seen, please follow the [recommended escalation practices](https://github.com/kubernetes/community/blob/master/contributors/guide/pull-requests.md#why-is-my-pull-request-not-getting-reviewed). Also, for tips and tricks in the contribution process you may want to read the [Kubernetes contributor cheat sheet](https://git.k8s.io/community/contributors/guide/contributor-cheatsheet/README.md). We want to make sure your contribution gets all the attention it needs! <br><br>Thank you, and welcome to Kubernetes. :smiley:

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-10T07:39:17Z

Hi @yaroslava-serdiuk. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-10T10:48:52Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T15:28:23Z

/assign @tenzen-y

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T15:29:28Z

/release-note-none

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-12T11:32:27Z

I will review this PR after you addressed https://github.com/kubernetes-sigs/kueue/pull/1191#discussion_r1353109319.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:53:39Z

otherwise lgtm
/approve

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:04:46Z

Part of https://github.com/kubernetes-sigs/kueue/issues/1119

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:54:20Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-23T18:54:26Z

LGTM label has been added.  <details>Git tree hash: 2424ac73dc35a869dc4cf901a7ee663cfc7d3d2e</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-23T18:54:29Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1191#issuecomment-1775828231" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1191#issuecomment-1773075322" title="Approved">tenzen-y</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1191#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor,tenzen-y]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-10T08:49:47Z

- **test/util/kubeflowjob_utils.go:48** by [@mimowo](https://github.com/mimowo) — 2023-10-10T08:39:18Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
```

  I think this could be a dedicated function to verify priority.

- **test/util/kubeflowjob_utils.go:45** by [@mimowo](https://github.com/mimowo) — 2023-10-10T08:40:00Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
```

  I would suggest this check outside of `CheckCreatedWorkload`.

- **test/util/kubeflowjob_utils.go:39** by [@mimowo](https://github.com/mimowo) — 2023-10-10T08:41:45Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
```

  ```suggestion
  func AwaitWorkloadCreated(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
  ```
  To reflect we may need to wait for this to happen.

- **test/util/kubeflowjob_utils.go:54** by [@mimowo](https://github.com/mimowo) — 2023-10-10T08:42:16Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	return createdWorkload
+}
+
+func VerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
```

  ```suggestion
  func AwaitWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
  ```

- **test/util/kubeflowjob_utils.go:64** by [@mimowo](https://github.com/mimowo) — 2023-10-10T08:48:05Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	return createdWorkload
+}
+
+func VerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
+}
+
+func CreateAdmission(createdWorkload *kueue.Workload, clusterQueueName string, masterCpu, workerCpu kueue.ResourceFlavorReference) *kueue.Admission {
```

  I don't have a strong view, but I'm not convinced about extracting the admission configuration as a function. 
  
  The reason is that when reading the test, it is not clear that the admission creates a "worker" and "master" pod sets. So it requires a jump between files to match the configuration with asserts later. 
  
  I think I would like to extract functions for which the name is descriptive enough that you can get what they do without a jump, for example `AwaitWorkloadQueueName`.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-10T11:13:02Z

- **test/util/kubeflowjob_utils.go:54** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-10T11:13:01Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	return createdWorkload
+}
+
+func VerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
```

  I think we await and verify the queue, maybe AwaitAndVerifyWorkloadQueueName (same for CheckCreatedWorkload)?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-10T11:13:11Z

- **test/util/kubeflowjob_utils.go:45** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-10T11:13:11Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
```

  Why?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-10T11:14:21Z

- **test/util/kubeflowjob_utils.go:64** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-10T11:14:21Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	return createdWorkload
+}
+
+func VerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
+}
+
+func CreateAdmission(createdWorkload *kueue.Workload, clusterQueueName string, masterCpu, workerCpu kueue.ResourceFlavorReference) *kueue.Admission {
```

  Configuration is the same for each test, so I think it make sense to define a separate function for that. It makes the function smaller and I think it improves readability

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-10T15:29:12Z

- **test/util/kubeflowjob_utils.go:28** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T15:29:12Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
```

  add an empty line after this

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-10T18:36:44Z

- **test/util/kubeflowjob_utils.go:40** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T18:27:42Z
```diff
@@ -0,0 +1,83 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func AwaitAndVerifyCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
```

  This is not that specific to kubeflow jobs, so it can be in the main `pkg/util/testing`

- **test/integration/controller/jobs/paddlejob/paddlejob_controller_test.go:104** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-10T18:36:39Z
```diff
@@ -104,12 +104,7 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
```

  It might be more valuable to have the whole test as a function.
  Something like:
  
  ```golang
  ginkgo.It("Should reconcile PaddleJobs", func() {
    job := testingpaddlejob.MakePaddleJob(....)
    kftesting.ShouldReconcileJob(job)
  })
  ```
  
  I'm potentially missing some parameters, but that's the high level idea.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T07:21:06Z

- **test/util/kubeflowjob_utils.go:45** by [@mimowo](https://github.com/mimowo) — 2023-10-11T07:21:06Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
```

  I think then the function would have more potential to be reused in other tests, which specify QueueName.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-11T07:32:50Z

- **test/util/kubeflowjob_utils.go:64** by [@mimowo](https://github.com/mimowo) — 2023-10-11T07:32:50Z
```diff
@@ -0,0 +1,82 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package util
+
+import (
+	"context"
+
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/utils/ptr"
+	"sigs.k8s.io/controller-runtime/pkg/client"
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+)
+
+const (
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+func CheckCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
+		return client.Get(ctx, wlLookupKey, createdWorkload)
+	}, Timeout, Interval).Should(gomega.Succeed())
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+	gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	return createdWorkload
+}
+
+func VerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
+}
+
+func CreateAdmission(createdWorkload *kueue.Workload, clusterQueueName string, masterCpu, workerCpu kueue.ResourceFlavorReference) *kueue.Admission {
```

  I see it helps to commonize, which is a gain, but it splits the bits which need to match between two files. Maybe  https://github.com/kubernetes-sigs/kueue/pull/1191/files#r1353109319 would help to handle both aspects.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-16T12:08:16Z

- **pkg/controller/jobs/kubeflow/jobs/paddlejob/paddlejob_controller_test.go:257** by [@mimowo](https://github.com/mimowo) — 2023-10-16T12:08:04Z
```diff
@@ -254,8 +254,8 @@ func TestReconciler(t *testing.T) {
 			reconcilerOptions: []jobframework.Option{
 				jobframework.WithManageJobsWithoutQueueName(true),
 			},
-			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
-			wantJob: testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
+			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").SetParallelism(2).Obj().(*kftraining.PaddleJob),
```

  I would suggest not to add the `Set` prefixes. It makes the diff large unnecessarily. It's common not to use the `set` prefix in builders.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-17T08:14:44Z

Suggestions around the common interface.

- **pkg/controller/jobs/kubeflow/jobs/paddlejob/paddlejob_controller_test.go:257** by [@mimowo](https://github.com/mimowo) — 2023-10-17T07:59:33Z
```diff
@@ -254,8 +254,8 @@ func TestReconciler(t *testing.T) {
 			reconcilerOptions: []jobframework.Option{
 				jobframework.WithManageJobsWithoutQueueName(true),
 			},
-			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
-			wantJob: testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
+			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj().(*kftraining.PaddleJob),
```

  I think it would be nice to avoid the cast at the end.

- **pkg/controller/jobs/kubeflow/jobs/paddlejob/paddlejob_controller_test.go:280** by [@mimowo](https://github.com/mimowo) — 2023-10-17T07:59:47Z
```diff
@@ -269,23 +269,23 @@ func TestReconciler(t *testing.T) {
 			reconcilerOptions: []jobframework.Option{
 				jobframework.WithManageJobsWithoutQueueName(false),
 			},
-			job:           testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
-			wantJob:       testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
+			job:           testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj().(*kftraining.PaddleJob),
+			wantJob:       testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj().(*kftraining.PaddleJob),
 			wantWorkloads: []kueue.Workload{},
 		},
 		"when workload is evicted, suspended is reset, restore node affinity": {
 			job: testingpaddlejob.MakePaddleJob("paddlejob", "ns").
 				Image("").
 				Args(nil).
+				NodeSelector("provisioning", "spot").
```

  Don't change the order of lines.

- **pkg/util/testingjobs/kubeflowjob/wrapper_interface.go:26** by [@mimowo](https://github.com/mimowo) — 2023-10-17T08:01:56Z
```diff
@@ -0,0 +1,39 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package testing
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+// KubeflowWrapper define interface for kubeflowjobs wrapper.
+type KubeflowjobWrapper interface {
```

  I think it would be preferred to have an interface just like:
  ```
  type Kubeflowjob interface {
  	Object() client.Object
  	RunPolicy() *kftraining.RunPolicy
   	ReplicaSpecs() map[kftraining.ReplicaType]*kftraining.ReplicaSpec
  	JobStatus() *kftraining.JobStatus
   }
   ```
   The other functions can be implemented by accessing the fields returned by the functions. For example, PriorityClass can be accessed via RunPolicy, which is common for all kubeflow jobs.
   
  Instances of this type can be then passed to `ShouldReconcileJob` as job and createdJob parameters. I think you could follow the `GenericJob` pattern here.

- **pkg/util/testingjobs/paddlejob/wrappers.go:86** by [@mimowo](https://github.com/mimowo) — 2023-10-17T08:11:45Z
```diff
@@ -83,21 +89,20 @@ func MakePaddleJob(name, ns string) *PaddleJobWrapper {
 }
 
 // PriorityClass updates job priorityclass.
-func (j *PaddleJobWrapper) PriorityClass(pc string) *PaddleJobWrapper {
```

  I think it is preferred to keep builder functions to return ` *PaddleJobWrapper`.
  
   I would suggest to follow the pattern we have for MPIJob and other implementations of GenericJob.
  
  I think we could declare the alias type 
  `type PaddleJob kftraining.PaddleJob`, and extend the alias with the `Object`, `RunPolicy`, `ReplicaSpecs`, and `JobStatus` functions, so that it implements `Kubeflowjob` as above. Then, if the wrapper embeds the extended type `type PaddleJobWrapper struct{ PaddleJob }` , then you should be able to implement the `Obj` by just returning the &j.PaddleJob.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-17T12:47:46Z

- **pkg/controller/jobs/kubeflow/jobs/paddlejob/paddlejob_controller_test.go:257** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-17T12:47:45Z
```diff
@@ -254,8 +254,8 @@ func TestReconciler(t *testing.T) {
 			reconcilerOptions: []jobframework.Option{
 				jobframework.WithManageJobsWithoutQueueName(true),
 			},
-			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
-			wantJob: testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj(),
+			job:     testingpaddlejob.MakePaddleJob("paddlejob", "ns").Parallelism(2).Obj().(*kftraining.PaddleJob),
```

  +1

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-17T12:59:46Z

Could you also include MXJob in this PR?

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-17T12:59:16Z
```diff
@@ -0,0 +1,304 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	workloadpaddlejob "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/paddlejob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	kftesting "sigs.k8s.io/kueue/pkg/util/testingjobs/kubeflowjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+var (
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kftesting.KubeflowjobWrapper, ns *corev1.Namespace, wlLookupKey types.NamespacedName, master, worker kftraining.ReplicaType) {
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.RunPolicy().SchedulingPolicy == nil {
+		job.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Obj())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Obj())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Obj().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      workloadpaddlejob.GetWorkloadNameForPaddleJob("second-workload"),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Obj(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.ReplicaSpecs()[worker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.ReplicaSpecs()[worker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend &&
+			len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.PodSets[1].Count == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is finished when job is completed")
+	createdJob.JobStatus().Conditions = append(createdJob.JobStatus().Conditions,
+		kftraining.JobCondition{
+			Type:               kftraining.JobSucceeded,
+			Status:             corev1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+		})
+	gomega.Expect(k8sClient.Status().Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		if err != nil || len(createdWorkload.Status.Conditions) == 2 {
+			return false
+		}
+
+		return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+}
+
+// CreateMasterAndWorkerPodSet creates a master and worker pod sets.
+func CreateMasterAndWorkerPodSet(createdWorkload *kueue.Workload, masterCpu, workerCpu kueue.ResourceFlavorReference) []kueue.PodSetAssignment {
+	return []kueue.PodSetAssignment{
```

  I think we shouldn't use this commonize function since not all kubeflow frameworks use the `master` and `worker` roles. For example, the MXJob uses `scheduler`, `server`, and `worker`.
  
  Also, this commonize seems to violate the DAMP principle. I would suggest to refer to the SIG scheduling contribution guide: https://github.com/kubernetes/community/blob/master/sig-scheduling/CONTRIBUTING.md#technical-and-style-guidelines

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-20T13:44:22Z

- **pkg/util/testingjobs/kubeflowjob/wrapper_interface.go:26** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-20T13:44:22Z
```diff
@@ -0,0 +1,39 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package testing
+
+import (
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	corev1 "k8s.io/api/core/v1"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+)
+
+// KubeflowWrapper define interface for kubeflowjobs wrapper.
+type KubeflowjobWrapper interface {
```

  We have KFJobControl interface which is similar to what you suggest, so I reused that one.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-20T13:48:07Z

- **pkg/util/testingjobs/paddlejob/wrappers.go:86** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-20T13:48:06Z
```diff
@@ -83,21 +89,20 @@ func MakePaddleJob(name, ns string) *PaddleJobWrapper {
 }
 
 // PriorityClass updates job priorityclass.
-func (j *PaddleJobWrapper) PriorityClass(pc string) *PaddleJobWrapper {
```

  Yes, I see that PaddleJobWrapper is a builder, so I shouldn't modify it. I didn't create a new type PaddleJob, because in that case I'll need to change existing unit test. For first integration test seems I don't need a special wrapper. I'll probably need something for other integration tests, but I would like to split it to another PR.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-20T13:51:24Z

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:219** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-20T13:51:24Z
```diff
@@ -0,0 +1,304 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	workloadpaddlejob "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/paddlejob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	kftesting "sigs.k8s.io/kueue/pkg/util/testingjobs/kubeflowjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+var (
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kftesting.KubeflowjobWrapper, ns *corev1.Namespace, wlLookupKey types.NamespacedName, master, worker kftraining.ReplicaType) {
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.RunPolicy().SchedulingPolicy == nil {
+		job.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Obj())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Obj())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Obj().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      workloadpaddlejob.GetWorkloadNameForPaddleJob("second-workload"),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Obj(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.ReplicaSpecs()[worker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.ReplicaSpecs()[worker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend &&
+			len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.PodSets[1].Count == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is finished when job is completed")
+	createdJob.JobStatus().Conditions = append(createdJob.JobStatus().Conditions,
+		kftraining.JobCondition{
+			Type:               kftraining.JobSucceeded,
+			Status:             corev1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+		})
+	gomega.Expect(k8sClient.Status().Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		if err != nil || len(createdWorkload.Status.Conditions) == 2 {
+			return false
+		}
+
+		return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+}
+
+// CreateMasterAndWorkerPodSet creates a master and worker pod sets.
+func CreateMasterAndWorkerPodSet(createdWorkload *kueue.Workload, masterCpu, workerCpu kueue.ResourceFlavorReference) []kueue.PodSetAssignment {
+	return []kueue.PodSetAssignment{
```

  I extracted it to a function because it appears 2 times in test. Now the function works for all kubeflow jobs, is it better?

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-20T15:22:03Z

Generally LGTM. I think commonizing the other test cases could be left for follow up PRs as this one sets the direction.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T15:53:50Z

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:219** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T15:53:50Z
```diff
@@ -0,0 +1,304 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	"github.com/google/go-cmp/cmp/cmpopts"
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	workloadpaddlejob "sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/jobs/paddlejob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+	kftesting "sigs.k8s.io/kueue/pkg/util/testingjobs/kubeflowjob"
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+var (
+	ignoreConditionTimestamps = cmpopts.IgnoreFields(metav1.Condition{}, "LastTransitionTime")
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kftesting.KubeflowjobWrapper, ns *corev1.Namespace, wlLookupKey types.NamespacedName, master, worker kftraining.ReplicaType) {
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.RunPolicy().SchedulingPolicy == nil {
+		job.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Obj())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Obj())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Obj().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      workloadpaddlejob.GetWorkloadNameForPaddleJob("second-workload"),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Obj(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.ReplicaSpecs()[worker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.ReplicaSpecs()[worker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return createdJob.RunPolicy().Suspend != nil && *createdJob.RunPolicy().Suspend &&
+			len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.PodSets[1].Count == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreateMasterAndWorkerPodSet(createdWorkload, "on-demand", "spot")...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Obj()); err != nil {
+			return false
+		}
+		return !*createdJob.RunPolicy().Suspend
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[master].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	gomega.Expect(len(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.ReplicaSpecs()[worker].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is finished when job is completed")
+	createdJob.JobStatus().Conditions = append(createdJob.JobStatus().Conditions,
+		kftraining.JobCondition{
+			Type:               kftraining.JobSucceeded,
+			Status:             corev1.ConditionTrue,
+			LastTransitionTime: metav1.Now(),
+		})
+	gomega.Expect(k8sClient.Status().Update(ctx, createdJob.Obj())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		if err != nil || len(createdWorkload.Status.Conditions) == 2 {
+			return false
+		}
+
+		return apimeta.IsStatusConditionTrue(createdWorkload.Status.Conditions, kueue.WorkloadFinished)
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+}
+
+// CreateMasterAndWorkerPodSet creates a master and worker pod sets.
+func CreateMasterAndWorkerPodSet(createdWorkload *kueue.Workload, masterCpu, workerCpu kueue.ResourceFlavorReference) []kueue.PodSetAssignment {
+	return []kueue.PodSetAssignment{
```

  I'm ok with the updated function. It looks like keeps readability.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T16:53:01Z

- **pkg/controller/jobs/kubeflow/kubeflowjob/kubeflowjob_controller.go:89** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:09:50Z
```diff
@@ -84,6 +84,9 @@ func (j *KubeflowJob) RestorePodSetsInfo(podSetsInfo []jobframework.PodSetInfo)
 func (j *KubeflowJob) Finished() (metav1.Condition, bool) {
 	var conditionType kftraining.JobConditionType
 	var finished bool
+	if j.KFJobControl.JobStatus() == nil {
+		return metav1.Condition{}, false
+	}
```

  This is a good early return. Thanks.

- **pkg/util/testingjobs/paddlejob/wrappers.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:15:32Z
```diff
@@ -32,6 +32,9 @@ type PaddleJobWrapper struct{ kftraining.PaddleJob }
 
 // MakePaddleJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakePaddleJob(name, ns string) *PaddleJobWrapper {
+	if name == "" && ns == "" {
+		return &PaddleJobWrapper{kftraining.PaddleJob{}}
+	}
```

  I don't think that this is needed only in PaddleJob, PyTorchJob, and XGBoostJob.
  If you think that this is needed, you can open a PR to apply similar changes to all integrations included not KubeflowJobs.
  
  This means that let's revert these lines.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T19:28:49Z

- **pkg/util/testingjobs/paddlejob/wrappers.go:37** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:04:19Z
```diff
@@ -32,6 +32,9 @@ type PaddleJobWrapper struct{ kftraining.PaddleJob }
 
 // MakePaddleJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakePaddleJob(name, ns string) *PaddleJobWrapper {
+	if name == "" && ns == "" {
+		return &PaddleJobWrapper{kftraining.PaddleJob{}}
+	}
```

  I don't think we need this at all.... see my comment in `mxjob_controller_test.go`

- **test/integration/controller/jobs/mxjob/mxjob_controller_test.go:88** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:16:01Z
```diff
@@ -86,219 +84,23 @@ var _ = ginkgo.Describe("Job controller", ginkgo.Ordered, ginkgo.ContinueOnFailu
 	})
 
 	ginkgo.It("Should reconcile MXJobs", func() {
-		ginkgo.By("checking the job gets suspended when created unsuspended")
-		priorityClass := testing.MakePriorityClass(priorityClassName).
-			PriorityValue(int32(priorityValue)).Obj()
-		gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
-
-		job := testingmxjob.MakeMXJob(jobName, ns.Name).PriorityClass(priorityClassName).Obj()
-		err := k8sClient.Create(ctx, job)
-		gomega.Expect(err).To(gomega.Succeed())
-		createdJob := &kftraining.MXJob{}
-
-		gomega.Eventually(func() bool {
-			if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob); err != nil {
-				return false
-			}
-			return createdJob.Spec.RunPolicy.Suspend != nil && *createdJob.Spec.RunPolicy.Suspend
-		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
-
-		ginkgo.By("checking the workload is created without queue assigned")
-		createdWorkload := &kueue.Workload{}
-		gomega.Eventually(func() error {
-			return k8sClient.Get(ctx, wlLookupKey, createdWorkload)
-		}, util.Timeout, util.Interval).Should(gomega.Succeed())
-		gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
-		gomega.Expect(metav1.IsControlledBy(createdWorkload, createdJob)).To(gomega.BeTrue(), "The Workload should be owned by the Job")
-
-		ginkgo.By("checking the workload is created with priority and priorityName")
-		gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
-		gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
-
-		ginkgo.By("checking the workload is updated with queue name when the job does")
-		jobQueueName := "test-queue"
-		createdJob.Annotations = map[string]string{constants.QueueAnnotation: jobQueueName}
-		gomega.Expect(k8sClient.Update(ctx, createdJob)).Should(gomega.Succeed())
-		gomega.Eventually(func() bool {
-			if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
-				return false
-			}
-			return createdWorkload.Spec.QueueName == jobQueueName
-		}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+		kfJob := kubeflowjob.KubeflowJob{KFJobControl: (*workloadmxjob.JobControl)(testingmxjob.MakeMXJob(jobName, ns.Name).Obj())}
+		createdJob := kubeflowjob.KubeflowJob{KFJobControl: (*workloadmxjob.JobControl)(testingmxjob.MakeMXJob("", "").Obj())}
```

  I don't see why we need the wrapper here:
  
  ```suggestion
  		createdJob := kubeflowjob.KubeflowJob{KFJobControl: (*workloadmxjob.JobControl)&kftraining.MXJOb{}}
  ```

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:199** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:25:15Z
```diff
@@ -0,0 +1,251 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+var ReplicaTypeWorker = kftraining.ReplicaType("Worker")
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kubeflowjob.KubeflowJob, ns *corev1.Namespace, wlLookupKey types.NamespacedName, podSetsResources []PodSetsResource) {
+	ginkgo.By("checking the job gets suspended when created unsuspended")
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.KFJobControl.RunPolicy().SchedulingPolicy == nil {
+		job.KFJobControl.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.KFJobControl.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Object())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Object())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Object().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      jobframework.GetWorkloadNameForOwnerWithGVK("second-workload", job.GVK()),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Object(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended() &&
+			len(createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return workerPodSetsCount(createdWorkload, podSetsResources) == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
```

  This comment is beyond the refactoring.
  
  @tenzen-y what are we checking here? Can we use a more explicit comparison about the specific conditions?
  
  ```golang
  gomega.Consistently(func() []kueue.WorkloadCondition {
    ...
  }, duration, interval).Should(gomega.CompareTo([]{...}))
  ```
  
  Do we really need the check to be consistent? That's just going to add runtime without much benefit. Same comment for other Consistent checks above.

- **test/util/util.go:444** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:26:49Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
```

  ```suggestion
  	gomega.EventuallyWithOffset(1, func() bool {
  ```

- **test/util/util.go:460** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:27:10Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
```

  ```suggestion
  		return createdWorkload.Spec.QueueName
  	}, Timeout, Interval).Should(gomega.BeEqual(jobQueueName))
  ```

- **test/util/util.go:454** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:28:10Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
+}
+
+func AwaitAndVerifyCreatedWorkload(ctx context.Context, client client.Client, wlLookupKey types.NamespacedName, createdJob metav1.Object) *kueue.Workload {
+	createdWorkload := &kueue.Workload{}
+	gomega.Eventually(func() error {
```

  use `WithOffset` here and in `Expect` below

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T19:51:13Z

- **pkg/util/testingjobs/paddlejob/wrappers.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T19:51:12Z
```diff
@@ -32,6 +32,9 @@ type PaddleJobWrapper struct{ kftraining.PaddleJob }
 
 // MakePaddleJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakePaddleJob(name, ns string) *PaddleJobWrapper {
+	if name == "" && ns == "" {
+		return &PaddleJobWrapper{kftraining.PaddleJob{}}
+	}
```

  I think so too.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T20:03:34Z

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:199** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:03:34Z
```diff
@@ -0,0 +1,251 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+var ReplicaTypeWorker = kftraining.ReplicaType("Worker")
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kubeflowjob.KubeflowJob, ns *corev1.Namespace, wlLookupKey types.NamespacedName, podSetsResources []PodSetsResource) {
+	ginkgo.By("checking the job gets suspended when created unsuspended")
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.KFJobControl.RunPolicy().SchedulingPolicy == nil {
+		job.KFJobControl.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.KFJobControl.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Object())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Object())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Object().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      jobframework.GetWorkloadNameForOwnerWithGVK("second-workload", job.GVK()),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Object(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended() &&
+			len(createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return workerPodSetsCount(createdWorkload, podSetsResources) == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
```

  IIRC, I copied the test case from https://github.com/kubernetes-sigs/kueue/blob/cacde8bc8566b84d3e113e366bc9cc5c62346750/test/integration/controller/jobs/mpijob/mpijob_controller_test.go#L202-L207 here. So we should refactor all kubeflowjobs and mpijob tests as you suggested.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T20:08:01Z

- **pkg/util/testingjobs/paddlejob/wrappers.go:37** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:08:01Z
```diff
@@ -32,6 +32,9 @@ type PaddleJobWrapper struct{ kftraining.PaddleJob }
 
 // MakePaddleJob creates a wrapper for a suspended job with a single container and parallelism=1.
 func MakePaddleJob(name, ns string) *PaddleJobWrapper {
+	if name == "" && ns == "" {
+		return &PaddleJobWrapper{kftraining.PaddleJob{}}
+	}
```

  I just wanted to say that we should revert these lines.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T20:10:05Z

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:199** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T20:10:05Z
```diff
@@ -0,0 +1,251 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+var ReplicaTypeWorker = kftraining.ReplicaType("Worker")
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kubeflowjob.KubeflowJob, ns *corev1.Namespace, wlLookupKey types.NamespacedName, podSetsResources []PodSetsResource) {
+	ginkgo.By("checking the job gets suspended when created unsuspended")
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.KFJobControl.RunPolicy().SchedulingPolicy == nil {
+		job.KFJobControl.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.KFJobControl.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Object())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Object())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Object().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      jobframework.GetWorkloadNameForOwnerWithGVK("second-workload", job.GVK()),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Object(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended() &&
+			len(createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return workerPodSetsCount(createdWorkload, podSetsResources) == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
```

  We should clean them up. Our suite is taking too long overall #1234

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T20:14:30Z

- **test/integration/controller/jobs/kubeflow/kubeflowjob.go:199** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:14:30Z
```diff
@@ -0,0 +1,251 @@
+/*
+Copyright 2023 The Kubernetes Authors.
+
+Licensed under the Apache License, Version 2.0 (the "License");
+you may not use this file except in compliance with the License.
+You may obtain a copy of the License at
+
+    http://www.apache.org/licenses/LICENSE-2.0
+
+Unless required by applicable law or agreed to in writing, software
+distributed under the License is distributed on an "AS IS" BASIS,
+WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
+See the License for the specific language governing permissions and
+limitations under the License.
+*/
+
+package kubeflow
+
+import (
+	"context"
+	"fmt"
+
+	kftraining "github.com/kubeflow/training-operator/pkg/apis/kubeflow.org/v1"
+	"github.com/onsi/ginkgo/v2"
+	"github.com/onsi/gomega"
+	corev1 "k8s.io/api/core/v1"
+	apimeta "k8s.io/apimachinery/pkg/api/meta"
+	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
+	"k8s.io/apimachinery/pkg/types"
+	"k8s.io/client-go/kubernetes/scheme"
+	"k8s.io/utils/ptr"
+	ctrl "sigs.k8s.io/controller-runtime"
+
+	"sigs.k8s.io/controller-runtime/pkg/client"
+
+	kueue "sigs.k8s.io/kueue/apis/kueue/v1beta1"
+	"sigs.k8s.io/kueue/pkg/controller/constants"
+	"sigs.k8s.io/kueue/pkg/controller/jobframework"
+	"sigs.k8s.io/kueue/pkg/controller/jobs/kubeflow/kubeflowjob"
+	"sigs.k8s.io/kueue/pkg/util/testing"
+
+	"sigs.k8s.io/kueue/test/util"
+)
+
+const (
+	jobName           = "test-job"
+	instanceKey       = "cloud.provider.com/instance"
+	priorityClassName = "test-priority-class"
+	priorityValue     = 10
+	jobQueueName      = "test-queue"
+)
+
+type PodsReadyTestSpec struct {
+	BeforeJobStatus *kftraining.JobStatus
+	BeforeCondition *metav1.Condition
+	JobStatus       kftraining.JobStatus
+	Suspended       bool
+	WantCondition   *metav1.Condition
+}
+
+var ReplicaTypeWorker = kftraining.ReplicaType("Worker")
+
+func ShouldReconcileJob(ctx context.Context, k8sClient client.Client, job, createdJob kubeflowjob.KubeflowJob, ns *corev1.Namespace, wlLookupKey types.NamespacedName, podSetsResources []PodSetsResource) {
+	ginkgo.By("checking the job gets suspended when created unsuspended")
+	priorityClass := testing.MakePriorityClass(priorityClassName).
+		PriorityValue(int32(priorityValue)).Obj()
+	gomega.Expect(k8sClient.Create(ctx, priorityClass)).Should(gomega.Succeed())
+
+	if job.KFJobControl.RunPolicy().SchedulingPolicy == nil {
+		job.KFJobControl.RunPolicy().SchedulingPolicy = &kftraining.SchedulingPolicy{}
+	}
+	job.KFJobControl.RunPolicy().SchedulingPolicy.PriorityClass = priorityClassName
+	err := k8sClient.Create(ctx, job.Object())
+	gomega.Expect(err).To(gomega.Succeed())
+
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, types.NamespacedName{Name: jobName, Namespace: ns.Name}, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is created without queue assigned")
+	createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob.Object())
+	util.VerifyWorkloadPriority(createdWorkload, priorityClassName, priorityValue)
+	gomega.Expect(createdWorkload.Spec.QueueName).Should(gomega.Equal(""), "The Workload shouldn't have .spec.queueName set")
+
+	ginkgo.By("checking the workload is created with priority and priorityName")
+	gomega.Expect(createdWorkload.Spec.PriorityClassName).Should(gomega.Equal(priorityClassName))
+	gomega.Expect(*createdWorkload.Spec.Priority).Should(gomega.Equal(int32(priorityValue)))
+
+	ginkgo.By("checking the workload is updated with queue name when the job does")
+	createdJob.Object().SetAnnotations(map[string]string{constants.QueueAnnotation: jobQueueName})
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	util.AwaitAndVerifyWorkloadQueueName(ctx, k8sClient, createdWorkload, wlLookupKey, jobQueueName)
+
+	ginkgo.By("checking a second non-matching workload is deleted")
+	secondWl := &kueue.Workload{
+		ObjectMeta: metav1.ObjectMeta{
+			Name:      jobframework.GetWorkloadNameForOwnerWithGVK("second-workload", job.GVK()),
+			Namespace: createdWorkload.Namespace,
+		},
+		Spec: *createdWorkload.Spec.DeepCopy(),
+	}
+	gomega.Expect(ctrl.SetControllerReference(createdJob.Object(), secondWl, scheme.Scheme)).Should(gomega.Succeed())
+	secondWl.Spec.PodSets[0].Count += 1
+
+	gomega.Expect(k8sClient.Create(ctx, secondWl)).Should(gomega.Succeed())
+	gomega.Eventually(func() error {
+		wl := &kueue.Workload{}
+		key := types.NamespacedName{Name: secondWl.Name, Namespace: secondWl.Namespace}
+		return k8sClient.Get(ctx, key, wl)
+	}, util.Timeout, util.Interval).Should(testing.BeNotFoundError())
+	// check the original wl is still there
+	gomega.Consistently(func() bool {
+		err := k8sClient.Get(ctx, wlLookupKey, createdWorkload)
+		return err == nil
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job is unsuspended when workload is assigned")
+	onDemandFlavor := testing.MakeResourceFlavor("on-demand").Label(instanceKey, "on-demand").Obj()
+	gomega.Expect(k8sClient.Create(ctx, onDemandFlavor)).Should(gomega.Succeed())
+	spotFlavor := testing.MakeResourceFlavor("spot").Label(instanceKey, "spot").Obj()
+	gomega.Expect(k8sClient.Create(ctx, spotFlavor)).Should(gomega.Succeed())
+	clusterQueue := testing.MakeClusterQueue("cluster-queue").
+		ResourceGroup(
+			*testing.MakeFlavorQuotas("on-demand").Resource(corev1.ResourceCPU, "5").Obj(),
+			*testing.MakeFlavorQuotas("spot").Resource(corev1.ResourceCPU, "5").Obj(),
+		).Obj()
+	admission := testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	lookupKey := types.NamespacedName{Name: jobName, Namespace: ns.Name}
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "Started", corev1.EventTypeNormal, fmt.Sprintf("Admitted by clusterQueue %v", clusterQueue.Name))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return len(createdWorkload.Status.Conditions) == 2
+	}, util.ConsistentDuration, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the job gets suspended when parallelism changes and the added node selectors are removed")
+	parallelism := ptr.Deref(job.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas, 1)
+	newParallelism := parallelism + 1
+	createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Replicas = &newParallelism
+	gomega.Expect(k8sClient.Update(ctx, createdJob.Object())).Should(gomega.Succeed())
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return createdJob.IsSuspended() &&
+			len(createdJob.KFJobControl.ReplicaSpecs()[ReplicaTypeWorker].Template.Spec.NodeSelector) == 0
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Eventually(func() bool {
+		ok, _ := testing.CheckLatestEvent(ctx, k8sClient, "DeletedWorkload", corev1.EventTypeNormal, fmt.Sprintf("Deleted not matching Workload: %v", wlLookupKey.String()))
+		return ok
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+
+	ginkgo.By("checking the workload is updated with new count")
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return workerPodSetsCount(createdWorkload, podSetsResources) == newParallelism
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(createdWorkload.Status.Admission).Should(gomega.BeNil())
+
+	ginkgo.By("checking the job is unsuspended and selectors added when workload is assigned again")
+	admission = testing.MakeAdmission(clusterQueue.Name).PodSets(CreatePodSetAssigment(createdWorkload, podSetsResources)...).Obj()
+	gomega.Expect(util.SetQuotaReservation(ctx, k8sClient, createdWorkload, admission)).Should(gomega.Succeed())
+	util.SyncAdmittedConditionForWorkloads(ctx, k8sClient, createdWorkload)
+	gomega.Eventually(func() bool {
+		if err := k8sClient.Get(ctx, lookupKey, createdJob.Object()); err != nil {
+			return false
+		}
+		return !createdJob.IsSuspended()
+	}, util.Timeout, util.Interval).Should(gomega.BeTrue())
+	gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+	gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[podSetsResources[0].NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(onDemandFlavor.Name))
+	for _, psr := range podSetsResources[1:] {
+		gomega.Expect(len(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector)).Should(gomega.Equal(1))
+		gomega.Expect(createdJob.KFJobControl.ReplicaSpecs()[psr.NodeName].Template.Spec.NodeSelector[instanceKey]).Should(gomega.Equal(spotFlavor.Name))
+	}
+	gomega.Consistently(func() bool {
```

  I agree. Thanks for pointing this out.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-23T08:24:06Z

- **test/util/util.go:460** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-23T08:24:06Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
```

  It asserts no error and jobQueueName

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T14:49:02Z

Just one nit

- **test/util/util.go:460** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T14:47:35Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
```

  Oh right. However, this has the problem that if the result is false, we don't know which of the 2 conditions happened. But we can simplify:
  
  The `Eventually` should be  just for `client.Get`. You can check the queueName outside of `Eventually`, as it's not supposed to change after the Workload is created.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-23T18:44:36Z

- **test/util/util.go:460** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-23T18:44:36Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
```

  I think we need Eventually here, because it is also used after "Update", so Workload is already created but not updated yet and we need to check queue name few times. And I don't think we should split the function to two "Eventually".

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T18:53:57Z

- **test/util/util.go:460** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:53:56Z
```diff
@@ -436,3 +439,27 @@ func SetAdmissionCheckActive(ctx context.Context, k8sClient client.Client, admis
 		return k8sClient.Status().Update(ctx, &updatedAc)
 	}, Timeout, Interval).Should(gomega.Succeed())
 }
+
+func AwaitAndVerifyWorkloadQueueName(ctx context.Context, client client.Client, createdWorkload *kueue.Workload, wlLookupKey types.NamespacedName, jobQueueName string) {
+	gomega.Eventually(func() bool {
+		if err := client.Get(ctx, wlLookupKey, createdWorkload); err != nil {
+			return false
+		}
+		return createdWorkload.Spec.QueueName == jobQueueName
+	}, Timeout, Interval).Should(gomega.BeTrue())
```

  Another approach would be like this: https://github.com/kubernetes-sigs/kueue/blob/9a4c73d1dbbf795e2afd0b29178b95507fc5d98b/test/e2e/e2e_test.go#L239-L243
  
  But this is on the nit side, so let's merge.
