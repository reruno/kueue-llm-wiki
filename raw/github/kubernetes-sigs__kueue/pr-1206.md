# PR #1206: Report Reserving queue metrics and  stats.

**Summary**: Report Reserving queue metrics and  stats.

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1206

**Last updated**: 2023-10-25T17:47:17Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@trasc](https://github.com/trasc)
- **Branch**: `cq-reserved-in-status` → `main`
- **Created**: 2023-10-13T10:24:45Z
- **Updated**: 2023-10-25T17:47:17Z
- **Merged**: 2023-10-20T17:28:47Z
- **Closed**: 2023-10-20T17:28:48Z
- **Labels**: `kind/feature`, `release-note`, `lgtm`, `size/XXL`, `approved`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@tenzen-y](https://github.com/tenzen-y)
- **Requested reviewers**: [@alculquicondor](https://github.com/alculquicondor), [@denkensk](https://github.com/denkensk), [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 33   **+1110 / -357**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?

/kind feature

<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Change the way _localQueues_ and _clusterQueues_ stats and mertics are reported to account for the difference between _Reserving_ and _Admitted_ workloads. 
#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1195 

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
Metrics for usage and reservations in ClusterQueues and LocalQueues.
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-13T10:24:54Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c0e78d0480cd8d2ba161b2376a1fd8bc7bcfbc55 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6532b3bf10a77000083d1754 |

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T16:24:07Z

/lgtm
/approve

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-20T16:24:14Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1206#issuecomment-1773036392" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1206#" title="Author self-approved">trasc</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:27:11Z

/hold

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:29:24Z

/lgtm cancel

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:31:07Z

otherwise lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-20T17:10:10Z

LGTM label has been added.  <details>Git tree hash: 9d29bc2b4356f1a2c0b170037a95056719cffdd2</details>

### Comment by [@trasc](https://github.com/trasc) — 2023-10-20T17:25:37Z

/unhold

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-13T12:56:01Z

I reviewed only APIs.

- **apis/kueue/v1beta1/clusterqueue_types.go:195** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:42:04Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservations are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservations []FlavorUsage `json:"flavorsReservations"`
```

  ```suggestion
  	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
  ```
  
  To use the consistent rule, this form would be better.

- **apis/kueue/v1beta1/clusterqueue_types.go:213** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:46:21Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservations are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservations []FlavorUsage `json:"flavorsReservations"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
 
 	// pendingWorkloads is the number of workloads currently waiting to be
 	// admitted to this clusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
 
+	// reservingdWorkloads is the number of workloads currently reserving quota in this
+	// clusterQueue.
+	// +optional
+	ReservingdWorkloads int32 `json:"quotaReservedWorkloads"`
```

  ```suggestion
  	ReservingWorkloads int32 `json:"reservedWorkloads"`
  ```
  
  Uhm, `quotaReservedWorkloads` sounds a bit redundant.
  
  However, I would like to know what @alculquicondor thinks.

- **apis/kueue/v1beta1/localqueue_types.go:73** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:51:22Z
```diff
@@ -51,13 +56,21 @@ type LocalQueueStatus struct {
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 
-	// flavorUsage are the used quotas, by flavor currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor currently in use by the
+	// workloads assigned to this LocalQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsReservation []LocalQueueFlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor currently in use by the
 	// workloads assigned to this LocalQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorUsage []LocalQueueFlavorUsage `json:"flavorUsage"`
+	FlavorsUsage []LocalQueueFlavorUsage `json:"flavorsUsage"`
```

  ```suggestion
  	FlavorUsage []LocalQueueFlavorUsage `json:"flavorUsage"`
  ```
  
  Oh, we should use the `flavorsUsage`. However, we can not fix this name along with a breaking change since our API version is beta.

- **apis/kueue/v1beta1/localqueue_types.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-13T12:55:40Z
  @trasc, Can you work on the new feature against LocalQueue at a separate PR?
  I think the only minimal change should be included in one PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-16T10:06:58Z

- **apis/kueue/v1beta1/localqueue_types.go:1** by [@trasc](https://github.com/trasc) — 2023-10-16T10:06:58Z
  We could do it like that, but the changes related to local queue are not that many and some comments might be relevant for both (local and cluster queue).

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-18T19:43:50Z

- **apis/kueue/v1beta1/clusterqueue_types.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:09:58Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservations are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservations []FlavorUsage `json:"flavorsReservations"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
 
 	// pendingWorkloads is the number of workloads currently waiting to be
 	// admitted to this clusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
 
+	// reservingdWorkloads is the number of workloads currently reserving quota in this
+	// clusterQueue.
+	// +optional
+	ReservingdWorkloads int32 `json:"quotaReservedWorkloads"`
```

  `reservingWorkloads` sgtm.

- **apis/kueue/v1beta1/localqueue_types.go:1** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:10:23Z
  I'm fine either way

- **apis/kueue/v1beta1/localqueue_types.go:73** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:12:04Z
```diff
@@ -51,13 +56,21 @@ type LocalQueueStatus struct {
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 
-	// flavorUsage are the used quotas, by flavor currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor currently in use by the
+	// workloads assigned to this LocalQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsReservation []LocalQueueFlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor currently in use by the
 	// workloads assigned to this LocalQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorUsage []LocalQueueFlavorUsage `json:"flavorUsage"`
+	FlavorsUsage []LocalQueueFlavorUsage `json:"flavorsUsage"`
```

  True :(
  I added it to the wishlist #768

- **pkg/cache/cache.go:334** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:14:30Z
```diff
@@ -314,11 +328,13 @@ func (c *Cache) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) err
 	for _, q := range queues.Items {
 		qKey := queueKey(&q)
 		qImpl := &queue{
-			key:               qKey,
-			admittedWorkloads: 0,
-			usage:             make(FlavorResourceQuantities),
+			key:                qKey,
+			reservingWorkloads: 0,
+			admittedWorkloads:  0,
+			usage:              make(FlavorResourceQuantities),
```

  Are you keeping `usage` to mean the reserved quota?
  If you are doing this to minimize the diff, that's ok, but please add a comment in the queue struct, with a TODO to rename in the future.

- **pkg/cache/cache.go:574** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:17:38Z
```diff
@@ -554,8 +570,21 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 	return nil
 }
 
-// Usage reports the used resources and number of workloads admitted by the ClusterQueue.
-func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
+// ReservationStats reports the reserved resources and number of workloads holding them in the ClusterQueue.
+func (c *Cache) ReservationStats(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
```

  We should return all the values (usage, reservations and their counters) under the same lock.
  Otherwise they might return state that are incompatible.
  
  I would return a struct that contains the 4 fields, as opposed to returning 4 values, which can be confusing from a readability perspective.

- **pkg/cache/cache.go:635** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:17:58Z
```diff
@@ -594,7 +627,23 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, erro
 			usage = append(usage, outFlvUsage)
 		}
 	}
-	return usage, len(cq.Workloads), nil
+	return usage
+}
+
+func (c *Cache) LocalQueueReservations(qObj *kueue.LocalQueue) ([]kueue.LocalQueueFlavorUsage, error) {
+	c.RLock()
+	defer c.RUnlock()
```

  Same comment about the lock

- **pkg/cache/clusterqueue.go:362** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:27:35Z
```diff
@@ -341,18 +347,42 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	// we only increase the AllocatableResourceGeneration cause the add of workload won't make more
 	// workloads fit in ClusterQueue.
 	c.AllocatableResourceGeneration++
+
 	delete(c.Workloads, k)
-	reportAdmittedActiveWorkloads(wi.ClusterQueue, len(c.Workloads))
+	c.reportActiveWorkloads()
+}
+
+func (c *ClusterQueue) reportActiveWorkloads() {
+	metrics.AdmittedActiveWorkloads.WithLabelValues(c.Name).Set(float64(c.admittedWorkloadsCount()))
+	metrics.ReservingActiveWorkloads.WithLabelValues(c.Name).Set(float64(len(c.Workloads)))
+}
+
+func (c *ClusterQueue) admittedWorkloadsCount() int {
+	count := 0
+	for _, wi := range c.Workloads {
```

  keep a counter instead
  Otherwise, for each add/remove Workload, we are iterating over all workloads.

- **pkg/cache/clusterqueue.go:365** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:31:54Z
```diff
@@ -341,18 +347,42 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	// we only increase the AllocatableResourceGeneration cause the add of workload won't make more
 	// workloads fit in ClusterQueue.
 	c.AllocatableResourceGeneration++
+
 	delete(c.Workloads, k)
-	reportAdmittedActiveWorkloads(wi.ClusterQueue, len(c.Workloads))
+	c.reportActiveWorkloads()
+}
+
+func (c *ClusterQueue) reportActiveWorkloads() {
+	metrics.AdmittedActiveWorkloads.WithLabelValues(c.Name).Set(float64(c.admittedWorkloadsCount()))
+	metrics.ReservingActiveWorkloads.WithLabelValues(c.Name).Set(float64(len(c.Workloads)))
+}
+
+func (c *ClusterQueue) admittedWorkloadsCount() int {
+	count := 0
+	for _, wi := range c.Workloads {
+		if workload.IsAdmitted(wi.Obj) {
+			count++
+		}
+	}
+	return count
 }
 
 // updateWorkloadUsage updates the usage of the ClusterQueue for the workload
 // and the number of admitted workloads for local queues.
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
+	admitted := workload.IsAdmitted(wi.Obj)
```

  You could increase a counter here, based on the value of `m`.

- **pkg/controller/core/localqueue_controller.go:275** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:42:57Z
```diff
@@ -272,14 +272,21 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 		r.log.Error(err, failedUpdateLqStatusMsg)
 		return err
 	}
+	resservations, err := r.cache.LocalQueueReservations(queue)
```

  reservations

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-19T04:53:00Z

- **apis/kueue/v1beta1/localqueue_types.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-19T04:53:00Z
  > We could do it like that, but the changes related to local queue are not that many and some comments might be relevant for both (local and cluster queue).
  
  I see. Thanks for clarifying. I'm ok with working on extending lq and cq in this PR.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-19T09:51:00Z

- **apis/kueue/v1beta1/clusterqueue_types.go:195** by [@trasc](https://github.com/trasc) — 2023-10-19T07:59:12Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservations are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservations []FlavorUsage `json:"flavorsReservations"`
```

  done

- **apis/kueue/v1beta1/clusterqueue_types.go:213** by [@trasc](https://github.com/trasc) — 2023-10-19T07:59:54Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservations are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservations []FlavorUsage `json:"flavorsReservations"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
 
 	// pendingWorkloads is the number of workloads currently waiting to be
 	// admitted to this clusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
 
+	// reservingdWorkloads is the number of workloads currently reserving quota in this
+	// clusterQueue.
+	// +optional
+	ReservingdWorkloads int32 `json:"quotaReservedWorkloads"`
```

  done

- **apis/kueue/v1beta1/localqueue_types.go:73** by [@trasc](https://github.com/trasc) — 2023-10-19T08:01:43Z
```diff
@@ -51,13 +56,21 @@ type LocalQueueStatus struct {
 	// +patchMergeKey=type
 	Conditions []metav1.Condition `json:"conditions,omitempty" patchStrategy:"merge" patchMergeKey:"type"`
 
-	// flavorUsage are the used quotas, by flavor currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor currently in use by the
+	// workloads assigned to this LocalQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsReservation []LocalQueueFlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor currently in use by the
 	// workloads assigned to this LocalQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorUsage []LocalQueueFlavorUsage `json:"flavorUsage"`
+	FlavorsUsage []LocalQueueFlavorUsage `json:"flavorsUsage"`
```

  done

- **pkg/cache/cache.go:334** by [@trasc](https://github.com/trasc) — 2023-10-19T08:07:27Z
```diff
@@ -314,11 +328,13 @@ func (c *Cache) AddClusterQueue(ctx context.Context, cq *kueue.ClusterQueue) err
 	for _, q := range queues.Items {
 		qKey := queueKey(&q)
 		qImpl := &queue{
-			key:               qKey,
-			admittedWorkloads: 0,
-			usage:             make(FlavorResourceQuantities),
+			key:                qKey,
+			reservingWorkloads: 0,
+			admittedWorkloads:  0,
+			usage:              make(FlavorResourceQuantities),
```

  yes and done

- **pkg/cache/cache.go:574** by [@trasc](https://github.com/trasc) — 2023-10-19T08:44:51Z
```diff
@@ -554,8 +570,21 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 	return nil
 }
 
-// Usage reports the used resources and number of workloads admitted by the ClusterQueue.
-func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
+// ReservationStats reports the reserved resources and number of workloads holding them in the ClusterQueue.
+func (c *Cache) ReservationStats(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
```

  done

- **pkg/cache/cache.go:635** by [@trasc](https://github.com/trasc) — 2023-10-19T09:17:58Z
```diff
@@ -594,7 +627,23 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, erro
 			usage = append(usage, outFlvUsage)
 		}
 	}
-	return usage, len(cq.Workloads), nil
+	return usage
+}
+
+func (c *Cache) LocalQueueReservations(qObj *kueue.LocalQueue) ([]kueue.LocalQueueFlavorUsage, error) {
+	c.RLock()
+	defer c.RUnlock()
```

  done

- **pkg/cache/clusterqueue.go:362** by [@trasc](https://github.com/trasc) — 2023-10-19T09:37:24Z
```diff
@@ -341,18 +347,42 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	// we only increase the AllocatableResourceGeneration cause the add of workload won't make more
 	// workloads fit in ClusterQueue.
 	c.AllocatableResourceGeneration++
+
 	delete(c.Workloads, k)
-	reportAdmittedActiveWorkloads(wi.ClusterQueue, len(c.Workloads))
+	c.reportActiveWorkloads()
+}
+
+func (c *ClusterQueue) reportActiveWorkloads() {
+	metrics.AdmittedActiveWorkloads.WithLabelValues(c.Name).Set(float64(c.admittedWorkloadsCount()))
+	metrics.ReservingActiveWorkloads.WithLabelValues(c.Name).Set(float64(len(c.Workloads)))
+}
+
+func (c *ClusterQueue) admittedWorkloadsCount() int {
+	count := 0
+	for _, wi := range c.Workloads {
```

  done

- **pkg/cache/clusterqueue.go:365** by [@trasc](https://github.com/trasc) — 2023-10-19T09:37:34Z
```diff
@@ -341,18 +347,42 @@ func (c *ClusterQueue) deleteWorkload(w *kueue.Workload) {
 	// we only increase the AllocatableResourceGeneration cause the add of workload won't make more
 	// workloads fit in ClusterQueue.
 	c.AllocatableResourceGeneration++
+
 	delete(c.Workloads, k)
-	reportAdmittedActiveWorkloads(wi.ClusterQueue, len(c.Workloads))
+	c.reportActiveWorkloads()
+}
+
+func (c *ClusterQueue) reportActiveWorkloads() {
+	metrics.AdmittedActiveWorkloads.WithLabelValues(c.Name).Set(float64(c.admittedWorkloadsCount()))
+	metrics.ReservingActiveWorkloads.WithLabelValues(c.Name).Set(float64(len(c.Workloads)))
+}
+
+func (c *ClusterQueue) admittedWorkloadsCount() int {
+	count := 0
+	for _, wi := range c.Workloads {
+		if workload.IsAdmitted(wi.Obj) {
+			count++
+		}
+	}
+	return count
 }
 
 // updateWorkloadUsage updates the usage of the ClusterQueue for the workload
 // and the number of admitted workloads for local queues.
 func (c *ClusterQueue) updateWorkloadUsage(wi *workload.Info, m int64) {
+	admitted := workload.IsAdmitted(wi.Obj)
```

  done

- **pkg/controller/core/localqueue_controller.go:275** by [@trasc](https://github.com/trasc) — 2023-10-19T09:38:43Z
```diff
@@ -272,14 +272,21 @@ func (r *LocalQueueReconciler) UpdateStatusIfChanged(
 		r.log.Error(err, failedUpdateLqStatusMsg)
 		return err
 	}
+	resservations, err := r.cache.LocalQueueReservations(queue)
```

  removed

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T15:21:47Z

a few typos

- **apis/kueue/v1beta1/clusterqueue_types.go:213** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:15:00Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
 
 	// pendingWorkloads is the number of workloads currently waiting to be
 	// admitted to this clusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
 
+	// reservingdWorkloads is the number of workloads currently reserving quota in this
+	// clusterQueue.
+	// +optional
+	ReservingdWorkloads int32 `json:"reservingdWorkloads"`
```

  ```suggestion
  	ReservingWorkloads int32 `json:"reservingWorkloads"`
  ```

- **pkg/cache/cache.go:574** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:16:53Z
```diff
@@ -554,20 +571,36 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 	return nil
 }
 
-// Usage reports the used resources and number of workloads admitted by the ClusterQueue.
-func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
+type LusterQueueUsageStats struct {
```

  ```suggestion
  type ClusterQueueUsageStats struct {
  ```

- **pkg/cache/cache.go:633** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:18:07Z
```diff
@@ -594,26 +627,38 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, erro
 			usage = append(usage, outFlvUsage)
 		}
 	}
-	return usage, len(cq.Workloads), nil
+	return usage
+}
+
+type LocakQueueUsageStats struct {
```

  ```suggestion
  type LocalQueueUsageStats struct {
  ```

- **pkg/cache/cache.go:633** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:18:32Z
```diff
@@ -594,26 +627,38 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, erro
 			usage = append(usage, outFlvUsage)
 		}
 	}
-	return usage, len(cq.Workloads), nil
+	return usage
+}
+
+type LocakQueueUsageStats struct {
```

  Let's put the counts in here

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T07:31:18Z

- **apis/kueue/v1beta1/clusterqueue_types.go:213** by [@trasc](https://github.com/trasc) — 2023-10-20T07:19:09Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
 
 	// pendingWorkloads is the number of workloads currently waiting to be
 	// admitted to this clusterQueue.
 	// +optional
 	PendingWorkloads int32 `json:"pendingWorkloads"`
 
+	// reservingdWorkloads is the number of workloads currently reserving quota in this
+	// clusterQueue.
+	// +optional
+	ReservingdWorkloads int32 `json:"reservingdWorkloads"`
```

  done

- **pkg/cache/cache.go:574** by [@trasc](https://github.com/trasc) — 2023-10-20T07:28:00Z
```diff
@@ -554,20 +571,36 @@ func (c *Cache) ForgetWorkload(w *kueue.Workload) error {
 	return nil
 }
 
-// Usage reports the used resources and number of workloads admitted by the ClusterQueue.
-func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, error) {
+type LusterQueueUsageStats struct {
```

  done

- **pkg/cache/cache.go:633** by [@trasc](https://github.com/trasc) — 2023-10-20T07:28:16Z
```diff
@@ -594,26 +627,38 @@ func (c *Cache) Usage(cqObj *kueue.ClusterQueue) ([]kueue.FlavorUsage, int, erro
 			usage = append(usage, outFlvUsage)
 		}
 	}
-	return usage, len(cq.Workloads), nil
+	return usage
+}
+
+type LocakQueueUsageStats struct {
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T16:29:11Z

- **apis/kueue/v1beta1/clusterqueue_types.go:203** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T16:29:10Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
```

  ```suggestion
  	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
  ```
  
  We shouldn't break the existing API.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T16:38:25Z

- **apis/kueue/v1beta1/clusterqueue_types.go:203** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T16:38:25Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
```

  Oops, missed it thanks.
  Yes, please revert

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T17:07:49Z

- **apis/kueue/v1beta1/clusterqueue_types.go:203** by [@trasc](https://github.com/trasc) — 2023-10-20T17:07:49Z
```diff
@@ -186,19 +186,32 @@ type ResourceFlavorReference string
 
 // ClusterQueueStatus defines the observed state of ClusterQueue
 type ClusterQueueStatus struct {
-	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// flavorsReservation are the reserved quotas, by flavor, currently in use by the
 	// workloads assigned to this ClusterQueue.
 	// +listType=map
 	// +listMapKey=name
 	// +kubebuilder:validation:MaxItems=16
 	// +optional
-	FlavorsUsage []FlavorUsage `json:"flavorsUsage"`
+	FlavorsReservation []FlavorUsage `json:"flavorsReservation"`
+
+	// flavorsUsage are the used quotas, by flavor, currently in use by the
+	// workloads admitted in this ClusterQueue.
+	// +listType=map
+	// +listMapKey=name
+	// +kubebuilder:validation:MaxItems=16
+	// +optional
+	FlavorsUsage []FlavorUsage `json:"flavorUsage"`
```

  done

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T17:10:05Z

/lgtm
