# PR #1220: Validating admissionchecks

**Summary**: Validating admissionchecks

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1220

**Last updated**: 2024-05-23T09:59:13Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@stuton](https://github.com/stuton)
- **Branch**: `validate-workload-podsetupdates` → `main`
- **Created**: 2023-10-18T10:05:21Z
- **Updated**: 2024-05-23T09:59:13Z
- **Merged**: 2023-10-24T19:23:15Z
- **Closed**: 2023-10-24T19:23:15Z
- **Labels**: `kind/feature`, `size/L`, `lgtm`, `release-note-none`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@kerthcet](https://github.com/kerthcet)
- **Changed files**: 2   **+244 / -0**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind bug
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind documentation
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
Validate PodSetUpdates field to avoid errors during workload admission

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->
Fixes #1187

#### Special notes for your reviewer:

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
NONE
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-18T10:05:28Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | c7ea96df0e93e5b850b0cef5e6b73dc80a3ffaa1 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/6537bda3e526230008b54ddf |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1220--kubernetes-sigs-kueue.netlify.app](https://deploy-preview-1220--kubernetes-sigs-kueue.netlify.app) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEyMjAtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.EdRd_pj0o5TtbjcD13iAPi3wqwhDzWTEuo6_P_L3Q-s)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-18T10:05:31Z

Hi @stuton. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-18T11:26:51Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T14:32:35Z

/assign @mimowo

### Comment by [@trasc](https://github.com/trasc) — 2023-10-19T11:40:46Z

@stuton , @mimowo  should we also make sure that the `PodSetUpdates` of a admission check  is not changed when
`oldAC.State == newAC.State == Ready`

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-19T11:53:39Z

> @stuton , @mimowo should we also make sure that the PodSetUpdates of a admission check is not changed when
oldAC.State == newAC.State == Ready

I think so, it's easier to relax if needed, than make validation strong later.

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T17:01:53Z

@stuton @mimowo Is this bugs? It looks like `cleanup` or `feature` request.

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T17:27:37Z

/remove-kind bug
/kind feature

### Comment by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T17:44:08Z

otherwise lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T18:57:46Z

LGTM label has been added.  <details>Git tree hash: c4b53789967d7ecbcaeea5badcdae0eb511cfc4f</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T18:57:46Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1220#pullrequestreview-1695674074" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1220#" title="Author self-approved">stuton</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[pkg/webhooks/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/pkg/webhooks/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-25T17:49:35Z

/release-note-edit
```release-note
NONE
```
Because it's a continuation to #1045 and #1180

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T14:45:38Z

- **pkg/webhooks/workload_webhook.go:200** by [@mimowo](https://github.com/mimowo) — 2023-10-19T14:25:57Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
```

  Use `sets.Set[string]`  to represent the pod set names

- **pkg/webhooks/workload_webhook.go:201** by [@mimowo](https://github.com/mimowo) — 2023-10-19T14:27:11Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
```

  No need for this. If you use sets, you can convert them to list with `sets.List`.

- **pkg/webhooks/workload_webhook.go:241** by [@mimowo](https://github.com/mimowo) — 2023-10-19T14:38:41Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSetNames))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
+	}
+	return allErrs
+}
+
+// validateTolerations is extracted from git.k8s.io/kubernetes/pkg/apis/core/validation/validation.go
+func validateTolerations(tolerations []corev1.Toleration, fldPath *field.Path) field.ErrorList {
```

  Oh, I don't really like this but found we have also other copy-paste places like this. https://github.com/kubernetes-sigs/kueue/blob/525098b838eb28ee241cf1aaee5e53f161d30a0c/pkg/webhooks/resourceflavor_webhook.go#L134. Is there no way to depend on the `git.k8s.io/kubernetes/pkg/apis/core/validation/` package?

- **pkg/webhooks/workload_webhook_test.go:237** by [@mimowo](https://github.com/mimowo) — 2023-10-19T14:41:26Z
```diff
@@ -232,6 +234,35 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should have a valid podSetUpdate name": {
```

  Please also add tests which verify the set of names in `podSetUpdates` to be not matching these in `podSets`

- **pkg/webhooks/workload_webhook.go:213** by [@mimowo](https://github.com/mimowo) — 2023-10-19T14:43:35Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
```

  I think currently the implementation only works then the set in podSets equals the set in podSetUpdates. Not sure this is expected. @stuton please confirm.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-19T16:14:20Z

- **pkg/webhooks/workload_webhook.go:241** by [@stuton](https://github.com/stuton) — 2023-10-19T16:14:19Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSetNames))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
+	}
+	return allErrs
+}
+
+// validateTolerations is extracted from git.k8s.io/kubernetes/pkg/apis/core/validation/validation.go
+func validateTolerations(tolerations []corev1.Toleration, fldPath *field.Path) field.ErrorList {
```

  As I know, libraries are published as standalone modules and can be referenced directly. I've never met validation library as standalone module. We can consider depending on k8s.io/kubernetes directly but it's not recommended: https://github.com/kubernetes/kubernetes/issues/79384#issuecomment-505627280

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T16:20:06Z

- **pkg/webhooks/workload_webhook.go:241** by [@mimowo](https://github.com/mimowo) — 2023-10-19T16:20:06Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSetNames))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
+	}
+	return allErrs
+}
+
+// validateTolerations is extracted from git.k8s.io/kubernetes/pkg/apis/core/validation/validation.go
+func validateTolerations(tolerations []corev1.Toleration, fldPath *field.Path) field.ErrorList {
```

  Ok, I would suggest adding the link to the comment so that we have a pointer.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-19T16:20:46Z

- **pkg/webhooks/workload_webhook.go:213** by [@stuton](https://github.com/stuton) — 2023-10-19T16:20:46Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
```

  when the names of podSetUpdates don't equal the names of podSets we will provide "notsupported" error

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-19T16:28:01Z

- **pkg/webhooks/workload_webhook.go:241** by [@stuton](https://github.com/stuton) — 2023-10-19T16:28:00Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSetNames))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
+	}
+	return allErrs
+}
+
+// validateTolerations is extracted from git.k8s.io/kubernetes/pkg/apis/core/validation/validation.go
+func validateTolerations(tolerations []corev1.Toleration, fldPath *field.Path) field.ErrorList {
```

  added comment https://github.com/kubernetes-sigs/kueue/pull/1220/commits/dd3dde19094c7988fcd3440d59aaf40d1dafee6b

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-19T16:28:20Z

- **pkg/webhooks/workload_webhook.go:201** by [@stuton](https://github.com/stuton) — 2023-10-19T16:28:20Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
```

  done https://github.com/kubernetes-sigs/kueue/pull/1220/commits/dd3dde19094c7988fcd3440d59aaf40d1dafee6b

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T16:33:37Z

- **pkg/webhooks/workload_webhook.go:213** by [@mimowo](https://github.com/mimowo) — 2023-10-19T16:33:37Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(map[string]struct{}, len(obj.Spec.PodSets))
+	knowPodSetNames := make([]string, len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets[name] = struct{}{}
+		knowPodSetNames = append(knowPodSetNames, name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
+		if _, found := knowPodSets[psu.Name]; !found {
```

  IIUC currently in code you only check that every name in podSetUpdates is in known set. So you are not checking equality of the sets, but you are checking that the set of names is podSetUpdates is a subset of the names in podSets.

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-20T11:52:19Z

- **pkg/webhooks/workload_webhook.go:204** by [@trasc](https://github.com/trasc) — 2023-10-20T11:41:44Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(sets.Set[string], len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets.Insert(name)
+	}
```

  I think  this can be replace with something like
  ```suggestion
  	knowPodSets := sets.New(slices.Map(obj.Spec.PodSets,{// return name here})...)
  ```
  and maybe do it only once it the caller function

- **pkg/webhooks/workload_webhook.go:210** by [@trasc](https://github.com/trasc) — 2023-10-20T11:45:10Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(sets.Set[string], len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets.Insert(name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
```

  We don't really care if it's `IsDNS1123Label` as long as the name is found in the list of podsets.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-20T12:25:02Z

- **pkg/webhooks/workload_webhook.go:210** by [@stuton](https://github.com/stuton) — 2023-10-20T12:25:02Z
```diff
@@ -182,6 +184,87 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := make(sets.Set[string], len(obj.Spec.PodSets))
+	for i := range obj.Spec.PodSets {
+		name := obj.Spec.PodSets[i].Name
+		knowPodSets.Insert(name)
+	}
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		for _, msg := range validation.IsDNS1123Label(psu.Name) {
+			allErrs = append(allErrs, field.Invalid(psuPath.Child("name"), psu.Name, msg))
+		}
```

  removed https://github.com/kubernetes-sigs/kueue/pull/1220/commits/0943f326b3dc45a32e58f6899a8bbf4b755e07db

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-20T12:25:16Z

- **pkg/webhooks/workload_webhook_test.go:237** by [@stuton](https://github.com/stuton) — 2023-10-20T12:25:16Z
```diff
@@ -232,6 +234,35 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should have a valid podSetUpdate name": {
```

  added https://github.com/kubernetes-sigs/kueue/pull/1220/commits/0943f326b3dc45a32e58f6899a8bbf4b755e07db

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T17:43:43Z

- **pkg/webhooks/workload_webhook.go:211** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T17:25:47Z
```diff
@@ -182,6 +185,103 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		if len(ac.PodSetUpdates) > 0 && (len(ac.PodSetUpdates) != len(obj.Spec.PodSets)) {
+			allErrs = append(allErrs, field.Invalid(admissionChecksPath.Child("podSetUpdates"), field.OmitValueType{}, "must have the same number of podSetUpdates as the podSets"))
+		}
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := sets.New(slices.Map(obj.Spec.PodSets, func(ps *kueue.PodSet) string {
+		return ps.Name
+	})...)
+
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		if found := knowPodSets.Has(psu.Name); !found {
```

  ```suggestion
  		if !knowPodSets.Has(psu.Name) {
  ```

- **pkg/webhooks/workload_webhook.go:217** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T17:40:28Z
```diff
@@ -182,6 +185,103 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		if len(ac.PodSetUpdates) > 0 && (len(ac.PodSetUpdates) != len(obj.Spec.PodSets)) {
+			allErrs = append(allErrs, field.Invalid(admissionChecksPath.Child("podSetUpdates"), field.OmitValueType{}, "must have the same number of podSetUpdates as the podSets"))
+		}
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := sets.New(slices.Map(obj.Spec.PodSets, func(ps *kueue.PodSet) string {
+		return ps.Name
+	})...)
+
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		if found := knowPodSets.Has(psu.Name); !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
```

  Can we validate nodeSelector, as well?
  
  https://github.com/kubernetes/kubernetes/blob/57d3cc66050545d7fefb0841e750a0a41df14144/pkg/apis/core/validation/validation.go#L4218-L4219

- **pkg/webhooks/workload_webhook_test.go:1** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T17:43:36Z
  Can you update the valid case?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T18:24:15Z

- **pkg/webhooks/workload_webhook.go:193** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T18:06:24Z
```diff
@@ -182,6 +185,103 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		if len(ac.PodSetUpdates) > 0 && (len(ac.PodSetUpdates) != len(obj.Spec.PodSets)) {
```

  nit
  ```suggestion
  		if len(ac.PodSetUpdates) > 0 && len(ac.PodSetUpdates) != len(obj.Spec.PodSets) {
  ```

- **pkg/webhooks/workload_webhook.go:217** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T18:11:40Z
```diff
@@ -182,6 +185,103 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		if len(ac.PodSetUpdates) > 0 && (len(ac.PodSetUpdates) != len(obj.Spec.PodSets)) {
+			allErrs = append(allErrs, field.Invalid(admissionChecksPath.Child("podSetUpdates"), field.OmitValueType{}, "must have the same number of podSetUpdates as the podSets"))
+		}
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := sets.New(slices.Map(obj.Spec.PodSets, func(ps *kueue.PodSet) string {
+		return ps.Name
+	})...)
+
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		if found := knowPodSets.Has(psu.Name); !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
```

  The nodeSelector is much simpler than that. We can just use `ValidateLabels` again.

- **pkg/webhooks/workload_webhook_test.go:244** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T18:22:13Z
```diff
@@ -232,6 +234,48 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should podSetUpdates have the same number of podSets": {
```

  Add one case for empty podSetUpdates, as that's valid

- **pkg/webhooks/workload_webhook_test.go:571** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T18:23:03Z
```diff
@@ -466,6 +510,25 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 				field.Invalid(field.NewPath("spec").Child("priorityClassName"), nil, ""),
 			},
 		},
+		"podSetUpdates should be immutable when state is ready": {
```

  Add a case for updates in other fields where the podSetUpdates don't change

- **pkg/webhooks/workload_webhook_test.go:248** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T18:23:55Z
```diff
@@ -232,6 +234,48 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should podSetUpdates have the same number of podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(podSetUpdatePath, nil, "must have the same number of podSetUpdates as the podSets"),
+			},
+		},
+		"should the set of names in podSetUpdates to be matching with podSets": {
```

  add a case where they match

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T18:27:21Z

- **pkg/webhooks/workload_webhook.go:217** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:27:21Z
```diff
@@ -182,6 +185,103 @@ func validateContainer(c *corev1.Container, path *field.Path) field.ErrorList {
 	return allErrs
 }
 
+func validateAdmissionChecks(obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+	for i := range obj.Status.AdmissionChecks {
+		admissionChecksPath := basePath.Index(i)
+		ac := &obj.Status.AdmissionChecks[i]
+		if len(ac.PodSetUpdates) > 0 && (len(ac.PodSetUpdates) != len(obj.Spec.PodSets)) {
+			allErrs = append(allErrs, field.Invalid(admissionChecksPath.Child("podSetUpdates"), field.OmitValueType{}, "must have the same number of podSetUpdates as the podSets"))
+		}
+		allErrs = append(allErrs, validatePodSetUpdates(ac, obj, admissionChecksPath.Child("podSetUpdates"))...)
+	}
+	return allErrs
+}
+
+func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload, basePath *field.Path) field.ErrorList {
+	var allErrs field.ErrorList
+
+	knowPodSets := sets.New(slices.Map(obj.Spec.PodSets, func(ps *kueue.PodSet) string {
+		return ps.Name
+	})...)
+
+	for i := range acs.PodSetUpdates {
+		psu := &acs.PodSetUpdates[i]
+		psuPath := basePath.Index(i)
+		if found := knowPodSets.Has(psu.Name); !found {
+			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
+		}
+		allErrs = append(allErrs, validateTolerations(psu.Tolerations, psuPath.Child("tolerations"))...)
+		allErrs = append(allErrs, apivalidation.ValidateAnnotations(psu.Annotations, psuPath.Child("annotations"))...)
+		allErrs = append(allErrs, metav1validation.ValidateLabels(psu.Labels, psuPath.Child("labels"))...)
```

  It makes sense. We don't need to validate `operator`.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-23T09:41:23Z

- **pkg/webhooks/workload_webhook_test.go:571** by [@stuton](https://github.com/stuton) — 2023-10-23T09:41:23Z
```diff
@@ -466,6 +510,25 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 				field.Invalid(field.NewPath("spec").Child("priorityClassName"), nil, ""),
 			},
 		},
+		"podSetUpdates should be immutable when state is ready": {
```

  IIUC this test specifically checks for changes in the label field when `podsetupdates` must be immutable. You'd like to see a test that allows us to change fields?

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-23T10:09:41Z

- **pkg/webhooks/workload_webhook_test.go:244** by [@stuton](https://github.com/stuton) — 2023-10-23T10:09:40Z
```diff
@@ -232,6 +234,48 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should podSetUpdates have the same number of podSets": {
```

  added https://github.com/kubernetes-sigs/kueue/pull/1220/commits/7799023ffef622e04d748162ae003fbf416f5ac9

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-23T10:10:45Z

- **pkg/webhooks/workload_webhook_test.go:1** by [@stuton](https://github.com/stuton) — 2023-10-23T10:10:45Z
  added case with checking nodeselector https://github.com/kubernetes-sigs/kueue/pull/1220/commits/7799023ffef622e04d748162ae003fbf416f5ac9

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-23T10:11:30Z

- **pkg/webhooks/workload_webhook_test.go:248** by [@stuton](https://github.com/stuton) — 2023-10-23T10:11:29Z
```diff
@@ -232,6 +234,48 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should podSetUpdates have the same number of podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(podSetUpdatePath, nil, "must have the same number of podSetUpdates as the podSets"),
+			},
+		},
+		"should the set of names in podSetUpdates to be matching with podSets": {
```

  added https://github.com/kubernetes-sigs/kueue/pull/1220/commits/7799023ffef622e04d748162ae003fbf416f5ac9

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-23T10:21:02Z

- **pkg/webhooks/workload_webhook.go:212** by [@mimowo](https://github.com/mimowo) — 2023-10-23T10:18:14Z
```diff
@@ -208,28 +208,28 @@ func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload,
 	for i := range acs.PodSetUpdates {
 		psu := &acs.PodSetUpdates[i]
 		psuPath := basePath.Index(i)
-		if found := knowPodSets.Has(psu.Name); !found {
+		if !knowPodSets.Has(psu.Name) {
 			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
```

  nit: I would suggest to return the list sorted. Returning unsorted could add something to remember about in integration tests.

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-23T10:23:52Z

- **pkg/webhooks/workload_webhook.go:212** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-23T10:23:52Z
```diff
@@ -208,28 +208,28 @@ func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload,
 	for i := range acs.PodSetUpdates {
 		psu := &acs.PodSetUpdates[i]
 		psuPath := basePath.Index(i)
-		if found := knowPodSets.Has(psu.Name); !found {
+		if !knowPodSets.Has(psu.Name) {
 			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
```

  +1
  
  We can use the `sets.List()` function.

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-23T10:33:07Z

- **pkg/webhooks/workload_webhook.go:212** by [@stuton](https://github.com/stuton) — 2023-10-23T10:33:07Z
```diff
@@ -208,28 +208,28 @@ func validatePodSetUpdates(acs *kueue.AdmissionCheckState, obj *kueue.Workload,
 	for i := range acs.PodSetUpdates {
 		psu := &acs.PodSetUpdates[i]
 		psuPath := basePath.Index(i)
-		if found := knowPodSets.Has(psu.Name); !found {
+		if !knowPodSets.Has(psu.Name) {
 			allErrs = append(allErrs, field.NotSupported(psuPath.Child("name"), psu.Name, knowPodSets.UnsortedList()))
```

  updated https://github.com/kubernetes-sigs/kueue/pull/1220/commits/b219249d415c88de69843728c446aadc55cc43ea

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-23T14:00:16Z

- **pkg/webhooks/workload_webhook_test.go:268** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-23T13:59:34Z
```diff
@@ -232,6 +234,70 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"empty podSetUpdates": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).AdmissionChecks(kueue.AdmissionCheckState{}).Obj(),
+			wantErr:  nil,
+		},
+		"should podSetUpdates have the same number of podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(podSetUpdatePath, nil, "must have the same number of podSetUpdates as the podSets"),
+			},
+		},
+		"mismatched names in podSetUpdates with names in podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}, {Name: "third"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.NotSupported(firstAdmissionChecksPath.Child("podSetUpdates").Index(1).Child("name"), nil, nil),
+			},
+		},
+		"matched names in podSetUpdates with names in podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}, {Name: "second"}}},
```

  Can we add valid values such as annotations, tolerations, and so on?

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T17:22:20Z

- **pkg/webhooks/workload_webhook_test.go:571** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T17:22:19Z
```diff
@@ -466,6 +510,25 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 				field.Invalid(field.NewPath("spec").Child("priorityClassName"), nil, ""),
 			},
 		},
+		"podSetUpdates should be immutable when state is ready": {
```

  I'd like to see a test where `podSetUpdates` don't change, but other fields do. In other words: a test for the case where the validation passes.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T17:24:38Z

production code LGTM
This is just missing test cases for objects that pass validation (on create and on update)

### Review by [@stuton](https://github.com/stuton) (commented) — 2023-10-24T13:59:10Z

- **pkg/webhooks/workload_webhook_test.go:268** by [@stuton](https://github.com/stuton) — 2023-10-24T13:59:10Z
```diff
@@ -232,6 +234,70 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"empty podSetUpdates": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).AdmissionChecks(kueue.AdmissionCheckState{}).Obj(),
+			wantErr:  nil,
+		},
+		"should podSetUpdates have the same number of podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.Invalid(podSetUpdatePath, nil, "must have the same number of podSetUpdates as the podSets"),
+			},
+		},
+		"mismatched names in podSetUpdates with names in podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}, {Name: "third"}}},
+			).Obj(),
+			wantErr: field.ErrorList{
+				field.NotSupported(firstAdmissionChecksPath.Child("podSetUpdates").Index(1).Child("name"), nil, nil),
+			},
+		},
+		"matched names in podSetUpdates with names in podSets": {
+			workload: testingutil.MakeWorkload(testWorkloadName, testWorkloadNamespace).PodSets(
+				*testingutil.MakePodSet("first", 1).Obj(),
+				*testingutil.MakePodSet("second", 1).Obj(),
+			).AdmissionChecks(
+				kueue.AdmissionCheckState{PodSetUpdates: []kueue.PodSetUpdate{{Name: "first"}, {Name: "second"}}},
```

  added https://github.com/kubernetes-sigs/kueue/pull/1220/commits/950ad18ea9deb450786c49b83c54e7fc31510a24

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-24T18:57:38Z

/lgtm
/approve

- **pkg/webhooks/workload_webhook_test.go:244** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-24T18:57:33Z
```diff
@@ -232,6 +234,48 @@ func TestValidateWorkload(t *testing.T) {
 				field.Invalid(firstPodSetSpecPath.Child("containers").Index(0).Child("resources", "requests").Key(string(corev1.ResourcePods)), nil, ""),
 			},
 		},
+		"should podSetUpdates have the same number of podSets": {
```

  ah, sorry, I missed it.
