# PR #1223: Update workload when job changes before admission

**Summary**: Update workload when job changes before admission

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1223

**Last updated**: 2023-11-03T09:18:08Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk)
- **Branch**: `workload-update` → `main`
- **Created**: 2023-10-18T19:11:20Z
- **Updated**: 2023-11-03T09:18:08Z
- **Merged**: 2023-10-31T16:38:46Z
- **Closed**: 2023-10-31T16:38:46Z
- **Labels**: `kind/feature`, `release-note`, `size/L`, `lgtm`, `approved`, `ok-to-test`, `cncf-cla: yes`
- **Assignees**: [@alculquicondor](https://github.com/alculquicondor), [@mimowo](https://github.com/mimowo)
- **Requested reviewers**: [@denkensk](https://github.com/denkensk), [@mimowo](https://github.com/mimowo), [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 6   **+218 / -13**

## Description

#### What type of PR is this?
/kind feature

#### Which issue(s) this PR fixes:
Fixes #1038

#### Does this PR introduce a user-facing change?
```release-note
Jobs preserve their position in the queue if the number of pods change before being admitted
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-18T19:11:27Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* canceled.


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 9df2318516fcc92239fe9db297afee71e586b1db |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/653fc8cb0ab3aa0008e041be |

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-18T19:11:30Z

Hi @yaroslava-serdiuk. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-18T19:56:38Z

/ok-to-test
/assign @mimowo

### Comment by [@mimowo](https://github.com/mimowo) — 2023-10-19T09:30:04Z

/lgtm
/assign @alculquicondor

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T17:00:53Z

/lgtm cancel

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-26T10:54:57Z

@alculquicondor 
I added a test that modifies parallelism and verify the creation stamp doesn't changed. The pod template requests are immutable fields, so I wanted to write the test you suggested in a way that job doesn't tolerate spot taint, so the job can't fit and with the toleration job can run. However I hit that adding toleration to the job does't update workload (or recreate in the old/current logic): https://github.com/kubernetes-sigs/kueue/issues/1264

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-27T11:59:37Z

LGTM label has been added.  <details>Git tree hash: ae7a2bf2a9c60f07070d2f22969c82beea38bf60</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:04:12Z

/release-note-edit

```release-note
Jobs preserve their position in the queue if their resource requests change before being admitted
```

### Comment by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-27T17:22:54Z

> Jobs preserve their position in the queue if their resource requests change before being admitted

Resources requests are immutable, the user can't change them

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:27:08Z

/release-note-edit
```release-note
Jobs preserve their position in the queue if the number of pods change before being admitted
```

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-31T16:14:00Z

LGTM label has been added.  <details>Git tree hash: a1a77f08b74d82f96dfe792cdb45de5e582e1ff1</details>

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-31T16:14:01Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1223#pullrequestreview-1706728290" title="LGTM">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1223#" title="Author self-approved">yaroslava-serdiuk</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

## Reviews

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T07:26:39Z

Generally LGTM, just a question regarding the scenario when the job is running.

- **pkg/controller/jobframework/reconciler.go:529** by [@mimowo](https://github.com/mimowo) — 2023-10-19T07:21:33Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  More of a question: are we sure the job is suspended at this point? For example, i'm thinking if this code is executed when the job is running, but a user manually modified the admitted workload.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-19T08:04:59Z

- **pkg/controller/jobframework/reconciler.go:529** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-19T08:04:58Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  This function is called if the job is suspended and the workload is not admitted. Or do you mean that that state of the job was changed during the Reconcile() execution? I don't think we can do anything about it.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T08:26:36Z

- **pkg/controller/jobframework/reconciler.go:529** by [@mimowo](https://github.com/mimowo) — 2023-10-19T08:26:36Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  > This function is called if the job is suspended and the workload is not admitted.
  
  I looked in the code a bit, but could not see any `if` that guarantees the `ensureOneWorkload` function is called only for suspended jobs.  Actually the comment `// If there's no workload exists and job is unsuspended, we'll stop it immediately.` suggests under some circumstances the job might be unsuspended. Please double check. 
  
  If there are some scenarios in which the job is unsuspeded, maybe we should just modify the message as `"Updated not matching Workload for job: %v`.

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-19T08:47:28Z

- **pkg/controller/jobframework/reconciler.go:529** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-19T08:47:28Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  Right, the job which is passed to ensureOneWorkload could be unsuspended and then we may suspend it (line 427),  however for updating we check the status of job earlier (line 421).
  
  If the job is unsuspended and workload is modified we unsuspend the job and recreate the workload. My understanding of the issue is that we want to update workloads only for suspended job.

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-19T09:02:06Z

- **pkg/controller/jobframework/reconciler.go:529** by [@mimowo](https://github.com/mimowo) — 2023-10-19T09:02:06Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  Oh, I see, you actually have this check inside the function `job.IsSuspended() && !workload.IsAdmitted(toDelete[0])` which makes sure the updating is only for suspended jobs.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-19T17:00:43Z

I think we also need to modify the workload_webhook, as it currently has PodSets as immutable.

Also please add a integration test for Job that starts with one that doesn't fit in the quota, (record the creationTimestmap of the Worklad at this point) and gets admitted after being updated to a smaller size (ensure that the creationTimestamp didn't change)

- **pkg/controller/jobframework/reconciler.go:425** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T16:54:11Z
```diff
@@ -421,6 +421,12 @@ func (r *JobReconciler) ensureOneWorkload(ctx context.Context, job GenericJob, o
 		}
 	}
 
+	var toUpdate *kueue.Workload
+	if match == nil && len(toDelete) > 0 && job.IsSuspended() && !workload.IsAdmitted(toDelete[0]) {
```

  ```suggestion
  	if match == nil && len(toDelete) > 0 && job.IsSuspended() && !workload.HasQuotaReservation(toDelete[0]) {
  ```

- **pkg/controller/jobframework/reconciler.go:529** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T16:57:49Z
```diff
@@ -509,6 +519,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
+		return nil, fmt.Errorf("updating existed workload: %w", err)
+	}
+
+	r.record.Eventf(object, corev1.EventTypeNormal, "UpdatedWorkload",
+		"Updated not matching Workload for suspended job: %v", wl)
```

  And Update will fail if Workload was updated in the meantime :+1:

### Review by [@mimowo](https://github.com/mimowo) (commented) — 2023-10-27T11:59:32Z

/lgtm

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-27T17:11:31Z

- **pkg/controller/jobs/job/job_controller_test.go:877** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:11:31Z
```diff
@@ -874,6 +874,26 @@ func TestReconciler(t *testing.T) {
 			},
 			wantErr: jobframework.ErrNoMatchingWorkloads,
 		},
+		"non-matching non-admitted workload is updated": {
```

  Let's add a case for when the Workload has a quota reservation. In that case we would remove the existing Workload, right?
  
  We might want to change what to do with a Workload that is admitted when the requests change, but that's for #77

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-27T17:29:40Z

- **pkg/controller/jobs/job/job_controller_test.go:1010** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:21:26Z
```diff
@@ -955,6 +975,39 @@ func TestReconciler(t *testing.T) {
 					Obj(),
 			},
 		},
+		"the workload is updated when queue name has changed for suspended job": {
+			job: *baseJobWrapper.
+				Clone().
+				Suspend(true).
+				Queue("test-queue-new").
+				UID("test-uid").
+				Obj(),
+			wantJob: *baseJobWrapper.
+				Clone().
+				Queue("test-queue").
+				UID("test-uid").
+				Obj(),
+			workloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("test-queue").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+					}).
+					Obj(),
+			},
+			wantWorkloads: []kueue.Workload{
+				*utiltesting.MakeWorkload("job", "ns").Finalizers(kueue.ResourceInUseFinalizerName).
+					PodSets(*utiltesting.MakePodSet(kueue.DefaultPodSetName, 10).Request(corev1.ResourceCPU, "1").Obj()).
+					Queue("test-queue-new").
+					Priority(0).
+					Labels(map[string]string{
+						controllerconsts.JobUIDLabel: "test-uid",
+					}).
+					Obj(),
+			},
+		},
```

  add a test cases for changing the priority class

- **pkg/webhooks/workload_webhook.go:348** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:22:07Z
```diff
@@ -344,9 +344,12 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	specPath := field.NewPath("spec")
 	statusPath := field.NewPath("status")
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassSource, oldObj.Spec.PriorityClassSource, specPath.Child("priorityClassSource"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassName, oldObj.Spec.PriorityClassName, specPath.Child("priorityClassName"))...)
+
+	if workload.HasQuotaReservation(oldObj) {
```

  What is the rationale for only checking `oldObj`?

- **pkg/webhooks/workload_webhook_test.go:399** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:22:52Z
```diff
@@ -397,7 +397,7 @@ func TestValidateWorkloadUpdate(t *testing.T) {
 		wantErr       field.ErrorList
 	}{
 		"podSets should not be updated: count": {
```

  ```suggestion
  		"podSets should not be updated when has quota reservation: count": {
  ```

- **test/integration/controller/jobs/job/job_controller_test.go:1578** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-27T17:29:32Z
```diff
@@ -1543,6 +1549,51 @@ var _ = ginkgo.Describe("Job controller interacting with scheduler", ginkgo.Orde
 			})
 		})
 	})
+	ginkgo.It("Should schedule updated job and update the workload", func() {
+		localQueue := testing.MakeLocalQueue("local-queue", ns.Name).ClusterQueue(prodClusterQ.Name).Obj()
+		ginkgo.By("create a localQueue", func() {
+			gomega.Expect(k8sClient.Create(ctx, localQueue)).Should(gomega.Succeed())
+		})
+
+		job := testingjob.MakeJob(jobName, ns.Name).Queue(localQueue.Name).Request(corev1.ResourceCPU, "3").Parallelism(2).Suspend(false).Obj()
+		lookupKey := types.NamespacedName{Name: job.Name, Namespace: job.Namespace}
+		createdJob := &batchv1.Job{}
+
+		ginkgo.By("creating the job that doesn't fit", func() {
+			gomega.Expect(k8sClient.Create(ctx, job)).Should(gomega.Succeed())
+		})
+
+		ginkgo.By("job should be suspend", func() {
+			gomega.Eventually(func() *bool {
+				gomega.Expect(k8sClient.Get(ctx, lookupKey, createdJob)).Should(gomega.Succeed())
+				return createdJob.Spec.Suspend
+			}, util.Timeout, util.Interval).Should(gomega.Equal(ptr.To(true)))
+		})
+
+		wlLookupKey := types.NamespacedName{Name: workloadjob.GetWorkloadNameForJob(jobName), Namespace: ns.Name}
+		createdWorkload := util.AwaitAndVerifyCreatedWorkload(ctx, k8sClient, wlLookupKey, createdJob)
+		createdTime := createdWorkload.CreationTimestamp
+
+		newParallelism := int32(1)
+		createdJob.Spec.Parallelism = &newParallelism
```

  ```suggestion
  		createdJob.Spec.Parallelism = ptr.To[int32](1)
  ```

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-28T07:50:26Z

- **pkg/webhooks/workload_webhook.go:348** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-28T07:50:26Z
```diff
@@ -344,9 +344,12 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	specPath := field.NewPath("spec")
 	statusPath := field.NewPath("status")
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassSource, oldObj.Spec.PriorityClassSource, specPath.Child("priorityClassSource"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassName, oldObj.Spec.PriorityClassName, specPath.Child("priorityClassName"))...)
+
+	if workload.HasQuotaReservation(oldObj) {
```

  I think we want to update the workload that doesn't have a quota reservation and this is oldObj. 
  I think if the update function is called for new object that has quota reservation and the old doesn't (not sure what the real case for that), it's should be the same as we call update first for new obj without quota reservation and then reserve the quota. 
  My understanding why we check the quota is that we can't guarantee the quota for new object, so we want to do updates when the quota is not reserved. If the quota is fine for new object, than we are fine. 
  Do I miss something?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-10-30T11:03:08Z

- **pkg/controller/jobs/job/job_controller_test.go:877** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-10-30T11:03:08Z
```diff
@@ -874,6 +874,26 @@ func TestReconciler(t *testing.T) {
 			},
 			wantErr: jobframework.ErrNoMatchingWorkloads,
 		},
+		"non-matching non-admitted workload is updated": {
```

  we have the case for that just above current test ("non-matching admitted workload is deleted")

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-31T16:12:25Z

- **pkg/webhooks/workload_webhook.go:348** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-31T16:12:25Z
```diff
@@ -344,9 +344,12 @@ func ValidateWorkloadUpdate(newObj, oldObj *kueue.Workload) field.ErrorList {
 	specPath := field.NewPath("spec")
 	statusPath := field.NewPath("status")
 	allErrs = append(allErrs, ValidateWorkload(newObj)...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PodSets, oldObj.Spec.PodSets, specPath.Child("podSets"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassSource, oldObj.Spec.PriorityClassSource, specPath.Child("priorityClassSource"))...)
-	allErrs = append(allErrs, apivalidation.ValidateImmutableField(newObj.Spec.PriorityClassName, oldObj.Spec.PriorityClassName, specPath.Child("priorityClassName"))...)
+
+	if workload.HasQuotaReservation(oldObj) {
```

  Yeah, I think that's fine.
  I just remembered that it is not possible to update Spec and Status at the same time.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-31T16:13:55Z

/approve
/lgtm

### Review by [@kerthcet](https://github.com/kerthcet) (commented) — 2023-11-03T07:03:45Z

- **pkg/controller/jobframework/reconciler.go:524** by [@kerthcet](https://github.com/kerthcet) — 2023-11-03T07:03:38Z
```diff
@@ -505,6 +515,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
```

  So the key point here is we'll replace the unadmitted workload with the newest spec. right?

### Review by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) (commented) — 2023-11-03T09:18:08Z

- **pkg/controller/jobframework/reconciler.go:524** by [@yaroslava-serdiuk](https://github.com/yaroslava-serdiuk) — 2023-11-03T09:18:08Z
```diff
@@ -505,6 +515,21 @@ func (r *JobReconciler) equivalentToWorkload(job GenericJob, object client.Objec
 	return true
 }
 
+func (r *JobReconciler) updateWorkloadToMatchJob(ctx context.Context, job GenericJob, object client.Object, wl *kueue.Workload) (*kueue.Workload, error) {
+	newWl, err := r.constructWorkload(ctx, job, object)
+	if err != nil {
+		return nil, fmt.Errorf("can't construct workload for update: %w", err)
+	}
+	wl.Spec = newWl.Spec
+	if err = r.client.Update(ctx, wl); err != nil {
```

  Correct. However note, that currently we can update only the amount of pods/containers: https://github.com/kubernetes-sigs/kueue/issues/1264
