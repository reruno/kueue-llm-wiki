# PR #1226: update document for FlavorFungibility

**Summary**: update document for FlavorFungibility

**Sources**: https://github.com/kubernetes-sigs/kueue/pull/1226

**Last updated**: 2023-10-25T10:39:42Z

---

## Metadata

- **State**: closed (merged)
- **Author**: [@KunWuLuan](https://github.com/KunWuLuan)
- **Branch**: `documents` → `main`
- **Created**: 2023-10-19T11:38:52Z
- **Updated**: 2023-10-25T10:39:42Z
- **Merged**: 2023-10-25T10:39:40Z
- **Closed**: 2023-10-25T10:39:40Z
- **Labels**: `lgtm`, `release-note-none`, `approved`, `size/M`, `ok-to-test`, `kind/documentation`, `cncf-cla: yes`
- **Assignees**: [@trasc](https://github.com/trasc)
- **Requested reviewers**: [@ahg-g](https://github.com/ahg-g)
- **Changed files**: 1   **+38 / -1**

## Description

<!--  Thanks for sending a pull request!  Here are some tips for you:

1. If this is your first time, please read our contributor guidelines: https://git.k8s.io/community/contributors/guide/first-contribution.md#your-first-contribution and developer guide https://git.k8s.io/community/contributors/devel/development.md#development-guide
2. Please label this pull request according to what type of issue you are addressing, especially if this is a release targeted pull request. For reference on required PR/issue labels, read here:
https://git.k8s.io/community/contributors/devel/sig-release/release.md#issuepr-kind-label
3. Ensure you have added or ran the appropriate tests for your PR: https://git.k8s.io/community/contributors/devel/sig-testing/testing.md
4. If you want *faster* PR reviews, read how: https://git.k8s.io/community/contributors/guide/pull-requests.md#best-practices-for-faster-reviews
5. If the PR is unfinished, see how to mark it: https://git.k8s.io/community/contributors/guide/pull-requests.md#marking-unfinished-pull-requests
-->

#### What type of PR is this?
/kind documentation
<!--
Add one of the following kinds:
/kind bug
/kind cleanup
/kind feature

Optionally add one or more of the following kinds if applicable:
/kind api-change
/kind deprecation
/kind failing-test
/kind flake
/kind regression
-->

#### What this PR does / why we need it:
update document for FlavorFungibility

#### Which issue(s) this PR fixes:
<!--
*Automatically closes linked issue when PR is merged.
Usage: `Fixes #<issue number>`, or `Fixes (paste link of issue)`.
_If PR is about `failing-tests or flakes`, please post the related issues/tests in a comment and do not use `Fixes`_*
-->

#### Special notes for your reviewer:
For issue #582

#### Does this PR introduce a user-facing change?
<!--
If no, just write "NONE" in the release-note block below.
If yes, a release note is required:
Enter your extended release note in the block below. If the PR requires additional action from users switching to the new release, include the string "action required".
-->
```release-note
```

## Discussion

### Comment by [@netlify[bot]](https://github.com/apps/netlify) — 2023-10-19T11:38:58Z

### <span aria-hidden="true">✅</span> Deploy Preview for *kubernetes-sigs-kueue* ready!


|  Name | Link |
|:-:|------------------------|
|<span aria-hidden="true">🔨</span> Latest commit | 953af6873a24af81fc76e5a626e365a61d679753 |
|<span aria-hidden="true">🔍</span> Latest deploy log | https://app.netlify.com/sites/kubernetes-sigs-kueue/deploys/653738803f3ab40008be87f0 |
|<span aria-hidden="true">😎</span> Deploy Preview | [https://deploy-preview-1226--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue](https://deploy-preview-1226--kubernetes-sigs-kueue.netlify.app/docs/concepts/cluster_queue) |
|<span aria-hidden="true">📱</span> Preview on mobile | <details><summary> Toggle QR Code... </summary><br /><br />![QR Code](https://app.netlify.com/qr-code/eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJ1cmwiOiJodHRwczovL2RlcGxveS1wcmV2aWV3LTEyMjYtLWt1YmVybmV0ZXMtc2lncy1rdWV1ZS5uZXRsaWZ5LmFwcCJ9.Jis_L35pYFTy48BqD-HfJi304VZwdXRm1kGSZs7iulk)<br /><br />_Use your smartphone camera to open QR code link._</details> |
---

_To edit notification comments on pull requests, go to your [Netlify site configuration](https://app.netlify.com/sites/kubernetes-sigs-kueue/configuration/deploys#deploy-notifications)._

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-19T11:39:01Z

Hi @KunWuLuan. Thanks for your PR.

I'm waiting for a [kubernetes-sigs](https://github.com/orgs/kubernetes-sigs/people) member to verify that this patch is reasonable to test. If it is, they should reply with `/ok-to-test` on its own line. Until that is done, I will not automatically test new commits in this PR, but the usual testing commands by org members will still work. Regular contributors should [join the org](https://git.k8s.io/community/community-membership.md#member) to skip this step.

Once the patch is verified, the new status will be reflected by the `ok-to-test` label.

I understand the commands that are listed [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

<details>

Instructions for interacting with me using PR comments are available [here](https://git.k8s.io/community/contributors/guide/pull-requests.md).  If you have questions or suggestions related to my behavior, please file an issue against the [kubernetes/test-infra](https://github.com/kubernetes/test-infra/issues/new?title=Prow%20issue:) repository.
</details>

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:27:38Z

/ok-to-test

### Comment by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-19T15:27:50Z

/release-note-none

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-24T17:26:51Z

[APPROVALNOTIFIER] This PR is **APPROVED**

This pull-request has been approved by: *<a href="https://github.com/kubernetes-sigs/kueue/pull/1226#pullrequestreview-1695488024" title="Approved">alculquicondor</a>*, *<a href="https://github.com/kubernetes-sigs/kueue/pull/1226#" title="Author self-approved">KunWuLuan</a>*

The full list of commands accepted by this bot can be found [here](https://go.k8s.io/bot-commands?repo=kubernetes-sigs%2Fkueue).

The pull request process is described [here](https://git.k8s.io/community/contributors/guide/owners.md#the-code-review-process)

<details >
Needs approval from an approver in each of these files:

- ~~[site/OWNERS](https://github.com/kubernetes-sigs/kueue/blob/main/site/OWNERS)~~ [alculquicondor]

Approvers can indicate their approval by writing `/approve` in a comment
Approvers can cancel approval by writing `/approve cancel` in a comment
</details>
<!-- META={"approvers":[]} -->

### Comment by [@trasc](https://github.com/trasc) — 2023-10-25T10:33:21Z

/lgtm

### Comment by [@k8s-ci-robot](https://github.com/k8s-ci-robot) — 2023-10-25T10:33:27Z

LGTM label has been added.  <details>Git tree hash: 2f37e805bf917806ebd1dd5d61f1c7e03fe51651</details>

## Reviews

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T18:40:53Z

/assign @trasc

- **site/content/en/docs/concepts/cluster_queue.md:361** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:29:48Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
```

  ```suggestion
  
  ```yaml
  ```

- **site/content/en/docs/concepts/cluster_queue.md:374** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:36:48Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
```

  ```suggestion
    - `Borrow` (default): ClusterQueue stops finding a better assignment.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:39:09Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  I think we should clarify what is `the next flavor`.

- **site/content/en/docs/concepts/cluster_queue.md:377** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T18:40:33Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops to try preempting in current ResourceFlavor and start from the next one if preempting failed.
```

  ```suggestion
    - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-20T19:54:51Z

- **site/content/en/docs/concepts/cluster_queue.md:354** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:46:52Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
```

  Add a note in the `Flavors and borrowing semantics` that some semantics can be modified based on the `flavorFungibility` field, and add a link to this section.

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:47:38Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  Yeah, we can remind readers that kueue evaluates the flavors in a ClusterQueue in order.

- **site/content/en/docs/concepts/cluster_queue.md:356** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:50:37Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
```

  ```suggestion
  And preemption will be triggered only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:358** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:51:27Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
```

  ```suggestion
  You can configure ClusterQueues to try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:52:10Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  But generally, we introduce the possible options first, followed by what's the default.

- **site/content/en/docs/concepts/cluster_queue.md:380** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-20T19:54:46Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops to try preempting in current ResourceFlavor and start from the next one if preempting failed.
+  - `TryNextFlavor` (default): ClusterQueue will try the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
+
+Noted that if `whenCanBorrow` is `Borrow` and `whenCanPreempt` is `Preempt`, or both `whenCanBorrow` and `whenCanPreempt` is `TryNextFlavor`, borrowing has a higher priority than preemption.
```

  This is highly confusing. Maybe simplify by saying that Kueue tries to avoid preemptions by borrowing, whenever possible and the configured policy allows it?

### Review by [@tenzen-y](https://github.com/tenzen-y) (commented) — 2023-10-20T20:17:48Z

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@tenzen-y](https://github.com/tenzen-y) — 2023-10-20T20:17:48Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  agree.

### Review by [@cortespao](https://github.com/cortespao) (commented) — 2023-10-20T21:47:36Z

Sending some editorial suggestions. Thanks!

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:37:14Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  ```suggestion
  By default, the incoming workload  stops trying the next flavor if the workload can get enough borrowed resources. 
  ```
  
  I'm making a lot of tech assumptions. Feel free to disregard if the meaning was affected

- **site/content/en/docs/concepts/cluster_queue.md:356** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:40:12Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
```

  ```suggestion
  And Kueue triggers preemption after considering all ResourceFlavors.
  ```
  Changing to active voice. I assume the subject of the sentence is Kueue, but feel free to change it,

- **site/content/en/docs/concepts/cluster_queue.md:372** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:41:16Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
```

  ```suggestion
  The fields above do the following:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:375** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:41:45Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
```

  ```suggestion
    - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:378** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:42:18Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops to try preempting in current ResourceFlavor and start from the next one if preempting failed.
+  - `TryNextFlavor` (default): ClusterQueue will try the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
```

  ```suggestion
    - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:380** by [@cortespao](https://github.com/cortespao) — 2023-10-20T21:47:00Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops to try preempting in current ResourceFlavor and start from the next one if preempting failed.
+  - `TryNextFlavor` (default): ClusterQueue will try the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
+
+Noted that if `whenCanBorrow` is `Borrow` and `whenCanPreempt` is `Preempt`, or both `whenCanBorrow` and `whenCanPreempt` is `TryNextFlavor`, borrowing has a higher priority than preemption.
```

  ```suggestion
  To instruct Kueue to attempt to borrow first instead of preempt, you can use any of the following value combinations:
  *  `whenCanBorrow = "Borrow"` and `whenCanPreempt = "Preempt"`
  *  `whenCanBorrow = "TryNextFlavor"` and `whenCanPreempt = "TryNextFlavor"`
  ```

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-23T01:29:35Z

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-23T01:29:34Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
```

  Sure, I will update this

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-23T02:09:12Z

- **site/content/en/docs/concepts/cluster_queue.md:372** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-23T02:09:12Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
```

  https://github.com/kubernetes-sigs/kueue/blob/40bd411beddb5bd415d880a0c841c92ababc0458/site/content/en/docs/concepts/cluster_queue.md?plain=1#L324C47-L324C47
  
  I have also made modifications to the identical expression within the text.

### Review by [@KunWuLuan](https://github.com/KunWuLuan) (commented) — 2023-10-23T02:12:01Z

- **site/content/en/docs/concepts/cluster_queue.md:380** by [@KunWuLuan](https://github.com/KunWuLuan) — 2023-10-23T02:12:01Z
```diff
@@ -348,6 +348,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort. 
+
+By default the incoming workload will stop trying the next flavor if it can get enough resource by borrowing. 
+And preemption will be triggered after all ResourceFlavors were considered.
+
+ClusterQueues can also try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above have the following semantics:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops to find a better assignment.
+  - `TryNextFlavor`: ClusterQueue will try the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops to try preempting in current ResourceFlavor and start from the next one if preempting failed.
+  - `TryNextFlavor` (default): ClusterQueue will try the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
+
+Noted that if `whenCanBorrow` is `Borrow` and `whenCanPreempt` is `Preempt`, or both `whenCanBorrow` and `whenCanPreempt` is `TryNextFlavor`, borrowing has a higher priority than preemption.
```

  I think that such expression is sufficient to convey the meaning: 
  
  ```
  Note that whenever possible and the configured policy allows, Kueue tries to avoid preemptions by borrowing.
  ```

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-23T18:49:55Z

Final pass

- **site/content/en/docs/concepts/cluster_queue.md:198** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:41:01Z
```diff
@@ -195,6 +195,8 @@ semantics:
 - For each pod set resource in a Workload, a ClusterQueue can only borrow quota
   for one flavor.
 
+Note that when [`flavorFungibility`](/docs/concepts/cluster_queue#FlavorFungibility) is set in ClusterQueue, some semantics can be modified.
```

  ```suggestion
  You can influence some semantics of flavor selection and borrowing
  by setting a [`flavorFungibility`](/docs/concepts/cluster_queue#FlavorFungibility) in ClusterQueue.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:355** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:44:46Z
```diff
@@ -348,6 +350,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort.
```

  ```suggestion
  When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
  quota or preempt running Workloads in the ClusterQueue or Cohort.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:357** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:48:11Z
```diff
@@ -348,6 +350,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort.
+
+Kueue evaluates the flavors in a ClusterQueue in order. You can configure ClusterQueues to try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
```

  ```suggestion
  Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
  preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
  setting the `flavorFungibility` field.
  ```

- **site/content/en/docs/concepts/cluster_queue.md:359** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:48:42Z
```diff
@@ -348,6 +350,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort.
+
+Kueue evaluates the flavors in a ClusterQueue in order. You can configure ClusterQueues to try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
```

  ```suggestion
  A configuration for a ClusterQueue that configures this behavior looks like the following:
  ```

- **site/content/en/docs/concepts/cluster_queue.md:382** by [@alculquicondor](https://github.com/alculquicondor) — 2023-10-23T18:49:48Z
```diff
@@ -348,6 +350,37 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming workload can borrow or preempt in ClusterQueue or Cohort.
+
+Kueue evaluates the flavors in a ClusterQueue in order. You can configure ClusterQueues to try preemption whenever preemption might help before trying next ResourceFlavor or only borrowing after all ResourceFlavors have been considered by setting `FlavorFungibility`.
+
+A configuration for a ClusterQueue that changes this behavior looks like the following:
+```yaml
+apiVersion: kueue.x-k8s.io/v1beta1
+kind: ClusterQueue
+metadata:
+  name: "team-a-cq"
+spec:
+  flavorFungibility:
+    whenCanBorrow: TryNextFlavor
+    whenCanPreempt: Preempt
+```
+
+The fields above do the following:
+- `whenCanBorrow` determines whether a workload should stop finding a better assignment if it can get enough resource by borrowing in current ResourceFlavor. The possible values are:
+  - `Borrow` (default): ClusterQueue stops finding a better assignment.
+  - `TryNextFlavor`: ClusterQueue tries the next ResourceFlavor to see if the workload can get a better assignment.
+- `whenCanPreempt` determines whether a workload should try preemtion in current ResourceFlavor before try the next one. The possible values are:
+  - `Preempt`: ClusterQueue stops trying preemption in current ResourceFlavor and starts from the next one if preempting failed.
+  - `TryNextFlavor` (default): ClusterQueue tries the next ResourceFlavor to see if the workload can fit in the ResourceFlavor.
+
+By default, the incoming workload stops trying the next flavor if the workload can get enough borrowed resources. 
+And Kueue triggers preemption only after Kueue determines that the remaining ResourceFlavors can't fit the workload.
+
+Note that whenever possible and the configured policy allows, Kueue tries to avoid preemptions by borrowing. 
```

  ```suggestion
  Note that, whenever possible and when the configured policy allows it, Kueue avoids preemptions if it can fit a Workload by borrowing. 
  ```

### Review by [@trasc](https://github.com/trasc) (commented) — 2023-10-24T06:22:36Z

One nit. Otherwise LGTM

- **site/content/en/docs/concepts/cluster_queue.md:363** by [@trasc](https://github.com/trasc) — 2023-10-24T06:22:19Z
```diff
@@ -348,6 +351,40 @@ Workloads as possible, preferring Workloads with these characteristics:
 - Workloads with the lowest priority.
 - Workloads that have been admitted more recently.
 
+## FlavorFungibility
+
+When there is not enough nominal quota of resources in a ResourceFlavor, the incoming Workload can borrow
+quota or preempt running Workloads in the ClusterQueue or Cohort.
+
+Kueue evaluates the flavors in a ClusterQueue in order. You can influence whether to prioritize
+preemptions or borrowing in a flavor before trying to accommodate the Workload in the next flavor, by
+setting the `flavorFungibility` field.
+
+A configuration for a ClusterQueue that configures this behavior looks like the following:
```

  nit:
  
  ```suggestion
  The definition of a ClusterQueue that configures this behavior looks like the following:
  ```
  "configuration  .. that configures" sounds a bit strange to me.

### Review by [@alculquicondor](https://github.com/alculquicondor) (commented) — 2023-10-24T17:26:44Z

/approve
Leaving `/lgtm` to @trasc
